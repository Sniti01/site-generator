// Данные сайта для гейта контраста из `@factory/core/gates`.
// Пара — кортеж [fg, bg, min, label]: два токена из src/styles/global.css,
// порог (4.5 для текста, 3 для элементов интерфейса)
// и подпись для отчёта.
//
// Список — собственность сайта: пороги общие для всех сайтов, сочетания —
// нет. Пара обязана покрывать каждое сочетание, которое реально встречается
// на странице: пары, которой нет, гейт не считает.
//
// Тема «зимняя ночь» (сессия 7, П72): список пересмотрен под новые токены.
// Семнадцать пар каркаса (П62) — шасси первого сайта без эпох и пергамина —
// остаются: их сочетания печатают шасси и блоки ядра, которые придут
// с маршрутом и пачкой 0; где сочетания сегодня нет, подпись говорит
// «на вырост». Сверено с отрисованной главной (1440, 390, открытое меню, обход
// Tab): текст — ink и ink-muted на bg и bg-band; ink-on-accent на accent —
// ссылка «к содержанию» при фокусе и выделение; обводка фокуса accent-text —
// на bg (шапка) и на bg-band (подвал, ящик меню). Всё в списке (сессия 7).
// Сессия 9 (главная с рядами, лентой и плашками) — сверено заново по
// вычисленным стилям собранной главной на 1440 и 390: к прежним добавились
// bg на ink (плашки, заголовки карточек), accent-text и ink-muted на surface
// (приписки карточек ленты), accent на bg и на bg-band (узел и линия трассы);
// подписи пар ниже называют, где сочетание стоит на главной.
//
// Полоса прокрутки (сессия 8, П75 п. 1): ядро задаёт всем один механизм —
// `scrollbar-color: ink-muted bg-band` (core/styles/base.css); ползунок —
// элемент интерфейса, порог 3, пара — в списке. С base.css пара связана
// только подписью: гейт ядра читает global.css и этот файл, но не base.css,
// а по тем же токенам стоит текстовая пара ink-muted/bg-band с порогом 4,5 —
// отдельно пара ползунка упасть не может. Если ядро сменит токен ползунка,
// пару надо сменить вместе с ним (раунд 1 «судью судят», сессия 8).
//
// Чего гейт не считает — и это названо, а не принято на глаз (доклад
// сессии 7, раунд 1 «судью судят»; дополнено сессией 8):
// ползунок под курсором и при нажатии — цвет даёт браузер, не токен
// (Chrome и Edge 153, headless: около 4,4:1 при пороге 3);
// зерно ядра Grain поверх всей страницы (soft-light, 0.085) — даже в крайнем
// случае формулы W3C soft-light (шум 0 под текстом и 1 под фоном при полной
// альфе — на деле недостижимо) контраст самой тесной пары, danger на bg,
// не ниже 5,35:1 (без зерна 5,69), все пары выше порога;
// составные сочетания — скрим героя и полосы призыва поверх арта, подсветки
// под курсором (wash, wash-strong), color-mix, дымка полосы-цитаты
// и полупрозрачная шапка поверх содержимого. С сессии 9 (П79) на главной
// есть арт: скрим героя и призыва — оверрайд витрины от токена --bg
// (литерал ядра rgb(13 12 10 / α) — бэклог 56 п. 2), дымка цитаты — сталью.
// Текст поверх арта принимается замером пикселей по реальному кадру (текст
// скрыт, зерно снято, худший пиксель фона в рамке каждой строки; полоса
// из 31 окна от 360 до 1920 px, страница грузится заново на каждое окно;
// инструмент — docs/reports/2026-09-23-7thserpent-glavnaya/instrumenty/
// kontrast-art.mjs, съёмка — kontrast-art-snyatie.js). На сборке 4abc2f9:
// лид героя не ниже 5,72:1 (1440), лид призыва 5,73:1 (414), подписи
// отметок 7,76:1 (1920), приписка цитаты 7,66:1 (900) — всё выше 4,5.
// Прежний замер по четырём ширинам пропустил провал лида героя до 1,3:1
// на 641–1000 и 1025–1179 (раунд 1 «судью судят», сессия 9, находка K1).
// Составное сочетание, которое меняет арт, скрим, дымку или раскладку
// первого экрана, мерится заново той же полосой окон.

const pairs = [
  // текст на фонах шасси
  ['ink', 'bg', 4.5, 'основной текст на фоне страницы'],
  ['ink', 'bg-band', 4.5, 'основной текст на полосе, в подвале и ящике меню'],
  ['ink', 'surface', 4.5, 'основной текст на карточке (на вырост)'],
  ['ink', 'surface-2', 4.5, 'основной текст на поднятой поверхности, тихая кнопка (на вырост)'],
  ['ink-muted', 'bg', 4.5, 'второстепенный текст на фоне страницы'],
  ['ink-muted', 'bg-band', 4.5, 'второстепенный текст на полосе и в подвале'],
  ['ink-muted', 'surface', 4.5, 'второстепенный текст на карточке: приписка места и разделитель в ленте главной'],
  ['ink-muted', 'surface-2', 4.5, 'второстепенный текст на поднятой поверхности (на вырост)'],
  // марка
  ['accent-text', 'bg', 4.5, 'ссылка, текстовый акцент и обводка фокуса на фоне страницы'],
  ['accent-text', 'bg-band', 4.5, 'ссылка, текстовый акцент и обводка фокуса на полосе'],
  ['accent-text', 'surface', 4.5, 'текстовый акцент на карточке: предмет страницы в ленте главной'],
  ['accent-text', 'surface-2', 4.5, 'ссылка/текстовый акцент на поднятой поверхности (на вырост)'],
  ['ink-on-accent', 'accent', 4.5, 'надпись на заливке: кнопка, ссылка «к содержанию», выделение'],
  ['accent', 'bg', 3, 'заливка акцента как элемент интерфейса; узел и линия трассы главной на фоне'],
  // Узел и линия трассы проходят и по рядам на полосе (2003, ремейк) —
  // «судью судят», сессия 9, находка K2.
  ['accent', 'bg-band', 3, 'узел и линия трассы главной на полосе'],
  // полоса прокрутки шасси: `scrollbar-color` в core/styles/base.css
  ['ink-muted', 'bg-band', 3, 'ползунок полосы прокрутки на дорожке — элемент интерфейса'],
  // Сталь печатает счётчик группы `.stack__count` в LinkColumns — кегль роли
  // title 21px, но гарнитура заголовков Bodoni Moda 600. У первого сайта порог
  // был 3 («крупный текст 21px/600»); по WCAG крупный — от 18pt или от 14pt
  // полужирным (700), а 21px = 15,75pt при 600 — обычный текст. Порог 4,5
  // (раунд 2 «судью судят», сессия 7); сталь его держит с запасом.
  ['accent-2', 'bg', 4.5, 'сталь: счётчик группы (текст 21px/600) и орнамент (на вырост)'],
  ['accent-2', 'surface', 4.5, 'сталь на карточке (на вырост)'],
  // Правило «новый цвет — со своей парой»: опасности на страницах пока нет.
  ['danger', 'bg', 4.5, 'сообщение об опасности (на вырост)'],
  // Главная, сессия 9 (П79, «Панели и подписи»): плашка рассказчика — снег
  // `ink` под текстом цвета ночи `bg` (подписи под кадрами рядов, реплика
  // в полосе цитаты, заголовки карточек ленты). Новых цветов нет — новое
  // сочетание двух токенов.
  ['bg', 'ink', 4.5, 'текст цвета ночи на снежной плашке: подписи рядов, цитата, заголовки карточек ленты'],
];

export default pairs;
