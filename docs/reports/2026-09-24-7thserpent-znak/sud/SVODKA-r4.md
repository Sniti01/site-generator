######## R4-SVERKA-1 [дефект → подтверждено/дефект]
Custom property со значением-блоком обходит строгий разбор. `--accent: {}`, `--accent: red {}`, `--accent: { color: red }` и `--accent:hover {}` токенайзер читает как вложенное правило, а не как объявление, и отвечает «сверено»; на странице трассер чёрный

ДОКАЗАТЕЛЬСТВО: Стенд D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/css-r4.mjs, итог в css-r4.json и css-r4.log. Цепочка: настоящая sverka() → Tailwind 4.3.3 (compile → Scanner → build → optimize, как @tailwindcss/vite) → Chromium 151.0.7922.34 без окна, свой сервер 127.0.0.1:4955.
- K0 (чистые входы): «сверено»; --accent = "#eca84a"; заливка трассера rgb(236, 168, 74).
- A1 `:root { --accent: {}; }` в конце листа: «сверено», сборка ok, в выходе 2 объявления `--accent:`. На странице --accent = "{}", заливка трассера rgb(0, 0, 0).
- A2 `.hdr { --accent: {}; }`: «сверено»; в .hdr --accent = "{}", заливка rgb(0, 0, 0).
- A3 `--accent: #ff0000 {}` → "red {}"; A4 `--accent: { color: red }` → "{ color: red }"; A6 `--accent:hover {}` → "hover {}". Везде «сверено», заливка rgb(0, 0, 0).
- Контроль A5 `--accent: {#ff0000}`: отказ, но по чужой причине — «объявление не читается: «#ff0000»».
Причина. На «{» (znak.mjs:145-152) буфер `--accent:` становится prelude вложенного правила, и объявления в deklaracje не появляется. В CSS для custom property блок {} — законная часть значения: браузер объявляет --accent. Против обещаний: шапка :18-21 («Всё, чего разбор не понимает, — громкий отказ, а не пропуск»), :22-26 («больше нигде в листе … это имя не объявлено»), DESIGN.md:684 («лист, которого разбор не понимает, — отказ»), CLAUDE.md сайта:167 («без переопределений»). Вес малый: форма нарочитая.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:145-152 (prelude на «{»), 120-128 (dodajDecl); обещания — шапка :18-26, DESIGN.md:679-684, CLAUDE.md сайта:167

ПРЕДЛОЖЕНИЕ: На «{» проверять буфер. Если он после trim начинается с имени custom property и двоеточия (`/^--[^:{}]*:/`), это объявление со значением-блоком. Тогда либо громкий отказ «лист: у --имя значение-блок {…} — браузер читает это как объявление», либо дочитать значение до «;» или «}» своего уровня и отдать его в decls: kraska сама скажет «не как #rrggbb» или «переопределён». Добавить в selftest пробы A1, A2 и A6. Это правка судьи, после неё снова «судью судят».

СКЕПТИК: Опровергнуть не удалось. Воспроизвёл своим стендом, линзы не брал: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/skeptik-sverka/css.mjs, итог в css.json и css.log, выход сборки в css-out/<id>.css. Цепочка ближе к сборке, чем у линзы: настоящая sverka(), затем Tailwind 4.3.3 (compile, Scanner, build, optimize minify), затем второй проход, как у минификатора Vite 8 (lightningcss.transform, цели chrome111/firefox114/safari16.4). Страница — настоящая dist/index.html со знаками шапки и подвала, правила Znak взяты из листа сборки дословно. Chromium 151 без окна, порт 4971.

Что показали пробы:
- A1, A3, A4, A6: «сверено». В выходе второе `--accent:{}` (`red {}`, `{ color: red }`, `hover {}`). На :root --accent равен этому значению, трассер и ромб знака шапки и подвала — rgb(0,0,0).
- A2 `.hdr { --accent: {} }`: «сверено», чёрный только знак шапки.
- Сверх линзы, A7 (`.hdr { --accent: {} }` без «;»): то же.
- Сверх линзы, A8: поздний `@theme { --font-display: {}; }` даёт «сверено». Tailwind выпускает `--font-display:{}`, заголовок роли по CDP рисуется «Public Sans Thin SemiBold». Обход бьёт и по проверке 2, не только по краскам.
- Контроль A9 `.hdr { --accent: #ff0000 }`: отказ (11).

Код линза прочла верно: на «{» (:145-152) буфер становится prelude вложенного правила, в decls объявления нет. Токенайзер расходится с CSS в самом разборе, так что по мерке R3-SVERKA-1 «как знак рисует браузер» в ПРЕДЕЛАХ этого не покрывает. Шапка, DESIGN.md:679-684 и BACKLOG §60 такую форму не называют. Вес малый, форма нарочитая, но по R2-SVERKA-7 вес класс не меняет.

######## R4-SVERKA-2 [дефект → подтверждено/дефект]
`@property` у токена знака сверка не видит. Регистрация `--accent` с `inherits: false` красит трассер в красный, при синтаксисе `<length>` или при `--ink` с `inherits: false` части становятся чёрными, а ответ — «сверено»

ДОКАЗАТЕЛЬСТВО: Стенд D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/css-r4b.mjs, итог в css-r4b.json. Цепочка та же: sverka() → Tailwind 4.3.3 → Chromium 151. В выходе Tailwind у каждой пробы 1 правило @property.
- P1 `@property --accent { syntax: '<color>'; inherits: false; initial-value: #ff0000; }`: «сверено», сборка ok. На :root --accent = rgb(236, 168, 74), а заливка трассера — rgb(255, 0, 0). Иконки при этом остаются #eca84a.
- P2 `@property --accent { syntax: '<length>'; inherits: true; initial-value: 0px; }`: «сверено»; --accent = "0px", заливка rgb(0, 0, 0).
- P4 `@property --ink { syntax: '*'; inherits: false; }`: «сверено»; заливка частей ink — rgb(0, 0, 0).
- Контроль P3 (`inherits: true`, initial-value #eca84a): «сверено», страница цела.
Причина. У at-правила `@property --accent` объявления syntax, inherits и initial-value не начинаются с «--». deklaracje (:171-183) и kraska (:184-195) его не замечают. Против обещаний: шапка :22-26 («больше нигде в листе … это имя не объявлено»), CLAUDE.md сайта:167 («без переопределений»), текст отказа :189 («знак на странице и иконки могут разойтись») — здесь они расходятся молча. @property — законный современный CSS: им регистрируют токены, чтобы анимировать цвет.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:171-195 (deklaracje, kraska); обещания — шапка :22-26, CLAUDE.md сайта:167

ПРЕДЛОЖЕНИЕ: Если в листе есть `@property --имя` (имя at-правила без учёта регистра, на любой глубине) для токена, которым красится знак или иконки, либо для --font-display, давать отказ: «токен --имя зарегистрирован @property — наследование, синтаксис и начальное значение меняют его на странице». Можно разрешить только `inherits: true` с тем же initial-value, но проще отказывать всегда. Добавить в selftest пробы P1 и P4.

СКЕПТИК: Опровергнуть не удалось, цепочка та же (css.mjs, css.json, css-out/P*.css).

Что показали пробы:
- P1: «сверено». На :root --accent = rgb(236,168,74), а трассер и ромб шапки и подвала — rgb(255,0,0).
- P2: «сверено». На :root --accent = "0px", части знака чёрные.
- P4: «сверено». Перекладина и надписи (ink) чёрные, акцент цел.
- Контроли P3 (inherits: true, #eca84a) и сверх линзы P5 (inherits: false с тем же #eca84a): страница цела.

Сверх линзы, в выходе Tailwind у регистрации есть запасной блок для браузеров без @property: `@layer properties{@supports(…){*,:before,:after,::backdrop{--accent:red}}}` в P1, `*{--ink:initial}` в P4. Значит, в выпущенном CSS имя --accent объявлено вне верхнего :root буквально.

Почему не предел «как знак рисует браузер (каскад)» шапки :48-51. В P2 недействительным становится само объявление верхнего :root: оно ровно #rrggbb, но компонент его через var() не получает. Это прямо против :22-24. В P1 и P4 то же даёт запасной блок Tailwind. Сегодня @property в листах сайта и ядра нет (grep). В шапке, DESIGN.md и BACKLOG §60 это не названо. Отказ на `@property --имя` для токенов знака, иконок и --font-display дёшев: drzewoCss уже даёт такой узел.

######## R4-SVERKA-3 [дефект → подтверждено/дефект]
Правку R3-SVERKA-5 можно обойти. Своя `@font-face` Бодони с другим написанием имени гарнитуры или свойства, как и вложенная в `@media`, даёт «сверено», а заголовки рисуются Georgia; мутант без обхода в глубину проходит самопроверку 68/68

ДОКАЗАТЕЛЬСТВО: Стенды css-r4.mjs и css-r4b.mjs (итоги в css-r4.json и css-r4b.json), различители — razlichiteli.mjs (razlichiteli.json), мутанты — mutanty.mjs (mutanty.json). Всё в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/. Во всех пробах src: local('Georgia'), font-weight 600; гарнитура заголовка роли взята по CDP CSS.getPlatformFontsForNode.
- F2 `font-family: Bodoni  Moda` (без кавычек, два пробела): «сверено», заголовок — Georgia.
- F3 `font-family: Bodoni\ Moda`: «сверено», Georgia.
- F4 `font-family: 'Bodoni\20 Moda'`: «сверено», Georgia.
- F6 `font-family: Bodoni` + перевод строки + `Moda`: «сверено», Georgia.
- F7 `FONT-FAMILY: 'Bodoni Moda'`: «сверено», Georgia.
- Контроли F5 (`'Bodoni Moda'`) и F8 (`'bodoni moda'`): отказ «своя @font-face».
- Мутант MISK: снята рекурсия `iskat(u.children ?? [])` (:292). --selftest копии — 68/68, exit 0; --check — exit 0. Различитель `@media all { @font-face { font-family: 'Bodoni Moda'; … local('Georgia') } }`: настоящий судья даёт отказ «своя @font-face», мутант — «сверено», на странице Georgia. Проба :441 ставит @font-face только на верхний уровень.
Причина. :290 сравнивает имя свойства точно (`d.imie === 'font-family'`), а значение — сырой регуляркой `/bodoni moda/i`. CSS же разбирает имя свойства без учёта регистра, раскрывает экранирование, а пробельные символы между словами имени без кавычек сводит к одному пробелу. Обещание шапки :29-30 «своей `@font-face` для 'Bodoni Moda' в листе нет» здесь не держится. Вес малый: формы необычные, но F2 — правдоподобная опечатка.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:290 (сравнение имени и значения), :292 (рекурсия без пробы), проба :441; обещание — шапка :29-30

ПРЕДЛОЖЕНИЕ: Имя свойства сравнивать без учёта регистра. Значение font-family нормализовать до сравнения: раскрыть CSS-экранирование, снять кавычки, пробельные символы CSS свести к одному пробелу, привести к нижнему регистру — и только потом сравнивать с «bodoni moda». Добавить в selftest пробы F2, F3, F7 и @font-face внутри `@media all { … }`: последняя убивает MISK.

СКЕПТИК: Опровергнуть не удалось. Пробы в css.mjs и css.json, мутант в mutanty.mjs и mutanty.json, копии в stand/<id>/. Во всех пробах src: local('Georgia'), гарнитура заголовка роли — по CDP getPlatformFontsForNode.

Что показали пробы:
- F2 (`Bodoni  Moda`), F3 (`Bodoni\ Moda`), F4 (`'Bodoni\20 Moda'`), F6 (перевод строки) и F7 (`FONT-FAMILY`): «сверено», заголовок — Georgia.
- Контроли с отказом «своя @font-face»: F5 `'Bodoni Moda'`, F8 `'bodoni moda'`. Сверх линзы: F9 (@font-face внутри `@media all`, рекурсия у настоящего судьи работает), F10 `@FONT-FACE` (регулярка с /i), F11 `Bodoni Moda` без кавычек с одним пробелом.
- Мутант MISK (снята рекурсия :292): --selftest 68/68, exit 0; --check exit 0. На входе F9 настоящий судья даёт отказ, мутант — «сверено». Контроль M00: 68/68. Кусачесть стенда показывает MX: 63/68.

Код прочитан верно: :290 сравнивает имя точно, значение сырой регуляркой; проба :441 ставит @font-face только на верхний уровень. Это обход обещания шапки :29-30, которое дала правка R3-SVERKA-5. Мерка — R3-SVERKA-2: обход закрытого класса есть дефект. F2 — правдоподобная опечатка.

######## R4-SVERKA-4 [дефект → подтверждено/дефект]
У строгой самопроверки дыра в пробах с двумя ожидаемыми видами отказа. «@theme reference» и «@import только внутри @media» засчитываются по любому одному виду, поэтому мутанты MFD3 (нет --font-display в @theme — «сверено») и MI (импорт в блоке) проходят 68/68. MI — регресс: в раунде 3 его убивала своя проба

ДОКАЗАТЕЛЬСТВО: Стенд D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/mutanty.mjs, итог в mutanty.json. Каждый мутант — копия stand/<id>/ с одной точной заменой (вхождение ровно одно). Таблица проб настоящего судьи — tablica.mjs (tablica.json, tablica.txt).
- На настоящем судье обе пробы получают по две строки: «@theme reference» → «не начинается» ×1 и «объявлен вне верхнего @theme» ×1; «@import только внутри @media» → «внутри блока» ×1 и «нет ровно @import» ×1. Критерий :477 (`bledy.every(b => vidy.some(...))`) засчитывает пробу, даже когда одна из двух строк пропала.
- MFD3: в :300 снято `!fdTheme.length ||`. --selftest копии — 68/68, exit 0; --check — exit 0. Различитель — строка --font-display удалена из @theme. Настоящий судья отказывает: «--font-display в верхнем @theme не начинается с 'Bodoni Moda' (нет)». Мутант отвечает «сверено». На странице (razlichiteli.json, R-MFD3) --font-display в выходе Tailwind 0 раз, заголовок — «Public Sans Thin SemiBold».
- MI: снят отказ «импорт Бодони внутри блока» (:296). Итог 68/68, exit 0. На 4c11041 проба ждала только 'внутри блока' (`git show 4c11041:…/znak.mjs`, строка 364) и в раунде 3 убивала мутант JI (47/48, «ЧИСТО (sverka)» sud-r3/SVODKA.md:294). Теперь не убивает.
Шапка :43-46 обещает: «проба не засчитывается по чужой причине».

ГДЕ: sites/7thserpent.com/tools/znak.mjs:435 и :439 (пробы-массивы), :476-477 (критерий), :296 и :300 (неохраняемые ветви); обещание — шапка :43-46

ПРЕДЛОЖЕНИЕ: Для ожидания-массива требовать, чтобы каждый названный вид встретился хотя бы раз и каждая строка была одного из видов. Либо разбить пробу на две, по одному виду в каждой. Добавить пробу «строка --font-display удалена из @theme» → «не начинается … (нет)». После правки MFD3 и MI должны ронять свою пробу.

СКЕПТИК: Опровергнуть не удалось, но с уточнением веса. Стенд — mutanty.mjs, итог в mutanty.json и mutanty.log; вред на странице — razl.mjs и razl.json.

- Настоящий --selftest: обе пробы-массива получают отказ (2) обоих видов.
- MI и MFD3: 68/68, exit 0, --check exit 0.
- Различители. MI: верхний @import цел, плюс `@media print { @import …latin-600 }` — настоящий судья даёт «внутри блока», мутант «сверено». MFD3: строка --font-display удалена из @theme — настоящий даёт «(нет)», мутант «сверено».
- Регресс подтверждён чтением кода. На 4c11041 проба :364 ждала только 'внутри блока', критерий :394 — `bledy.some(...)`, и JI падал 47/48 (sud-r3/SVODKA.md:294). Шапка :36 тогда обещала «каждая ветвь — хотя бы одной пробой»; 1faf396 заменил это обещание на «по чужой причине не засчитывается».
- Под MI проба «@import только внутри @media» проходит только отказом другой ветви («нет ровно @import»). Это та самая чужая причина: массив ожидания её узаконил.
- Предложение проверено. Копия S00 (только строгий критерий: каждый вид встретился) на настоящем коде даёт 68/68. MI+S и MFD3+S — по 67/68, каждый роняет свою пробу.

Уточнение веса. Ложное «сверено» MI на странице безвредно: Tailwind вкладывает импорт как `@media print{@font-face…}`, заголовок — Bodoni Moda (R-MI и сверх линзы R-MI2 с `@media screen`). Вредит MFD3: --font-display в выходе 0, заголовок — «Public Sans Thin SemiBold». Мерка — R3-SVERKA-4: мутант с ложным «сверено» и вредом на странице проходит самопроверку.

######## R4-SVERKA-5 [предел → подтверждено/предел]
Ещё девять ветвей без пробы: мутанты MT, MQ, ML, MF, MUQ, MK, MN, MLS и MKF проходят --selftest 68/68 и --check. Все с безопасной стороны: ложный отказ, неверный диагноз или сбой вместо отказа, но ни одна проба их не сторожит

ДОКАЗАТЕЛЬСТВО: mutanty.mjs, итог в mutanty.json. У каждого мутанта --selftest 68/68 с exit 0 и --check с exit 0. Различители (настоящий судья → мутант):
- MT: THEME = /^@theme$/ (:169). `@theme inline` → «сверено» → отказ (2) «не начинается … / вне верхнего @theme».
- MQ: импорт только в одинарных кавычках (:281). `@import "…latin-600.css"` → «сверено» → «нет ровно @import».
- ML: снят toLowerCase (:192). Поздний `:root { --accent: #ECA84A }` → «сверено» → отказ (17) «разными значениями».
- MF: любая @font-face — отказ (:290). `@font-face { font-family: 'Local' }` → «сверено» → «своя @font-face».
- MUQ: снята проверка кавычки в url( (:138). `url("a)b")` → «сверено» → «незакрытый блок».
- MK: у ICO пиксели сравниваются только в последней записи (:263). Чужая запись 16 → «пиксели записи 16 другие» → «пиксели записей те же (перекодирован или поля каталога)».
- MN: снята проверка длины каталога ICO (:233). ICO, обрезанный до 20 байт → «не ICO» → исключение «The value of "offset" is out of range».
- MLS (katalog читает любой элемент как файл) и MKF (katalog читает все имена): самопроверка через katalog идёт только по настоящей public/, поэтому починку R3-SVERKA-9 ни одна проба не держит. Сама починка на диске работает, см. ЧИСТО.
Почему предел: шапка :43-46 покрытия каждой ветви не обещает, отказ или exit 1 остаются. Но по мерке R3-SVERKA-4 и R2-SVERKA-6 эти ветви не сторожит никто.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:138, 169, 192, 233, 263, 281, 290, 359-361; проба — :392-472

ПРЕДЛОЖЕНИЕ: Добавить пробы «сверено»: `@theme inline {` и `@theme static {`, `@import "…"`, `#ECA84A` в позднем :root, чужая @font-face, `url("a)b")`. Добавить пробы отказа: чужая запись 16 в ICO → «пиксели записи 16 другие», обрезанный каталог → «не ICO». katalog проверять на копии папки на диске или вынести фильтр имён и lstat в чистую функцию над списком, которую можно гонять в памяти.

СКЕПТИК: Опровергнуть не удалось. Класс «предел» верен. Стенд — mutanty.mjs, итог в mutanty.json.

Все девять мутантов — MT, MQ, ML, MF, MUQ, MK, MN, MLS, MKF — дают --selftest 68/68 и exit 0, --check exit 0. Различители по линзе, настоящий судья → мутант:
- MT: `@theme inline` — «сверено» → отказ (2).
- MQ: `@import "…"` — «сверено» → «нет ровно @import».
- ML: `#ECA84A` — «сверено» → отказ (17).
- MF: чужая @font-face — «сверено» → «своя @font-face».
- MUQ: `url("a)b")` — «сверено» → «незакрытый блок».
- MK: чужая запись 16 — «пиксели записи 16 другие» → «пиксели записей те же».
- MN: ICO в 20 байт — «не ICO» → исключение «offset … Received 18».

На диске, по копии M00 против мутанта:
- MLS, папка public/favicon-dir: «не обычный файл» → EISDIR.
- MKF, различитель сверх линзы: неиконочный public/notes.txt без права чтения (icacls /deny, потом снят и удалён). Настоящий — «сверено», exit 0; мутант — «ОТКАЗ — EPERM», exit 1. Починка R3-SVERKA-9 на диске видна, но ни одна проба её не держит: selftest зовёт katalog только на настоящей public/.

Все мутанты на безопасной стороне: ложный отказ, неверный диагноз или сбой. Покрытие каждой ветви шапка с 1faf396 не обещает (на 4c11041 обещала, строка :36). Мерка — R3-SVERKA-9 и R2-SVERKA-10.

######## R4-SVERKA-6 [дефект → иной класс/предел]
Строка итога падает, когда у рисунка нет czesci или prostokaty, хотя модель ikonaSvg это допускает. Иконка 16 из одних многоугольников даёт в --check ложный «ОТКАЗ — Cannot read properties of undefined», а запись сначала пишет файлы, потом печатает «ОТКАЗ» с exit 1

ДОКАЗАТЕЛЬСТВО: Стенд D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/stand2.mjs, итог в stand2.json, случай S2. В копии znak.json у ikona16 нет prostokaty, а в czesci — одна перекладина ink.
- Запись `node tools/znak.mjs`: exit 1, «znak: ОТКАЗ — Cannot read properties of undefined (reading 'map')». При этом favicon-16x16.png и favicon.ico в public/ копии уже переписаны (sha256 до и после разные).
- `--check` сразу после записи: exit 1, та же строка. Сверка своих отказов не дала, упала печать итога.
Причина. :507 зовёт `w.znak.ikona.czesci.map` и `w.znak.ikona16.prostokaty.map` без `?? []`, а ikonaSvg (:202-203) рисует обе части у любой иконки через `?? []`. Запись файлов (:496-500) стоит до вычисления итога (:506-509). Выходит, что «ОТКАЗ» печатается после записи (мерка R2-SVERKA-9: сообщение врёт о действии), а --check падает на данных, которые модель допускает.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:507 (без `?? []`), 496-500 (запись раньше итога); модель — :202-203

ПРЕДЛОЖЕНИЕ: Имена красок для итога собирать из тех же вызовов farba(), которыми строятся файлы (например, Set внутри sverka, который она и возвращает). Это же закрывает R4-SVERKA-7. Итог считать до записи. Добавить пробу: иконка 16 только из czesci → «сверено».

СКЕПТИК: Факты воспроизведены. Стенд — stand2.mjs, итог в stand2.json, копии stand/T*/ с настоящим znak.mjs и правленым znak.json.
- T2 (у ikona16 нет prostokaty, в czesci перекладина ink): запись даёт exit 1 и «ОТКАЗ — Cannot read properties of undefined (reading 'map')». При этом favicon-16x16.png и favicon.ico переписаны (sha256 другой). --check: exit 1, та же строка.
- Сверх линзы, T4 (у ikona нет czesci, рисунок из prostokaty): то же, переписаны пять файлов иконки 32.
- Контроль T0: запись и --check exit 0, файлы не изменились.

Код прочитан верно: :507 без `?? []`, запись :496-500 стоит раньше итога :506-509, модель :202-203 допускает обе формы.

Почему не дефект. Ложного «сверено» или «записано» нет, брак не проходит, exit 1 в обоих режимах. Файлы записаны верные: у sverka ноль отказов, падает только печать итога. Громкий сбой вместо отказа проект классифицирует как предел: R1-IKONY-9, R2-SVERKA-10, R3-SVERKA-9. «ОТКАЗ» после записи — мерка R2-SVERKA-9 («сообщение врёт о действии»), а там класс «документ». Сегодня в данных есть обе части, так что сбой не срабатывает. Предложение линзы верное: собирать имена красок из вызовов farba() и считать итог до записи. Оно закрывает и R4-SVERKA-7.

######## R4-SVERKA-7 [документ → подтверждено/документ]
Правка R3-SVERKA-7 неполна: строка «краски трёх рисунков (с фонами иконок)» не называет краски ikona16.czesci и ikona.prostokaty

ДОКАЗАТЕЛЬСТВО: stand2.mjs, итог в stand2.json.
- S1: в копии znak.json у ikona16 есть czesci — уголок краски surface. Запись и --check дают exit 0 и строку «краски трёх рисунков (с фонами иконок): ink #e5eaee, accent #eca84a, bg #090c11», слова surface в ней нет. При этом пиксель (0,0) у stand/S1/public/favicon-16x16.png — 19,24,30, то есть #13181e (surface).
- S3: у ikona есть prostokaty краски danger. Запись: exit 0, слова danger в строке нет.
Список :507 собирает имена из shapka.czesci, shapka.napisy, ikona.tlo, ikona.czesci, ikona16.tlo и ikona16.prostokaty. R3-SVERKA-7 предлагал добавить и ikona16.czesci, а надёжнее — собирать имена из вызовов farba(). Сделано только про фоны. Сама сверка краски S1 и S3 судит, неверна только строка.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:506-509

ПРЕДЛОЖЕНИЕ: Как в R4-SVERKA-6: собирать имена из вызовов farba() при построении файлов, тогда строка не отстанет от рисунков.

СКЕПТИК: Опровергнуть не удалось. Стенд — stand2.mjs, итог в stand2.json.
- T1 (у ikona16 czesci — уголок краски surface): запись и --check дают exit 0 и строку «краски трёх рисунков (с фонами иконок): ink #e5eaee, accent #eca84a, bg #090c11». Слова surface в ней нет. Пиксель (0,0) у stand/T1/public/favicon-16x16.png — 19,24,30,255, то есть #13181e (surface); у T0 — 9,12,17.
- T3 (у ikona prostokaty краски danger): exit 0, danger в строке нет, а в favicon.svg есть fill #e85c57.

Список :507 — shapka.czesci, shapka.napisy, ikona.tlo, ikona.czesci, ikona16.tlo, ikona16.prostokaty. В нём нет ikona16.czesci и ikona.prostokaty. Правка R3-SVERKA-7 исполнена только про фоны. Сами краски сверка судит через farba() в ikonaSvg, неверна только строка. Класс и мерка — как у R3-SVERKA-7.

######## R4-SVERKA-8 [предел → иной класс/дефект]
«--font-display начинается с 'Bodoni Moda'» проверяется регуляркой по началу текста. Пропущенная запятая или непарная кавычка дают «сверено», а заголовки идут Public Sans или Times New Roman

ДОКАЗАТЕЛЬСТВО: css-r4.mjs (F1) и css-r4c.mjs (F1b, F1c), итоги в css-r4.json и css-r4c.json, D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/. Цепочка: sverka() → Tailwind 4.3.3 (сборка ok) → Chromium 151, гарнитура заголовка роли по CDP.
- F1 `'Bodoni Moda' ui-serif, …` (нет запятой): «сверено», «Public Sans Thin SemiBold».
- F1c `'Bodoni Moda'ui-serif, …`: «сверено», «Public Sans Thin SemiBold».
- F1b `'Bodoni Moda", ui-serif, Georgia, serif'` (непарные кавычки, одна строка): «сверено», «Times New Roman».
- K0: «Bodoni Moda 11pt SemiBold».
Регулярка :300 `/^['"]Bodoni Moda['"]/` смотрит только начало текста и принимает непарные кавычки. Буква шапки :29-31 («начинается с 'Bodoni Moda'») соблюдена, поэтому класс — предел. Но причина проверки 2 («гарнитура — тема») тут не выполняется молча. Мерка — R3-SVERKA-5 (там тоже предел).

ГДЕ: sites/7thserpent.com/tools/znak.mjs:300; шапка :29-31

ПРЕДЛОЖЕНИЕ: Регулярка с обратной ссылкой и концом имени: `/^(['"])Bodoni Moda\1\s*(,|$)/`. Добавить в selftest пробы F1 и F1b. Либо назвать это в ПРЕДЕЛАХ: «грамматику списка гарнитур сверка не судит».

СКЕПТИК: Факты воспроизведены, класс уточняю вверх. Пробы в css.mjs и css.json, цепочка до Chromium 151, гарнитура по CDP.
- G1 (нет запятой) и G1c: «сверено», «Public Sans Thin SemiBold».
- G1b (непарные кавычки): «сверено», «Times New Roman».
- Сверх линзы, G1d (`'Bodoni Moda' 'x', serif`, две строки подряд): «сверено», Public Sans.
- K0: «Bodoni Moda 11pt SemiBold».

Почему не предел. У G1, G1c и G1d буква шапки :29-32 («начинается с 'Bodoni Moda'») соблюдена — сами по себе это был бы предел. У G1b — нет. Значение начинается с `'Bodoni Moda"`, а не с `'Bodoni Moda'`: даже текстуально закрывающая кавычка другая. Собственный токенайзер сверки (chitatStroku) читает одну строку до последней «'», и первая гарнитура — «Bodoni Moda", ui-serif, Georgia, serif». Регулярка :300 `['"]…['"]` принимает непарную пару, то есть сверка неверно читает свой вход. По мерке R3-SVERKA-5 («сверка читает свой вход верно» → предел) это уже не предел, а дефект. Вес малый.

Предложенная регулярка `/^(['"])Bodoni Moda\1\s*(,|$)/` проверена: принимает настоящее значение и `"Bodoni Moda"`, отвергает G1, G1c, G1b, G1d и 'Playfair…, Bodoni'. Она закрывает все четыре формы.

######## R4-SVERKA-9 [предел → подтверждено/предел]
Строгий разбор громко отказывает на законном CSS: `@theme { --color-*: initial; … }` (идиома Tailwind), безвредный `--font-*: initial` в начале @theme и custom property с не-ASCII именем (`--цвет`) дают «объявление не читается»

ДОКАЗАТЕЛЬСТВО: css-r4.mjs, итог в css-r4.json. Цепочка: sverka() → Tailwind 4.3.3 → Chromium 151.
- L11 `@theme {` + `--color-*: initial;` в начале блока: отказ (1) «лист: объявление не читается: «--color-*: initial» — сверка не знает, что оно объявляет». Сборка ok, на странице --accent #eca84a, --font-display на месте, заголовок Бодони.
- L12 `--font-*: initial;` в начале @theme: такой же отказ, страница цела (в раунде 3 это доказано безвредным: «ЧИСТО (sverka)», sud-r3/SVODKA.md:289).
- L24 `.x { --цвет: red; }`: отказ «объявление не читается», страница цела.
Из 38 форм законного CSS отказ дали 5: L11, L12, L24 и известные с раунда 3 L25 (:root в @layer) и L36 (@import с layer()). Остальные 33 — «сверено», страница цела (см. ЧИСТО). Шапка :19-21 называет отказом только экранирование в имени и `--font-*`. `--color-*` и прочие сбросы пространств имён Tailwind, а также не-ASCII имена не названы. Регулярка имени :125 — `[a-zA-Z0-9_-]`. Отказ громкий, с безопасной стороны, поэтому предел.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:125-126; шапка :19-21

ПРЕДЛОЖЕНИЕ: В верхнем @theme читать `--<пространство>-*: initial` и `--*: initial` как сброс. Отказывать, только если сброс накрывает font-display (`--*`, `--font-*`) и стоит после --font-display по порядку листа; остальные пространства пропускать. В имени разрешить кодовые точки идентификатора CSS (`[-\w\u0080-￿]`). Либо назвать в шапке: «любой сброс пространства имён @theme и не-ASCII имя — отказ».

СКЕПТИК: Факты воспроизведены, класс «предел» верен. Пробы в css.mjs и css.json.
- L11 `--color-*: initial`, L12 `--font-*: initial` (обе в начале @theme) и L24 `.x { --цвет: red }`: отказ (1) «объявление не читается».
- При этом сборка ok, на странице --accent #eca84a, знак цел, заголовок Bodoni Moda.
- Контроль сверх линзы L24b `--_x`: «сверено». Регулярка :125 знает только ASCII.

Поправка к линзе. L12 шапка называет буквально: :20 — «объявление с нечитаемым именем (экранирование в имени, `--font-*`)». Это обдуманный выбор раунда 3: скептик R3-SVERKA-2 рекомендовал громкий отказ, который «сразу закрывает --font-* и --*», и на это есть пробы :433-434. Безвредность сброса в начале блока (sud-r3/SVODKA.md:289) этот выбор не отменяет. Не названы только `--color-*` (та же форма, что `--font-*`, так что названа по аналогии) и не-ASCII имена. Отказ громкий, на безопасной стороне, мерка — R3-SVERKA-9. Остаток — строка в шапке или разбор сбросов пространств имён.

######## ЧИСТО (sverka)
- Настоящие входы: `node tools/znak.mjs --check --dist` — exit 0, «сверено — 6 иконок public/ и dist/»; `--selftest` — 68/68, exit 0, 1,3 с (selftest-nastoyashchiy.txt). git status после всех прогонов чист, файлы репозитория не тронуты, все мои файлы — в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/sverka/.
- ТАБЛИЦА «проба → ждали → факт», настоящий судья, копия stand/LOG с одной вставкой печати (tablica.json, tablica.txt). Все 68 — ok, у каждой пробы все строки отказа ожидаемого вида.
- «сверено» → сверено (6): чистые входы; robots.txt; BOM; url() с «;» и «{»; экранированная скобка; значение только в комментарии.
- «не токен темы» → 1 строка (4): комментарий в :root; комментарий наверху; объявление в строке; токен в @media.
- «разными значениями» → 11 строк (4): «}» в строке; экранированная кавычка; другое значение в :root; после вложенного правила.
- «есть только: @theme / вложенный блок / другой блок» → по 1 строке (3).
- «не как #rrggbb» → 11 строк (4): rgb(), var(), #rgb, !important.
- «переопределён вне верхнего :root» → 11 строк (1).
- «текст вне блока» → 1 (2); «лишняя }» → 1 (1); «незакрытый блок» → 1 (1); «объявление не читается» → 1 (5).
- «@theme reference» → 2 строки двух видов.
- «нет ровно @import» → 1 (4); «@import только внутри @media» → 2 строки двух видов; «своя @font-face» → 1 (1).
- «не начинается с 'Bodoni Moda'» → 1 (3); «вне верхнего @theme» → 1 (1).
- «не равен пересчёту»: woff 700 — 2 строки, правка контура — 1, пустой контур — 1, параметр — 1.
- По 1 строке: «нет @font-face normal 600», «поле font», «нет надписей», «иконка 16: краска «glow»», «иконка 32, фон», «не PNG», «пиксели те же», «пиксели другие», «PNG не читается», «текст другой», «не ICO» ×2, «каталог ICO другой», «пиксели записи 32 другие», «пиксели записей те же», «лишний иконочный файл» ×2, «не обычный файл», «файла нет», «dist/favicon.svg», «dist/favicon-48x48.png: лишний», «dist/favicon.ico: файла нет».
- ТАБЛИЦА мутантов (mutanty.json). Всего 29 мутантов и контроль M00 (68/68, --check exit 0).
- Убиты своей именной пробой (17): MS1 (снято экранирование в строке) — 67/68, проба «экранированная кавычка»; MS2 (без строк) — 64/68, «объявление внутри строки», «}» в строке, экранированная кавычка, «@import только в строке»; MC (без комментариев) — 0/68, --check exit 1; ME — 67/68, «экранированная скобка»; MU — 67/68, «url() без кавычек»; MG — 66/68, обе пробы «вне блока»; MX — 63/68, пять проб «не читается»; MY, MZ, MR («:root с классом»), MB («!important»), MFD («Бодони второй»), MD («лишний в dist»), MNP («не файл»), MKAT («высота записи»), MNA («надписей нет»), MCZ («имя только в @theme») — каждый 67/68.
- Выжившие мутанты раунда 3 (JS1, JS2, JR, JF, JD, JC) теперь убиты.
- Выжили 12: MI и MFD3 (R4-SVERKA-4), MISK (R4-SVERKA-3), MT, MQ, ML, MF, MUQ, MK, MN, MLS и MKF (R4-SVERKA-5).
- Законный CSS, 33 из 38 форм — «сверено», сборка Tailwind ok, на странице --accent #eca84a, заливка rgb(236, 168, 74), заголовок Bodoni Moda (css-r4.json, L1–L38 без L11, L12, L24, L25, L36):
- вложенность с & и `.y &`; @supports и @container внутри правила; @media внутри вложенного &:hover; вложенный a:hover без &;
- @property --x; `@layer a, b;`; `@layer theme, base, components, utilities;` в начале; @import с layer() для a11y.css;
- пустые правила и «;;»; !important у обычного свойства; значение-блок у не-токена; селектор с запятой; :root в @layer без токенов; `:root, :host { --x }`;
- @custom-variant; @utility (обе формы); @variant; @apply; `@source inline("{flex,block}")`; `@theme inline`;
- @keyframes; @scope; @starting-style; @page с @top-center; @font-feature-values; @media (width >= 40rem);
- атрибут-селектор со строкой "{;}"; url("a(b).png"); поздний `#ECA84A` (регистр); `--empty:;`; «:» в значении; свойство -WebKit-Appearance.
- Токенайзер и разбор Tailwind согласны (U1–U6 перед верхним :root): url(data:a;b{c), url( data:a;b{c ) с пробелами, `.a\{b`, `.a\;b`, `content: "\"}"`, `content: '/*'`. Везде «сверено», и токены на странице на месте — у Tailwind нет своей ошибки разбора, которую сверка пропустила бы.
- Пробелы Юникода (N1–N8): сверка снимает NBSP и U+FEFF через JS trim и \s так же, как Tailwind. У `:root NBSP {`, `:root FEFF {`, `--accent:NBSP#eca84a`, `#eca84a NBSP`, `@theme NBSP {`, `--font-display:NBSP'…'` и `--accent NBSP :` на странице токены и Бодони на месте. Контроль N7 `.hdr { --accent NBSP : #ff0000 }` — отказ «переопределён», и на странице .hdr действительно red: отказ верный.
- Контроли: A5 `{#ff0000}` — отказ; F5 и F8 (`'Bodoni Moda'`, `'bodoni moda'`) — отказ «своя @font-face»; P3 (@property, inherits true, то же initial-value) — «сверено», страница цела. R-MISK2 `@layer x { @font-face … Georgia }` — отказ, хотя страница Бодони: ложный отказ с безопасной стороны. R-MI — вложенный импорт при верхнем импорте на страницу не влияет.
- Диск (stand2.json):
- D1: висячий узел каталога public/fonts. --check и запись — exit 0, «сверено» и «записано»: R3-SVERKA-9 закрыт.
- D2: узел каталога public/icon-set. D3: висячий узел public/favicon-old. Оба — отказ (1) «иконочное имя — не обычный файл».
- W1: устаревший favicon.svg ('<svg/>'). Запись его восстановила, затем --check — exit 0.
Узлы после прогона сняты.
- Шапка и ПРЕДЕЛЫ против кода:
- fontkitten импортируется на верхнем уровне (:90), так что «инструмент не запустится ни в каком режиме» — верно.
- katalog отбирает иконочные имена до lstat (:359-361), так что «прочие имена не читаются» — верно.
- Диагноз ICO по записям работает: на чужой записи 16 настоящий судья пишет «пиксели записи 16 другие» (различитель MK).
- В selftest 68 проб, как в сообщении 1faf396.
- @theme reference даёт отказ; `--font-*` и `--*` в позднем @theme — отказ. R3-SVERKA-1, -2, -3 и -6 закрыты своими пробами, мутанты MG, MX, MKAT и MT-обратный (THEME с \b) убиты.
- Раскрытие: один скрипт своей папки (css-r4c.mjs) я создал heredoc'ом оболочки, а не инструментом Write. Файл вне репозитория и вне git, LF; остальные файлы — через Write и Edit. Запрещённой подстроки нет ни в одной моей команде, описании, имени файла или переменной.

######## R4-BRAUZER-1 [дефект → подтверждено/дефект]
Рамка снимка — знак плюс 2 px: рамку, тень, отражение и копию знака дальше 2 px не видит ни рисунок (а), ни наложение (б), ни проверка стиля

ДОКАЗАТЕЛЬСТВО: Копия судьи sud4.mjs: код суда дословно, заменены точной строкой только импорт путей и главный блок (zrob-kopie.mjs, diff приложен в логе). Окно 1440 DPR 1, все 5 состояний. Мутации: a1 `.hdr__brand svg.znak{outline:2px solid #eca84a;outline-offset:4px}`, a2 `filter:drop-shadow(0 42px 0 #eca84a)`, a3 `drop-shadow(150px 0 0 #eca84a)`, a4 `-webkit-box-reflect: below 4px`, a5 `box-shadow:0 44px 0 0 #eca84a`. Итог: у всех «сверено», в каждом состоянии а=0 б=0 (log-a.txt, wyniki/proby-a.json). Настоящая страница (fakt.mjs: кадр 420×110 вокруг знака против чистой, fakt-proby-a-…json): a1 — 2 352 субпикселя, оранжевая рамка вокруг знака (fakt/a1-вверху.png); a2 — 3 867; a3 — 3 857, полная оранжевая копия знака поверх меню (fakt/a3-вверху.png); a4 — 3 868; a5 — 15 897. Судья печатает «знак … равен эскизу A и не перекрыт».

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:373-375 (clip = floor(r)−2 … ceil(r)+2), :50 («Рамка — коробка знака плюс 2 px»), :273-292 (stanZnakow не читает filter, box-shadow, outline, -webkit-box-reflect у svg и предков); sites/7thserpent.com/DESIGN.md:629 («Без прозрачности, градиентов, фильтров и теней»), :686-690; стенд — .playwright-mcp/sesja-11/sud-r4/brauzer/

ПРЕДЛОЖЕНИЕ: В sadStylu отказывать, если у svg.znak и его частей вычисленные filter, box-shadow, -webkit-box-reflect не none или outline-style не none (вне состояния «фокус»). И/или сравнение (б) делать в широкой рамке: полоса шапки целиком и полоса подвала ± 60 px. Там «страница как есть» против «подложка без знака + эскиз» ловит всё, что знак рисует вне коробки. Мутации a1 и a3 добавить в --selftest.

СКЕПТИК: Воспроизвёл на своей копии sk4.mjs (собрана kopia.mjs: код суда дословно, заменены только импорт путей и вход главного блока, diff проверен), окно 1440 при DPR 1. outline 2px с отступом 4px и drop-shadow(150px 0 0) у svg.znak дают «сверено», во всех 5 состояниях а=0 б=0 (log-p1.txt). На странице в кадре 420×110 это 2352 и 3629 субпикселей против чистой (fakt.json, r1-outline.png, r1-cien150.png). Контроль: тень 3px вниз попадает в рамку и ловится, а=3543 б=2684. Значит, судья видит только то, что заходит в коробку знака ±2 px (:373-375). stanZnakow (:273-292) не читает filter, box-shadow, outline и -webkit-box-reflect. tools/znak.mjs этих свойств тоже не читает. В ПРЕДЕЛАХ :79-97 это не названо, а DESIGN.md:629 запрещает фильтры и тени. Кадры эталона при DPR 1 увидели бы копию в шапке в покое. Та же мутация под :hover, :focus-visible или при плотности от 2 не видна никому. Стенд: .playwright-mcp/sesja-11/sud-r4/skeptik-brauzer/.

######## R4-BRAUZER-2 [дефект → подтверждено/дефект]
«Движения нет» проверяется один раз, сразу после загрузки: анимация, которая начинается при наведении, в фокусе или позже, проходит — снимок её гасит

ДОКАЗАТЕЛЬСТВО: Копия sud4.mjs, 1440 DPR 1. Мутации: a6 `.hdr__brand:hover svg{animation:r4a 1s infinite}`, a7 то же на :focus-visible, a8 `el.animate([{opacity:1},{opacity:.2},{opacity:1}],{duration:1000,iterations:Infinity})` через 1 500 мс после загрузки. У всех «сверено», в каждом состоянии а=0 б=0 (log-a.txt). На странице при animations:'allow' (fakt.mjs; «мигание» — два кадра с паузой 500 мс):
- a6 при наведении — 3 578 субпикселей против чистой, мигание 3 703;
- a7 в фокусе — 3 677, мигание 3 703;
- a8 — мигание во всех состояниях 3 702–4 640.
Знак пульсирует у каждого зрителя, который навёл мышь или пришёл Tab'ом.

ГДЕ: wiernosc.mjs:261-265 (ruch — один раз в otkryt, до состояний), :338 (animations: 'disabled' гасит бесконечные анимации при снимке), шапка :34-37; DESIGN.md:671 («Движения нет»), :690-691

ПРЕДЛОЖЕНИЕ: Проверку getAnimations() повторять в конце prep каждого состояния (наведение, фокус, ящик, подвал) и ещё раз через 3 с после загрузки. Сверх того — страж движения: два снимка рамки знака с animations: 'allow' через 500 мс, различие — отказ «движение». Мутации a6 и a8 добавить в --selftest. SMIL уже ловится (см. чисто) — как «растр нестабилен».

СКЕПТИК: Копия sk4, 1440 при DPR 1. Бесконечная анимация под .hdr__brand:hover и WAAPI через 1,5 с дают «сверено», во всех состояниях а=0 б=0 (log-p1.txt, r2a, r2b). На странице (fakt.json r2): до наведения у документа одна анимация — законченная trasa-pula. После наведения — «sk4a running на svg». Два кадра с паузой 500 мс при animations: 'allow' расходятся на 3687 субпикселей, при 'disabled' — на 0: снимок гасит анимацию. Код: ruch считается один раз в otkryt (:261-265), до всех prep. Шапка :34-37 обещает, что анимаций у знака нет, и не оговаривает, что это проверено только сразу после загрузки. В ПРЕДЕЛАХ не названо; DESIGN.md:671 — «Движения нет». CSS-переходы исключены намеренно (CSSTransition), к находке это не относится.

######## R4-BRAUZER-3 [дефект → подтверждено/дефект]
Перекрытие «попадание только в сам знак» даёт ложный отказ на законном pointer-events: none у svg знака

ДОКАЗАТЕЛЬСТВО: Копия sud4.mjs, мутация a9 `svg.znak { pointer-events: none }`. Знак от неё не меняется: во всех пяти состояниях а=0 б=0 (макс 2). Щелчок по-прежнему уходит ссылке — svg внутри неё. При этом 5 отказов «перекрыт: 1440 DPR 1, шапка, вверху: в центре частей — не знак: polygon: a.hdr__brand; …» — в шапке во всех состояниях и в подвале (div.ft__brand) (log-a.txt). pointer-events: none у иконки внутри ссылки — обычный приём.

ГДЕ: wiernosc.mjs:407-419 (zakryt: `el === svg || svg.contains(el)`); правка раунда 3 по R3-BRAUZER-1 (прежде принимался и предок)

ПРЕДЛОЖЕНИЕ: На время zakryt ставить лист `svg.znak, svg.znak * { pointer-events: auto !important }`: псевдоэлемент или элемент поверх по-прежнему попадёт в точку и даст отказ, а своё pointer-events знака отказа не даст. Пробу a9 («сверено» ждём) добавить в --selftest как контроль ложного отказа.

СКЕПТИК: Копия sk4, мутация svg.znak { pointer-events: none }: знак не меняется (а=0, б=0, макс 2 во всех состояниях), но судья выдаёт ОТКАЗ (5) «перекрыт … в центре частей — не знак: polygon: a.hdr__brand …» (log-p1.txt, r3). elementFromPoint в центрах всех пяти частей возвращает a.hdr__brand (fakt.json r3). Это ложный отказ. Приём обычный: ядро само ставит pointer-events: none иконкам шапки (core/chrome/SiteHeader.astro:274, :314). Сегодня отказ не срабатывает: у svg.znak сайта этого свойства нет. Строгость (:415, только svg и его потомки) введена в раунде 3 ради псевдоэлементов предка: попадание в ::after даёт сам предок. Предложение линзы (pointer-events: auto !important на svg.znak и потомков на время zakryt) эту строгость сохраняет.

######## R4-BRAUZER-4 [дефект → подтверждено/дефект]
«При прокрутке» — одна точка, scrollY 2000: секцию над шапкой на других прокрутках судья не видит, хотя README и DESIGN.md обещают «ничем не перекрыт при прокрутке»

ДОКАЗАТЕЛЬСТВО: Копия sud4.mjs, 1440 DPR 1:
- a10 `.noc{position:relative;z-index:60}` (первый экран над шапкой) — «сверено», все состояния а=0 б=0;
- a11 `.rail-sec{position:relative;z-index:51}` (лента) — «сверено».
На странице (fakt.mjs):
- a10 при scrollY 300 — 108 766 субпикселей: знак почти целиком под заголовком героя (fakt/a10-прокрутка300.png);
- a11 при scrollY 3900 — 75 927: заголовок и текст ленты поверх знака (fakt/a11-прокрутка3900.png);
- при 0 и 2000 обе секции не под знаком (probe.json: noc 69+810, rail-sec 3749+761).
kontrast-znaka ловит a10 только потому, что герой светлый (копия kontrast4, 390: «замер светлее границы — rgb(229 234 238) при scrollY 395»). Тёмную секцию над шапкой не поймает никто. Мутация самопроверки «содержимое над шапкой» поднимает весь main и потому попадает в 2000.

ГДЕ: wiernosc.mjs:451 (przewinDo(page, 2000) — единственная точка «при прокрутке»); sites/7thserpent.com/_baseline/README.md:123-125 («знак ничем не перекрыт … при прокрутке»); DESIGN.md:686-689

ПРЕДЛОЖЕНИЕ: В состоянии «при прокрутке» пройти страницу шагом в полвысоты окна, как в oknoPrzewin, и в каждой точке делать zakryt и (б) для знака шапки. Хватит одного окна на плотность, 1440 и 390 при DPR 1. Либо назвать предел: «перекрытие при прокрутке судится только при scrollY 2000». Мутации a10 и a11 добавить в --selftest.

СКЕПТИК: Копия sk4, 1440 при DPR 1. .noc { position: relative; z-index: 60 } и .rail-sec { z-index: 51 } дают «сверено», во всех состояниях а=0 б=0 (log-p1.txt, r4a, r4b). На странице прошёл прокрутку шагом 100 и в каждой точке проверил elementFromPoint в центрах частей (fakt.json r4). .noc перекрывает знак при scrollY 100–800 (8 точек), .rail-sec — при 3800–4400 (7 точек); при 2000 не перекрывает ни одна. Код: «при прокрутке» — одна точка, przewinDo(page, 2000) (:451). Мутация самопроверки поднимает весь main и поэтому попадает и в 2000. README эталона :123-125 и DESIGN.md:686-689 обещают «ничем не перекрыт … при прокрутке» без оговорки об одной точке. В ПРЕДЕЛАХ :79-97 этого нет. kontrast-znaka ловит .noc только потому, что первый экран светлый; тёмную секцию над шапкой не увидит никто.

######## R4-BRAUZER-5 [документ → подтверждено/документ]
«Сборка = dist/»: ответы окон пикселей не сверяются — 18 подменённых картинок окна DPR 2 прошли со «сверено» (остаток R3-BRAUZER-7)

ДОКАЗАТЕЛЬСТВО: probe.mjs: окно пикселей 1440 при DPR 2 берёт 18 файлов (mp1-k05…, mp3-k15…), которых окна прокрутки при DPR 1 не берут (tolko-dpr2.json). Мой сервер 4953 (srv.mjs, режим dpr2img) отдавал эти 18 файлов с чужими последними 64 байтами, статус 200. Все 18 подмен ушли браузеру (podmeny-4953.jsonl — 18 строк). Копия sud4.mjs без мутации, набор {пиксели 1440 DPR 2, прокрутка 1440 и 390} — «сверено», сверка сборки молчит (wyniki/proby-d-d1.json). Шапка судьи обещает «каждый ответ сервера со своего адреса … статус 200 и побайтно равен файлу dist/», итог — «сборка = dist/». Предложение R3-BRAUZER-7 «сверять ответы и в окнах пикселей» не исполнено и в ПРЕДЕЛАХ не названо.

ГДЕ: wiernosc.mjs:21-23 (п. 1 шапки), :616-631 (сверка только в oknoPrzewin и только без мутации), :423-427 (oknoPikseli ответов не читает), :727 (итоговая строка); DESIGN.md:692-693 («ответы сервера = dist/»)

ПРЕДЛОЖЕНИЕ: Собирать page.on('response') и в oknoPikseli и oknoDrzewa, сверять тем же кодом (статус 200 и байты = dist/). Либо сузить шапку, итог и DESIGN.md до «ответы окон прокрутки 1440 и 390 при DPR 1».

СКЕПТИК: Код: ответы сверяются только в oknoPrzewin и только без мутации (:616-631). oknoPikseli ответов не читает (:427 берёт из otkryt лишь ctx, page, ruch); oknoDrzewa — тоже. Замер (fakt.json r5). Окно пикселей 1440 при DPR 2 — верх, scrollY 2000, подвал, как у судьи — берёт со своего адреса 27 путей. 18 из них — кандидаты srcset webp (/_astro/mp1-k13…, mp2-k00…), окна прокрутки 1440 и 390 при DPR 1 их не берут. Эти файлы судья не сверяет ни по статусу, ни по байтам. Шапка :21-23 («каждый ответ сервера»), итог :727 («сборка = dist/») и DESIGN.md:692-693 говорят шире. Это неисполненная часть предложения R3-BRAUZER-7 (сводка раунда 3, строка 411: «сверять ответы и в окнах пикселей»); в ПРЕДЕЛАХ не названо. Подмену сервера из 18 файлов сам не повторял: чтения кода достаточно. Класс «документ» — достаточно сузить шапку, итог и DESIGN.md; сверка в окнах пикселей тоже закрывает находку.

######## R4-BRAUZER-6 [дефект → подтверждено/дефект]
Подмены своего сервера в --selftest не судят условие «статус 200»: обе проходят и на судье без этого условия

ДОКАЗАТЕЛЬСТВО: Вторая копия sud4-bez-statusa.mjs — строка `const rowny = r.status() === 200 && !!buf && …` заменена на `const rowny = !!buf && …`, больше ничего. Окно прокрутки 1440 без мутации (как PODMENY), свои серверы на 127.0.0.1:
- 4975 (лист 203 + «/*x*/», подмена самопроверки): копия без статуса — ОТКАЗ «ответ 203, sha256 6fd02f543fba; dist/ a0674b3bb3ca» (ловят байты);
- 4977 (шрифты 404, подмена самопроверки): копия без статуса — ОТКАЗ (9): «сбой загрузки … net::ERR_ABORTED» и байты;
- 4976 (лист 203 теми же байтами): копия без статуса — «сверено»; sud4 — ОТКАЗ «ответ 203, sha256 a0674b3bb3ca; dist/ a0674b3bb3ca (ждали 200)».
Значит, самопроверка прошла бы и после отката правки R3-BRAUZER-7 (условия статуса). Попутно: первый прогон на 4955 был недействителен — порт одновременно слушал чужой node (PID 36452, [::]:4955), и localhost ушёл к нему. Прогон повторён на 4975–4977 через 127.0.0.1.

ГДЕ: wiernosc.mjs:241-245 (PODMENY), :626 (условие статуса)

ПРЕДЛОЖЕНИЕ: Добавить подмену «лист стилей со статусом 203 теми же байтами»: её ловит только условие статуса. Для разделения причин — ещё «шрифт 200 чужими байтами». Каждую подмену ждать своим видом отказа.

СКЕПТИК: Воспроизвёл сам: sk4.mjs и sk4-bez-statusa.mjs (заменена точной строкой только :626, на 127.0.0.1:4961–4964). Без подмены обе копии дают «сверено». Лист 203 теми же байтами: sk4 — ОТКАЗ «ответ 203 … (ждали 200)», копия без статуса — «сверено». Подмена 1 самопроверки (203 и «/*x*/») и подмена 2 (шрифт 404): копия без статуса отказывает на обеих. Первую ловят байты (sha 6fd02f543fba против a0674b3bb3ca). Вторую ловят сбой загрузки net::ERR_ABORTED и пустое тело e3b0c44298fc (log-p3.txt, log-p3-bez.txt). Значит, --selftest прошёл бы и после отката условия «статус 200» (R3-BRAUZER-7). Мутант судьи выживает — дефект самопроверки, не судьи. Кроме того, trafil (:730) засчитывает любую строку, начинающуюся с «сборка», поэтому причины подмен не разделены.

######## R4-BRAUZER-7 [предел → подтверждено/предел]
При DPR 2,5 нетронутая сборка получает отказ: вне 7 замеренных плотностей не держатся ни «(а) = 0», ни «допуск 3 — округление»

ДОКАЗАТЕЛЬСТВО: Копия sud4.mjs на чистой странице (log-b.txt, log-e.txt, log-f.txt):
- 1440 DPR 2,5 — отказ «пиксели: подвал … 1970 субпикселей, макс 255», три прогона одинаково; (б) в том же состоянии 0;
- 1440 DPR 2,5 с полосой — (б) шапка 3 субпикселя, макс 28, во всех состояниях; (а) подвал 1 892;
- 1536 × 864 при 2,5 (3840 при 250 %) — (б) шапка 3 субпикселя, макс 56, подвал 6, макс 54; два прогона одинаково; с полосой — «сверено»;
- 390 DPR 2,5 — (а) шапка 4, макс 63; подвал 976.
Лист dpr25-podval-1440.png (снимки судьи, копия sud4-zrzut.mjs): различия (а) — по горизонтальным краям букв «SERPENT», во всю строку. Страница под SKRYJ ставит знак подвала в другую вертикальную фазу, чем страница как есть: (б) там 0. Значит, предпосылка (а) «SKRYJ не меняет растр знака» при 2,5 неверна. Различие 56 уровней в (б) — не «округление сложения альфы» (комментарий :153-155).
Чисто в 11 других окнах: 1440 × 1,1 / 1,2 / 1,33 / 2,25, 1920 × 1, 1536 × 1,25, 1280 × 1,5, 360 × 3, 412 × 2,625, 393 × 3 — (а)=0, (б)=0, максимум (б) ≤ 3.

ГДЕ: wiernosc.mjs:153-155 (TOL_NAKLADKI и его обоснование), :157-161 (SKRYJ), :164-168 («0 во всех 42 сочетаниях»), :188 (PLOTNOSTI), ПРЕДЕЛЫ :79-97 — о плотностях вне набора ни слова

ПРЕДЛОЖЕНИЕ: Назвать в ПРЕДЕЛАХ: 0 и допуск 3 замерены только на плотностях 1, 1,25, 1,5, 1,51, 1,75, 2, 3; при 2,5 (4K при 250 %) судья даёт ложный отказ. Либо добавить окна 1536 и 1440 при 2,5 и сделать (а) нечувствительным к раскладке слоёв (например, сверять (а) с эскизом только там, где (б) не сходится). Выбор — технический, за ведущим.

СКЕПТИК: Воспроизвёл дважды на копии sk4: чистая страница 1440 при DPR 2,5 даёт ОТКАЗ «пиксели: подвал … 1970 субпикселей, макс 255». В том же состоянии (б) = 0 (макс 2), в остальных четырёх состояниях а=0 б=0. Контроль при DPR 2 — «сверено» (log-p2.txt). Сам знак равен эскизу поверх подложки — ломается предпосылка (а), что SKRYJ не меняет растр. Окна 1536 × 864 при 2,5 и (б) с максимумом 56 не повторял. В наборе судьи 2,5 нет (PLOTNOSTI :188), поэтому в его прогонах ложного отказа нет. Это не дефект, а неназванный предел. Комментарий к допуску :153-155 уже сужен до замеренного («в 3 окнах из 23»). Но ПРЕДЕЛЫ :79-97 не говорят, что 0 в (а) и допуск 3 держатся только на плотностях набора. Выбор — назвать предел или добавить окна 2,5 и переделать (а) — технический.

######## R4-BRAUZER-8 [дефект → подтверждено/дефект]
zapas-menu.mjs не видит перенос пунктов рядами: меню в две строки поверх края шапки даёт «сверено» и больший «запас»

ДОКАЗАТЕЛЬСТВО: Копия zapas4.mjs: код замера дословно, заменены только импорт путей и список проб (zrob-zapas.mjs). Проба `.hdr__list{flex-wrap:wrap}` плюс 8 копий последнего пункта, ширины 961–1000, 320, 400, 960. Итог — «сверено» («НЕТ … ждём отказ «перенос», факт сверено», wyniki/zapas4-proby.json). На странице (zapas-fakt.json):
- на 961 пункты в двух рядах, второй ряд ниже шапки высотой 69 (fakt/z1-961.png);
- «запас» 497,75 против чистых 279,25 — метрика меряет до последнего li, а он во втором ряду.
Перенос считается только строками текста внутри одной ссылки. Разные top у самих li и нижний край списка против шапки не проверяются.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/zapas-menu.mjs:56-64 (li, strok по Range, zapas = n.right − li[last].right), шапка :4-8; sites/7thserpent.com/DESIGN.md:621 («пункты не переносятся ни на одной ширине 961–1180»), :624-625 («Шапка остаётся 68px»)

ПРЕДЛОЖЕНИЕ: Отказ, если у li больше одного значения Math.round(top) или нижний край списка ниже нижнего края .hdr. Писать в выгрузку высоту шапки и число рядов. Пробу «flex-wrap и лишние пункты» добавить в --selftest.

СКЕПТИК: Факт на странице (fakt.json r8, r8-961.png). На 961 с .hdr__list { flex-wrap: wrap } и 8 копиями последнего пункта пункты стоят в двух рядах (top −5 и 42). Низ списка 73,39 ниже низа шапки 69; переполнения нет (scrollWidth не больше clientWidth); «запас» 497,75, потому что меряется до последнего li во втором ряду. sad() (:84-93) отказывает только на перенос внутри ссылки (Range, :58), переполнение, запас меньше 0, размер знака и знак на бургере. Ни одно из этого не срабатывает, итог — «сверено»; это видно из кода по снятым числам. Уточнение к силе находки: в ядре у .hdr__list flex-wrap не задан (core/chrome/SiteHeader.astro:236-242, значит nowrap). Сегодня лишние пункты дают перенос текста или переполнение, и инструмент их ловит. Дыра открывается только после правки ядра, но судья и нужен против таких правок. DESIGN.md:621 («пункты не переносятся») и :624-625 («шапка остаётся 68px») этой проверкой не держатся.

######## R4-BRAUZER-9 [предел → подтверждено/предел]
zapas-menu.mjs в режиме ящика по-прежнему не видит бургер за краем окна (остаток R3-BRAUZER-10, не назван)

ДОКАЗАТЕЛЬСТВО: Копия zapas4.mjs, проба `@media (max-width:960px){.hdr__brand{margin-right:260px}}` — «сверено» (wyniki/zapas4-proby.json). На странице (zapas-fakt.json): на 320 и 400 бургер стоит в x 438, ширина 0 — на экране его нет. Правка раунда 3 добавила только «знак налезает на бургер» (зазор < 0) и размер знака. Предложение R3-BRAUZER-10 «отказывать, если бургер или знак выходят за окно», либо назвать предел, не исполнено. Шапка инструмента и DESIGN.md:622 («на 320 от знака до бургера 118px») об этом молчат.

ГДЕ: zapas-menu.mjs:50-53 (режим ящика — только znak_burger), :90 (отказ только при зазоре < 0); DESIGN.md:622

ПРЕДЛОЖЕНИЕ: В режиме ящика отказ, если burger.right > innerWidth, ширина бургера 0 или правый край знака за окном. Либо назвать это пределом в шапке инструмента и в DESIGN.md.

СКЕПТИК: Факт (fakt.json r9): на 320 с .hdr__brand { margin-right: 260px } у бургера left = right = 438 при innerWidth 320, ширина 0; znak_burger 284 не меньше 0. Отказа нет (:90 проверяет только зазор меньше 0). Раунд 3 признал R3-BRAUZER-10 пределом; предложение — отказ за краем окна или названный предел. Не исполнено ни то, ни другое. Поиск по репозиторию (вне .playwright-mcp) не нашёл ни одной оговорки о бургере за краем окна: ни в шапке zapas-menu.mjs :1-20, ни в DESIGN.md:622, ни в бэклоге §60.

######## R4-BRAUZER-10 [документ → подтверждено/документ]
kontrast-znaka.mjs судит токены, а не то, чем знак закрашен и есть ли кольцо: тёмная заливка частей и кольцо с outline-style: none проходят

ДОКАЗАТЕЛЬСТВО: Копия kontrast4.mjs: код дословно, заменены импорт путей, список проб и имя выгрузки (zrob-kontrast.mjs). Окно 390 DPR 1, первые 600 px:
- `.hdr__brand .znak polygon, .hdr__brand .znak path{fill:#3a3020 !important}` — «сверено»: берутся --accent и --ink из :root;
- `.hdr__brand:focus-visible{outline-style:none !important}` — «сверено», в выгрузке «обводка_фокуса 8.43» у кольца, которого не рисуется: outline-color вычисляется и при none.
(wyniki/kontrast4-proby.json). Заливку ловит wiernosc п. 3 (краска = литерал эскиза), но шапка контраста эту зависимость не называет. Кольцо не судит никто, а число контраста кольца читается так, будто кольцо проверено.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs:12-15 («считает контраст красок знака … и кольца фокуса»), :77-93 (краски — токены :root; кольцо — только outlineColor)

ПРЕДЛОЖЕНИЕ: Читать вычисленную заливку частей знака шапки и отказывать, если она не равна токену. Для кольца требовать outline-style ≠ none и outline-width ≥ 2px, иначе отказ или пометка «кольца нет». Либо в шапке прямо написать: контраст — токенов, связь «заливка = токен» держит wiernosc п. 3, наличие кольца не судится.

СКЕПТИК: Воспроизвёл копией kontrast-sk4.mjs (код дословно, заменены импорт путей, список проб и имя выгрузки), 390 при DPR 1. Тёмная заливка частей знака шапки (#3a3020) — «сверено»; outline-style: none у кольца — «сверено» (log-kontrast.txt). Код: краски — токены :root (:79), кольцо — только getComputedStyle().outlineColor (:88). Вычисленный цвет существует и без кольца: при outline-style none — outline-color rgb(242, 189, 110) и outline-width 2px (fakt.json r10). Поэтому одна проверка ширины, как в предложении линзы, не поможет — нужна проверка стиля. Сужение: что кольцо «не судит никто», уже сказано (README эталона :128, DESIGN.md:693-694, ПРЕДЕЛЫ wiernosc :88-90). Остаётся шапка kontrast-znaka.mjs :12-15: число названо контрастом «красок знака и кольца фокуса», хотя это контраст токенов и цвета кольца. Шапка не говорит, что связь «заливка = литерал» держит wiernosc п. 3, а наличие кольца не проверяется. То же у DESIGN.md:333-334 («обводка фокуса … 8,43:1»).

######## R4-BRAUZER-11 [предел → иной класс/дефект]
kontrast-znaka --selftest: у трёх ветвей отказа из семи проб нет (остаток R3-POLNOTA-2); сами ветви работают

ДОКАЗАТЕЛЬСТВО: В zamery/kontrast-znaka-selftest.json 5 проб: чистая, тёмный фонарь, прозрачная шапка, лист над шапкой, кольцо не того цвета. Без проб остаются «замер светлее границы» (новая ветвь раунда 3), «токены краски не #rrggbb», «фокус на ссылке знака не встал», а также «не загружено картинок». Мои пробы копией kontrast4.mjs (wyniki/kontrast4-proby.json):
- `.noc{position:relative;z-index:60}` — «замер светлее границы — rgb(229 234 238) при scrollY 395 против rgb(39 42 46)»;
- `--accent: rgb(236 168 74)` — «токены краски не #rrggbb — --accent: «rgb(236 168 74)»»;
- `.hdr__brand{visibility:hidden}` — «фокус на ссылке знака не встал».
Все три — ок. Коммит 9de3a99 пишет «--selftest 5/5», R3-POLNOTA-2 требовал мутацию на каждую ветвь.

ГДЕ: kontrast-znaka.mjs:157-163 (proby), :85, :91, :134-135 (ветви без проб)

ПРЕДЛОЖЕНИЕ: Добавить три пробы выше (дёшевы, проходят) и пробу «не загружено картинок» (например, img с src на 404 своего сервера).

СКЕПТИК: Список проб (:157-163) — 5 проб, ветвей отказа больше. Без проб остаются «замер светлее границы» (:134), «токены краски не #rrggbb» (:85), «фокус не встал» (:91), «не загружено картинок» (:135) и отказ sborkaIliOtkaz. Ветви рабочие: копия kontrast-sk4 даёт 6/6 (log-kontrast.txt). .noc z-index 60 — «замер светлее границы — rgb(229 234 238) при scrollY 395»; --accent: rgb(236 168 74) — «токены краски не #rrggbb»; .hdr__brand { visibility: hidden } — «фокус на ссылке знака не встал». Класс уточняю: это не предел инструмента, а дефект самопроверки того же рода, что R4-BRAUZER-6. Удаление любой из этих ветвей --selftest не заметит, а R3-POLNOTA-2 требовал «на каждую ветвь отказа — хотя бы одна мутация». Шапка :36-38 честно перечисляет, какие пробы есть. Строка «--selftest 5/5» коммита 9de3a99 полноты не утверждает.

######## R4-BRAUZER-12 [предел → подтверждено/предел]
sborka.mjs разбором HTML видит не всё, что браузер берёт: @import, import() модулей, poster, <use>/<image href>, <object data>, url() в style="" и листы с rel='stylesheet' не сверяются

ДОКАЗАТЕЛЬСТВО: Поддельная сборка lipa/dist. Копия помощника lipa-sborka.mjs — заменён точной строкой только SITE. Сервер 127.0.0.1:4978, итог в lipa.json.
- Браузер запросил, сверка не взяла: /b.css (@import в листе), /chunk.js (import() во встроенном модуле), /obj.svg (<object data>), /poster.png (<video poster>), /sprite.svg (<use href>), /svg-image.png (<image href>), /styl-atr.png (style="background-image:url()"); bledy [].
- Листы c.css (`rel='stylesheet'`) и d.css (`rel="preload stylesheet"`) взяты как файлы, но их url() не читались.
На нынешней сборке дыры нет: sborka('http://localhost:4331/') — 82 файла, ошибок 0; не сверены только /sitemap-0.xml и /sitemap-index.xml, на них ничто не ссылается. Все 82 sha равны выгрузке kontrast-znaka.json.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/sborka.mjs:5-7 (шапка: «всё, на что он ссылается … href, src и srcset элементов, url() внутри отданных листов»), :68 (7 имён тегов), :73 (`rel=("?)stylesheet\1`), :79 (url() — только у листов из HTML, без @import-строк и рекурсии); коммит 9de3a99 («все ресурсы страницы»)

ПРЕДЛОЖЕНИЕ: Назвать предел в шапке помощника (что разбор не читает). Либо сверять то, что страница реально запросила со своего адреса, — браузером, как wiernosc, с отказом на ответ не 200.

СКЕПТИК: Чтение кода подтверждает каждую дыру. Теги — только link, script, img, source, video, audio, iframe (:68), атрибуты — href, src, srcset, imagesrcset (:73-74). Значит, не читаются poster, <object data>, <use> и <image> SVG, url() в style="". /\brel=("?)stylesheet\1/ (:73) не узнаёт rel='stylesheet' и rel="preload stylesheet". url() разбирается только у листов из HTML, без рекурсии, а @import "x" без url() не видит (:79). На нынешней сборке дыры нет. В dist/index.html нет poster, <use>, <image>, <object>, url() в style и rel в одинарных кавычках. Оба script type="module" встроенные, без import. В листе нет @import. Файлов 82: index.html, 6 иконок и 75 в _astro; вне сверки только sitemap-0.xml и sitemap-index.xml, на них ничто не ссылается. Шапка :5-7 («всё, на что он ссылается») говорит шире, чем читает разбор, — неназванный предел.

######## R4-BRAUZER-13 [документ → иной класс/дефект]
pochodzenie: trim() срезает ведущий пробел первой строки git status --porcelain — «грязь» первого файла пишется искажённой

ДОКАЗАТЕЛЬСТВО: В zamery/wiernosc.json gryaz_instrumentov = ["M docs/…/sborka.mjs", " M docs/…/wiernosc.mjs"], gryaz_sajta = ["M sites/7thserpent.com/tools/znak.mjs"]. В kontrast-znaka.json первая строка — "M docs/…/kontrast-znaka.mjs", остальные шесть — " M …". Строка «M путь» с одним пробелом не бывает строкой формата porcelain (XY путь): изменённый в рабочем дереве — « M путь», в индексе — «M  путь». Причина — `.trim()` над всем выводом: node -e `' M a/sborka.mjs\n M b/wiernosc.mjs\n'.trim().split('\n')` → ["M a/sborka.mjs"," M b/wiernosc.mjs"]. Остальное происхождение верно: sha256 инструментов в выгрузках равны блобам 9de3a99 (wiernosc cced4a7c, sborka 826bae3c, zapas-menu e5255d8f, kontrast 88d81eac, znak-podvala-okno 19e5f66c).

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/sborka.mjs:29 (git(...).trim()), :38-39

ПРЕДЛОЖЕНИЕ: trimEnd() вместо trim() (или split('\n') до обрезки) — первая строка сохранит код состояния.

СКЕПТИК: Подтверждаю: sborka.mjs:29 делает git(...).trim() над всем выводом, и у первой строки porcelain срезается ведущий пробел. node: ' M a\n M b\n'.trim().split('\n') даёт ["M a"," M b"], а trimEnd() — [" M a"," M b"]. В выгрузках первая строка искажена: wiernosc.json gryaz_sajta ["M sites/7thserpent.com/tools/znak.mjs"]; kontrast-znaka.json — "M …/CLAUDE.md" и "M …/kontrast-znaka.mjs" при остальных " M …"; zapas-menu.json так же. Строка «M путь» с одним пробелом — не формат XY, её можно прочесть как изменение в индексе. Класс уточняю: неверна не проза, а запись, которую пишет код. Правка — в коде (trimEnd), поэтому дефект, хотя и мелкий: ни на один суд не влияет. Уже снятые выгрузки несут искажённую первую строку.

######## ЧИСТО (brauzer)
- Внешние запросы: 6 из 6 браузерных запросов на чужой адрес пойманы «запросы». Проверены speculationrules prefetch и prerender, link rel=compression-dictionary, fetchLater, preload только с imagesrcset и контрольная картинка. Журнал чужого сервера zhurnal-4952.jsonl подтверждает, что все 6 запросов ушли (proby-c.json).
- Смешивание и фильтры, контроли: mix-blend-mode: difference у частей знака — (а) 77 субпикселей, (б) 2 490. html opacity .9 — (а) 3 505. saturate(0) у .hdr — (а) 2 962. Все три — отказ.
- SMIL: <animate> из другого svg по href на трассер знака — отказ «растр нестабилен» во всех состояниях шапки. На странице мигание 399–400 субпикселей.
- Подмены своего сервера ловятся. Лист 203 с чужим байтом, шрифт 404 и лист 203 теми же байтами — отказ «сборка» у судьи. Шрифт 404 — ещё и «сбой загрузки» (8 шрифтов net::ERR_ABORTED).
- Допуск наложения 3: в 11 окнах вне набора судьи на нетронутой сборке (а)=0 и (б)=0, максимум (б) ≤ 3. Окна: 1440 × 1,1 / 1,2 / 1,33 / 2,25, 1920 × 1, 1536 × 1,25, 1280 × 1,5, 360 × 3, 412 × 2,625, 393 × 3, 1536 × 2,5 с полосой. Исключение — DPR 2,5 без полосы (R4-BRAUZER-7).
- Полоса прокрутки в окнах pasek действительно есть: рамка знака x 125 против 130 на 1440 при 1,75 и 502 против 507 на 2194 (zamery/wiernosc.json).
- Чистый прогон копии судьи на 1440 DPR 1 совпал с выгрузкой: 5 состояний, а=0, б=0, максимум (б) 2.
- zapas-menu: крупный шрифт через :root 125 % и 150 % равен настройке браузера (CDP Page.setFontSizes 20 и 24). Числа 181,31 и 83,39 на 961 и 279,25 без крупного шрифта воспроизведены. Медиазапросы в rem у .container перекрыты правилом ядра вне слоя (max-width: var(--container)).
- zapas-menu --selftest: 5 проб проходят по своей причине; повторил 1 из 5 на копии.
- kontrast-znaka: полный прогон копии на 4 окнах совпал с выгрузкой: граница 7,05 / 11,9 / 8,43; замер 7,32 и 12,37 (390, scrollY 3730), 7,54 и 12,73, 7,61 и 12,85 (1440, 4210); кольцо rgb(242, 189, 110); bledy [].
- kontrast-znaka --selftest: 5 проб выгрузки проходят по своей причине. Ветви «замер светлее границы», «не #rrggbb» и «фокус не встал» проверены моими пробами — работают.
- znak-podvala-okno: признак растра первого окна воспроизведён в 3 прогонах: 252 523 на 1440 и 20 901 на 390 — как в выгрузке.
- sborka.mjs на нынешней сборке: 82 файла из 84 dist/ (не сверены только два sitemap, на них ничто не ссылается), ошибок 0; все 82 sha256 равны выгрузке kontrast-znaka.json.
- sha256 инструментов во всех выгрузках zamery/ равны блобам коммита 9de3a99. Правка 9b896e5 меняет только самопроверку судьи.
- Репозиторий не тронут: git status пуст. Мои серверы (4952–4957, 4975–4978) остановлены. Всё своё — в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/brauzer/.

######## R4-MATERIALY-1 [документ → подтверждено/документ]
В окне Chromium при 1,25, 1,5 и 1,75 перекладина шапки в полутоне, как в подвале. Чёткая шапка бывает только в безоконном растре, а материалы «экрана владельца» и DESIGN.md показывают и называют её чёткой

ДОКАЗАТЕЛЬСТВО: 1) kletki.mjs: все 24 клетки okno-plotnosti.png извлечены точно. Неоднородных блоков 3×3 — 0, выхода за край — 0. Все 12 чисел совпали с materialy.json: как вырезано, после выравнивания и dy (kletki.json).
2) perekladina-okno.mjs: столбец в 6 CSS px от края знака, зелёный канал.
- Шапка в окне: при 1,25 — «180 234×4 71», при 1,5 — «126 234×5 126», при 1,75 — «71 234×6 180».
- Шапка без окна: «234×5», «234×6», «234×7», полутонов 0.
- По 11 столбцам 2–12 CSS: в окне полутон в 11 из 11, без окна — в 0 из 11 (shapka-vs-podval.json).
3) shapka-vs-podval.mjs: в окне знак шапки совпадает со знаком подвала — 0 расхождений (макс 0 / 1 / 0) при 1,25 / 1,5 / 1,75. Без окна шапка против подвала даёт 644 / 1006 / 840. Это и есть числа листа «после выравнивания» 638 / 1006 / 840: без окна шапка отличается от собственного оконного растра.
4) Независимый оконный кадр: vkladka-1_75x-svetlaya.png и -temnaya.png (окно vkladka-dowod.mjs, 900 DIP, вариант 0, прокрутка 0). Перекладина знака шапки в 6 столбцах — «68 234×6 178». При масштабе 1 — «234×4», при 2 — «234×8» (vkladka-perekladina.json).
5) Без окна полутон подвала от прокрутки не зависит: 6150–6157 при 1,25 / 1,5 / 1,75 дают один профиль (podval-prokrutka.json). Значит, фраза DESIGN.md о подвале верна для безоконного растра. В окне же сдвинута фаза растра всей страницы, и шапке это тоже достаётся.
6) Где сказано обратное:
- DESIGN.md:661-666 — «в шапке это держится на высотах 68 и 38»;
- подпись podval-plotnosti.png (materialy-priemki.mjs:194) — «мягче, чем в шапке»;
- znak-plotnosti.png — «DPR 1,75 — экран владельца (175 %)» и ekran-vladelca.png («Экран владельца») показывают безоконную чёткую шапку;
- подпись okno-plotnosti.png даёт голое «840 (макс 110)»;
- README эталона:129-131 отсылает к этим листам.
Окон я не открывал. Все числа — из оконных кадров самих инструментов, Chromium 151; браузер владельца — Chrome 153.

ГДЕ: sites/7thserpent.com/DESIGN.md:661-666; docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:186, 193-194, 222, 350; znak-plotnosti.png, ekran-vladelca.png, okno-plotnosti.png, vkladka-1_75x-*.png; sites/7thserpent.com/_baseline/README.md:129-131; улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/materialy/{kletki.mjs, kletki.json, kletki/, perekladina-okno.mjs, perekladina-okno.json, shapka-vs-podval.mjs, shapka-vs-podval.json, vkladka-perekladina.mjs, vkladka-perekladina.json, podval-prokrutka.mjs, podval-prokrutka.json, pg-175-shapka.png}

ПРЕДЛОЖЕНИЕ: 1) DESIGN.md «Чёткость перекладины»: написать, что в Chromium с окном при 1,25 / 1,5 / 1,75 край перекладины шапки — две полутоновые строки, как у подвала: растр окна у шапки и подвала один, 0 расхождений. Чёткая шапка — только без окна; фаза в окне страницей не управляется.
2) В ekran-vladelca.png добавить оконный вырез шапки при 1,75 из блока окна с подписью.
3) В подписях znak-plotnosti.png, ekran-vladelca.png и okno-plotnosti.png: «в окне шапка = подвал (полутон), без окна шапка чёткая».
4) В доклад — строкой приёмки глазами на экране владельца (175 %, его Chrome).
Это правка инструмента приёмки, после неё — снова «судью судят».

СКЕПТИК: Опровергнуть не удалось. Проверил на независимом оконном кадре своим профилем (skeptik-materialy/vkladka-profil.mjs → vkladka-profil.json). Кадры vkladka-*.png, вариант 0: начало страницы нашёл сам, смотрел все 12 столбцов перекладины, от 33 до 44 CSS px.
- 1,75, светлая и тёмная тема: «67–70, 234×6, 178–179» — полутон в 12 столбцах из 12. Край на 33,75 от начала содержимого (153), по раскладке — 35,0.
- Масштабы 1 и 2: «234×4» и «234×8», полутонов 0.
Значит, в окне Chromium 151 растр всей страницы стоит в дробной фазе. Фаза не своя у каждого окна: окно 900×420 даёт то же, что окно 1100×560 у materialy-priemki.
Сверил и остальное. В materialy.json «okno» при 1,75: ramka_okno.y 15,000001, ramka_bez_okna.y 15. Шапка стоит в одной точке раскладки, а растр различается. kletki.json: 12 из 12 совпадают, неоднородных блоков 0. shapka-vs-podval.json: в окне шапка = подвал (0/0, 0/1, 0/0).
Пределом это не названо нигде:
- DESIGN.md:661-666 («в шапке это держится на высотах 68 и 38») без оговорки «без окна»;
- znak-plotnosti.png (materialy-priemki.mjs:186, 193) подписывает безоконную чёткую шапку «DPR 1,75 — экран владельца»;
- README:129-131 отсылает к листам и ничего не оговаривает.
Лист ekran-vladelca.png честно подписан «снимки Chromium без окна» (:222), но что в окне шапка в полутоне, не сказано и там.
Изделие не бракуется: фазу задаёт панель браузера, а не страница. Поэтому класс «документ».
Уточнения:
1) Формулировка «чёткая шапка только без окна» слишком широка. В окне при 1 и 2 шапка чёткая. При дробном масштабе фаза — дробная часть высоты панели браузера (DIP) × масштаб. Моя оценка по рядам начала страницы 87/109/131/153/174 — около 85 DIP (≡ 1 mod 4), отсюда 0,25/0,5/0,75.
2) У Chrome 153 владельца своя панель (закладки и прочее), его фаза не проверена. Писать «в окнах Chromium 151 инструментов — полутон; у браузера владельца — по его панели, приёмка глазами».
3) Подпись podval-plotnosti.png «мягче, чем в шапке» верна для безоконных снимков на самом листе. Нужна оговорка «без окна», а не замена.

######## R4-MATERIALY-2 [дефект → подтверждено/дефект]
Правило «не то состояние — файл не пишется» выполняется только для fokus-1440.png и yashchik-390.png. При незагруженных картинках, недостигнутой прокрутке, неоткрытом ящике на 1,75, отсутствии полосы прокрутки и без принудительных цветов файлы ложатся под принятыми именами

ДОКАЗАТЕЛЬСТВО: proba-stanow.mjs собирает копию инструмента в памяти. Изменено только это: вырезаны блоки «Плотности» и «Растр окна» и добавлено zapis.okno ??= {} для итоговой строки печати. Сервер — свой, над dist/, порт 4955; мутации — в своём браузере.
- kartinki (запросы hero*.webp абортированы): exit 1, 13 ошибок «видимые картинки не загружены». Записаны все 13 PNG, ne_zapisano []. pervy-ekran-1440.png — hero с alt-текстом вместо арта (sha 9c971845… против принятого 607c1051…), и он внесён в pliki с sha256.
- prokrutka (скрыт <main> в блоке «шапка над содержимым»): exit 1, «прокрутка 0, ждали 4210» и «551, ждали 3730». shapka-nad-soderzhimym.png записан с подписями «прокрутка 4210 — худший фон под знаком…».
- yashchik175 (бургер без слушателя только в окне 390 при 1,75): exit 1. ekran-vladelca.png записан 1350×724 вместо 1350×1431 — строки ящика молча нет, ne_zapisano [].
- pasek (2194 открыт обычным browser): exit 0, bledy []. Подпись: «с полосой прокрутки Windows 0 px — …; знак с x 509».
- forced (forcedColors: 'none'): exit 0, bledy []. prinuditelnye-cveta.png показывает обычные цвета под подписями «принудительные цвета, схема light/dark».
- Метка не найдена (строка 298): ox = Infinity, и sharp.extract бросает «Expected integer … received Infinity» (проверено). Прогон обрывается в para(), когда все безоконные файлы уже переписаны, а materialy.json остаётся прежним.
По коду: zapiszEsli стоит только в строке 174, fokus — в 166-167. otkryt (87), прокрутка (234), ящик 1,75 (213-214 — строка просто пропускается) и ikony (392-393) лишь пополняют bledy и пишут файл дальше. Полоса (205-207) и forcedColors (218, 244) не проверяются вовсе. Подпись ekran-vladelca «обводки «TH» при этой плотности нет» — литерал, не вычисленная ob. Шапка скрипта (строки 10-11) и коммит 9de3a99 («при неверном состоянии файл не пишется, подписи — из проверок») обещают больше.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:10-11, 75, 87, 166-167, 174, 205-207, 213-214, 218, 222, 234, 244, 298, 316, 392-393; улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/materialy/{proba-stanow.mjs, stan-kartinki.json, stan-prokrutka.json, stan-yashchik175.json, stan-pasek.json, stan-forced.json, stan-*/, pe-kart.png, pasek-2194.png, forced-none.png}

ПРЕДЛОЖЕНИЕ: 1) Как предлагал R3-MATERIALY-3: писать во временную папку и переносить в OUT вместе с materialy.json только при пустом bledy.
2) Проверять перед снимком: pasek > 0 у окна «с полосой», matchMedia('(forced-colors: active)') и схему, devicePixelRatio каждого окна.
3) Ящик на 1,75 — через zapiszEsli всего листа или отказ.
4) При не найденной метке — отказ до записи.
5) Подпись об обводке — из вычисленной обводки.
6) Эти мутации — в --selftest инструмента.

СКЕПТИК: Опровергнуть не удалось, воспроизвёл сам (skeptik-materialy/proba-kartinki.mjs → proba-kartinki.json, out-kartinki/). Копию инструмента собрал в памяти: вырезаны блоки «Плотности» и «Растр окна», свой сервер над dist/ на 4961, запросы hero*.webp абортированы.
Итог — exit 1, «ОТКАЗ — 13», но записаны все 13 PNG, ne_zapisano [], все 13 внесены в pliki с sha256. Не совпадают с принятыми pervy-ekran-1440.png (9c971845… против 607c1051… — то же, что у линзы), pervy-ekran-390.png, yashchik-390.png и ekran-vladelca.png.
Код прочитан верно:
- zapiszEsli — только в :174, пропуск fokus — :166-167;
- otkryt (:87), прокрутка (:234) и ikony (:392-393) только пополняют bledy;
- ящик 1,75 (:213-214) молча выпадает из листа;
- полосу (:205-207) и forcedColors (:218, :244) никто не проверяет;
- подпись об обводке в :222 — литерал.
sharp.extract с Infinity бросает «Expected integer … received Infinity» — проверил.
Шапка инструмента (:10-11) обещает «файл не пишется; подпись — из проверки». Это остаток подтверждённого дефекта R3-MATERIALY-3: коммит 9de3a99 закрыл его только для двух файлов.
Уточнения:
1) Когда метка не найдена, прогон обрывается в para() до блока «Иконки». ikony.png не переписывается, materialy.json остаётся прежним, и sha256 в нём расходятся с переписанными файлами. «Все безоконные файлы» — неточно.
2) Мутации pasek и forced — правки кода, а не сбой среды. Но проверки состояния, которую обещает шапка, у них нет. В pasek подпись честно печатает «0 px», и всё равно файл под принятым именем пишется.
3) Закоммиченные материалы сняты чистым прогоном: bledy [], ne_zapisano []. Дефект скрытый, при сбое громкий по exit, но он подменяет файлы.

######## R4-MATERIALY-3 [документ → подтверждено/документ]
okno-plotnosti.png объясняет сдвиг выравнивания «остатком меньше пикселя», но записанные рамки его не дают. Шапка 1,75: остаток 0, сдвиг +1. Подвал 1,75: остаток −0,23, сдвиг +1, знак противоположный. Подвал 1,5: остаток −0,55, сдвиг −2. Предложения R3 (метка не у края, vp, вывод и хеш okno.ps1) не сделаны

ДОКАЗАТЕЛЬСТВО: Остаток (ramka_bez_okna.y − ramka_okno.y)·s по materialy.json «okno» и выбранный сдвиг:
- подвал 1,25: −0,227 → dy −1;
- подвал 1,5: −0,547 → dy −2;
- шапка 1,75: −0,0000017 → dy +1;
- подвал 1,75: −0,227 → dy +1.
roznica сравнивает a[y] окна с b[y+dy], поэтому отрицательный остаток ждёт dy ≤ 0.
Числа листа верны: kletki.json — 12 из 12, лучший сдвиг тот же. Неверно объяснение в materialy-priemki.mjs:305-307 и в подписи :350: «Знак без окна ставится в ту же точку окна; остаток сдвига меньше пикселя даёт сдвиг растра».
Что стоит за сдвигом по данным:
- при 1,75 оконный растр выше раскладки: перекладина шапки по раскладке на 35,0 пикселя устройства, а в клетке окна первая светлая строка — 17 («71») при cy 16, то есть край на 33,75. То же в vkladka-1_75x-*.png (od 33, «68 …»). Без окна — 35,0;
- при 1,5 у подвала −2 — безоконная постановка: R3, skeptik-materialy/podval-geometria.json, po_dy −2 → 0.
Гипотеза R3 «метку закрывает край панели» не проверяема той же меткой. В vkladka-1_75x-svetlaya.png строки 151–152 — разделитель панели (225,227,225), содержимое чисто начинается со 153, и метка в (0,0) не показывает, где начало страницы под разделителем. Ряд начала страницы y−x = 100 / 120 / 141 при 80·s = 100 / 120 / 140.
Из предложений R3-MATERIALY-1 и -8 не сделано:
- метка по-прежнему в (0,0), сплошной блок 8s×8s не проверяется (строки 292, 296);
- vp окна в materialy.json нет;
- вывод okno.ps1 отбрасывается (287);
- sha256 okno.ps1 не пишется нигде: pochodzenie хеширует только сам скрипт и sborka.mjs (sborka.mjs:31-40).
Любой сдвиг до 3 пикселей принимается и с рамками не сверяется.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:287, 292, 296, 305-307, 321, 333, 350; instrumenty/sborka.mjs:31-40; materialy.json «okno»; улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/materialy/{kletki.json, geom-175.mjs, geom-175.json, vkladka-perekladina.json}; D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r3/skeptik-materialy/podval-geometria.json

ПРЕДЛОЖЕНИЕ: 1) Заменить объяснение в подписи и комментарии фактом: «сдвиг из рамок не выводится; при 1,75 оконный растр на 1,25 пикселя выше раскладки, при 1,5 у подвала −2 — безоконная прокрутка на полупикселе».
2) Писать ожидаемый сдвиг Δ·s рядом с найденным и помечать или отказывать при |dy − Δ·s| > 1.
3) Метку ставить в (16,16) CSS и требовать сплошной блок.
4) Писать vp, вывод okno.ps1 и sha256 okno.ps1.
Это правка инструмента приёмки, после неё — снова «судью судят».

СКЕПТИК: Опровергнуть не удалось. Остатки пересчитал по materialy.json «okno»:
- подвал 1,25: (214,34375 − 214,525)·1,25 = −0,227 → dy −1;
- подвал 1,5: (213,34375 − 213,7083)·1,5 = −0,547 → dy −2;
- шапка 1,75: (15 − 15,000001)·1,75 ≈ 0 → dy +1;
- подвал 1,75: −0,227 → dy +1.
roznica берёт b[y+dy], поэтому объяснение «остаток сдвига меньше пикселя даёт сдвиг растра» (:305-307, подпись :350) не сходится в трёх случаях из четырёх. У шапки 1,75 остатка нет, а сдвиг +1. У подвала 1,5 сдвиг 2 при остатке 0,55. У подвала 1,75 знак противоположный.
Настоящая причина видна в моём vkladka-profil.json. При 1,75 край перекладины шапки лежит на 33,75 от первой чистой строки содержимого (153), по раскладке — 35. Фаза страницы в окне дробная, а метка в (0,0) видна только с первой незакрытой строки. Отсюда +1 и у шапки, и у подвала 1,75.
Проверил по коду, что не сделано из R3-MATERIALY-1 и -8:
- метка 8×8 в (0,0), сплошной блок не проверяется (:292, :296);
- vp в wynik нет (:334);
- вывод execFileSync отбрасывается (:287);
- pochodzenie хеширует только инструмент и sborka.mjs (sborka.mjs:31-40);
- любой |dy| ≤ 3 принимается без сверки с рамками (:321).
Числа листа верны (kletki.json, 12 из 12). Неверны объяснение и полнота записи — класс «документ».
Уточнение: okno.ps1 лежит в папке instrumenty/. Грязь этой папки пишется (gryaz_instrumentov), так что при чистом дереве его содержимое восстановимо по head_pri_progone. Отсутствие sha256 okno.ps1 — неполнота, а не потеря.

######## R4-MATERIALY-4 [дефект → подтверждено/дефект]
vkladka-dowod.mjs по-прежнему пишет vkladka-*.png, dowod-*.png и vkladka-para.png до выводов, а vkladka.json — выгрузка рядом с этими файлами — не содержит ни bledy, ни wniosek. Остаток R3-MATERIALY-3 не закрыт

ДОКАЗАТЕЛЬСТВО: - vkladka-dowod.mjs:184-192: снимки пишутся в цикле запусков.
- Выводы и bledy — в строках 199-220.
- vkladka-para.png пишется в строке 264, до выгрузок; exitCode выставляется в 289.
- При любом отказе файлы под принятыми именами уже переписаны: рамка не найдена (169), зелёного меньше 0,9 (205), (г) не красный (212), (б) не равно (0) (218).
- Ключи vkladka.json: instrument, brauzer, instrument_sha256, head_pri_progone, gryaz_sajta, gryaz_instrumentov, dist_index_sha256, snimki, przebiegi_0, para, dowod. Нет bledy, wniosek, chrome и pomoshchnik_sha256; в dowod.json они есть.
- R3-MATERIALY-3 ПРЕДЛ: «В vkladka-dowod.mjs — тот же порядок: выводы сначала, файлы потом». Коммит 9de3a99 правит порядок записи только в materialy-priemki.mjs.
- Прогоном не проверено: окна открывать нельзя. Порядок записи установлен по коду и от данных не зависит.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/vkladka-dowod.mjs:184-192, 199-220, 261-266, 283-284, 289; vkladka.json

ПРЕДЛОЖЕНИЕ: 1) Держать кадры в памяти или во временной папке: сначала выводы, запись — только при пустом bledy.
2) В vkladka.json писать bledy и wniosek (или хотя бы «годен: да/нет»), chrome и pomoshchnik_sha256.

СКЕПТИК: Опровергнуть не удалось. Порядок в vkladka-dowod.mjs прочитан верно:
- vkladka-*.png и dowod-*.png пишутся в цикле (:184-192);
- выводы и bledy — :199-220;
- vkladka-para.png — :264;
- выгрузки — :283-284;
- exitCode — :289.
Ключи закоммиченного vkladka.json сверил: instrument, brauzer, instrument_sha256, head_pri_progone, gryaz_sajta, gryaz_instrumentov, dist_index_sha256, snimki, przebiegi_0, para, dowod. Нет bledy, wniosek, chrome и pomoshchnik_sha256. В dowod.json они есть; bledy [], 16 sha256 из 16 совпадают с файлами. R3-MATERIALY-3 (подтверждён как дефект) в предложении прямо называл vkladka-dowod.mjs. Коммит 9de3a99 его не тронул — остаток дефекта.
Уточнения:
1) Если рамка не найдена вовсе (x1 < 0), прогон падает в том же запуске на Buffer.alloc(Infinity) в :173 (RangeError — проверил). Это раньше записи и раньше JSON. Запуски (а) идут первыми, поэтому vkladka-*.png тогда не тронуты. Переписаны только dowod-a-*.png прежних запусков, а dowod.json и vkladka.json остаются старыми с расходящимися хешами. Рамка не того размера, (а)/(в) не зелёные, (г) не красная, (б) ≠ (0) — все PNG и vkladka-para.png переписываются, bledy пишется только в dowod.json.
2) Шапка vkladka-dowod.mjs, в отличие от materialy-priemki, не обещает «файл не пишется». Но правило R3-MATERIALY-3 принято для обоих инструментов.
3) vkladka.json ссылается на dowod/dowod.json, где вывод есть. Отсутствие в нём bledy и wniosek — неполнота, а не ложь.

######## R4-MATERIALY-5 [документ → подтверждено/документ]
Контроль (г) показал, что без SVG вкладка и при масштабе 1 берёт favicon-32x32.png. Пиксельный рисунок на сетке 16 Chromium не показывает нигде, а проза говорит лишь «растровую иконку»

ДОКАЗАТЕЛЬСТВО: - dowod.json: у d-1-svetlaya и d-2-svetlaya zapros_ikon = ["/favicon-32x32.png"], czerwonych 1. /favicon-16x16.png не запрошен ни в одном из 18 запусков.
- В <img> Chromium берёт из ICO запись 32: в materialy.json naturalWidth 32. Мой прогон ico-img.mjs: <img> 32 = запись 32 (0 из 3072), <img> 16 против записи 16 — 62 субпикселя, макс 122.
- С SVG вкладку рисует favicon.svg (варианты а и в).
- Base.astro:51 объявляет favicon-16x16.png с sizes="16x16".
- DESIGN.md:699-712: «три рисунка … все три одобрены вместе с эскизом A, их подобие шапке — приёмка глазами» и «без ссылки на SVG вкладка берёт растровую иконку». Какую именно — не сказано.
Итог: рисунок ikona16 владелец видит только увеличенным файлом (ikony.png ×4), а не так, как его показывает клиент.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/dowod/dowod.json (d-1-svetlaya, d-2-svetlaya); sites/7thserpent.com/DESIGN.md:699-712; sites/7thserpent.com/src/layouts/Base.astro:51; улика — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/materialy/{ico-img.mjs, ico-img.json}

ПРЕДЛОЖЕНИЕ: 1) В DESIGN.md «Фавикон» и в докладе написать: «без SVG Chromium 151 берёт PNG 32 и при масштабе 1; пиксельный рисунок 16 (PNG 16 и запись ICO 16) в Chromium не виден нигде — остаются только другие клиенты, не проверены (бэклог 60)».
2) Нужен ли ikona16 и как его принимать — вопрос владельцу.

СКЕПТИК: Опровергнуть не удалось, но пробел узкий. В dowod.json за 18 запусков zapros_ikon — только /favicon.svg и, у d-1-svetlaya и d-2-svetlaya, /favicon-32x32.png (красных 1). /favicon-16x16.png не запрошен ни разу. В materialy.json stany: naturalWidth ICO в <img> 32, «<img> 16 против записи 16» — 62. ico-img.json линзы: <img> 32 = запись 32, 0 из 3072. Base.astro:51 объявляет PNG 16 с sizes="16x16".
Частично это уже сказано:
- DESIGN.md:708-710 — «пиксельную запись 16 он там [в <img>] не показывает»;
- DESIGN.md:711-712 — «PNG, ICO и apple-touch-icon — для других клиентов … не проверялось (бэклог 60)»;
- BACKLOG 60 п. 4 — другие движки не прогнаны.
Не сказано другое. Во вкладке (г) Chromium сам берёт PNG 32 — и при масштабе 1, так что «PNG — для других клиентов» с (г) расходится. Какую «растровую иконку» берёт вкладка (DESIGN.md:705-706), тоже не названо. И нигде не записано, что рисунок на сетке 16 в Chromium не появился ни в одном прогоне.
Уточнение: «не показывает нигде» проверено только на вкладке при масштабах 1 и 2 и в <img> при DPR 1. Закладки, история и ярлыки не смотрелись. Нужен ли ikona16 — политический вопрос владельцу, не правка.

######## R4-MATERIALY-6 [документ → подтверждено/документ]
ekran-vladelca.png: «окно 2194 … с полосой прокрутки Windows 10 px». Полосы на вырезе нет, и это полоса безоконного Chromium (18 пикселей устройства при 1,75), а окно Chromium при 1,75 рисует её шириной 16

ДОКАЗАТЕЛЬСТВО: - Вырез — левые 760 CSS px (materialy-priemki.mjs:207); полоса на x 2184–2194 в кадр не попадает.
- pasek-bez-okna.mjs, те же флаги, что у browserPasek: при 1,75 на 2194 и 886 innerWidth−clientWidth = 10, дорожка (14,18,23) — 18 пикселей устройства; при 1 — 10, при 2 — 20.
- Оконные кадры vkladka-*.png (вариант 0), дорожка и ползунок: при 1 — столбцы 882–891 (10), при 1,75 — 1547–1562 (16, в светлой и тёмной), при 2 — 1767–1786 (20).
- Значит, при 175 % окно даёт 16 / 1,75 = 9,14 CSS px, а не 10.
- На 2194 фаза знака целая в обоих случаях: 882 и 883 по той же формуле, что дала 504 и 127.
- На 1440 с полосой при 1,75 без окна знак стоит на x 127 → 222,25 пикселя устройства. В окне по той же формуле: (2520 − 16) / 1,75 → x 127,43 → 223,0. То есть горизонтальная фаза «с полосой» без окна — не оконная. Это расчёт, окон я не открывал.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:70, 204-207; ekran-vladelca.png; vkladka-1x-svetlaya.png, vkladka-1_75x-svetlaya.png, vkladka-1_75x-temnaya.png, vkladka-2x-svetlaya.png; улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/materialy/{pasek-bez-okna.mjs, pasek-bez-okna.json, pasek-okno.mjs}

ПРЕДЛОЖЕНИЕ: 1) Подпись: «полоса безоконного Chromium — 10 CSS px, 18 пикселей устройства; окно Chromium при 1,75 рисует 16 пикселей устройства».
2) Вырез 2194 взять с правым краем, где видна полоса.
3) Если «с полосой» должно значить «как в окне», ширину полосы брать из оконного кадра.
4) Окна судьи «с полосой» при 1,75 устроены так же — вне линзы, строкой в отчёт.

СКЕПТИК: Существо подтверждаю своими замерами, одну часть снимаю.
1) Оконные кадры (skeptik-materialy/pasek-vkladka.mjs → pasek-vkladka.json), дорожка (14,18,23) между фоном шапки и рамкой окна:
- при 1 — столбцы 882–891, 10 px;
- при 1,75 — 1547–1562, 16 px, в светлой и тёмной теме;
- при 2 — 1767–1786, 20 px.
2) Без окна, те же флаги, что у browserPasek (ignoreDefaultArgs --hide-scrollbars; pasek-bez.mjs → pasek-bez.json, свой сервер на 4962). На 2194 и 1440 при 1,75 innerWidth − clientWidth = 10, дорожка 18 пикселей устройства, знак x 504 (882) и 127 (222,25). При 2 — 20.
Значит, «полоса прокрутки Windows 10 px» в подписи :207 — полоса безоконного Chromium. Окно при 175 % даёт 16/1,75 = 9,14 CSS px, и геометрия «экрана владельца» с полосой взята не из окна. Ни README:117, ни DESIGN.md, ни шапка wiernosc.mjs (:186) этого не оговаривают.
Что снимаю: «полосы на вырезе нет» — не ошибка. Подпись сама говорит «левая часть 760 px», а лист — «снимки Chromium без окна» (:222).
Положение знака в окне (x ≈ 504,57 → 883,0 и 127,43 → 223,0) — расчёт линзы, окон никто не открывал. Он опирается на 16 px из оконного кадра 900 DIP, а не из окна 2194. Строка о судьях wiernosc «с полосой» — вне линзы, в отчёт.

######## R4-MATERIALY-7 [предел → подтверждено/предел]
Для приёмки на 175 % не хватает: кольца фокуса при 1,75, принудительных цветов в тёмной схеме при 1,75 и в окне, оконной шапки в геометрии владельца (2194 с полосой), подвала полным кадром при 1,75 и вкладки (б), (в), (г) при 1,75

ДОКАЗАТЕЛЬСТВО: - fokus-1440.png — только DPR 1 и 2 (materialy-priemki.mjs:146-166). Кольцо фокуса не судит ни один судья (подпись самого листа), а при плотности владельца его не показывает ни один файл.
- Принудительные цвета: в ekran-vladelca.png — только светлая схема при 1,75 (218), prinuditelnye-cveta.png — только DPR 1 (244), в окне — нигде.
- okno-plotnosti.png: окно 1100×560 DIP со скрытой полосой (271, 275), только вырезы знака; геометрии 2194 с полосой в окне нет.
- Подвал при 1,75 — только вырез ×3 в podval-plotnosti.png.
- vkladka-dowod ZAPUSKI: (б), (в), (г) — только масштабы 1 и 2 (:105); при 1,75 снят только (а).
- Шапка в окне при 1,75 — в полутоне (R4-MATERIALY-1), и ни один полный кадр этого не показывает.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:146-166, 196-223, 241-253, 267-275; instrumenty/vkladka-dowod.mjs:100-106

ПРЕДЛОЖЕНИЕ: 1) Добавить в ekran-vladelca.png фокус при 1,75 и принудительные цвета тёмной схемы при 1,75.
2) Снять оконный кадр шапки 1440 и 2194 при 1,75 с видимой полосой в геометрии владельца.
3) Вкладку (г) при 1,75 — одним запуском.
4) Либо назвать эти пробелы в докладе и дать владельцу короткий список: открыть http://localhost:4331/ в своём Chrome при 175 % — фокус с Tab, контрастная тема, шапка и подвал вблизи.

СКЕПТИК: Пробелы по коду подтверждаю, одно утверждение снимаю.
По коду:
- fokus-1440.png — только otkryt(1440, 900, 1) и (…, 2) (:146-166);
- принудительные цвета при 1,75 — только светлая схема (:218), тёмная — только при DPR 1 (:244), в окне — нигде;
- okno-plotnosti.png — окно 1100×560 со скрытой полосой (:271, :275), только вырезы знака;
- подвал при 1,75 — только вырез ×3 в podval-plotnosti.png;
- в vkladka-dowod ZAPUSKI при 1,75 только (а) и (0) — (б), (в), (г) только при 1 и 2 (:105).
Что снимаю: «шапка в окне при 1,75 — ни один полный кадр этого не показывает» — неверно. vkladka-1_75x-svetlaya.png и -temnaya.png — оконный кадр верха страницы при 1,75 во всю ширину окна, с видимой полосой 16 px. Полутон перекладины на нём виден (я мерил по нему, vkladka-profil.json). Раскладка там узкая (около 877 CSS px, с бургером), не 1440 и не 2194.
Пределом в целом это не названо, но рамку задаёт README:132-134. Там сказано, что фокус, плотности и принудительные цвета в П83 п. 4 не входят и показываются материалами. Значит, это пробел полноты материалов для приёмки на 175 %, а не дыра в П83 п. 4. Изделие не бракуется — класс «предел».
Дешёвый путь линзы (п. 4: владелец сам открывает страницу в своём Chrome 153 при 175 % — фокус с Tab, контрастная тема, шапка и подвал вблизи) закрывает и R4-MATERIALY-1: фазу окна его браузера не снимет ни один инструмент.

######## ЧИСТО (materialy)
- sha256 всех 32 PNG совпадают с выгрузками: 16 — с materialy.json «pliki», 16 — с vkladka.json и dowod.json «snimki» (meta.mjs)
- Происхождение сходится: instrument_sha256 материалов (bc58fa45…), вкладки (d61c7096…) и pomoshchnik_sha256 (826bae3c…) равны блобам в 9de3a99, be41ad9, 9b896e5 и файлам рабочего дерева; okno.ps1 (18686460…) не менялся с 77e4f1d
- Размеры PNG сходятся с заявленной плотностью: ekran-vladelca.png 1350×1431 (клетки 760×1,75 = 1330, переполнение fullPage снял целиком), vkladka-1_75x 1575×263 = round(150·1,75), dowod-a-1_75x 875×70; 8 кадров (первый экран, шапки, ящик, подвалы, znak-dpr1-x4) и vkladka-1x/2x побайтно те же блобы, что в 4c11041
- okno-plotnosti.png: 24 клетки извлечены точно (неоднородных блоков 3×3 — 0, выхода за край — 0), 12 из 12 чисел (как вырезано, после выравнивания, dy) равны materialy.json. Выравнивание прячет только целый перенос: перебор 49 сдвигов по внутренней области, числа после выравнивания честные
- ikony.png, R3-MATERIALY-2 закрыт: клетка «<img> 16» побайтно равна моему безоконному снимку (0 из 256 пикселей), 62 субпикселя против записи 16 воспроизведены, <img> 32 = запись 32 (0 из 3072), обе записи ICO полностью непрозрачны — сравнение с белым фоном снимка честное
- shapka-nad-soderzhimym.png: подписи равны kontrast-znaka.json (7,54 / 12,73; 7,61 / 12,85; 7,32 / 12,37 дважды; граница 7,05 / 11,9), прокрутки 4210 и 3730
- vkladka-para.png, R3-MATERIALY-9 (2) закрыт: увеличение ×8 / ×4 / ×4 по масштабу, доли и максимумы 0,188 / 68, 0,122 / 55, 0,004 / 50 равны vkladka.json
- Контроль (г) обоснован, R3-MATERIALY-7 закрыт: d-1 и d-2 — красного 1,0 в рамке варианта (а) и запрос PNG; (б) — красного 0, против (0) доля 0 и макс 0; dowod-*.png глазами — зелёный / «7» / зелёный / красный, рамки 16, 28 и 32
- Вкладка при 1,75 снята, R3-MATERIALY-4 закрыт: рамка 28×28 в (96, 21), a-1,75 — зелёного 1,0, у варианта 0 контрольного цвета нет
- okno.ps1: 21 метка (18 вкладки и 3 окна) — ни одна не подстрока другой
- Состояния ekran-vladelca воспроизведены без окна: 2194 при 1,75 без --hide-scrollbars — clientWidth 2184, полоса 10, знак x 504; ящик 390 при 1,75 открыт, высота 279,375
- Проверки фокуса и ящика 390 при DPR 3 по коду пишут файл только при ok (materialy-priemki.mjs:166-167, 174)
- DESIGN.md «в подвале — от высоты содержимого» верно для безоконного растра: профиль перекладины подвала один при прокрутках 6150–6157 на 1,25, 1,5 и 1,75 (24 замера)
- Репозиторий не тронут: git status чистый, свой сервер 4955 остановлен, окон не открывал; всё своё — в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r4/materialy/

######## R4-PROZA-1 [документ → подтверждено/документ]
«Фон, рамку и кольцо фокуса самой ссылки знака не судит никто» — неверно: в покое при DPR 1 их видит эталон, краску кольца судят kontrast-znaka.mjs и гейт

ДОКАЗАТЕЛЬСТВО: Формулировку правка раунда 3 взяла у линзы R3-PROZA-2 без уточнений двух скептиков. Скептик R3-PROZA-2 писал: «краску кольца судят гейт контраста и kontrast-znaka.mjs». Скептик R3-BRAUZER-4 писал: «рамку ссылки в покое видит эталон при DPR 1» (sud-r3/SVODKA.md:678, :371).
(1) Эталон. У кадров первого экрана зон шума нет: зоны по szum.json стоят только на baseline-mobile-full-390, blok-around и desktop-full-1440. Мой прогон sud-r4/proza/p4-etalon-ssylka.mjs (свой сервер :4951, строки 0–67 против baseline-*-hero): чистая страница — 0 субпикселей на 1440 и на 390. Фон ссылки #eca84a — 14 349 и 14 349. Рамка 1px — 13 381 и 4 739. Тень 0 0 0 2px — 2 160 и 2 160. Максимум везде 227, так что accept откажет.
(2) Кольцо. kontrast-znaka.mjs:12-14, 32, 86-92 читает вычисленный outline-color в :focus-visible и отказывает, если он не --accent-text. Проба «кольцо фокуса не того цвета» даёт ok: true (zamery/kontrast-znaka-selftest.json). Граница кольца 8,43:1 (zamery/kontrast-znaka.json). Гейт gates/contrast.mjs:103-104 держит пары accent-text/bg и accent-text/bg-band.
(3) Шапка wiernosc.mjs сама себе противоречит. На :86-87 написано «положение видят кадры эталона при DPR 1», а на :88-90 про фон и рамку — «не судит их никто».

ГДЕ: sites/7thserpent.com/_baseline/README.md:128-129; sites/7thserpent.com/DESIGN.md:693-694; docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:88-90

ПРЕДЛОЖЕНИЕ: Во всех трёх местах: «Фон, рамку и тень ссылки знака при наведении, в фокусе, с ящиком и при DPR больше 1, а также наличие и вид кольца фокуса не судит никто. В покое при DPR 1 их видят кадры первого экрана эталона (зон там нет). Краску кольца судят kontrast-znaka.mjs (outline-color = --accent-text, 8,43:1) и гейт контраста. Кольцо показано в fokus-1440.png».

СКЕПТИК: Проверил сам, опровергнуть не удалось. Скрипт skeptik-proza/s1-etalon-ssylka.mjs, свой сервер :4961, вывод в s1-etalon-ssylka.json. Сравнивал шапку (строки 0–67) с baseline-*-hero при DPR 1. Чистая страница — 0 различий на 1440 и на 390. Полупрозрачный фон ссылки даёт 14 336 и 14 334 субпикселя (макс 36), рамка 1px — 13 381 и 4 739 (макс 227), outline в покое — 2 160 и 2 160. По szum.json у кадров hero зон нет, значит accept откажет. Правило «:focus-visible outline: none» в покое даёт 0: наличие кольца эталон не видит. kontrast-znaka.mjs:86-92 читает вычисленный outline-color в :focus-visible и отказывает, если он не равен --accent-text. Проба «кольцо фокуса не того цвета» в самопроверке есть. Гейт gates/contrast.mjs:103-104 считает пары токенов accent-text/bg и bg-band, а не отрисованное кольцо. Скептики R3-PROZA-2 и R3-BRAUZER-4 (sud-r3/SVODKA.md:678, :371) это уже писали, правка раунда 3 их не учла. Отсюда фраза «не судит никто» в README:128, DESIGN.md:693-694 и wiernosc.mjs:89 неверна. Уточнение к предложению: outline-color вычисляется и при outline-style: none, поэтому kontrast-znaka судит только краску кольца, а не его наличие. Формула линзы «наличие и вид кольца не судит никто» верна. Кадры эталона — тоже ручной суд через accept:frames, а не гейт сборки.

######## R4-PROZA-2 [документ → подтверждено/документ]
DESIGN «Чёткость перекладины»: правило «положение знака ложится на пиксель устройства» ложно на обоих примерах того же абзаца

ДОКАЗАТЕЛЬСТВО: DESIGN.md:662-663: «чёткий, только когда положение знака ложится на пиксель устройства: в шапке это держится на высотах 68 и 38».
Шапка. Верх svg — 15 CSS px (sud-r4/proza/p3-perekladina.txt). В пикселях устройства это 18,75 при 1,25, 22,5 при 1,5 и 26,25 при 1,75, то есть не на пикселе. При этом полутоновых строк 0 на 1440 и 2194, с полосой и без.
Подвал 1440. svg стоит на 6584,19, Chromium округляет его до 6584. 6584 × 1,25 = 8230, × 1,5 = 9876, × 1,75 = 11522 — целые. При этом полутоновых строк 2 при 1,25, 1,5 и 1,75.
Предсказывает верно правило для перекладины: верх знака, округлённый до целого CSS px, плюс 5 (znak.json shapka: перекладина y 5–9). Шапка: 20 → 25 / 30 / 35, чёткий край. Подвал: 6589 → 8236,25 / 9883,5 / 11530,75, полутон. На 2194: 6699 × 1,75 = 11723,25, полутон — совпадает с замером.
Это остаток (а) скептика R3-PROZA-7 (sud-r3/SVODKA.md:733): «правильно: положение перекладины, округлённое Chromium до целого CSS px». Правка раунда 3 переписала фразу, но слово «знака» оставила.

ГДЕ: sites/7thserpent.com/DESIGN.md:661-666

ПРЕДЛОЖЕНИЕ: «При дробной плотности край чёткий, только когда верх перекладины ложится на пиксель устройства. Chromium ставит знак на целый CSS px, перекладина на 5 px ниже. В шапке это 20 px (25 / 30 / 35 пикселей устройства); в подвале 1440 — 6589 (8236,25 / 9883,5 / 11530,75), полутон при 1,25, 1,5 и 1,75, в том числе в окне владельца 2194 с полосой».

СКЕПТИК: Проверил по числам линзы (p3-perekladina.json) и по доле покрытия в столбце. В шапке верх svg — 15 CSS px, в пикселях устройства это 18,75, 22,5 и 26,25, не на пикселе, а край чёткий. Значит, буквальное правило DESIGN.md:662-663 («положение знака ложится на пиксель устройства») ложно уже на шапке. Подвал 1440 при 1,75: столбец 71/234×6/180, то есть верхняя строка покрыта на 0,25, начало перекладины на дробной части 0,75. Это совпадает с 6589 × 1,75 = 11530,75, где 6589 — svg, округлённый Chromium до 6584, плюс 5. Без округления вышло бы 6589,1875 × 1,75 = 11531,08, покрытие 0,92, а в столбце не так. При 1,5: 126 → 0,5, 9883,5 — совпадает. При 1,25: 180 → 0,75, 8236,25 — совпадает. 2194: 6699 × 1,75 = 11723,25, столбец 180/…/71 — совпадает. То же по 390 (R3-PROZA-7): svg 8287 при 1,25 даёт 10358,75, не на пикселе, а край чёткий, потому что перекладина 8292 × 1,25 = 10365. Правило «верх перекладины = round(верх svg) + 5 на пикселе устройства» сходится со всеми замерами. Это остаток (а) скептика R3-PROZA-7 (SVODKA:733), в be41ad9 слово «знака» осталось. Предложение линзы верно.

######## R4-PROZA-3 [документ → подтверждено/документ]
Правка R3-PROZA-11 неполна: тот же коммит называет «растром окна» первый смысл, а «растром окна браузера» — второй

ДОКАЗАТЕЛЬСТВО: В раунде 3 второй смысл получил своё имя «растр окна браузера (headless: false)»: README:119, 129, wiernosc.mjs:85, materialy-priemki.mjs:33.
Но новый абзац того же коммита be41ad9 называет «растром окна» первый смысл — растр кадра окна без окна. README:354 «а не растром окна (первое окно полного кадра отличается от кадра окна…)», :356 «знак подвала в растре окна и в растре полного кадра один и тот же», :362 «при растре окна — отказ». Так же пишет шапка znak-podvala-okno.mjs:8-12, :41 («Признак растра окна») и сообщение отказа :64.
Сообщение be41ad9 одновременно утверждает «растр окна браузера отделён от оконного растра» и пишет «знак подвала в растре окна — закрыт замером».
Грепом «растр\S* окна» по README находятся строки 119, 354, 356, 362. Читатель строки 129 («Растра окна браузера судьи нет») через 225 строк видит «в растре окна … один и тот же» и «замером закрыто» — та самая путаница, которую закрывал R3-PROZA-11.

ГДЕ: sites/7thserpent.com/_baseline/README.md:353-362 против :119, :129; docs/reports/2026-09-24-7thserpent-znak/instrumenty/znak-podvala-okno.mjs:8-12, 41, 64

ПРЕДЛОЖЕНИЕ: В README:353-362 и в znak-podvala-okno.mjs писать термином README «оконный растр» (или «растр кадра окна»). «Растр окна браузера» оставить только за headless: false.

СКЕПТИК: Искал в README «растр\S* окна|оконн\S* растр». Первый смысл (полный кадр без окна снят растром кадра окна) везде назван «оконный растр»: строки 164–188, 239–323, 338–349, 376–397. Второй смысл назван «растр окна браузера (Chromium с окном, headless: false)», строка 119. Новый абзац be41ad9 на строках 354, 356 и 362 называет первый смысл «растр окна». Это отличается от имени второго смысла одним словом «браузера» и прямо соседствует со строкой 129 «Растра окна браузера судьи нет». znak-podvala-okno.mjs:7-10, :41, :64 пишет тем же словом: «признак растра окна», «снят растром окна». Сообщение be41ad9 одновременно говорит «растр окна браузера отделён от оконного растра» и «знак подвала в растре окна — закрыт замером». Скобка на :354 определяет смысл, так что внимательный читатель его восстановит. Но R3-PROZA-11 закрывался как раз разведением имён, и это разведение вновь нарушено. Предложение линзы — в первом смысле писать «оконный растр» — верно.

######## R4-PROZA-4 [документ → подтверждено/документ]
«Пути Playwright — одно место, все инструменты доклада берут пути оттуда» — неверно: materialy.mjs и render.mjs держат свои жёсткие пути

ДОКАЗАТЕЛЬСТВО: Grep «9833c18b2d85bc59|chromium-1234» по docs/reports/2026-09-24-7thserpent-znak/instrumenty находит, кроме sborka.mjs:26-27, ещё materialy.mjs:20, 62 и render.mjs:19, 37, 165. Оба лежат в instrumenty/, то есть это инструменты доклада, и оба ставят свой createRequire(...npx/9833c18b2d85bc59...) и executablePath .../chromium-1234/....
В R3-PROZA-10 эти файлы названы поимённо. Правка перевела на sborka.mjs только wiernosc.mjs (:103), а в прозе написала «все».
То же неверное утверждение стоит в шапке sborka.mjs:13-14 («Пути к Playwright и Chromium — здесь, в одном месте») и в сообщении 9de3a99 («пути Playwright — одно место»).

ГДЕ: docs/BACKLOG.md:2961-2967 (§60 п. 9); docs/reports/2026-09-24-7thserpent-znak/instrumenty/sborka.mjs:13-14

ПРЕДЛОЖЕНИЕ: Или перевести materialy.mjs и render.mjs на sborka.mjs, или написать: «у судей и материалов приёмки пути в sborka.mjs; у материалов эскизов (materialy.mjs, render.mjs) — свои жёсткие пути».

СКЕПТИК: Искал «9833c18b2d85bc59|chromium-1234» по instrumenty/. Нашлись sborka.mjs:26-27, materialy.mjs:20, 62 и render.mjs:19, 37, 165. У materialy.mjs и render.mjs свои createRequire(...npx/9833c18b2d85bc59...) и executablePath .../chromium-1234/..., sborka.mjs они не импортируют. sborka.mjs импортируют kontrast-znaka, materialy-priemki, vkladka-dowod, wiernosc (:103), zapas-menu и znak-podvala-okno. Оба файла лежат в instrumenty/ доклада. Это материалы вопроса владельцу об эскизах, по шапкам — «Материалы вопроса владельцу о знаке» и «Лист эскиза знака». Их поимённо называли R3-PROZA-10 и его скептик (SVODKA:760, :766). Поэтому «все инструменты доклада берут пути оттуда» (BACKLOG §60 п. 9, :2963-2965) и «здесь, в одном месте» (sborka.mjs:13-14) неверны. Вред «очистка кеша сломает» от этого не меняется, неверен только указатель, где чинить.

######## R4-PROZA-5 [документ → подтверждено/документ]
Шапка допуска наложения «1–6 субпикселей в 3 окнах из 23» не совпадает с выгрузкой того же судьи; допуск равен замеренному максимуму

ДОКАЗАТЕЛЬСТВО: wiernosc.mjs:153-154 (новые строки раунда 3): «до 3 уровней на краях, 1–6 субпикселей в 3 окнах из 23».
zamery/wiernosc.json снят той же версией (instrument_sha256 cced4a7c… = blob 9de3a99). В нём nakladka.max = 3 в 4 окнах: 1440 DPR 1,25 (подвал), 390 DPR 1,5 (при прокрутке), 1440 DPR 1 и 390 DPR 1 в принудительных цветах light (вверху).
Моя копия судья дословно, добавлена только гистограмма уровней (sud-r4/proza/sud-kopia.mjs, wyn/wiernosc-kopia.json, exit 0; максимумы во всех состояниях равны выгрузке). Субпикселей уровня 3 — 1, 2, 6 и 6, то есть в 4 окнах, а не в 3. Уровни 1–2 есть в 20 окнах из 23, до 3 924 субпикселей на состояние (1440 и 390 DPR 3, вверху). Без различий только 3 окна принудительных цветов dark.
Допуск TOL_NAKLADKI = 3 (:155) равен замеренному максимуму, запаса нет.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:153-155

ПРЕДЛОЖЕНИЕ: «Замерено: уровни 1–2 — в 20 окнах из 23, до 3 924 субпикселей на состояние; уровень 3 — 1–6 субпикселей в 4 окнах (1440 при 1,25 в подвале, 390 при 1,5 при прокрутке, принудительные цвета light при DPR 1). Запаса над замером нет: уровень 4 на нетронутой сборке даст отказ». Либо обосновать 3 расчётом округления альфы.

СКЕПТИК: Сверил выгрузку zamery/wiernosc.json. Её instrument_sha256 cced4a7c… равен sha256 wiernosc.mjs в 9de3a99. В ней nakladka.max = 3 в 4 окнах из 23: 1440 при 1,25 (подвал), 390 при 1,5 (при прокрутке), 1440 при DPR 1 и 390 при DPR 1 в принудительных цветах light (вверху). Максимум 1–2 — в 16 окнах, 0 — в 3 окнах принудительных цветов dark. Гистограмма копии линзы (wyn/wiernosc-kopia.json) сходится с выгрузкой по максимумам. Уровень 3 — 1, 2, 6 и 6 субпикселей. Уровни 1–2 — в 20 окнах, до 3 924 (уровень 1) + 7 (уровень 2) на состояние при DPR 3. Шапка :153-154 («1–6 субпикселей в 3 окнах из 23») расходится с выгрузкой той же версии: окон 4. TOL_NAKLADKI = 3 (:155) равен замеренному максимуму, и в шапке это не сказано. Отказ при уровне 4 уйдёт в безопасную сторону (ложный отказ), так что это вопрос прозы, а не пропуска. Мелкая поправка к предложению: на уровнях 1–2 до 3 931 субпикселя на состояние, а не 3 924.

######## R4-PROZA-6 [документ → подтверждено/документ]
README и DESIGN относят принудительные цвета ко всему ряду плотностей 1–3, а судья снимает их только при DPR 1, 2 и 3

ДОКАЗАТЕЛЬСТВО: README:123-127: «…на 1440 и 390 при DPR 1, 1,25, 1,5, 1,51, 1,75, 2 и 3, с полосой прокрутки … — вверху, … и в подвале; в принудительных цветах — только вверху и в подвале». DESIGN.md:687-690 пишет так же («при плотностях 1–3 … в принудительных цветах — вверху и в подвале»). Ограничение названо по состояниям, а не по плотностям.
В PELNY (wiernosc.mjs:193-194) и в zamery/wiernosc.json окон принудительных цветов шесть: 1440 при DPR 1 и 2 (light и dark), 390 при DPR 1 (light) и 3 (dark). Плотностей 1,25, 1,5, 1,51 и 1,75 (экран владельца) в принудительных цветах нет, окон с полосой прокрутки тоже.
В листе стоит отдельное правило @media (forced-colors:active) and (resolution<=1.5x) — обводка «TH» CanvasText. Его дробные растры не судятся. Принудительные цвета при 1,75 показывает только ekran-vladelca.png (1440, светлая схема). Шапка судьи в п. 6 честна: «DPR 1, 2, 3».

ГДЕ: sites/7thserpent.com/_baseline/README.md:123-127; sites/7thserpent.com/DESIGN.md:687-690

ПРЕДЛОЖЕНИЕ: «в принудительных цветах — только вверху и в подвале, 1440 при DPR 1 и 2 и 390 при 1 и 3 (светлая и тёмная схема); 1,25–1,75 и полоса прокрутки в принудительных цветах не судятся, 1,75 показан в ekran-vladelca.png».

СКЕПТИК: В PELNY (wiernosc.mjs:193-194) окон принудительных цветов шесть: 1440 при DPR 1 и 2 (light и dark), 390 при 1 (light) и при 3 (dark). В zamery/wiernosc.json те же шесть. Плотностей 1,25, 1,5, 1,51 и 1,75 и полосы прокрутки в принудительных цветах нет. README:123-127 и DESIGN.md:687-690 после перечня плотностей 1–3 и полосы при 1,75 ограничивают принудительные цвета только состояниями («вверху и в подвале»), а не плотностями. В собранном CSS есть отдельное правило @media (forced-colors:active) and (resolution<=1.5x){… .znak__obwodka{stroke:canvastext}}. Его ветку судит только DPR 1, дробные растры (1,25 и 1,5) не судятся. Шапка судьи в п. 6 честна («DPR 1, 2, 3»). Но предел в шапке инструмента не делает верным расширенный текст README и DESIGN, поэтому класс — документ. Предложение линзы верно.

######## R4-PROZA-7 [документ → подтверждено/документ]
DESIGN «ни одного внешнего запроса» шире судьи: отложенный запрос, запрос по наведению и по фокусу, WebSocket — «сверено»

ДОКАЗАТЕЛЬСТВО: Абзац DESIGN.md:685-694 переписан в be41ad9 и по-прежнему обещает «ни одного внешнего запроса». Предел R3-BRAUZER-8 шапка судьи назвала (wiernosc.mjs:92-93: около трёх секунд после загрузки, без действий пользователя, WebSocket не видны), а предложение сузить DESIGN.md не выполнено.
Моя копия судья дословно (sud-r4/proza/sud-kopia2.mjs: малый набор, пиксели 1440×1 с наведением и фокусом, прокрутка 1440 и 390, свои мутации; лог wyn2/sud-kopia2.log): 2/6. Чистая страница — «сверено». Контроль «запрос сразу» — отказ «запросы». «Запрос через 5 с», «запрос по mouseenter на ссылке знака», «запрос по focus на ссылке знака» и «WebSocket wss://example.invalid» — все «сверено».
По коду: окна пикселей наводят и ставят фокус, но список запросов не читают (oknoPikseli), а запросы судит только oknoPrzewin.

ГДЕ: sites/7thserpent.com/DESIGN.md:691-692

ПРЕДЛОЖЕНИЕ: «ни одного внешнего запроса за около трёх секунд после загрузки и прокрутки на 1440 и 390, без действий пользователя; запросы по наведению, фокусу и ящику и WebSocket не судятся».

СКЕПТИК: Проверил код. Запросы пишет otkryt() (:254) в каждом окне, но читает их только oknoPrzewin (:612-615). Там прокрутка шагом в полэкрана по 120 мс, networkidle и 300 мс, без наведения, фокуса и ящика. oknoPikseli (:427) наводит, ставит фокус и открывает ящик, а zaprosy не берёт. page.on('request') WebSocket не видит. Лог копии линзы (wyn2/sud-kopia2.log) это подтверждает. Копия дословная, изменены только набор окон и мутации (сверил diff). Контроль «запрос сразу» — отказ. Запрос через 5 с, по mouseenter, по focus и WebSocket — «сверено». Предел назван в шапке судьи (:92-93, R3-BRAUZER-8 — предел). Но DESIGN.md:692 после правки be41ad9 по-прежнему обещает «ни одного внешнего запроса» без оговорки, и именованный предел инструмента эту прозу верной не делает. Предложение сузить DESIGN верно.

######## R4-PROZA-8 [документ → подтверждено/документ]
tools/znak.mjs: «кроме Chromium 151 это не проверялось» — назначение ICO, 192 и 180 не проверено и в Chromium

ДОКАЗАТЕЛЬСТВО: znak.mjs:60-63 (правка R3-POLNOTA-6 в 1faf396): «Назначение ICO, PNG, 192 и 180 — клиентам, которые берут /favicon.ico сами, браузерам без SVG-иконок, ярлыку Android и домашнему экрану iOS…; кроме Chromium 151 это не проверялось». Сообщение 1faf396: «проверено только в Chromium 151».
По dowod/dowod.json, во всех 18 прогонах вкладка запросила только /favicon.svg. Исключение — вариант г (без ссылки на SVG, масштабы 1 и 2): там запрошен /favicon-32x32.png. /favicon.ico, /icon-192.png и /apple-touch-icon.png не запрошены ни разу. ICO в Chromium открывался только в <img> (materialy.json: naturalWidth 32).
Итого в Chromium 151 проверено одно назначение из четырёх: PNG 32 для вкладки без SVG. Base.astro:15-16 и DESIGN.md:711-712 сказаны точнее.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:60-63

ПРЕДЛОЖЕНИЕ: «В Chromium 151 проверено одно: без ссылки на SVG вкладка берёт favicon-32x32.png (vkladka-dowod.mjs, вариант г). Назначение ICO, 192 и 180 не проверялось нигде (бэклог 60 п. 4)».

СКЕПТИК: Разобрал dowod/dowod.json, все 18 прогонов. В вариантах 0, а, б и в при масштабах 1, 1,75 и 2 zapros_ikon = [/favicon.svg]. В варианте г (без ссылки на SVG, масштабы 1 и 2) — [/favicon-32x32.png]. /favicon.ico, /icon-192.png и /apple-touch-icon.png не запрошены ни разу. ICO Chromium открывал только в <img> (materialy-priemki, ikony.png). Ярлык Android и домашний экран iOS в Chromium для ПК не проверяются в принципе. Фраза znak.mjs:60-63 «кроме Chromium 151 это не проверялось» читается как «в Chromium 151 проверено», а там проверено одно назначение: PNG 32 для вкладки без SVG. Это формула предложения R3-POLNOTA-6, её подтвердил скептик раунда 3, и она оказалась шире фактов. Base.astro и DESIGN.md:711-712 сказаны точнее. Предложение линзы верно.

######## R4-PROZA-9 [документ → подтверждено/документ]
DESIGN «Фавикон» описывает вкладку при масштабе 1 и 2 и молчит о 1,75 — масштабе владельца, где ореолы тоже есть

ДОКАЗАТЕЛЬСТВО: DESIGN.md:706-708 (be41ad9): «при масштабе 1 … тёмные ореолы …; при 2 — почти то же, что в <img>». Масштаб 1,75 в раунде 3 снят (R3-MATERIALY-4): vkladka.json para 0-1.75 — доля 0,122, макс 55; для сравнения 1 — 0,188, 2 — 0,004.
Мой счёт по рамке из dowod.json (96, 21, 28×28; sud-r4/proza/p2-oreoly.txt), в обеих темах одинаково. При 1,75: 48 из 784 пикселей с суммой каналов меньше 32, 108 не светлее плитки, самый тёмный rgb(0 0 0). При 1: 33 из 256. При 2: 0 из 1024. Ореолы видны и на vkladka-para.png, во втором ряду.
Варианты б, в и г (правка PNG и ICO, обратный порядок, без SVG) сняты только при 1 и 2 в светлой теме (dowod.json; vkladka-dowod.mjs:202, 207). При 1,75 проверен только вариант а.
Шапка vkladka-dowod.mjs:25 пишет «клетка 128 px»; при 1,75 клетка 28 × 4 = 112 (код :247 — «до 128»).

ГДЕ: sites/7thserpent.com/DESIGN.md:703-710; docs/reports/2026-09-24-7thserpent-znak/instrumenty/vkladka-dowod.mjs:25-27

ПРЕДЛОЖЕНИЕ: «при масштабе 1 и 1,75 (экран владельца) вкладка пересэмплирует иконку, и у неё тёмные ореолы (доли 0,188 и 0,122); при 2 — почти как в <img> (0,004). Варианты б–г сняты только при 1 и 2». В шапке инструмента — «клетка до 128 px».

СКЕПТИК: Пересчитал сам по рамке dowod.json (1.75: 96, 21, 28×28). В vkladka-1_75x-svetlaya.png и -temnaya.png по 48 из 784 пикселей темнее плитки (сумма каналов меньше 32 при плитке 38), минимальная сумма 0. При 1 — 33 из 256, при 2 — 0 из 1024. В vkladka.json para 0-1.75: доля 0,122, макс 55; при 1 — 0,188 и 68; при 2 — 0,004 и 50. DESIGN.md:706-708 описывает только масштабы 1 и 2, а масштаб владельца 1,75 с теми же ореолами пропущен. В vkladka-dowod.mjs варианты б, в и г идут по [1, 2] (:105, :207). DESIGN.md:703-706 выдаёт их выводы без масштаба. Шапка :25 пишет «клетка 128 px», а код :249 даёт Math.floor(128/28) = 4, то есть 112 px при 1,75. Предложение линзы верно.

######## R4-PROZA-10 [документ → подтверждено/документ]
Сравнение 252 523 / 20 901 с генератором снято в разных условиях, и оно показывает, что зона 9 «зерно» при 1440 не от зерна

ДОКАЗАТЕЛЬСТВО: README:353-356 (раунд 3): «252 523 … на 1440 и 20 901 на 390 — та же природа, что у генератора, 252 321 и 21 011». Числа инструмента сняты без зерна (display: none, znak-podvala-okno.mjs:36), числа генератора — с зерном.
Мой прогон порядком инструмента (sud-r4/proza/p6-pervoe-okno.txt, свой сервер :4953). С зерном: 1440 — 252 321 = 190 777 в рамке .hero__art + 61 544 вне арта, ровно числа генератора. 390 — 21 002 = 20 917 + 85. Без зерна: 1440 — 252 523 = 191 035 + 61 488; 390 — 20 901, все в арте, вне арта 0.
Значит, на 1440 из 61 544 различий вне арта 61 488 остаются и без зерна. Зона 9 названа «9-зерно-первое-окно-1440-полный», таблица замера 3 пишет «первое окно вне арта (зерно, 1440×900)» (README:262, 282), но на 1440 её различия дают не зерно. На 390 зона 2 (85) — действительно зерно.
Сравнение раунда 3 вывод «та же природа» не опровергает. Но условия разные, и атрибуцию зоны 9 оно опровергает.

ГДЕ: sites/7thserpent.com/_baseline/README.md:353-356; :262, :282 (атрибуция зоны 9); szum.json, зона 9

ПРЕДЛОЖЕНИЕ: В README:353-356 указать условия: «без зерна; с зерном тот же порядок даёт ровно 252 321 = 190 777 + 61 544». В описании зоны 9 написать: «первое окно вне арта: зерно ни при чём — без зерна 61 488 из 61 544 остаются; источник не установлен (скрим под color-mix?)». Рамку зоны не трогать — пересъёмка и зоны решает владелец.

СКЕПТИК: Воспроизвёл сам: skeptik-proza/s10-pervoe-okno.mjs, свой сервер :4962, порядок znak-podvala-okno.mjs, вывод в s10-pervoe-okno.json. 1440 с зерном: 252 321 = 190 777 в .hero__art + 61 544 вне арта, макс 2, рамка вне арта 1392×643 от (0, 255). Это ровно числа и рамка зоны 9 из замеров 3 и 4. Без зерна: 252 523 = 191 035 + 61 488, рамка вне арта 460×623 от (0, 255), то есть левая колонка. У множеств вне арта 61 486 общих позиций субпикселей. Значит, зерну обязаны около 58 субпикселей зоны 9, всё прочее — не зерно. У 390 зона 2 и правда зерно: по линзе 85 с зерном и 0 без. README:353-356 сравнивает числа без зерна с числами генератора, снятыми с зерном, и условия не называет. Вывод «та же природа» от этого не рушится. Но атрибуция «зерно» в имени и описании зоны 9 и в таблицах README:262, :282 неверна. Оговорка: имя и «что» зоны 9 лежат в _baseline/szum.json (инвариант 6). Рамку не трогать, а правку имени и «что» в szum.json лучше вынести владельцу. Прозу README править можно, как в прошлых раундах.

######## R4-PROZA-11 [документ → подтверждено/документ]
CLAUDE.md сайта:167 — половина R3-PROZA-3 не сделана: судья не назван ручным, CanvasText — без «до решения владельца»

ДОКАЗАТЕЛЬСТВО: Предложение R3-PROZA-3 (sud-r3/SVODKA.md:687): «То же одной вставкой — в строку CLAUDE.md». be41ad9 правит в CLAUDE.md только строки 166 и 329. В строке 167 по-прежнему «собранную страницу в браузере судит … wiernosc.mjs — знак … = эскиз A по пикселям, ссылки иконок в <head>, имена …, внешние запросы, сервер = dist/»: нет «запускается руками, в сборку, гейты и accept не входит».
Та же строка пишет «принудительные цвета — CanvasText» как правило, а :329 той же правкой — «отступление от «цвета из токенов», до решения владельца».
Новых пунктов суда (перекрытие, смешивание, движение, полоса прокрутки) в строке тоже нет.

ГДЕ: sites/7thserpent.com/CLAUDE.md:167

ПРЕДЛОЖЕНИЕ: Вставить: «…судит wiernosc.mjs — ручной судья вне сборки, гейтов и accept (бэклог 60 п. 3): …; принудительные цвета — CanvasText (до решения владельца)».

СКЕПТИК: git show be41ad9 -- sites/7thserpent.com/CLAUDE.md правит только строку _baseline/ (волос подвала) и строку П83 в §6 (CanvasText — до решения владельца). Строка 167 не тронута. В ней «собранную страницу в браузере судит … wiernosc.mjs — …», и нет слов, что судья ручной и в сборку, гейты и accept не входит. Вторая половина предложения R3-PROZA-3 (SVODKA:687, «то же одной вставкой — в строку CLAUDE.md») не выполнена, хотя сообщение be41ad9 заявляет «назван ручным судьёй», имея в виду только README. Часть про CanvasText слабее: :167 описывает, что делает компонент, а статус «до решения владельца» стоит в том же файле на :329. Это неполнота, а не противоречие, но вставка уместна. Нехватка новых пунктов суда — вопрос полноты перечня, не ошибка.

######## R4-PROZA-12 [документ → подтверждено/документ]
Бэклог 60 п. 10: имя сайта есть не «только в aria-label ссылки знака»; список размеров, не кратных 48, неполон

ДОКАЗАТЕЛЬСТВО: В dist/index.html поиск «7th serpent» без учёта регистра находит две записи. Первая — aria-label ссылки шапки «7th Serpent — 7thserpent.com, home». Вторая — aria-label svg подвала с role="img": «7th Serpent — 7thserpent.com».
Судья сам требует ровно два таких узла (wiernosc.mjs:537, 550-551). <title> и og:title имени сайта не содержат, og:site_name и ld+json нет — эта часть пункта верна.
Список «не кратны 48» называет favicon-32x32.png и favicon.ico. Он пропускает favicon-16x16.png (rel="icon", Base.astro:51) и apple-touch-icon.png (180, Base.astro:53).

ГДЕ: docs/BACKLOG.md:2973-2977 (§60 п. 10)

ПРЕДЛОЖЕНИЕ: «Имя сайта сейчас — только в двух aria-label: у ссылки знака и у знака подвала (role=img)»; «не кратны 48 — favicon-16x16.png, favicon-32x32.png, favicon.ico и apple-touch-icon.png (180)».

СКЕПТИК: Проверил по dist/index.html. Поиск «7th serpent» без учёта регистра даёт два aria-label: у a.hdr__brand («7th Serpent — 7thserpent.com, home») и у svg.znak подвала с role="img" («7th Serpent — 7thserpent.com»). Судья сам требует ровно эти два узла (wiernosc.mjs:550-551). В <title> и og:title имени сайта нет, og:site_name и ld+json нет (0 совпадений). Эта часть п. 10 верна, неверно слово «только в aria-label ссылки знака». В <head> шесть ссылок, среди них rel="icon" favicon-16x16.png (16) и apple-touch-icon.png (180). Google принимает и apple-touch-icon как источник фавикона, и ни 16, ни 180 не кратны 48. Список «не кратны 48» в п. 10 неполон. Предложение линзы верно.

######## R4-PROZA-13 [документ → подтверждено/документ]
Бэклог 60 п. 11: вред «на год» подан без оговорки образца «сегодня мёртво — статику отдаёт nginx»; .webmanifest у второго сайта нет

ДОКАЗАТЕЛЬСТВО: sites/ac4bf-thewatch.com/public/.htaccess:87, прямо над цитируемыми :88-89: «Dziś martwe (statykę oddaje nginx) — zapis zamiaru». Сами строки :88-89 процитированы верно.
Значит, перенос образца как есть на этом хостинге кеш не задаст. Задержку дадут настройки nginx, если повторят образец. Критик R3-POLNOTA-11 это называл, строка бэклога — нет.
Фраза «MIME для .ico и .webmanifest там не заданы» верна (AddType :116-120), но .webmanifest у второго сайта нет: в dist/ его нет, в <head> 6 ссылок-иконок без manifest (LINKI wiernosc.mjs:145-152).

ГДЕ: docs/BACKLOG.md:2978-2984 (§60 п. 11)

ПРЕДЛОЖЕНИЕ: Дописать: «в образце блок кеша сегодня мёртв (статику отдаёт nginx, .htaccess:87); риск — в настройке nginx при публикации». .webmanifest убрать или пометить «на вырост».

СКЕПТИК: sites/ac4bf-thewatch.com/public/.htaccess:87, прямо над цитируемыми :88-89: «Dziś martwe (statykę oddaje nginx) — zapis zamiaru». Шапка файла :9-16: на этом хостинге до Apache доходят только запросы HTML, а «długi cache statyki ustawia się w panelu hostingu». Иконки в корне — тоже статика nginx. Значит, на этом хостинге перенос блока как есть кеш не задаст, и риск «на год» сидит в настройке панели или nginx. BACKLOG §60 п. 11 это пропускает, хотя критик R3-POLNOTA-11 называл nginx. Оговорка: хостер второго сайта не выбран (CLAUDE.md сайта, «публикация — не начата»). На сервере, где .htaccess отдаёт статику, вред из бэклога был бы верен. Поэтому это неполнота, а не ложь. .webmanifest у второго сайта нет ни в dist/, ни в public/, ни в <head>. Его упоминание верно по образцу (AddType :116-120), но к делу не относится.

######## R4-PROZA-14 [документ → подтверждено/документ]
«ekran-vladelca.png» снят без окна, а README ставит его рядом с «растр окна браузера … показан»; при 1,75 окно владельца рисует знак иначе

ДОКАЗАТЕЛЬСТВО: Подпись самого листа: «снимки Chromium без окна». Шапка materialy-priemki.mjs:24-28 этого не говорит («ekran-vladelca.png — экран владельца, DPR 1,75 …»). README:129-131: «Растра окна браузера судьи нет — он показан числом и глазами в материалах приёмки (okno-plotnosti.png; там же znak-plotnosti.png, ekran-vladelca.png, prinuditelnye-cveta.png)».
При 1,75 растр настоящего окна отличается от снимка без окна в знаке шапки: 840 из 20 736 пикселей, макс 110, после выравнивания. В подвале — 0 (materialy.json, okno["1.75 шапка"].tolko_znak.posle_vyravnivaniya). Лист «экран владельца» показывает шапку не так, как её рисует Chrome владельца. Это видно только на okno-plotnosti.png.

ГДЕ: sites/7thserpent.com/_baseline/README.md:129-131; docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:24-28

ПРЕДЛОЖЕНИЕ: В шапке и в README: «ekran-vladelca.png — плотность и ширина экрана владельца, растр без окна. Растр окна при 1,75 — только okno-plotnosti.png (знак шапки: 840 пикселей из 20 736)».

СКЕПТИК: materialy.json okno, «только знак» после выравнивания: при 1,75 шапка 840 из 20 736 (макс 110, dy 1), подвал 0. При 1,25 шапка 638, подвал 810; при 1,5 шапка 1 006, подвал 0. Подпись самого листа честна: materialy-priemki.mjs:222 — «снимки Chromium без окна». Шапка того же инструмента (:24-28) про ekran-vladelca.png этого не говорит. README:129-131 ставит ekran-vladelca.png через «там же» в скобку к «растр окна браузера … показан числом и глазами», и это читается как «лист показывает то, что рисует Chrome владельца». В шапке при 1,75 это не так. Дефект — только в группировке README и в шапке инструмента, сам лист верен. Предложение линзы верно.

######## ЧИСТО (proza)
- README:117 «знак на 1440 сдвигается с x 132 на 127»: полоса 10 CSS px, x 127 у шапки и подвала при DPR 1, 1,25, 1,5, 1,75 и 2 (sud-r4/proza/p5-pasek.txt); в wiernosc.json clip 125 (1440 с полосой), 502 и 507 (2194 с полосой и без)
- wiernosc.json: 23 окна = 14 + 3 (1440 и 2194 с полосой, 2194 без полосы) + 6 с принудительными цветами; в окнах с полосой все пять состояний, в принудительных цветах — только «вверху» и «подвал»; bledy 0. Моя дословная копия с гистограммой — exit 0, максимумы всех состояний равны выгрузке
- Временная папка сессии 10: серия s10-fix/pw — 16 папок, seria-4981.json на 16 записей, точка (1077, 4048) = rgb(54 59 63) у 16 из 16; линза s10-etalon (shots, w151-1…3, w153-1…12) — 16 из 16 rgb(54 59 63). Лишняя папка old9 с rgb(50 55 59) прогоном линзы не является (sud-r4/proza/p1-schet.txt)
- proverka-r2: proba-4972.json — 20 записей, proba-4993.json — 24 (итого 44); proba-4971.json (1 запись) лежит только во временной папке r2-kandydat/pw — как в README:319-321
- zamery/znak-podvala-okno.json: 252 523 и 20 901, polny_v_rastre_okna false, знак подвала 0/0; при растре окна код отказывает (znak-podvala-okno.mjs:64)
- CLAUDE.md:166: волос подвала — строки 6486–6487 и 8189–8190 (rgb 36 39 44 и 40 44 49), фон с 6488 и 8191; зона 16 кончается на 6487, зона 5 — на 8190; зоны стоят только на трёх кадрах, у кадров первого экрана зон нет
- DESIGN «Размер»: zapas-menu.json — 861 ширина, 279,25 на 961, крупный шрифт 181,31 и 83,39, znak_ne_138x38_na пуст, selftest 5/5. Эмуляция шрифта через :root честна: у html своего размера нет, меню переключается на 961px в px, core .container вне слоя перекрывает брейкпойнты Tailwind в rem
- DESIGN: условие владельца «цвета из токенов темы» дословно есть в DECISIONS.md:5834 и :5860
- DESIGN: подвал 1440 в полутон при 1,25, 1,5 и 1,75 (по 2 строки); на 2194 с полосой (окно владельца) подвал на 6694,19 — тоже полутон, шапка — 0 строк (sud-r4/proza/p3-perekladina.txt)
- DESIGN, сетка иконки 32 (znak.json ikona): перекладина 6–21,3 × 8–12, ромб 20,6 24,10 20,14 16,10 (центр (20; 10), полудиагональ 4), выступ 24 − 21,3 = 2,7; у шапки 20,7 − 18,5 = 2,2
- Base.astro:13-14: favicon.ico — две записи, 16 и 32, обе PNG (89504e47), 32 — наибольшая
- Бэклог 60 п. 11: цитата .htaccess:88-89 верна, AddType :116-120 без ico и webmanifest; п. 10: icon-192 — rel=icon sizes 192x192 (Base.astro:52), og:site_name и ld+json нет, robots.txt в dist нет; п. 3 существует и называется так, как на него ссылаются
- sborka.mjs: 82 файла = все ресурсы, на которые ссылается страница; в dist 84 — не упомянуты только sitemap-0.xml и sitemap-index.xml; 82 из 82 равны dist в kontrast-znaka, materialy, zapas-menu и znak-podvala-okno
- Происхождение закрыто (R3-PROZA-9): instrument_sha256 всех выгрузок равен блобам 9de3a99 — wiernosc cced4a7c…, kontrast 88d81eac…, zapas e5255d8f…, podval-okno 19e5f66c…, materialy bc58fa45…, vkladka-dowod d61c7096…, sborka 826bae3c…
- dist после пересборки в 05:21 побайтно прежний: index.html 13f4aaae…, index.CmgQJVSa.css a0674b3b…
- tools/znak.mjs --selftest — 68/68, exit 0; --check --dist — exit 0, «сверено»; дерево после прогонов чистое
- kontrast-znaka.json: граница 7,05 / 11,90 / 8,43 во всех четырёх окнах, кольцо rgb(242,189,110) = --accent-text #f2bd6e, полоса 69; selftest 5/5
- dowod.json и vkladka.json: вариант а — зелёного 1,0 при 1, 1,75 и 2 в обеих темах; в — 1,0; б — красного 0, против варианта 0 доля 0 и макс 0; г — красного 1,0, запрос /favicon-32x32.png; пары 0,188/68, 0,122/55, 0,004/50
- materialy.json: окно при 1,5 и 1,75 в подвале после выравнивания 0, в шапке при 1,75 — 840 (как в 9de3a99); экран владельца — знак x 504, полоса 10
- README: «бэклог 60 п. 3», ekran-vladelca.png и fokus-1440.png существуют; П83 п. 4 пересказан верно

######## КРИТИК
R4-POLNOTA-1 [итог самопроверки wiernosc раунда 3 не записан] Файл zamery/wiernosc-selftest.json — прогон 89296df: в шапке kommit 89296df, 23 пробы, 3 окна. Нынешняя самопроверка — 32 мутации и 2 подмены на 7 окнах набора KROTKI — не выгружалась ни разу. Коммит 9de3a99 обещал «итог — отдельным коммитом». Но, по словам 9b896e5, самопроверка той редакции обрывалась на 16-й пробе (ReferenceError), а 9b896e5 содержит только починку, итога в нём нет. Линзы раунда 4 гоняли копии судьи, а не HEAD. Как закрыть: на 9b896e5 запустить `node wiernosc.mjs --selftest`, закоммитить zamery/wiernosc-selftest.json с instrument_sha256 3df1c9e3… отдельным коммитом, число проб внести в доклад.
R4-POLNOTA-2 [сравнение (б) «наложение» не изолировано ни одной мутацией; критерий самопроверки нестрогий] Функция trafil (wiernosc.mjs:730) засчитывает пробу по любой строке любого ожидаемого вида. Каждая мутация на перекрытие даёт ещё и «перекрыт» — точки elementFromPoint (:407-418). Мутация «смешивание» даёт ещё и «смешивание» из sadStylu (:307). По коду мутант с выключенным (б) (N.n всегда 0) проходит самопроверку. Это класс R3-SVERKA-4 и R4-SVERKA-4, но у wiernosc его не назвала ни одна линза. Тот же нестрогий критерий `some` — у kontrast-znaka.mjs:167 и zapas-menu.mjs:123. Как закрыть: строгий критерий, как у znak.mjs (каждая строка отказа — ожидаемого вида), и мутация, которую видит только (б): непрозрачный блок поверх знака с pointer-events: none. Точки проходят сквозь него, (а) его прячет, поэтому ждём только «наложение».
R4-POLNOTA-3 [ветви суда иконок в wiernosc без мутаций] Пункт 10 и проверка «иконки dist/ = public/» (ikony(), :646-681) в самопроверке идут только на чистом сервере 4331: PODMENY вызывают лишь oknoPrzewin (:746). Отказы «favicon.svg ≠ эскиз», «PNG ≠ эскиз по пикселям», «записи ICO не 16 и 32» и «dist/ ≠ public/» не проверяла ни одна проба ни в одном раунде. Как закрыть: подменить на своём сервере /favicon-32x32.png (чужой пиксель), /favicon.ico (одна запись) и /favicon.svg и вызывать ikony() с адресом подмены.
R4-POLNOTA-4 [браузерный масштаб и плотности телефонов не мерил никто] Судьи и листы берут 7 плотностей. DESIGN «Чёткость перекладины» (:661-666) и «плотности 1–3» (:688) о других молчат. Мой замер без окна (scratchpad r4k/faza-y.mjs и faza-y2.mjs, прод-превью 4331, столбец x 9 знака шапки):
  - перекладина шапки в полутоне при 0,67, 0,8, 0,9, 1,1 (масштаб 110 %), 1,33, 1,925 (175 % владельца × 110 %), 2,1875 и 2,625 (Android, 412 px). Например, при 1,1 нижний край на 26,4 и пиксель 97,101,105; при 2,625 верхний край на 52,5 и пиксель 119,124,128;
  - перекладина чёткая при 1,25, 1,5, 1,75, 2, 2,25, 2,5, 2,75, 3 и 3,5.
  Чёткость держится на целых 20·DPR и 24·DPR (перекладина лежит на y 20–24 документа), а не на «высотах 68 и 38». Обводка «TH» (плотность до 1,5) при DPR меньше 1 тоже не снята. Как закрыть: плотности 0,9, 1,1, 1,925 и 2,625 добавить в wiernosc и в znak-plotnosti.png, либо назвать пределом в DESIGN и README эталона. R4-BRAUZER-7 — сторона судьи того же пробела.
R4-POLNOTA-5 [финальные проверки П83 п. 5 на HEAD не записаны]
  - Маркер @source записан только словами «после отката маркеров» (be41ad9): какой маркер, в каких файлах src и ядра и нашёлся ли он в CSS — нигде.
  - Дифф утилит против main числом не записан ни разу; «утилит не прибавилось» (c1ec5e6, 24d629a) — без базы сравнения.
  - Детектор impeccable в последний раз записан в 24d629a, и только по Base.astro.
  - astro check — в c1ec5e6.
  - accept против эталона 846a73d на финале не записан.
  Как закрыть: таблица приёмки в докладе с числами (ниже, в СВЕРКЕ); маркер повторить с записью; astro check и accept прогнать на финальном коммите.
R4-POLNOTA-6 [ветви znak-podvala-okno.mjs и sborka.mjs без мутаций, остаток R3-POLNOTA-2] Признак растра (R3-BRAUZER-13) раунд 4 проверил только по предпосылке (brauzer/podval-lokalno.mjs). Отказы «различия в знаке подвала» и «полный кадр снят растром окна» не вызывались ни разу. У sborka.mjs нет --selftest: ветви «ответ не 200», «нет листа стилей» и «url() листа ≠ dist» трогал только стенд lipa раунда 4. Как закрыть: --selftest с подменой своего сервера — лист со статусом 203 и тем же телом (судится только статус), знак подвала, сдвинутый на 1 px только в кадре окна.
R4-POLNOTA-7 [сводки раундов вне git] Файлы sud-r1…r4/SVODKA.md и стенды лежат только в .playwright-mcp/ (.gitignore:15). На номера R* ссылаются README эталона (12 раз), BACKLOG (16 раз), шапки инструментов и сообщения коммитов, а пути к ним у владельца нет (правило памяти «отправлено — только с путём»; прошлые сессии поступали так же). Как закрыть: сводки положить в docs/reports/2026-09-24-7thserpent-znak/sud/ вместе с докладом, про стенды — строка «вне git».
R4-POLNOTA-8 [сходимости нет — политический вопрос для доклада] Класс «проба засчитана по чужой причине» повторяется каждый раунд: R2-SVERKA-6 → R3-SVERKA-4 → R4-SVERKA-4, причём мутант MI, убитый в раунде 3, снова жив. «Сборка = dist/»: R3-BRAUZER-7 → R4-BRAUZER-5 и -6. Правки по раунду 4 снова тронут znak.mjs, wiernosc.mjs, kontrast-znaka.mjs, zapas-menu.mjs, sborka.mjs, materialy-priemki.mjs и vkladka-dowod.mjs, так что по инварианту 1 нужен раунд 5. Как закрыть: вынести владельцу три варианта:
  - (а) раунд 5 только по правкам R4, затем стоп;
  - (б) заморозить судей и назвать дефекты R4 пределами в шапках;
  - (в) часть судей включить в сборку (бэклог 60 п. 3).
  Рекомендация — (а), с правилом «каждая правка судьи — своя мутация, критерий строгий».
R4-POLNOTA-9 [заголовок бэклога 60 отстал] Заголовок §60 (BACKLOG.md:2900) не называет п. 10 (фавикон в выдаче) и п. 11 (кеш иконок при публикации). Как закрыть: дописать обе темы в заголовок.
R3-POLNOTA-1: закрыт по существу.
  - В kontrast-znaka.mjs появились отказ «замер светлее границы» (:134), кольцо фокуса по вычисленному outline-color в :focus-visible против --accent-text (:88-92), проверка листа между шапкой и содержимым через elementsFromPoint (:112-119), полоса по высоте шапки (:82).
  - --selftest 5/5 (тёмный фонарь, прозрачная шапка, лист над шапкой, кольцо не того цвета) выгружен в zamery/kontrast-znaka-selftest.json.
  - Линза раунда 4 по инструменту была (kontrast4.mjs, у скептика kontrast-sk4.mjs).
  - Плотности по-прежнему 1 и 2, это названо в шапке (:28).
  Остатки: R4-BRAUZER-10, -11 и R4-POLNOTA-2.
R3-POLNOTA-2: не закрыт.
  - --selftest есть у kontrast-znaka (5/5) и zapas-menu (5/5).
  - У wiernosc в коде есть мутации «плотность от 2» и «принудительные цвета, тёмная схема», а KROTKI содержит окна 1440×2 и 390×1 тёмной схемы. Но итог самопроверки не выгружен (R4-POLNOTA-1).
  - У znak-podvala-okno.mjs, sborka.mjs, materialy-priemki.mjs (проверки состояний — R4-MATERIALY-2) и vkladka-dowod.mjs самопроверки нет (R4-POLNOTA-6).
R3-POLNOTA-3: закрыт частично.
  - У wiernosc есть окна 1440 и 2194 с полосой при 1,75 и 2194 без полосы (:192).
  - ekran-vladelca.png снят на 2194 с полосой; остаток — R4-MATERIALY-6.
  - README эталона :117 называет полосу, которую эталон не видит.
  - znak-plotnosti.png и podval-plotnosti.png по-прежнему без полосы, и это нигде не названо.
  - Горизонтальная фаза, по моему замеру, не пробел: Chromium 151 без окна ставит x знака на пиксель устройства. Левый край перекладины чёткий при 1441 и 1443 (DPR 1, x 132,5 и 133,5), 1441 (DPR 2), 414 (DPR 2), 768 (DPR 1 и 2) и 430 (DPR 3) — scratchpad r4k/faza-x.mjs. Это нужно записать в DESIGN.
  - Вертикальная фаза открыта: R4-PROZA-2 и R4-POLNOTA-4.
R3-POLNOTA-4: закрыт пределом: ПРЕДЕЛЫ wiernosc.mjs:81-82, README эталона :126-127 и DESIGN :689-690 — «в принудительных цветах только вверху и в подвале». Остаток — R4-PROZA-6.
R3-POLNOTA-5: закрыт частично.
  - Оговорка «до решения владельца» есть в DESIGN :636-638 и в CLAUDE.md сайта :329.
  - В CLAUDE.md сайта :167 её нет (R4-PROZA-11).
  - Вопрос с вариантами и листом prinuditelnye-cveta.png не вынесен: доклада нет.
R3-POLNOTA-6: закрыт: tools/znak.mjs:57-64 — «кроме Chromium 151 это не проверялось (бэклог 60 п. 4)», фраза «Windows Vista+ и все браузеры» из кода убрана. Остаток — R4-PROZA-8.
R3-POLNOTA-7: закрыт частично: строка заведена — бэклог 60 п. 10 (BACKLOG.md:2971). og:site_name и видимое имя в политические вопросы не вынесены: доклада нет. Остаток — R4-PROZA-12.
R3-POLNOTA-8: не закрыт. В DECISIONS.md после 13b13a4 нет ни одной записи (на ветке только a99fee5 и 13b13a4). Ведущий делает это с докладом.
R3-POLNOTA-9: закрыт.
  - В zamery/zapas-menu.json, раздел krupny_shrift: запас на 961 — 181,31 px при 20 px и 83 px при 24 px, переносов нет; в DESIGN — :623-624.
  - Эмуляция `:root { font-size: 125 % и 150 % }` равна настройке кегля в Chrome: корневой кегль не задают ни сайт, ни ядро, а точки перелома шапки заданы в px (core/chrome/SiteHeader.astro:373, 382, 395).
  Остаток — R4-BRAUZER-8.
R3-POLNOTA-10: закрыт по факту: раунд 4 прошёл всеми линзами, со скептиками. План раунда в доклад не записан, потому что доклада нет: R2-POLNOTA-17 открыт, BACKLOG.md:2904 и CLAUDE.md сайта:29 ведут на несуществующий docs/reports/2026-09-24-7thserpent-znak.md. Правки по R4 требуют раунда 5 (R4-POLNOTA-8).
R3-POLNOTA-11: закрыт: бэклог 60 п. 11 (BACKLOG.md:2978). Остаток — R4-PROZA-13.
СВЕРКА: ядро и первый сайт — ни байта. `git diff fef35e9..9b896e5` по core/, sites/ac4bf-thewatch.com/, package.json, package-lock.json и .gitattributes пуст. Вне sites/7thserpent.com и папки доклада изменены только DECISIONS.md и docs/BACKLOG.md.
СВЕРКА: окончания строк и ветка. У 53 текстовых файлов ветки i/lf = w/lf, 62 двоичных помечены -text, CRLF нет. Корневого dist/ нет. HEAD = origin/7thserpent-znak = 9b896e5, дерево чистое.
СВЕРКА: утилиты против main. Прогнал Tailwind 4 (compile → Scanner → build) по src из main и из HEAD; global.css и ядро в обоих одни и те же.
  - Утилит 14, текст слоя utilities побайтно равен: relative, container, block, flex, hidden, transform, resize, flex-wrap, border, text-wrap, outline, filter, backdrop-filter, transition.
  - Кандидатов стало 1106 вместо 1075, новых утилит нет; в CSS dist/ те же 14.
  - С классами index.html совпадает только .container. Это известное совпадение с неслоёным .container ядра (core/styles/layout.css:5-7), оно было и на main.
СВЕРКА: @source без маркера. Сканер видит 23 файла src/ (в том числе Znak.astro, znak.json и Base.astro) и 18 файлов .astro ядра (в том числе chrome/SiteHeader.astro), других файлов нет — оба пути живы. Итог маркера самого ведущего нигде не записан.
СВЕРКА: гейты и сверка знака. Гейты 4/4, exit 0. `znak --check --dist` — «сверено», exit 0; `--selftest` — 68/68.
СВЕРКА: детектор impeccable. По src сайта и файлам знака — 0 находок. По dist/index.html детектор работает в режиме DEGRADED: нет htmlparser2, css-select, css-tree и domutils. Ноль там — недосчёт, а не чистый лист (BACKLOG.md:2802).
СВЕРКА: сборка и внешние адреса. index.html (sha256 13f4aaae…) и CSS (sha256 a0674b3b…) равны снимку 24d629a. Внешний адрес в HTML один — rel=canonical https://www.7thserpent.com/, и это не запрос. url() и @import в CSS нет.
СВЕРКА: происхождение выгрузок.
  - instrument_sha256 в выгрузках kontrast-znaka, zapas-menu, znak-podvala-okno, materialy, vkladka и dowod равен нынешним файлам инструментов.
  - wiernosc.json снят судьёй редакции 9de3a99 (cced4a7c…); нынешняя редакция 3df1c9e3… отличается только самопроверкой (правка 9b896e5).
  - Все выгрузки сняты при HEAD 4c11041 или 1faf396 с незакоммиченными правками. Какой код их снял, устанавливает sha256, как и задумано.
  - Листы материалов совпадают с sha256 в materialy.json и vkladka.json.
СВЕРКА: эскиз. Одобренный эскиз A не менялся с c680edd, public/favicon.svg побайтно равен его favicon.svg.
Файлы: C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/896b5a82-d44a-477b-9484-b96a7c2fb756/scratchpad/r4k/utility.mjs, zrodla.mjs, faza-x.mjs, faza-y.mjs, faza-y2.mjs (выгрузки src из main и HEAD лежат рядом, в main/ и head/); D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/zamery/wiernosc-selftest.json (устаревшая выгрузка); D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs.
