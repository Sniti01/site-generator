# 2026-09-18 · Сессия 3 трека «второй сайт» — бриф `7thserpent.com` и каркас `sites/7thserpent.com/`; вливание `techdolg-kadry` перемоткой; семантика — второй формат читалки в сессии 4

Ветка `7thserpent-brief`, заведена от `main` = `0b90bd4` после перемотки
`techdolg-kadry` по слову владельца (П62 п. 2); дерево на старте чистое,
один untracked — выгрузка семантики в корне. Слова владельца — семь полей
брифа и файл (П62, дословно) и четыре ответа старта: вливание — сессией
перемоткой; новый сайт — LF; канонический адрес — `https://www.7thserpent.com`;
семантика — файл окончательный, читалка ядра учится второму формату
в сессии 4. Уточнение при утверждении плана: «Кластеры в семантике есть,
имя кластера в колонке "Название группы"». **Объём, зафиксированный
на старте:** строка 3 таблицы §6 плана трека (`2026-09-15-plan-vtoroj-sajt.md`)
— бриф в `sites/<домен>/brief/site.yaml`, каркас `sites/<домен>/`
(воркспейс, `astro.config`, гейты, `_baseline/` пустой). Объём не расширялся;
найденное вне объёма — бэклог 52.

---

## Первой строкой, как велит П33

**Второй сайт: `npm run gates -w 7thserpent.com` — `4/4`, `exit=0`;
`npm run build -w 7thserpent.com` — `exit=0`, 1 страница, `h1: 1`,
якоря без отказа, sitemap `https://www.7thserpent.com/`; `astro check` —
0 ошибок, 0 предупреждений, 0 подсказок.** **Первый сайт после смены
`package-lock.json`: гейты `4/4`, сборка 32 страницы (h1 32, kotwice 32,
linki 979/32, korytarz 28 + 4 null, art 52/150/29), `npm run accept` —
`225/225`, `exit=0`.** Проба `@source` по инварианту 8: маркер `z-[61]`
в `core/chrome/SiteHeader.astro` → `z-index:61` в `dist/_astro/*.css`
есть → маркер откачен, `core/` чист. Окончания: все файлы
`sites/7thserpent.com/` — `i/lf w/lf` (xlsx `i/-text`), `DECISIONS.md`,
`docs/`, `package-lock.json` — LF; `git add --renormalize .` перед каждым
коммитом — пусто. Ветка запушена после каждого коммита.

## Сверка открытых вопросов прошлого доклада — по П38

| Вопрос доклада `2026-09-16-techdolg-kadry.md` | Исход | Где |
|---|---|---|
| 1. Приёмка по кадрам | принята | П61 п. 2 |
| 2. Галереи Odyssey/Origins/Unity | (б) волна 2 | П61 п. 1, бэклог 51 п. 1 |
| 3. Вливание `techdolg-kadry` в `main` | **сделано этой сессией перемоткой по слову владельца** — `main` 5183c12 → `0b90bd4` = `origin/main` | П62 п. 2 |
| 4. Лист второго сайта (ниша/бриф, семантика, домен) | **закрыт** — бриф, выгрузка, домен получены; сессия 3 исполнена | П62 п. 1, этот доклад |
| 4. Приёмка П53 п. 1–3, `/404/` глазами, сайт в сети раньше ящика, бэклог 47 п. 9, бэклог 49 | переносятся | «Политические» п. 6 |

---

# 1. Счёт сессии

| Что | Число |
|---|---|
| Перемотка `main` | `5183c12` → `0b90bd4` (ff-only, push) |
| Коммитов в `7thserpent-brief` | 4 + доклад: `a271a44` П62 · `67ecbfd` бриф и выгрузка · `5726b9f` бриф — поправка по рецензии · `d413f63` каркас + lock |
| Файлов нового сайта | 15: бриф, выгрузка, `package.json`, `astro.config.mjs`, `tsconfig.json`, `gates/` 2, `structure/structure.json`, `src/` 6, `_baseline/README.md` |
| Строк семантики | 508 фраз, 103 группы («Название группы»: 102 + «Некластеризовано» 59), 63 470/мес; sha256 `f24f8988…790a6e`, 59 560 байт |
| Состязательная проверка каркаса (ultracode) | 3 линзы → 9 находок → по 2 опровергателя: 6 подтверждены (все — расхождения с буквой плана/П62, блокеров нет), 3 опровергнуты; исправлено до коммита `d413f63` и коммитом `5726b9f`; остальное — дополнение П62 |
| Записей | П62 + дополнение (7 п.), бэклог 52 (11 п.) |

# 2. Ветка и перемотка (шаг 0)

`git switch main` → `git merge --ff-only techdolg-kadry` → `git push origin
main`: `main` = `origin/main` = `0b90bd4`, 19 файлов, слияния не было
(прецедент П59-дополнение). Ветка `7thserpent-brief` от `main`, запушена
до первого коммита. Имя — решение исполнителя.

# 3. П62 (`a271a44`) — до правок

Форма П60: слова владельца дословно (семь полей, путь, четыре ответа,
уточнение о кластерах), пять решений владельца, решения исполнителя со
статусом «приёмка по докладу». Дополнение той же сессии (коммит доклада) —
семь поправок по рецензии, главная: **«S0/S2 перезапишут `total_queries`»
— неверно**, поле никто не пишет (`build-tree.mjs:479` — `site: doc.site`
как есть), гейт сверяет его с фактом при непустом дереве
(`check-structure.mjs:101`); бэклог 52 п. 3.

# 4. Бриф и выгрузка (`67ecbfd`, `5726b9f`)

`sites/7thserpent.com/brief/site.yaml` по `docs/01_BRIEF_TEMPLATE.yaml`:
семь полей владельца — **ровно две опечатки поправлены** («посвящённый»,
«нужную»), остальное дословно (первая редакция нормализовала шире —
запятая, регистр, строчная буква; рецензия поймала, `5726b9f` вернул форму
владельца); `mode: normal`; `structure.source: clusters`, `input` —
выгрузка, `texts: false`, `depth: 2`, `page_types: []` (закрытый список
контракта, не шаблона — бэклог 52 п. 4); `references`, `blocks`, `design`,
`seo` — пусто; `extra` — только известное: фан-сайт без идентики издателя,
цель «видео» при отсутствии видео-блока в каталоге, формат семантики,
аналитика — нет по умолчанию.

Выгрузка перенесена (не скопирована) из корня в `input/` **под именем
владельца** `Clustering_max-payne_with_volume.xlsx` — `meta.source`
читалки = basename, провенанс — sha256; переименование по образцу первого
сайта — если появится переэкспорт.

# 5. Каркас (`d413f63`)

| Файл | Что |
|---|---|
| `package.json` | скрипты `dev/build/preview/check/contrast/gates`; без `accept*` (батарея прибита к первому сайту — бэклог 52 п. 5) и без `tools/`; зависимости — подмножество первого сайта без `@fontsource`, версии те же |
| `astro.config.mjs` | `site: 'https://www.7thserpent.com'`, `trailingSlash: 'always'`, `build.format: 'directory'`; интеграции `sitemap()`, `afterBuild()`, `anchors()`; `links`/`corridor` — с деревом S2 (отказывают на пустой структуре), `art` — по 47 п. 9; комментарий в файле |
| `tsconfig.json` | как у первого сайта |
| `gates/assets.mjs` | `[]` — свежий клон |
| `gates/contrast.mjs` | 17 пар шасси/марки (первый сайт без эпох и пергамина) — 17/17, «Zapis palety: 11 par, zgodnych 11» |
| `structure/structure.json` | шапка `site` (5 полей `schema.json`), `pages: []` — «etap przed S2»; `semantics_date: 2026-09-18` — дата создания файла, допущение; `total_queries: 508` — рукой, по факту |
| `src/styles/global.css` | **заглушка до сессии 7:** шасси + четыре токена марки первого сайта, системные гарнитуры, без эпох/пергамина/`--rail`; 32 токена и 8 ролей `ROLES.md`, `--era*` = акценту, порядок `@import` по §4, `html.menu-otwarte`; в шапке — почему имена блоков в комментариях не писать (п. 7 дополнения П62) |
| `src/layouts/Base.astro` | `lang="en"`, `og:locale en_US`, canonical от `Astro.site`, skip-link → единственный `<main id="content">`; без крошек (`getPage` бросает при `pages: []`), favicon, JSON-LD |
| `src/pages/index.astro` | один `<h1>` литералом (временное отступление от П24 п. 3 до S2), лид «unofficial fan site … under construction» — без обещаний категорий (рецензия) |
| `src/components/SiteHeader.astro`, `SiteFooter.astro` | обёртки над `@factory/core/chrome/*`: английские подписи, знак — текст в `.t-title`, две inline-иконки; подвал — одна колонка, дисклеймер по `PRODUCT.md` **без имён компаний и класса лицензии**, `© 2026` |
| `src/data/site.ts` | `site { name, tagline, lang }`, `nav = [Home /]` |
| `_baseline/README.md` | эталона нет намеренно; первый — сессия 7 |

**Регистрация воркспейса:** `npm install --no-audit --no-fund` в корне —
`package-lock.json` 21+/3−: `node_modules/7thserpent.com` (link) и блок
`sites/7thserpent.com` (19 строк) плюс реконсиляция устаревшего lock —
peer `sharp ^0.35.4` ядра (`core/package.json` с `ad4c471`, lock тогда
не пересобирался) и снятие `devOptional` у `sharp`/`@img/colour`; версии
не менялись; lock LF; корневой и оба посайтовых манифеста не тронуты
(md5 до/после равны). Одна попытка.

**Что упало и как починено до коммита:** первый прогон гейта контраста —
«11 пар без пары» при полном совпадении hex: `check-contrast.mjs:35-45`
ищет `:root` первым вхождением строки, а слово стояло в шапке-комментарии
`global.css` до блоков. Комментарий переписан; правка судьи — бэклог 52 п. 2.

# 6. Таблица гейтов и проверок

| Проверка | Результат |
|---|---|
| `git rev-parse main` после ff | `0b90bd4` = `origin/main` |
| `npm run gates -w 7thserpent.com` | 4/4, `exit=0`: kontrast 17/17 (palety 11/11), tokeny 32 файла (5 src + 27 core), zasoby «świeży klon», struktura «pages: 0 — etap przed S2» |
| `npm run build -w 7thserpent.com` | `exit=0`; 1 страница; h1 1; kotwice ok; `sitemap-0.xml` — `https://www.7thserpent.com/`; canonical тот же; `<html lang="en">`; `dist/` только в папке сайта, в корне нет |
| `npm run check -w 7thserpent.com` (после сборки) | 0 / 0 / 0 |
| Проба `@source` (инвариант 8) | маркер `z-[61]` в `core/chrome/SiteHeader.astro` → `z-index:61` в CSS есть → откат, `git status core/` пуст, пересборка — 0 |
| Первый сайт после lock | gates 4/4; build 32 страницы, сторожа те же, что в докладе 2026-09-16; accept 225/225 |
| Окончания | `git ls-files --eol sites/7thserpent.com package-lock.json DECISIONS.md docs/BACKLOG.md` → `i/lf w/lf`, xlsx `i/-text w/-text`; `file` — без «CRLF»; `git add --renormalize .` — пусто |
| Состязательная проверка (3 линзы, 2 опровергателя на находку) | 9 → 6 подтверждены (лид, бриф ×2, `design`, `tagline`, `total_queries`, lock ×2 как заметки к докладу), 3 опровергнуты; блокеров 0 |
| Git | `git status` чисто после каждого коммита; `git log origin/7thserpent-brief..7thserpent-brief` — пусто |

# 7. Факты, важные будущим сессиям

1. **Вход сессии 4 — второй формат читалки** (`clustering.mjs`): контракт
   между читалкой и потребителями — не xlsx, а её выход: `phrases[]
   {phrase, cluster, google, urls}`, `clusters[] {name, phrases, google,
   topPhrase}`, `meta {source, sha256, phrases, clusters}`; поля
   `ws/unmatched/legend/googlePhrase/match/yoy/competition/rankWs` никто не
   читает. Сводная `clusters[]` — группировкой по «Название группы»
   (число фраз — счётом строк: «Фраз в группе» у «Некластеризовано» = 1
   при 59; сумма = «Частотность группы» — совпадает с суммой по строкам
   во всех 103 группах; `topPhrase` = максимум). Бэклог 52 п. 1.
2. **Чем новая выгрузка отличается по данным:** `% Агрегаторов` заполнен
   у 78 фраз (10/20) — признак C.2 `05_STRUCTURE_TASK` может заработать
   «как написано», у первого сайта колонка была пуста; `Главных страниц`
   — 27 фраз; «Топоним» — прочерк у всех; фраз без объёма нет (все ≥ 10);
   1 136 уникальных URL на 342 хостах (reddit 130, youtube 115,
   maxpayne.fandom 57, steamcommunity 42, gamefaqs 36) — корпус втрое
   меньше первого сайта; значения «Частотность» на сетке 16 000 / 9 100 /
   4 700 / 2 800 — не сетка Keyword Planner (гипотеза — Ahrefs US, слова
   владельца нет).
3. **Пороги хвоста долями объёма** (`rules-s0.json` первого сайта):
   голова 95 % → ≥ 100/мес, 45 кластеров; середина 99 % → ≥ 40/мес,
   81 кластер; хвост < 40 — 21 кластер.
4. **`fetch-corpus.mjs` первого сайта новый файл читает** (свой читатель,
   три колонки `:227-228`); `recon-s0.mjs` и `build-tree.mjs` — нет
   (импортируют `readClustering`). Посайтовое в инструментах S0 — бэклог
   52 п. 6–7.
5. **Каркас с `pages: []`:** гейт структуры семантику не читает, `links`/
   `corridor` подключать нельзя (отказ на пустой структуре), `getPage`/
   `getBreadcrumbs` бросают — `h1` главной литералом до S2.
6. **Корневые `npm run build|check|gates`** (`--workspaces --if-present`)
   с этой сессии захватывают оба сайта; `npm run accept` из корня нет.
   `.gitignore:1-3` покрывает `dist/`, `.astro/`, `node_modules/` нового
   сайта без правок. `astro check` — только после первой сборки
   (`.astro/types.d.ts`). `paths` деплоя первого сайта включают
   `package-lock.json` — `AC4BF_DEPLOY` не задана, выкладки нет.
7. **Гейт контраста слеп к комментариям:** имена блоков `@theme`/`:root`
   в комментарии `global.css` до блоков уводят сверку пар (бэклог 52 п. 2).
8. **Счётчик CR в bash-цикле (`grep -c $'\r'` внутри `$(…)`) даёт ложные
   срабатывания в этой оболочке** — проверять окончания `file`, `od -c`
   и `git ls-files --eol`.
9. **Write пишет LF** — при решении «весь сайт LF» ручного перевода
   в CRLF нет (у первого сайта он был обязателен).

# 8. Найденное вне объёма — бэклог 52

Второй формат читалки (п. 1), судья контраста (п. 2), `total_queries`
без скрипта (п. 3), `page_types` шаблона брифа vs контракт (п. 4),
`accept`/`glyphs` прибиты к первому сайту (п. 5), два читателя xlsx (п. 6),
посайтовое в `rules-s0.json`/константах снимка (п. 7), деплой — префикс
секретов буквенный, `paths: core/**` (п. 8), `sites/_template/` не
существует — каркас им является (п. 9), посайтовый `CLAUDE.md` (п. 10),
lock отставал от манифеста ядра (п. 11).

## Технические — решены

- Ветка от `main` после перемотки по слову владельца; имя `7thserpent-brief`.
- Выгрузка — под именем владельца в `input/`; провенанс — sha256.
- Бриф — две опечатки, форма владельца (после рецензии); `design` пуст.
- `total_queries` — рукой, сверяет гейт при непустом дереве (П62 доп. п. 1).
- Интеграции каркаса — три из пяти, причины в файле и П62.
- `global.css` — заглушка шасси до сессии 7; `--era*` = акценту.
- `main id="content"`, подписи по-английски, комментарии по-русски.
- Дисклеймер подвала без имён компаний и класса лицензии — до сессии арта.
- `npm install` — одна попытка, дифф lock объяснён построчно.
- Комментарий `global.css` без слов `@theme`/`:root` — обход судьи, не
  правка судьи.

## Политические — владельцу

1. **Приёмка сессии 3 — по докладу:** каркас без витрины, эталона нет,
   глазами смотреть нечего (одна страница-заглушка: заголовок, фраза,
   шапка и подвал ядра). Кадр не снимался намеренно. **Рекомендация —
   принять по докладу.**
2. **К сессии 4 (S0) — нужно слово:** (а) источник и гео колонки
   «Частотность» (Ahrefs US? другое?) и дата снимка выдачи — сейчас
   `semantics_date: 2026-09-18` по дате файла; (б) **судьбы «неудобных»
   кластеров** — категорий нет в правилах первого сайта: взрослое
   (`max payne xxx` 100/мес) → exclusions; пиратство (`max payne iso` 180,
   `download` 120, `3 free` 70, `2 iso` 50; crack/repack/rom
   в «Некластеризовано») → `no_page` «нелегальное распространение»;
   `max payne apk` 500 (из них официальный «max payne mobile» 400) —
   ключ хаба MP1, «apk download» в `no_page`; читы/трейнеры (280 + 190 +
   80 + 40 + 20) — страницы; ESRB/parents guide (80 + 70) — тема «rating»;
   **фильм 2008** (`max payne (film)` 7 590 = 12 % объёма, `watch` 320,
   `2 movie` 150) — в дереве как тема серии, если «фан-сайт игры» включает
   экранизацию; мерч (shirt 290, coat 200, figure 90, poster 60, statue 50,
   tattoo 40, tie 30) → `no_page`; ключи (`3 key` 70) → `no_page`,
   `steam`/`gog` (70/90) — слить в хаб; чужие («young nudy» 150 — рэп,
   «strain» 30) → exclusions. **Рекомендация — подтвердить таблицу
   целиком, спорное — фильм;** (в) забор корпуса — только широкий
   (дерева нет; бэклог 9), бэклог 8 — наблюдение.
3. **К сессии 6 (S3) и 8:** имя видео-блока — после анатомии S3
   (YouTube — второй хост выдачи; прецедент П28); приватность при видео —
   миниатюры `i.ytimg.com` = внешний запрос (бэклог 31).
4. **К сессии 7 (тема):** (а) шасси первого сайта с оверрайдами палитры
   и гарнитур под нуар, impeccable — только для них; (б) новая тема
   целиком по референсам (`references.global` пуст — нужны слова «что
   именно нравится»). **Рекомендация — (а):** ниша игровая, `PRODUCT.md`
   — одно шасси. Заглушка `global.css` выбором темы не считается.
5. **К пачке 0 и публикации:** класс лицензии кадров Remedy/Rockstar
   и источники (Steam-витрины MP1–MP3, Commons) — «да» владельца; бэклог
   47 п. 9 (гейт `art` до первого `gameart`); тексты — П42 заново
   (рекомендация (а), для страниц с тонким корпусом — (в)); приватность
   для США без RODO-ссылок, ящик на домене, аналитика — нет; деплой:
   префикс секретов `SERPENT_*`, тот же хостер/FTPS?, DNS/регистрация
   домена; посайтовый `CLAUDE.md` — сессия 4 (рекомендация).
6. **Переносятся без ответа:** приёмка П53 п. 1–3, `/404/` глазами, сайт
   в сети раньше ящика, бэклог 47 п. 9, бэклог 49; **бэклог 52 п. 2–4, 9**
   — правки судьи/контракта/шаблона, владелец.
7. **Вливание `7thserpent-brief` в `main`** — перемоткой (П21, П51 п. 5,
   П59, П61 п. 2); вливает владелец на приёмке или сессия по прямому
   слову. Автовыкладки нет, живого сайта у второго домена нет.

**Дальше — по команде владельца: сессия 4 (S0) стартует с адаптера
второго формата (П62 п. 5, бэклог 52 п. 1) и ответов п. 2; без команды
не идти.**

---

# Записи, обещанные этой сессией — по П36

| Что | Где | Источник |
|---|---|---|
| перемотка `main` по слову владельца | `main` = `0b90bd4` | П62 п. 2 |
| П62 на старте, до правок | `a271a44` | форма П60 |
| бриф и выгрузка | `67ecbfd`, поправка `5726b9f` | план §2, рецензия |
| каркас + lock | `d413f63` | план §3 |
| дополнение П62 (семь поправок), бэклог 52 | коммит доклада | рецензия, CLAUDE.md «что решаешь сам» |
| доклад | этот файл | CLAUDE.md «Финал» |

**Обещано и НЕ сделано:** ничего из объёма. Вне объёма и не тронуто:
читалка ядра, инструменты S0, тема, `public/`, деплой, посайтовый
`CLAUDE.md`, правка судьи контраста.
