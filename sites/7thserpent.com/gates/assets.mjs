// Данные сайта для гейта ресурсов из `@factory/core/gates`.
// Запись — кортеж [путь, описание]: путь считается от `src/assets/`.
//
// Список — собственность сайта, не ядра. Ядро умеет проверить, лежит ли
// файл на диске; только сайт знает, каких файлов его готовая страница
// действительно требует.
//
// ПУСТОЙ СПИСОК КОРРЕКТЕН и означает свежий клон шаблона — гейт проходит
// (core/gates/check-assets.mjs). Это не дефект: каркас собирается и выглядит
// законченно до того, как кто-то запустит `npm run art` или `gameart`.
// Запись добавляется тогда, когда страница без этого файла выглядит хуже,
// чем должна, — не тогда, когда файл просто существует.
export default [];
