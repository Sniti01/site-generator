# MARSHRUT — находок 3
## P2-R3-MARSHRUT-1 [safe] sites/7thserpent.com/src/content.config.ts:131-139 (superRefine byline) — остаток P2-R2-MARSHRUT-2
CLAIM: P2-R2-MARSHRUT-2 закрыт только для не календарных дат. У календарной, но древней даты '0000-01-01' с dateLabel 'January 1, 0' по-прежнему два отказа, и второй подсказывает неверно: «[dateLabel] не та же дата словами: ждали «January 1, 1»». Intl печатает год 0 как 1 год до н. э. без эры. Если послушаться подсказки, остаётся один отказ «раньше 2000 года». Ложного прохода нет: сборка падает на законной причине.
EVID: byline-vyvod.txt: '0000-01-01'/'January 1, 0' → «[date] дата подписи раньше 2000 года | [dateLabel] не та же дата словами: ждали «January 1, 1»»; '0000-01-01'/'January 1, 1' → только «раньше 2000 года». '2026-02-31', '2026-13-01', '2026-00-10', '2026-09-00', '2026-02-29' — теперь ровно один отказ «[date] не календарная дата», в том числе при 'March 3, 2026' в dateLabel.
REPRO: node C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/3579b79f-5acf-478f-90e1-ee2488a3d042/scratchpad/sud-r3/marshrut/byline-roditel.mjs — раздел «не календарные и крайние даты».
FIX: Правка не обязательна. Если нужна: при отказе «раньше 2000 года» выходить из superRefine до сверки dateLabel (return после addIssue). Либо назвать остаток в докладе одной строкой.

## P2-R3-MARSHRUT-2 [safe] sites/7thserpent.com/src/content.config.ts:110-113, 135-137 («День сборки — местная календарная дата машины сборки»); .github/workflows/deploy-ac4bf.yml:66 (runs-on: ubuntu-latest)
CLAIM: Правило верно для сборки на машине владельца, но «местная дата машины» зависит от пояса машины, а не автора. На раннере в UTC (так собирается первый сайт: ubuntu-latest, TZ не задан) ложный отказ «сегодняшней по Варшаве» даты с 00:00 до 02:00 CEST (зимой — до 01:00) вернётся ровно в виде P2-R2-MARSHRUT-1. Сейчас выкладки 7thserpent через CI нет, поэтому это предел на будущее, а не дефект. Для года машины меньше 1000 или больше 9999 строковое сравнение ломается (год без padStart до 4 знаков), но это возможно только при сбитых часах.
EVID: byline-vyvod.txt: всего 84 случая (TZ Europe/Warsaw, America/Los_Angeles, Pacific/Kiritimati, UTC, Pago_Pago, Apia; границы суток, переходы DST 25.10 и 08.03, новый год), неверных 0 против оракула Intl toLocaleDateString('sv-SE',{timeZone}). В TZ=UTC при 2026-09-25T23:59:59.999Z (у автора в Варшаве 01:59 26.09) дата 2026-09-26 отказывает: «дата подписи 2026-09-26 позже дня сборки 2026-09-25». Сбитые часы: при 0999-06-01 проходит '2026-09-26' ('2026…' < '999-…'), при +010000 отказывают все даты.
REPRO: node …/sud-r3/marshrut/byline-roditel.mjs (ребёнок byline-rebenok.mjs сверяет, что копия правил — подстрока content.config.ts, и подменяет Date только для new Date() без аргументов).
FIX: Сейчас правка не нужна. Когда появится выкладка 7thserpent через CI, задать в ней TZ=Europe/Warsaw или назвать предел в комментарии схемы: «день сборки — по поясу машины; на раннере UTC — по UTC».

## P2-R3-MARSHRUT-3 [safe] sites/7thserpent.com/src/pages/[...slug].astro:581-583 (комментарий «на 320 и 360 px подпись переносится»), :587-598 (.podpis-geroya, text-wrap: balance)
CLAIM: balance выравнивает строки, но плашку не сужает: у абсолютного блока с шириной по содержимому ширина считается до выравнивания. На 320–373 px плашка по-прежнему во всю ширину героя, а текст занимает левые 57 % (строки 12–175 и 12–195 px). Справа остаётся пустая тёмная полоса 125–175 px поверх арта. До правки первая строка доходила до 286–310 px. Площадь, которую плашка закрывает, не изменилась (320×45,7 в обоих режимах). В комментарии «на 320 и 360 px» диапазон назван неточно: перенос идёт на всех ширинах 320–373, с 374 подпись в одну строку.
EVID: Замер через Range по словам, свой контекст, порт 4471, dist-r3. 320: balance «Pictured: the original Max» / «Payne (2001), not the remake» (12–175 / 12–195), wrap 12–286 / 12–83 «the remake»; box [0,154.5,320,45.7] в обоих режимах. 330–370 — те же строки balance, box во всю ширину. 374 — одна строка, box 372,8×26,8. 480, 641, 700, 800, 1000, 1024, 1025, 1440 — одна строка, box 372,8×26,8, balance ничего не меняет. Кадры crop-320x640-balance.png и crop-320x640-wrap.png.
REPRO: Свой контекст Playwright, /remake/ при ширине 320–373: getBoundingClientRect у .podpis-geroya и Range по словам, с el.style.textWrap='wrap' и без.
FIX: Место вкуса, на приёмку глазами. Можно уточнить в комментарии диапазон «320–373 px». Если пустая полоса мешает: text-wrap: pretty (последняя строка не из одного слова, первая почти во всю ширину) или text-align: end, чтобы текст держался правого угла.

### checked_ok
- P2-R2-MARSHRUT-1 закрыт по-настоящему. Правила byline скопированы дословно (подстрока content.config.ts проверена в скрипте), zod 4.5.4 из node_modules репозитория, new Date() подменён, TZ задаётся процессу-ребёнку. Проверено 84 случая на границах суток в Europe/Warsaw, America/Los_Angeles, Pacific/Kiritimati, UTC, Pacific/Pago_Pago и Pacific/Apia (там пропуск дня 2011-12-30), с переходами DST и сменой года. Вчера и сегодня по местному календарю проходят, завтра отказывает; расхождений с оракулом Intl нет. Сообщение внятное и называет обе даты: «дата подписи 2026-09-27 позже дня сборки 2026-09-26»
- Строковое сравнение: равные даты ('2026-09-26' > '2026-09-26' = false) проходят. Год обеих сторон — 4 цифры (регулярное выражение date и реальные часы), поэтому лексикографический порядок совпадает с хронологическим. padStart месяца и дня верен
- Выход из superRefine при не календарной дате: '2026-02-31' (с любым dateLabel, включая 'March 3, 2026'), '2026-13-01', '2026-00-10', '2026-09-00', '2026-02-29' — ровно один отказ «[date] не календарная дата». '2024-02-29' проходит. Проверки формы ('2026-9-26', хвостовой или ведущий пробел, '\n', полноширинные цифры, 'T00:00') по-прежнему дают два сообщения о date (регулярное выражение и «не календарная»), но это refine, который не менялся с b987f27, и ложного указания на dateLabel нет
- '0999-12-31', '1999-12-31' — один отказ «раньше 2000 года» при верном label; '2000-01-01' проходит; '9999-12-31' — «позже дня сборки»
- Комментарий о кеше Astro верен и не обещает лишнего. glob.js:90-93 пропускает parseData, если digest содержимого файла тот же и filePath есть. content-layer.js:185-206 очищает хранилище при смене текста content.config.ts (utils.js:376 — хеш файла конфигурации), astro.config или версии Astro. Кеш сборки лежит в node_modules/.astro/data-store.json (paths.js:3, cacheDir) и читается в sync (sync/index.js:106-107); --force его стирает. Комментарий не называет astro.config, версию и --force, но это недоговорённость, а не завышенное обещание. Ложного прохода кеш не даёт: дата, однажды прошедшая «не позже», при ходе часов вперёд остаётся законной
- text-wrap: balance сохраняется в собранном CSS (Lightning CSS): в dist-r3/remake/index.html правило .podpis-geroya[data-astro-cid-n67f4zmd]{…;text-wrap:balance;…}, computed textWrap = balance
- Раскладка: на 641–1024 и выше (641, 700, 800, 900, 1000, 1024, 1025, 1440) и на 374–640 подпись в одну строку, box 372,8×26,8 при balance и при wrap один и тот же. Высота плашки на 320–373 та же (45,7), поэтому ни на что новое она не наезжает. Пересечений с .hero__text, .hero__title, .hero__lead и header нет ни на одной из 23 ширин, scrollWidth равен ширине окна
- Контраст парой снимков (с текстом и с color:transparent, зерно скрыто, DPR 1, dist-r3 на 4471). Нарисованный цвет букв равен объявленному (L 0.400 = 0.400) на всех окнах. balance/wrap: 320x640 — 7.00 и 6.67 / 7.00 и 6.67; 320x844 — 7.83 и 7.41 / 7.76 и 7.41; 360x740 — 7.25 и 6.84 / 6.69 и 6.84; 390x844 — 5.38 / 5.38 (светлейший фон rgb(46,52,59)); 414x896 — 5.39; 480x900 — 5.55. Это совпадает с r3-e35cab8-remake.txt (худший 5.38:1 на 390x844, y 159.5). balance контраст не ухудшает, на 360 улучшает первую строку (6.69 → 7.25)
- Число «около 4,4:1 над белым пикселем» в комментарии маршрута верно: 0,78·#090c11 + 0,22·255 = (63.1, 65.5, 69.4), против #a1abb3 — 4.35:1
- Дифф 1ae7c58..c0c9cfb: только content.config.ts (+11/−3: комментарий, условие выхода, denSborki и сообщение) и [...slug].astro (+7/−1: комментарий и text-wrap: balance). Всё названо в сообщении коммита, «заодно» нет. c0c9cfb..e35cab8 и c0c9cfb..6c127da по sites/ трогают только файлы содержания (другая линза). Окончания строк обоих файлов — i/lf w/lf attr/-text
- Процесс: опыты только в scratchpad/sud-r3/marshrut/ (byline-rebenok.mjs, byline-roditel.mjs, byline-vyvod.txt, kontrast.mjs, kontrast-vyvod.txt, kadry/, crop-*.png). Браузер — только в своих контекстах. Сервер на 4471 с dist-r3 остановлен. git status репозитория — те же две неотслеживаемые папки, что на старте

# SVERKA — находок 6
## P2-R3-SVERKA-1 [defect] docs/reports/2026-09-26-7thserpent-pachka-2/instrumenty/proby-p2.mjs:56-58 (ПРЕДЕЛЫ: «`related` и призыв (`cta`) свежестью не судятся (их судит сверка пачки 1, `proby-p1.mjs`…)»)
CLAIM: Отсылка к proby-p1 неверна. Сверка пачки 1 не судит `related` вовсе, а у `cta` проверяет только две вещи: есть ли `cta.title` подстрокой в сыром тексте HTML и верен ли `cta.href`. Поэтому `related.title`, `cta.lead` и `cta.label` не судит ни одна из двух сверок, и старая сборка проходит обе. В шапке сказано, что их судит сверка пачки 1.
EVID: Чтение proby-p1.mjs:145-158. Свежесть там — только `[...rows.map(r => r.title), dane.cta?.title]` через `tekstHtml.includes(t)`, затем число `section.cta` и href кнопки. Слова `related` в сверке нет. Опыт в копии сайта: в pc.md заменены `related.title` («Proba related title»), `cta.lead` и `cta.label`, сборка dist-v0 осталась старой (она побайтно равна dist-r3). sv-kopia.mjs --tolko-dist даёт «ok dist /pc/», копия proby-p1 (p1-kopia.mjs) — «ok dist /pc/ призыв объявлен». Обе молчат.
REPRO: В копии сайта поправить три поля pc.md, не пересобирая, затем node sv-kopia.mjs --tolko-dist --dist <dist-v0> и node p1-kopia.mjs --tolko-dist --dist <dist-v0> (обе копии лежат в scratchpad/sud-r3/sverka/). Строка /pc/ — ok в обеих.
FIX: Поправить шапку: «заголовок `related`, `cta.lead` и `cta.label` не судит никто; `proby-p1` судит только `cta.title` (подстрокой в HTML) и `cta.href`». Либо дописать в sverka пачки 2 четыре строки: h2 в `section.link-list` = `related.title`; заголовок, лид, надпись и href в `section.cta` = полям `cta`, полным равенством через `tekst`/`norm`; для каждой — порча в --proba. Это правка судьи, значит снова «судью судят».

## P2-R3-SVERKA-2 [defect] proby-p2.mjs:43-44 (шапка: «`section.layer` в <main> ровно столько, сколько рядов в файле») против :337-342 (zhdemRyady — только ряды, чья роль объявлена вхождением story-row)
CLAIM: Код считает не ряды файла, а ряды к печати. Если в файле содержания появился ряд с ролью, для которой в blocks[] нет вхождения story-row (новый ряд или опечатка в role), сверка молча выкидывает его из ожидания, и старая сборка проходит «ok». По шапке ряд сверх напечатанного — отказ. Предусловие: сборка на таком содержании падает («Ряды без своего вхождения story-row»), так что ложный проход бывает только при сверке старого dist после правки содержания — ровно тот случай, который стережёт свежесть.
EVID: exp3.mjs на dist-r3, /remake/: в копию содержания добавлен ряд {id: 'proba', role: 'proba', …} — ТИШИНА, 0 замечаний. Для сравнения: если у существующего ряда сменить роль на необъявленную, сверка ловит («рядов в <main> 6, в файле содержания к печати 5»), потому что пропадает напечатанный ряд. Текст замечания в коде честный («к печати»), неверна только шапка.
REPRO: node exp3.mjs --tolko-dist --dist <dist-r3> в scratchpad/sud-r3/sverka/, первая строка вывода.
FIX: Одна строка в sverka: ряды файла, у роли которых нет вхождения story-row в page.blocks (роль приводится как `(r.role || '')`), — замечание «ряд X с ролью Y без вхождения story-row: сборка отвергла бы это содержание, dist старый». Плюс порча в --proba. Либо поправить шапку: «сколько рядов к печати (ряд с необъявленной ролью не судится — его отвергает маршрут)».

## P2-R3-SVERKA-3 [defect] proby-p2.mjs:393-395 (фронтматтер — `/^﻿?---\r?\n([\s\S]*?)\r?\n---/`, затем yaml); шапка :63-64 («как `js-yaml` загрузчика Astro»)
CLAIM: Слияние ключей теперь совпадает с Astro, но вырезание фронтматтера — нет. Astro (@astrojs/internal-helpers/frontmatter.js:11, `(?:^﻿?|^\s*\n)(?:---|\+\+\+)([\s\S]*?\n)(?:---|\+\+\+)`) принимает пустые строки перед `---` и пробелы после открывающего `---`. Инструмент на таком файле получает dane = null. Выходит громкая ложная тревога с неверной причиной: «ПЛОХО dist …remake.md: адрес undefined не в структуре». Сверка при этом не оговаривает, что это расхождение разбора. Остальные расхождения пакетов на законном для сборки содержании тоже громкие: директива %YAML 1.1 (yaml даёт true, js-yaml — строку «yes») и больше 100 алиасов (yaml бросает «Excessive alias count», сверка падает со стеком).
EVID: Сборка копии сайта (varianty.mjs): у remake.md спереди «\n» — `astro build` код 0, сверка код 1 «адрес undefined не в структуре». С «--- \n» в первой строке то же самое. fm.mjs: parseFrontmatter из internal-helpers на обоих вариантах даёт {url:'/x/'}, регулярка инструмента — null. На всех восьми файлах содержания parseFrontmatter(md).frontmatter равен yamlParse(…, {merge:true}). yamltest.mjs: yaml 2.9.0 и js-yaml 4.3.2 (тот, что у internal-helpers) на восьми файлах и на трудных случаях совпадают: слияние (явный ключ до и после, список, вложенное), >-, |-, |+, CRLF, yes/on, восьмеричные, __proto__. Расходятся только громко: неверный источник слияния, дубль ключа, табуляция, неизвестный тег — у обоих исключение; дата без кавычек — Date против строки, это отвергает схема; !!set, %YAML 1.1 и алиасы свыше 100 описаны выше.
REPRO: node varianty.mjs pustaya-stroka-pered-fm probel-posle-otkryvayushchego (нужны ссылки kopia/node_modules и kopia/core — сейчас сняты); node fm.mjs; node yamltest.mjs — всё в scratchpad/sud-r3/sverka/
FIX: Брать фронтматтер тем же кодом, что и загрузчик: `const { parseFrontmatter } = await import(<путь из createRequire('.../node_modules/astro/package.json').resolve('@astrojs/internal-helpers/frontmatter')>)`, затем `parseFrontmatter(md).frontmatter`. Вырезание и js-yaml совпадут дословно, и опция merge станет не нужна. Если оставить yaml, при dane === null писать «фронтматтер не разобран сверкой», а не «адрес undefined не в структуре», и назвать расхождение пределом в шапке.

## P2-R3-SVERKA-4 [safe] proby-p2.mjs:422-508 (PORCHI) против :338-340 (роли), :395 (merge), :212 (`$` в klyuchAdresa), :218-220 (каждый кандидат srcset), :349, 351, 354 (norm стороны файла), :362 (ровно один кадр ряда); zamery/mutacii-sverki.mjs («59 мутант, 0 выживших»)
CLAIM: Мутационное испытание ведущего снимает только zam.push, поэтому его «0 выживших» ничего не говорит о логике, которую добавил раунд 2. Мутанты условий и разбора выживают и в --proba (77/77), и в сверке dist-r3 («всё как ждали»). Выжили 18 из 28. Существенные — те, что обслуживают исправления раунда 2. (1) Роли не учитываются, ряды идут в порядке файла: на всех восьми страницах ряд с ролью стоит последним в файле, поэтому порядок печати маршрута не доказан ничем. (2) Роль без приведения '' к «без роли». (3) YAML без merge. (4) klyuchAdresa без `$`: путь с `..` опять даёт ключ первого кадра. (5) Проверяется только первый кандидат srcset — в шапке «КАЖДОГО»; у /remake/ всего два кандидата, и второй равен src. (6) norm со стороны файла у body, year и meta снят. (7) Вместо «кадров ряда ≠ 1» — «= 0». Мелкие: части условия :356, граница блока byline и .layer__body, flip подстрокой, tekst с заменой тега на ''. Эти эквивалентны на нынешней разметке. Сверка сама по себе верна; не доказано её самоиспытание.
EVID: mut-usl.mjs (вывод в mut-usl-vyvod.txt): 28 мутант, выжило 18. Что эти мутанты нужны, показывают законные сборки копии сайта (varianty.mjs), где настоящая сверка зелёная. Мутант (1) даёт ложную тревогу «порядок рядов …», если ряд с ролью стоит в середине файла или вхождение story-row#max-payne-4 в blocks[] стоит первым. Мутант (3) — «ряд voice: класс band есть, в содержании band: false», когда ряд берёт band слиянием `<<: *rd`. Мутант (6) — «абзацы разошлись», когда абзац записан блоком `|` с двойным пробелом. Для сравнения: мутанты «klyuchAdresa без ^», «srcset: src не проверяется», «atr без границы» и «alt только по игре» порчи убивают. Раскладка причин по строкам (tagged.mjs, metki.txt) показывает, что причина каждой из 71 порчи — подстрока замечаний ровно одной ветви.
REPRO: node mut-usl.mjs в scratchpad/sud-r3/sverka/; законные варианты — node varianty.mjs rol-v-seredine rol-pervoj-v-blocks merge blok-skalyar с MUT=m1-rows.mjs / m2-nomerge.mjs / m3-body-nonorm.mjs (нужны ссылки kopia/node_modules и kopia/core)
FIX: Добавить в --proba. Контроль «роли»: page — вхождения story-row переставлены, в HTML ряд max-payne-4 перенесён в начало — замечаний 0. Порча: та же перестановка при старом HTML — «порядок рядов». Проверку слияния самой сверки на старте (yamlParse('a: &a {x: 1}\nb: {<<: *a}', {merge: true}).b.x === 1, иначе код 2) — или перейти на parseFrontmatter по P2-R3-SVERKA-3. Порчу `src` с `/../` (причина «ключи ?»). Порчу последнего кандидата srcset героя /max-payne-3/, у которого три кандидата. Контроль, где в копии содержания у body[0], year и meta ряда стоят NBSP, перевод строки и края. Порчу «второй div.foto.kadr-ryadu» (причина «кадров ряда 2»). В докладе назвать класс мутаций: «снятие zam.push; условия и разбор не мутировались».

## P2-R3-SVERKA-5 [safe] proby-p2.mjs:54-62 (ПРЕДЕЛЫ) против :368 (ноты берутся из всего html), :280 (--fokus — первое вхождение в style), :307 (лид), :324-330 (блок byline), :352-353 (абзацы только внутри .layer__body до первого </div>)
CLAIM: Часть дыр из списка P2-R2-SVERKA-5 так и не названа в шапке, хотя FIX раунда 2 просил сузить предел. Не названы: нота, перенесённая из подвала в <main> (в шапке «нота подвала», но ищется она по всей странице); второе свойство --fokus в style (браузер берёт последнее, сверка — первое); лид с hidden; лишний текст и второй byline__author в подписи; лишний абзац в ряду вне .layer__body или после вложенного </div> в теле; второй h2 в ряду. Предел «дубли элементов» с примерами покрывает лишь часть этих случаев. Маршрут ничего из этого сегодня не печатает, поэтому ложного прохода на настоящей сборке нет.
EVID: exp.mjs и exp2.mjs на dist-r3, во всех случаях 0 замечаний (ТИШИНА): «нота перенесена из подвала в <main>», «второй --fokus в style», «лид с hidden», «лишний текст в byline», «второй byline__author», «лишний абзац после вложенного </div> в .layer__body», «лишний абзац в .layer__text после тела», «второй h2 в ряду», «вторая img в рамке кадра ряда». Ловятся: data-href и data-id перед настоящим атрибутом, `..` в src, base-путь (громко), band-x вместо band, второй div.foto.kadr-ryadu, вложенная <section> в теле ряда с кадром.
REPRO: node exp.mjs --tolko-dist --dist <dist-r3>; node exp2.mjs --tolko-dist --dist <dist-r3> (scratchpad/sud-r3/sverka/, вывод exp-vyvod.txt)
FIX: Дописать в ПРЕДЕЛЫ одну строку: «место ноты (подвал), hidden, повтор CSS-свойства в style, лишний текст вне сверяемых элементов (в byline, в ряду вне .layer__body) не судятся». Дешёвая правка — искать ноты в <footer>, а не во всей странице.

## P2-R3-SVERKA-6 [safe] proby-p2.mjs:356 (замечание «поле ряда пусто у сверки») и порча :492
CLAIM: Ветвь :356 на законном содержании недостижима, а порча к ней моделирует содержание, которое схема отвергает: year '  ' (tekst() = trim().min(1)). Если разбор фронтматтера разойдётся с Astro, пустое у сверки поле уже ловят сравнения year, title, meta и абзацев: напечатанный текст непуст. Вред нулевой — это второе сообщение, а не единственная защита. Это тот же класс, что контроль NBSP в dateLabel из P2-R2-SVERKA-8(в).
EVID: content.config.ts:49, 157-160: year, title и meta — tekst(), body — nonempty массив tekst(). m2-nomerge на варианте merge даёт «класс band …», а не «поле пусто». Мутанты «пусто: без проверки абзацев» и «пусто: только year» выживают (mut-usl-vyvod.txt).
REPRO: Чтение content.config.ts:49, 157-160 и proby-p2.mjs:356, 492
FIX: Оставить и назвать в комментарии, что это страховка сообщения, а не сторож. Либо убрать ветвь вместе с порчей, если сверка перейдёт на parseFrontmatter.

### checked_ok
- Закрытие раунда 2, SVERKA-1. Ряды сверяются по месту и по числу, порядок равен порядку печати маршрута. На законных сборках копии сайта сверка зелёная: ряд с ролью в середине файла, story-row#max-payne-4 первым в blocks[], роль '' в структуре, flip без кадра, абзац блоком | с двойным пробелом, band через слияние <<: *rd. Всё это — 8/8 «всё как ждали» при `astro build` с кодом 0. Порчи «ряд удалён», «абзац сокращён», «абзац удалён», «переставлены», «id другой», «year», «заголовок», «meta», band, flip, bez-kadru и кадр ряда ловятся своей причиной
- Закрытие SVERKA-2. Порча «копия подписи в рамке арта» оставляет p.podpis-geroya на месте и срабатывает только в ветви :274 (метки — только L274)
- Закрытие SVERKA-3. Порчи «h1 снят», «h1 вынесен из героя», «картинка героя снята», «srcset героя снят», «текст подписи кадра другой» есть, каждая срабатывает в своей ветви
- Закрытие SVERKA-4. proby-p1 на dist-r3 кончается ПЛОХО только на /remake/ (нота), как и сказано в шапке. Кадры рядов теперь судит сама пачка 2 (src, каждый srcset, alt, класс bez-kadru)
- Закрытие SVERKA-6. yaml 2.9.0 поддерживает { merge: true }. На всех восьми файлах содержания разбор равен js-yaml 4.3.2 загрузчика Astro и parseFrontmatter самого Astro. Трудные случаи слияния (явный ключ до и после <<, список, обратный список, вложенное слияние, два << подряд), >-, |-, |+, CRLF, yes/on, восьмеричные, __proto__ дают одинаковый результат
- Закрытие SVERKA-8. «R23 R24 R13 R22 --dist <папка>» в копии сайта: пробы 4/4, затем сверка указанной папки, значение --dist за имя пробы больше не считается (строчные имена тоже принимаются). В строке вывода теперь «подпись кадра» и «byline» раздельно. Контроль byline — двойной пробел автора, а не NBSP в dateLabel
- Причины порч. Разметка замечаний номерами строк (tagged.mjs) показывает, что у каждой из 71 порчи причина — подстрока замечаний ровно одной ветви sverka. Шесть контролей дают 0 замечаний. Вывод копии на dist-r3 совпадает с журналами ведущего: сверка 8/8, проба 77/77
- Мутационное испытание ведущего повторено своей копией на dist-r3: снятие любого zam.push убивается своей порчей. Двойные мутанты из снятий push не выживают, потому что причина каждой порчи привязана к одной ветви
- vKonce `([^<]*(?:<(?!\/?p\b)[^<]*)*)<\/p>\s*$` — возврата с катастрофическим ростом нет: 10^6 знаков без </p>, с <i> через каждые 5 знаков, с тысячами </p> и с 10^6 пробелов перед хвостом — от 0,2 до 4,8 мс. <br>, <i> и <pre> внутри подписи принимаются (названный предел «теги внутри текста»), вложенный <p> и </P> отвергаются
- atr() с (?<=\s) — перевод строки, таб и сущности в значении работают (x&amp;y → x&y). data-href, data-id и data-style перед настоящим атрибутом не сходят за него. Одинарные кавычки и значение без кавычек дают громкую тревогу — Astro так не пишет. Значение, кончающееся на « src=» и стоящее раньше настоящего атрибута, обманывает поиск, но на разметке маршрута это недостижимо: src и srcset идут раньше alt, а остальные значения ограничены схемой
- klyuchAdresa: ?query и #фрагмент после имени — ключ верный; путь с `/../`, свой абсолютный адрес, чужой хост, base-путь и заглавные — «?», то есть громкая тревога. У сайта base и assetsPrefix не заданы
- Шапка: «удалённый, сокращённый или переставленный ряд старой сборки — отказ» верно (порчи и опыты на старом HTML). Неверна только отсылка про related и cta (P2-R3-SVERKA-1) и «сколько рядов в файле» (P2-R3-SVERKA-2)
- Повтор id рядов и ряд с ролью, не объявленной страницей, отвергает сборка: сторож anchors «zdublowany id» и отказ маршрута «Ряды без своего вхождения story-row»
- Процесс. Опыты шли только в scratchpad/sud-r3/sverka/: копия инструмента (правка одна — путь к пакету yaml через createRequire, для копии сайта ещё root), копия сайта kopia/sites/7thserpent.com со своим git, кэши Astro и Vite — в копии. Ссылки kopia/node_modules и kopia/core сняты через rmdir, цели целы. Браузер и сервер не понадобились, порт 4472 не поднимался. В репозитории я ничего не менял. В git status появилась чужая неотслеживаемая папка docs/reports/2026-09-26-7thserpent-pachka-2/sud/ — её создал не я

# KONTRAST — находок 4
## P2-R3-KONTRAST-1 [defect] docs/reports/2026-09-26-7thserpent-pachka-2/instrumenty/kontrast-geroy-snyatie.js:33-36 (п. 5 шапки: «на время проверки всем элементам и их псевдоэлементам ::before/::after возвращается pointer-events»), :40-42 (ПРЕДЕЛЫ: названо только встроенное !important), :120 (стиль проверки '*,*::before,*::after{pointer-events:auto!important}')
CLAIM: KR2-1 закрыт лишь частично, а шапка утверждает неверное. Правило проверки имеет специфичность универсального селектора (0,0,0 / 0,0,1). Поэтому его перебивает любое `pointer-events:none!important` в листе, если у селектора есть класс, а также `!important` внутри @layer: в каскаде у важных объявлений слои сильнее неслоёных. Сайт уже собирается со слоями Tailwind (@layer theme, base, components, utilities). Скрим `.hero::after{…;pointer-events:none!important}` или слой `.porcha{pointer-events:none!important}` проходят съёмку с «ok». Вычислитель при этом даёт 8,03 для лида, а нарисовано 2,14–2,20:1. В ПРЕДЕЛАХ назван только встроенный `style="…!important"`, а пояснение «встроенное !important сильнее листа» создаёт впечатление, что листовое !important проверка перебивает.
EVID: /remake/ 800x900, дословная копия тела съёмки 6c127da, dist-r3, свой контекст, сервер 4473. Порча a5 `.hero::after{content:'';position:absolute;inset:0;z-index:5;background:rgba(13,12,10,.6);pointer-events:none!important}` дала «ok: сборка e35cab8». Порча a6 (div.porcha с правилом листа `.porcha{pointer-events:none!important}`) — «ok». Порча a7 (`@layer z{.hero::after{pointer-events:none!important}}` при неслоёном ::after) — «ok». Копия вычислителя на всех трёх: заголовок 14.68, лид 8.03, кнопка 15.70, подпись 8.33, итог ≥4,5. Замер нарисованного (кадр с текстом против кадра без него, светлейший пиксель букв) для a5 и a7: лид 2.14–2.20:1, светлейший L букв 0.070 при объявленном 0.400. Без !important те же псевдоэлементы ловятся (см. checked_ok). На текущих страницах `pointer-events:none!important` нет (grep по _astro/*.css и HTML четырёх страниц), поэтому вердикт r3 не задет.
REPRO: Скрипты в scratchpad/sud-r3/kontrast/: node sluga.mjs <dist-r3> 4473 <out>; тело h-run1.js (сценарии a5, a6, a7 из sc1.json) передать в browser_run_code_unsafe; затем node rasklad.mjs, node kopiya-kontrast-art.mjs out/a5-after-hero-pe-none-important/remake out/a5-after-hero-pe-none-important/remake/boxes.json; нарисованное — out/nar/n-a5-important и n-a7-layer, node narisovano.mjs
FIX: Поправить п. 5 шапки: pointer-events возвращается только тем слоям, у которых нет собственного `!important`. В ПРЕДЕЛЫ добавить: «`pointer-events:none!important` в листе с селектором сильнее `*` или в @layer — у элемента и у псевдоэлемента». Для элементов можно усилить проверку: в JS проставить всем `el.style.setProperty('pointer-events','auto','important')`, а после проверки снять. Для псевдоэлементов лекарства внутри проверки попаданием нет, их закрывает только пиксельная проверка (KR2-2). Правка инструмента — снова «судью судят», поэтому при заморозке достаточно правки текста.

## P2-R3-KONTRAST-2 [safe] kontrast-geroy-snyatie.js:40-50 (перечень ПРЕДЕЛОВ п. 5: «каждый подтверждён опытом и даёт ложный проход»)
CLAIM: Перечень пределов неполон. Ещё пять случаев, подтверждённых опытом, дают «ok» при приглушённых буквах, и в шапке их нет. (1) Цвет в `::first-line` или `::first-letter`: нарисованный цвет не равен `color` носителя, класс text-fill-color. (2) `-webkit-text-stroke` тёмного цвета. (3) `mask-image` / `mask` у носителя или у предка: частичная прозрачность, которую проверка opacity не видит. (4) `filter` у самого носителя: в перечне названы только filter «предков». (5) `outline` соседа поверх букв: outline, как и box-shadow, в попадании не участвует. Заключение шапки «пределы надо закрывать пиксельной проверкой» остаётся верным, но перечень подан как полный.
EVID: /remake/ 800x900, дословная копия съёмки: все пять — «ok», копия вычислителя на лиде даёт 7.41 (outline — 8.03). Нарисовано (светлейший L букв против объявленного 0.400): ::first-line #3a3d42 — первая строка 1.60:1 (L 0.046), остальные строки 7.41–8.09; text-stroke 3px #0d0c0a — 1.00:1 (L 0.005); mask-image 30 % у .hero__lead — 1.74–1.80:1 (L 0.055–0.058), у .hero__text — тоже «ok»; filter:brightness(.35) у .hero__lead — 1.55–1.70:1 (L 0.044); outline 3000px rgba(13,12,10,.6) — 1.40–1.46:1 (L 0.035). ::first-letter у заголовка — «ok», вычислитель 11.93. Скан собранного CSS и HTML четырёх страниц: first-line, first-letter, text-stroke и mask — 0; outline только в :focus-visible и ссылке «к содержанию» (при съёмке не активны). Вердикт r3 не задет.
REPRO: sc1.json сценарии a8-first-line-color, a9-first-letter-title, b1-outline-soseda, b2-mask-nositelya, b3-mask-predka, b4-text-stroke, b5-filter-nositelya через h-run1.js; нарисованное — out/nar/n-a8-first-line, n-b1-outline, n-b2-mask, n-b4-stroke, n-b5-filter-sam + narisovano.mjs
FIX: Дописать пять случаев в ПРЕДЕЛЫ п. 5. Либо заменить перечень общим правилом: «проверка ловит только накрытие элементом, принимающим попадание, opacity предков и альфу `color`; любое иное свойство, меняющее нарисованные пиксели букв (::first-line/::first-letter, text-stroke, text-fill-color, mask, filter самого носителя и предков, blend, outline и box-shadow соседей, !important у pointer-events), не видно». Закрывает всё сразу пиксельная проверка из FIX KR2-2.

## P2-R3-KONTRAST-3 [safe] kontrast-geroy-snyatie.js:55-59 (п. 6: «…раздаваемая копия побайтно равна dist/ сборки коммита SBORKA (diff -r)… оба итога — в файлах замера»); zamery/kontrast-geroy/r3-e35cab8-*.txt, строка 1
CLAIM: Шапка обещает, что оба итога цепочки лежат в файлах замера. Итог proby-p2 там есть: proby-p2-r2.txt:26 «HEAD e35cab8, src и structure совпадают с HEAD». Итога `diff -r` нет: первая строка r3-файлов только утверждает «побайтно = dist/ коммита», вывода сравнения или хешей в папке замеров нет. Кроме того, «чистое дерево» в сверке proby-p2 покрывает только src и structure. core, public и astro.config цепочка не судит (в скобках шапки это названо). Сама цепочка на деле верна: я проверил её независимо.
EVID: Сборка копии e35cab8 (git archive e35cab8 sites/7thserpent.com в свою папку, node_modules и core — соединениями только для чтения, astro build, exit 0, после сборки соединения сняты, цели целы): 108 файлов побайтно равны dist-r3 за вычетом CSS. HTML отличается только именем CSS-файла (SmartImage.7MPk1Dfj против BMbqpUCi). CSS после замены путей шрифтов равен побайтно. Различие путей — артефакт моей копии: шрифты @fontsource через соединение на другом диске не были выпущены в _astro, отсюда 8 файлов woff/woff2 только в dist-r3. Сервер 4416 отдаёт /max-payne-1/, -2/, -3/, /remake/ и CSS побайтно равными dist-r3. git diff e35cab8 HEAD -- core sites пуст, git status — только неотслеживаемые папки docs/reports. Grep «diff -r|dist-r3» по докладу: вывода сравнения нет, только строка-утверждение.
REPRO: git archive e35cab8 sites/7thserpent.com | tar -x -C <папка>; соединения node_modules и core на репозиторий; astro build в копии; сравнить с dist-r3 после нормализации путей шрифтов (scratchpad/sud-r3/kontrast/kopiya, build-kopiya.log)
FIX: Положить в zamery/kontrast-geroy/ итог сравнения: вывод `diff -rq <dist коммита> <раздаваемая копия>` (пустой) или список sha256 файлов обеих папок. Либо в п. 6 заменить «оба итога — в файлах замера» на «итог proby-p2 — в proby-p2-r2.txt, равенство копии — заявление ведущего».

## P2-R3-KONTRAST-4 [safe] kontrast-geroy-snyatie.js:83-89 (ОКНА, наследованы копией: минимум 360); zamery/kontrast-geroy/r3-e35cab8-remake.txt:1 («text-wrap: balance»)
CLAIM: text-wrap: balance у подписи кадра меняет разбиение только ниже 375 px, а полоса из 31 окна начинается с 360. Ширины 320–359 (например, iPhone SE первого поколения, 320) замер не покрывает. Опыт показывает, что вердикт при этом верен: на 320 и 340 подпись не хуже 6.67:1, а это выше худшего числа полосы 5.38. На 320x568 окно слишком низкое для заголовка: вычислитель называет его строки «срезана/вне кадра». Если добавлять 320 в полосу, высоту брать ≥ 640.
EVID: Копия съёмки на /remake/ при 320x568, 320x640, 340x700, 360x740, 375x667. Подпись на 320–360 занимает две строки balance (12+163 px и 12+183 px, y 159.5 и 178.4), на 375 — одну (14+349). Вычислитель: подпись — худший 6.67:1 (320x568, y 178.4), p99 ≥ 7.07; лид 8.39, заголовок и кнопка 16.17 (на ≤480 текст стоит на сплошном --bg). Две ошибки замера на 320x568: строки заголовка y 536.3 и 568.2 срезаны или вне кадра.
REPRO: Сценарий «uzkie» (окна 320x568, 320x640, 340x700, 360x740, 375x667) на /remake/; node rasklad.mjs; node kopiya-kontrast-art.mjs out/uzkie/remake out/uzkie/remake/boxes.json
FIX: Правка не нужна. Строкой в доклад: «ширины ниже 360 полоса не мерит; разовый замер 320 и 340 — подпись 6.67:1». Если расширять полосу, то окнами 320x640 и 340x700.

### checked_ok
- KR2-1 закрыт для обычного случая. Скрим-псевдоэлемент с собственным pointer-events:none без !important теперь даёт СТОП: .hero::after и .hero::before — «накрыт: сверху section.hero», .hero__inner::after — «сверху div.hero__inner.container», .hero__text::after — «сверху div.hero__text» (/remake/ 800x900, дословная копия тела 6c127da, dist-r3, свой контекст, сервер 4473)
- Другие псевдоэлементы и слои. ::backdrop модального dialog с pointer-events:none — СТОП «сверху html» (модальность делает остальное инертным). ::marker соседнего li поверх заголовка — СТОП «сверху li». ::placeholder к героям неприменим. SVG-слой с атрибутом pointer-events="none" — СТОП «сверху rect». Слои canvas, iframe и position:fixed вне .hero с pointer-events:none — СТОП (canvas.porcha, iframe.porcha, div.porcha). content-visibility:hidden у лида или заголовка — СТОП «сверху div.hero__text»
- Названные пределы подтверждены опытом: проверка попаданием пропускает встроенный style pointer-events:none!important, атрибут inert, -webkit-text-fill-color, ::after самого носителя (.hero__title::after 80 %) и box-shadow соседа (0 0 0 3000px) — во всех пяти случаях «ok»
- transform: scale(1.05) у .hero__inner — не обход: рамки getClientRects учитывают преобразование, вычислитель даёт законные 11.93, 7.25, 15.23, 8.28. Светлая text-shadow соседа поверх заголовка проверку попаданием проходит, но остаётся в кадре фона, и вычислитель ловит её сам: заголовок 1.00:1, «Ниже 4,5:1 — 1»
- Форматы цвета. rgb(161 171 179 / 1), rgba(161,171,179,1.0), #a1abb3ff, currentColor и hsl(206 12% 67%) — «ok» без ложной тревоги (Chromium отдаёт rgb()). color(srgb 0.631 0.671 0.702), color(srgb 1 1 1 / 1) и oklch(…) — СТОП «не в формате rgb() — вычислитель его не прочтёт». transparent — СТОП «альфа цвета rgba(0, 0, 0, 0) меньше 1». rgb(… / .5) — СТОП с альфой 0.5. rgb(… / .998) — СТОП «rgba(161, 171, 179, 0.996)»: 8-битное округление альфы, рисуется не непрозрачно, отказ верен. Сообщения разделены, как требовал KR2-5; контроль с законным rgb проходит
- Стиль проверки снимается до снимка (proverka.remove() на :157 раньше addStyleTag скрытия на :159). Повторный контроль даёт 0 различных субпикселей в четырёх кадрах, boxes.json равны
- Наведение мыши на кадр не влияет. Мышь, поставленная до съёмки на кнопку, подпись, шапку или лид: 0 различных субпикселей в 4×4 кадрах и равные boxes.json. После page.goto состояние :hover в безголовом Chromium не восстанавливается (matches(':hover') = false и после goto, и после прокрутки), а съёмка перезагружает страницу на каждом окне. :hover у .btn-secondary меняет только фон (rgba(229,234,238,.06)) и рамку: фон скрыт вместе с колонкой, цвет букв rgb(229, 234, 238) тот же. Ссылка шапки при наведении цвет не меняет
- Воспроизводимость r3. /max-payne-2/ и /remake/ на 8 окнах (360x740, 390x844, 414x896, 480x800, 641x900, 800x900, 1025x900, 1440x900): 56 кадров попиксельно равны .playwright-mcp/ka-p2r3, 139 строк совпадают по x, y, w, h, тексту, цвету и худшему пикселю до сотых. Подпись 5.38 на 390x844 — число в число с r3-e35cab8-remake.txt; /remake/ 360x740 (две строки balance) — 6.84
- Цепочка SBORKA на деле. Моя сборка e35cab8 равна dist-r3 (108/108 файлов, CSS — после нормализации путей шрифтов моей копии); сервер 4416 отдаёт четыре страницы и CSS побайтно как dist-r3; core и sites в e35cab8 = HEAD; в proby-p2-r2.txt:26 записано «HEAD e35cab8, src и structure совпадают с HEAD»
- KR2-7 соблюдён в r3: в ka-p2r3 ровно 94/94/94/125 файлов (31×3+1 и 31×4+1), папка создана в 11:50:10, кадры сняты в 11:50:15–11:56:13, boxes.json записаны в 11:58:18–19, после всех кадров
- На текущих четырёх страницах нет обходов из P2-R3-KONTRAST-1/2: в собранном CSS и HTML нет pointer-events:none!important, first-line, first-letter, text-stroke и mask; outline есть только в :focus-visible и ссылке «к содержанию». Вердикт r3 «все места ≥4,5:1» в силе
- Процесс: всё в scratchpad/sud-r3/kontrast/ (gen.mjs, sc1.json, sc2.json, h-run1.js, h-run2.js, rasklad.mjs, pikseli.mjs, narisovano.mjs, sravni.mjs, kopiya/, out/). Браузер — только в своих контекстах, общая вкладка не открывалась. Сервер 4473 остановлен. Соединения node_modules и core в копии сняты rmdir, цели целы, кэши Astro и Vite остались в копии. dist-r3 не менялся. git status репозитория — только неотслеживаемые папки docs/reports (kadry, sud, zamery — их создал ведущий)

# FAKTY — находок 5
## P2-R3-FAKTY-1 [defect] sites/7thserpent.com/src/content/tresc/max-payne-2.md:117 (ряд physics, абзац 2, введено e35cab8): «Officially it needs no more than DirectX 8.1»
CLAIM: Правка R2-FMP2-1 заменила синонимами и сам факт. У Википедии «the game only supports DirectX 8.1» — это об API, на котором работает рендер. Новый текст говорит о требованиях к системе («needs no more than») и вдобавок называет их официальными. Официальных требований Max Payne 2 в корпусе нет, а все сторонние листинги требований называют минимумом DirectX 9. Так ещё и появилось прямое противоречие внутри сайта: /pc/ в ряду «What Max Payne 2 needs» называет минимумом «DirectX 9.0», а /max-payne-2/ теперь пишет, что игра «needs no more than DirectX 8.1». Правило брифа п. 1 («A fact not confirmed by the corpus or §2 is not printed») нарушено дважды: «Officially» и «needs».
EVID: Единственный источник о 8.1 — 02-en.wikipedia.org.lines:70: «Even though the game only supports DirectX 8.1, the graphics in Max Payne 2 mimic those generated by DirectX 9». Поиск по видимому тексту всего сырого корпуса (612 документов, scratchpad/sud-r3/fakty/grepraw.mjs, вывод в dx-all.txt): «DirectX 8.1» у Max Payne 2 встречается только в этой строке Википедии. Требования к Max Payne 2 во всех листингах: n=484 gamesystemrequirements «DX: DirectX 9.0» (минимум и рекомендуемые), n=488 systemrequirementslab «64 MB DirectX 9 compatible», n=486 pickgamer «DirectX 9 compatible … PIXEL SHADERS: 2.0», n=895 eneba «64MB DirectX 9». Лист фактов fakty/mp2.md:81 говорит «supports only DirectX 8.1», а :207 относит к not_found «Official PC system requirements (beyond 'DirectX' and the EULA)». docs/UNRESOLVED.md, строка /pc/ 2026-09-25: «официальных требований Max Payne 2 … только у трёх сторонних сайтов, их и печатаем без слова «official»». pc.md:27 title «What Max Payne 2 needs», :34 «DirectX 9.0». §2 (CLAUDE.md сайта) о DirectX ничего не говорит. Раунд 2 уже назвал кажущееся противоречие страниц (R2-FMP2-7, safe). Теперь оно стало буквальным: на двух страницах один глагол «needs» и разные числа.
REPRO: node C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/3579b79f-5acf-478f-90e1-ee2488a3d042/scratchpad/sud-r3/fakty/grepraw.mjs "Direct ?X[^A-Za-z]{0,12}(8|9)(\.\d)?[a-c]?" 90 — затем взять из вывода строки с max-payne-2, fall-of и 12150. Сравнить pc.md:27-34 и max-payne-2.md:117.
FIX: Вернуть утверждение об API и убрать «Officially»/«needs». Например: «It is a DirectX 8.1 game, yet ghosting, shader effects, reflections and refraction are pushed far enough that the picture looks like DirectX 9 work.» Или «The renderer works through DirectX 8.1 only, yet …». После правки снова прогнать ngram-stem.mjs 6–8 stem и chuzhie-p2.mjs. Строку R2-FMP2-7 (/pc/ DX 9.0 против /max-payne-2/ DX 8.1) записать в доклад: после правки это разные вещи, требования и API, и противоречия нет.

## P2-R3-FAKTY-2 [safe] sites/7thserpent.com/src/content/tresc/max-payne-2.md:117-123 (ряд physics, абзац 2) против 02-en.wikipedia.org.lines:70-72
CLAIM: Лексически абзац теперь свой: со срезом окончаний нет общего отрезка даже в 3 слова. Но структура по-прежнему идёт за Википедией предложение в предложение и клауза в клаузу: DX 8.1 → «похоже на DX 9» → перечень эффектов → «разработчики выделили сцену» → осознанные сны, картинка размыта и не в фокусе → больше полигонов, края глаже → больше выражений лица. Совет FIX раунда 2 («не повторяя порядок и перечень исходника») исполнен только для порядка слов в перечне. Сама замена синонимами («only supports» → «needs no more than») и породила дефект P2-R3-FAKTY-1. Правила §5 по n-граммам формально соблюдены.
EVID: Самый длинный общий отрезок после среза окончаний — меньше 3 слов (скрипт сравнения абзаца с 02:70-72). Общие леммы, кроме служебных: directx, 8.1, 9, effect, reflection, refraction, shader, ghosting, developer, scene, lucid, dream, focus, polygon, character, expression. Пары: «considered one particular scene in which effects are used well» ↔ «named … as a scene where the effects pay off»; «the screen appears fuzzy and out of focus» ↔ «the whole picture turns hazy and loses focus»; «polygon count … increased, which smooths out the edges» ↔ «more polygons than before, so their outlines look smoother».
REPRO: node sud-r2/fakty-mp2/ngram-stem.mjs dist-r3/max-payne-2/index.html 5 stem — в абзаце только «in the first game max» и «expression in the first game»; сравнить построчно max-payne-2.md:117-123 и 02-en.wikipedia.org.lines:70-72.
FIX: При правке по P2-R3-FAKTY-1 можно заодно разорвать параллель: например, начать с осознанных снов как примера, а DX 8.1/9 дать одним придаточным. Не обязательно, n-граммы чисты.

## P2-R3-FAKTY-3 [safe] sites/7thserpent.com/src/content/tresc/max-payne-3.md:89-91 (ряд rating): «and others in the same thread named the chapters where it shows up»
CLAIM: Число исправлено: «one player» верно, это sigina. Но «named the chapters» с определённым артиклем подаёт как согласие то, в чём ветка расходится. Двое говорят об одной сцене или одном уровне (sigina «only one scene», Pierce Dalton «only in one level»), Hackie P4ckie называет миссии 7 и 11 с оговоркой «The only missions I can think of», LArc7thHeaven — «Panama and brothel scene». Прямой неправды нет, для родителей смысл не искажён.
EVID: 08-steamcommunity.com.lines:8 (sigina, 23.03.2021: «Unfortunately you can not turn it off but there's only one scene with nudity»), :10 (Pierce Dalton: «nudity/sex only in one level»), :14-15 (LArc7thHeaven, 24.03.2021), :20 (Hackie P4ckie, 25.03.2021: «the 7th and the 11th»). Все сообщения — март 2021 года, одна ветка; в снимке 15 из 33 комментариев.
REPRO: Прочитать scratchpad/korpus/max-payne-3/08-steamcommunity.com.lines, строки 8-25.
FIX: По желанию: «…and others in the same thread named one or two chapters where it shows up». Не обязательно.

## P2-R3-FAKTY-4 [safe] sites/7thserpent.com/src/content/tresc/max-payne-1.md:52-53 («can be played on PS5 too»); remake.md:97 («notes that it can be played on PS5 as well»)
CLAIM: Витрина PS Store США пишет «playable on PS5» с оговоркой в том же предложении: «some features available on PS4 may be absent». Обе страницы оговорку опускают. Поскольку на /max-payne-1/ в той же клаузе стоят трофеи, читатель может решить, что трофеи и прочее на PS5 гарантированы. Прочее в клаузе верно: «adds trophies and 1080p rendering» = «full 1080p up-rendering and enhanced features such as Trophies».
EVID: sud-r2/fakty-ost/ps-64.txt (n=64, Region: United States): «Experience Max Payne® for PS2™ with full 1080p up-rendering and enhanced features such as Trophies, Shareplay, Remote Play…», «Although this game is playable on PS5, some features available on PS4 may be absent». ps-483.txt совпадает с ps-64 побайтно (cmp IDENTICAL).
REPRO: Прочитать ps-64.txt; сравнить с max-payne-1.md:49-55 и remake.md:96-97.
FIX: По желанию: «…and runs on PS5, though the store warns some PS4 features may be missing». Не обязательно.

## P2-R3-FAKTY-5 [safe] docs/UNRESOLVED.md, строка 2026-09-26 /max-payne-2/: «(только страница GOG Dreamlist с призывом «Vote to bring this game to GOG», как у первой игры)»
CLAIM: «как у первой игры» неточно: у первой игры в корпусе две страницы GOG, Dreamlist n=383 и прежний Community wishlist n=384 (так записано и в строке /max-payne-1/). У второй игры — только Dreamlist n=388. На вывод это не влияет.
EVID: manifest.jsonl: n=383 dreamlist/game/max-payne, n=384 wishlist/games/max_payne («+25 832 Max Payne» — только первая игра), n=387 remake, n=388 max-payne-2-the-fall-of-max-payne, n=389 max-payne-3. Фраза «Vote to bring this game to GOG and help preserve it» есть в gog-383/387/388/389.
REPRO: grep -i gog.com по manifest.jsonl; sud-r2/fakty-ost/gog-*.txt
FIX: «…только страница GOG Dreamlist с призывом «Vote to bring this game to GOG» (у первой игры есть ещё прежний wishlist)» или просто убрать «как у первой игры». Не обязательно.

### checked_ok
- dist-r3 собран из e35cab8: все четыре новые формулировки напечатаны («Officially it needs», «remaking both games from scratch», «others in the same thread», «1080p rendering»), старых («every report covered», «rebuilt from the ground up», «each made up of», «imitate DirectX», «ported it to Xbox») нет
- R2-FMP2-1 закрыт лексически: со срезом окончаний у абзаца о DirectX нет общего отрезка с Википедией даже в 3 слова; 5-граммы со срезом дают только «in the first game max» и «expression in the first game». Дефект фактов, внесённый этой правкой, — P2-R3-FAKTY-1
- R2-FMP2-2 закрыт: «split into three parts» = IGN 06:4 (Walkthrough: Part One / Part Two / Part Three) и блог PART 1; «in several levels of parts two and three you play as Mona» = 02:23 («several levels from the second and third chapters»); слово chapter на странице /max-payne-2/ больше не встречается
- R2-FMP2-3 закрыт: «which came out within seven weeks of the PC version» верно по инфобоксу 02:3 и meta ряда: NA от ПК 15.10 до Xbox 25.11 — 41 день, до PS2 02.12 — 48; PAL от 24.10 до 05.12 — 42; витрина Xbox 12/3/2003 — ровно 49, то есть тоже «within»
- R2-FMP2-4 закрыт: «“an okay job”» в кавычках, атрибуция — рецензент New Zealand Herald (02:83 таблица, 02:86-87)
- R2-FMP2-6 закрыт: номинации вынесены отдельным предложением; «nominated five times, Game of the Year among them» = 02:96, пять категорий (GOTY, Computer GOTY, Computer Action/Adventure, Console Action/Adventure, Character Performance – Male за McCaffrey — Википедия относит их к игре); награды — Golden Satellite 2004, Editors’ Choice GamePro/IGN/GameSpy/GameSpot, Game of the Month Game Informer — верно
- R2-FO-1 закрыт: каждый из трёх отчётов подтверждён. Август 2025 — PSU 12.08.2025 дословно цитирует отчёт Remedy «remains in full production» (n=316, «Source – Remedy Financial Report»). Май 2026 — RockstarINTEL 05.05.2026 05:18 «still in full production» по Business Review Q1 2026. Август 2026 — Wccftech 11.08.2026 08:4 «reiterated that the remake is in full production», ResetEra 13:10. Первый источник — цитата отчёта, два других — пересказ изданий; у самих отчётов Remedy в корпусе копий нет, но все три пересказа согласны, §2:122-125 не противоречит
- R2-FO-3 закрыт: «Remedy is remaking both games from scratch on Northlight, its in-house engine» = AltChar 09:3 и Push Square («ground up», «proprietary»), Remedy 12:5-6 (Northlight developed by Remedy, remake as a single title); 7-граммы со срезом окончаний на /remake/ — только названия игр и платформ
- R2-FO-4 закрыт в части числа: «one player on the Steam forums in 2021» = sigina, 23.03.2021 (08:8); остаток — P2-R3-FAKTY-3
- R2-FO-5: «adds trophies and 1080p rendering» и «playable on PS5» — дословно на витрине США n=64 (= n=483, cmp IDENTICAL); индийская n=69 — PS4, EP1004-CUSA04488
- /remake/ в dist-r3: «as of September 2026» есть в каждом абзаце тела (11 абзацев рядов, лид, призыв) и во всех шести meta рядов; без даты только подпись кадра и надзаголовки t-label
- UNRESOLVED R2-FO-2: во всех четырёх страницах Dreamlist напечатано «Vote to bring this game to GOG and help preserve it» (gog-383/387/388/389). У n=384 «+25 832 Max Payne». Страниц магазина GOG в манифесте нет (dreamlist ×4, wishlist ×1, reddit robots-disallowed). Слова gog нет в ключах брифов max-payne-1/2/3 и remake
- UNRESOLVED «списка трофеев не даёт никто»: все трофейные адреса о первой игре в манифесте — http-error (psnprofiles 344/347, truetrophies 351, exophase 348, playstationtrophies 345, fandom 343), reddit 350 — robots-disallowed; плейлист n=279 и IGN n=349 — о Max Payne 3
- Сторож chuzhie-p2.mjs на dist-r3: 0 строк с чужой последовательностью по семи страницам, код 0. Со срезом окончаний (ngram-stem.mjs): при N=8 — только названия игр и цитата Take-Two в кавычках («the continued disappointing sale of max payne 2»); при N=7 — ещё имена и организации («the academy of interactive art science nominated» — неизменённый ряд cast), «use of drug and alcohol online interaction» (дескрипторы ESRB), «limit set in the nvidia control panel» (/pc/, пачка 1). Вывод — scratchpad/sud-r3/fakty/ngram78-stem.txt, ngram6-stem.txt, ngram5-stem.txt
- Окончания строк пяти изменённых файлов: i/lf w/lf attr/-text
- Процесс: опыты только чтением и скриптами в scratchpad/sud-r3/fakty/ (grepraw.mjs, выводы n-грамм), браузер и сервер не понадобились, порт 4474 не поднимался; репозиторий не тронут, в git status только неотслеживаемые папки ведущего (kadry/, sud/, zamery/)

