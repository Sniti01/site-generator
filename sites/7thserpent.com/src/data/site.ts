/**
 * Данные витрины каркаса (сессия 3, П62). Меню несёт только адреса, которые
 * есть в `dist/`: сторож `links` ядра, когда подключится вместе с деревом S2,
 * потребует от каждой внутренней ссылки адреса из структуры.
 */

export const site = {
  name: '7thserpent.com',
  tagline: 'An unofficial Max Payne fan site',
  lang: 'en',
};

export const nav = [{ label: 'Home', href: '/' }];
