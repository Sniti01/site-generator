# «Судью судят», раунд 3 (последний по П89) — сессия 14

Что судилось: правки раунда 2 (`acd33f6..734afa7`) — схема `cta.href`, сверка `dist` в `proby-p1.mjs`,
`proba-sverki-dist.mjs`, `href-re.mjs`, `vneshnie.mjs`, `chuzhie-teksty.mjs`, проза скрипта съёмки
контраста; тексты после правок раунда 2 (линза фактов). Три линзы — отдельные агенты, только чтение.

| Линза | Дефектов | Безопасных | Пределов | Прозы | Опровергнуто |
|---|---|---|---|---|---|
| схема и пробы p1 | 1 | 1 | 2 | 3 | 7 |
| инструменты | 3 (два — регрессии правки раунда 2) | 2 | 3 | 2 | 5 |
| факты текстов | 2 искажено (одно внесла правка раунда 2), 2 близко к источнику, 3 сомнительно | — | — | — | — |

**Сходимости нет** (раунды 1–3 у инструментов: 5 → 8 → 3 дефектов; у схемы и проб: 1 → 1 → 1). По П89
(«не больше трёх раундов; не сошлось — заморозка, таблица пределов, вопрос в докладе») инструменты приёмки
сессии и схема заморожены на `734afa7`; ни один найденный дефект не срабатывает на настоящей сборке, итоги
в `zamery/` не меняются (каждая линза проверяла это отдельно). Тексты — не судьи: их исправления раунда 3
внесены (`cdb7bc5`) и проверены сторожем 8-словных последовательностей (0) и сборкой (коридоры).

## Пределы после раунда 3 (заморожено на `734afa7`)

| Инструмент | Предел | Направление | Откуда |
|---|---|---|---|
| `proby-p1.mjs` (сверка `dist`) | свежесть — только по заголовкам рядов и призыва: сборка со старыми телами абзацев проходит; поэтому сверку запускать только сразу после `npm run build` последнего коммита (так и сделано) | ложное «ok» на старой сборке | R3-MARSHRUT-3 |
| `proby-p1.mjs` | кадр не привязан к своему ряду (сдвиг всех кадров на ряд — «ok»); кнопка призыва — первая в `<main>`, а не в `section.cta`; нота ищется по классу во всём HTML | ложное «ok» на подлоге | R3-MARSHRUT-4, -5 |
| `proby-p1.mjs` | YAML — пакетом `yaml`, у Astro — `js-yaml` (расходятся на ключе слияния `<<`); обход берёт файлы с точкой в начале имени | ложный отказ | R3-MARSHRUT-6 |
| схема `cta.href` | порт > 65535, `|`, `<`, `^` в хосте, неверный IPv4, невидимые знаки IDNA, полноширинные разделители проходят схему — сборку роняет `links` с ложной причиной «bez korzenia»; `HTTPS://`, IPv6, пустой порт — ложный отказ схемы | безопасное (отказ сборки) | R3-MARSHRUT-1, -2 |
| `vneshnie.mjs` | атрибут `style` разбирается только при `url(` или `@import` в сыром значении: `image-set(…)` и `u\72 l(…)` без `url(` не видны (регрессия раунда 2) | ложное «0» на подлоге | R3-INSTR-1 |
| `vneshnie.mjs` | комментарии CSS снимаются регуляркой до разбора: `/*` внутри строки или url прячет загрузку (регрессия раунда 2) | ложное «0» на подлоге | R3-INSTR-2 |
| `vneshnie.mjs` | подозрения в JS — только строки, которые разбирает `new URL` (`'https://%s/api'` — нет) | ложное «0» на подлоге | R3-INSTR-3 |
| `vneshnie.mjs` | `url(` в тексте любого атрибута (`alt`, `title`) считается загрузкой; список «свой абсолютный» — по сырой строке | ложный отказ / справочно | R3-INSTR-4, -8 |
| `vneshnie.mjs` | разбор тегов не по стандарту HTML (повтор атрибута, `<!-->`, `</script x>`, тип скрипта по всей строке атрибутов, meta refresh без `url=`, `on…=`, `srcdoc`, `data:`) | ложное «0» на подлоге | R2-INSTR-11, -12, -14 |
| `chuzhie-teksty.mjs` | строка, различная в двух вариантах снятия тегов, считается дважды; смешанный случай (разнесённые `<span>` и строчный тег посреди слова) не ловится ни одним вариантом; проба не различает варианты | счёт / ложное «0» на подлоге | R3-INSTR-5, -6, -7 |
| `chuzhie-teksty.mjs` | подпись «строк N (+M)» не учитывает строки второго варианта (номера идут дальше) | проза | R3-INSTR-9 |
| `href-re.mjs` | комментарий в строке 12 записан кодами `\u…` | проза | R3-MARSHRUT-7 |

Главный довод «внешних загрузок нет» держит браузерная проверка (`zamery/vneshnie-zaprosy.json`), не
статический разбор; «чужих формулировок нет» — сторож с проверенной пробой и двумя вариантами извлечения;
«новые ветви маршрута судятся» — 9 разовых проб, у каждой — своя причина отказа.

## Факты раунда 3 — внесены (`cdb7bc5`)

R3-FAKT-1: слова автора звукового фикса («later called that patch obsolete…») стоят сразу за его фиксом, а не
за методом Squeaky. R3-FAKT-2: совет из снятого гайда Citizen Cook (сглаживание и заставка) убран; вместе
с ним — совет FixItAll про F2 в панелях (снятый гайд). R3-FAKT-3: геймпад через Big Picture — «can work, though
some players report the game never sees the pad». R3-FAKT-5, -6: Days Gone — «as a maybe» с оговоркой автора
ответа; The Last of Us, The Suffering, The Darkness — «come close in gritty personal drama, but…». R3-FAKT-7:
две формулы, близкие к Википедии, переписаны. R3-FAKT-4 (звуковая карта тоже дана диапазоном) — текст её не
называет и ложного не утверждает; оставлено.
