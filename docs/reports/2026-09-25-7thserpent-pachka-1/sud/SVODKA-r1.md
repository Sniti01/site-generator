# «Судью судят», раунд 1 — сессия 14 (пачка 1 второго сайта, П89)

Что судилось (коммит `9a682b9`): правка маршрута `src/pages/[...slug].astro` и схемы
`src/content.config.ts` (ветвь `cta-band`, кадр ряда `art`, правило `.page-head`); заместители
в замороженных `tools/proby-tresci.mjs` (N14, P4) и `tools/brief-strony.mjs` (литерал); новые
инструменты приёмки сессии (`instrumenty/`); тексты трёх страниц против корпуса и §2 (линза
фактов — не судья, а проверка изделия, которое идёт без вычитки владельца, П42/П85).

Четыре линзы — отдельные агенты, только чтение репозитория, опыты в своей папке скретчпада.
Скептическая сверка — ведущим: каждая находка класса ДЕФЕКТ и каждая фактическая перепроверена
по коду или по тексту документа корпуса (окна текста — `okno.mjs` скретчпада), регулярка
`href` — скриптом на наборе адресов с классификатором сторожа `links`.

| Линза | Находок | Дефектов | Пределов | Прозы | Опровергнуто |
|---|---|---|---|---|---|
| маршрут и схема | 20 | 1 | 7 | 1 | 11 |
| заместители | 9 | 1 (тот же, что у маршрута) | 4 | 1 | 3 |
| инструменты приёмки | 15 | 5 + 1 безопасный | 7 | 1 | 1 |
| факты текстов | 30 | 7 искажено, 3 не подтверждено, 7 близко к источнику, 1 §5 | — | — | — |

## Дефекты и что сделано

| Находка | Суть | Исход |
|---|---|---|
| R1-MARSHRUT-1, R1-ZAM-9 | схема `cta.href` пропускала `//host/…` и `/\host/…` как «адрес от корня»; браузер уводит на чужой хост, `links` принимал `/\host/` за главную | регулярка `^(?:\/(?![/\\])\S*|https:\/\/[^/\\\s]\S*)$`, проверена на 15 адресах (`href-re.mjs` скретчпада); проза схемы уточнена; классификация `/\` в ядре — бэклог 63 |
| R1-INSTR-1, -3 | `chuzhie-teksty.mjs`: строчный тег рвал фразу; без `<main>` — «0» | строчные теги снимаются без пробела; отказ без ровно одного `<main>` и при < 100 слов; проба вставляет фразу в HTML до извлечения, со ссылкой посередине |
| R1-INSTR-2 | не проверялись `alt`, `<title>`, описания | добавлены строками |
| R1-INSTR-7 | `vneshnie.mjs` видел 7 из 28 видов загрузки | разбор тегов с кавычками, сущности, `\`→`/`, SVG, `object`, `base`, `background`, `imagesrcset`, `meta refresh`, `image-set`, экранирование CSS, файлы `.svg`/`.js`, исполняемые скрипты — на подлоге линзы 25 загрузок и 2 подозрения; `ping` назван пределом |
| R1-INSTR-9 | `sloy.mjs` читал только `_astro/*.css` верхнего уровня | все листы по дереву, встроенные `<style>`, слой `utilities` на любой глубине — на подлоге линзы 7 из 7 |
| R1-INSTR-10, -11 | `server.mjs`: граница папки по префиксу; битое `%` роняло процесс | граница по разделителю пути; код 400 |
| R1-INSTR-13 | съёмка контраста принимала ответ 404 со страницей /404/ | стоп при коде ≠ 200 и при canonical ≠ адресу страницы |

## Факты — все 30 учтены в текстах (`da7ee83`)

Искажённое и неподтверждённое исправлено или снято: «usual cure» → «a long-standing option» и
слово самого автора фикса об общем фиксе (R1-FAKT-4); метод Squeaky — его гайд, не его фикс (5);
раскладка ComfortPlay снята со Steam — убрана (8); «default» и правая кнопка мыши у bullet time —
убраны, заголовок «Keyboard controls» (9); RDR2 — 2018 и платформы по RAWG раздельно (12); группа
The Last of Us, The Suffering, The Darkness и предупреждение про все три (16); арт обложки Max
Payne 3 «first shown in fall 2011», а не «announcement» (22); «belong to their owners» (23); Anthrox —
«pre-development concept art» (27); спецификации Max Payne 2 — «as system-requirement sites list
them» (3); заголовок призыва `/media/` без обещания трейлеров (30). Близкое к источнику переписано
(11, 17–19, 21, 24, 25); §5 — «Until then» снято (20); сомнительное — с атрибуцией (1, 2, 6, 7, 10,
13–15, 26, 28, 29).

## Пределы — в таблицу доклада

R1-MARSHRUT-2 (пропорция кадра не судится; баннер в ряду), -3 (кнопка на плановый адрес), -5
(`.page-head` опирается на верх первого блока), -6 (повтор ключа, отказ `kadr()` без страницы), -7
(хост внешнего адреса не судится); R1-ZAM-4 (срок жизни заместителей — вопрос владельцу), -5, -6,
-7 (новые ветви без проб договора → разовые пробы `proby-p1.mjs`), -8 (шапка замороженных проб о
`links` устарела); R1-INSTR-4, -6, -8, -12, -15. Проза: R1-MARSHRUT-4 (причина в отказе — сделано),
-9 (двойной класс — уточнено), R1-INSTR-5 (подпись итога — уточнена).
