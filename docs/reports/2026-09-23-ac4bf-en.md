# Доклад: ac4bf-thewatch.com переведён на английский (П73)

**Нарушение инварианта 1 — первой строкой.** В команде проверки доклада
на остатки заготовок я по ошибке набрал
`sed -i.bak '…' /dev/null 2>/dev/null`. `sed -i` запрещён всегда. Цель —
`/dev/null`, это не обычный файл, и GNU sed его не правит. Проверено
сразу: `git status` показывает только задуманные правки (`DECISIONS.md`,
этот доклад), файлов `.bak` нет. Класс тот же, что в сессии S3 второго
сайта (П71): случайный фрагмент в составной команде. Кроме того, около
шести раз файлы читались через `sed -n 'a,bp'` и дважды `sed` без `-i`
форматировал вывод в конвейере. Инвариант 1 этим не нарушен, но нарушено
правило из памяти исполнителя: «слово `sed` не писать вообще». Ни одна
правка файлов этой сессии через `sed` не шла: только Edit, Write и
запись целого файла скриптом со сверкой окончаний строк.

Ветка `ac4bf-en` (от `main` = `c86ef2b`), отдельная папка `git worktree`
`../site-generator-ac4bf-en`, параллельно сессии 7 второго сайта.
Сессия 7 шла в основной папке, её ветку и дерево эта работа не трогала.
Коммиты: `13426bb` (П73), `3ef0052` и `2d96796` (инструменты), `28468e9`
(перевод), затем доклад.

## 1. Что сделано

- **Весь сайт на английском.**
  - 31 текст страниц (около 39,7 тыс. слов против 34,5 тыс. польских).
  - Обвязка: шапка, подвал, главная, подписи блоков, крошки.
  - Данные кадров: 74 описания скриншотов, 32 alt фото, строки лицензии.
  - Контракт: h1, title и description 32 страниц.
  - `lang="en"`, `og:locale` = `en_US`, американский английский, даты «September 12, 2026».
  - Бренд — «The Brotherhood».
- **Польская специфика убрана без новых фактов** (ответ 5 П73).
  - Убрано: польская локализация и дубляж, польские даты выхода, Cenega и Ubisoft Polska, цены в злотых.
  - Убраны польские рецензии и сайты: Gry-Online, CD-Action, Filmweb, Dama Gier, Spider’s Web, «польская пресса» и Eurogamer, названный среди польских рецензий.
  - Убраны польское название комикса и польский прокат «Lineage».
  - Абзацы не удалялись: число рядов и абзацев на каждой странице прежнее.
- **Семь адресов английские, со старых — 301 одним скачком.**

  | Старый адрес | Новый адрес |
  |---|---|
  | `/poradniki/` | `/guides/` |
  | `/poradniki/od-czego-zaczac/` | `/guides/where-to-start/` |
  | `/assassins-creed-za-darmo/` | `/assassins-creed-free/` |
  | `/assassins-creed-rodowod/` | `/assassins-creed-lineage/` |
  | `/assassins-creed-ii-wojna-swiatowa/` | `/assassins-creed-world-war-2/` |
  | `/mapa-miejsc-historycznych/` | `/historical-locations-map/` |
  | `/prywatnosc/` | `/privacy/` |

  Карта лежит в поле `прежний_url` в `structure/pages-s2.json`. По ней работают `build-tree`, `check-live` и `markup-diff`. Правила 301 — в `public/.htaccess`.
- **Коридоры длины: × 1,0393, у пяти страниц верх поднят до длины перевода** (ответ «k + пять исключений», дополнение к П73).
- **Судья перевода** — новый инструмент `tools/markup-diff.mjs` («разметка равна до текста»). Два раунда «судью судят».
- **`build-tree` знает про переименование.** Замер анатомии и именованный коридор ищутся по `прежний_url`.
- **`check-live` обновлён.**
  - Маркер 404 — title из структуры.
  - 301 со всех четырёх сочетаний схемы и хоста, цель редиректа отвечает 200.
  - sitemap сверяется набором адресов.

## 2. Гейты и проверки

| Проверка | Результат |
|---|---|
| `npm run build` | exit 0; источники 4/4; результат: h1 32, якоря, ссылки, коридор, арт — зелёные; 32 страницы |
| `markup-diff` польская сборка (`c86ef2b`) против английской | 32 страницы, **0 расхождений разметки, 0 остатков польского**; CSS отличается только мёртвыми правилами новых классов (п. 4.3) |
| `markup-diff --selftest` | 69/69 |
| Список судьи «равно польскому на той же позиции» (`--same`) | 219 строк, все законные: годы, имена, платформы, названия игр, подписи Commons, «Menu», «Start» |
| Доля польских текстовых узлов, которые ловит эвристика судьи | около 90 % по узлам, изменённым переводом (раунд 2), остальное видно в списке `--same` |
| `build-tree --selftest` | 46/46 (+7 проб `прежний_url`) |
| `npm run tree` против нового `structure.json` | 0 байт диффа; учёт 1426 из 1426; 30 коридоров «из контракта» |
| `astro check` | 0 ошибок, 0 предупреждений, 2 подсказки |
| Валидатор пар PL↔EN (scratchpad, для переводчиков) | 31/31 OK: форма YAML, технические поля, комментарии, ссылки по карте |
| Окончания строк | `i/` = `w/` у всех изменённых файлов; CRLF-файлы остались CRLF (счёт `\r\n` = `\n`); `git add --renormalize .` не меняет ничего |
| Взгляд: кадры headless Chrome, 1440 и 375 | шапка с «The Brotherhood» помещается, меню, h1, кнопки, эпохи и крошки — английские. Обрез справа на 375 — артефакт минимальной ширины окна Chrome: польский живой сайт снимается с тем же обрезом |
| `check-live` против живого (ещё польского) сайта | 39/83 — дымовая проба кода; новые адреса ждут выкладки |

## 3. Решения и отклонения

1. **Мой довод владельцу был неверен.** Я говорил, что английский
   короче польского и страницы выпадут вниз. Измерено обратное:
   английский длиннее, k = 1,0393 по 28 страницам. Решение
   «коэффициентом» от направления не зависит.

   Пять страниц и после × k выходили за верх: Valhalla +200 знаков,
   Eivor +158, карта мест +50, Ezio +17, Free +1. Все пять проверены по
   абзацам на добавки: их нет, рост даёт английская грамматика, а
   польские тексты этих страниц стояли у потолка. Владелец выбрал
   «k + пять исключений».
2. **Title у `/404/` и `/privacy/` несут « — The Brotherhood».**
   По-польски они несли « — Bractwo». Бриф переводчикам по ошибке
   запрещал имя сайта в title, при сборке меты я вернул его.
3. **`id`, якоря, классы, имена файлов и картинок не менялись**
   (П73 п. 2). `keywords` и `cluster` остались польскими: это запись
   о семантике Keyword Planner PL, её сверяет гейт структуры.
4. **Tailwind v4 сделал семь мёртвых утилит из английских слов текста**
   (`fixed`, `table`, `visible`, `invisible`, `collapse`, `shadow`,
   `ring`). Их не носит ни один элемент, вид не меняется. `@source` не
   тронут (инвариант 8); судья принимает только такие вставки.
5. **Выкладка — кнопкой владельца.** У исполнителя нет `gh`, и все
   прежние выкладки делались кнопкой.

## 4. Судью судят

- **Раунд 1** — шесть дефектов, все исправлены коммитом `2d96796`:
  - остаток польского ловился плохо: не было «ó», слова сверялись с учётом регистра, список был мал;
  - сломанный `.htaccess` давал exit 0;
  - в `build-tree` именованный коридор под старым адресом терялся;
  - лишний файл прятался за хешем в имени;
  - склеенные слова и абзац из U+200B проходили;
  - два старых адреса на один новый проходили.

  Ложная тревога: смена хеша CSS из-за Tailwind, п. 3.4.
- **Раунд 2.** На реальной паре сборок рецензент независимо подтвердил:
  102 вставленных токена CSS — ровно 14 переменных `--tw-*`, 7 мёртвых
  утилит и 14 блоков `@property`; маркер 404, набор sitemap и все
  четыре варианта редиректов проверяются верно.

  Дыры — обходы вручную собранными входами и слабые пробы. Всё закрыто
  коммитом `2413dd6`:
  - CSS с комментариями или строками;
  - `.htaccess` без учёта порядка строк и лишних строк;
  - битая ссылка на `/_astro/`;
  - около 10 % польского мимо эвристики — теперь список `--same`;
  - склейка слов через тег, U+00AD и U+200E/F;
  - мутанты кода, которые проходили пробы: `compareDists` не имел проб
    вовсе, `build-tree` не различал порядок поиска коридора.

  Ложные срабатывания «i.e.», «ale», «pod», «lat» сняты.
- **Раунд 3 (узкий, только исправления раунда 2).** Реальный прогон
  снова чист: exit 0, 0 расхождений, 0 остатков, `--same` — 219
  законных строк. Пробы 69/69 и 46/46. Эвристика ловит 96 % польского,
  но это мера на той же выборке, по которой пополнялся словарь.

  | Дефект раунда 2 | Итог |
  |---|---|
  | `.htaccess`: порядок строк и лишние строки | закрыт |
  | Битая ссылка `/_astro/` | закрыт |
  | Мутанты `build-tree` | все убиты |
  | CSS: комментарии и строки | частично |
  | Остаток польского | частично |
  | Склейка слов и невидимые символы | частично |
- **Раунды остановлены** (прецедент П70). Каждая правка судьи требует
  нового раунда. Оставшиеся дыры — входы, собранные вручную: их не
  производят ни Astro, ни Tailwind, ни переводчик. Приёмка этого перевода
  подтверждена на реальной паре тремя независимыми прогонами.

**Известные пределы судьи** — на случай повторного использования
`markup-diff`, не для этой приёмки:

1. **CSS.** Кавычка в дескрипторе вставленного `@property --tw-*` может
   открыть строку через `}`: резка на токены идёт раньше проверки
   кавычек. Повторная регистрация `@property --tw-*` и повтор `--tw-*`
   в `*` проходят.
2. **`.htaccess`.** Вставленный комментарий с `\` в конце склеивается
   Apache со следующей строкой и гасит директиву. Это следствие допуска
   комментариев; на живом сервере не проверялось. `check-live` поймал бы
   это после выкладки.
3. **Остаток польского.** Не раскрываются сущности `&lstrok;`,
   `&sacute;` и родня. Имя в JSON-LD не попадает в `--same`: на этом
   сайте оно берётся из того же h1, что и видимые крошки. Словарное
   «dane» ловило бы английское «Dane» («Ivarr the Dane»); в тексте его
   сейчас нет.
4. **Невидимые символы.** U+FEFF считается пробелом `\s` в JavaScript.
   Не покрыты U+2061, U+2066–2069, U+202A–202E, `&zwj;`, `&zwnj;`,
   `&NoBreak;`.
5. **Выжившие мутанты проб.** Защита «внутри вставки нет своих»,
   заголовок `@property` не из `--tw-*`, часть новых правил (снятие
   комментариев, кавычки, контекст `--tw`, `engineOn`, повтор 301,
   край узла с одной стороны, `&lrm;`, U+200C–F, `--same`, `data-era`).

## 5. Факты для будущих сессий

- **Pull в `main` может откатить эксперимент.** Workflow выкладки
  срабатывает на push в `main` при изменениях в `sites/ac4bf-thewatch.com/**`,
  `core/**` и `package*.json`, если `AC4BF_DEPLOY=on`. Вливание
  польского `main` с правкой ядра — например, ветки второго сайта —
  выложит польский сайт поверх английского, пока `ac4bf-en` не влит.
  См. политический вопрос 2.
- **Инструменты производства текстов первого сайта — польские.**
  `brief-strony`, `proby-tresci`, `korpus` и анатомия S3 меряли польскую
  выдачу. Для английского сайта они не применимы без новой семантики.
- **`games.json` (`zrzuty[].opis`) — польский.** Это вход `zrzuty-add`,
  из него собирается `game-art.json`. Повторный прогон инструмента
  вернёт польские alt.
- **Комментарии в коде и YAML остались польскими**, они не печатаются.
- **Валидатор пар, скрипты переноса и карта адресов** лежат в
  scratchpad сессии и в репозиторий не входят. Воспроизводимость
  перевода держат коммит `28468e9` и `markup-diff`.

## 6. Находки вне объёма (строкой, не правкой)

1. **Старые адреса домена.** Промо-сайт Ubisoft 2013 года жил под
   `/initiates/`, в Wayback 36 адресов (снимки 2013 и 2022 годов). Если на них есть внешние ссылки,
   301 на главную вернёт их вес.
2. **`@source not` для `src/content/`** убрал бы мёртвые утилиты Tailwind
   из текстов (п. 3.4).
3. **Переводчики заметили возможные ошибки источника.** Переведено как
   в оригинале, не исправлено:
   - Invictus «из анонсов 2026 года» (анонс был в 2022);
   - The Curse of the Pharaohs — 13 марта 2018 (вероятно, 6 марта);
   - Revelations — «шесть последовательностей» Альтаира (вероятно, пять);
   - Valhalla — отплытие из Норвегии в 873 против 872 на странице Eivor;
   - Brotherhood — «один город вместо четырёх» против «нескольких городов»;
   - Discovery — «игра 2D» в description против «2,5D» в тексте;
   - Freedom Cry — Адевале «квартирмейстер» против «заместитель»;
   - AC1 — «после трилогии Эцио или перед Revelations»;
   - Mirage — роль Нехал в памяти Локи;
   - AC2 — дата Battle of Forlì.
4. **Eurogamer** в Valhalla, Liberation и Mirage (два места) не помечен
   польским, но может быть Eurogamer.pl. Оставлен.
5. **Статистика времени прохождения** Odyssey и Origins (источник —
   Gry-Online) оставлена без имени сайта. В Rogue такая же фраза убрана
   целиком — разнобой переводчиков.
6. **Ряд «Lineage»** несёт годы `2010–2017`, где 2017 — год польского
   проката.

## 7. Открытые вопросы

**Технические (решены):**

- **Длина.** Английский длиннее — коэффициент и пять исключений по ответу владельца.
- **Tailwind.** Правило судьи для CSS; `@source` не тронут.
- **Карта адресов.** Одно поле `прежний_url` на все три инструмента.
- **Title служебных страниц.** Имя сайта возвращено, как у польских.
- **«BC».** Выровнено на «BCE» по всему сайту.

**Политические (для владельца):**

1. **Выкладка.** Actions → «Deploy ac4bf-thewatch.com» → Run workflow →
   ветка **`ac4bf-en`**. После неё я запускаю `npm run live:check`:
   83 проверки, включая 28 редиректов и 7 целей.
2. **Вливание `ac4bf-en` в `main`.**
   - **(а, рекомендую)** Сразу после приёмки живьём. `main` = живой
     сайт, и вливание других веток не откатит перевод. Откат
     эксперимента — `git revert` и выкладка.
   - **(б)** Держать ветку до вердикта эксперимента. Тогда до вливания
     нельзя вливать в `main` ничего с `core/**`, либо нужно выключить
     `AC4BF_DEPLOY`.
3. **Search Console — руками владельца.** Отправить `sitemap-index.xml`
   заново, запросить индексацию главной и нескольких игр. Семь старых
   адресов уйдут в «Страница с переадресацией», это норма.
4. **Английская семантика.** Сейчас это перевод польского: title и h1
   не подбирались под английские запросы.
   - **(а, рекомендую)** Подождать 2–4 недели данных Search Console, потом решать.
   - **(б)** Сразу собрать семантику US/UK (Ahrefs US, как у второго сайта).
5. **Старые адреса `/initiates/`** (находка 1).
   - **(а, рекомендую)** Проверить ссылочный профиль в Ahrefs; есть ссылки — 301 на главную.
   - **(б)** Не трогать.
6. **Ошибки источника** (находка 3). Исправить в обоих языках отдельной
   сессией или оставить. Рекомендую отдельной короткой сессией
   с проверкой по источникам.
