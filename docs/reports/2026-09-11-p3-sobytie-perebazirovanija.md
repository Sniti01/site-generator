# 2026-09-11 · Событие перебазирования (П40): шаги 14 → 24 → эталон игры сделаны, П32 — п.2 внесён, п.1 подготовлен, пп. 3–4 упёрлись в развилки владельца

Ветка `phase-3`, вершина на старте — `3b6f14f`, дерево чисто.
Объём зафиксирован на старте: четыре шага П40 в порядке
14 → 24 → эталон игры → П32; политические развилки П32 — владельцу.
**Событие не закрыто:** пересъёмка главной не состоялась, потому что пп. 3–4
П32 требуют решений, записанных как владельческие. Кадры `_baseline/`
главной не тронуты.

---

## Первой строкой, как велит П33

**`npm run accept` — `80/80`, `exit=0`. `npm run gates` — `4/4`, `exit=0`.**
`npm run build` — `exit=0`, оба сторожа сборки зелёные:
`h1: 2 strony, na każdej dokładnie jeden nagłówek` и
`kotwice: 2 strony, każda kotwica ma cel, żaden id się nie powtarza`.
`astro check` — 0 ошибок, 0 предупреждений, 2 подсказки (обе старые,
`tools/fetch-corpus.mjs`).

## Сверка открытых вопросов прошлого отчёта — по П38

Прошлый отчёт (`2026-09-11-p3-h1-i-otvety.md`) политических вопросов
не открывал: «Нет». Шесть записей П36 прошлого отчёта — **все на месте**
(инвариант 1 `CLAUDE.md`, строка `UNRESOLVED`, пункты 20 и 24, `ROLES.md`,
шапка `HeroKeyArt.astro`).

---

# Таблица маппинга — по дополнению к П40

Каждый кадр эталона, который расходится с текущей сборкой `8fcd625`, →
один пункт списка. Кадр без строки — стоп; таких нет.

| Кадр эталона | Что разошлось | Пункт |
|---|---|---|
| главная, `desktop-hero-1440x900` | 158×26 от (176, 21) — знак и слово в шапке, 5204 субпикселя; **плюс** прежнее принятое расхождение реестра (строки 1–2: чистка хрома, шрифт) | **П32 п.2** |
| главная, `mobile-hero-390x844` | 158×26 от (18, 21), 5205 | **П32 п.2** |
| главная, `desktop-full-1440` | не судья с 2026-09-09 (высота 8819 → 8633, `ne-sudyi.json`); из нового — тот же прямоугольник шапки | **П32 п.2** |
| главная, `mobile-full-390` | не судья с 2026-09-09; из нового — шапка | **П32 п.2** |
| главная, три кадра слоёв | не судьи с 2026-09-09 (содержимое слоя после шага Ш); наведение по якорю — **14**; из нового — липкая шапка в верхних 68 px | **14** (наведение) + **П32 п.2** (шапка) |
| игра, все пять кадров | шапка — 158×26 от (134, 21) на 1440 и от (18, 21) на 390, 5205; у полных кадров сверх того известный не-судья второго кадра галереи (пункт 25) | **П32 п.2**; эталон снят шагом 3 |

**Шаг 24 кадров не тронул**: шесть кадров главной из семи побайтово против
сборки до правки, седьмой — известный не-судья пункта 18; геометрия совпала.
**Шаг 14 кадров не тронул** по устройству: правка судьи, не страницы.

**Условие приёмки шага 24, одной строкой:** `core/gates/anchors.mjs` берёт
список страниц из `pages` хука `astro:build:done` (`anchors.mjs:118`, `:122`),
как починенный `after-build.mjs`; `readdirSync` в файле не импортирован;
глобом по `dist/` не ходит. Проверено чтением файла на `3b6f14f` и на `8fcd625`.

---

# Шаг 1 — пункт 14: кадры слоёв по якорю, сдвиг числом (П39)

Коммит `6951840`, починка после проверки — `574ffa0`.

| Где | Что |
|---|---|
| `core/accept/frames.mjs` | `К_ПОЛОСЕ(y)` → `К_ЯКОРЮ(id)`: слой по `id`, центрирование `round(offsetTop + (высота − окно) / 2)` на числах `getBoundingClientRect()`; нет якоря — отказ. Новый скрипт `ГЕОМЕТРИЯ`: условия съёмки (`clientWidth`, `clientHeight`, `dpr`), высота страницы, `offsetTop`/высота каждого слоя. `check` читает `geometria.json` у эталона и у снятого, печатает `ГЕОМЕТРИЯ: совпала — вьюпортов N, слоёв M` или строки `СДВИГ … — выше слоя / внутри слоя / страница` |
| `core/accept/selftest.mjs` | батарея геометрии: 17 образцов, `accept` 63 → **80** |
| `_baseline/README.md`, `MIGRATION.md` §6 | таблица `scrollY` → якоря; прежняя редакция П30 п.2 — рядом, **дословно** (сверено с `3b6f14f` автоматически) |
| `docs/BACKLOG.md` п.14, `docs/11_PRIEMKA.md` | статус, числа, что делает `frames.mjs` |

**Измерено на сборке `836f48f` тем же драйвером, что снимал прошлые сессии:**
три кадра слоёв по якорю против эталона — 17,4 / 14,2 / 21,8 % вместо
20,1 / 28,3 / 46,5 % по литералу. Первая причина (положение) снята, вторая
(содержимое слоя после смены гарнитуры) ждёт пересъёмки — как пункт 14
и предсказывал. Геометрия главной сегодня: страница 8633 / 11545,
`offsetTop` слоёв 879 / 1777,8 / 5806,61 (1440), высоты 898,8 / 871,92 / 948,95.

## Судью судили — три линзы, 26 находок, 14 пережили, восемь классов починены

Состязательная проверка правки `frames.mjs` (правило `CLAUDE.md`): три
независимые атаки — скрипты в браузере, сверка геометрии, записи против
кода; каждая находка — двум скептикам на опровержение. Пережившие,
схлопнутые в классы:

| # | Что было | Чем проявилось бы | Что сделано |
|---|---|---|---|
| 1 | «совпала» при нуле сравнённого: эталон `{}`, без `слои`, с ключом `1440x900` | **ложный пропуск**: эталон без слоёв делал любую съёмку совпавшей | обход по объединению ключей, счёт в строке, пустой эталон — строка «сверять нечего», лишний слой снятого назван |
| 2 | `ГЕОМЕТРИЯ` писала `null` для опечатки в `id`; `check` на таком эталоне падал `TypeError` до сверки кадров | падение вместо вердикта | `ГЕОМЕТРИЯ` отказывает, как `К_ЯКОРЮ`; сверка любую дыру превращает в строку |
| 3 | `СДВИГ` с хвостом: `−6.179999999999836` | цитировать в докладе нельзя | округление до 0,01 |
| 4 | формула в README/JSDoc/BACKLOG ≠ формуле кода (место `round`) | запись ≠ код; на дробных входах ±1 px | записана формула кода, объяснено округление снаружи |
| 5 | порядок съёмки отсылал к `geometria.json` эталона, которого при первой съёмке нет | тупик первой съёмки, набор якорей по вкусу оператора | источник один — таблица README; пример `protocol` для тех же трёх якорей |
| 6 | одна фраза «выше слоёв что-то изменилось» на три вида сдвига | причину починили бы не в том месте | у каждого `СДВИГ` — вид |
| 7 | условия съёмки не записывались: видимая полоса прокрутки даёт на 390 высоту **11514 против 11545** при нетронутой странице | **ложный отказ**: шесть строк СДВИГ на призрак | `ГЕОМЕТРИЯ` пишет `clientWidth/clientHeight/dpr`, сверка условий первая и одной строкой; протокол требует `--hide-scrollbars` |
| 8 | цитата «прежней редакции дословно» несла вставки `= 892` | слово «дословно» ложно | приведена к `3b6f14f`, литералы вынесены за цитату, совпадение проверено скриптом |

Двенадцать опровергнутых — в журнале проверки; среди них честно снятое:
код возврата `check` при СДВИГ остаётся `0` по правилу `11_PRIEMKA.md`
(вердикт читается из вывода), и это записано в README шагом 6 — строка
`ГЕОМЕТРИЯ` цитируется в докладе дословно.

**Побочный урон проверки, названный ею же:** линзы писали в общий скретчпад
и переписали мой драйвер `cdp.mjs`, заняли порт 9333. Восстановлен из копии;
съёмка после этого — только после завершения проверки. Урок: следующей
проверке судьи давать **отдельную папку** явным словом в задании.

---

# Шаг 2 — пункт 24: якоря шапки абсолютные, гейт якорей подключён

Коммит `836f48f`.

| Где | Что |
|---|---|
| `src/data/site.ts` (CRLF, прямая запись с проверкой единственности) | `#zejscie` → `/#zejscie`, `#katalog` → `/#katalog`; комментарий о доводе 5а — верен для главной и только для неё |
| `astro.config.mjs` | `anchors()` рядом с `afterBuild()` — той же правкой |

| Проверка | Факт |
|---|---|
| сборка | зелёная с первого запуска, гейт: 2 страницы, каждая котвица с целью |
| манифест | 56 → 56, изменились ровно два HTML |
| диффа HTML | **ровно четыре `href` на страницу** — шапка и выдвижное меню, по два; ничего больше |
| пиксель главной против сборки до правки | шесть кадров из семи побайтово; `desktop-full` — 702 субпикселя в известном квадрате пункта 18; `londyn` 627 при максимуме 1 — **контроль на той же сборке даёт 1256 при максимуме 2**: шум, не вёрстка |
| геометрия | совпала |
| **отрицательная проба** | голые якоря возвращены на время → `exit=1`, отказ назвал **ровно четыре якоря** `/assassins-creed-4-black-flag/`, главная чиста; проба снята, `git ls-files --eol` — CRLF цел |

---

# Шаг 3 — эталон страницы игры

Коммит `9b872b7`. `_baseline/assassins-creed-4-black-flag/` — папка на страницу
(корень остаётся за главной, имена файлов те же), `geometria.json`,
`ne-sudyi.json` (пуст). Запись — `DECISIONS.md` от 2026-09-11 (техническое).

| Кадр | Размер | Роль |
|---|---|---|
| `desktop-hero-1440x900` | 1440×900 | судья |
| `desktop-full-1440` | 1440×4627 | сторож |
| `desktop-layer-co-jest-w-grze-1440x900` | 1440×900 | судья ряда рассказа, по якорю |
| `mobile-hero-390x844` | 390×844 | судья |
| `mobile-full-390` | 390×5896 | сторож |

Состав — по записанному протоколу, не шире: четыре кадра §6 плюс оконный
кадр слоя на каждое сочетание `flip`/`band` (одно). **Два прогона подряд
на сборке `574ffa0`: три оконных кадра побайтово, геометрия совпала;** полные
кадры расходятся ровно в прямоугольнике второго `<img>` галереи
(`.foto--gra`: 574×358 от (733, 2294) на 1440; 356×222 от (17, 2794) на 390;
отклонение в основном 1, максимум 19–23) — **пункт 25** бэклога, тот же класс,
что пункт 18 у главной; в объёме не чинится.

Блоки ниже первого экрана (подпись, оглавление, галерея, вердикт, ссылки,
призыв) имеют только сторожа. Завести им судей по якорю — вопрос владельца
ниже.

---

# Шаг 4 — П32

## п.2 — пункт 6: внесён, ждёт приёмки глазами

Коммит `8fcd625`. `.hdr__brand { display: flex }` в `core/chrome/SiteHeader.astro`.

**Измерено до и после, оба вьюпорта:** коробка ссылки 36,05 → **28**, центр
видимой группы 29,97 → **34,00** — центр шапки и центр бургера.
**Новый факт против П22:** сдвиг был одинаков на 390 **и на 1440** (4,03 px
там и там) — механизм один, правка одна, шапка сдвигается везде. Это
следствие записанной починки, поэтому дифф на настольных кадрах маппится
на тот же П32 п.2, а не «вне списка».

| Судья | Факт |
|---|---|
| правил CSS | 337 → 337, разошлась **ровно одна тройка** |
| пиксель | на всех семи кадрах главной и трёх оконных игры — **один прямоугольник 158×26**, 5204–5205 субпикселей; у полных кадров игры сверх того ровно известный не-судья: 168 349 = 5205 + 163 144 |
| геометрия | не дрогнула на обеих страницах |
| капкан | слово-кандидат в комментарии CSS разбудило сканер Tailwind: `+1` правило `.inline-flex`. Комментарий переписан без токена. **Инвариант 8 работает и на комментарии** |

Кадры «до/после» для 390 и 1440 отправлены владельцу файлами. Эталон
не тронут: реестр README получил строку 4 со статусом **«ждёт приёмки
глазами»**; закрепится единой пересъёмкой (П17, П22).

## п.1 — пункт 5: материал для решения глазами, правка не внесена

Вариант собран пробой, измерен, снят:
`.rail-sec__track { scroll-padding-left: max(var(--gutter), calc((100% - var(--container)) / 2 + var(--gutter))) }`.

| | 1440 | 390 |
|---|---|---|
| заголовок секции, `x` | 174 | 16 |
| первая карточка как есть | 84 | 0 |
| первая карточка в варианте | **174** | **16** |
| `scrollLeft` в покое | 124 → 34 | 40 → 24 |
| «назад» выключена в покое | да → да | да → да |

Почему не распоркой: она считается от `100vw`, а дорожка на ≥1200 короче
на `--rail`, и между распоркой и первой карточкой стоит `gap` — при
`scrollLeft = 0` карточка встала бы на 208, не 174. Кадры «как есть /
вариант» для 1440 и 390 отправлены владельцу файлами. Второй путь —
оставить и переписать комментарий (П18). Записано в пункт 5.

## пп. 3–4 — возврат главной к плану и переезд на маршрут: шаг 0, без правок

Измерено на сборке `8fcd625`, до единой строки кода — по практике шага 0.

**Что печатает главная сегодня (порядок секций `dist/index.html`):**
`hero` (+ `chip-nav` из 5 чипов) → `layer` ×2 → `creed` (`band-quote`) →
`layer` ×2 → `rail-sec` (`card-rail`) → `layer` → `stack` (`link-columns`,
`id="katalog"`) → `cta`. Плюс `DescentRail` (шкала) по `data-era-section`
пяти слоёв и обёртка `<div id="zejscie">`.

**Что объявляет `blocks[]` главной:** `hero-key-art → byline → story-row →
band-quote → card-rail → link-list#numeracja-serii → gallery → link-columns
→ cta-band`.

**Что умеет маршрут `[...slug].astro` (`PORZADEK`):** `hero-key-art → byline
→ toc → story-row → card-rail → gallery → link-columns → verdict-box →
link-list → cta-band`; все `story-row` — одним циклом подряд; `band-quote`
не импортирован; роль умеет только `card-rail`; `skala={false}` всем.

**Расхождения, каждое — решение, не правка:**

| # | Что | Чья это развилка |
|---|---|---|
| 1 | **Ритм главной** — слои перебиты `band-quote` и `card-rail` («iglica → две → пауза → две → шина → последняя → stóg», комментарий `index.astro`). Маршрут печатает все ряды подряд; `blocks[]` чередование выразить не может (блок в списке один раз) | **вид и контракт** |
| 2 | **Порядок**: `link-list` раньше `gallery` и `link-columns` — два конфликта с `PORZADEK`, названные 2026-09-10; плюс `link-list#numeracja-serii` — роль, которую маршрут отвергает отказом, а страница сегодня **не печатает вовсе** | **контракт или диспетчер** — вопрос, отложенный владельцем до переезда |
| 3 | `band-quote` есть в ядре, но не в маршруте; орнамент и польские фразы — витринные | техническое, если п.1 решён |
| 4 | Герой: `chip-nav` из пяти чипов к эпохам, `<i>` в лиде, запасной `Viewpoint` — маршрут ничего из этого не печатает | вид |
| 5 | Слои: палитра `layer--<эпоха>` **на каждый ряд**, `data-era-section` для шкалы, список из трёх поradników с ссылкой «страница гры» в месте `tresc` (5 × `layer__guides`), табличка `tabliczka` у `flip`, `layer__hero` в мете — маршрут даёт один модификатор на страницу, без `tresc`, без табличек | вид и контракт содержания |
| 6 | Лента `fresh`: дата, вид, место, палитра карточки, кадр — схема `cards.items` знает только `href/title/kind/place` | контракт содержания |
| 7 | `byline` и `gallery` (П32 п.3): содержания нет — чья подпись и дата у главной, какие кадры в галерее | **содержание** |
| 8 | `skala`: маршрут выключает шкалу всем, главной она нужна — по типу `home` | техническое |
| 9 | SEO: `h1` «Cała seria według lat» → «Cała seria Assassin’s Creed, część po części» (длиннее — переверстается герой), `title`/`description` — из структуры | П32 п.4 по букве, дифф кадра героя маппится на него |
| 10 | Якоря главной: `<div id="zejscie">` и `id="katalog"` у стога — цели ссылок шапки (`/#zejscie`, `/#katalog`) и героя (`#zejscie`, `#katalog`); маршрут этих `id` не ставит. Гейт якорей поймает только внутристраничные (герой), межстраничные `/#…` — вне его рамки по П40 | техническое, но обязано быть названо до переезда |
| 11 | **43 из 53 внутренних адресов главной — призраки** (не в дереве из 30 страниц): 15 поradników эпох, 5 ленты, 20 каталога, 4 подвала (`/o-nas/`, `/redakcja/`, `/kontakt/`, `/prywatnosc/`). Вопрос 2 отчёта s5 («лента каталога») ответа не получил; при переезде содержание главной придётся собирать из дерева | **содержание и политика** |

**Вывод шага 0:** «переезд главной на маршрут» — не перенос файла, а
перевод страницы с собственным замыслом на общий шаблон: маршрут не умеет
ни ритма, ни чипов, ни поradników в слоях, ни пасма-цитаты, ни шкалы.
Либо маршрут вырастает до всего этого (полем `role` у `story-row`, новыми
полями схемы, диспетчером порядка), либо главная остаётся своим шаблоном,
который **читает SEO-поля и `h1` из структуры и сверяет свой `blocks[]`
с напечатанным** тем же предикатом, что маршрут. Второе дешевле на порядок
и закрывает букву П32 п.4 (SEO из структуры, `h1` из структуры, страница
вне структуры роняет сборку — она уже в структуре), но не его слово
«на маршрут». Решать — владельцу; без решения пересъёмка невозможна,
потому что список П32 закрыт и его четвёртый пункт не исполнен.

---

# Гейты и проверки

| Проверка | Результат |
|---|---|
| `npm run accept` | 80/80, `exit=0` |
| `npm run gates` | 4/4, `exit=0` |
| `npm run build` | `exit=0`; `after-build` и `anchors` зелёные |
| `astro check` | 0 ошибок, 0 предупреждений, 2 старые подсказки |
| манифест старт → финал | 56 → 56; изменились два HTML (шаг 24: четыре `href`; п.2: скрут листа), лист `EraMedia` сменил хеш (п.2) |
| правил CSS | 337 → 337; одна тройка `.hdr__brand` |
| `git ls-files --eol` | `i/` = `w/` у всех правленных; `site.ts` CRLF цел; `git add --renormalize .` не тронул ничего |
| пиксель | таблица маппинга выше; вне списка — ничего |
| геометрия | обе страницы совпали на всех сборках сессии |

---

# Факты, важные будущим сессиям

1. **Драйвер съёмки** — `cdp.mjs` + `snjat.mjs` в скретчпаде сессии
   (копия драйвера сессии 2026-09-09 с `replMode: true`); скрипты страницы
   берёт из `frames.mjs`. Воспроизводит кадры прошлых сессий побайтово.
   `--hide-scrollbars` обязателен — теперь это строка протокола.
2. **`replMode` CDP** допускает повторное объявление `const` между вызовами:
   `К_ЯКОРЮ` трижды подряд — без ошибок.
3. **Сканер Tailwind читает комментарии CSS** и делает утилиту из слова-кандидата.
4. **Пункт 19** (ГОТОВНОСТЬ виснет на ленивых) — обход сроком 15 с в драйвере,
   в исходники не вносился, как и прежде.
5. **Состязательная проверка пишет в общий скретчпад** — давать ей отдельную
   папку и не снимать кадры, пока она идёт.
6. Числа геометрии главной и игры — в `geometria.json` игры и в пункте 14.

---

# Открытые вопросы

## Технические — решены

- **Где лежат эталоны страниц волны 1** — папка на страницу, те же имена,
  свои `ne-sudyi.json` и `geometria.json`; запись в `DECISIONS.md`.
- **Состав эталона игры** — по протоколу, 4 + 1; расширение — владельцу.
- **Какой из двух однострочников для пункта 6** — `display: flex`: делает
  знак блочным флекс-элементом, подбора нет; измерено.
- **Как выравнивать карточку в пункте 5, если владелец скажет «выравнивать»** —
  `scroll-padding-left` от ширины дорожки, а не правкой распорки; измерено.
- **Код возврата `check` при СДВИГ** — `0`, по правилу `11_PRIEMKA.md`;
  строка цитируется дословно.

## Политические — владельцу

**1. Пункт 6 — приёмка глазами** (П22, П17). Два кадра «до/после» отправлены.
Принято — правка закрепится пересъёмкой; не принято — откат одной строки.

**2. Пункт 5 — вид ленты.** Два кадра «как есть / вариант» отправлены.
(а) выровнять `scroll-padding-left` — одна строка, измерена;
(б) оставить и переписать комментарий у распорки. Рекомендация — **(а)**:
комментарий уже обещает колонку, а измерение показывает, что распорка
одна её дать не может.

**3. П32 п.4 — что значит «переезд главной на маршрут».** Шаг 0 выше:
одиннадцать расхождений, из них пять — вид и содержание.
(а) маршрут вырастает до главной: роль у `story-row`, поля схемы для чипов,
поradników в слоях, ленты с датами, пасма-цитаты; диспетчер порядка;
шкала по типу. Стоимость — сессия-две и рост контракта содержания.
(б) главная остаётся своим шаблоном `index.astro`, но берёт `title`,
`description`, `h1` из структуры и сверяет `blocks[]` с напечатанным
тем же предикатом `drukowane`, что маршрут; `byline` и `gallery` печатает
сама. Стоимость — одна сессия, контракт не растёт.
Рекомендация — **(б)**: буква П32 п.4 (SEO и `h1` из структуры, страница
в структуре) исполняется, а замысел главной — ритм, шкала, чипы — не ломается
ради слова «маршрут». Если владелец выбирает (а) — нужен ответ на порядок
блоков (контракт или диспетчер) и на п.5 ниже.

**4. П32 п.3 — содержание `byline` и `gallery` главной.** Чья подпись и дата
(у страницы игры — `Redakcja · Bractwo · 10 września 2026`), какие кадры
в галерее (сегодня на главной пять ключевых артов эпох плюс герой).
Без этого п.3 не исполним ни в (а), ни в (б).

**5. 43 адреса-призрака на главной** (пункт 11 шага 0) — чем заполнять
поradniki эпох, ленту и каталог, когда дерево — 30 страниц. Вопрос 2
отчёта s5 остался без ответа; при пересъёмке главной он встанет буквально:
эталон заморозит либо призраки, либо новое содержание.

**6. Судьи для блоков ниже первого экрана** у эталона игры — заводить ли
оконные кадры по якорю (`#galeria-tytul`, `#werdykt-tytul`, …). Сегодня
у галереи, вердикта, списка ссылок и призыва только сторож; съёмка по якорю
теперь дешёвая. Рекомендация — **завести на пересъёмке**, одним решением
о составе эталона страницы.

---

# Записи, обещанные этой сессией — по П36

| Что | Где | Источник |
|---|---|---|
| пункт 14 исполнен, числа, проверка судьи | **пункт 14** `docs/BACKLOG.md` | ход работы |
| пункт 24 исполнен, проба | **пункт 24** `docs/BACKLOG.md` | ход работы |
| пункт 6 внесён, ждёт глаз; капкан Tailwind | **пункт 6** `docs/BACKLOG.md` | ход работы |
| пункт 5 — вариант измерен, не внесён | **пункт 5** `docs/BACKLOG.md` | ход работы |
| не-судья галереи в полном кадре игры | **пункт 25** `docs/BACKLOG.md` | находка |
| папка на страницу, `geometria.json`, драйвер снаружи | **запись 2026-09-11** `DECISIONS.md` (техническое) | ход работы |
| таблица якорей, прежняя редакция дословно, эталон игры, реестр строка 4 | **`_baseline/README.md`** | П39, П40 |
| таблица §6 на якоря | **`MIGRATION.md` §6** | П39 |
| протокол: `--hide-scrollbars`, источник якорей, три вида сдвига | **`core/accept/frames.mjs`** `protocol` | проверка судьи |

**Обещано и НЕ сделано:** пересъёмка П32 — не начата, потому что пп. 3–4
ждут ответов выше; событие открыто, состояние ветки согласовано с таблицей
маппинга.
