# 2026-09-16 · Сессия 2 «кадры рядам» — событие вида закрыто: пересъёмка 28 кадров на `1d9b2ac`, состав игры 10 → 15, генератор судим, таблица по месту

Ветка `phase-4`, вершина на старте — `d595bed` (П58), `main` = `254574d`,
дерево чистое; прошлая сессия (1б) завершилась штатно — доклад, `/review`,
стоп глазами пройден П58. Команда владельца: «старт 2». Объём — **сессия 2
по разделу 10 плана** `2026-09-15-plan-kadry-ryadam.md` и П58 «Следствия»:
пункт 7 списка (дата подписи главной), копия генератора с местом `okruszki`
и пятью якорями слоёв, пробный прогон, «судью судят», пересъёмка 28 кадров
3/3/2, README/реестр/зоны, закрывающий доклад события с таблицей «кадр →
дифф → место → пункт». Правок вида — одна (подпись, пункт 7); объём не
расширялся; найденное вне объёма — строками (бэклог 49).

---

## Первой строкой, как велит П33

**`npm run accept` — `225/225`, `exit=0`.** **`npm run gates` — `4/4`,
`exit=0`.** `npm run build` (`1d9b2ac`) — `exit=0`, 32 страницы, 2,9 с
(кэш `webp` тёплый); сторожа: `h1: 32`, `kotwice: 32`, `linki: 975/32`,
`korytarz: 28 + 4 null`, `art: 52/62/25` — как в 1б. **Пиксельная приёмка
против `_baseline/` снова действует:** `check` каждого из трёх прогонов
обеих страниц против нового эталона — **0 различий 15/15 и 11/11,
геометрия совпала, `exit=0`**; судья бездействовал с `e7c626d` (сессия 1а)
до `8d9ba13` (этот коммит эталона) — три сессии, как и предупреждал план
(раздел 8, «цена»).

## Сверка открытых вопросов прошлого доклада — по П38

| Вопрос доклада 1б | Исход | Где |
|---|---|---|
| 1. Стоп глазами — Black Flag, Ezio, крошки на 390, главная | **приняты** по кадрам папки доклада; герой Ezio — (а) Revelations [3] | П58 п. 2–3 |
| 2. Выкладка кнопкой из `phase-4` | **отложена** — «когда будет всё готово», после сессии 3; живой сайт на `870b8bd` | П58 п. 1 |
| 3. Бэклог 48 меняет счёт пачек сессии 3 | **без возражений** — исполняется как названо | П58 п. 4 |
| 4. Переносы (тип 404, `noindex`, П53 п. 1–3, лист второго сайта, `/404/` глазами, сайт раньше ящика, бэклог 47 п. 9) | переносятся дальше | П58 п. 4 |

---

# 1. Пункт 7 списка — дата подписи главной → день пересъёмки (`1d9b2ac`)

`site.ts` `podpis`: `2026-09-15` / «15 września 2026» → `2026-09-16` /
«16 września 2026» (исполнение П41 п. 2, не пополнение списка — как П50
п. 6). Гейты 4/4, в `dist/` главной изменился ровно один `<time>` (подпись;
пять `<time>` ленты — 12 września, из `byline` страниц). Это единственная
правка вида сессии; **сборка съёмки — `1d9b2ac`**, все прогоны на ней.

# 2. Генератор съёмки — копия с четырьмя изменениями, проба, «судью судят»

**`docs/reports/2026-09-16-sobytie-kadry/sniazka-gen.mjs`** — копия
генератора П50 (`2026-09-15-sobytie-vida-2/sniazka-gen.mjs`):
(1) `ROOT` → `sesja-2026-09-16`, `снято` → 2026-09-16; (2) игра —
шесть якорей слоёв вместо одного (`co-jest-w-grze` + пять П51 п. 1),
состав 10 → 15; (3) место **`okruszki`** (`nav[aria-label="Okruszki"]`)
в списке мест обеих страниц с контролем: на игре обязано быть, на главной —
отсутствовать, иначе `СТОП`; (4) режим **`visitor`** — два кадра
`_baseline/visitor/` по протоколу папки (свежая загрузка на 390). Скрипты
ПРОКРУТКА / ГОТОВНОСТЬ под сроком 3 с / ГЕОМЕТРИЯ / К_ЯКОРЮ / К_ВЕРХУ /
осадка / MIEJSCA — дословно копии П50 (diff проверен судьёй). Рядом —
`unpack.mjs` (+ ветка «только log» для visitor), `delta.mjs` без изменений
и `mapowanie.mjs` сессии вида с одной поправкой **[K1]**: место с `brak:
true` (крошек на главной нет — запись по замыслу генератора) карту роняло
по проверке [F8]; теперь такое место в рамки не входит.

**Пробный прогон** (`proba`, до поправок судей): игра — 15 кадров,
11 якорей, `okruszki` есть (0, 69, 1440×43,84 / 390×66,69); главная —
11 кадров, 7 якорей, `okruszki` нет; visitor — 16/16, герой `Zz2I7a`.
`check` пробы против прежнего эталона `dc5fc16` — ожидаемые отказы
(раздел 4); карта по месту — остаток вне мест 0 на всех кадрах.

**Судью судили — две линзы до пересъёмки** (правило CLAUDE.md, инвариант 1):

| Линза | Находок | Стоп | Поправлено | Не правлено — строкой |
|---|---|---|---|---|
| **код** (diff против П50 и `frames.mjs`, имена кадров, контроль `okruszki`, visitor, порядок, экранирование) | 9 | 0 | контроль `okruszki` **отказывает** при ожидании вне {jest, brak} (был молчаливый пропуск класса «совпала при нуле сравнённого») и требует ненулевой коробки; visitor пишет строку условий съёмки; пятое отличие (хвост «якорей N» в логе) названо в шапке | `MIEJSCA` при `all: true` и нуле совпадений не пишет ни рамки, ни `brak` (унаследовано от П50); `SNIETO` литералом (как в П50); ГОТОВНОСТЬ под сроком — обход пункта 19, унаследован |
| **выход пробы** (состав, размеры PNG, `geometria.json`, центровка 18/18, `miejsca.json`, пять кадров — свои ряды глазами, логи, журналы `check`) | 4 | 0 | лог visitor не сохранялся — сохраняется (`wynik.json` → `log.txt`) | четыре из пяти новых рядов выше окна 900 — К_ЯКОРЮ режет края (норма П39, строка в README); список пяти кадров П51 после `d4b8864` покрывает четыре сочетания `flip`/`band` с повторами — назван владельцем дословно, не переспрашивается |

Проверено судьёй числом: 18/18 наведений `scrollY = ожидали`; ГОТОВНОСТЬ
11/11 (игра) и 16/16 (главная) на обоих вьюпортах; множества ключей
`geometria.json` равны эталонным; `okruszki` — сразу под шапкой (y 69);
`rzedy-1…8` по порядку DOM; журналы `check` — ничего сверх четырёх
изменений сборки против `dc5fc16` (крошки, кадры рядов, подвал, дата).
Разбор судьи, важный будущему: **все десять прежних кадров игры вне допуска
не из-за содержания, а из-за дробной высоты крошек 43,84 px** — фаза
субпикселя всего потока ниже шапки (`blok-galeria` 697 440 субпикселей:
281 399 по 1, 96 424 по 2, 20 902 > 64 на краях тех же картинок; полный
кадр, сдвинутый на 44 px, совпадает с прежним в герое до краёв букв).

**Проба после поправок** (`proba2`) — 28 кадров **побайтово равны** пробе
до поправок: поправки тронули только контроль и лог.

**Находка visitor — холодный браузер грузит 15 из 16.** Первый `proba2/
visitor` в свежем браузере остановился стопом «загружено 15 из 16»; диагноз:
пятая карточка ленты «Świeżo z Animusa» стоит при x = 1185 — за правым
краем окна 390, Chrome её лениво не грузит (карточки при x 601 и 893 —
грузит, в пределах своего отступа); в пробе 16/16 вышло потому, что кадр
уже лежал в кэше после прогона 1440 (Chrome грузит кэшированную
lazy-картинку сразу) — так же было снято «16 из 16» 2026-09-15. Измерено:
холодный прогон без стопа и тёплый — **оба кадра побайтово равны**; стоп
visitor переписан: считаются картинки с `left < clientWidth` (13/13),
те, что за краем, — строкой лога. Поправка в `visitor/README.md`.

# 3. Пересъёмка — три прогона игры и главной, два visitor

Сборка `1d9b2ac`, `astro preview`, Playwright MCP; v1 и v2 — каждый
в свежем браузере (холодный), p1–p3 — в одной вкладке подряд, как П50.
**Главная: p1 = p2 = p3 побайтово на всех 11 кадрах; игра: p1 = p2 = p3
побайтово на всех 15; visitor: v1 = v2; геометрия трёх прогонов равна**
(`progony.txt`). Эталон — p1 и v1; выбирать большинством (П50) не пришлось —
бимодального зерна полного кадра 390 (П50, раздел 5) в этот раз не было
ни в одном из пяти прогонов главной (три + две пробы). Восемь кадров
главной и первый экран visitor не менялись файлами (0 различий, те же
блобы): `hero` ×2, `layer-wlochy`, `layer-londyn`, четыре `blok-*`,
`visitor/mobile-hero`.

# 4. Таблица «кадр → дифф → место → пункт» — p1 против эталона `dc5fc16`

Места — с DOM при `scrollY = 0` (`miejsca-*.json`), карта —
`mapowanie.mjs` [K1], выходы `mapa-glowna.json`/`mapa-gra.json`
и `.txt`. **Остаток вне мест — 0 на всех 21 сравнимом кадре**; пять новых
кадров игры сравнивать не с чем (пункт 3 списка). Нумерация пунктов —
раздел 8 плана: 1 крошки, 2 кадры рядов, 3 пять кадров слоёв, 4 подвал
по странице, 7 дата подписи.

### Главная (`_baseline/`, 11 кадров)

| Кадр | Дифф (субпикселей, макс) → место → пункт |
|---|---|
| `desktop-hero`, `mobile-hero`, `layer-wlochy`, `layer-londyn`, `blok-swieze`, `blok-numeracja`, `blok-galeria`, `blok-katalog` | **0** — файлы не менялись |
| `layer-jerozolima` | 1250 (макс 2), 161×54 от (278, 0) → шапка над подписью: дата под полупрозрачной шапкой → **7** (в П50 при 12 → 15 — 695 у того же места) |
| `desktop-full-1440` | **размер 10270 → 9957**, вне сравнения 450 720 px; в общей части 171 444: подпись 431 (макс 149) → **7**; подвал 102 570 (макс 218) → **4** (блок «Zdjęcia» с десятью атрибуциями снят с главной); лента 36 371 (макс 2) и галерея 32 066 (макс 1) — зерно при новой высоте (зона 4); 6 субпикселей (макс 27) на x = 84 в строках 9870 и 9908 — левый край подвала, зачтён fixed-месту `skala` по порядку разбора → **4** |
| `mobile-full-390` | **размер 14719 → 13953**, вне сравнения 298 740 px; в общей части 249 124: подпись 669 → **7**; подвал 106 240 (макс 218) → **4**; остальное — зерно ≤ 2 по слоям, ленте, стогу (зона 4), в коробке героя ≤ 1; 6 субпикселей на x = 0 (строки 13801, 13933) — край подвала → **4** |

Положение семи якорей главной — то же до сотых: правка подвала ниже стога,
засечки шкалы не двигались (П44 D).

### Игра (`_baseline/assassins-creed-4-black-flag/`, 10 прежних + 5 новых)

| Кадр | Дифф → место → пункт |
|---|---|
| `desktop-hero` | 2 639 418 (макс 232): `okruszki` 158 933 — полоса крошек 1440×43,84 под шапкой → **1**; `hero` 2 477 071 — герой ниже на 43,84, содержание то же → **1** (сдвиг) |
| `mobile-hero` | 563 526 (макс 245): `okruszki` 78 350 — крошки в две строки 390×66,69 → **1**; `hero` 484 006 — сдвиг → **1** |
| `layer-co-jest-w-grze` | `offsetTop` 1609,52 → 1653,36 (+43,84) → **1**; 16 185 (макс 142) в `rzedy-1` — фаза субпикселя ряда 1 при дробном сдвиге (раздел 2) → **1**; зона `2-ряд-рассказа` не потребовалась |
| `blok-toc` | 1057,84 → 1101,69 → **1**; 5282: `toc` 780 (подчёркивание пункта), `rzedy-1` 4502 (надзаголовок ряда 1 на другой дробной позиции) → **1** |
| `blok-galeria` | 8630,89 → 9176,3 (+545,41 = крошки 43,84 → **1** + ряды 2–8 в две колонки 501,57 → **2**); 697 440 (макс 224) — те же картинки на другой фазе → **1** |
| `blok-werdykt` | 9267,94 → 9813,34 → **1 + 2**; 92 391 — строки текста на другой целой позиции, призыв ≤ 2 → **1** |
| `blok-powiazane` | 9614,95 → 10160,36 → **1 + 2**; 749 119 — то же → **1** |
| `blok-cta` | 10138,61 → 10684,02 → **1 + 2**; призыв 677 626 (макс 200) — фаза → **1**; подвал 25 803 (макс 218) — «Gry: Freedom Cry, Black Flag» вместо блока «Zdjęcia» → **4** |
| `desktop-full-1440` | **размер 11443 → 11821** (+378 = крошки +43,84 → **1**, ряды +501,57 → **2**, подвал −167,41 → **4**), вне сравнения 544 320 px; в общей части — всё ниже шапки сдвинуто (**1**); внутри: ряды 2–8 — шесть новых кадров Steam и две колонки (`rzedy-2…8` 0,77–2,6 млн каждый) → **2**; `podpis`, `toc`, `galeria`, `werdykt`, `powiazane`, `cta` — сдвиг → **1 + 2**; `stopka` 165 177 → **4** |
| `mobile-full-390` | **размер 13729 → 14831** (+1102 = крошки +66,69 → **1**, ряды +1617,66 → **2**, подвал −583 → **4**), вне сравнения 429 780 px; общая часть — то же разложение по местам |
| `layer-{edward-kenway,kawka,trzy-miasta,abstergo,freedom-cry-i-rogue}` | **новые** — эталон по П51 п. 1 → **3**; прежнего блоба нет; в трёх прогонах побайтово |

Рост рядов по местам (рамки `rzedy-N`, П50 → сейчас): 1440 —
`edward-kenway` +50, `kawka` +101, `trzy-miasta` 0 (был с кадром),
`abstergo` +101, `freedom-cry-i-rogue` +101, `wydania` +101, `dla-kogo` +50
(сумма 504 по целым рамкам, 501,57 по якорям); 390 — +269/+269/0/+270/
+270/+270/+270 = 1618. Подвал: 976 → 809 (1440), 2142 → 1559 (390).
Пункт 2 у Ezio (`148f5d2`) и пункт 5 (герои) эталонов не имеют — вне
таблицы по построению; пункт 6 (`visitor/`) — раздел 5; пункт 8 — этот
доклад, README и генератор.

# 5. README, зоны, реестр, `visitor/` — коммит `8d9ba13`

- **`_baseline/README.md`:** шапка — «Переснят 2026-09-16»; состав главной
  (высоты 9957 / 13953) и игры (**15 кадров**, таблица с пятью новыми
  рядами и новыми `offsetTop`; правило состава выполнено с запасом, четыре
  ряда выше окна — норма П39); зоны — рамки галереи по DOM, «пять новых
  кадров и шесть картинок Steam зон не имеют», наблюдение П51 п. 3 —
  дописано (сравнить с прежним эталоном нельзя, внутри сборки стабильно);
  **новый абзац «Дробная высота крошек сдвигает фазу субпикселя всего, что
  ниже шапки»** — следствие для будущих правок хрома; реестр пуст по П46;
  раздел «Пересъёмка 2026-09-16 — записи по П17» — хеши прежних блобов
  (`3e5b9c5`) трёх кадров главной, десяти игры, `visitor/`, трёх JSON
  на страницу.
- **`szum.json` игры:** `2-галерея-полный-1440` (733, 8754) → **(733, 9299)**,
  `2-галерея-полный-390` (17, 9888) → **(17, 11572)** — коробка `<img>`
  с DOM (`pomiar-dom-1d9b2ac.json`), с оговоркой «три прогона побайтово —
  зона сохранена по прежнему измерению того же элемента» (как в П50);
  `2-ряд-рассказа` — та же коробка (748, 277) при `scrollY` 1589; зона 4
  без изменений; **новых зон нет** — между прогонами ничего не дрогнуло
  (П45/П51 п. 3). `szum.json` главной не менялся.
- **`geometria.json` обеих** — из p1 (главная: те же семь якорей, новые
  высоты, дата, сборка; игра: 11 якорей). **`ne-sudyi.json` обеих** — пусты
  до и после, пометка «обнулён» — 2026-09-16, `1d9b2ac`.
- **`visitor/`** (П16): `mobile-full-390.png` 390×14719 → **390×13953**,
  первый экран — тот же блоб; README папки — дата, сборка, скрипт-режим
  генератора, условия съёмки в логе, разница наборов 25,6 % / **12,8 %**
  (было 12,1 %), поправка к шагу 4 протокола (13 из 13 в полосе кадра,
  пятая карточка ленты за краем).
- Кадры семи прогонов — `.playwright-mcp/sesja-2026-09-16/` (вне git);
  в папке доклада — генератор, `unpack.mjs`, `delta.mjs`, `mapowanie.mjs`
  [K1], `miejsca-*.json`, `mapa-*.json/.txt`, 12 журналов `check`,
  `pomiar-dom-1d9b2ac.json`, `bloby-do-1d9b2ac.txt`, `progony.txt`.

# 6. Сверки после эталона

`check` p1, p2, p3 против нового эталона: **игра 15/15 — 0 различий,
главная 11/11 — 0 различий, геометрия совпала (22 / 14 якорей), `exit=0`
×6.** Отрицательная проба — прежний эталон `dc5fc16` против нового:
главная `exit=1` ровно на трёх кадрах (два полных — размер, `jerozolima` —
дата), игра `exit=1` на 15 из 15 (десять — крошки/ряды/подвал, пять —
не сняты). `npm run accept` — 225/225. Гейты 4/4.

# 7. Найденное по ходу — строкой, не правкой

- **Бэклог 49** (новый): находки судей генератора вне правки —
  `MIEJSCA` при `all: true` и нуле совпадений молчит; `SNIETO` литералом;
  словарь `scrollY` дублируется под обоими вьюпортами; `frames.mjs` пункт 19
  (ГОТОВНОСТЬ без предела — холодный браузер за 3 с догрузил 15/16)
  подтверждён в седьмой раз; проза `visitor/README.md` шаг 4 «все
  загружены» — уточнена этой сессией. Генератор живёт копиями в папках
  докладов — при третьем событии стоит вынести в `core/accept/` с судьёй
  (решение владельца, П17 не трогает).
- Пятая карточка ленты главной на телефоне грузится только при прокрутке
  ленты вправо — поведение `loading="lazy"`, не дефект; строка в факты.
- Классификатор среды дважды остановил `cp` в `_baseline/` как
  «необратимое»; исполнено узкими шагами по П58 (блобы в истории git) —
  к сведению будущих пересъёмок.
- **Происшествие после коммита эталона, названо, не спрятано:** текст
  дополнения к П58 с командой отката в обратных кавычках был подан
  в `node -e` через `Bash` — оболочка исполнила кавычки как подстановку,
  и `git checkout 3e5b9c5 -- _baseline` откатил индекс и рабочее дерево
  эталона (HEAD `8d9ba13` и `origin` целы); хвосты `DECISIONS.md`
  и `BACKLOG.md` при этом были дописаны с выеденными код-спанами.
  Восстановлено `git checkout HEAD` на три пути (в них других правок
  не было), проверено по маркерам (README, visitor, зоны, 15 кадров, блоб
  visitor); записи дописаны заново инструментом прямой записи. Бэклог 49
  п. 6 — правило: тексты с кодом только через Edit/Write.

---

# Гейты и проверки

| Проверка | Результат |
|---|---|
| `npm run accept` | 225/225, `exit=0` |
| `npm run gates` / `npm run build` (`1d9b2ac`) | 4/4; `exit=0`, 32 страницы; `h1` 32, `kotwice` 32, `linki` 975, `korytarz` 28 + 4 null, `art` 52/62/25 |
| `dist/` после пункта 7 | один `<time>` подписи главной изменился |
| генератор: diff против П50, `frames.mjs` | четыре заявленных изменения + хвост лога; скрипты дословно; `'\\n'` → `'\n'` в сгенерированном |
| «судью судят» | две линзы: 9 + 4 находки, стоп 0, поправлено 2 + 1 (лог visitor), проба после поправок побайтово равна пробе до |
| проба генератора | 28 кадров, 11 + 7 якорей, `okruszki` есть/нет как ожидалось, 18/18 `scrollY = ожидали` |
| пересъёмка | главная p1 = p2 = p3 (11/11), игра p1 = p2 = p3 (15/15), visitor v1 = v2, геометрия равна |
| `check` p1/p2/p3 vs новый эталон | 0 различий 15/15 и 11/11, `exit=0` ×6 |
| карта по месту (p1 vs `dc5fc16`) | остаток вне мест 0 на 21 кадре; каждое место — пункт списка |
| отрицательная проба (`dc5fc16` vs новый) | `exit=1`: главная 3 кадра, игра 15/15 |
| холодный/тёплый visitor | побайтово равны; первый экран = прежний блоб |
| `git ls-files --eol` | `i/` = `w/`: `site.ts` CRLF; `_baseline/*.json`, README, папка доклада — LF; `sed -i` не применялся; правки — Edit/Write прямой записью |
| `git status` | чисто после каждого коммита; `.playwright-mcp/` игнорируется |

Коммиты: `1d9b2ac` (пункт 7), `8d9ba13` (эталон по П17), далее — доклад,
бэклог 49, дополнение к П58 — одним коммитом, пуш сразу.

---

# Факты, важные будущим сессиям

1. **Эталон равен сборке `1d9b2ac`:** игра 15 кадров, 11821 / 14831;
   главная 9957 / 13953; судья тридцати снова действует.
2. **Дробная высота полосы над `<main>` (крошки 43,84 / 66,69) переснимает
   все оконные кадры страницы** — фаза субпикселя текста и краёв картинок,
   не содержание; считать до правки хрома.
3. **Три прогона одной сборки в этот раз побайтово равны на всех 26 кадрах**
   (и две пробы) — бимодальное зерно полного 390 главной (П50) не проявилось;
   правило «три прогона» остаётся дешёвой страховкой.
4. **Chrome не грузит lazy-картинку за правым краем окна** (карточка ленты
   при x = 1185 на 390); в тёплом браузере грузит из кэша. На кадр 390
   не влияет — байты равны; число «загружено» в протоколе visitor считать
   по полосе кадра.
5. **Первый визит холодного браузера не успевает за 3 с** (пункт 19):
   стоп — в норме; второй запуск того же скрипта проходит.
6. **`mapowanie.mjs` требует чисел у каждого места** — место без элемента
   пишется `brak: true` и должно фильтроваться ([K1]); `all: true` без
   совпадений молчит (бэклог 49).
7. **Четыре из пяти новых кадров слоёв режут ряд сверху и снизу**
   (ряды 933–1073 px в окне 900) — судья детерминирован, приёмка глазами
   ряда целиком — по полному кадру.
8. **Классификатор среды считает перезапись `_baseline/` необратимой** —
   узкие шаги проходят; аргумент — блобы в истории git и решение владельца.
9. **Обратные кавычки в строке, поданной через `Bash` (`node -e`,
   heredoc), исполняются как подстановка команд** — команда в тексте
   записи сработала по-настоящему (раздел 7). Тексты с кодом — только
   Edit/Write; это и есть буква инварианта 1 «правка — прямой записью».

---

# Открытые вопросы

## Технические — решены

- Эталон — p1/v1: все прогоны равны, большинство не понадобилось.
- Стоп visitor — по картинкам в полосе кадра; за краем — строкой.
- `mapowanie.mjs` [K1] — `brak`-места вне рамок; правка судьи названа
  в шапке файла и в этом докладе (судьи генератора её не видели —
  сделана после их старта; проверена отрицательно: до неё карта главной
  падала на всех 11 кадрах, после — остаток 0).
- Рамки зон галереи — по DOM с оговоркой, как в П50; новых зон нет.
- Хеши прежних блобов — по `3e5b9c5` (последняя пересъёмка), не по
  вершине ветки.

## Политические — владельцу (без стопа, П44/П50: между событием и пачками стопов нет)

1. **Приёмка события по таблице (раздел 4) — механическая:** остаток вне
   мест 0, каждое место — пункт закрытого списка, `check` 0 различий,
   отрицательная проба падает где должна. Брака по кадрам не вижу; если
   владелец считает иначе — стоп на кадр, эталон отступает на `dc5fc16`
   (`git checkout 3e5b9c5 -- sites/ac4bf-thewatch.com/_baseline`).
2. **Сессия 3 — пачки A, B, C** (план раздел 10; бэклог 48 меняет счёт:
   Pirates без BF, Rogue −3, Brotherhood ≈ 4 Commons, герой od-czego
   из Mirage) — по коммиту на страницу, после каждой пачки контрольная
   съёмка 26 кадров + `check`; герои Eivor, Rodowód, II wojna, od-czego
   с лидами по П42; доклад пачек. **Старт — командой владельца.**
3. **Выкладка** — по П58 п. 1 после сессии 3, кнопкой владельца; живой
   сайт — `870b8bd`.
4. **Переносятся** без ответа: тип 404, `noindex`, приёмка П53 п. 1–3,
   лист второго сайта, `/404/` глазами, сайт в сети раньше ящика, бэклог
   47 п. 9; бэклог 49 — при следующем событии вида.

**Дальше — сессия 3 по команде; без команды не идти.**

---

# Записи, обещанные этой сессией — по П36

| Что | Где | Источник |
|---|---|---|
| дата подписи главной | `1d9b2ac` (`site.ts`) | П57 список п. 7, П41 п. 2 |
| генератор с `okruszki` и пятью якорями, режим visitor; `unpack`, `delta`, `mapowanie` [K1] | `docs/reports/2026-09-16-sobytie-kadry/` | П58 «Следствия», план п. 8 |
| «судью судят» — две линзы, поправки | раздел 2 этого доклада; шапка генератора | CLAUDE.md инвариант 1 |
| эталон: 15 + 11 кадров, `geometria.json` ×2, `szum.json` игры, `ne-sudyi.json` ×2, README, `visitor/` | `8d9ba13` | П17, П51 п. 1, П16 |
| таблица «кадр → дифф → место → пункт», карты, места, журналы `check`, сводка прогонов | раздел 4; папка доклада | П44 |
| бэклог 49 | `docs/BACKLOG.md` | находки судей |
| команда «старт 2», исполнение, состав эталона 15 | дополнение к П58 в `DECISIONS.md` (исполнитель) | CLAUDE.md «что решаешь сам» |

**Обещано и НЕ сделано:** ничего из объёма сессии 2. Вне объёма и не
тронуто: пачки A–C, герои, выкладка, `frames.mjs` (пункт 19).
