

######## R1-IKONY-1 [дефект → подтверждено/дефект]
Токены читаются регуляркой по сырому global.css: имя из комментария сверка принимает за токен, а позднюю не-hex декларацию не видит
ДОК: Стенд: node D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/ikony/probe.mjs, итог в mut/itog.json. Ложное «сверено» (exit 0): B06 — в global.css только комментарий `/* было: --glow: #ffffff; */`, у «SERPENT» шапки farba = glow. На прод-превью :4331 (kaskad-prod.mjs, DOM правлю только в своём браузере) путь с var(--glow) получает fill rgb(0, 0, 0) на фоне rgb(9, 12, 17), то есть надпись невидима. B07 — то же у трассера иконки, public переписан; сверка печатает «краски: … glow #ffffff». B04 — в конец дописано `:root { --accent: rgb(255 0 0); }`: exit 0, на прод-превью трассер и ромб шапки становятся rgb(255, 0, 0), а иконки остаются #eca84a. B05 — `:root { --ink: var(--danger); }`: exit 0, в каскаде rgb(232, 92, 87). Ложные или неверно названные отказы: B08 — комментарий `/* прежде --accent: #ff0000; */` → «токен --accent объявлен дважды: #ff0000 и #eca84a», exit 1; B12 — `--accent: #eca84a !important;` (значение то же) → 18 строк «краска «accent» — не токен»; B09 — hairline, настоящий rgb()-токен → «не токен src/styles/global.css»; B10 — `--ink: #eee` → 12 строк «ink — не токен». Причина: znak.mjs:56 `/--([a-z0-9-]+):\s*(#[0-9a-f]{6})\s*;/gi` работает по тексту без снятия комментариев и знает только шестизначный hex.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:54-68. Заявления, которые этим опровергаются: znak.mjs:17-18 («краска части — не токен global.css»), DESIGN.md:643-644 («падает, если … краска — не токен»), CLAUDE.md сайта:194 («краски = токены»)
ПРЕДЛ: До поиска снимать комментарии, как делает bezKomentarzy в core/gates/check-contrast.mjs:26 (ядро не трогать — повторить у себя). Если у используемого имени (bg, ink, accent) есть любая декларация не в виде hex, выдавать ОТКАЗ «токен перекрыт не-hex значением», а не «не токен». Для rgb/var/3-значного hex писать точную причину: «токен есть, но не #rrggbb». Добавить самопроверки по образцу check-contrast.mjs:267-269: B04–B08 и B12 как отрицательные и положительные пробы.
СКЕПТИК: Опровергнуть не удалось. Я воспроизвёл всё сам, своим стендом, стендом линзы не пользовался. Файлы: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-1/probe.mjs (копии файлов сайта в stand/<id>, итог в itog.json) и kaskad.mjs (свой статический сервер отдаёт dist только на чтение, правки только в DOM своего браузера, порт 4391).

Код прочитан верно. В znak.mjs:56 стоит `/--([a-z0-9-]+):\s*(#[0-9a-f]{6})\s*;/gi`, он ищет по сырому css (строка 50), комментарии не снимает и видит только шестизначный hex.

Контроль K0: exit 0.

Ложное «сверено» (exit 0):
- S06: в css только комментарий `/* было: --glow: #ffffff; */`, у «SERPENT» краска glow. Контроль S06b (glow нигде нет) даёт exit 1 «не токен», значит пропускает именно комментарий. В каскаде fill пути rgb(0, 0, 0), --glow пуст, фон rgb(9, 12, 17): надпись невидима.
- S07: трассер иконки glow, public переписан. Вывод: «краски: … glow #ffffff».
- S04: в конце `:root{--accent: rgb(255 0 0)}`. Трассер и ромб шапки rgb(255, 0, 0), в иконках остаётся #eca84a.
- S05: `--ink: var(--danger)`. В каскаде rgb(232, 92, 87).
- Новое сверх линзы, S04b: поздний `--accent: #f00` тоже проходит молча (красный в каскаде). Контроль S04c (поздний `#ff0000`) даёт exit 1 «объявлен дважды». Выходит, обещание шапки «или токен объявлен дважды с разными значениями» держится только внутри шестизначного hex.

Ложные или неверно названные отказы (exit 1):
- S08: комментарий `/* прежде --accent: #ff0000; */` даёт «объявлен дважды: #ff0000 и #eca84a».
- S12: `!important` при том же значении даёт 18 строк «не токен».
- S09: hairline, настоящий rgb()-токен, объявлен «не токеном».
- S10: `--ink: #eee` даёт 12 строк «не токен».

Эти отказы громкие, вреда меньше, но причину судья называет ложно. S08b (комментарий с тем же значением) проходит, и это безвредно.

Почему это не предел:
- В ПРЕДЕЛАХ шапки (строки 30–36) названы только fontkitten, sharp и то, что рисунок проверка не судит. Про комментарии и не-hex там ничего нет. В DESIGN.md, CLAUDE.md сайта, README эталона и П83 я этого тоже не нашёл (grep по «комментар», «hex», «rrggbb»).
- Строку 54 «`--имя: #hex;` по всему файлу» можно было бы счесть названной грамматикой. Но она же утверждает «так же читает гейт контраста ядра», а это ложно: ядро с 2026-09-18 снимает комментарии (bezKomentarzy, check-contrast.mjs:26, бэклог 52 п. 2: «Token zapisany w komentarzu też nie jest tokenem»), принимает hex из 3–8 знаков и держит самопроверки 267–269. Инструмент сайта повторил дефект, который в ядре уже исправлен, и сослался на ядро как на образец. Это ещё и расхождение класса «документ» внутри дефекта.
- Заявления без оговорок опровергаются: znak.mjs:17–18, DESIGN.md:643–644 («падает, если … краска — не токен»), CLAUDE.md сайта:194 («краски = токены»). Судья печатает «сверено», когда краска знака на экране не равна литералу в иконках или равна несуществующему токену, а в других случаях отказывает с ложной причиной.

Мои правки к линзе:
1. Трёхзначный hex (S04b) — ещё одна тихая дыра.
2. Снимать комментарии надо сканером, который знает строки, как bezKomentarzy ядра. Иначе `/*` внутри `'…'` съест блок.
3. Сверку смягчает только то, что znak:check по решению владельца не входит в сборку и гейты. Её «сверено» от этого правдой не становится.

######## R1-IKONY-2 [дефект → подтверждено/дефект]
Гарнитура надписи берётся из проверяемых данных (поле font), а не из темы: 700, курсив и Public Sans проходят как «контуры равны гарнитуре»
ДОК: Во всех пробах d пересчитан тем же счётом, что у инструмента, и сверка отвечает exit 0 «надписей 2 — контуры равны гарнитуре». A13 — font = bodoni-moda-latin-700-normal.woff. A14 — font = bodoni-moda-latin-600-italic.woff. A16 — font = ../../public-sans/files/public-sans-latin-600-normal.woff (другое семейство: путь выходит за папку files). A15 — тема сменила импорт на @fontsource/bodoni-moda/latin-400.css, а --font-display на 'Playfair Display'; znak.json не менялся, и сверка этого не заметила. Код: znak.mjs:71 фиксирует папку @fontsource/bodoni-moda/files, znak.mjs:73 делает join(fontDir, n.font) из znak.json. global.css сверка не читает ни в части @import гарнитуры, ни в части --font-display.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:71-84. Заявления: znak.mjs:14-16 («буквы знака остаются контурами Bodoni Moda 600»), DESIGN.md:425-428 («контуры Bodoni Moda 600 темы … сверяются побайтно»)
ПРЕДЛ: Ожидаемый файл выводить из темы: взять из global.css `@import '@fontsource/bodoni-moda/latin-600.css'`, прочитать его @font-face (weight 600, style normal, src) и требовать, чтобы n.font совпадал с woff оттуда. В n.font не допускать разделителей пути. Отдельно сверять семейство в --font-display ('Bodoni Moda'). Проще всего убрать font из данных и брать файл только из импорта темы.
СКЕПТИК: Опровергнуть не удалось. Линза верно прочла код и числа.

Как проверял. Собрал зеркало файлов, которые читает сверка: tools/znak.mjs, src/data/znak.json, src/styles/global.css, src/layouts/Base.astro, package.json и шесть файлов public/. Оно лежит в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-2/site. Зависимости разрешаются в корневой node_modules; в node_modules сайта лежат только .astro и .vite. Пробы гонит proba.mjs в той же папке, копии проб лежат в kopie/. Контур d в пробах пересчитан тем же счётом, что в znak.mjs:72-82.

Результаты (вывод при exit 0 везде одинаковый: «надписей 2 — контуры равны гарнитуре»):
- K0, без изменений: exit 0.
- K1, контроль: font = 700, d не пересчитан. Exit 1, «контур … не равен пересчёту». Значит, сравнение контуров работает, и дефект именно в том, откуда берётся файл гарнитуры.
- A13, font = bodoni-moda-latin-700-normal.woff: у файла fullName «Bodoni Moda Bold». Контуры TH и SERPENT меняются (622→624 и 3951→3990 знаков). Exit 0.
- A14, font = bodoni-moda-latin-600-italic.woff: «Bodoni Moda SemiBold Italic». Exit 0.
- A16, font = ../../public-sans/files/public-sans-latin-600-normal.woff: path.join сворачивает «..» и выходит в папку другого пакета, файл «Public Sans SemiBold». Контур SERPENT 3951→2020 знаков. Exit 0.
- A15, в копии global.css: @import '@fontsource/bodoni-moda/latin-400.css' вместо latin-600, --font-display: 'Playfair Display'; znak.json не менялся. Exit 0.

Код подтверждает находку:
- znak.mjs:71 жёстко задаёт fontDir = @fontsource/bodoni-moda/files.
- znak.mjs:73 делает join(fontDir, n.font), где n.font взят из проверяемых данных.
- global.css читается только ради токенов (строки 50 и 56). Ни @import гарнитуры, ни --font-display сверка не смотрит.
- Znak.astro:46 рисует d прямо из znak.json. Поэтому при A13, A14 и A16 на сайте действительно окажутся жирные, курсивные или гротескные буквы, а сверка их пропустит.

Почему это не названный предел. В шапке znak.mjs раздел ПРЕДЕЛЫ (строки 30-36) называет только fontkitten, растр sharp и то, что проверка 3 не судит о качестве рисунка. О поле font там ничего нет. Шапка (строки 14-15) и поле «_» в znak.json раскрывают механизм: пересчёт идёт «по полям (font, size, track, x, y)». Но в той же фразе обещано «буквы знака остаются контурами Bodoni Moda 600» и сказано «из файла гарнитуры темы». То же обещают DESIGN.md:425-428 («контуры Bodoni Moda 600 темы … сверяются побайтно» — этим обосновано исключение из «Правила восьми ролей»), DESIGN.md:606 и 643-644, а также CLAUDE.md сайта, строки 167 и 194. Ни README эталона, ни DESIGN.md, ни CLAUDE.md сайта этот зазор пределом не называют.

Класс — дефект, а не документ. Сверка пропускает брак, прямо запрещённый в DESIGN («Имя — контуры Bodoni Moda 600»): вес 700, курсив, чужое семейство. При этом она печатает «контуры равны гарнитуре», а шапка обещает гарантию Bodoni Moda 600 темы, которой нет.

Уточнение веса. A13, A14 и A16 — дефект в чистом виде: меняется рисунок на сайте. A15 слабее: буквы знака остаются Bodoni 600, как требует DESIGN, не проверяется только связь «гарнитуры темы» с темой. Тем не менее это то же ложное заявление «из файла гарнитуры темы». Нужно учитывать и то, что znak:check не входит ни в сборку, ни в гейты (DESIGN.md:645-646), а пиксельная приёмка ловит изменение шапки только до пересъёмки эталона. Поэтому сверка — единственный судья этого свойства.

Предложение линзы по сути верно: брать ожидаемый woff из @font-face импорта темы (weight 600, style normal), запретить разделители пути в n.font или убрать поле совсем, отдельно сверять семейство в --font-display.

######## R1-IKONY-3 [дефект → подтверждено/дефект]
PNG сверяется декодером sharp «по пикселям»: формат файла и цветовые чанки не судятся — SVG под именем .png и PNG с gAMA проходят
ДОК: C07 — в favicon-32x32.png записаны байты favicon.svg: сверка exit 0, потому что sharp растрирует SVG при density 72 в те же 32×32. В Chromium 151 (chromium.mjs, файл отдан как image/png) img — onerror, картинка не декодируется. C05 — после IHDR вставлен чанк gAMA 1.0: сверка exit 0, но Chromium рисует px(0,0) 53,61,73 вместо 9,12,17 и центр 246,212,147 вместо 236,168,74. C06 — lossless WebP под именем .png: exit 0; Chromium определяет формат по содержимому и рисует так же (hash 2854397719 = эталон), другие клиенты (iOS apple-touch-icon) не проверены. Верно работает на C01–C04 (один пиксель, в том числе альфа), C08 (JPEG q100 → ОТКАЗ), C09 и C10 (16 бит и палитра → сверено, Chromium даёт тот же hash).
ГДЕ: sites/7thserpent.com/tools/znak.mjs:145-148 (piksele), 165. Заявление: znak.mjs:19-20 («PNG и ICO — по пикселям каждого размера»), DESIGN.md:644 («файл отстал от источника»)
ПРЕДЛ: Перед сравнением пикселей проверять `(await sharp(stary).metadata()).format === 'png'` и сигнатуру 89 50 4E 47. Список чанков файла должен совпадать с тем, что пишет инструмент: IHDR, IDAT, IEND, без gAMA, cHRM, iCCP и sRGB. Другой путь — сравнивать байты, как у SVG, а пиксельную разницу печатать только как пояснение. Смена версии sharp тогда даст громкий отказ, а не молчание.
СКЕПТИК: Опровергнуть не удалось. Я перепроверил всё сам на копии сайта: `.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-3/sajt`, znak.mjs побайтно равен репозиторному, sharp 0.35.4. Контроль: чистая копия даёт exit 0, а `node tools/znak.mjs` пишет public/ побайтно равным репозиторному.

Код линза прочла верно. `piksele()` (znak.mjs:145-148) сравнивает только ширину, высоту и сырые RGBA от sharp. Для PNG вызывается только она (строка 165), для записей ICO тоже (строка 163). Формат, сигнатура и чанки не проверяются.

Мутации (mut.mjs, ico.mjs) и Chromium 151.0.7922.34 (chromium.mjs, файл отдан как image/png; с флагом `--force-color-profile=srgb` и без него результат одинаковый):
- S01: байты favicon.svg под именем favicon-32x32.png. Сверка exit 0, «сверено». В Chromium img получает onerror.
- S02 и S03: gAMA 1.0 в favicon-32x32.png и apple-touch-icon.png. Сверка exit 0. Chromium рисует px(0,0) = 53,61,73 вместо 9,12,17, центр = 246,212,147 вместо 236,168,74. Числа линзы совпали до единицы.
- S05, находка сверх линзы: gAMA 0.45455 плюс cHRM с чужими первичными цветами. Сверка exit 0, а центр в Chromium = 0,253,125: фонарь стал зелёным. Это нарушает «Краски — только токены» (DESIGN.md:613).
- S07: lossless WebP под .png. Сверка exit 0. Chromium даёт тот же hash 2854397719, что у эталона: вреда в Chromium нет, другие клиенты не проверены.
- ICO, находка сверх линзы: слепота та же. Запись 32 с gAMA 1.0 даёт exit 0, запись 32 из байтов SVG тоже exit 0.
- Верно ловится: один пиксель (S08, ОТКАЗ) и, вопреки списку линзы, iCCP. Профиль Display P3 из PNG, созданного sharp, sharp применяет при декодировании, и сверка даёт ОТКАЗ. Чанк sRGB проходит, но безвреден: Chromium рисует тот же hash.

Почему это не названный предел. «ПРЕДЕЛЫ» в шапке znak.mjs (стр. 30-36) называют две вещи: PNG растрирует sharp (librsvg), а не браузер, и сверка «не судит, хорош ли рисунок». Речь там о происхождении растра и о качестве рисунка. Что декодер sharp и декодер браузера могут видеть один файл по-разному, там не сказано. Та же шапка утверждает, что проверка 3 «ловит правку источника без перезаписи и ручную правку файла». S01, S02 и S05 — ручные правки файла, и сверка их пропускает. DESIGN.md:644 обещает отказ, когда «файл отстал от источника», а CLAUDE.md сайта:194 пишет «public/ = источник». В итоге судья печатает «сверено» на файле, который браузер не декодирует или рисует красками не из токенов. По определению задачи это дефект: судья пропускает брак.

Уточнения:
1. Масштаб. Сам инструмент всегда пишет чистые PNG, дыра открывается только при ручной правке или стороннем оптимизаторе. Вкладку Chromium рисует favicon.svg, поэтому S01 бьёт по запасным PNG и тем клиентам, что читают PNG и ICO, а не по вкладке Chromium. Подмена SVG проходит только для размеров 16 и 32: для 180 и 192 размер не совпадает, и сверка отказывает.
2. Поправка к предложению. sharp пишет чанки IHDR, pHYs, IDAT, IEND, так что без pHYs белый список уронит чистые файлы. iCCP уже ловится пикселями, запрещать его ради этой находки не нужно. Проверку формата и чанков надо делать и для PNG внутри записей favicon.ico.

######## R1-IKONY-4 [дефект → подтверждено/дефект]
ICO: заголовок, каталог и формат вложенных картинок не судятся — файлы, которые Chromium не декодирует или рисует пустыми, проходят как «сверено»
ДОК: Сверка exit 0 во всех трёх случаях: D06 (тип 1→0) и D07 (reserved 0→1) — Chromium 151 даёт img onerror; D10 (PNG записи 32 заменён lossless WebP с теми же пикселями) — Chromium декодирует 32×32 полностью прозрачным (hash 0, px 0,0,0,0). Так же exit 0 на D02 (высота записи 16 → 32), D03 (глубина 32→8), D04 (плоскостей 0), D05 (тип CUR): кадр 32 Chromium через img рисует как эталон, кадр 16 через img не виден — это предел моего стенда. Обратный случай, D01: записи переставлены (32 первой), файл корректен, Chromium даёт hash 2854397719 = эталон, а сверка отказывает с неверной причиной «размер 16 отстал; размер 32 отстал». Код: icoObrazy (znak.mjs:135-144) читает только число записей, байт ширины, смещение и длину; сравнение идёт попарно по порядку (163). Мой стенд: page.route Playwright не перехватывает путь, оканчивающийся на favicon.ico (fetch падает, обработчик не зовётся), поэтому ICO отдавались под именем ikona.ico; файл сессии проверен и с прод-превью: 569 байт, равен public, img load 32×32.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:135-144, 158-164
ПРЕДЛ: Сравнивать байты заголовка и каталога (6+16n) с тем, что собрал бы ico(): тип, reserved, ширину, высоту, планы и глубину, а поля длины и смещения проверять на согласованность. Для каждой вложенной картинки проверять сигнатуру PNG. Либо целиком `stary.equals(nowy)`, а пиксельное сравнение оставить для текста сообщения.
СКЕПТИК: Опровергнуть не удалось. Всё воспроизведено независимо от стенда линзы.

Стенд. Неизменённая копия tools/znak.mjs, znak.json, global.css, Base.astro, package.json и шести файлов public/ лежит в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-4/sajt. Модули ищутся так же, как у сайта: из корневого node_modules, своего node_modules у сайта нет. На чистой копии --check даёт exit 0. Мутанты собирает mut.mjs, пересборка без изменений побайтно равна public/favicon.ico (569 B). Файлы отдаёт настоящий http-сервер по пути /Dxx/favicon.ico, без page.route; через chromium.mjs они открываются в HeadlessChrome/151 через img, картинка переносится на canvas.

Чтение кода верное. icoObrazy (znak.mjs:135-144) берёт число записей, байт ширины, длину и смещение. Сравнение (161-163) идёт попарно по порядку, по ширине и пикселям, которые декодирует sharp. Тип, reserved, высоту, число цветов, плоскости и глубину не читает никто, формат вложенной картинки тоже.

Результаты --check и Chromium:
- D06, тип 1→0: exit 0, «сверено»; Chromium даёт onerror.
- D07, reserved 0→1: exit 0; Chromium даёт onerror.
- D10, запись 32 заменена lossless WebP с равными по sharp пикселями (RIFF, 427 B): exit 0; Chromium даёт 32×32, ненулевых байтов 0, пиксель (0,0) = 0,0,0,0.
- D02, D03, D04, D05: exit 0; кадр 32 в Chromium равен эталону.
- D01, записи переставлены: exit 1 с причиной «размер 16 отстал; размер 32 отстал»; Chromium равен эталону.

Уточнение сверх линзы. Кадр 16 я проверил однокадровыми ICO, это снимает её предел стенда:
- высота 32 (как D02): Chromium декодирует 16×32 полностью прозрачным. Значит, D02 тоже брак, который проходит, а не только слепая зона;
- глубина 8, плоскостей 0, тип CUR: кадр 16 равен эталону. D03, D04 и D05 для Chromium безвредны, поведение других клиентов (оболочка Windows) не проверено.

Почему это не предел. Шапка инструмента обещает: «ловит правку источника без перезаписи и ручную правку файла». Назван один предел: «не судит, хорош ли рисунок». В CLAUDE.md сайта: «public/ = источник». DESIGN.md:643: «падает, если … файл отстал». Ни README эталона, ни DESIGN.md, ни шапка не говорят, что заголовок, каталог и формат вложений не судятся. Фраза «ICO — по пикселям каждого размера» описывает метод, но и она не исполнена: «размер» — это только байт ширины, высота не сравнивается. Итог: судья печатает «сверено» для файла, который отличается от того, что пишет ico(), и в Chromium не декодируется или пуст. Судья пропускает брак, это дефект; проза о ручной правке расходится с фактом вслед за кодом.

Оговорки о весе:
- Файл пишет только сам инструмент, поэтому угроза — ручная или сторонняя правка public/. Инструмент заявляет, что её ловит.
- D01 — ложный отказ с неточной причиной: «отстал» вместо «порядок записей». Сам отказ согласуется с контрактом «файл ≠ то, что написал бы инструмент», и npm run znak его чинит. Вреда нет, это текст сообщения.

К предложению линзы. Вариант целиком stary.equals(nowy) слишком строгий. Пиксельное сравнение PNG, судя по устройству проверки, выбрано ради устойчивости к версии кодировщика sharp/zlib. А поля длины и смещения каталога зависят от байтов PNG. Правильнее первый вариант линзы:
- поля каталога, кроме длины и смещения, сравнивать с тем, что собрал бы ico(): тип, reserved, ширина, высота, цвета, планы, глубина;
- длину и смещение проверять на согласованность с размером файла;
- проверять сигнатуру PNG каждого вложения;
- записи сопоставлять по размеру, а не по порядку, либо требовать порядок ico() с честной причиной.

Файлы: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-4/mut.mjs, chromium.mjs, mut/*.ico.

######## R1-IKONY-5 [дефект → подтверждено/дефект]
Ссылки в <head> ищутся подстрокой href="/имя" по всему Base.astro: ссылка в комментарии, в <body>, под {false && …}, с чужим rel, type или sizes проходит
ДОК: Сверка exit 0 во всех восьми пробах. E01 — ссылка на favicon.svg только в HTML-комментарии `<!-- <link rel="icon" href="/favicon.svg" …> -->`. E02 — ссылка удалена из <head>, в комментарии frontmatter дописано «Прежде: href="/favicon.svg"». E03 — ссылка перенесена в <body> (сверка при этом обещает «в <head>»). E04 — `{false && <link …/>}`. E05 — rel="alternate". E06 — у apple-touch-icon rel="preload" as="image". E07 — у SVG type="image/png". E08 — у 16×16 sizes="32x32". Ловит только полное удаление ссылки (E11): «нет ссылки href="/favicon.svg" в <head>», exit 1. Код: znak.mjs:174 `base.includes(`href="/${imie}"`)`.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:173-175. Заявления: znak.mjs:21 («ссылка на файл иконки не стоит в <head>»), Base.astro:9 («npm run znak:check сверяет файлы и эти ссылки»), DESIGN.md:644-645, CLAUDE.md сайта:194 («ссылки в <head>»)
ПРЕДЛ: Снимать комментарии HTML и frontmatter, брать только текст между <head> и </head>, разбирать теги <link> и сравнивать набор {rel, href, type, sizes} с ожидаемой таблицей инструмента по равенству в обе стороны. Надёжнее запускать ту же сверку по dist/index.html после сборки: сейчас в собранной <head> ровно 6 ссылок, вне <head> — 0.
СКЕПТИК: Опровергнуть находку не удалось. Я прогнал её сам: скопировал сайт в макет D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-5/makiety/<проба>/ (копия tools/znak.mjs, package.json, src/data/znak.json, src/styles/global.css, public/*, меняется только Base.astro), запускал probe.mjs, в каждой пробе node tools/znak.mjs --check. Результаты:
- E00 (без изменений) — exit 0.
- E01 (ссылка на SVG в HTML-комментарии) — exit 0.
- E02 (ссылка удалена, в комментарий frontmatter дописано «Прежде: href="/favicon.svg"») — exit 0.
- E03 (ссылка перенесена в начало <body>) — exit 0.
- E04 ({false && <link …/>}) — exit 0.
- E05 (rel="alternate") — exit 0.
- E06 (у apple-touch-icon rel="preload" as="image") — exit 0.
- E07 (у SVG type="image/png") — exit 0.
- E08 (у 16×16 sizes="32x32") — exit 0.
- E11 (строка удалена целиком) — exit 1: «нет ссылки href="/favicon.svg" в <head>».

Код линза прочла верно: znak.mjs:51 читает Base.astro целиком, znak.mjs:173-175 проверяет только base.includes(`href="/${imie}"`). Не смотрятся ни границы <head>, ни комментарии, ни rel, type и sizes.

Пределом это нигде не названо. В шапке инструмента есть блок «ПРЕДЕЛЫ (названы)», но в нём только три вещи: fontkitten, растр sharp и то, что проверка 3 не судит о рисунке. О проверке 4 там ничего. В DESIGN.md:643-646, в CLAUDE.md сайта:194, в Base.astro:9, в README эталона, в DECISIONS (П83) и в BACKLOG тоже нет оговорки о поиске подстрокой.

Обещания при этом прямые. Шапка, znak.mjs:21: «ссылка на файл иконки не стоит в <head>». Текст отказа на строке 174 прямо говорит «в <head>». В E03 ссылка стоит вне <head>, а сверка выходит с exit 0 и печатает «сверено». Значит, сверка пропускает брак, который обещает ловить, — это дефект, а не только расхождение прозы с кодом. E01, E04 и E07 — правдоподобные правки, после которых вкладка теряет favicon.svg, а сверка остаётся зелёной.

Одна поправка к числам линзы. В собранной dist/index.html внутри <head> 8 тегов <link>, а не 6. Иконочных из них 6 (набор равен Base.astro:46-51), остальные два — не иконки. Вне <head> иконочных ссылок 0. На вывод это не влияет.

Предложение линзы разумно: разбирать теги <link> внутри <head>, сняв комментарии, и сравнивать набор {rel, href, type, sizes} с таблицей в обе стороны, либо сверять собранную dist/index.html. Выбор за исполнителем, в рамках «судью судят». Сверка в сборку и гейты не входит: так решил владелец, DESIGN.md:645-646.

######## R1-IKONY-6 [предел → подтверждено/предел]
Сверка односторонняя: лишний файл в public/, внешняя иконка и ссылка на несуществующую иконку проходят, и в сборке второго сайта их не ловит ни один гейт
ДОК: exit 0 во всех трёх пробах. C13 — в public/ лишний favicon-48x48.png (красный квадрат), он уедет в dist. E09 — в <head> добавлено `<link rel="icon" href="https://example.com/x.ico" sizes="any">`, то есть внешний запрос и чужая иконка. E10 — ссылка на /favicon-48x48.png, которого в public нет. Гейт links, читающий href всех тегов (core/gates/links.mjs:77-78), на втором сайте не подключён: astro.config.mjs:23 `integrations: [sitemap(), afterBuild(), anchors()]`. check-assets смотрит только src/assets. Так что мёртвую ссылку на иконку не видит никто.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:150-175
ПРЕДЛ: Проверять, что набор иконок в public/ (по маске favicon*, icon-*, apple-touch-icon*) равен шести файлам, а набор <link rel~=icon|apple-touch-icon> в <head> — ожидаемому, без лишних href и без внешних адресов. Либо назвать это пределом в ПРЕДЕЛАХ znak.mjs и в DESIGN.md.
СКЕПТИК: Опровергнуть не удалось, все три пробы воспроизводятся. Я прогнал их на копии входов tools/znak.mjs: package.json, znak.mjs, znak.json, global.css, Base.astro и public/*, побайтно равных репозиторию (cmp). Копия и пробы лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-6/ (proby.mjs, proba-komentarz.mjs). Результаты `node tools/znak.mjs --check`:
- K0, чистая копия: exit 0.
- C13, лишний красный public/favicon-48x48.png: exit 0.
- C13b, лишние public/favicon.png и site.webmanifest с внешним src иконки: exit 0.
- E09, в <head> добавлен `<link rel="icon" href="https://example.com/x.ico" sizes="any" />`: exit 0.
- E10, в <head> добавлен `<link rel="icon" href="/favicon-48x48.png">` без самого файла: exit 0.
- K1, убрана ссылка на favicon-16x16.png: exit 1. K2, удалён favicon-32x32.png: exit 1. Эти две контрольные мутации показывают, что стенд кусается, то есть exit 0 в пробах — не поломка стенда.

Код линза прочитала верно. Цикл проверки (znak.mjs:152-168) идёт только по ключам `pliki`, public/ он не перечисляет вовсе. Проверка ссылок (znak.mjs:173-175) — это `base.includes('href="/<имя>"')` для шести ожидаемых имён. Лишних и внешних href она не ищет. Текущий dist/ зеркалит public/: там лежат шесть иконок, так что лишний файл уехал бы в сборку.

В сборке это действительно не ловит ни один гейт:
- run.mjs держит четыре гейта исходников (kontrast, tokeny, zasoby, struktura). check-assets смотрит только src/assets по списку gates/assets.mjs.
- astro.config.mjs:23 подключает `[sitemap(), afterBuild(), anchors()]`. Без links — решение П62, оно названо там же, в строках 17-20.
- Сам znak:check в build не входит (DESIGN.md:645-646).
- В _baseline/ нет HTML, так что accept:html голову страницы тоже не стережёт.

Одна неточность в доказательстве линзы. Гейт links не поймал бы E09, даже будь он подключён: чужой домен — «poza ramą» (core/gates/links.mjs:25-26, 117). E10 он поймал бы: файла нет в dist, значит отказ (links.mjs:27-29).

Почему это предел, а не дефект. Судья не врёт: шапка znak.mjs (пункты 3 и 4) и DESIGN.md:643-645 описывают ровно проверку «в одну сторону»: ожидаемые файлы на месте и равны источнику, ожидаемые ссылки есть. Сообщение «сверено — 6 файлов public/» тоже не утверждает, что в public/ всего шесть иконок.

Но этот предел нигде не назван. Его нет ни в ПРЕДЕЛАХ znak.mjs (строки 30-36), ни в DESIGN.md «Site mark», ни в CLAUDE.md сайта, ни в README эталона, ни в BACKLOG. При этом он касается прямого требования владельца: П83 п. 2.2 «без внешних запросов». Внешняя иконка в <head> (E09) проходит сверку молча, а запрет внешних запросов стоит только в приёмке («внешних запросов в dist нет»), постоянного стража нет.

Итог: предел честный, но неназванный. Нужно либо назвать его в ПРЕДЕЛАХ znak.mjs и в DESIGN.md, либо сделать сверку двусторонней, как предлагает линза.

Две смежные вещи, вне этой находки, для оркестратора:
1. Проверка 4 обещает найти ссылку «в <head>», а ищет подстроку во всём Base.astro, включая комментарий. Проба K3: ссылка на favicon.svg убрана из <head>, `href="/favicon.svg"` оставлен в doc-комментарии фронтматтера — exit 0. Это проза против факта, класс «документ» или «дефект».
2. DESIGN.md:645-646 называет «в сборку и гейты сверка не входит» решением владельца, но в DECISIONS.md (П83 и дополнение) и в BACKLOG такой записи нет. Проверить на класс «документ».

######## R1-IKONY-7 [предел → подтверждено/предел]
Шапка и иконки — три отдельных рисунка одного файла, сверка их не связывает; «надписей 0 — контуры равны гарнитуре» проходит
ДОК: exit 0 во всех трёх пробах. A03 — точка перекладины шапки 0,5 → 3,5. A04 — napisy: [] (буквы убраны); сверка печатает «надписей 0 — контуры равны гарнитуре», то есть молча проходит на нуле — ровно тот класс, который гейт контраста запрещает явно (check-contrast.mjs:117-118: «zero par przechodziło po cichu»). A17 — трассер иконок 32 и 16 перекрашен в danger (#e85c57), public переписан, а в шапке он по-прежнему accent: «краски: … danger #e85c57, accent #eca84a». Координаты разные по построению: перекладина шапки 18,5×4 против 15,3×4 у иконки 32, клин трассера 2,6→5 в ширину и 29 в высоту против 3,4→6 и 18. Шапку сторожит пиксельный эталон; соответствие «фавикон из того же знака» не судит ничто, кроме глаз.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:72-86; src/data/znak.json (shapka / ikona / ikona16); DESIGN.md:637-640, 772-773 («держать знак одним рисунком»)
ПРЕДЛ: В сверку добавить: napisy.length === 2 с текстами TH и SERPENT, по три части у шапки и у иконки 32, одинаковая краска по роли (перекладина — ink, трассер и ромб — accent) у shapka, ikona и ikona16. В DESIGN.md назвать прямо: «один файл, три рисунка; подобие иконки шапке — приёмка глазами».
СКЕПТИК: Опровергнуть не удалось. Все пробы я прогнал на копиях сайта: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-7/probe.mjs, probe2.mjs и probes.json, файлы репозитория не тронуты, dist не пересобирался.

Воспроизведение. Контроль K0 на нетронутой копии: exit 0, «надписей 2». Пробы линзы: A03 (перекладина шапки начинается с 3,5 вместо 0,5) даёт exit 0. A04 (napisy: []) даёт exit 0 и печатает «надписей 0 — контуры равны гарнитуре». A17 (трассер иконок 32 и 16 перекрашен в danger, public/ переписан) даёт exit 0 при записи и при сверке. Мои пробы на границе. S1: иконка 32 заменена квадратом снега — exit 0. S2: у ikona нет частей, у ikona16 нет прямоугольников, то есть фавикон — голая плитка bg — exit 0, печать «краски: bg #090c11». Координаты у линзы верны: перекладина 18,5×4 против 15,3×4; клин 2,6→5 при высоте 29 против 3,4→6 при высоте 18. Отношение перекладины к высоте трассера — 0,64 у шапки и 0,85 у иконки. Иконка 32 к тому же нарушает правило «Геометрия» из DESIGN.md:620-622: ромб выступает за конец черты на 2,7 при толщине 4, а допустимо не больше 2,2 (у шапки ровно 2,2). Сверка это пропускает.

Почему это предел, а не дефект. В шапке tools/znak.mjs:13-21, в DESIGN.md:643-645 и в CLAUDE.md сайта (строки 167 и 194) сверка обещает четыре вещи: контуры равны гарнитуре, краски — токены, public/ равен источнику, ссылки стоят в head. Во всех пробах эти обещания выполнены. Подобия иконок шапке она не обещает. В шапке инструмента (строки 35-36) прямо сказано, что рисунок «не судит… это приёмка глазами». Приёмку фавикона глазами («вкладка браузера») владелец назначил сам — П83 п. 4. Шапку сторожит пиксельный эталон: Znak.astro:30 рисует прямо из znak.shapka, а шапка есть во всех 13 кадрах. Зоны szum.json, накрывающие шапку, допускают разницу 1–3 на канал, и A03 или A04 их не пройдут. Честно, но слепо на связи трёх рисунков — это предел. Неназванная часть: «один файл, три независимых рисунка» и проход при нуле надписей. Предложение линзы назвать это в DESIGN.md по делу.

Поправки к доказательству линзы.
(1) Сравнение с check-contrast.mjs:117-118 («ровно тот класс») натянуто. В гейте контраста к нулю пар вела любая осечка поиска. Здесь чтение прямое, и осечка шумная: проба S3 переименовала ключ napisy и получила TypeError, exit 1. Ноль получается только если данные нарочно опустошены. Счёт «надписей 0» печатается, а пустую шапку ловит эталон.
(2) Цитата «краски: … danger, accent» ничего не говорит о шапке. tools/znak.mjs:177 собирает строку «краски» только из znak.ikona (tlo и czesci), так что accent в ней — ромб иконки 32. Краски шапки и ikona16 не печатаются вообще. S6 (красный трассер только у иконки 16) печатает «bg, ink, accent» при exit 0. S7 (danger у трассера шапки) печатает то же самое. Строка «краски» неполна и может ввести в заблуждение при приёмке по докладу. Это отдельная строка в отчёт, вне этой находки.

Документная сторона. Проза «фавикон — тот же рисунок» (Znak.astro:5), «Та же семёрка» (DESIGN.md:647) и «держать знак одним рисунком» (DESIGN.md:772) расходится с данными: в файле три рисунка разных пропорций. Предложение линзы в DESIGN.md это покрывает. К её проверкам по составу стоит добавить сверку геометрии по правилу 2,2 и печать красок всех трёх рисунков.

######## R1-IKONY-8 [предел → опровергнуто/нет]
Сверка сравнивает контуры с woff, а браузеру тема отдаёт woff2 — это другой экземпляр гарнитуры (Bodoni Moda 11pt); расхождение на глаз не видно
ДОК: woff2.mjs: woff — «BodoniModa-SemiBold», woff2 — «BodoniModa11pt-SemiBold», у обоих upem 2000, вес 600, Version 2.005. В @font-face latin-600.css woff2 стоит первым, его и берёт Chromium. У T, H, S, P контуры равны; у E равна форма, но не запись (растр 10x — 0 px разницы); у R и N разное число точек (154/162 и 40/42), advance и bbox равны. woff2-raster.mjs, «SERPENT» теми же полями: при 1x различаются 13 px (не больше 7/255), при 2x — 30 px (не больше 12/255), при 10x — 269 px (не больше 28/255). d «TH» из woff2 равен d из woff, d «SERPENT» — нет. Картинка разницы: .playwright-mcp/sesja-11/sud-r1/ikony/woff2/serpent-woff-vs-woff2-10x.png.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:30-31 (ПРЕДЕЛЫ), 71-73; DESIGN.md:425-428; node_modules/@fontsource/bodoni-moda/latin-600.css
ПРЕДЛ: Назвать в ПРЕДЕЛАХ znak.mjs и в DESIGN.md: «контуры — из woff (BodoniModa-SemiBold); заголовки браузер рисует из woff2 (экземпляр 11pt), R и N различаются в долях пикселя». Менять d под woff2 не надо: это изменит знак и эталон, решает владелец.
СКЕПТИК: Я перепроверил всё своими скриптами: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-8/a.mjs, b.mjs, c.mjs, d.mjs.

Линза верно передала факты. У woff postscriptName «BodoniModa-SemiBold», у woff2 «BodoniModa11pt-SemiBold»; у обоих upem 2000, вес 600, Version 2.005. В latin-600.css woff2 стоит первым. d «TH» из woff2 равен d в znak.json, d «SERPENT» не равен. Числа 154/162 и 40/42 — это счёт чисел в d, то есть 77/81 и 20/21 точек, линза пишет «точек» неточно.

Ошибочна посылка «это другой экземпляр гарнитуры». Экземпляр один и тот же (оптический кегль 11pt, насыщенность 600), а файлы — две сборки:
1. В name/STAT у woff тоже есть метка «11pt» (ID 268).
2. У всех 190 глифов латиницы-1 advance и bbox равны, расхождений 0. Контуры 86 глифов совпадают побайтно.
3. Отличается только устройство контура. В woff2 перекрытия не слиты: у E 2 контура, у R 4, у N 2. В woff перекрытия удалены, у E 1, у R 2, у N 1. Отсюда разная запись d.
4. Заливка выходит той же фигурой. В Chromium 151 без сглаживания (crispEdges, масштаб 40) у E различий 0, у R и N — щепки на стыках перекрытий общей площадью 0,049 px². Со сглаживанием при DPR 1 различаются 4 px (не больше 29/255), при DPR 2 — 3 px. Числа линзы 13/30/269 px получены растром sharp, а не браузером.

Дефектом это не делает ничего:
- Судья не врёт. В шапке znak.mjs проверка 1 — «пересчёт из файла гарнитуры темы по её полям (font, …)», и поле font в znak.json прямо называет файл: bodoni-moda-latin-600-normal.woff. Текст ошибки тоже называет файл.
- woff — файл самой темы: он стоит в src того же @font-face, и в dist/_astro лежат оба файла.
- Судья ничего не упускает. Знак — это path, браузерный шрифт он не использует. Нигде (DESIGN.md 425–428 и 606–607, Znak.astro, znak.json «_», CLAUDE.md сайта) не утверждается, что буквы знака совпадают с растром заголовков: «голос h1» — это кегль и разрядка, а не тождество. Цель проверки — «контуры гарнитуры, а не рисунок на глаз» — достигается и на woff.

Предложенная правка текста сама внесла бы неправду: будто woff — не экземпляр 11pt и будто R и N различаются формой, а не записью.

Вне объёма находки одна строка: global.css:8 пишет «в сборку уезжают woff2 сайта», а в dist едет и woff. Фраза неполна, но не ложна.

######## R1-IKONY-9 [предел → подтверждено/предел]
Битый ICO роняет сверку стеком Node, а не штатным «ОТКАЗ»: остальные проверки не докладываются
ДОК: Во всех пяти пробах exit 1, но вывод — стек, строки «znak: ОТКАЗ» нет. D12 — обрезан до 100 байт: sharp output.cjs:159. D13 — 4 байта и D16 — PNG под именем .ico: ERR_OUT_OF_RANGE. D14 — пустой файл: ERR_BUFFER_OUT_OF_BOUNDS. D15 — смещение за концом: «Input Buffer is empty». Код возврата верный (регрессия = exit≠0), но проверки ссылок Base.astro и следующих файлов до конца не доходят.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:135-148, 158-164
ПРЕДЛ: Разбор и декод каждого файла обернуть в try/catch и писать строку в bledy: «public/favicon.ico: не читается (…)». Остальные проверки тогда отработают и попадут в доклад.
СКЕПТИК: Опровергнуть не удалось. Я прогнал всё сам на копии сайта: .playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-9/site/ (tools/znak.mjs побайтно равен рабочему по cmp; sharp, fontkitten и @fontsource берутся из корневого node_modules, как и у сайта). Пробы лежат в proby.mjs, прогоны — в run/K*. Контроль K00 (чистая копия): exit 0, «сверено». Пять проб линзы повторены в точности. K12 (ICO обрезан до 100 байт): exit 1, стек «vipspng: libpng read error» из sharp dist/output.cjs:159, путь через piksele znak.mjs:146 и :163. K13 (4 байта): RangeError ERR_OUT_OF_RANGE. K14 (пустой файл): ERR_BUFFER_OUT_OF_BOUNDS. K15 (смещение за концом): «Input Buffer is empty». K16 (PNG под именем .ico): ERR_OUT_OF_RANGE. Строки «znak: ОТКАЗ» нет ни в одной пробе. Маскировку проверил отдельно. K20: пустой ICO, чужой icon-192.png и выбитая ссылка apple-touch-icon в Base.astro. Итог — только стек, две настоящие ошибки не названы. K21: то же без битого ICO. Итог — «ОТКАЗ — 2», обе строки на месте. Теряются не только проверки после ICO. Всё, что уже накопилось в bledy, тоже пропадает: токены, контуры надписей, favicon.svg. Массив печатается только в строках 178–181, а до них падение не доходит. Уточнение к «Где»: тот же класс есть и в строке 165, на PNG. K30 (пустой favicon-32x32.png) и K31 (верный заголовок ICO, внутри мусор) тоже дают exit 1 со стеком. Правка из предложения должна накрыть и ветку PNG, а не только 135–148 и 158–164. Почему класс «предел», а не «дефект»: во всех девяти пробах с битым файлом exit 1. Регрессия по CLAUDE.md — это exit≠0. Брак не проходит, ложного «сверено» нет, судья не врёт. Неполон только доклад одного прогона: после npm run znak (он переписывает все шесть файлов) следующий --check покажет оставшееся. Почему не «документ»: шапка znak.mjs:12–21, DESIGN.md:643–646 и CLAUDE.md сайта:194 обещают «выход 1 / падает, если…». Это правда, а полноты списка они не обещают. При этом предел нигде не назван. В разделе ПРЕДЕЛЫ шапки (строки 30–36) «упадёт громко» сказано только о пропаже fontkitten. В DESIGN.md, CLAUDE.md сайта и _baseline/README.md о битом файле ничего нет, отчёта сессии пока нет. Значит, это предел без названия: его надо либо назвать в шапке, либо закрыть try/catch со строкой в bledy. Побочно, вне этой находки (K40, proba-zagolovok.mjs): ICO с типом 2 (CUR) вместо 1, высотой 0, planes 0 и bpp 0 в обеих записях сверку проходит с exit 0, «сверено». icoObrazy читает только число записей, ширину, длину и смещение. Это отдельная находка для линзы. Моя оплошность: в одной команде оболочки я завёл безвредную переменную, в имени которой была запрещённая подстрока из трёх букв. Сама утилита не вызывалась, файлы не менялись. Файлы репозитория не тронуты, dist не пересобирался, git не использовался.

######## R1-IKONY-10 [документ → подтверждено/документ]
Комментарий инструмента «так же читает гейт контраста ядра» неверен: гейт снимает комментарии, принимает hex из 3–8 знаков и держит на это отрицательные пробы
ДОК: znak.mjs:54 — «Токены: `--имя: #hex;` по всему файлу (так же читает гейт контраста ядра)», регулярка `#[0-9a-f]{6}`, комментарии не снимаются. Гейт иначе: check-contrast.mjs:165 `bezKomentarzy(readFileSync(cssPath))`, :61 `#[0-9a-fA-F]{3,8}`, :267 «tokeny: z komentarza nie liczą się», :268 «powtórka w komentarzu to nie powtórka». Проба B08 это расхождение и показывает: гейт принял бы файл, znak:check его отвергает.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:54
ПРЕДЛ: Исправить само чтение (R1-IKONY-1) так, чтобы комментарий стал правдой. Иначе переписать комментарий: «по сырому тексту, только #rrggbb, комментарии не снимаются — в отличие от гейта контраста».
СКЕПТИК: Опровергнуть не удалось. Линза верно прочитала код. В znak.mjs:50 файл global.css читается сырым, без снятия комментариев. Регулярка на строке 56 такая: /--([a-z0-9-]+):\s*(#[0-9a-f]{6})\s*;/gi. Гейт ядра устроен иначе. check-contrast.mjs:165 сначала вызывает bezKomentarzy(...), регулярка на :61 — #[0-9a-fA-F]{3,8} с флагом g. Отрицательные пробы гейта — :267 и :268.

Воспроизвёл сам: взял настоящий znak.mjs --check и настоящий гейт, прогнал оба на копии сайта. Копия и прогон — в .playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-10/ (sajt/ и proby.mjs), после прогона копия побайтно восстановлена.
- P0, чистая копия: оба exit 0.
- P1, проба B08 (`/* прежде --accent: #ff0000; */`): znak exit 1 («токен --accent объявлен дважды: #ff0000 и #eca84a»), гейт exit 0.
- P3, `--ink` и `--color-ink` = `#eee`: znak exit 1 (ink — «не токен», 12 отказов), гейт exit 0.
- P4, восьмизначный `#eca84aff`: znak exit 1 (18 отказов), гейт exit 0.
- P5, повтор с тем же значением: гейт exit 1 (POWTÓRZONA NAZWA, :65 ловит любой повтор), znak exit 0 (:59 ловит только разные значения). Этого расхождения линза не назвала, а оно подкрепляет находку.
- Ещё одно расхождение: у znak флаг i, поэтому имена в верхнем регистре он принимает, а гейт — нет.

Так что на одном и том же входе вердикты расходятся в обе стороны. Буквально правдива только часть «по всему файлу»: collectTokens гейта тоже собирает токены со всего файла, а не из блока. Утверждение «так же читает» в целом ложно.

Пределом это нигде не названо. В шапке znak.mjs, в разделе «ПРЕДЕЛЫ» (строки 30–36), речь только о fontkitten, sharp и границе проверки 3. DESIGN.md (строки 637–647 и 770–773) и CLAUDE.md сайта про способ чтения токенов молчат. Отчёта сессии пока нет. Наоборот, global.css:36 («Гейт снимает комментарии…») вместе с этой строкой внушает читателю, что знак читает так же.

Почему класс «документ», а не «дефект»: сама строка комментария брака не пропускает. Брак здесь — ложный пропуск P2: токен только в комментарии даёт exit 0. Он же и ложный отказ B08/P1. Это предмет R1-IKONY-1. Здесь же проза расходится с фактом.

Правка по предложению линзы верна. Либо исправить чтение в R1-IKONY-1: снимать комментарии, принимать hex из 3–8 знаков, ловить любой повтор. Либо переписать строку 54 честно: «сырой текст, только #rrggbb, комментарии не снимаются, повтор с тем же значением не ловится — в отличие от гейта контраста».

######## R1-IKONY-11 [документ → подтверждено/документ]
DESIGN.md ссылается на «решение владельца», по которому сверка не входит в сборку и гейты; такой записи нет
ДОК: DESIGN.md:645-646: «В сборку и гейты сверка не входит — это решение владельца». В DECISIONS.md «znak» встречается только в строках 3687, 5066, 5807 и в П83 с дополнением (5819-5916); об исключении znak:check из сборки там ничего нет. П83 п. 4 запрещает только правку ядра (core/gates/run.mjs), а свой build сайта (`"build": "npm run gates && astro build"`, sites/7thserpent.com/package.json) — не ядро. Прецедент DECISIONS.md:97-101 гласит: «обязательность даёт билд, а не чек-лист». Сейчас судью никто не обязан запускать: он ручной.
ГДЕ: sites/7thserpent.com/DESIGN.md:645-646
ПРЕДЛ: Либо сослаться на запись, если она есть вне DECISIONS.md, либо переписать: «в гейты ядра не входит (П83 п. 4 — ядро не трогаем); включение в build сайта — вопрос владельцу». Вопрос вынести владельцу как политический: (а) `npm run znak:check &&` в build сайта (рекомендую — по записи 2026-09-04); (б) оставить ручным и назвать это в CLAUDE.md сайта.
СКЕПТИК: Опровергнуть не удалось, я всё перепроверил сам.

1. Цитата верна. sites/7thserpent.com/DESIGN.md:645-646 дословно: «В сборку и гейты сверка не входит — это решение владельца».

2. Записи о таком решении в DECISIONS.md нет. Grep «znak» даёт строки 3687, 5066 и 5807 (все вне темы), остальные совпадения лежат внутри П83 (5819-5916) с дополнением. Там в п. 2.1 «core/ — ни байта», в п. 3 ядро вне объёма, в п. 4 правка ядра идёт «вопросом, не правкой». П83 п. 5 (приёмка: «гейты 4/4, сборка, astro check…») про znak:check не говорит ничего. Дополнение к П83 содержит три вопроса и три ответа: знак A, подвал, favicon.ico. VOPROSY.md упоминает гейт только в связи с токенами и парами контраста.

3. Слова владельца в стенограммах сессии я проверил отдельно: 896b5a82…jsonl и 29e61480…jsonl, выборка сообщений человека и ответов инструмента вопросов. Там есть стартовое сообщение П83 и один вызов вопросов с ответами «A — Семёрка из трассы», «Тот же знак и в подвале», «Да, добавить favicon.ico». О том, входит ли сверка в сборку, владельца не спрашивали, и он об этом не говорил. Решение не записано и не произносилось.

4. Факт «судья ручной» подтверждается. В sites/7thserpent.com/package.json стоит "build": "npm run gates && astro build". core/gates/run.mjs запускает четыре гейта (kontrast, tokeny, zasoby, struktura) и tools/znak.mjs не вызывает. В sites/7thserpent.com/gates/ лежат только assets.mjs и contrast.mjs. Отставание public/ от znak.json сборка пропустит. Шапка tools/znak.mjs называет свои пределы (fontkitten, растр sharp, «не судит, хорош ли рисунок»), но не говорит, что инструмент ручной и почему. Предела, под который попала бы находка, в документах нет. Прецедент DECISIONS.md:97-101 («обязательность даёт билд, а не чек-лист») процитирован верно.

5. Нюанс. Фразу «это решение владельца» можно прочесть и как «решать владельцу». Но и при таком чтении вопрос владельцу нигде не вынесен: доклада docs/reports/2026-09-24-7thserpent-znak.md ещё нет, в VOPROSY.md этого вопроса нет. Проза всё равно расходится с фактом.

Класс — «документ», а не «дефект». Сама сверка znak:check не врёт и брак не пропускает, когда её запускают. Неверна только ссылка на несуществующее решение, которой прикрыт ручной статус судьи.

Уточнение к предложению линзы. В шапке core/gates/run.mjs есть соглашение: «Nowa bramka ŹRÓDEŁ dochodzi tutaj, do listy — nie do package.json każdej witryny» (новый гейт источников добавляется сюда, в список, а не в package.json каждого сайта). Вариант (а), то есть «npm run znak:check &&» в build сайта, идёт в обход этого соглашения. Путь через ядро закрыт П83. Выбор между (а) и (б) касается устройства гейтов, а решать это вправе только владелец. Поэтому вопрос политический, исполнителю его не решить. Правка прозы по формулировке линзы («в гейты ядра не входит — П83, ядро не трогаем; включение в build сайта — вопрос владельцу») верна. В вопросе владельцу стоит назвать и запись 2026-09-04, и соглашение в run.mjs.

######## R1-IKONY-12 [предел → подтверждено/предел]
theme-color в Base.astro — литерал, который со --bg не сверяется, хотя сверка уже читает оба файла
ДОК: E12 — `<meta name="theme-color" content="#ff0000">` вместо #090c11: exit 0. Правило DESIGN.md:769 («менять theme-color в Base.astro вместе с {colors.bg}») держится только на памяти. В сборке сейчас theme-color = #090c11 = --bg (chisto.mjs).
ГДЕ: sites/7thserpent.com/src/layouts/Base.astro:45; sites/7thserpent.com/tools/znak.mjs:51
ПРЕДЛ: Раз znak.mjs уже читает Base.astro и токены, добавить строку: content у theme-color === tokeny.bg, иначе ОТКАЗ. Либо назвать в DESIGN.md, что это правило вручную.
СКЕПТИК: Опровергнуть не удалось. Линза верно прочла код и числа, я повторил доказательство на копии сайта. Файлы репозитория не менялись, копия и сценарии лежат в .playwright-mcp/sesja-11/sud-r1/skeptik-R1-IKONY-12/ (e12.mjs, e12c.mjs), dist не пересобирался.

Что проверено в коде:
- znak.mjs:50–51 действительно читает global.css и Base.astro.
- Из Base.astro инструмент берёт только одно: ищет подстроки href="/<файл иконки>" (строки 173–175). Строку theme-color он не смотрит.
- В шапке инструмента (строки 13–21) перечислены ровно четыре проверки: контуры надписей, краски-токены, файлы public/ и ссылки в <head>. theme-color среди них нет.
- Grep по всему репозиторию (без node_modules и dist): ни один гейт ядра (run.mjs → check-assets, check-contrast, check-structure, check-tokens) и ни один инструмент сайта theme-color не читает.
- Base.astro:45 содержит content="#090c11". Токены global.css:40 --color-bg и :60 --bg равны #090c11. В dist/index.html theme-color = #090c11, то есть сейчас значения совпадают.

Воспроизведение:
- K0, как есть: exit 0.
- E12, content="#ff0000": exit 0, вывод тот же, «сверено — 6 файлов…».
- E12b, meta theme-color удалён совсем: тоже exit 0.
- E12c, реальный путь из DESIGN.md:769. На копии меняю --bg и --color-bg на #0a0d14, theme-color не трогаю. Сначала --check даёт exit 1 с семью отказами, и все они отсылают только к «npm run znak». После npm run znak повторный --check даёт exit 0 и печатает «краски: bg #0a0d14», а theme-color по-прежнему #090c11. Сверка ведёт исполнителя через правку иконок к зелёному итогу и ни разу не напоминает о второй половине того же правила.

Почему это предел, а не дефект. Судья не врёт: своего охвата он не превышает, theme-color не иконка и в его перечне не заявлен. Брака в своей зоне он не пропускает.

Почему класс подтверждён, а не опровергнут. Предел нигде не назван:
- в блоке «ПРЕДЕЛЫ (названы)» шапки znak.mjs его нет;
- в DESIGN.md:277–278 «литерал равен токену» — утверждение о нынешнем состоянии, про сверку там ничего;
- в README эталона theme-color не упоминается;
- DECISIONS.md:4876 записывает «theme-color = bg» как состояние темы, способ удержания не назван.

Уточнение к классу, со стороны документа. DESIGN.md:769–771 сводит в один пункт две половины: «менять theme-color … вместе с {colors.bg} и перезаписывать иконки (npm run znak) …» и в конце «; сверка — npm run znak:check». Хвост «сверка» грамматически можно отнести ко всему пункту, и тогда проза обещает проверку theme-color, которой нет. Строже он читается как пара к «npm run znak», поэтому основной класс остаётся «предел», а двусмысленность стоит учесть при исправлении.

Любой из двух путей, предложенных линзой, закрывает находку:
- одна строка в znak.mjs: content у meta theme-color в Base.astro === tokeny.bg, иначе ОТКАЗ с подсказкой «theme-color»;
- либо в DESIGN.md:769 и в ПРЕДЕЛАХ znak.mjs назвать, что theme-color сверяется вручную.

Правка znak.mjs — это правка судьи: по инварианту 1 CLAUDE.md она сама пойдёт на состязательную проверку.

######## R1-ETALON-1 [предел → иной класс/документ]
Предел «знак в оконном растре не снят» на 390 закрыт прямым замером: 5 прогонов из 40 дали оконный растр, у всех exit 0, числа те же, что в замере 5
ДОК: Скрипт D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/etalon/proba-okno.mjs повторяет порядок proverka-r2/proba.mjs на dist c1ec5e6, Chromium 151.0.7922.34. Прогонов 40: один проверочный и 39 в пачках. Десятый прогон пачки 4985 я оборвал сам, чтобы уложиться в 40. У пяти прогонов (o-4983-1, o-4983-4, o-4983-9, o-4984-2, o-4984-6) первое окно полного 390 побайтово равно кадру окна: 0 субпикселей против 21 011 у остальных 35. `node core/accept/szum.mjs …/o-4983-9 sites/7thserpent.com/_baseline` (годен, 20/20 на обоих вьюпортах): 12 кадров — 0 различий, полный 390 в допуске: зона 1 — 20926 (макс 3), 2 — 85 (1), 3 — 734 (1), 4 — 19 (2), 5 — 27921 (1), 19 — 11 (1), exit=0. Это ровно числа замера 5. У остальных четырёх полный 390 даёт те же числа. Отдельно по полосам, все пять прогонов (okno-vs-pelny.mjs): шапка, строки 0–68, — 1 субпиксель, макс 1, в точке (132, 32). Это зона 2 (макс 1). Та же точка расходится и в прежнем эталоне 22827b6 (hero 390 против полного, 1 субпиксель) и в p3. Без зерна точка даёт 0, то есть это зерно, а не знак. Подвал, строки 8190–9514, — 0 различий.
ГДЕ: sites/7thserpent.com/_baseline/README.md:275-286 (замер 6, «Предел — шапка в оконном растре»); szum.json:2 («знак в оконном растре не измерен — предел»)
ПРЕДЛ: Записать замер 7: оконный растр 390 на c1ec5e6 получен в 5 прогонах из 40 (playwright-core, порядок линзы). Числа зон 1–5 и 19 те же, что в замере 5, в шапке прибавилось 0 (прежняя точка зерна (132, 32)), в подвале 0. Для 390 предел снять. Прогоны лежат в .playwright-mcp/sesja-11/sud-r1/etalon/o-49xx-*. Для будущих съёмок полезен факт: 4 из 5 оконных растров выпали в прогонах с недогрузом картинок на 1440 (8/20 или 17/20) при четырёх прогонах параллельно.
СКЕПТИК: Суть воспроизводится, но заголовок находки неверен в одном числе, а класс другой.

ПОДТВЕРЖДАЕТСЯ (сам пересчитал по JSON и кадрам линзы). Прогонов 40 (1+10+10+10+9). У пяти (o-4983-1, -4, -9, o-4984-2, -6) первое окно полного 390 побайтово равно кадру окна (0 против 21 011). Полный 390 у всех пяти побайтово одинаков. Хром 151.0.7922.34. szum.mjs даёт по полному 390 зоны 1–5 и 19: 20926/85/734/19/27921/11, это числа замера 5. Шапка, строки 0–68, против эталона: 1 субпиксель Δ1 в (132, 32), зона 2. Та же точка есть в прежнем эталоне 22827b6 (hero против full). В кадрах znak-okno без зерна она даёт 0. Подвал, строки 8191–9514, — 0.

ОПРОВЕРГАЕТСЯ «у всех exit 0». `node core/accept/szum.mjs` против _baseline даёт exit=1 у o-4983-1, o-4983-4, o-4984-2 и o-4984-6. Полный 1440 у них вне допуска: 1 144 289 и 420 945 субпикселей, макс 191, в панелях. Причина — картинки не догрузились: g1440 8/20 и 17/20. exit 0 и 12 кадров с 0 различий — только у o-4983-9. Это единственный из пяти, у кого «ГОТОВНОСТЬ» 20/20 на обоих вьюпортах. README (замер 5) считает только годные прогоны, как «45 прогонов, годных — 40». По этому счёту здесь годных 15 из 40, оконный растр 390 — у одного. Формулировка «5 из 40» в предложенном замере 7 смешала бы годные и негодные.

ИНОЙ КЛАСС. Судья не был слеп. Предел в README:283-286 и в szum.json:2 («новая шапка в оконном растре не снята» / «знак в оконном растре не измерен») расходится с фактом уже в коммите эталона. Кадры окна эталона сами сняты в оконном растре с новой шапкой. Шапка в sticky-кадрах стоит только сверху. По определению оконного растра первое окно полного кадра побайтово равно кадру окна. Поэтому ответ давали сами кадры _baseline. Сверка baseline-mobile-hero-390 с baseline-mobile-full-390 по строкам 0–68 даёт 1 субпиксель Δ1 в (132, 32), внутри зоны 2 (макс 1). Та же сверка на 1440 даёт 0. Во всех 40 прогонах числа «первое окно против окна» (21 011 на 390 и 252 321 на 1440) совпали с числами p3 и s2, снятыми при прежней шапке. Значит, предел снимается не только для 390, как предлагает линза, но и для 1440. Запись «для 390 снять» оставила бы мнимый предел на 1440.

Мелочь. «Подвал 0» ничего не говорит об оконном растре: по p3 оконный растр 390 держится примерно до строки 5034, и подвал лежит в растре полного кадра. В предел подвал и не входил. Его знак без зерна в окне равен полному на обоих вьюпортах (znak-okno, bez-zerna).

Мои файлы: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-ETALON-1/ — szum-o-49xx-*.txt (вывод szum.mjs), shapka.mjs/shapka.txt, zerno.mjs/zerno.txt.

######## R1-ETALON-2 [предел → иной класс/документ]
Предел на 1440 сформулирован неверно: шапку в оконном растре показывает любой кадр hero; не назван знак подвала, которого не накрывает ни одна зона
ДОК: Прямого оконного растра 1440 не дал ни один из 40 прогонов: у первого окна полного 1440 против hero во всех 40 — 252 321 субпиксель. Но признак оконного растра — «первое окно полного кадра = кадр окна». Значит, шапка полного кадра в оконном растре — это шапка hero. В эталоне z1 hero-1440 против полного-1440, строки 0–68: 0 различий. На 390 — 1 субпиксель, макс 1, в (132, 32). Замерено `okno-vs-pelny.mjs … 0 0 69`. У старых оконных растров то же: s2 против q2 — 0, v3 против v1 — 0, p3 против p1 — 1 в (132, 32). Итого на 1440 прибавок к зоне 9 — 0, предел по шапке закрывается числом без нового прогона. Чего README не называет: при растре всего кадра, как у s2 и v3, в оконный растр попадает и подвал. Знак подвала (DOM: 132, 6584,19, 138×38 на 1440; 16, 8286,52 на 390) лежит вне всех 19 зон, и любое его различие дало бы отказ. Замер znak-okno.mjs: зерно скрыто, кадр окна у подвала против полного кадра того же состояния, рамка знака подвала — 0 различий на 1440 и на 390 (znak-okno/wynik.json). Со включённым зерном расходится 12 325 субпикселей (макс 3) — это зерно окна. Старый текст подвала у v3 против v1 (строки 6488–7133) — 0.
ГДЕ: sites/7thserpent.com/_baseline/README.md:281-286; szum.json:2
ПРЕДЛ: Переписать предел числами. Шапка в оконном растре = кадр hero: 0 на 1440 и 1 субпиксель зерна на 390, в зонах 9 и 2. Остаток предела — знак подвала 1440 при растре всего кадра. Его назвать явно (зоны нет, различие даст отказ, то есть судья ошибётся в безопасную сторону) и приложить косвенный замер без зерна: 0 на обоих вьюпортах. Расширять зоны не нужно.
СКЕПТИК: Опровергнуть не удалось: все числа линзы воспроизводятся. Но по сути это не предел судьи, а проза README, которая расходится с фактом, поэтому класс — «документ».

Что я сверил сам, своим скриптом `.playwright-mcp/sesja-11/sud-r1/skeptik-R1-ETALON-2/polosa.mjs`:
- Эталон z1, шапка (строки 0–68), первый экран против полного кадра: 1440 — 0 различий; 390 — 1 субпиксель, макс 1, в (132, 32). Всё первое окно 1440 — 252 321 (макс 3), 390 — 21 011: полный кадр эталона снят в обычном растре.
- Старые оконные растры. Первое окно полного кадра побайтово равно первому экрану: s2 — 0, v3 — 0, p3 — 0. Шапка: s2 против q2 — 0, v3 против v1 — 0, p3 против p1 — 1 в (132, 32). Подвал v3 против v1 (строки 6488–7133) — 0; подвал s2 против q2 (6464–7109) — 0.
- Рамки DOM знака подвала верны: (132, 6584,19, 138×38) на 1440 и (16, 8286,52) на 390. Ни одна из 19 зон их не накрывает: зона 16 кончается на 6488, зона 5 — на 8191. Любое различие там — отказ, то есть ошибка в безопасную сторону.
- Замер без зерна повторил своим прогоном `podval.mjs` (playwright-core, Chromium 151.0.7922.34, 20/20 картинок; результат — `podval-displaynone.json`). Кадр окна при scrollY 6057 (положение кадра блока `start`) и 6234 против полного кадра: знак подвала 1440 — 0, весь подвал ниже призыва — 0. На 390 при трёх положениях прокрутки знак — 0. Первое окно при этом по-прежнему расходится (252 523), значит растры остаются разными.
- Попутно: при `visibility:hidden` зерно не пропало. Числа в рамке знака совпали с кадрами «с зерном» (12 181 и 12 325). Годится только `display:none`, как у линзы.

Почему класс «документ», а не «предел»:
1. Фраза README (строки 282–286) «Предел — шапка в оконном растре… Новая… в оконном растре не снята» неверна по сути. Каждый оконный кадр и есть оконный растр, и по признаку, который README сам же вводит, первое окно такого полного кадра равно первому экрану. Шапка первого экрана z1 равна шапке полного кадра: 0 различий на 1440 и 1 субпиксель на 390 — это в пределах зоны 2 (макс 1). Названный предел пуст.
2. На 390 предел закрыт даже напрямую, и линза этого не назвала. Её же прогоны o-4983-1, o-4983-4, o-4983-9 и o-4984-2 на c1ec5e6 дали полный 390 в оконном растре. Шапка против эталона — 1 субпиксель в (132, 32), знак подвала — 0. `szum.mjs` против эталона: полный 390 в допуске, числа зон 1–5 и 19 совпадают с замером 5 (20 926 / 85 / 734 / 19 / 27 921 / 11).
3. Настоящий неснятый случай — знак подвала 1440, когда весь кадр в оконном растре, как у s2 и v3. Его README не называет. `szum.json:2` и `CLAUDE.md:166` сайта пишут о нём обобщённо («знак в оконном растре не измерен»), но отсылают к README, а там речь только о шапке. Судья этот знак видит: различие вне зон даёт отказ. Значит, это не «честен, но не видит», а ложный отказ в безопасную сторону. Косвенно он уже закрыт: 0 без зерна. Тот же знак в оконном растре лежит и в кадре-судье блока `start`, но там его перекрывает зерно: 12 181 субпиксель, макс 3.

Уточнение к доказательству линзы: из 40 прогонов годных на 1440 (20/20 картинок) только 15, остальные 25 — от 8/20 до 19/20. Вывод от этого не меняется.

Итог: брака судья не пропускает и не врёт. Расходится проза: README называет несуществующий предел (шапку) и молчит о случае знака подвала 1440. Предложение линзы — переписать абзац числами, зоны не расширять — верное.

######## R1-ETALON-3 [документ → подтверждено/документ]
Таблица «Состав» README даёт полному 390 прежний размер 390×9505 вместо 390×9515
ДОК: README.md:92: «| `baseline-mobile-full-390.png` | 390×9505 | сторож |». В README.md:76 записано «390 — 9515 px», geometria.json:10 — «высота»: 9515. В check.txt z1–z5 — «эталон 390×9515, снято 390×9515». diff.mjs против блоба 15df949: «размеры 390×9505 против 390×9515».
ГДЕ: sites/7thserpent.com/_baseline/README.md:92
ПРЕДЛ: Исправить на 390×9515. Это правка разметки, содержание пересъёмки она не меняет.
СКЕПТИК: Опровергнуть не удалось, линза права. Проверил сам. Заголовок PNG D:/SEO/cloud/site-generator/sites/7thserpent.com/_baseline/baseline-mobile-full-390.png (IHDR, байты 16–23) даёт 390x9515. geometria.json, ключ «390».«высота», тоже 9515. В README.md та же высота 9515 стоит в четырёх местах: строка 59 («9505 → 9515»), строка 76 («390 — **9515** px»), строка 395 («9505 → 9515») и строка 405 («390×9505 → 390×9515»). Выпадает только строка 92 таблицы «Состав»: «| `baseline-mobile-full-390.png` | 390×9505 | сторож |». Это высота прежнего эталона 22827b6 (блоб 15df949). Её оставили, когда переписывали таблицу на c1ec5e6. Соседняя строка 81 (1440×7134) при этом верна. Пределом это нигде не названо. Судья тоже не врёт. frames.mjs check и szum.mjs берут размеры из самих PNG и из geometria.json, а из таблиц README frames.mjs читает только столбец «Якорь» (frames.mjs:319–320). Столбец «Размер» никакой инструмент не читает. Так что брак сверка не пропускает: расходится только проза с фактом, это класс «документ». Предложенная правка на 390×9515 — правка разметки, содержание пересъёмки она не меняет. Строка 495 с «390×9505» относится к разделу сессии 10 и там верна.

######## R1-ETALON-4 [документ → подтверждено/документ]
Общая часть «Зоны шума» не обновлена на эталон z1: счёт замеров и прогонов устарел, у точки (1077, 4048) слова «как эталон / в ином состоянии» теперь говорят обратное
ДОК: README.md:119: «Пять замеров: 21 прогон генератором…». С замером 6 замеров шесть, z1–z5 не упомянуты. README.md:129: «Оконные кадры-судьи за 29 прогонов генератора разошлись дважды», а с z1–z5 прогонов 34. README.md:131–133: список полных кадров с точкой (1077, 4048) — w6 без z5. README.md:162–164: «Точка (1077, 4048) — в ином, чем у эталона, состоянии у v3 и w6…; v1, v2, v4, v5 и w1–w5 — как эталон». Пиксель (1077, 4048) полного 1440 (sharp extract): новый эталон z1 и z2–z4 — 54,59,63; v3, w6, linza-e1, r-4972-5, r-4993-3, r-4993-22 — тоже 54,59,63. Прежний эталон w1, v1, v2, v4, v5, w2–w5, z5, r-4972-6 — 54,60,64. Против нынешнего эталона фраза перевёрнута: «как эталон» теперь v3, w6 и прогоны playwright-core, а v1–v5 (кроме v3), w1–w5 и z5 — «в ином состоянии».
ГДЕ: sites/7thserpent.com/_baseline/README.md:116-133, 156-165
ПРЕДЛ: Обновить абзацы. Замеров шесть, прогонов генератора 34 (z1–z5 добавили 0 расхождений оконных кадров, z5 — точку (1077, 4048)). Состояние точки указать относительно эталона z1 (54,59,63) либо назвать оба состояния значениями, не словом «эталон». Зоне 7 это не мешает: она принимает оба состояния.
СКЕПТИК: Опровергнуть не удалось. Я сам повторил замер: скрипт D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-ETALON-4/piksel.mjs (sharp, пиксель полного 1440) и md5 кадров z1–z5.

1) Точка (1077, 4048). У нынешнего эталона _baseline (= z1, хеши совпали на всех 13 кадрах) и у z2–z4 значение 54,59,63. То же у v3, w6, linza-e1, r-4972-5, r-4993-3 и r-4993-22. У v1, v2, v4, v5, w1–w5, z5 и r-4972-6 значение 54,60,64. Строки README.md:162–164 («в ином, чем у эталона, состоянии у v3 и w6…; v1, v2, v4, v5 и w1–w5 — как эталон») перенесены из пакета сессии 10 без изменений (docs/reports/2026-09-24-7thserpent-remake-paneli/paket-etalona/README.md:155–157). Писались они против эталона w1/v1 (54,60,64). Абзац стоит в общей части, оговорки, к какому эталону он относится, у него нет. Оговорка в строках 326–330 («прогоны сессии 10 … к этому эталону не относятся») касается только абзаца «С зонами (сессия 10)». Поэтому против z1 фраза говорит обратное. Я искал, не назван ли этот абзац устаревшим где-нибудь ещё: ни в README, ни в szum.json, ни в DESIGN.md такой оговорки нет.

2) Счёт. Строка 129: «29 прогонов генератора» — это p5 + q5 + s6 + t2 + v5 + w6 = 29, прогоны z1–z5 не учтены, с ними выходит 34. Оконные кадры z1–z5 совпадают побайтово (md5), так что «разошлись дважды» верно и для 34. Строки 131–133: в списке полных кадров, разошедшихся в точке (1077, 4048), есть w6, но нет z5. А z5 от z1 отличается как раз в этой точке, что подтверждают строки 277–279 и 403. Строка 119 «Пять замеров»: смягчающий довод есть — зоны заведены и уточнены пятью замерами, замер 6 их не менял. Но раздел сам описывает замер 6 (строки 275–286), и в перечне основы зон его нет, так что счёт всё равно устарел.

Класс: документ. Судья цел: зона 7 принимает оба состояния, frames.mjs check z1–z5 даёт exit 0, брака он не пропускает. С фактом расходится только проза, а в строках 162–164 она прямо противоречит нынешнему эталону.

Вне объёма находки, одной строкой: README.md:92 в таблице «Состав» указывает для baseline-mobile-full-390.png размер 390×9505, а в строке 76 и у самого кадра 9515.

######## R1-ETALON-5 [документ → подтверждено/документ]
«С 8290 — подвал со знаком и всё ниже на 10 px» верно не целиком: две строки текста подвала не совпадают ни при каком целом сдвиге
ДОК: diff390.mjs и grupy390.mjs, прежний полный 390 (блоб 15df949) против нового. Строки 0–68: 6523 субпикселя, макс 227, рамка 165×34 от (16, 18). Строки 69–8289: 0. Со сдвигом 10 остаются три группы. Строки 8290–8323 — знак вместо текста, 6165 субпикселей, макс 222. Строки 9157–9170 — 6221 субпиксель, макс 156, x 16–371, строка «store pages and remain the property…». Строки 9475–9487 — 2939 субпикселей, макс 156, строка «© 2026 7thserpent.com». При сдвигах 9 и 11 эти строки тоже расходятся. Со сдвигом 10 совпадает всё, что ниже 9488. Глазами строки те же (krop-stary-9120.png и krop-novy-9130.png): подвал сдвинулся на дробное число пикселей, и две строки легли в другую субпиксельную фазу. В таблице у полного 390 нет чисел («— | — | —»).
ГДЕ: sites/7thserpent.com/_baseline/README.md:405
ПРЕДЛ: Дописать в строку таблицы разложение: со сдвигом 10 остаются знак (8290–8323, 6165) и две строки текста в другой субпиксельной фазе (9157–9170: 6221; 9475–9487: 2939; макс 156). На вердикт это не влияет: кадр принят как новый эталон.
СКЕПТИК: Находка подтверждается как расхождение документа с фактом. Но её заголовок и объяснение механизма я опроверг: сверка это показывает прямо.

Как воспроизвёл. Сверку писал сам, git не трогал. Прежний кадр .playwright-mcp/sesja-11/sud-r1/etalon/stary/baseline-mobile-full-390.png: хеш git-блоба посчитан вручную как sha1("blob <размер>\0"+байты) и равен 15df949ea88df4c3368f42dc628583760fd24880, то есть это действительно блоб из README:424. Новый кадр — sites/7thserpent.com/_baseline/baseline-mobile-full-390.png, 390×9515.

Что совпало с линзой, число в число:
- без сдвига строки 18–51 дают 6523 субпикселя, макс 227, x 16–180, то есть рамку 165×34 от (16, 18); строки 69–8289 — 0;
- со сдвигом 10 остаются ровно три группы нового кадра: 8290–8323 (6165, макс 222), 9157–9170 (6221, макс 156, x 16–371) и 9475–9487 (2939, макс 156, x 16–172);
- ниже 9487 со сдвигом 10 различий нет.

Значит, строка README:405 «с 8290 — подвал со знаком и всё ниже на 10 px» действительно верна не целиком, а столбцы «— | — | —» пусты. Класс «документ» верен.

Что линза прочла неверно. Утверждения «не совпадают ни при каком целом сдвиге», «при сдвигах 9 и 11 эти строки тоже расходятся» и «легли в другую субпиксельную фазу» ложны. Обе строки побайтово совпадают при целом сдвиге 9: построчно все 14 строк 9157–9170 и все 13 строк 9475–9487 дают 0 субпикселей (перебор s от −30 до 40; нулевой сдвиг у каждой из двух групп единственный — 9). По соседям видно, что каждая из двух строк поднялась на 1 px: зазор над ней в новом кадре 5 строк вместо 6, под ней — 6 вместо 5. Это та же растеризация, только позиция строки округлена иначе (дробный сдвиг раскладки), а не другая субпиксельная фаза. Вероятная причина ошибки: diff390.mjs суммирует сдвиги 9, 10 и 11 по всей полосе от 8290 до низа, и при сдвиге 9 сумму дают остальные строки, сдвинутые на 10; эту сумму приписали двум строкам.

Поправка к предложению. Дописать в строку таблицы: со сдвигом 10 остаются знак (8290–8323; 6165; макс 222) и две строки текста, сдвинутые на 9 px, а не на 10 (9157–9170: 6221; 9475–9487: 2939; макс 156). Со сдвигом 9 у этих строк 0 различий. Слова про «другую субпиксельную фазу» писать нельзя. На вердикт это не влияет: кадр принят новым эталоном.

Вне объёма находки, строкой в отчёт: README:92 в таблице состава на c1ec5e6 даёт baseline-mobile-full-390.png как 390×9505, а README:76 и сам файл — 9515.

Мои файлы лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-ETALON-5/: sverka.mjs и sverka.txt, polosy.mjs, polosy-9100-9200.txt, polosy-9440-9514.txt, blob.mjs и bloby.txt.

######## R1-ETALON-6 [предел → иной класс/дефект]
Эталон видит знак только в состоянии покоя при DPR 1: ветку без обводки «TH» (плотность больше 1,5), открытый ящик меню и фокус кадры не судят, README этого не называет
ДОК: src/components/Znak.astro:65-67 — `@media (max-resolution: 1.5dppx) { .znak__obwodka { stroke-width: 0.3px` (вне запроса stroke-width 0, :58). geometria.json:8 и :59 — «dpr»: 1 на обоих вьюпортах. В 13 кадрах нет состояния с открытым ящиком и нет фокуса ссылки знака. В README.md поиск «ящик|фокус|DPR» находит только :404 «меню — в ящике» и описание dpr 1. Эти состояния сессия показывает глазами (docs/reports/2026-09-24-7thserpent-znak/yashchik-390.png, fokus-1440.png, vkladka-2x-*.png). Кодовой сверки у них нет.
ГДЕ: sites/7thserpent.com/_baseline/README.md (раздел «Пересъёмка 2026-09-24 (сессия 11)»); src/components/Znak.astro:58-67
ПРЕДЛ: Одной строкой в README назвать, чего эталон не видит: знак при плотности больше 1,5 (чистый контур без обводки), ящик меню, фокус. Судятся они приёмкой глазами по П83 п. 4. Расширять эталон — решение владельца, не исполнителя.
СКЕПТИК: Проверял сам: статическая раздача готового dist без пересборки, playwright-core, Chromium 1234. Скрипты и журналы лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-ETALON-6/ (proverka.mjs и log.json, specifichnost.mjs и specifichnost.json). Шапка при DPR 1 совпадает с кадрами эталона побайтово на 1440 и на 390 (0 субпикселей), значит dist — та же сборка, c1ec5e6.

ЧТО ПОДТВЕРЖДАЕТСЯ, это предел:
- geometria.json: dpr 1 на обоих вьюпортах, строки :8 и :60 (у линзы ошибочно :59).
- В kadry-gen.mjs нет ни нажатия на .hdr__burger, ни Tab. Ящика и фокуса нет ни в одном из 13 кадров. Фокус на ссылке знака (A.hdr__brand) при 1440 отличается от кадра эталона на 2352 субпикселя, максимум 233.
- В README эталона из пределов знака назван только оконный растр (:281-286). На него же ссылается CLAUDE.md сайта :166. DPR, ящик и фокус не названы нигде. Ящик П83 п. 4 прямо отдаёт приёмке глазами, фокус П83 не упоминает.

ЧТО ОПРОВЕРГАЕТСЯ, и почему класс другой. Линза исходит из того, что при DPR 1 кадры видят ветку с обводкой «TH», а ветка без обводки остаётся невидимой. Это неверно: обводки нет ни при какой плотности. В собранном CSS (dist/_astro/index.0TxqFWC-.css) правило `.znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]{stroke-width:0}` имеет специфичность (0,3,1). Правило `@media (resolution<=1.5x){.znak__obwodka[data-astro-cid-yruwqcb3]{stroke-width:.3px}}` имеет (0,2,0) и проигрывает. Замеры:
- Вычисленный stroke-width у .hdr__brand .znak__obwodka равен 0px при DPR 1, 1.25, 1.5, 1.75, 2 и 3.
- Если при DPR 1 принудительно снять обводку, в шапке меняется 0 субпикселей, на 1440 и на 390.
- Если добавить правило задуманной силы (`.znak[cid] path.znak__obwodka[cid]` в том же запросе), при DPR 1 получается 0.3px и шапка меняется на 615 субпикселей, максимум 84. При DPR 2 меняется 0.

Выходит, что оптическая поправка src/components/Znak.astro:61-69 мертва в проде. Эталон c1ec5e6 закрепил шапку и подвал без поправки. Ни один судья этого не ловит: znak:check сверяет контуры, краски, файлы и `<head>`, а вычисленный стиль не проверяет. Проза утверждает обратное — DESIGN.md:629-631, комментарий Znak.astro:56-64 и сообщение коммита c1ec5e6. Предложенная линзой строка README («не видит знак при плотности больше 1,5, чистый контур без обводки») сама была бы ложной: при DPR 1 кадры показывают как раз чистый контур.

Что предлагаю:
- Главное — дефект, это технический вопрос. Поднять специфичность правила обводки, например `.znak path.znak__obwodka`, отдельным коммитом. После этого шапка при DPR 1 меняется во всех 13 кадрах, а подвал — в кадре блока start и в обоих полных, так что эталон придётся переснять. П83 п. 2.3 разрешает пересъёмку внутри сессии, но решать это ведущему или владельцу.
- Одну строку о пределе (ящик, фокус, другая растеризация при плотности больше 1) в README эталона стоит дописать, но только после правки и без ложной части про обводку.

Попутно, вне объёма: README эталона :92 даёт baseline-mobile-full-390.png размер 390×9505, а :76 и geometria.json — 9515.

######## R1-KONTRAST-1 [дефект → подтверждено/дефект]
Оптическая обводка «TH» не работает ни при какой плотности: правило `.znak path { stroke-width: 0 }` перебивает медиа-правило по специфичности; ни один судья этого не поймал
ДОК: В собранном CSS (dist/_astro/index.0TxqFWC-.css) стоят два правила: `.znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]{stroke-width:0}` со специфичностью 0-3-1 и `@media (resolution<=1.5x){.znak__obwodka[data-astro-cid-yruwqcb3]{stroke-width:.3px}}` со специфичностью 0-2-0. Первое сильнее, поэтому обводка не включается. Прогон `node obwodka.mjs` (1440; DPR 1, 1,25 и 1,5): matchMedia('(max-resolution: 1.5dppx)') = true, оба правила совпадают с `path.znak__obwodka`, но вычисленный stroke-width = "0px". Если принудительно задать 0,3px, в рамке шапки меняются 213, 264 и 327 пикселей (разница до 84–95 уровней). Поперечина H в «TH» при DPR 1: самый яркий пиксель rgb(65 69 74), это 2,03:1 к bg (покрытие по строкам в сумме 0,38). С обводкой — rgb(118 123 126), 4,57:1 (покрытие 0,84). В эскизе (voprosy/A-semerka-trassa/opis.md:66-69) записано «поперечина H — 0,83 (было 0,39)»: на сайте осталось прежнее «было». То же в подвале: stili.json показывает sw 0px у обоих path. Почему судьи молчат: znak:check сверяет контуры, краски и файлы, а не действие CSS; эталон 776c24e снят с этой же сломанной сборки c1ec5e6; гейт пар считает номинальную пару ink/bg (16,17:1), а подпись «знак в шапке — перекладина и имя» не видит тонкий штрих. Проза расходится с фактом: DESIGN.md:629-631 («обводится той же краской в 0,3px»), Znak.astro:61-69, сообщение коммита c1ec5e6.
ГДЕ: D:/SEO/cloud/site-generator/sites/7thserpent.com/src/components/Znak.astro:57-69; D:/SEO/cloud/site-generator/sites/7thserpent.com/DESIGN.md:629-631; проверка — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/obwodka.mjs, obwodka-fix.mjs, obwodka-dpr1-jest.png и obwodka-dpr1-wymuszona.png
ПРЕДЛ: В медиа-правиле написать `.znak .znak__obwodka { stroke-width: 0.3px; }`. После скрута получается 0-4-0, это сильнее 0-3-1. Проверено инъекцией (obwodka-fix.mjs): у «TH» 0.3px при DPR 1 и 1,5, 0px при DPR 2 и 3; у «SERPENT» везде 0px. Правка меняет шапку и подвал во всех кадрах эталона, поэтому пересъёмка — решение владельца (инвариант 6): вынести ему вместе с правкой. В приёмку знака добавить сверку вычисленного stroke-width у `path.znak__obwodka` при DPR 1 (ожидается 0.3px) и DPR 2 (ожидается 0px). Для справки: WCAG 1.4.3 не требует контраста от логотипов, так что это нарушение собственного правила сайта, а не WCAG.
СКЕПТИК: Опровергнуть не удалось, всё воспроизведено независимо. Мой скрипт: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-1/proverka.mjs. Он поднимает свой статический сервер над dist (dist только читается), результаты лежат рядом: proverka.json, th-dpr1-jest.png, th-dpr1-z-poprawka.png.

1) Код и сборка. В dist/_astro/index.0TxqFWC-.css стоят два правила подряд: `.znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]{stroke-width:0}` и `@media (resolution<=1.5x){.znak__obwodka[data-astro-cid-yruwqcb3]{stroke-width:.3px}}`. У первого специфичность 0-3-1, у второго 0-2-0. Медиа-блок на специфичность не влияет, поэтому порядок следования не спасает. В index.html у обоих `<path>` (в шапке и в подвале) есть атрибут data-astro-cid-yruwqcb3, а у «TH» ещё и класс znak__obwodka. Значит, совпадают оба правила.

2) Браузер: Chromium 1234, 1440, DPR 1, 1,25, 1,5 и 2. При DPR ≤1,5 matchMedia('(max-resolution: 1.5dppx)') = true, и оба правила совпадают с path.znak__obwodka. Вычисленный stroke-width при этом 0px у всех четырёх path, в шапке и в подвале. Контрольный опыт: я убрал stroke-width из правила `.znak path` через CSSOM. Тогда при DPR ≤1,5 у «TH» получается 0.3px, значит мешает именно это правило, а медиа-запрос срабатывает. При DPR 2 получается 1px (начальное значение при stroke из style). Отсюда вывод: базовое правило нужно оставить, и чинить надо специфичностью, как и предлагает линза.

3) Числа. Поперечина H в шапке при DPR 1: фон rgb(9 13 18), самый яркий пиксель rgb(65 69 74)/rgb(66 70 75), покрытие по столбцам 0,367–0,376. По формуле WCAG это около 2,02:1. Затем я вставил правку линзы в том виде, какой она примет после скрута Astro: `@media (max-resolution:1.5dppx){.znak[cid] .znak__obwodka[cid]{stroke-width:.3px}}`, специфичность 0-4-0. Результат: rgb(118 123 126), покрытие 0,819–0,828; у «TH» 0.3px, у «SERPENT» 0px. Это совпадает с линзой (0,38 → 0,84; 2,03:1 → 4,57:1) и с тем, что эскиз обещал: opis.md:66-69, «0,83 (было 0,39)». Сейчас сайт показывает именно «было». Оговорка: неscoped `.znak .znak__obwodka`, вставленный стилем со стороны, имеет специфичность 0-2-0 и тоже не срабатывает. Правка годится только внутри `<style>` компонента.

4) Предел ли это, названный в документах? Нет. Шапка tools/znak.mjs перечисляет четыре проверки и свои ПРЕДЕЛЫ (fontkitten, растр sharp, «не судит, хорош ли рисунок»), о действии CSS там ни слова. Шапка gates/contrast.mjs говорит о номинальных парах токенов. В _baseline/README.md, в CLAUDE.md сайта, в DECISIONS.md (П83) и в VOPROSY.md обводка не упомянута. А вот Znak.astro:61-64, DESIGN.md:629-631 и сообщение c1ec5e6 («оптическая обводка «TH» 0,3 px при плотности до 1,5») утверждают, что она действует, и это неправда. Владелец выбрал A по эскизу znak.html, где обводка работала: там не было конкурирующего `stroke-width:0`. Эталон 776c24e переснят со сборки c1ec5e6, то есть закрепил брак как норму.

Уточнение класса. Брак в продукте настоящий: правило мёртвое, обещанная поправка эскиза не доставлена. Вся цепочка приёмки его пропустила, а проза компонента, DESIGN.md и коммита утверждает обратное. Поэтому основной класс — «дефект». Для самих знак:check и гейта пар это «предел»: их объявленный объём честен и действие CSS не включает. Расхождение DESIGN.md:629-631 с фактом относится к классу «документ». Предложение линзы корректно: проверку вычисленного stroke-width (DPR 1 → 0.3px, DPR 2 → 0px) стоит добавить в приёмку знака. Правка меняет пиксели шапки и подвала, поэтому пересъёмка эталона — решение владельца (инвариант 6).

######## R1-KONTRAST-2 [дефект → подтверждено/дефект]
kontrast-znaka.mjs мерит не то, что видит зритель: снимок, обрезанный по рамке знака, по-другому считает размытие шапки
ДОК: `node klip.mjs`: одна и та же страница и позиция прокрутки, меняется только clip снимка. 1440, scrollY 4160: clip = рамка знака — 29 33 37 (7,92:1); рамка ±5 px — 31 35 39 (7,73); шапка во всю ширину, всё окно и снимок без clip — 32 36 40 (7,64). 1440, 4200: по рамке 33 36 41 (7,61), а на экране 31 35 39 (7,73). 390, 2780: по рамке 36 39 44 (7,325), на экране 34 37 42 (7,515). Ошибка идёт в обе стороны. Отсюда: худший фон, который сессия приводит для 1440 — «rgb(33 36 41) при scrollY 4200», 7,61 и 12,85 (я повторил прогон инструмента и получил те же числа) — на экране не встречается. Мой замер по экрану (skan2.mjs: снимок полосы шапки 0..69 во всю ширину, шаг 5; на 32 проверках против полного окна 0 расхождений) даёт на 1440 при DPR 1 rgb(33 37 41) при 4210, то есть 7,54 и 12,73. На 390 значение 36 39 44 (7,325 и 12,37) настоящее, но на экране оно при 3730, а не при 2780 (там 34 37 42).
ГДЕ: D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs:44; числа — D:/SEO/cloud/site-generator/sites/7thserpent.com/gates/contrast.mjs:79-80; проверка — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/klip.mjs, skan2.mjs, skan2-1440-dpr1-k5-czysty.json
ПРЕДЛ: Снимать всю полосу шапки ({x:0, y:0, width:w, height:69}) или окно без clip, а рамку знака вырезать уже в памяти. Числа для 1440 в gates/contrast.mjs исправить на 7,54 и 12,73 (DPR 1) либо заменить все числа аналитической границей из R1-KONTRAST-4. Вывод «выше порогов» от этого не меняется.
СКЕПТИК: Опровергнуть не удалось, воспроизвёл сам. Всё моё лежит в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-2/. Страница с localhost:4331 побайтно равна dist/index.html.

1) Это не время и не прокрутка (proverka.mjs, proverka.json). Предзагрузка в точности как в инструменте, scrollY проверен до и после. На каждой позиции снимки шли вперемешку и с повтором: сырой CDP Page.captureScreenshot без clip (дважды), Playwright без clip, clip по рамке знака (дважды), рамка ±5 px, полоса шапки {0,0,w,69}. Результаты:
- Сырой CDP = Playwright без clip = полоса шапки: 0 различий, повторы тоже совпадают.
- Clip по рамке стабилен (R1 = R2), но отличается от окна в ~11–13 тыс. из 15 732 значений каналов, до 4–9 уровней. Рамка ±5 даёт третий вариант.
- Числа линзы совпали: 1440 при 4200 — рамка 33 36 41 (7,61), окно 31 35 39 (7,73); при 4210 — рамка 31 35 39, окно 33 37 41 (7,542); 390 при 2780 — рамка 36 39 44 (7,325), окно 34 37 42 (7,515); при 3730 оба дают 36 39 44.

Механизм: blur(14px) шапки (core/chrome/SiteHeader.astro:205) считается по фону, обрезанному clip. Снимок, накрывающий всю коробку шапки, равен окну, частичный — нет. Ошибка идёт в обе стороны.

2) Сам инструмент (dvojnoj.mjs, dvojnoj.json). Его цикл без изменений: шаг 20, scrollTo(0, y) плюс 40 мс, расхождений scrollY 0. На каждом кадре два снимка — clip по рамке и полоса шапки.
- Способ инструмента воспроизводит его вывод: 390 — rgb(36 39 44) при 2780, 7,325/12,368; 1440 — rgb(33 36 41) при 4200, 7,61/12,85.
- На экране на той же сетке: 390 — 35 39 43 при 3720, 7,351/12,413; 1440 — 32 36 40 при 4160, 7,636/12,894.
- Clip расходится с экраном в 411 из 434 кадров (390) и в 226 из 312 (1440).

Итог: числа и позиции в gates/contrast.mjs:79-80 и DESIGN.md:332-333 сняты с отрисовки, которой зритель не видит. Число 390 совпадает с правдой случайно: 36 39 44 на экране есть, но при 3730, а не при 2780.

3) Как предел нигде не названо. «clip» не встречается ни в DESIGN.md, ни в README эталона, ни в шапках инструментов. Шапка kontrast-znaka.mjs обещает «худший пиксель фона в рамке знака», то есть на экране. Поэтому это не предел, а дефект: судья печатает не то, что видно.

Уточнения к линзе:
(а) Разрыв 7,61 → 7,54 на 1440 лишь отчасти от clip. Часть — от объявленного шага 20 («шагом 20 px»): 4210 вне сетки. Если исправить только съёмку, инструмент напечатает 7,64/12,89 (1440) и 7,35/12,41 (390). Чтобы получить 7,54/12,73, шаг тоже нужно уменьшить до 10 или 5.
(б) Сам цвет 33 36 41 на экране встречается (у линзы — при 4175 и 4190). Не встречается пара «33 36 41 при 4200», и это не худшее для 1440.
(в) На сетке инструмента артефакт сделал обе итоговые цифры чуть строже экранных. На вывод «выше порогов 3 и 4,5» он не влияет.

Предложение линзы верно: снимать полосу шапки или окно без clip и вырезать рамку в памяти.

Побочно, вне объёма: в proverka.mjs первая позиция 1440 встала на 4327 вместо 4160 — это instant-прокрутка моего скрипта сразу после плавной предзагрузки. К инструменту не относится: в dvojnoj расхождений scrollY 0.

######## R1-KONTRAST-3 [дефект → опровергнуто/нет]
Предзагрузка kontrast-znaka.mjs не долистывает страницу: при scroll-behavior: smooth картинки не загружаются, хотя комментарий обещает «картинки — после прокрутки»
ДОК: В global.css:117 стоит `html { scroll-behavior: smooth }`, поэтому `window.scrollTo(0, y)` в цикле предзагрузки (шаг 400, пауза 60 мс) анимируется. `node plavno.mjs`: к концу цикла scrollY = 983 из 8671 на 390 и 732 из 6234 на 1440; незагруженных картинок 13 из 20 и 10 из 20. Promise.race выходит по таймауту 3000 мс. В точной копии цикла (skan.mjs, режим orig, 390) в кадрах 4860–5040 (10 кадров) под шапкой лежали по 2 незагруженные картинки. В этом прогоне на результат это не повлияло: яркость кадров совпала с замером при загруженных картинках. Ещё: краски в инструменте зашиты литералами (строка 20: #eca84a и #e5eaee), а не читаются из global.css.
ГДЕ: D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs:8, 20, 28-34; проверка — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/plavno.mjs, skan-390-dpr1-k20-orig.json
ПРЕДЛ: Снять плавность: `document.documentElement.style.scrollBehavior='auto'` или `scrollTo({top, behavior:'instant'})`. Поставить картинкам loading='eager' и дождаться load и decode() без таймаута. В каждом кадре проверять, что под шапкой нет незагруженных картинок, и падать, если есть. Краски брать из global.css.
СКЕПТИК: Я воспроизвёл находку сам. Мои скрипты: .playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-3/povtor.mjs и sravnenie.mjs, результаты в povtor.json и sravnenie.json. Сервер localhost:4331, dist не пересобирался, prefers-reduced-motion: no-preference, scroll-behavior: smooth.

1. Что у линзы верно. Сам цикл предзагрузки (for, шаг 400, пауза 60 мс) до низа не доходит: плавная прокрутка каждый раз прерывается следующим вызовом. У меня к концу цикла scrollY = 1308 на 390 и 704 на 1440, незагружено 13 и 10 картинок из 20. Это близко к числам линзы (983 и 732).

2. Где линза ошиблась. Она считала состояние сразу после цикла, а инструмент после цикла ещё ждёт Promise.race. За это время продолжается плавная прокрутка к последней цели цикла, то есть к низу страницы, и ленивые картинки успевают загрузиться. Состояние на выходе из предзагрузки (povtor.json):
- 1440: race закончился за 272 мс, а не по таймауту; незагруженных 0 из 20.
- 390: scrollY = 8671, это конец страницы; незагруженных 2 из 20. Это mp3-k10 и mp1-k05 горизонтальной ленты карточек с x = 1193 и x = 1485 при ширине окна 390. Они за правым краем окна. Таймаут 3000 мс на 390 даёт ожидание именно этих двух картинок.

3. «По 2 незагруженные картинки под шапкой в кадрах 4860–5040» — это те же две карточки ленты. Фильтр линзы в skan.mjs:64 проверяет только вертикаль (b.bottom > 0 && b.top < 68) и не проверяет x. Я добавил проверку по обеим осям с учётом обрезки переполнением предка: 0 кадров из 434 на 390 и 0 из 312 на 1440, где незагруженная картинка пересекает рамку знака (16..154 и 132..270 по x).

4. Повлиять на замер это не может. Я прогнал один и тот же проход замера после трёх вариантов подготовки: предзагрузка инструмента дословно, чистая (scroll-behavior снят, eager, instant, load и decode) и вовсе без предзагрузки. Покадровый максимум пикселя совпал во всех трёх: 0 расходящихся кадров из 434 и из 312. Итоги тоже одни: 390 — rgb(36 39 44), фонарь 7,32, снег 12,37; 1440 — rgb(33 36 41), фонарь 7,61, снег 12,85. Это ровно те числа, что записаны в gates/contrast.mjs:79–80 и DESIGN.md:332–333. Причина: проход шагом 20 px сам подгружает ленивые картинки с запасом отступа Chromium, поэтому к моменту, когда картинка доходит до шапки, она уже загружена. Кроме того, scrollY совпал с целью во всех кадрах прохода: прокрутка на 20 px успевает за 40 мс.

5. Комментарий в строках 7–8 («картинки — после прокрутки») соответствует факту. После предзагрузки загружено всё, что может попасть в рамку знака; незагружены только две карточки за правым краем окна на 390, в рамку они не попадают. Судья не пропускает брак и не врёт.

6. Литералы красок в строке 20 (#eca84a и #e5eaee) побайтно равны --accent и --ink в sites/7thserpent.com/src/styles/global.css:64 и :68. Инструмент отчёта привязан к сборке c1ec5e6: так записано в contrast.mjs:78, и это не гейт. Неверного числа сейчас нет; при смене красок это вопрос сопровождения, а не класс судьи.

Остаток вне класса, строкой в отчёт: в инструменте kontrast-znaka.mjs цикл предзагрузки как код не работает, картинки подгружает последняя плавная прокрутка во время ожидания Promise.race, а сама предзагрузка избыточна. Правка по предложению линзы (снять плавность или прокручивать с behavior: 'instant') допустима как чистка, но ни одного числа замера она не меняет.

######## R1-KONTRAST-4 [предел → подтверждено/предел]
«Худший фон» — минимум по выборке (шаг 20, две ширины), а не граница; при этом аналитическая граница закрывает любое содержимое, ширину и плотность
ДОК: На экране (skan2, 1440, DPR 1) шаг 20 даёт 32 36 40 при 4160 (7,64), а шаг 5 — 33 37 41 при 4210 (7,54). Яркость скачет с периодом около 5 px: 4205 — L 0,01722, 4210 — 0,01807, 4215 — 0,0164. Значит, шаг 20 худшее место пропускает. Минимумы по 11 прогонам на экране: 390 при DPR 1 и 2, 768 и 1024 — 7,325 и 12,368; 360 и 961 — 7,351; 414 — 7,343; 1280 и 1440 при DPR 2 — 7,61; 1440 при DPR 1 и 1920 — 7,542. Граница: шапка — bg с альфой 0,88, в 8 битах 224/255 (подтверждено: 0,8784×[9 12 17] + 0,1216×[229 234 238] = 35,8 39,0 43,9, ровно измеренный худший rgb(36 39 44), то есть под шапкой снег плашки). Над чисто белым фоном шапка даёт rgb(39 42 46): фонарь 7,05:1, снег 11,90:1, обводка фокуса (accent-text) 8,43:1. Это выше порогов 3 и 4,5 при любом содержимом, ширине, DPR и шаге. Размытие — выпуклое усреднение и выйти за белый не может.
ГДЕ: D:/SEO/cloud/site-generator/sites/7thserpent.com/gates/contrast.mjs:74-80; D:/SEO/cloud/site-generator/sites/7thserpent.com/DESIGN.md:331-333; данные — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/skan2-*.json, skan2-log.txt
ПРЕДЛ: Как гарантию записать в gates/contrast.mjs и DESIGN.md границу (7,05, 11,90 и 8,43 при альфе 224/255 над белым), а замер оставить иллюстрацией. Указать, что граница пересчитывается при смене доли color-mix шапки, токена --bg или порядка слоёв. Зерно она не учитывает, как и остальные пары: сослаться на абзац о зерне.
СКЕПТИК: Опровергнуть не удалось. Всё проверил сам: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-4/proverka.mjs, результат в proverka.json. Сервер 4331 отдаёт index.html, побайтно равный dist.

1) Выборка, а не граница. Взял 1440×900, DPR 1, прокрутку 4140–4240 с шагом 1 px и снимал шапку во всю ширину. Худшее место — scrollY 4182, rgb(33 37 42), фонарь 7,534. Если брать только каждое пятое положение, выходит 7,542 при 4210. Если каждое двадцатое — 7,636 при 4160. Яркость фона под размытием скачет от пикселя к пикселю (0,0164–0,0181 на 4170–4215). Поэтому шаг 20 худшее место пропускает. Это подтверждает и данные линзы skan2 (при прореживании до шага 20 то же 7,636). В gates/contrast.mjs:79 для 1440 стоит «7,61:1 … 12,85:1». Столько даёт исходный инструмент на шаге 20: логи линзы, на 4200 rgb(33 36 41). На самой 1440 при DPR 1 это не минимум: там 7,53/7,54.

2) Граница. В собранном CSS у .hdr одно правило: color-mix(in oklab, var(--bg) 88%, transparent) и blur(14px), z-index 50. Других слоёв в рамке .hdr__brand нет, зерно (z 60) лежит поверх. Посчитал на бумаге: альфа 224/255 над белым даёт rgb(39 42 46), то есть фонарь 7,047, снег 11,899, accent-text 8,429. Та же альфа над снегом плашки даёт rgb(36 39 44), 7,325 и 12,368 — ровно худший фон из документов. Проверил и на экране: подложил под шапку белый fixed-слой (z 49), знак скрыт, зерно снято. На 390 и 1440 (DPR 1 и 2) и на 1920 (DPR 1), при scrollY 0, 3730 и 4210 у всех 15 кадров rgb(39 42 46), 7,047/11,899/8,429. Альфа — именно 224/255: при 0,88 вышло бы 39 41 46. Размытие усредняет подложку и выйти за белый не может. Значит, граница закрывает любое содержимое, ширину и плотность. Выше порогов 3 и 4,5 она держится даже при альфе 0,85: 6,46/10,90/7,72.

3) Почему это не «уже названо». Шаг 20 и две ширины указаны: contrast.mjs:76, DESIGN.md:332, шапка kontrast-znaka.mjs. Но оговорки «минимум по выборке, не нижняя граница», которая в том же contrast.mjs стоит у замера арта (строки 63–65), здесь нет. DESIGN.md:331–333 называет rgb(36 39 44) «худшим фоном», не называя ни ширин, ни DPR.

4) Почему не дефект. Вывод «выше порогов» верен при любом содержимом — это доказывает граница, так что судья брак не пропускает. Главные числа DESIGN.md (7,32/12,37) совпадают с тем, что даёт снег плашки под шапкой, и опровергнуты не были. К «документу» относится только частное число 1440 в contrast.mjs:79 (7,61/12,85): на этой ширине оно не худшее. Это стоит поправить вместе с предложенной правкой: границу записать гарантией, замер оставить иллюстрацией, указать, при каких правках границу пересчитывать, и сослаться на абзац о зерне.

Мелочь в доводе линзы: «период около 5 px» слишком грубо — на шаге 1 скачки идут через 1–4 px. На вывод это не влияет.

######## R1-KONTRAST-5 [документ → подтверждено/документ]
В шапке gates/contrast.mjs один «худший фон» стоит на две ширины, и позиция «scrollY 2780» — артефакт обрезанного снимка
ДОК: gates/contrast.mjs:79-80: «худший фон rgb(36 39 44), фонарь 7,32:1 (390) и 7,61:1 (1440)». Читается как один фон для обеих ширин, но сам инструмент для 1440 печатает rgb(33 36 41) при scrollY 4200: повторный прогон `node kontrast-znaka.mjs`, 2 мин 35 с. Точка 2780 на 390, которую печатает инструмент, на экране даёт 34 37 42 (skan2-390-dpr1-k5: 2780 → 34 37 42, 3730 → 36 39 44). Число 7,32 и 12,37 для DESIGN.md:332-333 верно как минимум по ширинам.
ГДЕ: D:/SEO/cloud/site-generator/sites/7thserpent.com/gates/contrast.mjs:79-80
ПРЕДЛ: Если числа замера остаются, записать фон отдельно для каждой ширины: 390 — rgb(36 39 44) при 3730; 1440 — rgb(33 37 41) при 4210, 7,54 и 12,73 (после исправления R1-KONTRAST-2). Лучше сослаться на границу из R1-KONTRAST-4.
СКЕПТИК: Опровергнуть не удалось, всё воспроизведено независимо.

1) Счёт. По формуле WCAG против #eca84a и #e5eaee фон rgb(36 39 44) даёт 7,325 и 12,368. Числа 7,61 и 12,85 получаются только от rgb(33 36 41), у rgb(36 39 44) их быть не может. Фраза в gates/contrast.mjs:79-80 «худший фон rgb(36 39 44), фонарь 7,32:1 (390) и 7,61:1 (1440), снег 12,37:1 и 12,85:1» приписывает одному фону два разных значения. Проверка: .playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-5/cr.mjs.

2) Повторный прогон инструмента без изменений (docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs, localhost:4331, 23:43–23:46, вывод в orig-run1.txt). На 390 он печатает «rgb(36 39 44) при scrollY 2780», 7,32 и 12,37. На 1440 — «rgb(33 36 41) при scrollY 4200», 7,61 и 12,85. Значит, фон в шапке — это фон 390, а цифры 1440 взяты от другого фона, который нигде не назван.

3) Точка 2780. Проверял скриптом tochka.mjs (вывод в tochka.txt). Подготовка страницы дословно как в инструменте, прыжок behavior: instant, ожидание 600 мс, scrollY сверен. На 390 при 2780 два снимка рамкой подряд дают 36 39 44, а снимок окна даёт 34 37 42. При 3730 оба способа дают 36 39 44. Значит, дело не во времени прокрутки: снимок рамкой (clip) в этой точке рисует фон иначе, чем окно. Позиция 2780 — артефакт снимка рамкой, а на экране этот фон есть при 3730, как и говорит линза. На 1440 при 4200 рамка даёт 33 36 41, окно — 31 35 39; при 4210 окно даёт 33 37 41, это 7,54 и 12,73. Так подтверждается и число из предложения линзы (сама разница рамки и окна — предмет R1-KONTRAST-2).

4) Предела в документах нет. DESIGN.md:332-333 даёт один худший фон 36 39 44 с 7,32 и 12,37, без ширин. Как общий минимум по двум ширинам это верно, и этим он не противоречит. Шапки инструмента и гейта о разных фонах по ширинам ничего не говорят. README эталона замер знака не затрагивает. scrollY 2780 не встречается ни в одном документе, только в выводе инструмента.

Класс — документ, а не дефект: пороги 3 и 4,5 соблюдены с большим запасом при любом прочтении, вывод «выше порогов» верен, судья брак не пропускает. Расходится только проза шапки с выводом самого инструмента. Лишнее уточнение за пределами находки: первый кадр 1440 после подготовки встал на scrollY 4359 вместо 4180, а на 390 две картинки не догрузились.

######## R1-KONTRAST-6 [предел → подтверждено/предел]
Обводку фокуса ссылки знака над содержимым никто не мерил: рамка замера — коробка ссылки, а обводка лежит на 3–5 px снаружи
ДОК: base.css:39-41 задаёт `:focus-visible { outline: 2px solid var(--accent-text); outline-offset: 3px }`. У a.hdr__brand коробка 138×38 от (132, 15) на 1440 и от (16, 15) на 390 (stili.json), обводка — от 10 до 58 по y внутри полупрозрачной шапки (fokus-1440-tab2.png). kontrast-znaka.mjs берёт только коробку ссылки (строки 38 и 44). Пара accent-text/bg («обводка фокуса на фоне страницы») закрывает обводку только при scrollY 0. Мой замер полосы ±5 px вокруг коробки по 11 прогонам: минимум accent-text — 8,761:1 (390, 768, 1024, 360, 414, 961), 9,01–9,02 на 1280, 1440 и 1920. Порог 3 выдержан, граница над белым — 8,43.
ГДЕ: D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs:38,44; D:/SEO/cloud/site-generator/sites/7thserpent.com/gates/contrast.mjs:93; проверка — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/skan2.mjs (поле obwodka)
ПРЕДЛ: Расширить рамку замера на 5 px или сослаться на границу 8,43. В подписи пары accent-text/bg или в абзаце о знаке назвать обводку фокуса ссылки знака над содержимым.
СКЕПТИК: Опровергнуть не удалось. Линза верно прочла код и дала верные числа, класс «предел» правильный.

Код. В kontrast-znaka.mjs строка 38 берёт getBoundingClientRect() у .hdr__brand, строка 44 снимает только эту коробку (clip). core/styles/base.css:39-41 задаёт обводку 2px с отступом 3px, то есть она лежит на 3–5 px снаружи коробки. Коробка 16,15–154,53 на 390 и 132,15–270,53 на 1440, так что обводка занимает y 10–58 внутри шапки высотой 68. Пара accent-text/bg в gates/contrast.mjs:93 покрывает обводку только при scrollY 0: там под шапкой BODY (stili.json podShapka0).

Числа я воспроизвёл сам, по-другому, чем skan2 линзы. Ссылка действительно в фокусе: Tab, :focus-visible = true, фокус не терялся ни на одном кадре. Снимается всё окно без clip, зерно снято, знак не скрыт. Беру пиксели прямых участков обводки (3–5 px) и соседние (0–3 и 5–8 px), углы со скруглением исключены. Шаг 20 px по всей прокрутке. Минимумы: 390 — 8,761:1 (соседний пиксель rgb 36 39 44, sy 3760); 768 — 8,761; 1440 — 9,02; 1920 — 8,989. Линза для 1920 дала 9,011 при шаге 10 по полосе 0–5; у меня полоса другая, вывод это не меняет. Все пиксели обводки на прямых участках ровно rgb(242 189 110). Для проверки крайнего случая я подложил белый лист под шапку (z-index 49) и получил соседний пиксель rgb(39 42 46), контраст 8,429:1. Это совпадает с «границей над белым 8,43» у линзы. Значит, никакое содержимое под шапкой не опустит обводку ниже 8,43. Пороги 3 и 4,5 не могут быть нарушены.

Почему не дефект и не «документ». Инструмент честно объявляет свою рамку: «худший пиксель фона в рамке знака» (шапка kontrast-znaka.mjs, gates/contrast.mjs:71-80, DESIGN.md:331-333). Нигде не сказано, что обводка над содержимым замерена. Строка 16-17 гейта («обводка фокуса accent-text — на bg (шапка)») верна при scrollY 0.

Уточнения к находке.
(1) Этот предел уже назван в общем виде: gates/contrast.mjs:50-52 («составные сочетания — … и полупрозрачная шапка поверх содержимого» в списке «чего гейт не считает») и DESIGN.md:339-344. Находка его конкретизирует, а не открывает новое.
(2) Обводка — это :focus-visible ядра, а не знак. Сессия 11 её не вводила: ссылка .hdr__brand с текстом была и раньше, у пунктов меню и бургера над содержимым такая же незамеренная обводка. Сессия изменила только размер коробки.
(3) Из-за нижней границы 8,43 брак за этим пределом спрятаться не может. Предложение линзы (сослаться на 8,43 или назвать обводку в подписи пары или в абзаце о знаке) — необязательное уточнение формулировки, не устранение пропуска.

Мои материалы: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-6/obwodka-real.mjs, obwodka-real-{390,768,1440,1920}-k20.json, najgorszy-*.png, bialy-*.png. Сервер 4331 отдаёт index.html, побайтно равный dist (SHA256 3FC9ACDE…3A12). dist не пересобирался.

######## R1-KONTRAST-7 [предел → опровергнуто/нет]
Сочетание, которого нет ни в одном судье: ссылка «к содержанию» при фокусе ложится заливкой фонаря поверх знака
ДОК: Первый Tab (stili.json, fokus): a.skip-link — 196×39 от (16, 16), фон rgb(236,168,74), z-index 100 (core/styles/a11y.css:24-38). Знак — 138×38 от (16, 15) на 390 и от (132, 15) на 1440. На 390 знак закрыт целиком (fokus-390-tab1.png), на 1440 закрыты «7 TH SER» и видно только «PENT» (fokus-1440-tab1.png), обводка accent-text проходит вплотную к снежным буквам. Контраст не нарушен: текст ссылки — пара ink-on-accent/accent 9,37:1. Но DESIGN.md «Site mark» (стр. 615-617) говорит «на заливку фонаря знак не ставится», и ни гейт, ни замер, ни эталон этого состояния не описывают. Механизм — ядро, а над прежним текстовым знаком было то же самое.
ГДЕ: D:/SEO/cloud/site-generator/core/styles/a11y.css:24-38; D:/SEO/cloud/site-generator/sites/7thserpent.com/DESIGN.md:615-617; кадры — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/fokus-390-tab1.png, fokus-1440-tab1.png
ПРЕДЛ: Ядро вне объёма сессии, поэтому строкой в доклад и бэклог. Варианты для владельца: назвать это состояние в «Site mark» как допустимое (ссылка перекрывает знак, а не знак стоит на заливке) или в ядре опустить ссылку «к содержанию» под шапку (top ≥ 68px).
СКЕПТИК: Факты линзы я воспроизвёл сам и они верны, но из них не выходит ни дефекта судьи, ни предела, ни расхождения прозы с фактом.

Как проверял: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-7/perekrytie.mjs, результат в perekrytie.json и кадры B-<ширина>-dpr<1|2>.png, живая сборка на :4331, dist не трогал. Три кадра шапки: покой, покой со скрытым знаком, первый Tab. Окна 360, 390, 768, 961, 1024, 1440 и 1920, DPR 1 и 2.

Что подтвердилось. Первый Tab ставит фокус на a.skip-link: 196,14×39 от (16, 16), фон rgb(236,168,74), z-index 100. Обводка — 2px rgb(242,189,110) с отступом 3px. Шапка — sticky с z-index 50, поэтому ссылка лежит над ней. Знак — 138×38 от (16, 15) на 360 и 390, от (30,72, 15) на 768, от (32, 15) на 961 и 1024, от (132, 15) на 1440, от (372, 15) на 1920. На всех окнах до 1024 знак закрыт целиком. Мой счётчик показывал якобы видимые пиксели внутри заливки, но это трассер и ромб, чья краска accent совпадает с заливкой. На 1440 закрыта левая часть знака, видно «PENT». Обводка касается снежных букв примерно в 10 пикселях при DPR 1. На 1920 перекрытия нет. Контраст надписи ink-on-accent на accent пересчитал: 9,37:1. Ссылки на строки тоже верны: a11y.css:24-38, DESIGN.md:615-617.

Почему находка не держится.

1. Правило прочитано неверно. В DESIGN.md:615-617 правило «на заливку фонаря знак не ставится» говорит о подложке знака: «Фон — ночные поверхности: bg, bg-band и полупрозрачная шапка». Тот же смысл у исходника A-semerka-trassa/opis.md:149-151. Здесь же заливка не ложится под знак, а закрывает его непрозрачно: 100 против 50. Ни один пиксель знака не рисуется на заливке. Видимая часть («PENT» и полоска в 3-пиксельной щели между заливкой и обводкой) по-прежнему стоит на bg. Проза и факт совпадают, так что класс «документ» не подходит.

2. Судьям видеть нечего. Новых цветовых сочетаний это состояние не даёт: надпись ссылки — пара ink-on-accent/accent в gates/contrast.mjs:97 (в подписи прямо названа ссылка «к содержанию»), заливка на ночи — accent/bg с порогом 3 (:98), обводка на ночи — accent-text/bg (:93). Шапка того же файла (:14-17) записывает, что обход Tab, включая эту ссылку в фокусе, сверен ещё в сессии 7. Замер kontrast-znaka.mjs мерит подложку в рамке знака, а ссылка её не меняет — только закрывает. Эталон — статичные кадры без фокуса. Сама линза признаёт, что контраст не нарушен. Значит, нет брака, который какой-то судья пропускал бы, и это не предел.

3. Судью здесь не меняли. По П83 п. 2.5 «судью судят» идёт по изменённым судьям. Механизм ссылки — ядро, а ядро вне объёма сессии 11 (П83 п. 3–4), и ссылка перекрывала слот знака и при прежнем текстовом знаке. Это обычное поведение ссылки пропуска: она видна только при первом фокусе и уходит на втором Tab, когда фокус переходит на ссылку знака.

Уточнение к линзе. Перекрытие есть не только на 390 и 1440. Знак закрыт целиком на всех проверенных окнах от 360 до 1024; по геометрии контейнера так же будет примерно до 1325px. Частичное перекрытие — примерно на 1325–1610px. Если владелец захочет что-то менять, это его вопрос о дизайне ядра, а не находка против судьи.

######## R1-KONTRAST-8 [дефект → подтверждено/дефект]
Скрытый дефект гейта ядра: у токена в восьмизначном hex альфа молча отбрасывается, и полупрозрачный цвет считается непрозрачным
ДОК: В check-contrast.mjs:142 стоит `if (h.length === 8) h = h.slice(0, 6)`. Проба в своей папке (proba-bramki/: токен --ink-mgla: #e5eaee22 и пара ['ink-mgla','bg',4.5]), `node core/gates/check-contrast.mjs .`: «ok … 16.17:1 (min 4.5:1)», «2/2 par przechodzi», exit 0. На экране этот цвет над bg — rgb(38 42 47), 1,35:1. Регулярное выражение `#[0-9a-fA-F]{3,8}` пропускает и 5- и 7-значные записи, которые CSS не принимает. Сегодня не срабатывает: у сайта нет hex-токенов с альфой, а hairline и wash записаны через rgb(), гейт их не собирает, и пара с ними упала бы громко (BRAK TOKENU). Самопроверка 14/14 этот случай не покрывает.
ГДЕ: D:/SEO/cloud/site-generator/core/gates/check-contrast.mjs:61,139-147; проба — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/kontrast-znaka/proba-bramki/
ПРЕДЛ: Вне объёма (ядро — ни байта), поэтому строкой в бэклог. Гейт должен громко падать на hex с альфой не ff и на длине, отличной от 3, 6 и 8, либо накладывать цвет на фон пары. Добавить пробу в самопроверку.
СКЕПТИК: Опровергнуть не удалось. Находка воспроизводится, и на деле она сильнее, чем описала линза.

1. Код прочитан верно. core/gates/check-contrast.mjs:61 (и :108) собирает токены по `#[0-9a-fA-F]{3,8}`. В :142 luminance() делает `if (h.length === 8) h = h.slice(0, 6)`, то есть отбрасывает альфу и не накладывает цвет на фон пары.

2. Своя проба в .playwright-mcp/sesja-11/sud-r1/skeptik-R1-KONTRAST-8/proba/ (фон bg #090c11, контрольная пара ink #e5eaee, запуск `node core/gates/check-contrast.mjs <proba>`):
- #e5eaee22 даёт «ok 16.17:1»;
- #e5eaee00, то есть полностью прозрачный цвет, тоже «ok 16.17:1»;
- пятизначный #e5eae даёт «ok 15.02:1»;
- семизначный #e5eaee2 даёт «ok 16.17:1»;
- итог «5/5 par przechodzi», exit=0.
Четырёхзначный #eee2 падает громко («BŁĄD NaN:1», exit=1). Этот случай не тихий, и линза его дефектом не называла.

3. Что на экране. Замер в Chromium 1234 (skeptik-R1-KONTRAST-8/ekran.mjs):
- #e5eaee22 вычисляется как rgba(229, 234, 238, 0.133), пиксель над bg равен [39,41,47], контраст 1,35:1. Число линзы подтверждено, разница в одну единицу канала — это растр.
- #e5eaee00 даёт 1,00:1.
- Пяти- и семизначные значения при подстановке через var() становятся недействительными на этапе вычисления. Фон получается transparent, пиксель равен bg, контраст 1,00:1. Гейт же печатает для них 15–16:1.
Значит, судья не просто слеп: он печатает число, которого на экране нет, и пропускает брак с exit 0. Это класс «дефект», а не «предел».

4. Пределом это нигде не названо. Шапка sites/7thserpent.com/gates/contrast.mjs:42–80 называет несчитаемыми составные сочетания: wash, wash-strong, color-mix, полупрозрачную шапку, скримы. Эти токены записаны через rgb() (global.css:74–77), гейт их не собирает, и пара с ними упала бы громко: BRAK TOKENU, missing++. Про hex с альфой и неверную длину hex ни одного слова нет ни в шапке ядра («Zbiera `--nazwa: #hex;`»), ни в DESIGN.md, ни в BACKLOG.md, ни в DECISIONS.md. Поиск по «альф|alpha|length === 8» находит только саму строку :142. Больше того, шапка самопроверки ядра (:223–225) утверждает, что «Sam rachunek (luminancja, iloraz) to wzór WCAG bez rozgałęzień; sprawdza go każda para na żywej witrynie». Это неверно: у luminance есть ветвь для длины 8, и ни одна живая пара её не проходит. Самопроверка — 14/14, и в ней нет ни одной пробы на luminance/ratio (я пересчитал случаи и перезапустил: 14/14, exit 0).

5. «Сегодня не срабатывает» подтверждено. В sites/**/src/styles/*.css нет ни восьми-, ни четырёхзначных hex-токенов. Дефект скрытый, брака на сайте сейчас нет.

Класс: дефект. Судья врёт числом и пропускает брак, пусть сейчас и без живого повода. Попутно неверна проза самопроверки про «bez rozgałęzień» — это класс «документ», но он вторичен. Предложение линзы уместно: правка вне объёма (ядро — ни байта), поэтому строкой в бэклог. Гейт должен громко падать на альфе, отличной от ff, и на длине, отличной от 3, 6 и 8, либо накладывать цвет на фон пары. Нужна проба в самопроверке, включая #rrggbb00, 5- и 7-значные записи, и правка фразы про «bez rozgałęzień».

######## R1-VNEDRENIE-1 [дефект → подтверждено/дефект]
Оптическая обводка «TH» не работает: при DPR ≤1,5 знак на сайте отличается от одобренного эскиза A, а новый эталон закрепил это отличие
ДОК: Каскад в dist/_astro/index.0TxqFWC-.css: `.znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]{stroke-width:0}`, специфичность (0,3,1), перебивает `@media (resolution<=1.5x){.znak__obwodka[data-astro-cid-yruwqcb3]{stroke-width:.3px}}` со специфичностью (0,2,0). Исходник — Znak.astro:57-59 против :65-69. Прогон `node wiernosc.mjs` (сборка dist, свой сервер): при matchMedia('(max-resolution: 1.5dppx)')=true вычисленный stroke-width у path.znak__obwodka равен "0px", у .zk1-th эскиза — "0.3px". Шапка как есть против шапки с znak.html эскиза в .hdr__brand, числа одинаковы для 1440 и 390: DPR 1 — 213 px, наибольшее |Δ| 84; DPR 1,25 — 264 px (Δ 95); DPR 1,5 — 327 px (Δ 91); DPR 2 — 0. Все различия лежат в рамке «TH» (CSS x 156–182, y 19–33 на 1440; x 40–66 на 390). Подвал: DPR 1 — 213 px, DPR 2 — 0. Проверка причины: та же страница с правилом `.hdr__brand .znak .znak__obwodka{stroke-width:.3px!important}` под тем же media даёт 0 px против эскиза на всех восьми сочетаниях. Лупа kadry/lupa-TH.png: средняя яркость области «TH» — 52,3 у реализации против 64,6 у эскиза (DPR 1), 52,3 против 63,2 (1,25), 54,4 против 64,9 (1,5). Эталон: `node etalon-shapka.mjs` — в рамке знака кадров baseline-desktop-hero-1440x900 и baseline-mobile-hero-390x844 эталон отличается от реализации на 0 px, от эскиза — на 213 px. Листы, которые утвердил владелец (materialy.mjs, «DPR 1,25 / 1,5 · пиксели устройства»), сняты с обводкой.
ГДЕ: sites/7thserpent.com/src/components/Znak.astro:57-69; dist/_astro/index.0TxqFWC-.css; sites/7thserpent.com/_baseline (переснят на c1ec5e6); улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/vnedrenie/wiernosc.mjs, etalon-shapka.mjs, kadry/lupa-TH.png
ПРЕДЛ: Технически: в Znak.astro поднять специфичность правила поправки (`.znak .znak__obwodka` или `.znak path.znak__obwodka`). Проще — убрать `stroke` из style и правило `.znak path{stroke-width:0}` и задать обводку только классу obwodka в media, как в эскизе. После правки кадры с шапкой разойдутся с эталоном c1ec5e6 на 213 px с |Δ| до 84 в окнах hero и выше «макс» зон 2 и 9 в полных кадрах. Нужна повторная пересъёмка эталона, а это решение владельца (инвариант 6): вынести ему вопросом. В приёмку знака добавить судью «реализация = эскиз» (wiernosc.mjs: подстановка znak.html, 1440 и 390 × DPR 1/1,25/1,5/2, ожидается 0 px). Ни znak:check, ни эталон этого не видят.
СКЕПТИК: Опровергнуть не удалось: я воспроизвёл находку своим скриптом со своим сервером над dist, dist не пересобирал: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-1/proba.mjs.

1. Код. В dist/_astro/index.0TxqFWC-.css стоят правило `.znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]{stroke-width:0}` со специфичностью (0,3,1) и правило `@media (resolution<=1.5x){.znak__obwodka[data-astro-cid-yruwqcb3]{stroke-width:.3px}}` со специфичностью (0,2,0). Media-запрос специфичности не прибавляет, поэтому поправка проигрывает при любой плотности. От стратегии scoping это не зависит: без атрибутов пара выглядит как (0,1,1) против (0,1,0), и поправка тоже проигрывает. Источник — Znak.astro:57-59 против :65-69.

2. CSSOM. На path.znak__obwodka в шапке срабатывают оба правила, и media-запрос выполняется при DPR 1, 1,25 и 1,5. Вычисленный stroke-width при этом равен 0px на всех восьми сочетаниях (1440 и 390 × DPR 1/1,25/1,5/2). У .zk1-th эскиза при DPR ≤1,5 он равен 0.3px.

3. Пиксели шапки 68 px, реализация против эскиза A (znak.html внутри .hdr__brand). Шумовой пол равен 0.
   - DPR 1: 213 px, наибольшее |Δ| 84.
   - DPR 1,25: 264 px, |Δ| 95.
   - DPR 1,5: 327 px, |Δ| 91.
   - DPR 2: 0.
   Числа одинаковы на 1440 и на 390 и совпадают с числами линзы.

4. Проверка причины без !important. Я поднял специфичность правила до `.znak[cid] path.znak__obwodka[cid]` под тем же media. Именно в такой селектор Astro превратит `.znak path.znak__obwodka`. Результат: 0.3px и 0 px против эскиза на всех восьми сочетаниях. Значит, причина — только каскад.

5. Эталон c1ec5e6, рамка знака ±2 px, при DPR 1. В кадре baseline-desktop-hero-1440x900 эталон совпадает с реализацией (0 px) и отличается от эскиза на 213 px, |Δ| 84. В кадре baseline-mobile-hero-390x844 — то же самое. То есть эталон закрепил дефектный вид.

Предел или документ, а не дефект? Нет. Ни в одном документе это не названо пределом:
- DESIGN.md:629-631 утверждает, что при плотности до 1,5 «TH» обводится той же краской в 0,3px.
- Комментарий Znak.astro:61-64 говорит то же.
- Сообщение коммита c1ec5e6 заявляет «оптическая обводка «TH» 0,3 px при плотности до 1,5».
- В _baseline/README.md об этом нет ничего.

Одобренные листы эскиза (materialy.mjs берёт znak.html, кадры DPR 1,25 и 1,5) сняты с обводкой, так что сайт расходится с выбором владельца (вариант A, П83). Материалы приёмки (materialy-priemki.mjs) снимают шапку при DPR 1/2 и 390 при DPR 3, и крупным планом при DPR ≤1,5 разницу не показывают. znak:check честно не заявляет проверку отрисовки — это его предел, но суть находки не в нём.

Итог: это брак реализации, о котором документы и коммит говорят неправду, поэтому класс — дефект, как и указала линза. Проза DESIGN.md верна как требование, а не расходится с фактом. Факт неверен из-за кода, поэтому класс не «документ».

Предложение линзы технически верно: `.znak path.znak__obwodka` проверен и даёт 0 px против эскиза. После правки шапка разойдётся с эталоном c1ec5e6 на 213 px, |Δ| 84, в рамке знака hero при DPR 1. Пересъёмка эталона — решение владельца (инвариант 6).

######## R1-VNEDRENIE-2 [документ → подтверждено/документ]
Проза утверждает, что обводка «TH» 0,3 px при плотности до 1,5 применяется, а на деле её нет
ДОК: DESIGN.md:629-631: «волос «TH» (0,31px) при плотности экрана до 1,5 обводится той же краской в 0,3px». Znak.astro:61-64 — комментарий о том же. Сообщение коммита c1ec5e6: «оптическая обводка «TH» 0,3 px при плотности до 1,5». Замер (R1-VNEDRENIE-1): вычисленный stroke-width = 0px при DPR 1, 1,25 и 1,5 и на 1440, и на 390, в шапке и в подвале.
ГДЕ: sites/7thserpent.com/DESIGN.md:629-631; sites/7thserpent.com/src/components/Znak.astro:61-64; сообщение коммита c1ec5e6
ПРЕДЛ: Если чинить по R1-VNEDRENIE-1, проза станет верной. До починки в доклад строкой: поправка объявлена, но не действует (довод, на котором держится «знак = эскиз», неверен). Сообщение коммита не переписывать (история); поправку записать в доклад.
СКЕПТИК: Опровергнуть не удалось, доказательство я воспроизвёл сам на готовом dist, ничего не пересобирая. Сборочный CSS dist/_astro/index.0TxqFWC-.css содержит два правила. Первое: `.znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]{stroke-width:0}`, специфичность (0,3,1). Второе: `@media (resolution<=1.5x){.znak__obwodka[data-astro-cid-yruwqcb3]{stroke-width:.3px}}`, специфичность (0,2,0). Оба пути «TH» (шапка и подвал) несут class="znak__obwodka" и data-astro-cid, в znak.json у них obwodka: true.

Замер в Chromium 1234 (скрипт zamer.mjs). При DPR 1, 1,25 и 1,5 media-запрос совпадает (mq=true), и оба правила применяются к пути. Тем не менее вычисленный stroke-width = 0px на 1440 и на 390, в шапке и в подвале: обводку перебивает специфичность, а не условие. При DPR 2 тоже 0px, как и задумано.

Контроль: я включил обводку принудительно через правило повышенной специфичности (stroke-width стал 0.3px). Растр шапки при DPR 1 изменился (4990 → 5301 байт PNG). Покрытие чернилами поперечины «H» как есть — 0,37–0,38, с обводкой — 0,82–0,84; у тонких столбцов поперечины «T» — 0,25–0,5 против 0,77–0,97 (pokrytie.mjs). Эскиз A (voprosy/A-semerka-trassa/opis.md) записал 0,83 при обводке и 0,39 без неё. Значит, на сайте при плотности до 1,5 волос «TH» остаётся без поправки, и «знак = эскиз» для «TH» неверно. В эскизе обводка работала только потому, что правила stroke-width:0, с которым она могла бы конфликтовать, там не было.

В документах пределом это не названо. В README эталона, в CLAUDE.md сайта, в BACKLOG, в tools/znak.mjs и в инструментах приёмки об обводке нет ни слова. znak:check её не проверяет. Отчёта сессии 11 пока нет.

Класс «документ» верен для самой находки: комментарий Znak.astro:61-64 и сообщение коммита c1ec5e6 утверждают как факт то, чего нет. Уточнение: DESIGN.md:629-631 — норма, и там расходится код, а не проза. Причина — дефект специфичности CSS, это R1-VNEDRENIE-1. После его починки проза станет верной.

Важно для доклада: эталон 776c24e снят на c1ec5e6 уже без обводки. Починка R1-VNEDRENIE-1 изменит пиксели «TH» в шапке 1440 и 390 и в подвале против эталона. Это расхождение придётся назвать при приёмке, а решение о пересъёмке принимает владелец.

Материалы лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-2/: zamer.mjs, pokrytie.mjs, shapka-znak-dpr1-kak-est.png, shapka-znak-dpr1-s-obwodkoj.png.

######## R1-VNEDRENIE-3 [предел → подтверждено/предел]
В светлой теме принудительных цветов имя знака и перекладина исчезают (1,21:1); прежний текстовый знак был виден. Ни один судья режим принудительных цветов не смотрит
ДОК: Прогон `node wymuszone.mjs`: эмуляция forcedColors:'active' в Chromium 151, 1440, DPR 1. В светлой схеме фон body равен rgb(255,255,255), шапки — rgba(255,255,255,0.88). Краски частей знака не принуждаются: fill остаётся rgb(229,234,238) у перекладины и обеих надписей и rgb(236,168,74) у трассера и ромба. Контраст: #e5eaee на #fff — 1,21:1, #eca84a на #fff — 2,05:1. Кадр kadry/forced-light.png: «TH SERPENT» и перекладина почти невидимы. До сессии в .hdr__brand стоял текст <span class="t-title">7thserpent.com</span>, и в том же режиме его цвет принуждается к rgb(0,0,159), то есть виден. В тёмной схеме всё читается (17,34:1 и 10,27:1). У эскиза A та же особенность: краски заданы как fill: var(--ink), а листы эскиза режим не показывали.
ГДЕ: sites/7thserpent.com/src/components/Znak.astro:43,46 (fill задан inline style); gates/contrast.mjs (считает пары токенов, а не forced-colors); улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/vnedrenie/wymuszone.mjs, kadry/forced-light.png
ПРЕДЛ: Технически: в Znak.astro добавить `@media (forced-colors: active){ .znak polygon, .znak path { fill: CanvasText } }` или сделать краски через currentColor. DESIGN.md требует «Краски — только токены», а системный цвет — не токен, так что это исключение показать владельцу вопросом. Проверить на настоящей контрастной теме Windows. Строку о режиме добавить в приёмку знака.
СКЕПТИК: Находку опровергнуть не удалось. Я воспроизвёл её своим скриптом, без кода линзы: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-3/proverka.mjs. Сервер свой, над текущим dist, без пересборки; Chromium 151.0.7922.34; forcedColors 'active'; ширины 1440 и 390, DPR 1; схемы light и dark. Результат в wynik.json, кадры — forced-*-1440.png, forced-*-390.png и лупа forced-light-1440-lupa.png.

Светлая схема. Фон body — rgb(255,255,255), фон шапки — rgba(255,255,255,0.88), фон подвала — rgb(255,255,255). Краска частей знака не меняется ни в шапке, ни в подвале: перекладина и обе надписи — rgb(229,234,238), трассер и ромб — rgb(236,168,74). Контраст 1,21:1 и 2,05:1; по пикселям кадра то же: 1,21 и 2,04–2,05. Под лупой «TH SERPENT» и перекладина еле различимы. В тёмной схеме контраст 17,34:1 и 10,27:1. Без принудительных цветов краски прежние, фон rgb(9,12,17). Для светлой контрастной темы Windows 11 «Пустыня» (Canvas #fffaef) расчёт даёт ещё меньше: 1,16:1 и 1,96:1.

Прежний знак был виден — подтверждено. Отчёт docs/reports/2026-09-18-7thserpent-brief.md:104 описывает прежнюю шапку: «знак — текст в .t-title». Я подставил span.t-title в .hdr__brand: его цвет принуждается к LinkText rgb(0,0,159), это 13,99:1 на светлом фоне; в тёмной схеме — rgb(255,255,0), 19,56:1.

Предел нигде не назван. Поиск по репозиторию (forced, принудит, High Contrast, контрастн, CanvasText) не находит ничего о принудительных цветах. Смотрел: DESIGN.md (раздел «Site mark», «Правило посчитанного контраста», «Key Characteristics»), шапку gates/contrast.mjs, kontrast-znaka.mjs и materialy-priemki.mjs, CLAUDE.md сайта и ядро.

Класс — «предел»: судьи честны, но этого не видят. Гейт заявляет только пары токенов, а Canvas — не токен. Инструменты знака снимают только обычный режим и о принудительных цветах ничего не утверждают. Брак настоящий, но ни один судья не заявлял, что его видит. Поэтому это не «дефект» (судья не врёт и не пропускает брак в заявленной им области) и не «документ». Единственная натяжка на «документ»: пункт «Key Characteristics» в DESIGN.md перечисляет, «чего гейт не считает», как будто полный список, и принудительных цветов в нём нет. При исправлении стоит назвать этот предел и там, и в шапке gates/contrast.mjs.

Уточнения к доказательству линзы:
1. Причина не в том, что fill задан встроенным стилем. Встроенные стили браузер принуждает так же, как остальные. Причина в умолчании браузера для svg: forced-color-adjust: preserve-parent-color. Вычисленное значение — preserve-parent-color у svg и у всех пяти частей; сайт и ядро это свойство не задают. Поэтому любой явный fill не принуждается, а fill через currentColor и системные цвета подстраиваются.
2. Подвал затронут так же: role img, фон белый, те же краски. Линза показала только шапку.
3. Формально WCAG 1.4.3 не нарушен: для логотипов требования к контрасту нет, а соответствие WCAG оценивается в обычном режиме. Это регрессия по сравнению с прежним текстовым знаком, а не провал по порогу. Поэтому одна замена судьи на WCAG-замер в принудительных цветах её бы не поймала — нужна отдельная строка приёмки.
4. Accessible name не страдает: aria-label ссылки и подвала на месте.

Обе правки из предложения линзы работают в эмуляции, если подставить стиль на лету:
- @media (forced-colors: active) { fill: CanvasText } — все части становятся rgb(0,0,0) в светлой схеме и rgb(255,255,255) в тёмной;
- fill: currentColor — в шапке LinkText (rgb(0,0,159) и rgb(255,255,0)), в подвале CanvasText.

Любая из них расходится с правилом DESIGN.md «Краски — только токены», так что вопрос владельцу поставлен правильно. На настоящей контрастной теме Windows я не проверял: это меняет системные настройки и вне моих ограничений.

######## R1-VNEDRENIE-4 [дефект → подтверждено/дефект]
znak:check пропускает испорченный каталог favicon.ico, хотя обещает ловить ручную правку файла; Chromium при этом рисует иконку 16×256
ДОК: tools/znak.mjs:135-144 (icoObrazy) читает из записи только ширину (байт e), смещение и длину. Высоту, тип ICONDIR, planes, bpp, colorCount и хвост после данных он не смотрит; сравнение в :158-164 идёт по ширине и пикселям. Опыт в копии (kopia/: package.json, tools/znak.mjs, src/data/znak.json, src/styles/global.css, src/layouts/Base.astro, public/): исходная копия — `node tools/znak.mjs --check` → «сверено», exit=0. После `node porcha-ico.mjs` (type=2, то есть курсор; у записи 0 высота 0, то есть 256, и planes=0; у записи 1 bpp=0 и colorCount=7; хвост 4096 B, файл 4665 B) → снова «znak: сверено — 6 файлов…», exit=0. `node ico-chromium.mjs`: исходный ICO Chromium декодирует как 32×32, испорченный — как 16×256.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:135-144, 158-164, шапка :34-36 («ловит … ручную правку файла»); улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/vnedrenie/porcha-ico.mjs, ico-chromium.mjs, kopia/
ПРЕДЛ: В --check сверять каталог ICO полностью: reserved=0, type=1, count, ширина = высота = IHDR вложенного PNG, colorCount=0, planes=1, bpp=32, данные вплотную без дыр и хвоста. Или сравнивать ICO побайтно, если выход sharp детерминирован на закреплённой версии. Это правка судьи, значит после неё — «судью судят».
СКЕПТИК: Опровергнуть не удалось, линза код и числа прочла верно. Сделал свою копию: package.json, tools/znak.mjs (cmp с репозиторием — равен), src/data/znak.json, global.css, Base.astro и public/ лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-4/kopia. Исходная копия проходит --check с exit=0.

Код. icoObrazy (znak.mjs:135-144) берёт из записи каталога только байт e (ширина, 0 → 256), длину и смещение. На :163 сравнивается a[i][0] (только ширина) и пиксели вложенного PNG, декодированного sharp отдельно, а не ICO целиком. Высоту, reserved, type, planes, bpp, colorCount и хвост после данных никто не читает.

Опыт: по одному полю за раз, файл varianty.mjs. Все варианты ниже проходят с exit=0 и печатают «сверено»: высота записи 0 = 0 (то есть 256); высота 0 = 32; type = 2 (CUR, курсор); reserved = 5; planes0 = 0; bpp1 = 0; colorCount1 = 7; хвост 4096 B; всё сразу, как у линзы. Ловится только ширина 0 = 17 (exit=1). После восстановления файла снова exit=0.

Chromium 151.0.7922.34 (chromium.mjs: decode, затем canvas):
- исходный ICO — 32×32, 1024 непрозрачных пикселя;
- только высота 0 = 0 — 16×256, 0 непрозрачных пикселей, то есть иконка пустая;
- только reserved = 5 — «EncodingError: The source image cannot be decoded», файл не читается вовсе;
- набор линзы — 16×256 и тоже пустой.

Значит, правка одного байта даёт иконку, которую браузер не рисует или не декодирует, а судья пишет «сверено». Находка даже сильнее, чем у линзы: достаточно одного байта.

Почему это не предел. Шапка :34-36 обещает без оговорок: «ловит правку источника без перезаписи и ручную правку файла». Единственный названный предел — «не судит, хорош ли рисунок». Пункт 3 шапки говорит «ICO — по пикселям каждого размера», но размер записи сверяется лишь наполовину (высота не читается). О том, что каталог ICO не проверяется, нигде не сказано: ни в шапке, ни в DESIGN.md:643-645 («падает, если … файл отстал от источника»), ни в сайтовом CLAUDE.md:194. Другого стража нет: DESIGN.md сам говорит, что сверка не входит в сборку и гейты, а core/accept/zero-view.mjs считает .ico двоичным и его не читает. Судья пропускает брак и печатает «сверено» — это дефект, а не честный предел. Попутно расходится и проза, но это следствие того же дефекта.

К предложению линзы. На этой машине `node tools/znak.mjs` в копии пишет все шесть файлов, побайтно равных public/ репозитория, favicon.ico включительно. Каталог в репозитории исправен: reserved 0, type 1, count 2, 16/16 и 32/32, cc 0, planes 1, bpp 32, смещения 38 и 177 вплотную, длина 569. Поэтому годятся оба варианта: побайтная сверка ICO, как у SVG (оговорка — детерминизм sharp держится только на закреплённой версии), или полная сверка каталога. Нынешний favicon.ico не испорчен, дефект только в судье. Правка судьи требует раунда «судью судят».

######## R1-VNEDRENIE-5 [дефект → подтверждено/дефект]
Проверка 4 znak:check («ссылка стоит в <head>») — поиск подстроки по исходнику Base.astro: проходят закомментированная ссылка и чужой rel
ДОК: tools/znak.mjs:173-175: `base.includes(`href="/${imie}"`)` по всему тексту src/layouts/Base.astro. Опыт в копии kopia/src/layouts/Base.astro: ссылка favicon.svg заменена на JSX-комментарий `{/* <link rel="icon" href="/favicon.svg" type="image/svg+xml" /> — снято */}`, у ссылки favicon-32x32.png rel="icon" заменён на rel="alternate". `node tools/znak.mjs --check` → «znak: сверено — 6 файлов public/ …», exit=0. В такой сборке favicon.svg, единственную иконку, которую Chromium запрашивает (R1-VNEDRENIE-7, zaprosy.mjs), в <head> уже не найти.
ГДЕ: sites/7thserpent.com/tools/znak.mjs:173-175 и шапка :21; улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/vnedrenie/kopia/src/layouts/Base.astro
ПРЕДЛ: Сверять <link> в <head> собранного dist/index.html, разобранные как теги (rel, href, type, sizes), или хотя бы теги Base.astro вне комментариев. Проверять rel="icon" / "apple-touch-icon" и type. Это правка судьи — после неё «судью судят».
СКЕПТИК: Опровергнуть не удалось. Линза прочла код верно, и её опыт повторяется.

Что в коде. В tools/znak.mjs:51 Base.astro читается как сплошной текст, а в :173-175 для каждого из шести имён проверяется только base.includes(`href="/${imie}"`). Где стоит подстрока, неважно: в <head>, в <body>, в комментарии или во фронтматтере. rel, type и sizes не проверяются вовсе.

Воспроизведение: свои копии, D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-5/, скрипт varianty.mjs. Файлы znak.mjs и Base.astro побайтно равны файлам репозитория (md5 78fbbd6b… и 0f6798c0…).
- kontrol, файл без изменений: exit=0.
- A, опыт линзы (ссылка favicon.svg в JSX-комментарии, у 32x32.png rel="alternate"): exit=0, «сверено — 6 файлов public/».
- B, ссылка favicon.svg в HTML-комментарии <!-- -->: exit=0.
- C, все шесть ссылок перенесены из <head> в конец <body>: exit=0. При этом текст проверки обещает «в <head>».
- D, отрицательный контроль (ссылка svg удалена): exit=1, «нет ссылки href="/favicon.svg" в <head>». Значит, проверка живая, просто сверяет текст, а не теги.
- E, самый сильный случай: все шесть ссылок иконок удалены, а их href="/…" дописаны строкой в /** */ фронтматтера. В файле остался один <link>, canonical. Результат exit=0, «сверено».

Проверка компилятором Astro 2.13.1 (kompil.mjs). В A в шаблон не попадает ни одной ссылки favicon.svg, у 32x32 rel=alternate. В B ссылка favicon.svg уходит в вывод HTML-комментарием, то есть браузер её не видит. В C все шесть ссылок оказываются вне <head>. В E ссылок иконок нет совсем. Судья выдаёт exit 0 на сборке без единой иконки в <head>: это пропуск брака.

Пределом это нигде не названо. В шапке инструмента раздел ПРЕДЕЛЫ (:30-36) говорит о fontkitten, sharp и о том, что проверка 3 не судит рисунок, а о текстовой природе проверки 4 не пишет ничего. Пункт :21 обещает проверить, что ссылка «не стоит в `<head>`». То же обещают DESIGN.md:643-645 («ссылки на него нет в `<head>`») и CLAUDE.md сайта :167 и :194 («ссылки в <head>»). Отчёта сессии со словом «предел» об этом нет.

Итог: класс «дефект» — судья пропускает брак, и его шапка и документы обещают больше, чем он проверяет. Расхождение в прозе вытекает из того же дефекта, отдельным классом «документ» его не выношу.

Ограничения моей проверки. Сборку сайта и браузер я не запускал. Отдельное утверждение линзы из R1-VNEDRENIE-7 (Chromium запрашивает только favicon.svg) не проверял: для этой находки оно не нужно.

Предложение линзы разумно: разбирать теги <link> в <head> собранного dist/index.html, сверяя rel, href, type и sizes, или хотя бы теги Base.astro вне комментариев. Это правка судьи, и после неё нужна новая проверка «судью судят».

######## R1-VNEDRENIE-6 [предел → подтверждено/предел]
Перекладина знака в подвале на 1440 при DPR 1,25 и 1,5 размыта (полутоновые строки); в шапке она чёткая. Эталон при DPR 1 этого не видит
ДОК: Прогон `node perekladina.mjs`, столбец x = 8 CSS px через перекладину, зелёный канал (снег 234, фон 13 в шапке и 19 в подвале). Шапка (y=15): 0 полутоновых строк на 1440 и 390 при DPR 1/1,25/1,5/2. Подвал 1440 (y=431,1875 — дробный, зависит от высоты содержимого): при DPR 1,25 — 2 строки (181 и 72), при DPR 1,5 — 2 строки (127 и 127); при DPR 1 и 2 — 0. Подвал 390 (y=402,5156): 0 при всех DPR. DESIGN.md:603-604: «перекладина снегом (ink, прямоугольник на целых пикселях)» — это верно для координат рисунка, но не для растра в подвале. Эталон снят при DPR 1, где различия нет.
ГДЕ: sites/7thserpent.com/DESIGN.md:603-604; подвал (.ft__brand .znak); улика — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/vnedrenie/perekladina.mjs
ПРЕДЛ: Записать предел в DESIGN.md или в доклад: чёткость перекладины гарантирована в шапке (целый y), а в подвале зависит от дробной высоты содержимого над ним. Если нужна чёткость — показать владельцу (приёмка глазами подвала при 125–150%); отдельной правки не требуется.
СКЕПТИК: Опровергнуть факт не удалось. Поправить нужно объяснение линзы и предложенную ею формулировку.

1. Факт воспроизведён. Скрипт skeptik-R1-VNEDRENIE-6/proba.mjs снимает окно целиком, без дробного clip, три столбца через перекладину, dist не пересобирался. Подвал 1440, docY 6584,1875:
- DPR 1,25 — две полутоновые строки, 181 и 72 (покрытие 0,75 и 0,25);
- DPR 1,5 — две строки, 127 и 127 (покрытие 0,5);
- DPR 1 и 2 — 0 строк;
- при четырёх положениях прокрутки (по центру, низ, низ−37, низ−113) результат один и тот же, значит дело в положении в документе, а не в прокрутке.
Шапка на 1440 и 390 — 0 строк при всех DPR. Подвал 390 (docY 8286,5156) — 0 строк при всех DPR. Числа линзы совпали точно.

2. Механизм у линзы неверен. mehanizm.mjs сдвигает знак только в DOM браузера: вставка высотой d над подвалом и отступ знака шапки. Chromium округляет начало SVG до целого CSS px, поэтому дробная часть высоты содержимого не участвует: .1875 чётко при DPR 1 и 2, .5156 на 390 чётко при всех DPR. Условие чёткости — (round(y)+5)×DPR целое:
- при DPR 1,25 нужно round(y) ≡ 3 (mod 4);
- при DPR 1,5 — round(y) нечётный.
Предсказание совпало во всех 60 замерах подвала (d = 0…6,8125) и во всех 20 замерах шапки. Шапка при y = 16, 17, 18 — целых — даёт 2 полутоновые строки при 1,25; при 1,5 размыты 16 и 18. Шапка чёткая не «из-за целого y», а потому что (68−38)/2 = 15 и 15+5 = 20, это совпадение высот. Подвал при 1,25 размыт в 3 положениях из 4, при 1,5 — в 1 из 2. Любой сдвиг содержимого над подвалом на целые пиксели переключает чёткость.

3. Пределом это нигде не записано. В _baseline/README.md о DPR ни слова. В DESIGN.md DPR упоминается только в контексте контраста и мастеров арта. Эскиз voprosy/A-semerka-trassa/opis.md:27-28 ограничивал обещание: «y 20–24 на странице: край на DPR 1 не мылится». DESIGN.md:603-604 это условие опустил, но фраза «прямоугольник на целых пикселях» верна для координат рисунка, чёткость растра она прямо не обещает. Поэтому класс не «документ».

4. Судьи честны, но не видят:
- эталон снят при DPR 1, а при DPR 1 подвал чёткий в любом положении;
- материалы приёмки (materialy-priemki.mjs) снимают подвал при DPR 1 (1440) и DPR 3 (390), при целом DPR размытия нет;
- листы эскизов показывали DPR 1,25 и 1,5 только для шапки.
Это не брак: обычное сглаживание края при дробной плотности. Класс — предел.

5. Формулировку линзы в DESIGN.md или доклад так записывать нельзя: «гарантирована в шапке (целый y) … зависит от дробной высоты содержимого» ложна, и запись с ней станет находкой класса «документ». Верно так: при DPR 1 и 2 край чёткий везде; при 1,25 и 1,5 чёткость зависит от целого остатка положения знака (по модулю 4 и 2 CSS px). В шапке она держится на высотах 68 и 38. В подвале 1440 сейчас край в полутон, на 390 — чёткий. Показать владельцу можно, отдельной правки не требуется.

Нарушение с моей стороны: в одной команде оболочки была пустая переменная с запрещённой подстрокой («…_free=1»). Утилита не вызывалась, ничего не менялось.

Файлы: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-6/proba.mjs, D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-6/mehanizm.mjs.

######## R1-VNEDRENIE-7 [предел → опровергнуто/нет]
Пиксельный рисунок 16 px (favicon-16.svg эскиза) в Chromium не показывается ни с SVG, ни без него; «приёмка глазами во вкладке» его не видит
ДОК: `node zaprosy.mjs`, свой сервер над dist, лог сервера и page.on('request'): headless и с окном, 1440×DPR1, 1440×DPR2, 390×DPR2 — единственный запрос иконки /favicon.svg (рисунок сетки 32). `node wybor-ikony.mjs`, набор ссылок как в dist, но без SVG: при DPR 1 и 2 Chromium берёт /favicon-32x32.png, а не favicon-16x16.png. Рисунок ikona16 доходит только до тех, кто читает запись 16 в favicon.ico или явно просит PNG 16. Сам рисунок верен: favicon-16x16.png против favicon-16.svg эскиза — 0 из 256 px (и растр sharp, и растр Chromium).
ГДЕ: sites/7thserpent.com/DESIGN.md:647-650; src/layouts/Base.astro:46-51; улики — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/vnedrenie/zaprosy.mjs, wybor-ikony.mjs
ПРЕДЛ: В доклад и DESIGN.md строкой: вкладка Chromium показывает favicon.svg, пиксельные 16 px — только для клиентов ICO и PNG 16. На приёмке глазами показывать ikona16 отдельно (запись 16 из favicon.ico один к одному), иначе этот рисунок остаётся непринятым.
СКЕПТИК: Факты линзы верны, я их воспроизвёл. Вывод линзы — нет: предел уже назван в документах, а рисунок 16 уже показан на приёмке отдельно.

Что воспроизведено. Мои скрипты лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r1/skeptik-R1-VNEDRENIE-7/: proba.mjs и piksele.mjs. Сервер свой, над dist, dist только читался. Главную dist я открыл с окном и без него, при DPR 1 и 2 (и флагом запуска, и контекстом), на 1440 и 390. Во всех восьми прогонах единственный запрос иконки — /favicon.svg. Проба без SVG (ico32, p32, p16, p192) при DPR 1 и 2 берёт /favicon-32x32.png. Пара p16 и p32 тоже берёт 32. Файл /favicon-16x16.png Chromium берёт, только когда PNG 16 — единственная ссылка. Без ссылок он берёт /favicon.ico. Сравнение по пикселям с favicon-16.svg эскиза A: favicon-16x16.png — 0 различий из 256, запись 16 в favicon.ico — 0 из 256, запись 32 в ICO против favicon-32x32.png — 0 из 1024. Сетка 32, растрированная в 16, отличается от ikona16 на 27 px из 256: вкладка действительно показывает другой рисунок.

1) Предел уже назван, и почти теми же словами, что предлагает линза. DESIGN.md:649-650: «Вкладку Chromium рисует из favicon.svg при любом порядке ссылок, PNG — запасные». Base.astro:9-14: Chromium берёт favicon.svg при любом порядке ссылок, PNG — запасные для браузеров без SVG-иконок. tools/znak.mjs:23 — «favicon.svg (вкладка — его рисуют Chromium, Firefox)». Там же, в разделе «ПРЕДЕЛЫ» (:32-34): «вкладку Chromium рисует сам из favicon.svg». Владелец выбирал вариант, зная это. voprosy/A-semerka-trassa/opis.md:59-60 и :141-143: «16 px держит сам favicon.svg: вкладку в Chrome, Edge и Firefox рисует он. favicon-16.svg — только исходник запасного PNG 16». VOPROSY.md:102-103: «Со ссылками все проверенные наборы берут favicon.svg». Первая половина предложения линзы («В доклад и DESIGN.md строкой…») уже исполнена.

2) Утверждение «иначе этот рисунок остаётся непринятым» ложно. В материалах приёмки есть docs/reports/2026-09-24-7thserpent-znak/ikony.png. Его пишет materialy-priemki.mjs (строки 14-15, 95, 97). На листе отдельными клетками показаны «favicon-16x16.png · ×4» и «favicon.ico · 16 · ×4»: запись 16 взята прямо из ICO, увеличена ×4 с image-rendering: pixelated, каждый пиксель виден. Я открыл лист: обе клетки на месте, рисунок ikona16 отличим от сетки 32. Вторая половина предложения линзы тоже уже исполнена. Масштаб «один к одному» ничего не добавляет: при ×4 без сглаживания видны все пиксели.

3) Новый нюанс — Chromium без SVG берёт PNG 32, а не 16 — не противоречит ни одному документу. Документы не утверждают, какой PNG возьмёт браузер, а только что PNG — запасные. Проба «без SVG» в Chromium не моделирует браузер без поддержки SVG: это тот же Chromium со своим выбором размера. Фраза DESIGN.md:648 «На 16px — свой рисунок… (ikona16)» говорит о файлах 16 (PNG и запись ICO), и это верно. Следующее предложение прямо говорит, что вкладку Chromium рисует favicon.svg. Расхождения прозы с фактом нет.

Итог: судья не пропускает брак и не врёт. Предел честно назван в пяти местах, рисунок 16 предъявлен на приёмку отдельно. Находка ничего не добавляет.

######## КРИТИК
R1-POLNOTA-1 [судья без линзы] Владелец принимает знак и фавикон глазами (П83 п. 4) по материалам трёх новых инструментов: materialy-priemki.mjs, vkladka.mjs и okno.ps1. Ни одна из четырёх линз их не проверяла. Сами инструменты и 12 PNG не закоммичены (в git status они «??»), так что по правилу «отправлено — только с путём» материалы владельцу не отправлены. Как закрыть: провести по ним отдельную линзу до передачи владельцу, затем закоммитить материалы вместе с путём.

R1-POLNOTA-2 [утверждение без числа] Base.astro, DESIGN.md и c1ec5e6 утверждают: «Chromium 151 берёт favicon.svg (с окном и без, DPR 1 и 2) при любом порядке ссылок». Опора — журнал запросов vkladka.mjs, который выводится только в консоль и нигде не сохранён. Запрос файла ещё не значит, что его рисуют. Порядок ссылок ни один инструмент папки не переставляет, а «без окна» у вкладки нет вообще. Мой замер снимков папки: иконка во вкладке при DPR 1 (vkladka-1x-*.png, 16×16 от (56, 12); светлая и тёмная полоса совпадают в рамке иконки побайтно) ближе всего к favicon-32x32.png, уменьшенному 2:1 (SSE 640). От того, как Chromium сам рисует favicon.svg в <img> на 16 px (svodka.png, точка (236, 1089)), она отличается на SSE 26 379 при максимуме 68 уровней. С пиксельной иконкой 16 расхождение SSE 241 503, то есть её вкладка не показывает. Какой файл рисует вкладка, не установлено. Как закрыть: во временной копии dist перекрасить в контрольный цвет favicon-32x32.png вместе с favicon.ico, а в отдельном прогоне — favicon.svg, снять вкладку vkladka.mjs и назвать источник по цвету. Порядок ссылок переставлять в той же копии. Журнал запросов и browser.version() писать в JSON рядом со снимками.

R1-POLNOTA-3 [эскиз ≠ вкладка] Фавикон владелец выбирал по имитации вкладки: render.mjs и materialy.mjs вставляли в страницу <img src=favicon.svg width=16>. Настоящая вкладка при DPR 1 отличается от этой имитации на SSE 26 379 (максимум 68 уровней на канал), и верность имитации никто не мерил. Как закрыть: положить в материалы приёмки рядом обрезку «имитация эскиза» и обрезку «настоящая вкладка» с числом диффа, а в докладе прямо сказать, что лист эскизов показывал не вкладку.

R1-POLNOTA-4 [число не показывает запаса] Три утверждения о 961 px: «меню на 961 не переполнено (735 из 735)» в opis.md A, «помещается на 961 с запасом» в VOPROSY.md и «на 961 меню не переполняется» в DESIGN.md. Метрика render.mjs сравнивает list.scrollWidth > nav.clientWidth. У .hdr__list, блочного флекс-списка внутри .hdr__nav с flex:1, scrollWidth не бывает меньше clientWidth. Поэтому 735/735 — нижний предел метрики: переполнение она ловит, а запас не видит. Кроме того, замер снят подстановкой фрагмента в шапку, а не на собранной c1ec5e6. Как закрыть: на c1ec5e6 мерить правый край li:last-child против правого края nav и зазор между знаком и меню, полосой 961–1180 с шагом 1 px (в этой полосе gap sm) и на 320. «С запасом» подтвердить числом.

R1-POLNOTA-5 [судья повторяет генератор] Проверка 1 в znak:check пересчитывает контуры той же формулой и той же библиотекой, которые их породили (glify.mjs совпадает с znak.mjs, строки 72–83). Она ловит дрейф, но не ошибку. Систематическая ошибка генератора проходит как «контуры равны гарнитуре»: кернинга нет, а назван этот факт только в glify.mjs — ни в шапке znak.mjs, ни в DESIGN.md («контуры Bodoni Moda 600») его нет. Как закрыть: независимый судья — Chromium рисует <text> Bodoni Moda 600 (woff2 темы, font-kerning: none, letter-spacing = track) поверх path знака, и они сравниваются по пикселям. Отсутствие кернинга назвать пределом в DESIGN.md и в шапке znak.mjs.

R1-POLNOTA-6 [мутации не сохранены] Линзы нашли дефекты znak:check мутациями, но у инструмента нет --selftest с отрицательными пробами (как у tree:selftest, anatomy:selftest и проб гейта контраста). После починки R1-IKONY-1…5 и 9 следующую правку судьи нечем будет судить. Как закрыть: завести znak:selftest с набором мутаций, каждая с ожидаемым exit 1: токен в комментарии, 700 и курсив, SVG под именем .png, gAMA, битый каталог ICO, ссылка в комментарии, в <body> и с чужим rel.

R1-POLNOTA-7 [прогон против чтения] Все линзы судили исходник Znak.astro, а браузер получает минифицированный CSS. В dist @media (max-resolution: 1.5dppx) превратился в (resolution<=1.5x), а правило «.znak path» — в .znak[data-astro-cid-yruwqcb3] path[data-astro-cid-yruwqcb3]. Минификатор уже ломал правило (C1 сессии 9). Как закрыть: починку R1-KONTRAST-1 проверять по dist — getComputedStyle(path.znak__obwodka).strokeWidth при DPR 1, 1,25, 1,5, 1,51 и 2 на прод-превью. Назвать поддержку синтаксиса диапазонов: Safari младше 16.4 выбросит весь блок.

R1-POLNOTA-8 [политика: пересъёмка] Починка R1-KONTRAST-1 и R1-VNEDRENIE-1 изменит «TH» во всех 13 кадрах при DPR 1: эталон z1 закрепил дефект. Покрывает ли П83 п. 2.3 вторую пересъёмку, никто не проверял. Прецедент — сессия 10, где вторую пересъёмку оформили отдельно, пунктом П82 п. 2. Как закрыть: до правки — вопрос владельцу или ссылка на запись, которая пересъёмку покрывает.

R1-POLNOTA-9 [другой браузер] Всё снято одним движком Chromium. Ни разу не прогнаны утверждения: «favicon.svg рисуют Chromium, Firefox» (шапка znak.mjs); «PNG — для браузеров без SVG-иконок»; «sizes=32x32 — чтобы браузер, читающий размеры, не предпочёл ICO» (Base.astro); «icon-192 — ярлык Android»; «apple-touch-icon — домашний экран iOS». Как закрыть: прогнать Playwright firefox и webkit, а если их нет — назвать пределом в DESIGN.md и докладе. iOS и Android назвать пределом словами.

R1-POLNOTA-10 [версия движка] Эталон снят HeadlessChrome 153 (Playwright MCP). kontrast-znaka.mjs, materialy-priemki.mjs, vkladka.mjs и render.mjs работают на бинаре chromium-1234 из кеша (в доводах он назван «Chromium 151»), а playwright-core берут из временного _npx/9833c18b2d85bc59. Версию браузера ни один инструмент не печатает. Как закрыть: выводить browser.version() в каждом инструменте и сверить с эталоном хотя бы один кадр материалов, снятый тем же движком.

R1-POLNOTA-11 [растр] Все кадры знака сняты headless, то есть программным растром. GPU-растр в окне, какой владелец увидит на своём мониторе при масштабе Windows 125 или 150 %, снят только для полосы вкладок. Как закрыть: vkladka.mjs уже поднимает окно — добавить обрезку шапки и подвала при --force-device-scale-factor=1,25 и 1,5 и сравнить с headless-кадром того же DPR.

R1-POLNOTA-12 [материалы без дробного DPR] Листы эскизов показывали DPR 1,25 и 1,5, причём на фрагменте, где обводка работала. В материалах приёмки собранного знака (materialy-priemki.mjs) есть только DPR 1, 2 и 3, поэтому ни пропавшую обводку «TH», ни размытую перекладину подвала (R1-VNEDRENIE-6) владелец глазами не увидит. Как закрыть: добавить шапку и подвал при DPR 1,25 и 1,5.

R1-POLNOTA-13 [числа без выгрузки] kontrast-znaka.mjs выводит результат только в консоль, файла нет. Числа 7,32, 7,61, 12,37, 12,85 и rgb(36 39 44) в gates/contrast.mjs и DESIGN.md не подкреплены сохранённым выводом. Сборку на порту инструмент не сверяет даже по canonical, так что «сборка c1ec5e6» — только со слов. Как закрыть: писать JSON в папку доклада с адресом, версией браузера и хешами index.html и CSS, отданных портом.

R1-POLNOTA-14 [литералы в судье] В kontrast-znaka.mjs краски заданы литералами '#eca84a' и '#e5eaee' (строка 20), а не читаются из global.css. Сейчас они совпадают с токенами, но при смене ink или accent замер молча будет мерить старые краски. Как закрыть: читать токены из global.css тем же разбором, что гейт ядра (после починки R1-IKONY-1).

R1-POLNOTA-15 [правило DESIGN.md без судьи] В разделе «Геометрия» сказано: «выступ ромба за конец черты — не больше 2,2 px при толщине черты 4 px». В шапке выступ ровно 2,2 (20,7 − 18,5). В иконке 32 при той же черте 4 выступ 2,7 (24 − 21,3), в ikona16 — 2 при черте 2. Правило написано для знака целиком, и не сказано, нарушают его иконки или оно только о шапке. Числом это никто не мерил (рядом с R1-IKONY-7). Как закрыть: уточнить формулировку в DESIGN.md (или задать выступ долей черты) и мерить его из znak.json в znak:check.

R1-POLNOTA-16 [доступность прогоном] Подпись ссылки знака, role="img" в подвале и немоту svg в шапке проверяли только чтением разметки. Дерево доступности Chromium (CDP Accessibility.getFullAXTree или page.accessibility.snapshot) не снимали. Как закрыть: снять его на dist при 1440 и 390, в том числе с открытым ящиком. Ожидается: имя ссылки «7th Serpent — 7thserpent.com, home», в подвале одно изображение с именем, потомки svg в дереве не выставлены.

R1-POLNOTA-17 [состояние кадра не проверено] materialy-priemki.mjs и materialy.mjs снимают «фокус на ссылке знака» после двух нажатий Tab, не проверяя document.activeElement. Если порядок фокуса изменится (skip-link, поиск), снимок покажет другой элемент, а подпись останется прежней. Как закрыть: проверять activeElement.closest('.hdr__brand') и падать, если это не так; добавить фокус при DPR 1 (там действует ветка ≤1,5).

R1-POLNOTA-18 [не то окно] okno.ps1 берёт первое окно процесса chrome, в заголовке которого есть «Max Payne». Так он снимет и собственный Chrome владельца, если там открыта страница сайта: PID браузера, запущенного Playwright, не сверяется. Ответ «no window» роняет execFileSync без имени снимка. Как закрыть: искать окно по PID дочернего процесса browser.process().pid (или по уникальному заголовку через ?t=), а ошибку выдавать штатным отказом.

R1-POLNOTA-19 [ICO глазами] В ikony.png раздел «оба размера ICO» показывает PNG, которые инструмент сам вырезал из каталога, а не ICO так, как его декодирует клиент. Как закрыть: отрисовать <img src=favicon.ico> в Chromium на 16 и 32 и положить рядом с вырезанными (рядом с R1-IKONY-4).

R1-POLNOTA-20 [П83 п. 5 без судьи] Три пункта приёмки не проверила ни одна линза: «@source маркером» (инвариант 8), «детектор impeccable» и «внешних запросов в dist нет». Детектор в прошлой сессии работал только по исходникам, по живой странице — нет (бэклог 58 п. 9). Правка Base.astro «10 длинных тире → 0» не объяснена: в Znak.astro десять строк с длинным тире, в SiteFooter.astro восемь, и находок там нет. Как закрыть: в докладе по каждому пункту дать команду, область и число. Внешние запросы проверять через page.on('request') на 390 и 1440 по всей прокрутке, а не только grep по dist.

R1-POLNOTA-21 [SEO, политика] Текстовую ссылку «7thserpent.com» в шапке каждой страницы заменил немой SVG с aria-label. Видимого текста ссылки на главную больше нет, в подвале имя тоже есть только в aria-label, а og:site_name и WebSite не заведены. Как это влияет на анкор и имя сайта в выдаче, ни одна линза не рассматривала. Как закрыть: строкой в доклад как политический вопрос владельцу, с вариантами (текст visually-hidden в ссылке вместо aria-label; og:site_name), без правки.

R1-POLNOTA-22 [вес страницы] Знак встроен в HTML дважды, в шапке и в подвале: контуры надписей — около 4,6 KB на копию (TH — 622 знака, SERPENT — 3951). Прирост веса index.html ни один судья не назвал. Как закрыть: дать в докладе число до и после; вариант <symbol>/<use> — строкой вне объёма.

Файлы, о которых идёт речь:
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/vkladka.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/okno.ps1
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/render.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/glify.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/vkladka-1x-temnaya.png
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/voprosy/svodka.png
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/voprosy/A-semerka-trassa/opis.md
- D:/SEO/cloud/site-generator/sites/7thserpent.com/tools/znak.mjs
- D:/SEO/cloud/site-generator/sites/7thserpent.com/src/components/Znak.astro
- D:/SEO/cloud/site-generator/sites/7thserpent.com/src/data/znak.json
- D:/SEO/cloud/site-generator/sites/7thserpent.com/src/layouts/Base.astro
- D:/SEO/cloud/site-generator/sites/7thserpent.com/DESIGN.md
- D:/SEO/cloud/site-generator/sites/7thserpent.com/dist/_astro/index.0TxqFWC-.css