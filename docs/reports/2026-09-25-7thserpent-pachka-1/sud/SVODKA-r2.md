# «Судью судят», раунд 2 — сессия 14

Что судилось: правки раунда 1 (`9a682b9..acd33f6`) — схема и маршрут, переписанные тексты, исправленные
инструменты, новые разовые пробы `proby-p1.mjs`, `proba-sverki-dist.mjs`, `href-re.mjs`. Три линзы —
отдельные агенты, только чтение; скептическая сверка — ведущим.

| Линза | Дефектов | Безопасных | Пределов | Прозы | Опровергнуто |
|---|---|---|---|---|---|
| маршрут, схема, пробы p1 | 1 | 1 | 6 | 1 | 6 |
| инструменты | 8 (на настоящей сборке срабатывает 1) | 1 | 3 | 2 | 5 |
| факты текстов | 4 искажено, 1 не подтверждено, 1 §5, 1 близко, 4 сомнительно, 1 R1 не исправлено | — | — | — | — |

## Что сделано

| Находка | Исход |
|---|---|
| R2-MARSHRUT-1 — `https://?x`, `https://#x`, `https://:443/`, `https://@/`, `https://%2F/` проходили схему (сборку ронял `links` с ложной причиной) | хост внешнего адреса — непустой, без `/ \ ? # @ : %`, затем порт и путь; `href-re.mjs` — 28 адресов, все как ждали (законные: `/?q`, `/#top`, `&`, `@RockstarGames`, порт, IDN) |
| R2-MARSHRUT-2 — проба сверки `dist` без контроля | контрольный прогон чистой копии обязан дать 0; у каждой порчи — ожидаемая причина; 3 новые порчи (подмена кадра, нота вне подвала, старый текст) — 9/9 |
| R2-MARSHRUT-3, -4, -6, -7, -8 | сверка `dist`: кадры по ключам напечатанных `<img src>` в порядке рядов; нота — только в `p.ft__art-note`; адрес кнопки — после раскрытия сущностей; файлы содержания — по дереву и через пакет `yaml`; свежесть — заголовки рядов и призыва обязаны стоять в HTML |
| R2-INSTR-1 (на настоящей сборке: 13 склеек в списке ссылок) | строчные теги снимаются вплотную и через пробел, находки объединяются; итог на сборке — 0 |
| R2-INSTR-7, -8, -9, -10, -13 | адрес — `new URL` против страницы сайта и сравнение хоста; `url()` в значении любого атрибута; `@import` без пробела и с комментарием; на подлоге линзы — 9 загрузок `dist-vn2` и 3 `dist-vn3` пойманы |
| R2-FAKT-1…12 | тексты исправлены (слова автора фикса — «called it obsolete … pointed players to a newer all-in-one fix», без «updated regularly»; «патч 1.05» снят; диапазоны — только у процессора и видеокарты; Controller Companion — за Max Payne 2 и «a player in 2023»; совет по сглаживанию — «a Steam guide suggests it»; платформы RDR2 сняты; Sleeping Dogs — ветка без издания, RAWG — Definitive Edition; The Last of Us и соседи — своими словами; мемы — «about his painkillers: pills that fix a gunshot wound, and how many he takes»; лицо Макса — «a wider range»; обложка — другим порядком); адрес канала — доказательство по сырому HTML (`kanal-dokaz.mjs`, `zamery/kanal-dokaz.txt`) |
| проза: R2-MARSHRUT-9, R2-INSTR-4, -19 | исправлена |

## Пределы, названные в шапках (не чинятся)

R2-MARSHRUT-5 (порядок призыва — только среди `<section class=…>`); R2-INSTR-2, -3 (границы `<main>`,
комментариев и скриптов — регулярками; прочие теги — пробелом; `alt` — в двойных кавычках); R2-INSTR-11,
-12, -14 (разбор тегов не по стандарту HTML: повтор атрибута, `<!-->`, `</script x>`, тип скрипта по всей
строке атрибутов, meta refresh без `url=`, `on…=`, `srcdoc`, `data:` — у репозитория нет парсера HTML; основной
довод «внешних загрузок нет» — браузерная проверка); R2-INSTR-16 (прелюдия `@layer` — текстом).
Коридор `/pc/` после правок фактов — 3869 при верхе 3884: запас 15 знаков.
