# 2026-09-10 · Три стены волны 1, П38, П39 и решение пункта 22

Ветка `phase-3`, вершина на старте — `3de3b20`, дерево чисто.
Объём зафиксирован на старте: четвёртый пункт П36, снятие `card-rail`
у `topic`, отметки пунктов 10/11/12, новая редакция П30 п.2, три стены
волны 1, решение пункта 22. **Перебазирование в объём не входит** — оно
отдельной сессией, как велено.

---

## Первой строкой, как велит П33

**`npm run accept` — `44/44`, `exit=0`. `npm run gates` — `4/4`, `exit=0`.**
`astro check` — 0 ошибок, 0 предупреждений, 2 подсказки.

## Сверка открытых вопросов прошлого отчёта — по П38, который эта же сессия и завела

| Вопрос прошлого отчёта | Исход | Где |
|---|---|---|
| дыра П36 — чинить ли правило | **получен и исполнен** | П38 `DECISIONS.md`, коммит `ec32f64` |
| `card-rail` `type-default` у `topic` | **получен и исполнен** | 6 записей снято, коммит `ec32f64` |
| пункты 18, 19, 21, 22 бэклога | **получен и исполнен**: 18 и 19 остаются бэклогом, 21 закрыт, 22 решён | коммиты `caf963f`, `ec32f64` |

Список записей П36 прошлого отчёта — девять строк, **все на месте**.

---

# Часть 1. Три стены волны 1

## Стен было три, а корень один

**Машинная сверка не проверяла печать. Никогда.** Она сравнивала
`doZrobienia` — объявленное и реализованное — с `oczekiwana`, то есть с тем
же объявленным, отфильтрованным по рукописному списку имён. **Обе стороны
равенства выводились из `blocks[]`**, поэтому равенство говорило ровно одно:
порядок объявленного согласуется с порядком в шаблоне. О том, что шаблон
на самом деле рисует, оно не говорило ничего.

### Почему это тот же класс, что дублирование ссылок, — и почему сверка его не поймала

Владелец назвал класс верно: **шаблон печатает то, чего контракт
не объявлял.** У дублирования ссылок это была лента, дважды показывавшая
`related`; у героя — блок, который печатался на шести страницах, его
не объявляющих.

**Сверка не поймала его по устройству, а не по недосмотру:** предикат
«объявленное равно напечатанному» был реализован как «объявленное равно
объявленному». Второго операнда — напечатанного — в коде не существовало
вовсе. Поймать блок, печатающийся вне списка, эта конструкция не могла
ни при каком входе.

**И честная часть: одну из шести страниц она бы всё-таки остановила —
случайно, и я это сломал.** До 2026-09-10 `oczekiwana` была константой
из девяти имён, то есть требовала от каждой страницы полного комплекта.
Страница без `hero-key-art` не совпадала по длине и роняла сборку — не потому,
что герой печатается лишним, а потому, что список неполон. Мой же фильтр
(правка того же дня, побочный итог вопроса 4) снял это требование — и вместе
с ним снял случайную ловушку. Стена «21 страница из 29» ужалась до двух,
а безусловный герой стал невидим совсем.

**Отсюда вывод, ради которого это и разбирается:** восстанавливать константу
было бы неверно. Она ловила не тот дефект и не тем способом. Чинить надо
второй операнд.

## Что сделано

**Маршрут вычисляет `drukowane` — что он напечатает** — из тех же предикатов,
что стоят в условиях разметки, и сверяет с `doZrobienia`. Каждое условие
печати теперь начинается с `drukowane.includes(…)`, поэтому список
и расстановка считаются из одного источника и разойтись не могут.
Расхождение называет, чего не хватило: содержания или места в `PORZADEK`.

| Стена | Что было | Что стало |
|---|---|---|
| **1. Второе вхождение блока** | тождеством блока было имя; два `card-rail` на `/ezio-auditore/` схлопывались | тождество — пара `имя#role`; `card-rail` печатается циклом по вхождениям, содержание подбирается по `role`; поле `cards` в схеме стало списком |
| **2. `link-columns`** | объявлен на `/poradniki/`, реализован в ядре и **неизвестен шаблону** одновременно | маршрут импортирует и печатает; схема получила поле `columns` |
| **3. `hero-key-art`** | печатался **безусловно**; шесть страниц его не объявляют | условие такое же, как у остальных |

**`PORZADEK` получил `link-columns`, и позиция вычислена, а не выбрана.**
Топологическая сортировка `blocks[]` всех 29 страниц маршрута: согласованный
порядок **существует и он единственный** —
`hero-key-art, byline, toc, story-row, card-rail, gallery, link-columns,
verdict-box, link-list, cta-band`.

**`band-quote` не добавлен намеренно:** на маршруте он не встречается ни разу,
стоит только на главной. Появится страница с ним — сверка упадёт громко
и назовёт его.

**Что принесёт главная, когда переедет.** Ровно **два конфликта порядка**,
и это единственные два во всём дереве: `link-list` раньше `gallery`
(на всех 22 игровых — наоборот) и `link-list` раньше `link-columns`
(на `/poradniki/` — наоборот). Тогда либо `blocks[]` главной приводится
к общему порядку, либо шаблон получает настоящего диспетчера. Пока у главной
своя страница, выбирать не из чего.

## Приёмка

| Проверка | Факт |
|---|---|
| сверка по всем 29 страницам маршрута | **пройдёт 23, откажутся громко 6** — число ниже поправлено, см. часть 4 |
| правил CSS | **360 → 360**, мультимножество троек **совпало** |
| `@media` | 27 / 16, не дрогнули |
| манифест | 56 → 56, **6 расхождений**: импорт `LinkColumns` пересобрал куски CSS |
| **пиксель, главная** | **4 кадра из 4 побайтово** |
| **пиксель, страница игры** | **побайтово** |
| гейты, `accept`, `astro check` | зелёные |

Расхождение манифеста разобрано: правила те же, разъехались по листам,
и пиксель обеих страниц это подтверждает.

---

# Часть 2. `card-rail` — второй заход

Критерий владельца применён измеримо: запись имеет `"source": "type-default"`
(повода под ней не было) **и** страница объявляет `link-list` (ссылки уже
покрыты).

| Заход | Кого | Записей |
|---|---|---:|
| первый | 21 страница типа `game` | 21 |
| второй | 5 типа `topic` + 1 `hub` (`/poradniki/`) | **6** |
| **итого** | | **27 из 29** |

**Осталось два, оба намеренные.** Главная: критерий на ней **не выполняется**
и это проверено — её лента несёт поradniki, а `link-list` несёт нумерацию
серии (`role: numeracja-serii`). Вторая лента `/ezio-auditore/`:
`source: manual`, `confidence: high`, `role: the-ezio-collection`.

**Расширение за пределы слова «topic» названо прямо.** Владелец назвал тип
`topic`; критерий, который он же назвал, верен и для `hub` `/poradniki/` —
там лента без содержания стояла бы рядом с `link-columns`, то есть с полным
каталогом тех же поradników. Снял по критерию; отменяется одной строкой.

---

# Часть 3. Решение пункта 22

**Выбран первый путь: новые имена слотов. Инструмент до этого не запускается.**

| Путь | Цена |
|---|---|
| **1. Новые имена слотов** (выбран) | записи в `art.json` плюс запуск. **Пересъёмки нет** — новые слоты кормят новые места, первый экран и панели эпох не трогаются |
| 2. Фото Commons вытесняет материал издателя | правка `media.ts` **плюс пересъёмка целиком**: сменится первый экран и все пять панелей главной. Это решение о виде, а список П32 закрыт |

**Довод сверх цены.** Сайт о серии игр, и панели эпох показывают ключевой арт
самих игр — `game-art.json` называет игру у каждого слота. Заменить их
стоковыми историческими видами значит потерять то, что делает сайт похожим
на сайт об играх. Перекрытие `gry/` над `foto/` — записанное предпочтение,
и оно верное.

**Сторож не заведён намеренно:** проверка «идентификаторы `art.json` не
пересекаются с `game-art.json`» сегодня была бы красной на шести из шести
и уронила бы сборку, ничего не починив. Заводится вместе с новыми именами.

---

# Открытые вопросы

## Технические — решены

- **Почему сверка не ловила печать** — второго операнда не существовало;
  восстанавливать прежнюю константу неверно, она ловила не тот дефект.
- **Где взять позицию `link-columns`** — топологической сортировкой, а не
  на глаз.
- **Как поддержать второе вхождение блока, не трогая контракт** — ключ
  `имя#role` плюс список в схеме контента; поле `role` в `blocks[]` уже было.
- **Пункт 22** — первый путь, довод и цена записаны.

## Политические — владельцу

**1. Снятие `card-rail` у `hub`.** Вы назвали `topic`; я снял и у
`/poradniki/`, потому что ваш критерий там выполняется. Если намерение было
уже — возвращается одной строкой структуры.

**2. Порядок блоков главной перед её переездом.** Четвёртый пункт П32 принесёт
два конфликта порядка. Решать придётся одно из двух: привести `blocks[]`
главной к общему порядку (правка структуры, то есть контракта) или дать
шаблону настоящего диспетчера (правка маршрута). Первое дешевле и делает
дерево однороднее; второе честнее к странице, у которой свой замысел.
Вопрос встанет в сессии переезда, не раньше — но встанет обязательно.

---

# Записи, обещанные этой сессией — по П36

| Что | Где | Источник |
|---|---|---|
| четвёртый пункт П36 и его диагноз | **П38** `DECISIONS.md` | ответ владельца |
| кадры слоёв по якорю, сдвиг числом | **П39** `DECISIONS.md`, прежняя редакция рядом | ответ владельца |
| снятие `card-rail` обоими заходами | **пункт 20** `docs/BACKLOG.md` | ответ владельца |
| пункт 21 закрыт | **пункт 21** `docs/BACKLOG.md` | ход работы |
| решение пункта 22 с обоими путями | **пункт 22** `docs/BACKLOG.md` | ответ владельца |
| пункты 10, 11, 12 отмечены сделанными | **`docs/BACKLOG.md`** | ответ владельца |

**Обещано и НЕ сделано:** ничего.

---

# Часть 4. Состязательная проверка правки маршрута — три дефекта, два моих

*Дописано в тот же день. Проверка запущена сразу после коммита `caf963f`
с одним заданием: сломать правку, а не похвалить её.*

## Что нашлось и что подтвердилось моей перепроверкой

### 1. Та же классом дыра, что чинили — и внесена той же правкой

`drukowane` несёт ключи `имя#role`, а **девять условий печати из десяти
спрашивают голое имя**. Значит блок с ролью **прошёл бы сверку и не
напечатался**. Молча. Ровно тот класс, ради которого сверка и переписывалась.

**Случай уже существует:** главная носит `link-list` с
`role: numeracja-serii`. При её переезде секция исчезла бы при зелёной сборке.

**Починка — граница, а не видимость поддержки.** Роль умеет `card-rail`
(цикл по вхождениям плюс подбор содержания по `role`); любое другое вхождение
с ролью **роняет сборку с именем виновника**. Девяти условиям после этого
можно спрашивать голое имя: вхождение с ролью до них не дойдёт.

### 2. `h1` исчез на шести страницах — это внесла моя правка стены 3

`<h1>` во всём репозитории **ровно один**: `core/blocks/HeroKeyArt.astro:68`.
До сегодняшней правки герой печатался безусловно, поэтому заголовок был
всегда — **случайно**. Шесть страниц волны 1 блок не объявляют и собрались бы
**без заголовка при зелёных гейтах**, хотя `schema.json:40` требует `h1`
от каждой страницы.

**Гейт структуры этого не видит по устройству:** он проверяет поле
в договоре, а не тег в HTML.

Поставлен **отказ, а не ответ**: маршрут не соберёт страницу, у которой ни
один печатаемый блок не несёт `h1`. Кто должен его нести — пункт **23**
бэклога, решение владельца.

### 3. Мёртвый якорь оглавления — тоже моя правка

Пункт «Dokąd stąd» вёл на `#dokad-tytul`, а секции нет со снятия `card-rail`.
Проверено в собранном файле: строка встречалась один раз — как `href`,
элемента с таким `id` не было. Пункт снят; оглавление 5 → 4, страница
4666 → 4627.

## Число из части 1 поправлено

**Было записано «пройдёт 29, упадёт 0». Верно — «пройдёт 23, откажутся
громко 6».** Разница — те самые шесть страниц без `h1`. Прежнее число
измерялось до того, как дыра была найдена, и оно описывало сверку порядка,
не знавшую про заголовок.

**Отказ шести — верное состояние, а не регресс.** Они собирались бы без
заголовка; теперь не соберутся, пока владелец не скажет, какой блок его
несёт. Седьмой отказ ждёт главную — на роли `link-list`.

## Приёмка починки

| Проверка | Факт |
|---|---|
| манифест | 56 → 56, **изменился ровно один файл** — HTML страницы игры |
| главная | **не тронута побайтово** (её `index.html` в диффе отсутствует) |
| правил CSS | **360 → 360** |
| оба сторожа сработали на симуляции | роль — 1 страница (главная), `h1` — 6 страниц |
| гейты, `accept`, `astro check` | зелёные, 0 ошибок |

## Найдено рядом и не тронуто — инвариант 3

**Якоря `#zejscie` и `#katalog` в шапке мертвы на всех страницах, кроме
главной.** Четыре битые ссылки на странице волны 1, проверено в браузере.
Оставлены чисткой хрома 5а с доводом «это места на странице, не адреса» —
довод верен только для главной. На тридцати страницах это под сотню битых
ссылок, и все в шапке. Пункт **24** бэклога.

## Что это говорит о самой проверке

Правка, которую я в части 1 разбирал как закрытие целого класса дефектов,
**сама содержала два дефекта того же класса** — и один из них был бы
в точности тем, что она обещала ловить. Состязательная проверка нашла их
в тот же день, до приёмки владельцем. Это довод не в пользу моей
аккуратности, а в пользу того, чтобы такую проверку ставить после каждой
правки судьи: **судья, которого никто не судит, повторяет ошибку
подсудимого.**
