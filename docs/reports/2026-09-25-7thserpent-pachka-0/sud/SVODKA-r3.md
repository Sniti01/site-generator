# «Судью судят», раунд 3 (последний) — сводка

*Пачка 0 второго сайта (П85: «не больше трёх раундов; не сошлось — заморозка, таблица пределов, вопрос»). Судился дифф правок раунда 2 (42fa5f3): три линзы (судья головы с мутационной проверкой, пробы, маршрут-схема-брифы) и скептик на каждую; агенты только читали репозиторий (один скептик нарушил запрет — файл журнала «--» в корне, удалён ведущим). Полные тексты — вывод workflow wf_75cf3b9c-b37 (вне git).*

Итог: находок 17; confirmed/defect — 5; confirmed/limit — 9; confirmed/prose — 3. Критерий П73 п. 2 не достигнут. Судьи заморожены на 42fa5f3; дефекты и пределы — таблица «Пределы после раунда 3» доклада.

| id | вердикт скептика | класс | находка |
|---|---|---|---|
| R3-GLOWA-1 | confirmed | defect | Правка R2-GLOWA-1 перестала гасить <noscript>, и его содержимое стало границей для всего разбора. Это вид без JS; в браузере с JS строки-границы в двух noscript прячут вторую запись, а </nav> в noscript прячет лишнюю ссылку крошек. Регресс к fada5d3 |
| R3-GLOWA-2 | confirmed | limit | Мутационная проверка: 9 из 38 мутантов правок раунда 2 проходят самопроверку 92/92. Пять новых проверок текущего звена можно снять разом: у двух проб, названных «ссылка внутри текущего звена» и «незакрытая <a>», срабатывает только счёт ссылок |
| R3-GLOWA-3 | confirmed | defect | JSON-LD теперь ищется по тексту с погашенным сырым текстом и шаблонами. Строка «<title>», «<script>» или «<template>» в значении атрибута молча стирает настоящий блок, а блок с «разорванным» тегом пропадает без отказа. На fada5d3 был отказ |
| R3-GLOWA-4 | confirmed | defect | Имя навигации: принимается любой непустой aria-labelledby, даже ссылка на id, которого нет. Проба с таким id («x») закрепляет безымянную навигацию как законную; fada5d3 такое отвергал |
| R3-GLOWA-5 | confirmed | limit | Границы сырого текста и noscript — не как в браузере: <title/>, <textarea/>, <script/>, <noscript/>, </script x>, </title x>, </noscript x>. Назван только вариант с template (R2-GLOWA-3); R2-GLOWA-4 и новая п. 8 обходятся |
| R3-GLOWA-6 | confirmed | limit | Крошки кончаются на первом «</nav>»: после вложенной <nav></nav> внутри nav.crumbs лишняя видимая ссылка проходит. Это противоречит новой шапке «ВСЕ ссылки внутри неё» |
| R3-GLOWA-7 | confirmed | prose | Сводно: проза и сообщения после раунда 2, а также проверенное без находок |
| R3-PROBY-1 | confirmed | defect | R2-INSTRUMENTY-1 на деле не закрыт: после Ctrl+C в консоли Windows сборка через npm exec возвращает код 1, а не 0xC000013A, поэтому код 130 недостижим |
| R3-PROBY-2 | confirmed | defect | Под `npm run tresc:proby` Ctrl+C сразу возвращает приглашение с кодом 1, а пробы идут в фоне с подменёнными файлами на диске |
| R3-PROBY-3 | confirmed | limit | Ctrl+C вне сборки не останавливает прогон: во время glowa он даёт ПЛОХО, между запусками пропадает; ветка 378-381 мертва |
| R3-PROBY-4 | confirmed | limit | N25 и N27 держат только min(1) из tekst(): у year и body[] откат к z.string().min(1) проходит все 33 пробы |
| R3-PROBY-5 | confirmed | prose | Сводно: проза и разбор аргументов проб после раунда 2 |
| R3-MARSHRUT-BRIFY-1 | confirmed | limit | В dist/ лежит мастер-JPEG ключевого арта MP3, на который не ссылается ни одна страница: утечка из бэклога 59 п. 6 повторилась. Находка вне диффа раунда 2 |
| R3-MARSHRUT-BRIFY-2 | confirmed | limit | Две причины link-list сразу, то есть тот самый сценарий R2-MARSHRUT-5(а), не держит ни одна проба: мутант «одна причина из двух» проходит N17 и N20 |
| R3-MARSHRUT-BRIFY-3 | confirmed | limit | Самопроверка брифа не держит «малый мастер по ширине записи»: мутанты `<= 3840` и `true` проходят 22/22 |
| R3-MARSHRUT-BRIFY-4 | confirmed | limit | N25 и N27 держат только пустую строку: у year и body[] половина tekst() с обрезкой краёв по-прежнему без проб |
| R3-MARSHRUT-BRIFY-5 | confirmed | prose | Сводно: проза и сообщения после раунда 2 (маршрут, бриф, «33/33»); проверенное и верное — в конце |
