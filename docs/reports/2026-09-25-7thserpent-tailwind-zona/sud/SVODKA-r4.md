# «Судью судят», раунд 4 — сессия 13 (зона Tailwind)

Судили: правки инструментов по раунду 3 — дифф 489df2d..445fbf3. Узкий раунд: закрывают ли правки находки раунда 3 и не внесли ли новых дефектов. Воркфлоу: 2 линзы (сверка CSS; проза и самопроверки маркеров и стилей), скептик на каждую (4 агента). После раунда инструменты заморожены на 445fbf3 — доклад, раздел 6.

**Итог:** находок 10; по итогу скептиков — дефектов 1, пределов 4, прозы 5, опровергнуто (итог «нет») 0.

## Линза «css»

git status линзы: ` M docs/BACKLOG.md ?? docs/reports/2026-09-25-7thserpent-tailwind-zona.md (вывод git -C D:/SEO/cloud/site-generator status --short в начале и в конце работы одинаков; обе строки не мои)`; скептика: `M docs/BACKLOG.md ?? docs/reports/2026-09-25-7thserpent-tailwind-zona.md`

**Закрыты правками прошлого раунда (проверено опытом):**

- r3-css-1 — закрыта. `--ushlo '.a,.b{color:red}'` и `'@font-face{font-family:x}'` теперь дают код 2. На копиях do-u1/po-u1 с остатком `@media print{.a,.b{color:red}}` новый даёт код 2, р3 (489df2d) — «сверено», код 0. Мутанты m3 и m5 (отказ по @ или по списку снят) роняют --selftest до 65/66, так что правка закреплена. Побочный ложный отказ — r4-css-1.
- r3-css-2 — закрыта. Одинаковые вставки C1, C2, C3, C4, C6, C7 и K5, K7, K11 в index обеих настоящих сборок: новый отказывает («селектор .lowercase есть в «после»»), р3 давал «сверено». Каждый итог совпадает с css-tree. C10 `.x/*,.lowercase,*/` теперь «сверено» (css-tree: [".x"]), прежний ложный отказ снят. На 4 настоящих листах selektory нового кода, р3 и css-tree совпадают: 0 расхождений на 669 прелюдиях. Мутант m1 (снятие комментариев убрано) даёт 61/66.
- r3-css-3 — закрыта. C5 `.x/*'*/{color:red}` из командной строки даёт «ВЕРДИКТ: сверено», код 0 (р3: стек «незакрытая строка», код 1), K5 с " тоже без исключения. Перебор 400 000 строк: на 74 083 блоках, принятых razobrat, selektory не бросила ни разу. Мутант, где selektory бросает, даёт строку «разбор селекторов «после» — …» и вердикт «отказ», остальные листы сверяются.
- r3-css-4 — закрыта (проза). Новый текст п. 5 совпадает с поведением: I1 :is(.lowercase), I2 :where(.lowercase), I3 .\6c owercase → сверено; F1 .x{.lowercase{color:red}} и F1б .x{.lowercase{}} → ложный отказ; F2 .x{&.lowercase{…}} → сверено.
- r3-css-5 — закрыта. С голым объявлением в слое обеих сборок (index `@layer utilities{color:red}`, первым или последним ребёнком слоя LinkList, два объявления) и --schet 20:19 новый даёт «сверено», слой 20→19; р3 отказывал (21→20, 22→21). Код 0 и в командной строке (do-k/po-k). Объявление только в «после» (S7) и ушедшее правило, превращённое в голое объявление (S8), по-прежнему ловит сравнение листьев («пришло листьев 1»). Мутант m2 даёт 65/66. Шапка и строка вывода называют голые объявления.
- r3-css-6 — закрыта (проза). Каждый пункт нового перечня кода 2 проверен из командной строки: неизвестный флаг, флаг дважды, флаг без значения, '' в --sloy, --ushlo `.a{}` и список, --schet 1000000:0, одна и три папки, несуществующая папка, файл вместо папки — везде код 2. Сообщение теперь «ждали <до>:<после> по 1–6 цифр». 999999:0 принимается (отказ, код 1). Других источников кода 2 в razobratArgumenty и в запуске нет.
- r3-css-7 — закрыта для сущностей: h4 (`href=/_astro/LinkList.DHeL5m7t&#46;css` без кавычек в обеих сборках) даёт «сверено», как теперь и сказано в шапке. Остаток той же фразы (процентное кодирование) — r4-css-4.

| id | находка | класс линзы | скептик | итог |
|---|---|---|---|---|
| r4-css-1 | Новый отказ pravilo() по подстроке «/*» даёт код 2 на законном одном правиле, у которого «/*» стоит в строке селектора атрибута. Так пишет и сам Tailwind | дефект | подтверждено | **дефект** |
| r4-css-2 | selektory() снимает комментарий без разделителя и склеивает соседние токены: `.lower/**/case` читается как `.lowercase`, выходит ложный отказ | предел | предел | **предел** |
| r4-css-3 | Шапка: --ushlo — «ровно один простой селектор», но принимаются составные и сложные селекторы и пустой. Для сложного вложенная или иначе расставленная запись в «после» проверке 5 не видна | проза | подтверждено | **проза** |
| r4-css-4 | Правка r3-css-7 исключила только сущности, но «устаревшее имя в любом синтаксисе ловит счёт имён байтами» неверно и для процентного кодирования | проза | подтверждено | **проза** |

### r4-css-1 — Новый отказ pravilo() по подстроке «/*» даёт код 2 на законном одном правиле, у которого «/*» стоит в строке селектора атрибута. Так пишет и сам Tailwind

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:237 (`p.includes('/*')`), шапка :26-27 и :36

**Суть:** Правка r3-css-1 ищет комментарий в прелюдии --ushlo простым `p.includes('/*')`, не пропуская строки. razobrat() и selektory() строки пропускают, так что `[data-a="/*"]` для них — один селектор без комментария. Однако pravilo() его отвергает (код 2) с неверным диагнозом «ждали правило с одним селектором без @, списка и комментариев». Шапка обещает код 2 только для «не одно правило с объявлениями и одним простым селектором», а `[data-a="/*"]` — именно простой селектор. Вход реалистичный: Tailwind 4.3.3 из node_modules репозитория после optimize(minify) печатает для кандидата `data-[x='/*']:underline` правило `.data-\[x\=\'\/\*\'\]\:underline[data-x="/*"]{text-decoration-line:underline}`. Так же выходят `aria-[label='/*']` и `[&[href^='/*']]`. Версия 489df2d такие правила принимала и сверяла верно. Направление безопасное: ложного «сверено» нет. Итог сессии не меняется, потому что прогон v4 идёт с --ushlo по умолчанию (.lowercase). Приём законного входа самопроверка не закрепляет. Как чинить: искать `/*` вне строк и экранирования. Мутант m6 с такой проверкой проходит --selftest 66/66, принимает `[data-a="/*"]` и правило Tailwind и по-прежнему отвергает `.a/*c*/{…}`.

**Доказательство:** Всё лежит в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/css/. (1) tw.mjs и tw.log: Tailwind из node_modules, compile + optimize(minify), для `data-[x='/*']:underline` даёт `@layer utilities{.data-\[x\=\'\/\*\'\]\:underline[data-x="/*"]{text-decoration-line:underline}}`. selektory даёт ровно 1 селектор, а razobratArgumenty даёт «код 2 (--ushlo: ждали правило с одним селектором без @, списка и комментариев…)». (2) opyty.log, раздел 1: код 2 у нового и «принято» у р3 для `[data-a="/*"]{color:red}`, `[data-a='/*']{color:red}`, `a[href^="/*"]{color:red}` и правила Tailwind. Раздел 2: A1 (правило ушло из слоя LinkList, остатка нет) — новый бросает исключение pravilo, р3 даёт «сверено». A2 (остаток `@media print{[data-a="/*"]{color:red}}`) — новый бросает исключение, р3 верно отказывает («селектор [data-a="/*"] есть в «после»»). (3) Командная строка (opyty.log, раздел 9). Для `node sverka-css.mjs do-a1 po-a1 --ushlo '[data-a="/*"]{color:red}'` новый даёт код 2, р3 — «ВЕРДИКТ: сверено», код 0. Для do-tw/po-tw с правилом Tailwind то же: новый — код 2, р3 — сверено, код 0. (4) Перебор opyty.log, раздел 7: среди 400 000 случайных прелюдий нашлось 1227 с одним селектором и без настоящего комментария, которые отвергаются только из-за includes('/*'). (5) opyty2.log, раздел Г: мутант m6 (`/*` ищется после вырезания строк и экранирования) проходит --selftest 66/66, код 0, принимает `[data-a="/*"]` и правило Tailwind и отвергает `.a/*c*/{color:red}`.

**Скептик — подтверждено, итог «дефект»:** Опровергнуть не удалось, находку повторил на своих опытах. Всё лежит в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/css-skeptik/: сценарий opyt.mjs, журнал opyt.log, версия 489df2d в staryj/. (1) Разбор аргументов. `[data-a="/*"]{color:red}`, `[data-a='/*']{color:red}`, `a[href^="/*"]{color:red}` и `[data-a="a/*b*/c"]{color:red}` в новой версии дают код 2 с сообщением «ждали правило с одним селектором без @, списка и комментариев». 489df2d все четыре принимает. selektory() нового кода даёт для каждого ровно один селектор, потому что строки он пропускает раньше, чем ищет комментарий. Отвергает только `p.includes('/*')` в строке :237. Диагноз ложный: ни @, ни списка, ни комментария во входе нет. Шапка :36 обещает код 2 лишь тогда, когда ушедшее не «одно правило с объявлениями и одним простым селектором», а `[data-a="/*"]` — простой селектор по Selectors 4. (2) Tailwind 4.3.3 из node_modules репозитория после compile и optimize(minify) печатает `.data-\[x\=\'\/\*\'\]\:underline[data-x="/*"]{text-decoration-line:underline}`. Так же выходят `aria-[label='/*']` и `[&[href^='/*']]`. Новая версия каждое такое правило отвергает кодом 2, 489df2d принимает. Правило составное, но код и так принимает составные `.a.b` (см. r4-css-3), значит по фактическому договору это законный вход из области минифицированного вывода. (3) Полная сверка на копиях ветки. A1: правило ушло из слоя LinkList, остатка нет. Новая версия бросает исключение, 489df2d даёт «сверено», слой 20→19. A2: остаток лежит в @media print обеих сборок. Новая версия бросает исключение, 489df2d верно отказывает («селектор … есть в «после»»). Старая версия этот вход сверяла правильно. Из командной строки (do-a1/po-a1): новая версия — код 2, 489df2d — «ВЕРДИКТ: сверено», код 0. (4) Мутант (mutant/sverka-css.mjs), где `/*` ищется после вырезания строк и экранирования, проходит --selftest 66/66 с кодом 0. Он принимает оба правила и по-прежнему отвергает `.a/*c*/{…}` и `.a,.b{…}`. Правка r3-css-1 свою находку закрывает: список и @ дают код 2. Но она внесла ложный отказ на входе, который инструмент по своей шапке обязан принимать, а это ровно искомый класс раунда. Направление безопасное, ложного «сверено» нет. Итог сессии не меняется: живой прогон main→ветка с --ushlo по умолчанию и с --schet 20:19 даёт «сверено», код 0.

### r4-css-2 — selektory() снимает комментарий без разделителя и склеивает соседние токены: `.lower/**/case` читается как `.lowercase`, выходит ложный отказ

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:149-154 (снятие `/*…*/` в selektory), шапка :23-24

**Суть:** Правка r3-css-2 вырезает комментарий из прелюдии, ничего не оставляя на его месте. По CSS Syntax комментарий разделяет токены: `.lower/**/case` — это `.lower` и отдельный ident `case`, то есть невалидный селектор, и браузер правило отбрасывает (css-tree читает его как `.lower case`). Новый selektory даёт `.lowercase`, и проверка 5 отказывает: «селектор .lowercase есть в «после»». Версия 489df2d давала «сверено». Ложное «сверено» не возникает, направление безопасное. Вход вне области разбора: в минифицированном выводе комментариев в прелюдиях нет. На настоящих листах selektory нового кода, р3 и css-tree совпадают. Буква шапки («текстом селектора после снятия комментариев») такое поведение формально описывает. Итог сессии не меняется. Если захотят чинить: на месте комментария оставлять разделитель или сравнивать токенами.

**Доказательство:** C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/css/opyty2.log, раздел Б, одинаковая вставка в index.xofYExtq.css обеих сборок (копии dist-main и dist-posle). B1 `.lower/**/case{color:red}`: selektory нового — [".lowercase"], css-tree — [[".lower case"]]; новый даёт «отказ — селектор .lowercase есть в «после» (блоков: 1)», р3 — «сверено». B3 `.x,.lower/**/case{…}`: то же. Для сравнения B2 `./**/lowercase` (класс, по токенам это .lowercase): новый отказывает, и это верно.

**Скептик — предел, итог «предел»:** Наблюдение верно (opyt.log, раздел 4). Вставки одинаковые в index.xofYExtq.css обеих сборок. `.lower/**/case{color:red}` и `.x,.lower/**/case{…}`: selektory нового кода даёт `.lowercase` и `[".x",".lowercase"]`, итог — отказ «селектор .lowercase есть в «после» (блоков: 1)». 489df2d даёт `.lower/**/case` и «сверено». Токенизатор css-tree: Delim ".", Ident "lower", Comment, Ident "case". Комментарий разрывает идентификатор, поэтому селектора .lowercase там нет. По грамматике селекторов идущий после класса ident без пробела делает правило невалидным. Отказ нового кода ложный, но направление безопасное: ложного «сверено» правка не внесла, склейка может только добавить совпадения. Вход лежит вне области разбора: в минифицированном выводе комментариев в прелюдиях нет. Шапка :23-24 («текстом селектора после снятия комментариев») такое поведение буквально описывает. Итог сессии не меняется. Попутное уточнение: для `./**/lowercase` css-tree 3.2.1 выдаёт ошибку разбора «Identifier is expected». Поэтому слова находки «B2 … это .lowercase, отказ верен» опираются на браузерную токенизацию, а не на css-tree. На класс итога это не влияет.

### r4-css-3 — Шапка: --ushlo — «ровно один простой селектор», но принимаются составные и сложные селекторы и пустой. Для сложного вложенная или иначе расставленная запись в «после» проверке 5 не видна

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:26-27 («ровно один простой селектор без комментариев»), :36 («одним простым селектором»), комментарий :235-236; код :237 (проверяется только отсутствие @, списка и /*)

**Суть:** Новая проза называет ушедшее правило «ровно один простой селектор», а код 2 обещает всему, что не «одно правило … с одним простым селектором». pravilo() принимает `.x .y{…}`, `.a.b{…}`, `.a:hover{…}`, `:is(.a,.b){…}` и даже `{color:red}` с пустым селектором. Для сложного селектора текстовое сравнение п. 5 пропускает остаток в «после», записанный вложенно (`.x{.y{color:red}}` значит `.x .y`) или с другим пробелом (`.x  .y`, `.x /*c*/ .y` → после снятия комментария «.x  .y»). Итог «сверено», код 0. Шапка из невидимых форм называет только :is/:where/экранирование, а вложенность упоминает лишь как ложный отказ. Поведение прежнее (р3 то же). Неточен текст, внесённый правкой. Итог сессии не меняется: .lowercase — простой селектор. Правка текста: «один селектор, не список» и оговорка, что для составного или сложного селектора вложенная и иначе расставленная запись не видна. Либо отвергать селекторы с комбинаторами и пустую прелюдию.

**Доказательство:** C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/css/opyty.log. Раздел 1: `.x .y`, `.a.b`, `.a:hover`, `:is(.a,.b)` и `{color:red}` принимаются новым и р3. Раздел 2: B1 (--ushlo `.x .y{color:red}` ушло из слоя LinkList, в index обеих сборок `.x{.y{color:red}}`) — «сверено» в обеих версиях. B2 (остаток `@media print{.x .y{…}}`) — отказ. B3 (`{color:red}`) — сверено. Раздел 9: node sverka-css.mjs do-b1 po-b1 --ushlo '.x .y{color:red}' → «ВЕРДИКТ: сверено», код 0. opyty2.log, раздел А: A1 вложенный — сверено; A2 `.x /*c*/ .y` (css-tree: ".x .y") — сверено; A3 `.x  .y` — сверено; A4 контроль в @media — отказ.

**Скептик — подтверждено, итог «проза»:** Подтверждено (opyt.log, раздел 5). Новая версия, как и 489df2d, принимает в --ushlo `.x .y`, `.a.b`, `.a:hover`, `:is(.a,.b)`, `.x>.y`, а также `{color:red}` и ` {color:red}` с пустым селектором. Шапка :26-27 («ровно один простой селектор») и :36 (код 2 для не «одного простого селектора») этому противоречат. Код проверяет только отсутствие @, списка и подстроки `/*`. Для сложного селектора `.x .y{color:red}`, ушедшего из слоя LinkList, остаток в «после» проверка 5 не видит, итог «сверено», слой 20→19. Так ведут себя B1 `.x{.y{color:red}}` (вложенный), B2 `.x  .y` (двойной пробел) и B3 `.x /*c*/ .y`: после снятия комментария остаётся `.x  .y`. То же с B6: ушёл `.a.b`, в «после» стоит `.b.a`. Контроли B4 (`@media print{.x .y{…}}`) и B5 (дословно наверху) дают отказ. Из командной строки B1 (do-b1/po-b1): «ВЕРДИКТ: сверено», код 0. Поведение не новое, 489df2d даёт то же. Неверен текст, внесённый правкой r3-css-1: он обещает сужение до простого селектора, которого код не делает. Из-за этого читатель считает закрытыми и формы, названные в находке. Итог сессии не меняется: .lowercase — простой селектор.

### r4-css-4 — Правка r3-css-7 исключила только сущности, но «устаревшее имя в любом синтаксисе ловит счёт имён байтами» неверно и для процентного кодирования

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:46-48 («устаревшее имя листа в любом синтаксисе, кроме имени, записанного сущностями (`&#46;`), ловит счёт имён байтами»)

**Суть:** Исключение для сущностей правка добавила верно, h4 это подтверждает. Но утверждение осталось слишком широким. Имя с процентным кодированием (`href=/_astro/LinkList%2EDHeL5m7t.css` или `…c%73s`, без кавычек) байтов старого имени не содержит. Проверка ссылок его тоже не видит, потому что href без кавычек. Если такой HTML одинаков в обеих сборках, итог «сверено», хотя HTML «после» ведёт на старый лист. По RFC 3986 `%2E` и `.` эквивалентны. Сервер приёмки (scratchpad/tz/server.mjs: `decodeURIComponent(new URL(req.url…).pathname)`) отдаст по такому адресу файл _astro/LinkList.DHeL5m7t.css. Поведение прежнее (р3 то же), неточна проза, внесённая правкой. Astro так не пишет: в HTML обеих сборок ссылки только `<link rel="stylesheet" href="/_astro/…css">`. Итог сессии не меняется. Правка текста: «кроме имени, записанного сущностями или процентным кодированием». Или короче: счёт ловит только дословное имя.

**Доказательство:** C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/css/opyty.log, раздел 9. h1: index.html и 404/index.html обеих сборок содержат `<link rel="stylesheet" href=/_astro/LinkList%2EDHeL5m7t.css>` → «ВЕРДИКТ: сверено», код 0; staryj (489df2d) тоже даёт сверено. h3 `…DHeL5m7t.c%73s` → сверено, код 0. h2 (то же в кавычках) → отказ «ведёт мимо листов сборки «после»». h4 `&#46;` → сверено, как и названо в шапке. opyty2.log, раздел В: href /_astro/LinkList%2EDHeL5m7t.css → путь запроса WHATWG URL /_astro/LinkList%2EDHeL5m7t.css → файл сервера приёмки /_astro/LinkList.DHeL5m7t.css (для c%73s то же).

**Скептик — подтверждено, итог «проза»:** Подтверждено (opyt.log, раздел 6). Одна и та же вставка сделана в index.html и 404/index.html обеих сборок (main → ветка). h1 `<link rel="stylesheet" href=/_astro/LinkList%2EDHeL5m7t.css>` и h3 `…DHeL5m7t.c%73s` дают «сверено» в новой версии и в 489df2d. Из командной строки h1 (do-h1/po-h1): «ВЕРДИКТ: сверено», код 0. Сервер приёмки делает decodeURIComponent(new URL(…).pathname) и сводит оба адреса к _astro/LinkList.DHeL5m7t.css. Этого файла в «после» нет, в «до» он есть. Значит, HTML «после» ссылается на старый лист, а счёт имён байтами его не ловит: байтов `LinkList.DHeL5m7t.css` во вставке нет. Контроли. h2 (то же в кавычках) — отказ «ведёт мимо листов». h5 (дословное старое имя без кавычек) — отказ «осталось старое имя». h4 `&#46;` — «сверено», это названо в шапке. Правка r3-css-7 сделала ровно то, о чём просила находка, но формула «в любом синтаксисе, кроме имени, записанного сущностями» по-прежнему слишком широка: процентное кодирование тоже мимо. Поведение прежнее, неверна проза, внесённая правкой. Astro так не пишет, итог сессии не меняется. Точнее было бы так: «счёт ловит только дословное имя».

**Проверено — чисто:**

- Самопроверка: node sverka-css.mjs --selftest — 66/66, код 0.
- Живой прогон v4 на копиях dist-main и dist-posle (sud-r4/css/do0, po0): вывод с дописанной строкой «sverka v4 exit=0» побайтно равен zamery/v4/sverka-css.txt. JSON с --json побайтно равен zamery/v4/sverka-css.json. С --schet 20:19 — «сверено», код 0.
- pravilo(): законные одиночные правила принимаются — `:is(.a,.b)`, `.a:not(.b,.c)`, `[data-a="x,y"]`, `[data-a="*/"]`, `.a\,b`, `.w-1\/2`, `.a\/\*b`, псевдоэлементы `.a::before{content:"/*"}` и `.a::after{…}`, `.content-\[\'\/\*\'\]{--tw-content:'/*';…}` («/*» в теле не мешает). Отвергаются кодом 2 список, @-правило, комментарий в прелюдии (`.a/*c*/`, `.a /*c*/`), `/*c*/.a{…}` и `.a,{…}`. Исключение — строка с «/*» в прелюдии (r4-css-1).
- selektory(), строки и комментарии: «/*» и «*/» внутри строк атрибутов не принимаются за комментарий. Проверены K1 `[data-a="/*"],.lowercase`, K3, K4 `[data-a="/*"],.lowercase,[data-b="*/"]`, K13 в одинарных кавычках, K16 комментарий и затем строка с «*/», K17 `\"` внутри строки: везде верный отказ, в согласии с css-tree. K2 `[data-a="/*"]{…}` → сверено без исключения. Составные C9 `.x/*,*/.lowercase`, K9, K10 и потомок K12 `.x /*c*/ .lowercase` → сверено, css-tree согласен. Комментарии внутри :is() (K6, K7) не ломают счёт скобок.
- Незакрытый комментарий в прелюдии до selektory() не доходит: razobrat бросает раньше и в sverit (строка «разбор — …»), и в pravilo (код 2). Перебор 400 000 строк не нашёл ни одной прелюдии, принятой razobrat, на которой selektory бросила бы. Значит, новый catch в sverit — защитный. На мутанте он даёт отказ со строкой и не прерывает сверку остальных листов. Самопроверка его не закрепляет (мутант m7 без catch — 66/66), но на настоящем коде закреплять нечего.
- Счёт слоя без голых объявлений ложного «сверено» не открывает: любое добавленное или убранное объявление в слое видит сравнение листьев (S7, S8 → «пришло листьев 1», побайтно не равен). На настоящих листах счёт нового кода равен р3: LinkList 20 и 19, index 0.
- Вложенное `.x{/*c*/.lowercase{…}}` (K8) даёт отказ. Это ложный отказ для вложенности, и он назван в шапке.
- Новые случаи самопроверки различают правки: мутанты m1 (без снятия комментариев) — 61/66, m2 (счёт с объявлениями) — 65/66, m3, m4 и m5 (без отказов pravilo по @, /* и списку) — 65/66, во всех случаях код 1.
- Окончания строк: sverka-css.mjs, markery.mjs и stili-gen.mjs — i/lf w/lf, attr/-text.
- Репозиторий я не менял: ни одного созданного, изменённого или удалённого файла. Сборку и preview не запускал. Копии, сценарии (opyty.mjs, opyty2.mjs, tw.mjs), журналы (opyty.log, opyty2.log, tw.log, zhivoj.txt, v4-povtor.json), мутанты (mut/, mutant/) и версия 489df2d (staryj/) лежат в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/css/.

## Линза «proza»

git status линзы: `M docs/BACKLOG.md ?? docs/reports/2026-09-25-7thserpent-tailwind-zona.md`; скептика: ` M docs/BACKLOG.md ?? docs/reports/2026-09-25-7thserpent-tailwind-zona.md`

**Закрыты правками прошлого раунда (проверено опытом):**

- r3-css-1 — --ushlo '.a,.b{color:red}', '@font-face{font-family:x}' и '.a/*c*/{color:red}' дают код 2 (opyty-sverka.log, раздел A). Каждое из трёх условий pravilo закреплено самопроверкой: мутанты без @, без '/*' и без проверки списка дают 65/66 (mutacii-sverka.log).
- r3-css-2 — вставки C1–C4, C6, C7 в index обеих настоящих сборок дают отказ «селектор .lowercase есть в «после» (блоков: 1)», а прежний ложный отказ C10 теперь «сверено». Мутант без снятия комментариев в selektory даёт 61/66: падают Н35–Н38 и П7.
- r3-css-3 — C5 `.x/*'*/{color:red}` в обеих сборках даёт «сверено», исключения нет, П7 это закрепляет. catch вокруг blokovSSelektorom недостижим: перебор 506 442 прелюдий, принятых razobrat, — selektory не бросил ни разу.
- r3-css-4 — новая проза п. 5 совпадает с опытом на настоящих сборках: I1 :is(.lowercase), I2 :where(.lowercase) и I3 .\6c owercase — «сверено», F1 .x{.lowercase{}} — отказ, F2 .x{&.lowercase{}} — «сверено».
- r3-css-5 — S3 `@layer utilities{color:red}` в обеих сборках с --schet 20:19 даёт «сверено». Прочие формы (S1, S3б, S4, S4б, S5, S7) считаются ровно так, как пишет п. 3. Строка вывода совпадает с zamery/v4/sverka-css.txt:4. Мутант со старым фильтром даёт 65/66 (падает П8).
- r3-css-6 — перечень кода 2 в шапке теперь полон: флаг дважды, (''), «по 1–6 цифр», не две папки. '1000000:0' и '999999:1000000' дают код 2 с «по 1–6 цифр», а ' ' принимается, что согласно с «('')». Закрепление предела у второго числа — r4-proza-6.
- r3-css-7 — исключение для сущностей названо, и h-a (&#46; без кавычек в обеих сборках → «сверено») теперь названный предел. Но та же фраза неверна для %-кодирования — r4-proza-5.

| id | находка | класс линзы | скептик | итог |
|---|---|---|---|---|
| r4-proza-1 | Новый случай самопроверки markery закрепляет границу 999999 только у первого числа списка: хвост по-прежнему можно ослабить, и --selftest останется 26/26 | предел | предел | **предел** |
| r4-proza-2 | stili-gen, новый абзац о CORS: «без CORS (нет crossorigin или ACAO) — −1 и ложный отказ» неверно для ветки «crossorigin есть, CORS не прошёл»: там отказ верный | проза | подтверждено | **проза** |
| r4-proza-3 | Шапка sverka-css: «ровно один простой селектор» в п. 5 и в перечне кода 2 — код принимает составные и сложные селекторы | проза | подтверждено | **проза** |
| r4-proza-4 | pravilo ищет «/*» в сыром тексте прелюдии, а не комментарий: простой селектор атрибута со строкой «/*» получает код 2 | предел | предел | **предел** |
| r4-proza-5 | ПРЕДЕЛЫ ссылок sverka-css: «в любом синтаксисе, кроме сущностей» по-прежнему неверно — %-кодированное имя тоже проходит мимо счёта имён | проза | подтверждено | **проза** |
| r4-proza-6 | Самопроверка sverka-css не закрепляет две части правок раунда 3: предел 6 цифр у второго числа --schet и счёт инструкций в слое | предел | предел | **предел** |

### r4-proza-1 — Новый случай самопроверки markery закрепляет границу 999999 только у первого числа списка: хвост по-прежнему можно ослабить, и --selftest останется 26/26

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/markery.mjs:121 (новый случай argi), регулярка :70

**Суть:** Правка r3-markery-1 добавила один случай: 1000000 одиночным значением. Он ловит ослабление головы списка или всей регулярки. Скептик раунда 3 показал, что дыра шире, и прямо просил второй случай — неверный хвост списка (['d','--est','71','--net','73,1000000']). Этот случай не добавлен. Мутанты, ослабляющие только хвост `(,…)*`, проходят самопроверку 26/26, и два из них дают ложное «все там» на утёкшем маркере. Сам инструмент верен: на тех же входах код 2. Поэтому это пробел в охране от регрессии, а не ошибка суждения. Итог сессии не меняется. Правка: добавить в argi ['d','--est','71','--net','73,1000000'] → false и ['d','--est','71','--net','73,073'] (или 73,9007199254740993) → false.

**Доказательство:** Сценарий C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza/mutacii-markery.mjs, журнал mutacii-markery.log там же; мутанты — mut/<имя>/markery.mjs, собраны из копии 445fbf3, побайтно равной рабочему файлу. Голова закреплена: m7-oba, m15-oba и m7-golova дают 25/26, код 1, и падает ровно новый случай. Те же m7-oba и m15-oba без нового случая дают 25/25, код 0. Хвост не закреплён, все дают 26/26, код 0: m7-hvost `(,[1-9]\d{0,6})*`, m15-hvost, mhvost-lyuboj `(,\d+)*`, mhvost-bez-nulya `(,[1-9]\d*)*`, mhvost-nol-dopuschen `(,\d{1,6})*`. Фикстура fikstury/utechka-2p53 (в слое утечка `.z-\[9007199254740993\]{z-index:9007199254740993}`): mhvost-lyuboj с --est 71 --net 73,9007199254740993 печатает «да z-[9007199254740992] … ИТОГ: все маркеры там, где ждали», код 0; настоящий инструмент — код 2. Фикстура utechka-073 (утечка `.z-\[073\]{z-index:073}`): mhvost-nol-dopuschen с --net 74,073 ищет z-[73] и даёт «все там», код 0; настоящий — код 2.

**Скептик — предел, итог «предел»:** Опровергнуть не удалось, всё воспроизведено на своих мутантах. Сценарий C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza-skeptik/mut-markery.mjs, журнал mut-markery.log. Мутанты собраны из рабочего markery.mjs (445fbf3). Голова закреплена: golova7, oba7 и oba15 дают 25/26, код 1, и падает ровно новый случай «(r3-markery-1) семь цифр»; без этого случая они дают 25/25, код 0. Хвост не закреплён: hvost7, hvost15, hvost-lyuboj `(,\d+)*`, hvost-bez-nulya `(,[1-9]\d*)*` и hvost-nol `(,\d{1,6})*` — все 26/26, код 0. Ложное «все там» на утечке: фикстура `.z-\[9007199254740993\]{z-index:9007199254740993}` с `--net 73,9007199254740993` — hvost-lyuboj и hvost-bez-nulya печатают «да z-[9007199254740992] … ИТОГ: все маркеры там, где ждали», код 0. Фикстура `.z-\[073\]{z-index:073}` с `--net 74,073` — hvost-lyuboj и hvost-nol дают «все там», код 0. Настоящий инструмент на обоих входах даёт код 2. Сама правка r3-markery-1 закрывает находку раунда 3 в её собственной формулировке (1000000 одиночным значением). Остаток — не принятое расширение скептика раунда 3 (хвост списка). Инструмент нигде не выносит ложного суждения, итог сессии не меняется, поэтому «предел». Уточнение к правке (pravka-markery.log): вариант в скобках «(или 73,9007199254740993)» не равносилен 73,073. Мутант hvost-nol отвергает и 73,1000000, и 73,9007199254740993, так что пара «73,1000000 + 73,9007199254740993» пропускает его, а на утечке 073 он даёт ложное «все там». Нужны именно два случая: 73,1000000 и 73,073.

### r4-proza-2 — stili-gen, новый абзац о CORS: «без CORS (нет crossorigin или ACAO) — −1 и ложный отказ» неверно для ветки «crossorigin есть, CORS не прошёл»: там отказ верный

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/stili-gen.mjs:46-48

**Суть:** Правка r3-stili-1 взяла текст находки, а уточнение скептика раунда 3 не учла. В новой скобке «без CORS (нет crossorigin у ссылки или Access-Control-Allow-Origin в ответе)» слиты две ветки. (1) Нет crossorigin: лист загружен и применён, но cssRules бросает SecurityError. Выходит −1 и ложный отказ — тут текст верен. (2) crossorigin есть, CORS не прошёл: нет ACAO, или use-credentials при ACAO *. Chrome блокирует загрузку, лист не применяется, выходит −1 и СТОП «лист не загружен» — и этот отказ верный, а не ложный. Кроме того, скобка определяет «без CORS» неполно. В случае use-credentials + ACAO * есть и crossorigin, и ACAO, а CORS всё равно не проходит. По документации это сходится. CSSOM: геттер cssRules бросает SecurityError при снятом флаге origin-clean, а флаг ставится только для ответа CORS-same-origin. Chrome, blink-dev «CSS: Use the response URL for base URL and response type for security decisions»: «Consider stylesheets that failed to load to be cross-origin… accessing styleSheet.rules throws a SecurityError if the load failed». Хвост «с CORS правила читаются, и остановки нет» верен. Исход безопасный: ложного «сверено» нет. crossorigin в HTML обеих сборок встречается 0 раз, итог сессии не меняется. Правка текста — формулировка скептика раунда 3: «без crossorigin — −1 и ложный отказ (лист применён, правила не читаются); с crossorigin при неудавшемся CORS лист не загружается — −1 и верный отказ; с crossorigin и удачным CORS правила читаются, остановки нет».

**Доказательство:** Журнал скептика раунда 3 C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r3/stili-skeptik/opyt-dva-servera.log, настоящий Chrome, два сервера, без page.route. B (crossorigin=anonymous, без ACAO): «СТОП … (правил -1)», h1 «"Times New Roman" normal», фон rgba(0, 0, 0, 0), шрифтов 0, в консоли «blocked by CORS policy … net::ERR_FAILED»: лист не применён, отказ верный. E (use-credentials, ACAO *): то же, лист не применён, хотя в ответе есть ACAO. C и D (без crossorigin): −1 при применённом листе (Bodoni Moda 2.2px, rgb(9, 12, 17)) — ложный отказ, как в тексте. A (crossorigin + ACAO *): pravil 176, «итог сверено». Спецификация: drafts.csswg.org/cssom/#dom-cssstylesheet-cssrules — «If the origin-clean flag is unset, throw a SecurityError». Chrome: groups.google.com/a/chromium.org/g/blink-dev/c/7OSy00oxVpk, изменение №3 о листах, которые не загрузились.

**Скептик — подтверждено, итог «проза»:** Опровергнуть не удалось. Дифф 489df2d..445fbf3 меняет в stili-gen.mjs только шапку :46-48. Код :94-101 (`try { l.sheet ? l.sheet.cssRules.length : -1 } catch → -1`, СТОП при pravil ≤ 0) не тронут, поэтому опыт скептика раунда 3 на генераторе 489df2d применим к 445fbf3. Журнал C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r3/stili-skeptik/opyt-dva-servera.log прочитан, и его сценарий opyt-dva-servera.mjs сверен. Там настоящий Chrome и два http-сервера, page.route не используется. Ветка B (crossorigin=anonymous, без ACAO): «СТОП … лист не загружен или пуст … (правил -1)», h1 «Times New Roman» normal, фон rgba(0, 0, 0, 0), шрифтов 0, в консоли «blocked by CORS policy … net::ERR_FAILED». Лист не применён, СТОП говорит правду, отказ верный. Новый текст шапки («без CORS (нет crossorigin у ссылки или ACAO в ответе) даёт −1 правил и ложный отказ») называет этот отказ ложным, и это неверно. Ветка E (use-credentials, ACAO *): есть и crossorigin, и ACAO, значит по определению из скобки это «с CORS», и шапка обещает «правила читаются, и остановки нет». На деле −1 и СТОП, лист не применён: скобка определяет «без CORS» неполно. C и D (без crossorigin) — −1 при применённом листе, ложный отказ; тут текст верен. A (crossorigin + ACAO *) — 176 правил, «сверено»; тут верен хвост. Уточнение скептика раунда 3 правкой не учтено. Исход безопасный, crossorigin в HTML сборок нет, итог не меняется. Это неверный текст в строках, которые изменил дифф, — «проза».

### r4-proza-3 — Шапка sverka-css: «ровно один простой селектор» в п. 5 и в перечне кода 2 — код принимает составные и сложные селекторы

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:26-27 и :36 (шапка), :235-238 (комментарий и проверка в pravilo)

**Суть:** В терминах Selectors 4 простой селектор (simple selector) — это один класс, тип, атрибут или псевдокласс. Шапка обещает код 2 для --ushlo, если там «не … одним простым селектором». pravilo проверяет другое: прелюдия не начинается с @, в ней нет «/*», и selektory даёт один элемент. Поэтому принимаются составные `.a.b`, `.a:hover`, сложные `.x .y`, `.a>.b` и `:is(.a,.b)`. Сообщение об ошибке точнее шапки: «с одним селектором без @, списка и комментариев». Исход безопасный: проверка 5 сравнивает текст и для таких селекторов работает так же, итог сессии не меняется. Правка текста: «ровно один селектор (не список)».

**Доказательство:** C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza/opyty-sverka.mjs, раздел A, журнал opyty-sverka.log. razobratArgumenty(['a','b','--ushlo',X]) даёт «принято» для X = '.x .y{color:red}', '.a.b{color:red}', '.a:hover{color:red}', '.a>.b{color:red}', ':is(.a,.b){color:red}', '*{color:red}'. Код 2 получают только '.a,.b{…}', '@font-face{…}' и '.a/*c*/{…}'.

**Скептик — подтверждено, итог «проза»:** Опровергнуть не удалось (C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza-skeptik/opyty-sverka.mjs, раздел A, журнал opyty-sverka.log). razobratArgumenty(['a','b','--ushlo',X]) принимает составные и сложные селекторы: '.a.b{…}', '.a:hover{…}', '.x .y{…}', '.a>.b{…}', ':is(.a,.b){…}', '.a::before{…}', а также 'div{…}' и '*{…}'. Код 2 дают только список, @-правило и комментарий. Слова «простой селектор» стоят в трёх местах: шапка :26 («ровно один простой селектор»), перечень кода 2 :36 («одним простым селектором») и комментарий кода :236. В терминах Selectors 4 это термин: один класс, тип, атрибут или псевдокласс. Шапка в других местах пользуется терминами CSS точно, поэтому довод «простой здесь значит просто не список» слаб, тем более что :36 такого пояснения не даёт. Сообщение кода («с одним селектором») точнее шапки. Исход безопасный. Раздел C: для '.x .y' и '.a.b' проверка 5 работает текстом: остаток в «после» даёт отказ «селектор … есть в «после»», без остатка — «сверено». Итог сессии не меняется. Правка текста из находки — «ровно один селектор (не список)» — верна.

### r4-proza-4 — pravilo ищет «/*» в сыром тексте прелюдии, а не комментарий: простой селектор атрибута со строкой «/*» получает код 2

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:237 (`p.includes('/*')`); шапка :26, «без комментариев»

**Суть:** Правка r3-css-1 запрещает комментарии в --ushlo проверкой `p.includes('/*')`, а она не знает строк. Возьмём `[data-a="/*"]{color:red}` или `[data-a='x/*y']{color:red}`. Это один простой селектор, комментариев в нём нет: selektory это видит и возвращает один селектор. Но инструмент отвечает кодом 2 «ждали правило с одним селектором без @, списка и комментариев». До раунда 3 такой вход принимался. Это новый ложный отказ на входе, который по шапке допустим. Направление безопасное: вердикта нет, ложного «сверено» нет. Вход надуманный: Tailwind экранирует «/» и «*» в именах классов (`\/\*`), а для сессии 13 --ushlo — умолчание `.lowercase{…}`. Итог не меняется. Правка по желанию: искать комментарий вне строк, например `selektory(p)[0] !== p`, ведь selektory снимает комментарии и строки не трогает.

**Доказательство:** opyty-sverka.log, раздел A (C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza/). "[data-a=\"/*\"]{color:red}" → «код 2: --ushlo: ждали правило с одним селектором без @, списка и комментариев»; "[data-a='x/*y']{color:red}" → тот же код 2; при этом selektory('[data-a="/*"]') = ["[data-a=\"/*\"]"], один селектор. Для сравнения '[data-a=","]{color:red}' → принято.

**Скептик — предел, итог «предел»:** Наблюдение верно, опровергнуть не удалось (opyty-sverka.log, раздел A, C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza-skeptik/). razobrat отдаёт прелюдию '[data-a="/*"]' целой: кавычку он разбирает раньше, чем «/*» внутри неё. selektory даёт один селектор, но `p.includes('/*')` (:237) отвечает кодом 2 «…без @, списка и комментариев». То же для "[data-a='x/*y']". Для сравнения '[data-a="*/"]', '[data-a=","]', 'a[href^="http://"]', '.w-1\/2' и '.a\/\*' приняты. Это новый ложный отказ: по шапке :26 вход (один селектор атрибута без комментариев) допустим, до 445fbf3 он принимался. Класс «предел», а не «дефект», по прецеденту r3-css-3, r2-markery-3 и r2-stili-7. Отказ громкий (код 2 с сообщением), ложного «сверено» и вердикта нет. Вход надуманный: Tailwind экранирует «/» и «*», а в сессии --ushlo — умолчание .lowercase. Правка находки проверена опытом. Условие `selektory(p)[0] === p` принимает '[data-a="/*"]{…}' и '.a{…}' и отвергает '.a/*c*/', '.a /*c*/' и ':is(.a/*c*/)'. Мутант без `p.includes` (mut-sverka.log, bez-includes) даёт 65/66 — падает случай «(r3-css-1) комментарий в прелюдии», так что замену условия самопроверка сторожит.

### r4-proza-5 — ПРЕДЕЛЫ ссылок sverka-css: «в любом синтаксисе, кроме сущностей» по-прежнему неверно — %-кодированное имя тоже проходит мимо счёта имён

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:46-48

**Суть:** Правка r3-css-7 назвала одно исключение — сущности. Та же фраза обещает, что всё остальное ловит счёт имён байтами. Но счёт ищет только точные байты «LinkList.DHeL5m7t.css», и любое кодирование имени проходит мимо. Пример — процентное: `%2E` вместо точки или `%4C` вместо «L». Пусть в обеих сборках стоит устаревший href без кавычек: `href=/_astro/LinkList.DHeL5m7t%2Ecss`. Тогда «после» ссылается на лист, которого нет, а итог — «сверено». Это та же форма, что u4 раунда 2 и h-a раунда 3, только с другим кодированием. Astro так не пишет, итог сессии не меняется. Правка текста: «…ловит счёт имён байтами, если имя записано теми же байтами (сущности и %-кодирование — нет)». Попутно: счёт идёт только по *.html. Это уже сказано в п. 6 («HTML — в каждом файле…»), поэтому отдельной находкой не считаю.

**Доказательство:** opyty-sverka.log, раздел D (C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza/). Настоящие dist-main и dist-posle читались в память. В index.html и 404/index.html «до» ссылка заменена, HTML «после» равен HTML «до». h-p `href=/_astro/LinkList.DHeL5m7t%2Ecss` → «сверено (слой 20→19)», файла _astro/LinkList.DHeL5m7t.css в «после» нет (false). h-p2 `href=/_astro/%4CinkList.DHeL5m7t.css` → «сверено». Контроли: h-a (сущность &#46;) → «сверено», это названный теперь предел; h-c (без кавычек, как есть) → отказ «в «после» осталось старое имя листа»; h-pq (%2E в кавычках) → отказ «ведёт мимо листов сборки «после»». opyt-js.log: `_astro/x.js` со старым именем в обеих сборках → «сверено», это покрывает п. 6.

**Скептик — подтверждено, итог «проза»:** Опровергнуть не удалось (opyty-sverka.log, раздел D, C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza-skeptik/). Настоящие dist-main и dist-posle читались в память. Во всех HTML «до» ссылка на LinkList.DHeL5m7t.css заменена, HTML «после» равен HTML «до». Три варианта дают «сверено (слой 20→19)», хотя файла _astro/LinkList.DHeL5m7t.css в «после» нет: p1 `href=/_astro/LinkList.DHeL5m7t%2Ecss`, p2 `href=/_astro/%4CinkList.DHeL5m7t.css`, p3 `%2e`. Адрес достижим: по WHATWG URL путь запроса сохраняет %2E, а server.mjs приёмки делает decodeURIComponent и ищет /_astro/LinkList.DHeL5m7t.css. Значит, «после» ссылается на отсутствующий старый лист. Контроли: k1 (без кавычек, имя как есть) — отказ «осталось старое имя»; k2 (%2E в кавычках) — отказ «ведёт мимо листов». Новая фраза :46-48 («в любом синтаксисе, кроме имени, записанного сущностями, ловит счёт имён байтами») неверна и для %-кодирования. Та же форма, что r3-css-7, признанная «прозой». Правка r3-css-7 закрыла только названный пример, обобщение осталось неверным. Astro так не пишет, итог сессии не меняется.

### r4-proza-6 — Самопроверка sverka-css не закрепляет две части правок раунда 3: предел 6 цифр у второго числа --schet и счёт инструкций в слое

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:505-506 (случаи --schet), :137 (фильтр blok\|\|instr), :465 (П8)

**Суть:** Это тот же класс, что r3-markery-1. Случай «(r3-css-6) --schet из 7 цифр» проверяет только первое число ('1000000:0'). Регулярку можно ослабить до `^(\d{1,6}):(\d{1,7})$`, и самопроверка останется 66/66. Новый фильтр счёта слоя закреплён только со стороны объявлений (П8) и комментариев (П6). В слое образца нет ни одной инструкции, поэтому фильтр `x.tip === 'blok'` без instr тоже проходит 66/66, хотя шапка п. 3 обещает считать инструкцию. Настоящий инструмент верен в обоих местах. В реальном слое инструкций нет, итог сессии не меняется. Правка по желанию: в argi — ['a','b','--schet','999999:1000000'] → false; в sluchai — вставка `@layer q;` в слой обеих сборок с schet [7,6] → «сверено». catch вокруг blokovSSelektorom самопроверкой тоже не закреплён (его удаление даёт 66/66), но это не пробел: перебор показывает, что ветка недостижима.

**Доказательство:** C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza/mutacii-sverka.mjs, журнал mutacii-sverka.log. s-schet-7-tolko-posle → 66/66, код 0; s-sloy-bez-instr → 66/66, код 0. Для сравнения s-schet-7 → 65/66 и s-sloy-staryj → 65/66 (падает П8). Настоящий инструмент (opyty-sverka.log, разделы C и E): '999999:1000000' → код 2 «по 1–6 цифр»; S4 `@layer utilities{@layer q;}` в index обеих сборок с --schet 21:20 → «сверено (слой 21→20)». Недостижимость catch — fuzz-selektory.log: 1 000 000 строк, 252 296 разобрано, 506 442 прелюдии, selektory не бросил ни разу.

**Скептик — предел, итог «предел»:** Воспроизведено на своих мутантах (C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza-skeptik/mut-sverka.mjs, журнал mut-sverka.log). Без закрепления, 66/66 и код 0: schet-7-posle `^(\d{1,6}):(\d{1,7})$`, sloy-bez-instr (`x.tip === 'blok'` без instr) и bez-catch (снят try/catch вокруг blokovSSelektorom). С закреплением: schet-7-do — 65/66, падает «(r3-css-6) --schet из 7 цифр»; sloy-staryj (`!== 'komm'`) — 65/66, падает П8. Настоящий инструмент верен (opyty-sverka.log, раздел E): '999999:1000000' и '1000000:0' дают код 2 «по 1–6 цифр». Вставка `@layer utilities{@layer q;}` в index обеих сборок с --schet 21:20 даёт «сверено (слой 21→20)», то есть инструкция считается. sloyUtilit на `@layer utilities{@layer q;.a{b:c}color:red;/*k*/}` даёт pravil 2, как обещает шапка п. 3. Catch при разборе прелюдий, которые даёт razobrat, недостижим: у selektory тот же порядок разбора (`\`, комментарий, кавычки), и razobrat уже бросил бы на незакрытой строке или комментарии. Это пробел в охране от регрессии того же класса, что r3-markery-1. Ложного суждения инструмента нет, итог сессии не меняется, поэтому «предел».

**Проверено — чисто:**

- Самопроверки из репозитория: markery.mjs --selftest — 26/26, код 0; sverka-css.mjs --selftest — 66/66, код 0. Копии из 445fbf3 побайтно равны рабочим файлам (cmp). У всех трёх файлов окончания i/lf w/lf.
- markery, новый случай «(r3-markery-1) семь цифр — за границей» закрепляет границу для первого числа. m7-oba, m15-oba и m7-golova дают 25/26, код 1, и падает ровно этот случай. Без него те же мутанты дают 25/25. Старая регулярка раунда 1 — 21/26. Хвост списка — r4-proza-1.
- sverka, мутации правок раунда 3 (mutacii-sverka.log). Комментарий кладётся в текст — 64/66; i = j + 2 — 63/66; --schet \d{1,7} — 65/66, \d{1,5} — 65/66. Мутант «комментарий → пробел» даёт 66/66, но на реальных входах он равносилен правке: различие только в надуманном .lower/**/case.
- Живой прогон в памяти на dist-main и dist-posle: «сверено (слой 20→19)», с --schet 20:19 тоже «сверено». Строка слоя из CLI дословно совпадает с zamery/v4/sverka-css.txt:4.
- Шапка п. 3 против кода. sloyUtilit считает только blok и instr: для «@layer utilities{a:b;.c{d:e}@layer f;/*g*/@media x{.h{i:j}}}» — 3. Голое объявление и комментарий не считаются, инструкция с «;» и без «;», @supports и пустой блок считаются.
- Шапка п. 5 против кода, вставки в index обеих настоящих сборок. C1–C4, C6, C7, F1 — отказ; C5, C9, C10, I1–I3, F2 — «сверено». Всё как написано. K1 `.lower/**/case` даёт отказ: это прямо следует из «после снятия комментариев», направление безопасное, а в CSS такой селектор невалиден.
- Коды возврата против шапки. Флаг дважды, одна или три папки, «-schet» с одним дефисом (идёт как лишняя папка), '' у любого флага, арабские цифры в --schet — код 2. '999999:999999' принимается. «Иначе — отказ и код 1» не нарушено: новый catch превращает ошибку разбора селекторов в строку отказа.
- stili-gen, хвост нового абзаца: «с CORS правила читаются, и остановки нет» и «без crossorigin — −1 и ложный отказ» верны. Опыты A, C, D скептика раунда 3 (opyt-dva-servera.log) совпадают с CSSOM (cssRules бросает SecurityError при снятом origin-clean) и с записью Chrome blink-dev о листах, которые не загрузились. Неточна только ветка «crossorigin есть, CORS не прошёл» — r4-proza-2.
- Репозиторий я не менял: git status до и после опытов одинаков, там только чужие изменения (docs/BACKLOG.md и файл доклада). Все копии, мутанты, фикстуры, сценарии и журналы лежат в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r4/proza/: kopii/, mut/, mut-sverka/, fikstury/, mutacii-markery.mjs/.log, mutacii-sverka.mjs/.log, opyty-sverka.mjs/.log, opyt-js.mjs/.log, fuzz-selektory.mjs/.log. Сборку, preview и браузер не запускал.

