# BACKLOG

Что отложено сознательно: работа, у которой понятен смысл, но нет условия для
старта. Каждый пункт — с номером, датой и статусом; в статусе стоит не «когда»,
а **чего именно ждём**. Формат взят из `../seo-pipeline/kb/backlog.md`.

**Главное правило файла: пункт не начинается раньше контракта, на котором он
висит.** Прецедент — в разобранном источнике: у `../1weekinvr.com` поле `type`
принимает 14 свободных значений, и не читает его ни один скрипт; соответствие
«тип → набор блоков» так и осталось в голове автора (`docs/REUSE.md` §3.3, п.1).
Заведённое раньше контракта наполняется догадками о контракте.

| # | Что отложено | Ждём | Статус |
|---|---|---|---|
| 1 | **Режим автоматического анализа конкурентов** — два прохода по одному корпусу, выход в `blocks[]` | контракт `structure.json`: `blocks[]` должен нести источник и уверенность | отложено · 2026-09-04 |
| 2 | **Скачивание корпуса конкурентов** по колонке «URLs группы» из `input/clustering-google-2026-09-07.xlsx` | ничего — окно закрылось вовремя | **сделано · 2026-09-08** |
| 3 | **Пустое медиа-место в ленте карточек** — карточка без арта рисуется без кадра | первый сайт, у которого арт не скачан | отложено · 2026-09-07 |
| 4 | **Кнопка «назад» в ленте активна в исходном положении** | ничего — серия выносов P2 закрыта | **сделано · 2026-09-08** |
| 5 | **Первая карточка ленты не встаёт в колонку контейнера** — снап прижимает её к краю дорожки | решение владельца о виде: вылет к краю оставить или выровнять | отложено · 2026-09-08 |
| 6 | **Мобильная шапка выглядит несимметричной** — группа лого на 4 px выше центра | решение владельца о виде: выравнивать группу лого или оставить | отложено · 2026-09-08 |
| 7 | **Инструменты приёмки — в репозиторий** | ничего: первый шаг P3, до расщеплений | **запланировано · 2026-09-08** |
| 8 | **Данные корпуса едут в `main` вместе с ядром** | роста объёма на следующих сайтах | наблюдение · 2026-09-08 |

---

## 1. Режим автоматического анализа конкурентов

**Заведено:** 2026-09-04. **Статус:** отложено до сессии по `structure.json`.

**Вход — кластеры.** Конкуренты берутся из выдачи по запросам кластера, а не из
заранее известного списка сайтов. Ручной список — только перекрытие, когда
выдача мусорная. Форма входа уже есть в
`../1weekinvr.com/data/competitors.yaml`: у страницы записаны `clusters`,
`phrases_in_serp`, корзины `corpus` / `manual` / `dropped`, а у каждого URL —
`hits` и `share`, то есть сколько фраз кластера привели этот документ. Пороги
корпуса там же: `corpus_min: 6`, `corpus_target: 8`.

Проходов по одному и тому же корпусу два.

### Проход 1 — план содержания

**Уже реализован в `../1weekinvr.com`, переносится, не изобретается заново.**
`tools/fetch.py` собирает корпус, `tools/stats.py` считает:

- медиану объёма и коридор `±15 %` (`VOLUME_TOLERANCE = 0.15`);
- честный порог `VOLUME_MIN_DOCS = 4`: меньше четырёх своих документов — берётся
  медиана по всему корпусу, и число помечается ориентиром, а не нормой ниши;
- сводное дерево H2/H3: тема, которая есть у всех, — обязательная; тема
  у одного — гэп.

Чего в оригинале нет: генерации самого плана из `must_have` и статистики.
Написан 1 план из 17 (`docs/REUSE.md` §3.3, п.3), остальные страницы писались
по брифу напрямую. То есть переносится измеритель, а генератор ещё предстоит
собрать поверх него.

### Проход 2 — анатомия страницы

Тот же корпус, второй взгляд: **из чего страница сделана**, а не о чём она.
Блоки, хлебные крошки, оглавление, блок похожих, FAQ, таблицы, `schema.org`.

Часть инвентаря считается уже сейчас: `extract_structure()` в `fetch.py` кладёт
в `corpus/<slug>/structure.json` дерево заголовков и элементы — `tables`,
`table_rows`, `list_items`, `images`, `videos`. Хлебных крошек, оглавления,
блока похожих, FAQ и разметки в этом инвентаре нет.

**Решает порог частоты, а не глаз.** Элемент у 7 документов из 10 — норма
жанра, и на странице он обязателен. Элемент у 2 из 10 — не норма, решает
человек. Мера уже стоит в `stats.py` для лемм — документная частота
`doc_freq / total_docs`, — здесь она считается по элементам.

**Выход обоих проходов пишется в `blocks[]` структуры.**

### Границы: пол и вид не смешиваются

Анализ конкурентов задаёт **SEO-пол** — что обязано быть на странице, чтобы она
вообще читалась как страница этого жанра. Референсы (`references/1.png` …
`5.png`) задают **вид** — ритм, плотность, типографику. Смешивать эти два
источника нельзя: анализ, допущенный до внешнего вида, выпускает медиану рынка,
и фабрика начинает штамповать то, что у конкурентов уже есть, вместе с их
посредственностью.

### Воспроизводимость: корпус хранится с датой и запросом

Выдача меняется, поэтому корпус без даты и запроса — не данные, а мнение.
Запрос в оригинале записан (`clusters` в `competitors.yaml`), **даты нет
нигде**: ни там, ни в `corpus/<slug>/structure.json`, где запись документа это
`n, url, host, origin, hits, share, chars_no_space, headings, elements`.
Чинится при переносе, а не потом.

### Неопознанные блоки

Блок, не легший ни в одно известное имя, не выбрасывается и не приравнивается
к похожему: он идёт отдельным списком, с уверенностью и примерами. Жанр
готовый — `unresolved.md` у `../7dtd.com.pl`: отдельный файл «не удалось
установить» вместо молчания (`docs/REUSE.md` §7, строка «Не удалось
установить»). Нужен ли такой файл фабрике вообще — открытый вопрос §7.2, п. 6;
здесь он назван как место для этого списка.

### Что извлекается и что не извлекается никогда

Извлекается **структура страницы**: наличие блоков, их порядок, объём,
разметка. Чужой текст не извлекается — ни в план, ни в промпт, ни как пример
формулировки. Никогда.

### Почему отложено

Решается вместе с контрактом `structure.json`: формат `blocks[]` обязан нести
**источник** (проставлено человеком или получено анализом) и **уверенность**.
Без этих двух полей результат анализа некуда положить, не смешав его с решением
человека. Прецедент поля источника уже есть рядом: в записи документа
`1weekinvr` стоит `origin` — из выдачи документ пришёл или из ручного списка.

Шесть развилок `docs/REUSE.md` §7.2 ждут той же сессии.

---

## 2. Скачивание корпуса конкурентов

**Заведено:** 2026-09-07. **Статус: сделано 2026-09-08**, по указанию
владельца — не дожидаясь сессии структуры. Окно закрылось вовремя: корпус
соответствует снимку выдачи, потому что забран на следующий день после него.

**Вход — колонка «URLs группы»** в
`sites/ac4bf-thewatch.com/input/clustering-google-2026-09-07.xlsx`. Файл
положен коммитом «Семантика: кластеризация 2026-09-07…» как есть, бинарником,
и не разбирается до сессии структуры. Отсюда берутся только адреса; частотности,
кластеры и их судьбы — не здесь.

**Единственный пункт бэклога, у которого в графе «ждём» ничего не стоит,
и это не ошибка заполнения.** Правило файла — писать не «когда», а чего ждём;
здесь ждать нечего, зато есть обратное условие: **окно закрывается само.**
Колонка «URLs группы» — снимок выдачи от 2026-09-07, а выдача меняется.
Скачанное через месяц будет корпусом другой выдачи, и сопоставить его
с этой кластеризацией уже нельзя.

**Что именно скачивается.** Документы по адресам из колонки, с сохранением
даты скачивания и запроса рядом с каждым, — это прямо тот пробел, который
`docs/REUSE.md` §3 нашёл у `../1weekinvr.com`: запрос там записан
(`clusters` в `competitors.yaml`), а **даты нет нигде**. Чинится при переносе,
не потом. Пункт 1 этого файла опирается ровно на такой корпус.

**Гео и язык плана Keyword Planner проверены: Польша, польский.** Вопрос,
стоявший на листе «Легенда» внутри файла, закрыт — записано и здесь,
и в теле коммита семантики, чтобы не открывался заново.

**Чего этот пункт не делает.** Не решает судьбы кластеров, не строит
`structure.json`, не считает статистику — только забирает документы, пока
они соответствуют снимку. Разбор — пункт 1 и сессия структуры.

### Что получилось

Инструмент — `sites/ac4bf-thewatch.com/tools/fetch-corpus.mjs`
(`npm run corpus`, `npm run corpus:check`, `--report` пересчитывает сводку
без забора). Зависимостей не добавлял: `xlsx` распаковывается своим
читателем zip, `robots.txt` разбирается своим разбором.

3479 адресов из 258 групп на 537 хостах:

| Исход | Сколько |
|---|---|
| `ok` | 2098 |
| `http-error` | 757 |
| `robots-disallowed` | 517 |
| `not-html` | 103 |
| `network-error` | 3 |
| `timeout` | 1 |

**Где что лежит.** `input/corpus/manifest.jsonl` — запись на документ:
дата забора, группы, статус, хеш, размер. `input/corpus/queries.json` —
соответствие «фраза → группа и адреса», 1426 фраз. `input/corpus/run.json` —
сводка, выводимая из манифеста целиком. Сами документы —
`input/corpus/raw/<хост>/*.gz`, 221 МБ после сжатия (сырых 1,23 ГБ),
**в git не идут**: правило `.gitignore` взято у `../1weekinvr.com`, где
сырой корпус исключён по той же причине.

### Покрытие по кластерам — вход сессии структуры

Указание владельца от 2026-09-08: сессия структуры должна знать, каким
кластерам верить. Для каждого кластера считается, сколько документов его
выдачи удалось забрать, и кластер попадает в одну из трёх корзин.

| Корзина | Скачано | Что с этим делать | Кластеров из 257 |
|---|---|---|---|
| `high` | ≥7 | анатомия с порогом частоты | 102 |
| `mid` | 4–6 | анатомия с оговоркой | 86 |
| `low` | ≤3 | анатомию не считаем, только план содержания | 69 |

Медиана скачанного на кластер — 6. Некластеризованных фраз 264, и покрытие
у них считается **по фразе**, а не по группе: у каждой своя выдача, и
сведённые в одну строку они дали бы 2397 адресов и покрытие, не означающее
ничего. Их корзины: 114 / 120 / 30.

**Корзина задана абсолютным числом, а выдача кластера не всегда десять.**
В снимке она бывает и короче: у 24 кластеров адресов было три и меньше.
Такой кластер попадает в `low`, ничего при этом не потеряв, и путать его
с недобранным нельзя. Поэтому нижняя корзина разложена в сводке отдельно:

| Из 69 кластеров `low` | Сколько |
|---|---|
| в снимке было ≤3 адреса — терять нечего | 24 |
| в снимке было ≥4, добрали ≤3 — недобрано | 45 |
| не скачано ни одного документа | 7 |

Медианная доля скачанного: `high` — 80 %, `mid` — 63 %, `low` — 50 %.
Запись кластера в `run.json` несёт и `urls`, и `ok`, так что «3 из 3»
отличимо от «3 из 10» без пересчёта.

Порог `VOLUME_MIN_DOCS = 4` у `../1weekinvr.com` стоит ровно между второй
и третьей корзиной и заведён там по той же причине: меньше четырёх своих
документов — число перестаёт быть нормой ниши.

**Нижняя корзина не случайна.** 121 отказ HTTP и 46 запретов `robots`
в ней дают магазины и площадки: `allegro.pl` (42), `www.reddit.com` (22),
`assassinscreed.fandom.com` (20), `www.ceneo.pl` (18), `www.mediaexpert.pl`
(13). Недобраны ровно транзакционные выдачи — и сессия структуры увидит
это по корзине, а не догадается.

---

## 3. Пустое медиа-место в ленте карточек

**Заведено:** 2026-09-07. **Статус:** отложено до первого сайта без арта.
Решение владельца по П13.

**Что происходит.** `core/blocks/CardRail.astro` рисует карточки из данных,
и медиа каждой приходит готовым `ImageMetadata`. Запасной графики у карточки
нет: подать её можно было бы только слотом, а слот в цикле не размножается.
Карточка без `media` рисуется без медиа-места вовсе — не пустой рамкой.

**Почему не чиним сейчас.** Ядро вид не выдумывает: заглушка, нарисованная
им, была бы видом, которого никто не проектировал, а собственный набор
запасной графики в ядре — той посайтовостью, от которой уводил П4. Пустое
место читается как **сигнал незакрытого входа брифа**: арт для слота не
скачан. У одиночных кадров запас остаётся — `SmartImage` держит слот `zapas`,
и на сайте это `EraSkyline`.

**Обещание, к которому пункт привязан:** «свежий клон собирается и выглядит
законченным до первого запуска скриптов скачивания». Записано оно
в `docs/REUSE.md` §1.3 и в шапках `core/gates/check-assets.mjs`
и `<сайт>/gates/assets.mjs`; **в `PRODUCT.md` этой формулировки нет** —
там ближайшее принцип 2 «Проверяемое важнее оговорённого». Ссылка дана
на реальные места намеренно, чтобы не рассыпалась при первой проверке.

**Чем заняться, когда дойдёт.** Посмотреть на клон без арта живьём и выбрать
между вариантами отчёта `2026-09-07-p2-cardrail`: заглушка шасси в цвете
эпохи либо имя запасной графики строкой в записи карточки. Раньше живого
клона выбор будет догадкой.

---

## 4. Кнопка «назад» в ленте карточек активна с самого начала

**Заведено:** 2026-09-07. **Статус: сделано 2026-09-08**, коммитом `67ccaa0`,
первой правкой после закрытия серии выносов.

**Механика.** Дорожка `.rail-sec__track` несёт `scroll-snap-type: x mandatory`
и распорку `::before` шириной под колонку контейнера, поэтому в исходном
положении `scrollLeft` равен не нулю, а 124 (измерено на 1440). Скрипт
считает началом `scrollLeft <= 2`, и `prev.disabled` не выставляется никогда:
кнопка «назад» выглядит доступной там, где двигаться некуда.

**Дефект предсуществующий.** Код перенесён в `core/blocks/CardRail.astro`
дословно вместе с распоркой и скриптом; вынос его не создавал и не менял.
Найден при браузерной проверке ленты — строкой в отчёт, не правкой
(`CLAUDE.md`, инвариант 3).

**Чего ждём.** Закрытия серии выносов. Пока идут выносы, любая правка ленты
попадает в диффу приёмки и мешает отделить перенос от правки; чинится
отдельной правкой вне серии.

**Эскиз починки предлагал два пути, и один из них оказался неверным.**
«Вычесть ширину распорки из порога» не работает: на 1200 распорка нулевая,
а лента всё равно стоит на 56, потому что зазор flex ставится и перед первой
карточкой — распорка это лишь часть отступа. Взят второй путь, положение
первой карточки, и взят измерением, а не повторением формулы: положение
карточки в содержимом дорожки минус отступ снапа
(`getComputedStyle(track).scrollPaddingLeft`, сегодня `auto` → 0).
Формула не зависит от текущей прокрутки и переживёт починку выравнивания,
если до неё дойдёт.

**Проверено браузером на трёх ширинах**, как и требовал пункт: покой 124
на 1440, 56 на 1200, 40 на 390 — `prev.disabled` истинно на всех трёх.
Ход «дальше ×3 → назад ×3» гасит и зажигает обе кнопки там, где нужно.

**Цена — первая ненулевая пиксельная диффа серии, и она названа заранее.**
Погашенная кнопка рисуется с `opacity: 0.35`, поэтому кадр
`baseline-desktop-full-1440.png` разошёлся с `_baseline/` ровно на квадрат
44×44 этой кнопки (x 1254..1297, y 5285..5328) — 702 субпикселя из 50,8 млн.
Три остальных кадра эталона побайтово равны. Мобильных кадров правка
не касается вовсе: `.rail-sec__nav` под 560 скрыт.

**Найдено рядом и не тронуто** (инвариант 3): первая карточка не встаёт
в колонку контейнера. Заведено пунктом 5 ниже по решению владельца П18.

---

## 5. Первая карточка ленты не встаёт в колонку контейнера

**Заведено:** 2026-09-08. **Статус: исполнено 2026-09-11 — ответ владельца
4а «выровнять» (П41), коммит с `scroll-padding-left` в `CardRail.astro`;
кадром закрепится единой пересъёмкой.** Решение П18 о комментарии снято
самой правкой: комментарий у распорки теперь правдив и не переписывался.
Материал для решения был подготовлен пробой и кадрами «как есть / вариант».

**Вариант, измеренный пробой:** одна строка в `.rail-sec__track` —
`scroll-padding-left: max(var(--gutter), calc((100% - var(--container)) / 2 + var(--gutter)))`.
Результат: первая карточка `x` 84 → **174** на 1440 (заголовок 174),
0 → **16** на 390 (заголовок 16); `scrollLeft` в покое 124 → 34 и 40 → 24;
кнопка «назад» в покое выключена в обоих случаях — `start()` в скрипте
ленты читает `scrollPaddingLeft` и рассчитан на это. Почему не распорка:
распорка `::before` считается от `100vw`, а дорожка на ≥1200 короче
на `--rail` (84 px), и между распоркой и первой карточкой стоит `gap`
(24 px) — при `scrollLeft = 0` карточка встала бы на 208, а не 174.
`scroll-padding` считается от ширины самой дорожки и этих двух ошибок
не наследует.

**Другой путь без правки кода:** оставить как есть и переписать комментарий
у распорки под то, что есть (П18 запрещает переписывать его без решения).
Выбор — глазами владельца по двум кадрам.

**Что обещано и что происходит.** Комментарий у распорки в
`core/blocks/CardRail.astro` говорит: «Pierwsza karta staje w kolumnie
kontenera, a nie przy krawędzi ekranu». **Сейчас поведение с этим
комментарием расходится.** Распорка `::before` шириной
`max(0px, calc((100vw - var(--container)) / 2 - var(--gutter)))` действительно
отодвигает первую карточку — но только при `scrollLeft` = 0, а туда лента
не встаёт никогда: `scroll-snap-type: x mandatory` со `scroll-snap-align: start`
и без `scroll-padding-left` прижимает карточку к началу окна прокрутки сразу
при загрузке.

**Замер на 1440**, оба числа — координата x левого края:

| Что | x |
|---|---|
| заголовок секции (колонка контейнера) | **174** |
| первая карточка ленты | **84** |

Разница 90 пикселей: карточка стоит вплотную к дорожке спуска, а не под
заголовком своей секции.

**Комментарий не переписывается** — решение П18. Он свидетельство замысла,
а не ошибка; выравнивать код или комментарий владелец решит глазами.
Пункт заведён именно затем, чтобы расхождение было видно тому, кто читает
код, и он не поверил тексту.

**Дефект предсуществующий.** Распорка и снап перенесены в ядро дословно
вместе с лентой; вынос их не создавал и не менял. Найден при браузерной
проверке кнопки «назад» — пункт 4.

**Чего ждём.** Решения о виде. Вылет карточки к краю дорожки читается
и как намеренный приём, и как промах вёрстки; отличить их может только
глаз владельца. Выровнять — одна строка `scroll-padding-left` под ту же
величину, что у распорки; оставить — переписать комментарий под то, что есть.

**Цена промедления понятна и мала:** пиксель не меняется, пока решения нет,
а расхождение записано здесь и в `DECISIONS.md`.

---

## 6. Выравнивание мобильной шапки

**Заведено:** 2026-09-08 по итогам приёмки глазами. **Статус: правка
внесена 2026-09-11 (П32 п.2, шаг 4 события П40), ждёт приёмки глазами;
кадром закрепится единой пересъёмкой.** Решение П22: дефект
предсуществующий, вливание в `main` не задерживает, чинится после него
отдельной правкой вида с пиксельной приёмкой нового вида.

**Что сделано.** `core/chrome/SiteHeader.astro`: `.hdr__brand` получил
`display: flex` — одна строка из двух предложенных ниже. Измерено до и после
на обоих вьюпортах: коробка ссылки 36,05 → **28**, центр видимой группы
29,97 → **34,00** — ровно центр шапки и центр бургера. **Сдвиг был одинаков
на 390 и на 1440** (измерено 2026-09-11: 4,03 px там и там) — механизм один,
и правка одна: шапка сдвигается на обоих вьюпортах и на обеих страницах.
Судья: правил CSS 337 → 337, разошлась ровно одна тройка `.hdr__brand`;
пиксель — на всех семи кадрах главной и трёх оконных кадрах игры один и тот
же прямоугольник 158×26 (знак и слово) в 5204–5205 субпикселей, геометрия
страниц не дрогнула. Маппинг диффа эталона: **П32 п.2**.

**Капкан, найденный по дороге:** слово-кандидат в комментарии CSS будит
сканер Tailwind — упоминание строчного значения `display` в комментарии
к правке дало `+1` правило (`.inline-flex`) в листе. Комментарий переписан
без токена; правил 337 → 337. Инвариант 8 `CLAUDE.md` работает и на
комментарии.

**Замер на 390.** Шапка `.hdr` — 69 px (68 + 1 px линии снизу);
`.hdr__inner.container` — `padding: 0 16px`, высота 68, `align-items: center`,
то есть колонка контейнера идёт от 16 до 374.

| Что | Слева | Справа |
|---|---|---|
| коробка: `.hdr__brand` / `.hdr__burger` | 16 от края экрана | **4** от края экрана |
| видимый знак: ink SVG знака / ink иконки бургера | **18,63** | **18,67** |

**Коробки несимметричны намеренно.** У `.hdr__burger` стоит
`margin-right: calc(var(--space-xs) * -1)` = −12 px: кнопка 44×44 вылезает
за колонку контейнера вправо, чтобы её иконка 22×22 встала оптически
по краю. У `.hdr__brand` такой поправки нет и не нужно — знак начинается
прямо от края колонки.

**По вертикали расхождение настоящее.**

| Что | Верх | Низ | Центр |
|---|---|---|---|
| `.hdr__inner` | 0 | 68 | **34,00** |
| `.hdr__burger` (и его иконка) | 12 (23) | 56 (45) | **34,00** |
| `.hdr__brand` — коробка ссылки | 15,97 | 52,02 | 34,00 |
| **видимый знак и слово `BRACTWO`** | 15,97 | 43,97 | **29,97** |

Видимая группа лого сидит на **4,03 px выше** центра шапки и на те же
4,03 px выше центра бургера — вот что видно глазом.

**Причина названа механикой, а не догадкой.** `.hdr__brand` — блок,
внутри него `.wordmark` с `display: inline-flex`. Коробка ссылки выходит
36,05 px высотой: 28 px самого знака плюс 8,05 px инлайн-подбора под базовую
линию снизу. Флекс шапки центрирует **коробку ссылки** (её центр ровно 34,00),
а видимое содержимое прижато к её верху — отсюда сдвиг вверх ровно
на половину подбора.

**Одно из трёх наблюдений приёмки измерение не подтвердило, и я обязан
это назвать.** «У бургера справа воздуха заметно больше» — по видимому
знаку отступы равны: 18,63 слева против 18,67 справа, разница 0,04 px.
Отрицательное поле бургера эту симметрию как раз и создаёт. Несимметричны
только **коробки** (16 против 4), а коробка бургера — это область нажатия,
её не видно. Ощущение асимметрии, скорее всего, даёт вертикальный сдвиг
плюс пустая середина: между концом слова `BRACTWO` (193,5) и кнопкой (342)
лежит 148,5 px пустоты, и правая половина шапки читается как более
воздушная, чем левая.

Пункт от этого не отменяется: вертикальный сдвиг измерен и настоящий,
а решение о виде остаётся за владельцем.

**Чего ждём.** Решения о виде. Убрать инлайн-подбор — одна строка
(`display: flex` или `line-height: 0` у `.hdr__brand`), и группа встанет
по центру; но это правка вида, и её пиксельная приёмка — новая, потому что
эталон `_baseline/` показывает нынешнее положение.

**Дефект предсуществующий.** Шапка перенесена в `core/chrome/SiteHeader.astro`
дословно; вынос сдвига не создавал и не менял.

---

## 7. Инструменты приёмки — в репозиторий

**Заведено:** 2026-09-08 по итогам критика полноты сводного аудита.
**Статус: сделано 2026-09-08**, первым шагом P3, как и планировалось (П23).
Инструменты в `core/accept/`, запуск у сайта, самопроверка воспроизводит числа
отчётов P1 и P2; что чем исполняется и три решения, принятые при исполнении, —
`docs/11_PRIEMKA.md`. Открытый вопрос «где инструментам жить» закрыт там же.

**Что не так.** Правило приёмки записано в `docs/REUSE.md` §6 и
`MIGRATION.md` §6, а **исполняется инструментами, которых в репозитории
нет**. Каждая сессия пишет их заново и оставляет вне репозитория; следующая
пишет снова. Процедура, на которую опирается вся серия выносов,
воспроизводима только той сессией, что её выполняла, — а это ровно то
свойство, от которого правило приёмки должно было увести.

**Что переписывалось заново, по следу отчётов серии.**

| Инструмент | Где применялся |
|---|---|
| снимок и сверка манифеста `dist/` — 50 файлов, побайтово | каждый вынос P1 и P2 |
| попиксельное сравнение PNG: доля субпикселей, максимум отклонения, рамка отличий, распределение | `SiteHeader`, `CardRail`, закрытие серии |
| разбор CSS в тройки «условие + селектор + объявления» и сверка мультимножеств | `SiteFooter`, `SiteHeader`, `CardRail` — правило перегруппировки `@media` (П7) |
| нормализация скрутов `data-astro-cid-*` перед сверкой разметки | `CardRail` |
| локализация диффы `index.html` общим префиксом и суффиксом | закрытие серии |
| съёмка кадров по протоколу: `document.fonts.ready`, прокрутка до низа и обратно, смена вьюпорта без перезагрузки | каждая пиксельная сверка |

**Почему именно первым шагом P3.** P3 — расщепления `EraLayer` и `Hero`,
а у расщепления диффа **заведомо ненулевая**: правило `REUSE.md` §6 держит
этот случай шагом 3, то есть локализацией и пиксельной сверкой. Значит
инструментами будут пользоваться постоянно и с первого дня. Написать их
после расщеплений значит написать их в третий раз.

**Чего этот пункт не делает.** Не меняет правило приёмки — оно записано
и принято. Пункт даёт правилу исполняемую часть, ровно как гейты дали её
чек-листу QA.

**Открытый вопрос реализации — технический, решается при исполнении.**
Где инструментам жить: рядом с гейтами в `core/`, отдельной папкой уровня
репозитория или скриптами сайта. Гейты — образец решённого случая: механизм
в `core/gates/`, ожидания в `sites/<сайт>/gates/`. Отличие приёмки в том,
что она запускается руками между двумя сборками, а не внутри одной,
и билд не роняет.

---

## 8. Данные корпуса едут в `main` вместе с ядром

**Заведено:** 2026-09-08. **Статус: наблюдение**, не задача. Решение П23:
сейчас не трогать.

**Что наблюдаем.** Ветка серии выносов влила в `main` не только ядро:
вместе с ней уехали `input/corpus/manifest.jsonl` (1,7 МБ),
`queries.json` (1,2 МБ) и `run.json` (91 КБ) — около 3 МБ текстовых
данных исследования, плюс бинарные кадры эталона.

**Это законно и уже решено при заборе.** Сырой HTML в git не идёт
(правило `.gitignore`, прецедент `../1weekinvr.com`), а манифест идёт
намеренно: по нему корпус пересобирается, и хеши показывают расхождение
с сегодняшней страницей. Три мегабайта текста — цена воспроизводимости,
и она заплачена сознательно.

**Чего ждём.** Роста объёма на следующих сайтах. Один сайт — 3 МБ; десять
таких — 30 МБ манифестов в истории, и вопрос хранилища встанет сам.
Тогда и возвращаемся: выносить ли исследовательские данные из `git`
в отдельное хранилище, и что тогда играет роль хеша.

**Почему пункт заведён наблюдением, а не задачей.** Делать сейчас нечего,
а забыть — легко: данные растут молча, и первым признаком станет медленный
`git clone`. Пункт существует, чтобы признак был назван заранее.

---

## 9. Забор корпуса шире, чем нужен дереву

**Заведено:** 2026-09-08 по итогам S3. **Статус: ждём пересъёмки семантики.**
Решение П28: сейчас делать нечего, корпус уже собран.

**Что наблюдаем.** Корпус собирался по всей семантике, до того как появилось
дерево. Из 2098 успешно скачанных документов к кластерам тридцати страниц
волны 1 относится 281 — тринадцать процентов. Остальное принадлежит
кластерам, ушедшим в `no_page`, `exclusions` и слияния: их страниц нет,
и анатомию по ним никто не считает.

**Почему это не ошибка.** Порядок был обратный намеренно: корпус нужен был
разведке S0, чтобы считать корзины покрытия по всем кластерам, а дерево
строилось после. Широкий забор дал свободу менять состав страниц, не
возвращаясь к сети.

**Что решать при пересъёмке.** Узкий забор по кластерам дерева дешевле:
меньше трафика, меньше места (сегодня 216 МБ сырого HTML и 1,6 МБ манифеста),
меньше времени. Широкий оставляет право передумать. Решение зависит от того,
меняется ли дерево вместе с семантикой или остаётся: если меняется — широкий,
если нет — узкий.

**Признак, что пора.** Пересъёмка семантики или второй сайт фабрики: там
вопрос встанет сразу и уже не наблюдением.

---
## 10. Инструментам приёмки нужен режим плоского гашения скрутов

**Статус: СДЕЛАНО 2026-09-09, сессия 3.** Режим живёт флагом `--flat`
у `core/accept/css-triples.mjs`, реализация гашения — в `html-diff.mjs`,
условия записаны в `REUSE` §6. **Отмечено с опозданием 2026-09-10:** пункт
был закрыт работой и полгода числился открытым на бумаге. Расхождение
найдено при сборке сводки P3 и названо здесь.

*Найдено сессией `2026-09-09-p3-storyrow`, первым расщеплением серии P3.*

**Что не работает.** `core/accept/html-diff.mjs` приводит скруты **порядково**:
`normalizeScopes` нумерует их по первому появлению в файле. Для выноса
это верно — скрут меняет значение, но остаётся одним. Для **расщепления**
неверно по построению: один скрут превращается в два-три, нумерация всего,
что идёт после первого вхождения, сдвигается, и вердикта «файлы равны»
инструмент не напечатает ни при каком исходе. `core/accept/css-triples.mjs`
скруты не приводит вовсе — в файле нет ни одного упоминания `astro-cid`.

**Чем сессия обошлась.** Разовой командой в двух режимах, записанной в отчёт
дословно: **A** — все `[data-astro-cid-…]` гасятся в один постоянный токен
(видно, какие правила потеряли скрут, и их можно назвать поимённо); **B** —
скруты стираются целиком вместе с приведением хеша в имени собранного листа
(видно, изменилось ли хоть что-то, кроме скрутов). На выносе `story-row`
режим A дал ровно 16 ожидавшихся расхождений, режим B — ноль и побайтовое
совпадение `index.html`.

**Решено владельцем 2026-09-09, вариант (а): пункт закрыт.** Плоское гашение
записано штатным шагом в `REUSE` §6 — с тремя обязательными условиями:
гашение стирает только хеш и ничего больше; отчёт называет число погашенных
скрутов до и после; на расщеплении ожидается 1 → 3, любое другое число —
остановка и разбор. Формулировка владельца: «Мне нужен судья, а не повязка
на глаза».

**Режим добавляется в `core/accept/*` первым коммитом сессии 3, отдельной
сессией не заводится:** сессия расщепления без инструмента не принимается,
значит это её предусловие, а не самостоятельная работа. Записано
в `docs/PLAN-P3.md`, сессия 3.

**Объём.** Флаг `--flat` у `css-triples.mjs` и `html-diff.mjs` плюс счёт
погашенных скрутов в выводе; строка в `docs/11_PRIEMKA.md`. Логика сравнения
не меняется — меняется только приведение.

---
## 11. В `_baseline/` нет оконных кадров слоёв

**Статус: СДЕЛАНО 2026-09-09.** Три кадра заведены решением владельца при
приёмке сессии 2 — `jerozolima`, `wlochy`, `londyn`, все три сочетания
модификаторов, какие есть на странице. Состав и `scrollY` записаны
в `_baseline/README.md`. **Отмечено с опозданием 2026-09-10**, тем же
разбором, что пункт 10. Оговорка: с 2026-09-09 все три — **не судьи**
(`ne-sudyi.json`), и наводка по литералу переписывается решением П39.

*Найдено там же.*

Четыре эталона `_baseline/` сняты по протоколу `MIGRATION.md` §6, и оба
**оконных** кадра — при `scrollY = 0`, где виден только hero. Слои эпох
покрывает лишь полностраничный кадр, а по решению П12 полный кадр — сторож,
оконный — судья. То есть у самой рискованной части вёрстки — зеркальности
соседних слоёв — судьи нет, есть только сторож.

На выносе `story-row` это не помешало: полностраничный кадр совпал
с эталоном побайтово, ноль различий из 38 098 080 субпикселей. Но сессия 3
выносит `Hero`, и там та же нужда.

**Решено владельцем 2026-09-09: пункт закрыт, и не пересъёмкой.** Кадры
слоёв сняты **сейчас**, на сборке `725fc4a`, побайтово равной прошедшей
приёмку, — поэтому они наследуют её приёмку и отдельного решения
не потребовали. В `_baseline/` добавлены три оконных кадра 1440×900
с зафиксированными числами прокрутки: `jerozolima` 892 (`flip` нет,
`band` нет), `wlochy` 1818 (`flip` да, `band` да), `londyn` 6006
(`flip` нет, `band` да) — все три сочетания модификаторов, какие есть
на странице. Состав и порядок съёмки — `_baseline/README.md`, таблица —
`MIGRATION.md` §6.

**В пересъёмку кадры слоёв не входят.** Список пересъёмки закрыт и содержит
только то, что ломает действующие эталоны: пункты 5 и 6 этого файла плюс
возврат главной к её плану. Всё, что всплывёт после сессии 4, едет в волну 2;
пересъёмка остаётся одна.

---
## 12. Число `@media` в самопроверке — константа, которую двигают руками

**Статус: СДЕЛАНО 2026-09-09 решением П33.** Число снято вместе с двумя
другими константами живой сборки; взамен заведён рукописный образец, который
создаёт сам тест. `core/accept/selftest.mjs` печатает `@media` живой сборки
справочной строкой и **не сверяет** его — довод записан в самом файле.
**Отмечено с опозданием 2026-09-10**, тем же разбором, что пункты 10 и 11.

*Заведено решением владельца 2026-09-09 при приёмке сессии 2 Фазы 3.*

**Что не так.** `core/accept/selftest.mjs` сверяет живую сборку с константой:
было 24, сессия 2 законно сделала 26 и константу подвинула. Довод при этом
был верный — иначе инструмент остался бы красным навсегда, — но повторяемость
операции и есть проблема: **константа, которую руками двигают каждую сессию,
перестаёт что-либо проверять.** Каждое следующее расщепление разъединяет
одноусловные медиаблоки, то есть двигать её придётся ещё как минимум дважды
в P3.

**Закрыто 2026-09-09, в закрытии сессии 3 — то есть «к началу сессии 4»,
как и было велено.** Выбран второй путь: утверждение о живой сборке снято,
потому что инварианта для него не нашлось — число `@media` в собранном листе
зависит от того, сколько файлов со стилями содержат медиазапросы, а это
меняется каждым расщеплением по определению.

Взамен заведена проверка **самого счётчика на рукописном образце**: пять
правил, два at-правила, три вхождения `@media`, два различных условия,
и два блока с одинаковым условием подряд — случай, который расщепление
и создаёт. Она верна при любой сборке и ловит ошибку разбора, то есть
единственное, что здесь и стоило ловить. Живую сборку стережёт манифест.

Третьего ручного сдвига константы не случилось: к моменту закрытия число
успело бы стать 27 после расщепления `Hero`. Остальные проверки
самопроверки не задеты — они привязаны к фактам, которых расщепления
не меняют.

**Двойной счёт «P» и «П».** Критик полноты аудита назвал риск: этапы выносов
записаны латиницей (`P1`, `P2`, `P3` — `docs/REUSE.md` §6), решения
владельца — кириллицей (`П1`…`П23` — `DECISIONS.md`), и ветка `phase-2-p1`
несёт при этом весь P1 и весь P2. Читатель может принять «P2» за «П2».

**Пунктом не заводится. Решение П23, 2026-09-08.** Путаница теоретическая:
оба счёта живут каждый в своём документе и в своём контексте, ни одного
случая смешения за всю серию не было. Переименование ветки или перенумерация
решений стоит дороже пользы — обе операции переписывают ссылки в отчётах,
которые переписывать нельзя.

---

## 13. Палитра записана дважды: `@theme` и `:root`

*Заведено 2026-09-09 при разборе `REUSE` §7.4. Решение владельца: чинить
не сейчас; лгущую половину — проверку — починили сразу, отдельным шагом.*

**Что не так.** `sites/<сайт>/src/styles/global.css` держит палитру в двух
записях: `@theme { --color-X }` для Tailwind и `:root { --X }` для компонентов.
Значения переписаны руками. **Измерено 2026-09-09:** пар четырнадцать, все
четырнадцать сегодня совпадают.

**Что уже сделано и что осталось.** Гейт контраста больше не слеп: повторное
имя роняет сборку вместо тихой перезаписи последним вхождением, и каждая
из четырнадцати пар обязана совпадать — расхождение роняет. Проверено пробой:
подмена `--color-bg` уронила гейт с внятным сообщением. **Осталась сама
двойная запись:** её гейт теперь стережёт, но не отменяет.

**Дублирование уже дало измеренный промах — второй раз, и это не теория.**

1. **2026-09-09, токены.** Гейт контраста собирал `--имя: #hex` одной
   регуляркой по всему файлу и видел 43 независимых имени вместо 14 пар;
   правка одной из двух записей прошла бы зелёной. Починено.
2. **2026-09-10, список ролей.** Тот же жанр в другом месте: `global.css`
   объявляет **одиннадцать** ролей `.t-*`, `core/styles/ROLES.md` перечисляет
   только требуемые ядром, а список в `core/accept/glyphs.mjs` скопирован
   руками — **третья запись того же списка**. Пропущенной оказалась
   `t-wordmark`, и это не любая роль: Cinzel под `uppercase`, то есть
   **восемь скрытых сломанных знаков**, которых проверка не видела. Список
   ролей выводить из `global.css`, а не хранить копию — часть этого же пункта.

**Цена, чтобы владелец знал заранее.**

- **Путь (б) — одна запись `:root`, блок `@theme` генерируется из неё.**
  Примерно **одна сессия**: генератор плюс гейт «сгенерированное совпадает
  с записанным». Имена токенов не трогаются, договор 23 токенов
  (`core/styles/ROLES.md`, П34) остаётся как есть. Это же закрывает половину
  строки «генератора нет» из `docs/UNRESOLVED.md`.
- **Путь (а) — оставить только `@theme`, компоненты переводятся
  на `var(--color-*)`.** **Не одна сессия:** переименование затрагивает все
  23 токена договора, каждый блок ядра, каждый лист `core/styles/` и всю
  витрину, а заодно ломает П34, где договор назван по нынешним именам.
  Дешевле не станет — рекомендую (б).

---

## 14. Прокрутка к кадрам слоёв привязана к литералу, а не к якорю

*Заведено 2026-09-09 по прямому вопросу владельца: во сколько встаёт привязка
к элементу вместо числа пикселей, и делать ли это перед Г1.*

**Исполнено 2026-09-11, первым шагом события перебазирования (П40, П39).**
`К_ПОЛОСЕ(y)` заменён на `К_ЯКОРЮ(id)` в `core/accept/frames.mjs`: слой
находится по `id` и центрируется в окне —
`round(offsetTop + (высота слоя − высота окна) / 2)` на числах
`getBoundingClientRect()` с точностью 0,01; правило то же, что дало литералы
2026-09-09, арифметика другая (округление снаружи, потому что `offsetTop`
дробный), и на дробных входах результат может отличаться от прежнего на 1 px.
Сдвиг ловится числом: новый скрипт `ГЕОМЕТРИЯ` снимает условия съёмки
(`clientWidth`, `clientHeight`, `devicePixelRatio`), высоту страницы
и `offsetTop` каждого слоя, съёмка кладёт их в `geometria.json` рядом
с кадрами, а `check` сверяет с `geometria.json` эталона и печатает каждый
`СДВИГ` отдельной строкой с видом — выше слоя, внутри слоя, страница; чистые
функции `сравнитьГеометрию` и `счётГеометрии` — под батареей из семнадцати
образцов в `selftest.mjs`. Таблица `_baseline/README.md` и `MIGRATION.md` §6
переписаны на якоря, прежняя редакция П30 п.2 оставлена рядом дословно.
**Измерено на сборке `836f48f` тем же драйвером, что снимал кадры прошлых
сессий:** те же три кадра по якорю против эталона дают 17,4 / 14,2 / 21,8 %
(678 223 / 551 498 / 849 183 субпикселя из 3 888 000) вместо
20,1 / 28,3 / 46,5 % по литералу — первая причина снята, вторая (содержимое
слоя после смены гарнитуры) ждёт перебазирования. Геометрия той съёмки:
страница 8633, `offsetTop` 879 / 1777,8 / 5806,61, высоты слоёв
898,8 / 871,92 / 948,95. `geometria.json` у эталона главной появится вместе
с кадрами при пересъёмке и запишет числа той съёмки.

**Судью судили в тот же день** (правило `CLAUDE.md`): три линзы, 26 находок,
14 пережили опровержение — восемь классов, все починены до приёмки:
«совпала» при нуле сравнённого; `null` в геометрии эталона ронял `check`
до сверки кадров; хвост плавающей точки в `СДВИГ`; формула в записи
не совпадала с кодом; порядок съёмки отсылал к `geometria.json` эталона,
которого при первой съёмке нет; одна фраза «выше слоёв что-то изменилось»
на три разных вида сдвига; условия съёмки не записывались (видимая полоса
прокрутки даёт на 390 высоту 11514 против 11545 при нетронутой странице);
цитата прежней редакции не была дословной.

**Ответ одной строкой: код дешёвый, но перед Г1 делать нечего — три кадра
всё равно не воскреснут до перебазирования, потому что изменилось не только
их положение, но и содержимое.** Внутри слоёв стоят **15 элементов `t-title`
и 5 `t-year`**, а обе роли только что сменили гарнитуру. Идеально наведённый
кадр разошёлся бы с эталоном всё равно.

**Поэтому: делать вместе с перебазированием**, когда литералы и так
пересчитываются. До тех пор три кадра лежат в разделе «не судьи»
(`_baseline/ne-sudyi.json`).

**Что именно чинить.** Сегодня `К_ПОЛОСЕ` едет к числу и проверяет
`scrollY === литерал`. Числа получены центрированием слоя и **заморожены
намеренно** — П30 п.2: «сдвиг геометрии выше слоя обязан проявиться
расхождением кадра, а не молча уехать вместе с новым `offsetTop`».

**Прямая замена литерала якорем эту защиту снимает,** и это надо назвать
до, а не после: кадр перестанет замечать сдвиг сверху. Поэтому чинить
не заменой, а парой:

1. прокрутка наводится **по якорю слоя** (`[data-era-section="<id>"]`),
   и кадр остаётся сравнимым, что бы ни выросло выше;
2. сдвиг сверху продолжает ловиться — **числом, а не взрывом кадра**:
   съёмка уже меряет высоту страницы и `offsetTop` слоя, и расхождение
   с записанным называется отдельной строкой протокола.

Так обе цели П30 п.2 выполняются, но механизм меняется: «обязан проявиться
расхождением кадра» становится «обязан быть назван числом». **Это правка
записанного решения, поэтому требует слова владельца** — здесь она только
описана, не сделана.

**Цена:** несколько строк в `core/accept/frames.mjs` плюс перезапись таблицы
`scrollY` в `_baseline/README.md` на якоря. Внутри сессии перебазирования —
не отдельная работа.

---

## 15. Лента не умеет сказать «дальше есть ещё», когда содержимое почти помещается

*Заведено 2026-09-10 по приёмке глазами страницы волны 1. **Пункт был обещан
владельцу 2026-09-09 в половинном виде — «лента без признака прокрутки
под 560 px» — и не заведён. Оплошность исполнителя названа здесь, а не
умолчана.** Теперь заводится целиком, обеими сторонами.*

**Это один дефект, а не два.** `CardRail` — лента с `overflow-x: auto`
и `scroll-snap-type: x mandatory`. Признак «прокручивается» у неё один —
кусок следующей карточки, торчащий из-за края. Когда содержимое **почти**
помещается, признака не остаётся, и лента читается как сломанная вёрстка.
Две стороны одного:

**Сторона первая — телефон, под 560 px.** Стрелки скрыты
(`.rail-sec__nav { display: none }`), полоса прокрутки погашена
(`scrollbar-width: none` плюс `::-webkit-scrollbar { display: none }`).
Единственный признак — обрезанная карточка; других нет вовсе.

**Сторона вторая — десктоп 1440. Измерено 2026-09-10 на
`/assassins-creed-4-black-flag/`:** дорожка растекается до края окна
(84…1440), а весь остальной контент живёт в контейнере 1240 px
и кончается на 1340. Четыре карточки по 320 с зазором 24 дают правый край
последней на **1436** — за 4 px до края окна. Карточка **не обрезана**,
но упирается в окно без жёлоба и уходит на 96 px за край контента.

**Почему на главной то же устройство читается верно.** Там пять карточек:
пятая начинается на 1460 и режется посередине — видно, что дальше есть ещё.
Разница не в вёрстке, а в **совпадении числа карточек с шириной**.

**Что чинить — правило ядра, а не страницу.** Три пути, ни один не выбран:
жёлоб справа у дорожки; запрет последней карточке оканчиваться ближе
некоторого зазора к краю; отдельный признак прокрутки, не зависящий
от торчащей карточки. Первые два дешевле, третий честнее на телефоне.

**Внутри сессии 6 не чинится** — решение владельца 2026-09-10.

---

## 16. Воздух между секциями: `--section-y` против «плотного» варианта блока

**Статус: ЗАКРЫТ 2026-09-10 решением владельца при приёмке вида сессии 6.**
Стык двух содержательных секций остаётся **256 px** (128 + 128): вариант (а)
вопроса 1 отчёта `docs/reports/2026-09-10-p3-s6-vid.md`. Довод владельца —
**разница ритмов читается как иерархия**, а не как ошибка: служебные секции
после этой же приёмки встали на половинную отбивку, и контраст между
обвязкой и рассказом теперь работает на чтение страницы. Правка
`--section-y` не делается; «плотный вариант блока» не заводится.

*Разбор ниже оставлен целиком — им закрывается пункт, и по нему видно,
из чего выбирали.*

*Заведено 2026-09-10 решением владельца — как **возможная** правка,
к которой возвращаются после сессии 6, посмотрев на выровненный ритм глазами.*

**Откуда взялось.** На странице волны 1 стыки секций дали 176 px
(128 + 48), потому что секция `pokrewne` набрана отбивкой 48 вместо
`--section-y`. После сессии 6, когда `link-list` станет объявленным блоком
и его напечатает ядро, оба стыка станут **256 px** — норма двух блоков
подряд. Ритм выровняется, воздуха станет больше. Много ли — смотрит владелец.

**Если много, путей два, и они не равны.**

| Путь | Что меняется | Цена |
|---|---|---|
| **Правка `--section-y`** (сейчас `clamp(72px, 9vw, 128px)`) | одно число в листе сайта | ритм **всех** секций на всех тридцати страницах и на главной; высота страниц меняется, значит эталон переснимается целиком. Дёшево в коде, а в приёмке — ровно одна пересъёмка, которая и так предстоит |
| **«Плотный» вариант блока** | новый модификатор у блоков ядра, витрина ставит его там, где нужно | правит только помеченные секции, но **добавляет ручку в договор блока** — то есть кто-то должен решать на каждой странице, где плотно. Это контракт, а не стиль |

**Разница одной фразой:** токен меняет ритм **везде и одинаково**, вариант
блока — **местами и по решению**, и требует, чтобы это решение кто-то
принимал на каждой странице.

### Взят третий путь, 2026-09-10, приёмкой вида сессии 6

*Решение владельца: план из четырёх шагов. Пункт остаётся открытым — третий
путь закрыл служебные секции, а вопрос «много ли воздуха между секциями
содержательными» стоит по-прежнему.*

**Что сделано.** Класс `.section--light` рядом с `.section` в
`core/styles/layout.css`, величина — токен сайта `--section-y-light`
(`clamp(36px, 4.5vw, 64px)`, ровно половина `--section-y`). Служебные секции
— `byline`, `toc`, `gallery`, `verdict-box`, `link-list` — носят его,
содержательные `story-row`, `card-rail`, `link-columns` остаются на `.section`.

**Почему это третий путь, а не второй.** Ручка в договор блока **не
добавлена**: витрина не выбирает ритм на каждой странице, его выбрал один раз
компонент ядра по природе блока. Цена второго пути, названная в таблице выше
— «кто-то должен решать на каждой странице», — не заплачена.

**Цена, которая заплачена:** договор `core/styles/ROLES.md` вырос с 32 имён
до 33. Новый сайт обязан объявить `--section-y-light`, иначе служебные секции
останутся без отбивки вовсе — молча, как всякий пропавший `var()`.

**Что было открытым и как закрылось.** Стык двух содержательных секций
остался 256 px (`toc` → `story-row` даёт 192, `story-row` → `card-rail` — 256).
Первый путь таблицы, правка `--section-y`, вынесен владельцу вопросом 1 отчёта
приёмки вида и **отклонён**: остаётся как есть. Шапка пункта выше называет
довод.

**Поправка к записи выше, 2026-09-10, тем же решением владельца:** величина
лёгкого ритма считается делением `calc(var(--section-y) / 2)`, а не отдельным
токеном. Договор `core/styles/ROLES.md` **не вырос** — остался на 32 именах.
Цена, названная здесь абзацем выше, не заплачена вовсе: подробности —
дополнение к П37 в `DECISIONS.md`.

---

## 17. Хлебных крошек нет, а у страниц второго уровня без них нет пути наверх

*Заведено 2026-09-09 решением владельца: «умолчание называю решением — крошки
нужны». **Запись обещана тогда же и не сделана; заводится 2026-09-10.
Оплошность исполнителя названа здесь.***

**Что есть.** `structure.json` держит `parent` у каждой страницы, П24 п.2
прямо называет крошки и `BreadcrumbList` тем, что считается **одним обходом**
по этому полю. Значит данные для крошек уже есть и считать их нечем не надо.

**Чего нет.** Компонента. `/assassins-creed-4-black-flag/freedom-cry/` —
второй уровень дерева, и подняться со страницы на родителя нечем: ни крошек,
ни ссылки вверх. В волне 1 таких страниц две.

**Где имя.** В словаре `core/structure/blocks.json` — уровень **«хром»**,
рядом с `site-header` и `footer-columns`. **В `blocks[]` страницы имени нет
и не будет:** П28 закрыл этот вопрос — ось имён другая, крошки не секция
тела, а уровень страницы.

**В сессию 6 не тащится** — решение владельца 2026-09-09.

---

## 18. Полный кадр главной не судья в квадрате стрелки ленты

**Заведено:** 2026-09-10, приёмкой вида сессии 6. **Статус:** открыт, дефект
меры, а не вида.

**Что измерено.** Кадр `main-desktop-full-1440` расходится между двумя
сборками ровно на **702 субпикселя в квадрате 44×44** — это габариты кнопки
«назад» ленты карточек. То же число и тот же квадрат записаны пунктом 4 выше,
но там расхождение было **настоящим**: кнопка погасла с `opacity: 0.35`.
Здесь оно **ложное**.

**Почему ложное — измерение, а не довод.** Съёмка полного кадра сама
перезапускает переход. Три съёмки дали три значения прозрачности одной
и той же кнопки — **0.35, 0.44, 0.53**, — причём **0.53 и 0.44 сняты с одной
сборки**, а `getComputedStyle` непосредственно перед съёмкой каждый раз
отвечал `0.35` при нуле работающих анимаций. Значит значение принадлежит
процедуре съёмки, а не сборке.

**Механика.** `prev.disabled` ставит скрипт `CardRail`, а
`.rail-sec__arrow` несёт `transition: opacity 180ms`. Полнокадровая съёмка
меняет вьюпорт, `resize` зовёт `sync()`, и снимок ложится посреди перехода.

**Что это значит для приёмок до починки.** Полный кадр 1440 главной остаётся
судьёй **везде, кроме квадрата 44×44 стрелки**. Расхождение ровно в 702
субпикселя ровно в этом квадрате — не регрессия. Любое другое число или
другое место — регрессия.

**Пути починки, не выбранные.** Гасить переходы на время съёмки
(`animations: 'disabled'` у драйвера, либо `* { transition: none }` вставкой
перед кадром) — правит меру и ничего не говорит о продукте; либо не тянуть
переход на состояние, которое ставится один раз при загрузке, — правит
продукт. Выбор за владельцем: первое дешевле, второе честнее.

---

## 19. Скрипт «ГОТОВНОСТЬ» виснет на ленивых изображениях

**Заведено:** 2026-09-10, приёмкой вида сессии 6. **Статус:** открыт, дефект
меры.

**Что случилось.** `ГОТОВНОСТЬ` из `core/accept/frames.mjs` ждёт события
`load` **у всех** `document.images`, включая ленивые, которые ещё не попали
в поле зрения. У такого изображения `complete` ложно, а `load` не наступит
никогда — до прокрутки. Скрипт повис; вызов оборван по времени ожидания
драйвера.

**Почему раньше не проявлялось.** Порядок протокола ставит `ГОТОВНОСТЬ`
**перед** `ПРОКРУТКА` — то есть ровно в тот момент, когда ленивые изображения
гарантированно не загружены. Прошлые сессии проскакивали случайно: на главной
3 из 11 изображений успевают загрузиться сразу, остальные восемь и есть
ловушка.

**Обход, применённый в сессии** (в исходники не вносился): ожидание накрыто
предельным сроком, и обе части протокола выполнены одним вызовом. Числа
протокола при этом сошлись: после `ПРОКРУТКА` — 11 из 11.

**Починка одной строкой:** ждать `load` только у тех, у кого
`loading !== 'lazy'`, либо накрыть ожидание сроком прямо в скрипте
и печатать, скольких не дождались. Второе честнее: молчаливое ожидание —
тот же класс отказа, что молчаливый `var(--нет-такого)`.

**Подтверждено второй раз 2026-09-12** (кадры пачки 1 P4): вызов
`ГОТОВНОСТЬ` до `ПРОКРУТКА` на `/poradniki/od-czego-zaczac/` повис
на 30 минут до обрыва драйвером — 5 ленивых кадров из 5. Обход тот же:
прокрутка первой, ожидание под сроком 3 с на кадр, число недождавшихся
в ответе (0 из 5). Правка `frames.mjs` — правка судьи: «судью судят»
до приёмки; не заодно.

---

## 20. Возврат `card-rail` игровым и тематическим страницам — со своим содержанием, не с копией `related`

**Заведено:** 2026-09-10 решением владельца при приёмке вида сессии 6, вместе
со снятием блока. **Статус:** открыт, ждёт поля контракта под собственное
содержание ленты.

### Снятие прошло в два захода, и основание у обоих одно

*Второй заход — решение владельца того же дня, после сводки P3.*

**Основание не в типе страницы, а в том, что лента печатает то же, что
`link-list`, и своего содержания у неё нет.** Мерой этого служат два
наблюдаемых признака: запись имеет `"source": "type-default"` (пришла
из умолчания типа, то есть повода под ней не было) и страница объявляет
`link-list` (ссылки уже покрыты).

| Заход | Кого | Записей |
|---|---:|---:|
| первый — страницы игр | 21 страница типа `game` | **21** |
| второй — где критерий верен | 5 типа `topic` | **5** |
| **итого снято** | | **26** |

**Осталось три объявления, и все три намеренные:**

- **главная** — критерий на ней **не выполняется**, и это проверено, а не
  предположено: её лента несёт поradniki, а `link-list` — нумерацию серии
  (`role: numeracja-serii`). Разное содержание, дублирования нет.
- **`/ezio-auditore/`, вторая запись** — `"source": "manual"`,
  `"confidence": "high"`, `role: the-ezio-collection`. Осознанная лента про
  сборник, поставленная руками. Первая, `type-default`, снята.
- **`/poradniki/`** — см. следующий абзац.

**Расширение за пределы слова «topic» было сделано и откачено в тот же
день, 2026-09-11, решением владельца.** Я снял `card-rail` и у `hub`
`/poradniki/`, назвав это расширением его критерия. Владелец расширение
отклонил: его довод касался страниц, где лента **дублирует `link-list`**;
на `/poradniki/` содержание ленты своё — критерий не выполняется, значит
и решение не применяется. Запись восстановлена дословно на прежнее место
(между `hero-key-art` и `link-columns`), сверено с состоянием до снятия.
Урок записан здесь: измеримые признаки (`type-default` + объявленный
`link-list`) были **достаточным условием для игр и тем**, но не были
**самим критерием** — критерий про содержание, а его измеримая тень
на `hub` дала ложное срабатывание.

**Что снято и почему.** Страница `/assassins-creed-4-black-flag/` печатала
две секции с одними и теми же четырьмя ссылками: ленту «DOKĄD STĄD»
(`card-rail`) и список «POWIĄZANE STRONY» (`link-list`). Совпадение
не случайное — содержимое ленты писалось руками и повторило поле `related`
структуры адрес в адрес.

**Решение владельца:** `card-rail` снят из `blocks[]` **страниц типа `game`** —
21 страница из 30. Осталось у `home` (1), `topic` (5) и `hub` (1), итого **7**.

**Довод, снятый с данных, а не из вкуса.** У страниц игр `card-rail` стоял
с пометкой `"source": "type-default", "confidence": "low"` — то есть пришёл
из умолчания типа, а не из измерения корпуса. Все прочие блоки этой страницы,
кроме `hero-key-art` и `cta-band`, опираются на анатомию с уверенностью
`medium`/`high`. Снят блок с самой слабой опорой.

**Что снято вместе с ним.** Поле `cards` из
`src/content/tresc/assassins-creed-4-black-flag.md` — иначе в контенте осталась
бы данность без потребителя, то есть тихая заглушка. На её месте стоит
комментарий, называющий причину и этот пункт. Четыре снятых адреса:
`/assassins-creed-4-black-flag/freedom-cry/`, `/assassins-creed-rogue/`,
`/assassins-creed-3/`, `/assassins-creed-pirates/` — они же дословно лежат
в `related` этой страницы, восстанавливать их неоткуда не надо.

**Условие возврата — ровно одно: у ленты должно появиться СВОЁ содержание.**
Не «соседние части», которые уже перечисляет `link-list`, а что-то, чего
в `related` нет: части по хронологии мира, а не по родству; подборка по эпохе;
поводы вернуться в игру. Такого поля в дереве сегодня нет — значит возврат
идёт вместе с полем, а не раньше него.

**Цена возврата.** Одна запись в `blocks[]` каждой возвращаемой страницы —
игровой или тематической — плюс новое поле контракта под содержание ленты.
Разметка и стили блока никуда не делись: `CardRail` живёт в ядре, печатается
на главной и остаётся в собранном CSS — счёт правил при снятии не изменился
ни в первый заход, ни во второй: **360 → 360**.

**Полдороги к возврату уже пройдено, и это стоит знать.** Поле `cards`
в схеме контента 2026-09-10 стало **списком записей с необязательным
`role`**, а маршрут печатает `card-rail` циклом по вхождениям и подбирает
запись по `role`. То есть механизм «две ленты с разным содержанием на одной
странице» уже работает; недостаёт только самого содержания и решения, какое
оно. Смена формы поля стоила ноль: пользователей у `cards` в тот момент
не было ни одного.

**Что сделано по дороге и чем подтверждено.** Жёсткий список порядка блоков
в `src/pages/[...slug].astro` стал фильтром: до правки константа `oczekiwana`
перечисляла все девять имён и требовала комплект от каждой страницы волны 1 —
она сломалась бы и от снятия блока у `game`, и от его сохранения у `topic`.
Теперь `PORZADEK` говорит только очерёдность, а сверка с `blocks[]` осталась
прежней. Страница игры: 5184 → **4666** px, порядок секций сверен машиной.
Главная: **четыре кадра из четырёх побайтово**, ленту она сохранила.

**Найдено рядом и не тронуто** (инвариант 3): `/ezio-auditore/` (тип `topic`)
объявляет `card-rail` **дважды** в одном `blocks[]`, и второе вхождение
**намеренное** — оно несёт `role`. Заведено пунктом 21 ниже.

---

## 21. Маршрут не умеет второе вхождение блока, а контракт его уже разрешил полем `role`

**Заведено:** 2026-09-10, найдено при снятии `card-rail` со страниц игр.
**Статус: СДЕЛАНО 2026-09-10**, той же датой, первой из трёх стен волны 1
по решению владельца.

**Что сделано.** Тождеством блока на странице стала пара `имя#role`, а не имя:
её считает `kluczOd` в `src/pages/[...slug].astro`. `card-rail` печатается
циклом по вхождениям, содержание подбирается по `role`; поле `cards` в схеме
контента стало списком записей с необязательным `role`. Контракт не тронут —
поле `role` в `blocks[]` уже было. Сверка порядка сравнивает ключи, значит
второе вхождение видно и ей.

**Конкретный случай, с которого пункт завёлся, закрылся иначе.**
У `/ezio-auditore/` осталась **одна** лента: первая, `type-default`, снята
вторым заходом пункта 20 по критерию владельца; вторая, `manual`
с `role: the-ezio-collection`, осталась. То есть механизм готов раньше, чем
понадобился, — и это осознанно: он был условием, без которого страница
не собралась бы вовсе.

**Первая редакция этого пункта была неверной, и это названо здесь, а не
замолчано.** Она гласила «повтор имени — возможный дефект» и предлагала
владельцу развилку «запретить или разрешить». Развилка ложная: **контракт
уже разрешил повтор и уже назвал различитель.** Проверено перебором всех
`blocks[]` тридцати страниц.

**Что на самом деле в файле.** У `/ezio-auditore/` два вхождения `card-rail`
подряд, и они разной природы:

| # | `source` | `confidence` | `role` |
|---|---|---|---|
| первое | `type-default` | `low` | нет |
| второе | `manual` | **`high`** | **`the-ezio-collection`** |

Второе — осознанная лента про сборник, поставленная руками. Поле `role`
встречается в структуре **дважды на весь сайт**: здесь и у `link-list`
главной (`numeracja-serii`). То есть различитель вхождений в контракте есть
и им уже пользуются.

**Настоящий пробел — в маршруте, а не в структуре.** `PORZADEK`
в `src/pages/[...slug].astro` знает каждое имя по одному разу, а сверка
сравнивает списки строкой. На странице с двумя `card-rail` сверка упадёт —
и упадёт **справедливо**: маршрут действительно не умеет напечатать два
разных вхождения одного блока, потому что не умеет взять для каждого своё
содержание. Чинить надо печать, а не данные.

**Что чинится.** Маршрут обязан различать вхождения по `role` и брать
содержание по нему же; сверка порядка — сравнивать пары «имя + role», а не
имена. Контракт при этом не трогается: поле уже есть.

**Второе, помельче, но того же рода.** Первое вхождение у `/ezio-auditore/` —
`type-default`/`low`, то есть ровно тот безосновательный `card-rail`, который
решением владельца снят у всех страниц игр (пункт 20). Здесь он остался,
потому что страница типа `topic`, а решение было про `game`. Снимать ли
`type-default`-ленту у `topic`-страниц — вопрос владельцу, не мой; но
на `/ezio-auditore/` он совпадает с починкой выше и стоит рядом с ней.

**Цена:** правка печати и сверки в маршруте — один файл, без контракта.
Снятие лишней `type-default`-записи, если владелец так решит, — одна строка
структуры.

---

## 22. Запуск `fetch-art.mjs` сегодня скачал бы шесть фото, которых никто не увидит

**Заведено:** 2026-09-10, при выносе инструмента (сессия 7).
**Статус: РЕШЁН 2026-09-10** — поручение владельца решить до волны 1, а не
после. Выбран первый путь; второй отклонён с доводом. Работы под решение
нет: оно запрещает бесполезное действие, а не заводит новое.

### Решение: путь первый — новые имена слотов. Инструмент до этого не запускается

**`fetch-art.mjs` на ac4bf не запускается, пока `art.json` не понесёт
идентификаторы, которых нет в `game-art.json`.** Сегодня их ноль:
`hero, jerozolima, wlochy, japonia, karaiby, londyn` — шесть из шести
совпадают, и разрешатель отдаёт материал издателя.

### Оба пути с ценой, как велено

| Путь | Что меняется | Цена |
|---|---|---|
| **1. Новые имена слотов** (**выбран**) | записи в `art.json` с идентификаторами, которых нет у издателя: виды эпох под галерею, а не под первый экран | записи в манифесте плюс запуск. **Пересъёмки нет:** новые слоты кормят новые места, а первый экран и панели эпох не трогаются. Риск для замороженного эталона — нулевой |
| 2. Фото Commons вытесняет материал издателя | правка `media.ts`: `foto/` перестаёт уступать `gry/` | правка разрешателя **плюс пересъёмка эталона целиком**: сменится первый экран и все пять панелей эпох главной. Это не пересъёмка, а **решение о виде** — и список П32 закрыт владельцем, пополнению не подлежит |

**Довод, по которому второй путь отклонён, — не только цена.** Сайт
о серии игр, и панели эпох показывают ключевой арт самих игр:
`game-art.json` называет игру у каждого слота (`Shadows`, `AC 2`,
`Black Flag`, `Syndicate`). Заменить их стоковыми историческими видами —
Иерусалим, купол Флоренции, японский замок — значит потерять ровно то, что
делает сайт похожим на сайт об играх. Перекрытие `gry/` над `foto/`
в `media.ts` — не дефект, а записанное предпочтение, и оно верное.

**Что из этого следует для `art.json`.** Шесть нынешних слотов — мёртвые
имена: пока они называются как ключи издателя, ни один из них не покажется.
Переименование или замена — редакционная работа волны 1, вместе с тем, какие
кадры странице вообще нужны. Здесь она не выдумывается.

**Остаток закрыт 2026-09-11 заходом за артом пачки 0 P4 (П42).** Шесть
мёртвых имён заменены восемью живыми: пять видов эпох получили приставку
`foto-` (`foto-jerozolima` … `foto-londyn`) — кадры под галереи страниц игр
этой эпохи; слот `hero` снят (первый экран главной — арт игры); три новых
слота — первый экран страниц игр, которых нет в Steam: `assassins-creed-bloodlines`
(замок Кирения, Кипр), `assassins-creed-pirates` (фрегат под парусами),
`discovery` (Альгамбра). `npm run art` скачал восемь кадров Commons
(CC0 / CC BY / CC BY-SA / Public domain) в `src/assets/foto/`, атрибуции —
в `art-credits.json`; два кадра пересняты после взгляда (архивный
чёрно-белый HABS и двор с колодцем — запросы уточнены, `panorama` и `HABS`
в `avoid`). Пересечений с ключами `game-art.json` — ноль, проверено.
Диффы от этого — блок «Zdjęcia» в подвале всех страниц — узаконены
дополнением к П40 и записаны в реестр `_baseline/README.md`.

**Материал издателя для 13 игр** — той же пачкой: `games.json` получил
слоты по имени страницы с `kind: keyart` (ключевой арт библиотеки Steam;
где его нет — снимок экрана [0], проверенный глазами на HUD: AC III Remastered,
Revelations, Liberation HD, Chronicles, Freedom Cry, Dawn of Ragnarök),
`game-art.json` — 19 записей, каждая с классом лицензии (`licencja`),
подвал печатает класс отдельной строкой. Итого арт есть у **всех 21 страниц
игр**: 5 эпох + 13 Steam + 3 Commons.

**Сторож, не заведённый намеренно.** Проверку «ни один идентификатор
`art.json` не встречается в `game-art.json`» можно поставить в гейт ресурсов
сайта. Сегодня она была бы **красной на шести слотах из шести** и уронила бы
сборку, ничего при этом не починив. Заводить её стоит вместе с новыми
именами — тогда она сразу зелёная и дальше сторожит.

### Следствие остатка — записано заранее, 2026-09-11, по указанию владельца

**Когда в волне 1 имена в `art.json` починят и `fetch-art.mjs` запустится,
в выводе появятся фото. Эти диффы эталонов законны и маппятся на остаток
этого пункта.** Записано сейчас, чтобы будущий запуск не споткнулся
о правило П40 «дифф вне списка — стоп»: список события закрыт, а этот дифф
приходит после события и относится сюда.

**Что именно изменится, и это надо знать до запуска, а не после:**

| Где | Что | На каких страницах |
|---|---|---|
| новые слоты галереи | кадры в `foto/` под новыми именами, которых нет в `game-art.json` | страницы волны 1, объявляющие `gallery` — 23 |
| **подвал** | `art-credits.json` получит записи, и `SiteFooter.astro:26` напечатает блок атрибуций (`Object.values(artCredits)`) — **на каждой странице, включая главную** | **все**, главная в том числе |

Второе — не побочный эффект, а **обязательство лицензии**: снимки Commons
без атрибуции использовать нельзя. Значит дифф подвала главной при запуске
`fetch-art` **законен и ожидаем**, и это единственный случай, когда правка
без касания к главной меняет её кадры.

**Граница маппинга — узкая:** на остаток 22 маппятся **только** новые кадры
галерей и блок атрибуций подвала. Любой другой дифф того же запуска —
сдвиг сетки, смена первого экрана, панели эпох — **вне маппинга**, стоп
и доклад.

**Что измерено.** Разрешатель `src/data/media.ts:60–61` устроен так:

```ts
const gra = znajdz(gry, slot);
const zdjecie = gra ? undefined : znajdz(foto, slot);
```

Материал Steam из `src/assets/gry/` **закрывает** фото из `src/assets/foto/`
для того же имени слота. У всех шести имён манифеста `art.json` — `hero`,
`jerozolima`, `wlochy`, `japonia`, `karaiby`, `londyn` — файл в `gry/` уже
есть: **6 из 6 перекрыты**, проверено сверкой ключей `game-art.json`
со списком слотов.

**Следствие, названное числом.** `npm run art` скачал бы шесть снимков
в `foto/`, ни один из которых не попал бы ни в один кадр, и дописал бы шесть
записей в `art-credits.json` — то есть **шесть атрибуций в подвале
к невидимым фотографиям**. Подвал печатает их через
`SiteFooter.astro:26` (`Object.values(artCredits)`) без всякого условия
о том, показан ли снимок.

**Чем это отменяет ожидание сессии 6.** Её отчёт говорил: «Кадров два, потому
что у сайта два материала по этой игре; `fetch-art.mjs` (сессия 7) принесёт
остальные». Не принесёт: галерея берёт `karaiby` и `karaiby-okladka`, оба
из `gry/`, а новые кадры галереи требуют **новых имён слотов**, а не запуска
инструмента по нынешнему манифесту. Ожидание было записано до того, как
устройство разрешателя измерили.

**Что нужно, чтобы запуск стал осмысленным.** Либо имена слотов, которых нет
в `game-art.json` (эпохальные виды под галерею, а не под первый экран), либо
решение, что фото Commons вытесняет материал издателя, а не наоборот — второе
меняет разрешатель и вид первого экрана, то есть решение владельца.

**Цена:** первый путь — новые записи в `art.json` плюс запуск; второй —
правка `media.ts` и пересъёмка эталона.

---

## 23. Какой блок несёт `h1` на странице без `hero-key-art`

**Заведено:** 2026-09-10, состязательной проверкой правки маршрута.
**Статус: РЕШЁН И ИСПОЛНЕН 2026-09-11 — вариант (а), решение владельца.**
Никакой блок: `<h1>` печатает **страница** — маршрут в обеих ветках
и витрина главной, — а `hero-key-art` получил вместо заголовка место
`tytul` и отказ при его отсутствии. Что заголовок на каждой странице ровно
один, роняет или пропускает сборку `core/gates/after-build.mjs`
на `astro:build:done`; срабатывание доказано отрицательной пробой.
Роль `.t-display` вышла из договора ядра. Пиксель обеих страниц —
побайтовый ноль. Отчёт: `docs/reports/2026-09-11-p3-h1-i-otvety.md`.

*Разбор ниже оставлен как был — по нему видно, из чего выбирали.*

**Что измерено.** `<h1>` во всём репозитории существует ровно в одном
месте — `core/blocks/HeroKeyArt.astro:68`. Проверено grep-ом по `core/`
и по `sites/*/src/`: других вхождений нет.

**Шесть страниц волны 1 не объявляют `hero-key-art`:**
`/ezio-auditore/`, `/assassins-creed-valhalla/eivor/`,
`/poradniki/od-czego-zaczac/`, `/assassins-creed-za-darmo/`,
`/assassins-creed-rodowod/`, `/assassins-creed-ii-wojna-swiatowa/`.

**Как дефект появился и чем он был скрыт.** До 2026-09-10 `hero-key-art`
печатался **безусловно**, поэтому `h1` был всегда — случайно, а не по
устройству. Снятие безусловности (стена 3) было правильным и забрало с собой
единственный заголовок. `core/structure/schema.json:40` требует `h1`
от каждой страницы обязательным и уникальным полем, но гейт структуры
проверяет **поле в договоре**, а не **тег в HTML**, — значит без отдельного
условия шесть страниц собрались бы без заголовка при зелёных гейтах.

**Что сделано сразу:** маршрут отказывается собирать страницу, у которой
ни один печатаемый блок не несёт `h1`, и называет адрес, список блоков
и заголовок из структуры. **Это отказ, а не ответ.**

**Варианты для владельца.**

| Путь | Что меняется | Цена |
|---|---|---|
| **(а) заголовок страницы — уровень страницы, а не блока** | маршрут печатает `<h1>` сам, вне блоков, когда `hero-key-art` не объявлен | правка маршрута плюс место в раскладке; `hero-key-art` продолжает нести свой `h1`, значит нужен запрет двойного |
| **(б) второй блок учится нести `h1`** | например `byline` или новый «page-head» | имя в словарь, блок в ядро, вёрстка |
| **(в) `hero-key-art` объявляется всем тридцати** | правка `blocks[]` шести страниц | контракт; и это ровно тот `type-default`, за который сняли ленту — блок без основания |

**Рекомендую (а):** `h1` уникален по договору и принадлежит **странице**,
а не секции. Сегодняшняя привязка к блоку — наследство того, что первым
блоком всегда был герой.

---

## 24. Якоря `#zejscie` и `#katalog` в шапке мертвы везде, кроме главной

**Заведено:** 2026-09-10, найдено при проверке битых якорей на странице
волны 1. **Статус: исполнено 2026-09-11, вторым шагом события
перебазирования (П40).** `src/data/site.ts`: `#zejscie` → `/#zejscie`,
`#katalog` → `/#katalog`; той же правкой `anchors()` подключён
в `astro.config.mjs`. Сборка зелёная с первого запуска: `kotwice: 2 strony,
każda kotwica ma cel, żaden id się nie powtarza`. Манифест: изменились ровно
два файла, оба HTML, диффа — ровно четыре `href` на страницу (шапка
и выдвижное меню, по два). Пиксель главной: шесть кадров из семи побайтово
против сборки до правки, седьмой — известный не-судья пункта 18;
геометрия совпала. **Отрицательная проба:** голые якоря возвращены на время →
`exit=1`, отказ назвал ровно четыре якоря `/assassins-creed-4-black-flag/`,
главная чиста; проба снята. Ниже — состояние до починки, оставлено как есть.
**Решение владельца 2026-09-11 — абсолютные
адреса, чинится в сессии перебазирования, до съёмки эталонов.** Довод:
это правка хрома, то есть вида всех страниц; переснимать кадры с сотней
битых ссылок в шапке — значит заморозить дефект эталоном. **Дефект
предсуществующий, здесь не тронут** (инвариант 3).

**Сторож готов и ждёт починки, 2026-09-11 (П40).** `core/gates/anchors.mjs`
проверен батареей из 12 образцов (`accept` 63/63) и против настоящего
`dist/`: отказ **ровно на этих четырёх якорях**, главная чиста. **В сборку
не подключён намеренно** — подключение сегодня сделало бы `npm run build`
красным на этом же дефекте. Шаг 24 события: править `src/data/site.ts`
(`#zejscie` → `/#zejscie`, `#katalog` → `/#katalog`) **и той же правкой**
добавить `anchors()` в `astro.config.mjs` — гейт зелёный с первой сборки
и дальше сторожит все тридцать.

**Что измерено.** На `/assassins-creed-4-black-flag/` четыре ссылки ведут
в никуда: `#zejscie` дважды и `#katalog` дважды (шапка и выдвижное меню).
Проверено в браузере: `document.querySelector(href)` даёт `null` у всех
четырёх.

**Откуда.** `src/data/site.ts:19–20` — пункты меню `Epoki` → `#zejscie`
и `Poradniki` → `#katalog`. Оставлены чисткой хрома 5а (коммит `9bf5303`)
с доводом, записанным там же: «kotwice `#zejscie` i `#katalog` zostają,
bo to miejsca na stronie, nie adresy». Довод верен **для главной** —
и только для неё.

**Цена ошибки растёт с волной 1:** сегодня битых ссылок четыре на одной
странице, на тридцати их будет под сотню, и все в шапке — то есть на каждом
экране.

**Пути.** Либо адреса становятся абсолютными (`/#zejscie`, `/#katalog`) —
одна строка, и с любой страницы ведут на главную к нужному месту; либо
пункты меню меняются на адреса разделов дерева. Первое дешевле и сохраняет
замысел «места на странице»; второе честнее к навигации по дереву.
**Рекомендую первое.**

---

## 25. Полный кадр страницы игры не судья во втором кадре галереи

**Заведено:** 2026-09-11, при съёмке эталона `/assassins-creed-4-black-flag/`
(шаг 3 события П40). **Статус:** открыт, дефект меры — тот же класс, что
пункт 18 у главной. **В объёме события не чинится** (инвариант 3).

**Что измерено.** Два прогона съёмки на одной сборке `574ffa0` тем же
драйвером: три оконных кадра — побайтово, а оба полных кадра расходятся
**ровно в прямоугольнике одного `<img>`** внутри `.foto.foto--gra` второго
кадра галереи: на 1440 — 574×358 от (733, 2294), 163 144 субпикселя
(0,816 %); на 390 — 356×222 от (17, 2794), 63 096 (0,915 %). Отклонение
в основном 1 (148 025 из 163 144), максимум 19 и 23 из 255. Прямоугольник
совпадает с коробкой картинки до пикселя — локализовано, не догадка.

**Что это значит для приёмки.** Полный кадр остаётся сторожем за пределами
прямоугольника; внутри него расхождение не судится. Записано
в `_baseline/README.md` при эталоне. Оконный кадр галереи по якорю
(`#galeria-tytul`) дал бы этому блоку судью — но состав эталона страницы
решает владелец, не сессия.

**Чего не сделано.** Причина не названа: у главной та же нестабильность
была у кнопки-стрелки ленты, здесь — у картинки при съёмке вне окна
(`captureBeyondViewport`). Общее у обоих — растеризация элемента, которого
в окне нет. Проверить гипотезу (снять прямоугольник оконным кадром и сравнить
два прогона) — отдельной сессией.

**Дополнение 2026-09-11, вечер — гипотеза «вне окна» неполна.** Оконный кадр
ряда рассказа (`baseline-desktop-layer-co-jest-w-grze`) в **одном прогоне
из шести** разошёлся с остальными на 138 124 субпикселя (3,55 %, максимум 13)
ровно в прямоугольнике картинки `.foto--gra` 559×345 от (748, 277); пять
других прогонов — побайтово. Значит дрожит картинка, а не способ съёмки.
Второй факт: картинки галереи выбирают кандидата 432 px в коробку 574 px
(`sizes` галереи — 30vw), то есть **растягиваются** — растяжение с фильтрацией
и есть кандидат в источник дрожания полного кадра. Правило README
«сперва контроль тем же драйвером» покрывает оба случая; чинить — вне события.

**Дополнение 2026-09-11, вечер — гипотеза «вне окна» неполна.** Оконный кадр
ряда рассказа (`baseline-desktop-layer-co-jest-w-grze`) в **одном прогоне
из шести** разошёлся с остальными на 138 124 субпикселя (3,55 %, максимум 13)
ровно в прямоугольнике картинки `.foto--gra` 559×345 от (748, 277); пять
других прогонов — побайтово. Значит дрожит картинка, а не способ съёмки.
Второй факт: картинки галереи выбирают кандидата 432 px в коробку 574 px
(`sizes` галереи — 30vw), то есть **растягиваются** — растяжение с фильтрацией
и есть кандидат в источник дрожания полного кадра. Правило README
«сперва контроль тем же драйвером» покрывает оба случая; чинить — вне события.

---

## 26. `check` возвращает 0 при `СДВИГ` — вердикт живёт в прозе и `geometria.json`

**Заведено:** 2026-09-11 строкой по указанию владельца (П41), из находки
состязательной проверки судьи `frames.mjs`. **Статус:** открыт; **после
события перебазирования**, не внутри.

**Что есть.** `npm run accept:frames -- check` печатает `ГЕОМЕТРИЯ: сдвигов N`
и строки `СДВИГ …`, но завершается кодом `0` — по правилу
`docs/11_PRIEMKA.md`: у инструментов приёмки ненулевой код значит «инструмент
не смог», а не «расхождение». Сторожа сборки (`after-build`, `anchors`)
живут по другому духу: `exit≠0` — регрессия. Драйвер, читающий только код
возврата, `СДВИГ` и «сдвиг числом НЕ НАЗВАН» пропустит.

**Что решить.** Выровнять с духом сторожей: либо ненулевой код при
«НЕ НАЗВАН» (как минимум) и при `СДВИГ` — с оговоркой, что `СДВИГ`
законен на шаге 3 протокола и тогда код обязан читаться иначе, чем у гейтов;
либо записать в `11_PRIEMKA.md` явно, что `check` — только печать, а доклад
цитирует строку `ГЕОМЕТРИЯ` дословно (сегодня так и записано в README).
Решение меняет правило приёмки — владельца.

**Приписка 2026-09-12 (П45).** Пиксели теперь судятся кодом: `check`
сверяет каждую пару кадров по зонам `szum.json` эталона (`core/accept/szum.mjs`)
и завершается **`exit 1` при субпикселе вне допуска**, разошедшихся размерах
или не снятом кадре — второе исключение из правила `11_PRIEMKA.md`. **`СДВИГ`
геометрии код по-прежнему не меняет** — этот пункт открыт как был: сдвиг
законен на шаге 3 протокола, и решение о его коде — владельца. Код 0 даёт
только сам разбор геометрии; тот же сдвиг обычно приходит `exit 1` через
пиксели (разошедшиеся размеры полного кадра, дифф оконного).

---

## 27. Страница `/prywatnosc/` — ранним пунктом очереди текстов волны 1

**Заведено:** 2026-09-11 по указанию владельца (П41, дополнение). **Статус:**
открыт; **не в событии перебазирования** — в очереди текстов волны 1, рано.

**Почему рано.** Подвал каждой страницы несёт ссылку `Polityka prywatności`
→ `/prywatnosc/`, и это единственный адрес вне дерева, оставшийся в сборке
после ответа «подвал: (а)»: три служебные ссылки сняты, эта — обещание,
а не ссылка, снять её значит снять обещание. Страницы в структуре нет
(`structure.json` — 30 страниц, приватности среди них нет), значит сперва
запись в структуру (контракт — владелец), потом текст.

**Что нужно.** Тип страницы для служебных текстов (в словаре типов сегодня
`home`, `game`, `topic`, `guide`, `hub`, `map`), `blocks[]` для неё (скорее
всего `byline` и текст без `hero-key-art`), юридическое содержание —
от владельца или по его шаблону. После записи в структуру гейт якорей
и `getPage` перестанут видеть в подвале призрак сами.

**Исполнен 2026-09-11 пачкой 0 P4 (П42):** тип `legal`, запись в структуре,
текст по шаблону RODO без аналитики. **Остаток — контакт администратора
данных:** назван владельцем 2026-09-11 (П43 п.4: Jakub,
jakub@ac4bf-thewatch.com) с условием — до внесения подтвердить, что ящик
заведён и куда пересылается. Внесение отдельным заходом; вопрос в докладе
пачки 1.

**Внесён 2026-09-12 отдельным заходом (П44: «ящик заведён»):** абзац
в разделе 01 и отсылка в разделе 07 `prywatnosc.md`, дата подписи сдвинута
на день сборки. Куда пересылается ящик — владелец не назвал; внесено
по прямому указанию. **Пункт закрыт.**

---

## 28. Схема содержания требует поля героя от страницы без героя

**Заведено:** 2026-09-11, при первой странице без `hero-key-art`
(`/prywatnosc/`, пачка 0 P4). **Статус:** открыт, дефект схемы.

`content.config.ts` объявляет `lead`, `primary` и `secondary` обязательными
у каждой страницы, хотя печатает их только `hero-key-art`. Шесть страниц
волны 1 без героя (пять `topic`, `guide`) и `/prywatnosc/` обязаны носить
три поля, которых никто не увидит. У приватности они заполнены осмысленно
(лид — краткое содержание, ссылки — главная и хаб), но это заполнение
ради схемы, не ради страницы. Починка — сделать поля необязательными
и требовать их предикатом `maTresc['hero-key-art']` в маршруте: страница
с героем без лида должна падать, страница без героя — нет. Правка схемы
содержания — контракт содержания, слово владельца.

**Видно на первой странице без героя, 2026-09-12:** `od-czego-zaczac.md`
несёт `lead`/`primary`/`secondary` ради схемы (комментарий в файле);
кадры — `docs/reports/2026-09-12-p4-pachka-1/od-czego-zaczac/`.

---

## 29. Подпись `byline` прижата к левому краю, а не к колонке — на всех страницах

**Заведено:** 2026-09-11, измерено на `/assassins-creed-4-black-flag/`
и `/prywatnosc/` при пачке 0 P4. **Статус:** открыт, дефект вида ядра;
**к взгляду владельца в пачке 1** (первая страница без героя), чинится
после него.

**Измерено на 1440.** `.byline__line` несёт класс `container` (ширина 1240,
`padding-left` 32), но собственное правило блока ставит `margin: 0`
и перекрывает `margin-inline: auto` контейнера: строка начинается с x = 0,
текст — с 32, а колонка страницы — со 132 (на главной с шкалой — со 174).
На странице игры то же: `byline` 879–950, строка от 0. Приёмка вида
сессии 6 этого не поймала: подпись стоит под героем, за пределами первого
экрана и кадра ряда.

**Починка одной строкой** — снять `margin: 0` у `.byline__line` или задать
`margin-inline: auto`; пиксель изменится на всех страницах с подписью
(главная, игра, приватность) — то есть на эталонах. Значит после взгляда
владельца и с записью в реестр, не «заодно».

**Измерено на `/poradniki/od-czego-zaczac/` 2026-09-12:** строка подписи
от x = 0, текст с 32, `h1` и колонка со 132 (на 390: текст 16, колонка 16 —
на телефоне не видно). Кадр:
`docs/reports/2026-09-12-p4-pachka-1/od-czego-zaczac/desktop-hero-1440x900.png`,
подпись на y ≈ 504.

**Исполнен 2026-09-12** (сессия вида, `3a05410`): `margin-block: 0` вместо
`margin: 0` в `core/blocks/Byline.astro` — горизонталь у `.container`.
Пиксель — таблица пересъёмки доклада сессии вида.

---

## 30. `StoryRow` печатает пустую рамку арта у ряда без кадра

**Заведено:** 2026-09-11, `/prywatnosc/` (пачка 0 P4). **Статус:** открыт;
**решение о виде — взгляд владельца в пачке 1**.

`core/blocks/StoryRow.astro` печатает `.layer__media > .layer__art` всегда;
при пустом месте `kadr` остаётся рамка 560×~350 с линией 1 px — измерено:
`kadr_children: 0`, `border 1px`. Ряды без кадра будут у приватности (все
восемь), у `/poradniki/od-czego-zaczac/` и у тем. Варианты: (а) не печатать
`.layer__media`, когда места нет — текст остаётся в левой колонке сетки;
(б) при пустом кадре ряд раскладывать в одну колонку — правка сетки;
(в) давать текстовым рядам запасную графику (`Viewpoint`). Все три —
вид; выбирать глазами на первой странице без героя.

**Измерено на `/poradniki/od-czego-zaczac/` 2026-09-12:** у рядов без
`art` (`dwie-kolejnosci`, `chronologia`, `watek-wspolczesny`, `co-pominac`)
рамка 561×347 с линией 1 px, `kadr_children: 0`; кадр —
`docs/reports/2026-09-12-p4-pachka-1/od-czego-zaczac/desktop-layer-chronologia-bez-kadru-1440x900.png`,
рамка от (747, 276) в окне. Пять рядов с артом эпох (`wlochy`, `jerozolima`,
`karaiby`, `londyn`, `japonia`) — рядом для сравнения обеих форм.

**Решён владельцем 2026-09-12 — вариант (б), исполнен** (`df7085a`): ряд без
кадра — один столбец, медиа-место не печатается; предикат — отрисованное
содержимое места `kadr` (`Astro.slots.render` + trim, не `has()`). Столбец —
реакция блока на пустое место, не тип ряда: появился кадр — две колонки сами
(П44, дословно). Ширина строки тела остаётся ролью `.measure`.

---

## 31. Появилась аналитика → обновить `/prywatnosc/`

**Заведено:** 2026-09-11 по указанию владельца (П42). **Статус:** открыт,
сторожевая строка на будущее.

Текст `/prywatnosc/` (пачка 0 P4) утверждает: аналитики нет, cookies
и хранилища браузера сайт не пишет, внешних запросов при показе нет, данных
от читателя не собирается. Каждое утверждение проверено по сборке
2026-09-11 (в исходниках нет `localStorage`/`cookie`/`fetch`, в HTML нет
внешних скриптов и шрифтов). **Любой из этих фактов меняется — страница
обновляется до выкладки**, и дата подписи над текстом сдвигается: сама
страница это обещает читателю (раздел 08). Механической опоры пока нет;
кандидат — гейт, роняющий сборку при внешнем `<script src>` или `fetch(`
в исходниках, пока страница приватности говорит «нет». Заводить — после
пачки 1, вместе с гейтами текстов.

---

## 32. `/assassins-creed-4-black-flag/` — текст не в коридоре, страница считалась готовой по ошибке

**Заведено:** 2026-09-12, первым прогоном гейта коридора (П43). **Статус:**
открыт; **чего ждём** — слова владельца, в какую пачку P4 ставить текст.

Гейт измерил 1497 знаков без пробелов при коридоре S4 5403–7311. Страница
была стендом шаблона (П29 п.2): один ряд рассказа, два абзаца, вердикт,
две подписи. План P4 считал её «1 готовая» — счёт был по факту файла
содержания, не по коридору. До текста `corridor` страницы — `null`
именованным решением П43; **с текстом коридор 5403–7311 возвращается**,
и это проверяется гейтом, не памятью. Рекомендация: пачка 2 (игры,
на которые смотрит главная) — она и так строится на шаблоне этой
страницы; бриф — `npm run brief -- /assassins-creed-4-black-flag/`
(инструмент сегодня пропускает страницы с файлом содержания при `--all`,
адресом — собирает).

**Исполнен 2026-09-12 пачкой 2** (`92f1d02`): текст дописан до 7075 знаков без
пробелов, `corridor` страницы возвращён в `[5403, 7311]`; гейт зелёный.
**Пункт закрыт.**

---

## 33. Палитра рядов страницы без героя — `karaiby` по умолчанию, независимо от арта ряда

**Заведено:** 2026-09-12, первой страницей без героя
(`/poradniki/od-czego-zaczac/`). **Статус:** открыт, дефект маршрута;
**к взгляду владельца** вместе с 28–30.

`[...slug].astro` красит каждый `StoryRow` модификатором
`layer--${dane.art ?? 'karaiby'}` — от арта **страницы**. У страницы без
героя `art` нет, и все ряды получают палитру Карибов, хотя ряд с артом
`wlochy` показывает Венецию, а ряд с `londyn` — Лондон. На кадре
`desktop-layer-trzy-wejscia-1440x900.png` полоса `band` под артом двойки
тонирована карибским красным. Починка: модификатор от `rzad.art ?? dane.art
?? 'karaiby'` — правка маршрута, вид; после взгляда владельца.

**Поправка по факту, 2026-09-12 (сессия вида, П44 доп.; прежний текст
оставлен):** дефект описан неверно. Правил `.layer--<эпоха>` на страницах
маршрута не было вовсе — они стояли `:global` в `EraLayer.astro` главной
и попадали только в её CSS-чанк; все ряды маршрута, включая Black Flag
с `layer--karaiby`, красились `--era` из `:root`, то есть `--accent`
(брендовый красный), а не «карибским» — карибский цвет бирюзовый, и `.band`
от эпохи не зависит. Починка потребовала переноса пяти правил в `global.css`
(та же дорога, что `.layer__year` шагом 5б) плюс модификатор от эпохи ряда,
затем страницы, **без умолчания**; страница игры при этом получила бирюзовый
надзаголовок «2013» — именованная строка таблицы пересъёмки. **Исполнен
2026-09-12** (`1397410`).

---

## 34. Карточки ленты из файла содержания печатаются без кадра — маршрут не разрешает их медиа

**Заведено:** 2026-09-12, хабом `/poradniki/`. **Статус:** открыт, дефект
маршрута; **к взгляду владельца** (лента хаба — «глаза», план P4).

Схема `cards[].items[]` знает `href`, `title`, `kind`, `place` — без арта;
маршрут отдаёт их `CardRail` как есть, и ядро по своему правилу рисует
карточку без медиа-места вовсе (не пустой рамкой — пункт 3). На главной
`GuideRail` разрешает кадр эпохи через `mediaFor`, на маршруте — никто.
Четыре карточки хаба стоят текстом на плашке
(`desktop-blok-dokad-1440x900.png`). Починка: поле `art` у карточки
в схеме плюс `mediaFor` в маршруте, по образцу `GuideRail`; либо решение,
что карточки поradników без кадра — вид. Первая карточка стоит на x = 124
при колонке 132 — та же природа, что пункт 5 на главной, там выровнено
П41 ответ 4; проверить, дошло ли выравнивание до маршрута.

**Часть А исполнена 2026-09-12** (`b2445ae`): поле `art` у карточки, маршрут
разрешает кадр через `mediaFor`; палитры карточки нет (правила
`.rail-sec__item--*` до маршрута не доходят — тот же класс, что 33).
**Часть Б (8 px) — не в объёме, пункт 38.** Неизвестный ключ `art` молчит —
пункт 40.

---

## 35. Пункт меню «Rodowód serii» ведёт на страницу о фильме «Rodowód»

**Заведено:** 2026-09-12, при подборе карточек хаба. **Статус:** открыт;
**чего ждём** — решения владельца о ярлыке (главная под эталоном).

`src/data/site.ts` `nav`: «Rodowód serii» → `/assassins-creed-rodowod/`.
В структуре у этого адреса `h1` «Rodowód, czyli film o ojcu Ezia
z Assassin’s Creed II» — страница о короткометражном фильме 2009 года
(Lineage), не о родословной серии. Ярлык обещает одно, страница даёт
другое. Замена ярлыка — правка шапки на всех страницах, пиксель эталонов
обеих страниц; решение владельца. Кандидат: «Film Rodowód» или снять
пункт до текста страницы.

**Решён владельцем 2026-09-12 — «Film Rodowód», исполнен** (`e630ee7`)
для меню; подвал несёт прежний ярлык — пункт 39.

---

## 36. Длинный надзаголовок ряда (`year`) в роли `t-year` переносится на две строки

**Заведено:** 2026-09-12, проводником. **Статус:** наблюдение, правило
содержания; починки в коде не требует.

Поле `year` ряда печатается ролью `t-year` — крупный красный дисплей под
четыре цифры. На странице без героя ряды не всегда о годе: «od 431 p.n.e.»
(13 знаков) переносился в две строки и налезал на заголовок. Мера: на 1440
в одну строку помещается **до 9 знаков** («2007–2025»). Правило для
текстов: `year` ≤ 9 знаков; в проводнике заменено на «2017–2025»,
«Oś czasu», «Poboczne». Если рядам без года нужен длинный надзаголовок —
это новая роль, не растяжение `t-year`.

---

## 37. Карта игра → эпоха для палитры рядов страниц игр пачки 2

**Заведено:** 2026-09-12, сессией вида (П44 доп.). **Статус:** открыт;
**чего ждём** — пачки 2: решение о виде страниц игр вне пяти эпох.

Модификатор палитры ряда ставится от эпохи ряда, затем страницы, **без
умолчания** (`[...slug].astro`, `paletaRzedu`): ключ арта вне словаря
`stronaEpoki` (слаг игры `assassins-creed-3`, `foto-*`) палитры не даёт —
ряд остаётся в `--accent`. Из 13 игр пачки 2 с артом по слагу ни одна не
несёт эпоху: AC3, Rogue, Liberation, Freedom Cry могли бы взять `karaiby`
(Новый Свет), Brotherhood, Revelations — `wlochy`, Unity — ?, Origins,
Odyssey, Valhalla, Mirage — эпохи нет вовсе. Карта игра → эпоха — словарь
сайта (`site.ts`), решение о виде; до неё страницы пачки 2 красятся accent,
как красились до сессии вида все страницы маршрута.

---

## 38. Первая карточка ленты на маршруте стоит на x = 124 при колонке 132

**Заведено:** 2026-09-12, из пункта 34 (часть Б). **Статус:** открыт;
**чего ждём** — отдельной правки ядра с судьёй-главной.

На хабе `/poradniki/` первая карточка ленты — x = 124, заголовок ленты —
132: 8 px. На главной ту же природу (пункт 5) выровняли П41 ответ 4
(`scroll-padding-left`). Кандидат из разведки: распорка `CardRail.astro`
`- var(--gutter)` → `- var(--space-md)` — правка **ядра**, судья — лента
главной (`blok-swieze`, полные кадры); в закрытый список сессии вида не
входила (П44). Мерить до/после на обеих страницах; комментарий
`CardRail.astro:262` по П18 не переписывается.

---

## 39. Подвал несёт ярлык «Rodowód serii» на страницу о фильме

**Заведено:** 2026-09-12, из пункта 35. **Статус:** открыт; **чего ждём** —
слова владельца (подвал — во всех кадрах обеих эталонных страниц).

`SiteFooter.astro:47`: колонка «Poradniki», ссылка «Rodowód serii» →
`/assassins-creed-rodowod/`. Меню исправлено на «Film Rodowód» (пункт 35),
подвал — нет: список П44 закрыт. Пока текста страницы нет, шапка и подвал
называют один адрес по-разному. Правка — дифф подвала: полные кадры обеих
страниц, `blok-cta` игры (кадр судит и подвал под призывом), мобильные
полные.

---

## 40. Неизвестный ключ `art` карточки ленты молчит

**Заведено:** 2026-09-12, из пункта 34. **Статус:** открыт, сторожевая
строка; **чего ждём** — гейта ресурсов на ключи арта содержания.

`kartyDla` в маршруте: `mediaFor(k.art)` без файла в `gry/` и `foto/`
возвращает `src: undefined`, карточка печатается без медиа-места при зелёной
сборке — та же слепота, что у галереи (П13: пустое медиа-место — сигнал
незакрытого входа). Кандидат: проверка «каждый `art` файла содержания
(`art`, `rows[].art`, `gallery.items[].art`, `cards[].items[].art`)
разрешается в файл» — в `check-assets.mjs` или новый сторож; судью судят.

---

## 41. Герой хаба — дуотон Commons в `--accent`, а не в `--accent-2`

**Заведено:** 2026-09-12, разведкой пункта 34. **Статус:** наблюдение,
решение о виде; **чего ждём** — взгляда владельца на хаб после пересъёмки.

Правило `:global(.hero__art) { --era: var(--accent-2) }` (золото) живёт
в `Hero.astro` **главной**; маршрут печатает `HeroKeyArt` ядра напрямую,
и герой хаба `/poradniki/` (Commons `poradniki`, панорама Иерусалима)
идёт дуотоном в `--accent` (красный) — как и кадры Commons на карточках
ленты. Тот же класс, что 33: правило страницы не доходит до маршрута.
Если золото — замысел для всех героев с фото, правило переезжает
в `global.css`; если только для главной — оставить.

---

## 42. Статья «która najlepsza» для хаба `/poradniki/` — по данным Search Console волны 2

**Заведено:** 2026-09-12 по указанию владельца (П44). **Статус:** отложен;
**чего ждём** — данных Search Console по кластеру «najlepszy assassin creed»
после выкладки волны 1.

Хаб отвечает на «która najlepsza» раскладкой четырёх колонок, `blocks[]`
без блока тела, `corridor: null` (П43). Ниша со звёздочкой мерила
статьи-рейтинги (3900–5276). Если запросы кластера покажут спрос на текст —
`story-row` в `blocks[]` хаба (контракт, владелец) и статья словами
без балла (П28); коридор — заново по нише.
