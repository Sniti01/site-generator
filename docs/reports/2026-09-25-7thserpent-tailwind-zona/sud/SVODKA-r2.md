# «Судью судят», раунд 2 — сессия 13 (зона Tailwind)

Судили: правки инструментов по раунду 1 — коммит 6df34df (sverka-css.mjs, markery.mjs, stili-gen.mjs) и материалы второго прохода маркеров (zamery/v2/). Узкий раунд: закрывают ли правки находки раунда 1 и не внесли ли новых дефектов. Воркфлоу: 3 линзы, скептик на каждую (6 агентов).

**Итог:** находок 16; по итогу скептиков — дефектов 3, пределов 8, прозы 5, опровергнуто (итог «нет») 0.

## Линза «css»

git status линзы: `?? docs/reports/2026-09-25-7thserpent-tailwind-zona/sud/`; скептика: `?? docs/reports/2026-09-25-7thserpent-tailwind-zona/sud/`

**Закрыты правками прошлого раунда (проверено опытом):**

- css-1: на копиях настоящих сборок все варианты дают отказ. Взятый из main index.html «после» (c1a) и оба HTML из main (c1b) — «в «после» осталось старое имя листа LinkList.DHeL5m7t.css … новое 0 раз … ведёт мимо листов». Ссылка на несуществующий LinkList.ZZZZZZZZ.css (c1c) — тоже отказ. Битая ссылка без смены имени, одинаковая в обеих сборках (c1d), — отказ по <link>. Устаревший HTML без кавычек, с «preload stylesheet» и с одинарными кавычками — отказ побайтной проверкой имён (opyty3.log). Остаток — форма с сущностью и без кавычек, см. r2-css-3.
- css-2: опыты Д1 (\'), Д1 с \" и Д2 (\} и \{) на настоящем LinkList дают отказ. Ушедшим инструмент называет `[data-q=';.lowercase{…}\''] .c` или `@media print › .a\}.lowercase`, слой 21 → 21, «ждали уменьшения ровно на 1». Прежде было ложное «сверено». Экранированная косая перед * (`.a\/*{…}`) верно не открывает комментарий: css-tree тоже видит 3 правила.
- css-3: шапка (стр. 29–32) теперь называет \', \{, \; вне строк частью текста, а не разбора, и это верно. Опыт c5: пять правил Tailwind с кавычками (content-[''], ['a;b'], ['{'], ['}'], before:content-['x']) разобраны в пять верных селекторов.
- css-4: оба случая дают отказ «не валидный UTF-8»: Д3 (index.css, EF BF BD до и FF после) и Д3б (index.html, EF BF BD против FE). Сравнение действительно идёт по Buffer: лист или HTML с BOM только в «до» (LinkList, неизменный index.css, index.html) даёт отказ «побайтно — не равен» или «различаются», хотя TextDecoder снимает BOM и тексты равны.
- css-5 (предел, задет правкой css-2): одинаковые в обеих сборках правила Tailwind с кавычками теперь дают «сверено», слой 25 → 24. Прежде был отказ «незакрытая строка».
- css-6: Н-и (@layer utilities{.lowercase…} в index.css обеих сборок) → отказ «правило с прелюдией .lowercase есть в «после» (1 лист.)». @media print{.lowercase…} → отказ. --schet 21:20 и 20:18 на настоящих сборках → отказ, 20:19 → сверено. Остаток — вложенность и блок с одним комментарием, см. r2-css-1.
- css-8: при --sloy '@layer components' и уходе .q{color:red} — «сверено», слой 2 → 1, счёт идёт по заданной обёртке. Без --sloy — отказ «@layer utilities: 19 → 19». --sloy components с --schet 20:19 — отказ «ждали 20 → 19». На настоящих сборках --sloy '@layer components' — отказ «0 → 0».

| id | находка | класс линзы | скептик | итог |
|---|---|---|---|---|
| r2-css-1 | Проверка 5 «ушедшего нет в «после»» не видит прелюдию у блока без собственных объявлений: вложенность CSS или комментарий дают «ушедшего в «после» — 0» и «сверено» | дефект | подтверждено | **дефект** |
| r2-css-2 | Пустое значение флага принимается за отсутствие флага: --schet '' молча отключает сверку чисел, --ushlo '' и --sloy '' молча подставляют умолчания | дефект | подтверждено | **дефект** |
| r2-css-3 | Проверка <link rel=stylesheet href> узка по синтаксису и считает путь от корня сборки; имена листов заменяются по имени файла без папки | предел | предел | **предел** |
| r2-css-4 | «Прямых детей: правило или @media — по одному» — счёт слоя включает и комментарии | проза | подтверждено | **проза** |
| r2-css-5 | Шапка: «неверные аргументы — код 2», но неверный --ushlo и несуществующая папка дают необработанное исключение и код 1; отказ на невалидном UTF-8 в HTML назван только для листов | проза | подтверждено | **проза** |

### r2-css-1 — Проверка 5 «ушедшего нет в «после»» не видит прелюдию у блока без собственных объявлений: вложенность CSS или комментарий дают «ушедшего в «после» — 0» и «сверено»

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/sverka-css.mjs:235 (`lvPosle.filter(l => l.prelude === ozh.prelude)`), :100-118 (listya: прелюдия у листа есть только у группы объявлений и у пустого блока, у komm и instr она ''), шапка :20 («ни в каком обёртывании»)

**Суть:** Правка css-6 ищет ушедшее правило в «после» по листьям, а не по блокам дерева. Если у блока с прелюдией `.lowercase` в «после» нет собственных объявлений, а внутри вложенный блок (`&:hover{…}`, `@media print{…}`) или только комментарий, листа с прелюдией `.lowercase` не возникает. Тогда инструмент печатает «ушедшего в «после» — 0» и «сверено». Шапка при этом обещает, что прелюдии нет «ни в каком обёртывании». Это ложное «сверено» по собственному критерию инструмента, того же класса «объявленное ≠ напечатанное». Тяжесть низкая: нужна вложенность CSS. На итог сессии 13 не влияет: в листах «после» «lowercase» встречается 0 раз, «&» — 0 раз во всех четырёх листах обеих сборок. Как починить: обходить дерево razobrat() и считать каждый blok с prelude === ozh.prelude на любой глубине.

**Доказательство:** Все опыты — на копиях do0 и po0 в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/css/. Журнал opyty2.log (opyty2.mjs), в index.css обеих сборок дописано: `.lowercase{@media print{text-transform:lowercase}}` → «сверено; ушедшего в «после» по index — 0», хотя объявление text-transform:lowercase для .lowercase в «после» осталось; `.lowercase{&:hover{text-transform:lowercase}}` → «сверено; 0»; `.lowercase{/*x*/}` → «сверено; 0»; `@media print{.lowercase{/*x*/}}` → «сверено; 0». Контроль: `.lowercase{}` и `.lowercase{text-transform:lowercase;&:hover{color:red}}` → «отказ; правило с прелюдией .lowercase есть в «после»». Строка запуска: node sverka-css.mjs do-n po-n (папки готовит gotovit.mjs) → «_astro/index: … ушедшего в «после» — 0 / ВЕРДИКТ: сверено», код 0. Независимо css-tree из node_modules репозитория (nezavisimo.mjs) находит в «после» правило с прелюдией `.lowercase` (правила [.lowercase \| &:hover]).

**Скептик — подтверждено, итог «дефект»:** Опровергнуть не удалось, воспроизведено в памяти на реальных dist-main и dist-posle: скрипт C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/css-skeptik/proba.mjs, раздел 1, журнал proba-1.log. В этом опыте одинаковая вставка дописывалась в index.xofYExtq.css обеих сборок. Шесть вставок дали «сверено; ушедшего в «после» по index 0». A1 `.lowercase{@media print{text-transform:lowercase}}`. A2 `.lowercase{&:hover{…}}`. A3 `.lowercase{/*x*/}`. A4 `@media print{.lowercase{/*x*/}}`: прелюдия обёрнута в @media, то есть случай ровно из обещания шапки :20 «ни в каком обёртывании». A5 `@layer utilities{.lowercase{&{text-transform:lowercase}}}`: по смыслу это то самое ушедшее правило в том же слое, записанное через `&`, и при этом «сверено; слой 21→20». A6 `@media print{.lowercase{@supports …{…}}}`. Контроли K1 `.lowercase{}`, K2, K3 `@media print{.lowercase{text-transform:lowercase}}` и K4 (объявление после вложенного блока) дают отказ «правило с прелюдией .lowercase есть в «после»». Из командной строки (папки do-n и po-n, cli.log) то же: «ВЕРДИКТ: сверено», код 0. Причина — в коде: listya() даёт лист с prelude блока только группе собственных объявлений или пустому блоку, а у komm prelude = '', у вложенного блока — своя прелюдия (`.lowercase › @media print` с prelude «@media print»). Строка :235 сверяет только поле prelude листа. Проверку 5 добавили правкой раунда 1, так что это новый дефект правки: ложное «сверено» по критерию, который инструмент сам объявил. Тяжесть низкая. Сборка Astro/Tailwind понижает вложенность под свои цели, а в реальных листах обеих сборок «&» встречается 0 раз, «lowercase» в «после» 0 раз (LinkList.B4mIKaMJ.css и index.xofYExtq.css), в «до» 2 раза. Итог сессии 13 не меняется. Починка из находки верна: считать каждый blok дерева с prelude === ozh.prelude на любой глубине. Ею закрываются A1–A6, и K1–K4 остаются отказом. Отдельно, вне находки: `.lowercase,.x{…}` (список селекторов) тоже даёт «сверено» (X1). Буквально это не противоречит «его прелюдии нет»: прелюдия там другая.

### r2-css-2 — Пустое значение флага принимается за отсутствие флага: --schet '' молча отключает сверку чисел, --ushlo '' и --sloy '' молча подставляют умолчания

**Где:** sverka-css.mjs:433 (проверяется только `args[i + 1] === undefined`), :439-441 и :456 (проверка на истинность: `if (opt['--ushlo'])`, `if (opt['--sloy'])`, `if (opt['--schet'])`, `if (opt['--json'])`); шапка :28 («неверные аргументы — код 2»)

**Суть:** После правки разбора аргументов неизвестный флаг и флаг без значения в конце строки дают код 2. Но флаг с пустым значением проходит проверку и дальше считается отсутствующим. `--schet ''` убирает сверку абсолютных чисел, и инструмент выдаёт «сверено» (код 0) там, где `--schet 20:19` даёт отказ. Непустое неверное значение (`--schet abc`) даёт код 2. `--ushlo ''` молча сверяет `.lowercase`, `--sloy ''` молча берёт `@layer utilities`, `--json ''` молча не пишет файл. Правдоподобный путь — вызов из сценария с `--schet "$N"` при пустой переменной; это тот же класс, что zona-1 (опечатка во флаге молча убирает проверку). Тяжесть низкая: остальные проверки выполняются. На итог сессии 13 не влияет: вывод v2 печатает «20 → 19», это совпадает с П87 п. 3 и с моим прогоном `--schet 20:19` (сверено). Как починить: проверять `=== ''` вместе с undefined и давать код 2.

**Доказательство:** Папки do-k и po-k (gotovit.mjs) — копии do0 и po0, в которых комментарий /*k*/ стоит прямым ребёнком @layer utilities, так что слой 21 → 20. node sverka-css.mjs do-k po-k --schet 20:19 → «ОТКАЗ: слой @layer utilities: 21 → 20, по --schet ждали 20 → 19 / ВЕРДИКТ: отказ», код 1. node sverka-css.mjs do-k po-k --schet '' → «… 21 → 20 … ВЕРДИКТ: сверено», код 0. node sverka-css.mjs do0 po0 --schet abc → «--schet: ждали <до>:<после>», код 2. node sverka-css.mjs do0 po0 --sloy '' → «ВЕРДИКТ: сверено», код 0, а с `--sloy '@layer components'` → отказ «слой @layer components: 0 → 0». `--ushlo ''` → сверено, код 0. `--json ''` → сверено, код 0, файл не записан.

**Скептик — подтверждено, итог «дефект»:** Воспроизведено из командной строки (cli.sh, журнал cli.log в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/css-skeptik/). Папки do-q и po-q — копии сборок, в index.css обеих дописано `@layer utilities{.qq{color:red}}`, слой 21 → 20. Результаты. `--schet 20:19` → отказ «по --schet ждали 20 → 19», код 1. `--schet ''` → «ВЕРДИКТ: сверено», код 0. Тот же результат при `--schet "$N"` с пустой N: сценарий из находки воспроизведён буквально. `--schet abc` → код 2. `--schet` в конце строки → код 2. На do-0 и po-0: `--sloy ''` → сверено, слой молча @layer utilities, а `--sloy '@layer components'` → отказ. `--ushlo ''` → сверено с правилом по умолчанию. `--json ''` → сверено, код 0, файл не записан, а с путём файл пишется. Причина по коду: :433 проверяет только `=== undefined`, :439–441 и :456 проверяют на истинность. Шапка :28 обещает «неверные аргументы — код 2», а пустое значение --schet не соответствует `<до>:<после>` и молча снимает запрошенную проверку. Класс тот же, что у zona-1 раунда 1 (опечатка во флаге молча убирает проверку, признано дефектом), и разбор аргументов переписан именно в раунде 1. Тяжесть низкая: остальные проверки выполняются, числа слоя печатаются. На итог сессии не влияет: реальный прогон печатает 20 → 19, совпадает с П87 п. 3.

### r2-css-3 — Проверка <link rel=stylesheet href> узка по синтаксису и считает путь от корня сборки; имена листов заменяются по имени файла без папки

**Где:** sverka-css.mjs:190-199 (ssylkiNaListy: `\brel=["']?stylesheet\b`, href только в кавычках, `<link\b[^>]*>`), :281-284 (путь = href без ведущей «/», от корня, без учёта папки HTML и без раскрытия сущностей), :179 и :223 (пары замены — имена файлов без папки); шапка :21-24 и :33-35 (названа только форма `href="…css"` с `rel="stylesheet"`)

**Суть:** Устаревшее имя листа в обычной записи ловится побайтной проверкой имён при любом синтаксисе, так что css-1 в поставленном виде закрыт. У новой проверки ссылок есть слепые зоны. Она не видит ссылку, если href без кавычек, rel="preload stylesheet" или "alternate stylesheet", пробелы вокруг «=», data-href стоит перед href или «>» внутри значения атрибута. Относительный href из подпапки она разрешает от корня: `_astro/…` в 404/index.html проходит, хотя браузер пойдёт в /404/_astro/. На верных ссылках она ложно отказывает: `../_astro/…`, `#якорь`, абсолютный URL, data-rel="stylesheet" у иконки. Пары замены строятся по имени файла без папки: одноимённый лист в подпапке даёт ложный отказ, а вместе с href без кавычек — пропуск. Самый острый случай — ложное «сверено» на устаревшем HTML «после»: имя в href записано сущностью `&#46;` и без кавычек. Итог сессии 13 не меняется: во всех HTML обеих сборок ссылки только вида `<link rel="stylesheet" href="/_astro/…css">`, сущностей нет, старого имени DHeL5m7t в HTML «после» 0.

**Доказательство:** opyty.log — одинаковая битая ссылка в index.html обеих сборок: h0 (кавычки) → отказ; h1 `href=/_astro/ghost.css` → сверено; h3 `rel="preload stylesheet"` → сверено; h5 `rel = "stylesheet"` → сверено; h7 data-href перед href → сверено; h11 `title="a>b"` → сверено; h12 alternate → сверено; h14 относительная `_astro/…` в 404/index.html → сверено. Ложные отказы: h9 `#x` → отказ, h10 `https://…` → отказ, h13 data-rel → отказ, h15 `../_astro/…` → отказ «ведёт мимо листов». Подпапка: s2 (sub/LinkList.DHeL5m7t.css, верная ссылка в обеих) → отказ «осталось старое имя»; s3 («после» ссылается на несуществующий sub/LinkList.B4mIKaMJ.css без кавычек) → сверено; s4 (то же в кавычках) → отказ. opyty4.log: index.html «до» и «после» одинаково `<link rel=stylesheet href=/_astro/LinkList.DHeL5m7t&#46;css>` → «сверено», при этом _astro/LinkList.DHeL5m7t.css в «после»: false, в «до»: true. opyty3.log: устаревший HTML без кавычек, с «preload stylesheet» и с одинарными кавычками → отказ «в «после» осталось старое имя листа». grep по do0 и po0: ссылки только `rel="stylesheet" href="/_astro/…"`, «&#» — 0.

**Скептик — предел, итог «предел»:** Наблюдения верны, воспроизведены в памяти (proba.mjs, раздел 3, proba-3.log). Одинаковая ссылка-призрак в index.html обеих сборок. h0 (в кавычках) → отказ. h1 (href без кавычек), h3 (preload stylesheet), h5 (`rel = "stylesheet"`), h7 (data-href перед href: `\bhref=` цепляет data-href), h11 (`title="a>b"`), h12 (alternate) → сверено. h14 (относительный `_astro/…` в 404/index.html, при этом в кавычках и с rel="stylesheet") → сверено. Ложные отказы: h9 `#x`, h10 абсолютный URL, h13 data-rel у иконки, h15 верный `../_astro/…`. Подпапка: s2 (одноимённый лист в sub/, ссылки верные) → ложный отказ. s3 (ссылка без кавычек на несуществующий sub/LinkList.B4mIKaMJ.css) → сверено. s4 (то же в кавычках) → отказ. u4 (одинаковая `href=/_astro/LinkList.DHeL5m7t&#46;css` в обеих, файла в «после» нет) → сверено. Итог всё же «предел»: css-1 закрыт. Повтор опыта раунда 1 (proba-c1.mjs, proba-c1.log) — подмена index.html, 404/index.html или обоих на HTML из main — даёт отказ по счёту старых и новых имён, и этот счёт от синтаксиса ссылки не зависит. Устаревший HTML ловится во всех записях: u1 (кавычки), u2 (без кавычек), u3 (сущность только в «после»). Регрессия «до верно — после битое» ловится побайтной сверкой HTML после замены имён. Ссылочная проверка решает только для HTML, который уже побайтно равен «до». Ложное «сверено» на устаревшем HTML остаётся лишь при записи имени сущностью в обеих сборках, а Astro так не пишет. Шапка :33–35 называет узкую форму поиска. Не названы разрешение пути от корня, а не от папки HTML (h14 — внутри названной формы), и пары замены по имени без папки. Это можно дописать в шапку одной строкой, итог не меняется. В реальных HTML обеих сборок ссылки только `<link rel="stylesheet" href="/_astro/…css">`, «&#» — 0, DHeL5m7t в «после» — 0, B4mIKaMJ — по 1.

### r2-css-4 — «Прямых детей: правило или @media — по одному» — счёт слоя включает и комментарии

**Где:** sverka-css.mjs:16-17 (шапка), :122-126 (sloyUtilit: `n += d.deti.length`), :452 (строка вывода с той же фразой, добавлена в раунде 1)

**Суть:** sloyUtilit считает всех прямых детей блока. Комментарий, стоящий ребёнком @layer utilities, тоже попадает в счёт, хотя в шапке и в выводе сказано «правило или @media», а CSSOM комментарий правилом не считает. Инструкция `@layer x;` тоже считается, но она правило CSSOM, это верно. Исход безопасный: с --schet будет ложный отказ, ложного «сверено» нет. В реальных листах в слое утилит 20 = 15 правил + 5 @media (zona-6), комментариев в слое нет. Правка — в тексте: «прямых детей (правило, @-правило или комментарий)», либо не считать komm.

**Доказательство:** opyty.log k1: /*k*/ прямым ребёнком слоя в обеих сборках → «сверено; слой 21→20»; k1б с --schet 20:19 → «отказ; слой @layer utilities: 21 → 20, по --schet ждали 20 → 19». opyty5.log: `@layer x;` в слое обеих сборок → слой 21 → 20.

**Скептик — подтверждено, итог «проза»:** По коду :124 `n += d.deti.length` считает всех прямых детей. Опыт (proba.mjs, раздел 4, proba-4.log): в обе сборки дописано `@layer utilities{…}` и получено «сверено; слой 21→20», а с `--schet 20:19` → отказ. Так выходит для комментария `/*k*/` (k1), для инструкции `@layer x;` (k2), для голого объявления `color:red` (k3) и для `@supports` (k4). Шапка :16–17 и строка вывода :452 «(прямых детей: правило или @media — по одному)» неточны: считаются и комментарии, и голые объявления, а @supports и прочие @-правила формулой «правило или @media» не названы. Искал ложное «сверено» через этот счёт и не нашёл. Комментарий или объявление в слое меняют последовательность листьев («пришло» или «ушло»), а одинаковые в обеих сборках сдвигают оба числа на одно и то же, так что разница −1 сохраняется. С --schet будет только ложный отказ. Реальный состав слоя в LinkList: «до» — 15 правил и 5 @media = 20, «после» — 14 и 5 = 19, комментариев нет. Итог сессии верен, правка нужна только в тексте, или komm не считать.

### r2-css-5 — Шапка: «неверные аргументы — код 2», но неверный --ushlo и несуществующая папка дают необработанное исключение и код 1; отказ на невалидном UTF-8 в HTML назван только для листов

**Где:** sverka-css.mjs:27-28 (коды), :182-186 (pravilo бросает исключение внутри sverit), :310-321 (chitatPapku без обработки ошибок), :11 и :280 (UTF-8: шапка говорит только о листах, код отказывает и на HTML «после»)

**Суть:** Неверное правило в --ushlo (`abc`) и несуществующая папка сборки дают стек исключения и код 1, а код 1 — это код «отказа». Шапка обещает для неверных аргументов код 2. Исход безопасный: ложного «сверено» нет, но вызывающий сценарий примет ошибку аргумента за отказ сверки. Отдельно: если в HTML есть невалидный UTF-8, даже одинаковый в обеих сборках, инструмент отказывает («не валидный UTF-8»). Шапка (п. 2) говорит о таком отказе только для листов, а в п. 6 его нет. На итог сессии не влияет: реальные HTML — валидный UTF-8 (прогон v2 и мой: сверено).

**Доказательство:** node sverka-css.mjs do0 po0 --ushlo 'abc' → «throw new Error('--ushlo: ждали одно правило с объявлениями…')», код 1. node sverka-css.mjs nety po0 → «node:fs … binding.readdir(», код 1. opyty.log: одинаковый байт FF в 404/index.html обеих сборок → «отказ … 404/index.html: не валидный UTF-8».

**Скептик — подтверждено, итог «проза»:** Воспроизведено (cli.log). `--ushlo 'abc'` и `--ushlo '.a{}'` → необработанное исключение из pravilo() («throw new Error('--ushlo: …')»), строки «ВЕРДИКТ» нет, код 1. Несуществующая папка `nety` → ENOENT из readdirSync, код 1. Для сравнения: неизвестный флаг `--bogus 1` и лишний позиционный аргумент → код 2. Шапка :28 обещает для неверных аргументов код 2, а код 1 по той же шапке значит «отказ», так что сценарий, читающий только код возврата, примет ошибку аргумента за отказ сверки. Ложного «сверено» нет. UTF-8 (proba.mjs, раздел 5, proba-5.log): одинаковый байт FF в 404/index.html обеих сборок → отказ «404/index.html: не валидный UTF-8». Шапка называет это условие только для листов (п. 2), а в п. 6 (HTML) его нет. Формально «сверено — только если…» задаёт необходимое условие, и лишний отказ ему не противоречит, но перечень проверок неполон. Исход безопасный, реальные HTML — валидный UTF-8, итог сессии не меняется. Правка — в тексте шапки (или ловить исключения аргументов и выходить с кодом 2).

**Проверено — чисто:**

- Воспроизведение на копиях do0 и po0: node sverka-css.mjs do0 po0 даёт тот же вывод, что zamery/v2/sverka-css.txt: ушло #68 «@layer utilities › .lowercase {text-transform:lowercase}»; слой 20 → 19; побайтно равны 83; HTML проверено 2, из них равны после замены 2; ВЕРДИКТ: сверено, код 0. С --schet 20:19 — тоже сверено. --selftest — 37/37, код 0.
- Разбор аргументов после правки. Код 2 дают: неизвестный флаг (--ets), флаг без значения в конце строки (--schet), --ushlo=… через «=», лишний позиционный аргумент, --selftest не первым аргументом, --schet abc и --schet 20:19:1. Остаток с пустым значением — r2-css-2.
- Обратная косая вне строк на легальном выводе ложных отказов не даёт. Пять правил Tailwind с \', \{, \}, \; и :before, одинаковые в обеих сборках, дают сверено. \/* вне строки верно считается текстом, а не началом комментария: css-tree согласен, 3 правила.
- --ushlo, чья прелюдия — часть другой прелюдии. u1: .x .flex{display:flex} в index.css обеих сборок при уходе .flex из слоя — сверено, это верно. u2: в «до» только .x .flex{…}, в «после» остался «.x » — отказ. u3: .x,.flex{…} — отказ. u4: пробелы, оставшиеся на месте ушедшего, — сверено, это верно. u5: .flex{display:flex;} осталось — отказ. Ложного «сверено» нет.
- Лист вне _astro (styles/extra.css, одинаковый в обеих сборках) — сверено. Пустой лист в обеих сборках — сверено. Пустой лист со сменой хеша — сверено, на него нет ссылок. Лист с одним комментарием, ставший пустым, — отказ.
- Имя листа как подстрока другого имени (ax.KKKKKKKK.css содержит x.KKKKKKKK.css). При верном HTML — ложный отказ «осталось старое имя», при HTML со ссылкой на несуществующий ax.M… — отказ по трём проверкам. Ложного «сверено» нет: цепочку замен страхует сверка счёта новых имён.
- css-7 не исправлялся и правкой не задет: два листа с основой _astro/index в обеих сборках по-прежнему дают отказ. Шапка (стр. 33) теперь называет это верно.
- Побайтная проверка листа (ubratOdno по Buffer при единственном вхождении) и проверка ключа листа по-прежнему страхуют друг друга. Ни одним опытом раунда 2 не удалось получить «сверено» при другом месте удаления.
- Репозиторий не изменялся. Все копии, сценарии и журналы (opyty*.mjs, gotovit.mjs, nezavisimo.mjs, opyty*.log, do0, po0, do-k, po-k, do-n, po-n) лежат в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/css/. Неотслеживаемая папка docs/reports/…/sud/ была в выводе git status до начала моей работы.

## Линза «markery»

git status линзы: `?? docs/reports/2026-09-25-7thserpent-tailwind-zona/sud/  (Эту папку создал не я. В ней только SVODKA-r1.md со временем 19:46:37.53; папку этой линзы sud-r2 оркестратор создал в 19:46:37.91, то есть п`; скептика: `?? docs/reports/2026-09-25-7thserpent-tailwind-zona/sud/`

**Закрыты правками прошлого раунда (проверено опытом):**

- zona-1 — опыты находки повторены на исправленном markery.mjs (opyty.mjs). Пустая папка с --net 73,74 даёт код 2 («нет --est»). --ets 71,72 на dist-posle — код 2 («неизвестный флаг --ets»). Без списков — код 2. --net 7y и --est 7y — код 2. Пустая папка с --est 71 --net 73 — код 1 («в сборке нет ни одного листа *.css — проверять не на чем»). Лист без @layer utilities — код 1. Самопроверка 20/20 покрывает все эти случаи. Ложного «все там» на этих входах больше нет.
- zona-3 — sha256 состояний с маркерами теперь записаны до каждой сборки. По mtime: sha-A 19:40:44.9, журнал build-A 19:40:47–48, markery-A 19:40:49.2; sha-B 19:41:08.8, build-B 19:41:11–12, markery-B 19:41:12.7. Хеши сверены независимо. global.css в A = 345d7fa… = git show main, в B = 85ae387… = HEAD/70195ca/6df34df. Четыре хеша маркерных файлов я восстановил перебором (rekonstr.mjs): каждый — исходный файл плюс ровно одна строка маркера: z-[71] в Breadcrumbs.astro:3, z-[72] в [...slug].astro:3, «# МАРКЕР z-[73]» в 404.md:3 во фронтматтере, z-[74] в check-tokens.mjs:2. В A и B они одинаковы. sha-C даёт OK против sha-0, и текущее дерево равно sha-0.

| id | находка | класс линзы | скептик | итог |
|---|---|---|---|---|
| r2-markery-1 | Огромное число в --net молча округляется через Number: утёкший маркер не виден, итог «все маркеры там, где ждали» | дефект | подтверждено | **дефект** |
| r2-markery-2 | «есть» засчитывается в любом листе *.css папки, даже в листе, который не грузит ни одна страница; два блока @layer utilities не дают отказа | предел | предел | **предел** |
| r2-markery-3 | Строгое «есть» даёт ложный отказ, если минификатор слил правило маркера с соседним с тем же телом | предел | предел | **предел** |
| r2-markery-4 | В sha-A.txt строка «0» без подписи; в sha-B.txt на том же месте вывод другой команды | проза | подтверждено | **проза** |

### r2-markery-1 — Огромное число в --net молча округляется через Number: утёкший маркер не виден, итог «все маркеры там, где ждали»

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/markery.mjs:63-64 (регулярка /^\d+(,\d+)*$/, затем .map(Number)) и :44-45 (поиск по `${n}`); проза шапки :10-11

**Суть:** Правка разбора аргументов пропускает любую строку цифр без ограничения длины. Список сразу переводится в Number, а искомый текст строится из этого числа. Если число больше 2^53 и не представимо точно (например, 9007199254740993 даёт …992) или не меньше 1e21 (99999999999999999999 даёт 100000000000000000000, а длиннее — 1e+21 или Infinity), инструмент ищет не тот маркер. Tailwind 4.3.3 при этом сохраняет селектор как написан, а минификатор зажимает значение до z-index:2147483647. В итоге ни подстрока, ни регулярка не срабатывают, «нет» проходит впустую, и выходит ложное «все там» с кодом 0. Это ровно класс zona-1 — ложное «все там» на принятом входе, — только через другой вход. Самопроверка этот случай не покрывает. Шапка («ни подстроки `z-\[N\]`, ни объявления `z-index:N`») для таких N неверна. На итог сессии не влияет: в прогонах A и B маркеры 71–74, для них Number(s) и s совпадают. Как чинить: держать списки строками, снимать ведущие нули и отказывать с кодом 2, если !Number.isSafeInteger или длина больше, скажем, 9 цифр. Добавить случай в самопроверку.

**Доказательство:** Вывод Tailwind (scratchpad/tz/sud-r2/markery/tw-chisla.mjs: compile из node_modules, затем optimize и lightningcss 1.32.0 или esbuild 0.28.2) — «.z-\[2147483648\],.z-\[9007199254740993\],.z-\[99999999999999999999\]{z-index:2147483647}». Опыт (opyty.mjs, копия dist-posle, в @layer utilities вставлены 71, 72 и `.z-\[9007199254740993\]{z-index:2147483647}`): команда `node markery.mjs …/fikstury/bolshoe-2e53 --est 71,72 --net 9007199254740993` печатает «да  z-[9007199254740992] ждали НЕ в CSS: нет ни селектора, ни z-index:9007199254740992 / ИТОГ: все маркеры там, где ждали», код 0. С 99999999999999999999 — «да  z-[100000000000000000000] ждали НЕ в CSS … ИТОГ: все маркеры там, где ждали», код 0. Журнал: scratchpad/tz/sud-r2/markery/opyty.log.

**Скептик — подтверждено, итог «дефект»:** Проверил сам на своих копиях dist-posle (sud-r2/markery/skeptik/opyty.mjs, журнал skeptik/opyty.log). В слой вставлены 71, 72 и утёкший маркер. Результаты: `--net 9007199254740993` печатает «да z-[9007199254740992] … ИТОГ: все маркеры там, где ждали», код 0. `--net 99999999999999999999` даёт то же с z-[100000000000000000000], код 0. `--net 1000000000000000000000` даёт z-[1e+21], код 0. Зажим минификатором роли не играет: без зажима (z-index:9007199254740993 как написано) тоже код 0, потому что ищется другое число. Граница проверена: z-[9007199254740991] (2^53−1) ловится подстрокой, код 1. Дыра ровно там, где String(Number(s)) ≠ s. Вывод Tailwind подтверждён моим прогоном (skeptik/tw.mjs, tw.log). Tailwind 4.3.3 build даёт `.z-\[9007199254740993\]{z-index:9007199254740993}`, после optimize(minify) — `.z-\[9007199254740993\],.z-\[99999999999999999999\]{z-index:2147483647}`; плагин вызывает optimize в сборке. Это тот же класс, что zona-1: ложный ИТОГ на входе, который инструмент принял. Правка раунда 1 задела именно это место. Вердикт zona-1 требовал «числа проверяются»; правка добавила регулярку `/^\d+(,\d+)*$/`, но оставила `.map(Number)`. Шапка (стр. 10–11 и 13–14: «нечисловой список» — код 2) для таких N неверна. Самопроверка 20/20 этот случай не покрывает. Попутно проверено, что ведущие нули ложного ИТОГа не дают. На сжатом листе `--net 073` ловится регуляркой z-index:73 (код 1). На несжатом листе (skeptik/nuli.mjs, nuli.log) сама проверка «есть» отказывает: объявление с пробелом. Итог сессии не меняется: в прогонах A и B маркеры 71–74. Починка дешёвая: держать списки строками, отказывать при !Number.isSafeInteger, добавить случай в самопроверку.

### r2-markery-2 — «есть» засчитывается в любом листе *.css папки, даже в листе, который не грузит ни одна страница; два блока @layer utilities не дают отказа

**Где:** markery.mjs:21, 27-29 (sloev считается, но отказ только при 0), 30-38

**Суть:** Правка добавила отказ «ни в одном листе нет @layer utilities», но случай «слоёв больше одного» остался без проверки. Если в папке сборки лежит устаревший лист от прошлой сборки с маркерами 71 и 72, «есть» засчитывается по нему. Лист страниц, на который ссылается HTML, при этом маркеров не несёт: зона могла не читаться вовсе, и «нет» тогда проходит тривиально. Инструмент печатает «листов CSS 3, блоков @layer utilities 2», но выносит «все там». Итог сессии не меняется. Astro очищает outDir перед сборкой: node_modules/astro/dist/core/build/static-build.js:63-64, emptyOutDir: false у сайта не задан. В прогонах v2 «листов CSS 2, блоков @layer utilities 1», и маркеры найдены в LinkList.DV_wl0J6.css и LinkList.Bl5rmwFn.css, то есть в тех листах, что дала сборка тех же состояний в раунде 1. Дешёвый сторож — отказ при sloev !== 1 или требование, чтобы лист с маркерами «есть» был в `<link rel=stylesheet>` хотя бы одного HTML.

**Доказательство:** opyty.mjs, копия staryj-list: dist-posle и дополнительный файл _astro/LinkList.STARYJ00.css = «@layer utilities{.z-\[71\]{z-index:71}.z-\[72\]{z-index:72}}». Вывод: «листов CSS 3, блоков @layer utilities 2 / да  z-[71] … есть — _astro/LinkList.STARYJ00.css / да  z-[72] … / да  z-[73] ждали НЕ в CSS / да  z-[74] … / ИТОГ: все маркеры там, где ждали», код 0. Ссылки HTML в той же копии: index.html → ["_astro/LinkList.B4mIKaMJ.css","_astro/index.xofYExtq.css"], 404/index.html → ["_astro/LinkList.B4mIKaMJ.css"].

**Скептик — предел, итог «предел»:** Опыт повторён (skeptik/opyty.log, копия staryj). К dist-posle добавлен _astro/LinkList.OLDOLD00.css с `@layer utilities{.z-\[71\]…​.z-\[72\]…}`. Инструмент печатает «листов CSS 3, блоков @layer utilities 2», засчитывает «есть» по этому листу и выдаёт «все маркеры там, где ждали», код 0. Лист не грузит ни одна страница: index.html ссылается только на LinkList.B4mIKaMJ.css и index.xofYExtq.css, 404/index.html — только на B4mIKaMJ. В обратную сторону ошибка безопасна: устаревший лист с 73 даёт отказ (код 1). Практически это недостижимо, поэтому предел. Astro 7.2.10 чистит outDir перед сборкой: node_modules/astro/dist/core/build/static-build.js:63–64, условие `vite.build.emptyOutDir !== false`. В astro.config.mjs сайта нет ни emptyOutDir, ни outDir. Прогоны v2 шли по свежей dist: «сборка dist: листов CSS 2, блоков @layer utilities 1». Листы DV_wl0J6 и Bl5rmwFn те же, что в markery-do.txt и markery-posle.txt раунда 1. Поведение было и до правки: правка лишь добавила печать счётчика слоёв и нового не внесла. Шапка честна: «в каком-то листе *.css сборки». Устаревший лист может появиться только при копировании в непустую папку или при emptyOutDir:false. Замечание к совету: сторож «sloev !== 1» даст ложный отказ на сайте с несколькими точками входа Tailwind. Точнее требовать, чтобы лист с «есть» был в `<link rel=stylesheet>` хотя бы одной страницы.

### r2-markery-3 — Строгое «есть» даёт ложный отказ, если минификатор слил правило маркера с соседним с тем же телом

**Где:** markery.mjs:36 (y.prelude === `.z-\\[${n}\\]`)

**Суть:** lightningcss и esbuild сливают соседние правила с одинаковым телом в список селекторов. Тогда прелюдия маркера уже не равна «.z-\[N\]», и «есть» даёт НЕТ. Для этого нужен второй кандидат с тем же значением z-index рядом по сортировке, например z-[071] или z-71. Ошибка безопасная: отказ, а не ложное «все там». Условие «есть» правка не меняла. В прогонах v2 правила стоят отдельно, по одному на маркер. Записываю как предел для будущих прогонов: номера маркеров выбирать так, чтобы они не совпадали по значению с другими кандидатами.

**Доказательство:** tw-chisla.mjs: из кандидатов z-[0071] и z-[71] после optimize получается «.z-\[0071\],.z-\[71\]{z-index:71}». opyty.mjs, копия slitnoe-0071, команда --est 71,72 --net 73: «НЕТ z-[71] ждали В CSS: нет … ИТОГ: 1 ошибок», код 1.

**Скептик — предел, итог «предел»:** Подтверждено моим прогоном Tailwind 4.3.3 + optimize(minify) + lightningcss 1.32.0 (skeptik/tw.log). Кандидаты z-[71] и z-[0071] дают `.z-\[0071\],.z-\[71\]{z-index:71}`. Кандидаты z-[71] и именованный z-71 дают `.z-71,.z-\[71\]{z-index:71}`. Кандидаты z-[71] и z-[72] остаются отдельными правилами. На копии со слитым `.z-71,.z-\[71\]` (skeptik/opyty.log, slit-z71) `--est 71` даёт «НЕТ z-[71] ждали В CSS: нет», код 1. Отказ безопасный, ложного «все там» нет. На «нет» слияние не влияет: подстрока и регулярка по тексту его видят. Условие «есть» (стр. 36) правка не меняла; то же условие описано в выводах скептика zona раунда 1. В v2 правила маркеров стоят по одному. Итог сессии не меняется.

### r2-markery-4 — В sha-A.txt строка «0» без подписи; в sha-B.txt на том же месте вывод другой команды

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/zamery/v2/sha-A.txt:8 (и sha-B.txt:8-9)

**Суть:** После «global.css против main: равен» в sha-A стоит голое «0». Что оно считает, не сказано; вероятно, это счёт строк «@source not». В sha-B на этом месте стоят две строки @source, то есть вывод другой команды. Доказательную силу это не отнимает, потому что состояние global.css держится на sha256, а он сверен. Но читатель материалов владельца не поймёт, к чему относится «0». Правка — подпись строки при следующей записи материалов. Историю не трогать.

**Доказательство:** sha-A.txt: строка 7 «global.css против main: равен», строка 8 «0», строки 9–12 — маркеры. sha-B.txt: строка 7 «global.css против HEAD: равен», строки 8–9 «@source '../../../../core/**/*.astro';», «@source not '../content';».

**Скептик — подтверждено, итог «проза»:** Прочитано. В sha-A.txt строка 8 — голое «0» без подписи. В sha-B.txt на строках 8–9 стоят две строки @source. У global.css в main одна строка @source (`@source '../../../../core/**/*.astro';`, git show main:…​ \| grep -n). Значит, команда из B в состоянии A напечатала бы эту строку, а не «0». В A работала другая команда, по всей видимости счёт строк «@source not», которых в main 0. Доказательную силу это не отнимает: состояние global.css закреплено sha256 (стр. 2) и строкой «global.css против main: равен». Но в материалах владельца строка непонятна. Оба файла в git (git ls-files, коммит 6df34df). Правка — подпись строки при следующей записи материалов, историю не трогать.

**Проверено — чисто:**

- Самопроверка: node markery.mjs --selftest — 20/20, код 0 (11 случаев proverit, 9 случаев argumenty). Новые случаи «нет листов» и «нет слоя» проверяют сторожа отдельно от --est (est = []).
- Обходы разбора аргументов дают код 2. Один маркер в двух записях (--est 071 --net 71; --est 71 --net 0071): сравнение идёт по Number, поэтому «маркер в обоих списках: 71». --net дважды — «флаг дважды». --est=71 — «неизвестный флаг». «71, 72» и «71,» не проходят регулярку. Одиночный -x — «ждали ровно одну папку». Лишний позиционный аргумент — код 2. Если забыта папка, число становится путём, и выходит исключение ENOENT с кодом 1 — отказ, а не ложное «да».
- Ведущие нули и ноль безопасны. --est 071 ищет .z-\[71\] и находит маркер z-[71]. Маркер z-[073] в файле Tailwind с минификатором печатает как «.z-\[073\]{z-index:73}», и --net 073 или --net 73 ловит его регуляркой z-index:73: код 1. --est 0 — «НЕТ», код 1. --net 0 на листах сайта проходит: z-index:0 в них нет.
- Положительный и отрицательный контроль на копии dist-posle: вставка 71 и 72 в @layer utilities даёт «все там», код 0, а добавка утёкшего 73 — «НЕТ z-[73] ждали НЕ в CSS: есть», код 1.
- Прогоны v2 сделаны исправленной версией. Время изменения markery.mjs — 19:38:57, markery-A — 19:40:49. Строка «сборка dist: листов CSS 2, блоков @layer utilities 1» — формат новой строки 128.
- Хеши согласуются между файлами. sha-0-iskhod и sha-C (все OK) равны текущему рабочему дереву. Хеши 404.md, [...slug].astro, Breadcrumbs.astro и check-tokens.mjs в исходе равны git show main и HEAD. global.css: main = 345d7fa… (как в sha-A и в sha256-markery.txt раунда 1), 70195ca = 6df34df = HEAD = рабочее = 85ae387… (sha-B, sha-0). Копии v2 в scratchpad/tz/v2 побайтно равны закоммиченным zamery/v2 (cmp по 8 файлам).
- Разрыва «sha снят, а сборка шла с другим состоянием» не видно. Порядок sha, сборка, маркеры подтверждён mtime, окно между sha и сборкой 2–3 с. Имена листов сборок A и B (LinkList.DV_wl0J6 и LinkList.Bl5rmwFn) совпадают с именами сборок 2 и 3 раунда 1 для тех же состояний (markery-do.txt, markery-posle.txt). В раунде 1 global.css был 8810c0d, в v2 — 70195ca; они различаются только комментарием, это видно по git diff, а вывод CSS совпал. Остаток: sha после сборки и git status в A и B не записаны. Итог это не меняет: маркерные файлы восстановлены, результаты маркеров согласуются с состояниями.
- Материалы доказывают протокол. В A: global.css = main (без @source not), маркеры 71–74 стоят, z-[73] в CSS есть. В B: global.css ветки с @source not, те же маркерные файлы (хеши равны A), z-[73] нет, а 71 и 72 есть. Между A и B менялся только global.css, поэтому уход 73 приходится на @source not. В C после отката файлы равны исходным (sha256sum -c OK, git status — только папка доклада).
- Текущая сборка sites/7thserpent.com/dist после отката: LinkList.B4mIKaMJ.css с sha256 fd4d0871… равен dist-posle, подстроки «z-\[7» в нём 0. markery.mjs с --est 71,72 --net 73,74 на ней даёт код 1 («НЕТ z-[71]», «НЕТ z-[72]»): маркеров в сборке не осталось, инструмент это видит.
- Шапка markery.mjs в остальном соответствует коду: код 1 при ошибке маркера, при отсутствии листов или слоя; код 2 при неверных аргументах; оба списка обязательны. Ссылка на запись DECISIONS 2026-09-04 «`@source '../../../../core'` проверен пробой» существует (DECISIONS.md:135).

## Линза «stili»

git status линзы: `?? docs/reports/2026-09-25-7thserpent-tailwind-zona/sud/  (Этот вывод git -C D:/SEO/cloud/site-generator status --short совпадает с первым снятием, сделанным до всех моих опытов: папка sud/ была неотс`; скептика: `?? docs/reports/2026-09-25-7thserpent-tailwind-zona/sud/`

**Закрыты правками прошлого раунда (проверено опытом):**

- stili-1: опыт раунда 1 повторён на скриптах s2 дословно (свой Chrome, page.route). Abort LinkList на обоих портах даёт «СТОП: на http://127.0.0.1:4341/404/ лист не загружен или пуст: /_astro/LinkList.DHeL5m7t.css (правил -1)», так же на главной и при abort всех .css. Ответ 404, 200 text/html и пустой лист дают «(правил 0)» и СТОП. proba-blok-lista.js дословно — «ОСТАНОВИЛСЯ: СТОП … (правил -1)». Ложного «сверено» без листа больше нет.
- stili-2: опыт E2 раунда 1 повторён дословно: к мутации К3 добавлен outlineOffset у последнего элемента body. Вариант через page.route: style у h1 только на 4-й загрузке «после». Результат: «К3 28 / 65 — СЛЕПА; итог отказ», хотя все 8 первых различий К3 — ::after. Обратная сторона: 9 лишних различий до h1 в К2 (К2 12, h1 не в первых 8) дают «видит; итог сверено». Условия считаются по полному списку.
- stili-4: шапка (строки 34-41) называет prefers-reduced-motion и prefers-reduced-transparency, :target, ::selection, полосу прокрутки, ::first-letter/::first-line/::marker, ::placeholder, ::backdrop. Все @media листов (forced-colors, resolution, prefers-*, ширины) покрыты. Шесть скриптов прогнаны в порядке vse.js. Без ящика — hover [] и фокус BODY; в 390-yashik — hover на HTML.menu-otwarte…BUTTON.hdr__burger…path и фокус BUTTON.hdr__burger. Это совпадает с новой фразой шапки.
- stili-5: при style у h1 только на загрузке К1 вывод «К1 (повтор внутри сборки) 1 — ПРАВИЛО НЕ ИНЕРТНО; зрячесть: К2 3, К3 27 — видит; итог отказ». К1 печатается отдельно от зрячести, «СЛЕПЫ» при К1 ≠ 0 не пишется; остаток надписи — r2-stili-5.
- stili-6: копия генератора на одинаковых листах и на одинаковых портах завершается с кодом 2 («обязаны различаться»), файл не пишется. При верных разных именах и одной сборке на обоих портах (4341 отдаёт HTML ветки) скрипт останавливается: «СТОП: на http://127.0.0.1:4341/404/ нет листа LinkList.DHeL5m7t.css». Остаток предела (содержимое под тем же именем, другое написание) — r2-stili-2 и r2-stili-3.

| id | находка | класс линзы | скептик | итог |
|---|---|---|---|---|
| r2-stili-1 | «Несёт правила» значит одно правило верхнего уровня или больше: лист из одного @import или одной инструкции @layer на обоих портах проходит проверку, и страница без стилей даёт «сверено» | предел | предел | **предел** |
| r2-stili-2 | «На порту отвечает та сборка» проверяется только по имени листа в HTML: если под тем же именем отдано содержимое листа main, итог «сверено» | предел | предел | **предел** |
| r2-stili-3 | Запрет генератора «листы и порты обязаны различаться» сравнивает строки: один лист в двух написаниях и порт 04342 проходят, итог — сверка сборки с собой | предел | предел | **предел** |
| r2-stili-4 | Шапка: «лист, не отданный сервером, бросает при чтении правил» верно только для сетевого сбоя; ответ 404 даёт лист с 0 правил без исключения | проза | подтверждено | **проза** |
| r2-stili-5 | При К1 ≠ 0 печатается «ПРАВИЛО НЕ ИНЕРТНО», а шапка называет две причины: неинертное правило или невоспроизводимую перезагрузку | проза | подтверждено | **проза** |
| r2-stili-6 | metki удаляются до сохранения: довод «все различия К3 в ::after, среди К2 есть h1» и причина «СЛЕПА» из выгрузки не восстанавливаются | предел | предел | **предел** |
| r2-stili-7 | Проверка cssRules отказывает на любом листе с чужого адреса, даже если он загружен и применён: в будущем ложный отказ на сайте с внешним листом шрифтов | предел | предел | **предел** |

### r2-stili-1 — «Несёт правила» значит одно правило верхнего уровня или больше: лист из одного @import или одной инструкции @layer на обоих портах проходит проверку, и страница без стилей даёт «сверено»

**Где:** D:/SEO/cloud/site-generator/docs/reports/2026-09-25-7thserpent-tailwind-zona/instrumenty/stili-gen.mjs:83-90 (pravil = l.sheet.cssRules.length, условие pravil > 0); проза шапки — строки 9-11

**Суть:** Правка stili-1 закрывает отказы загрузки: сбой сети, 404, пустой лист, неверный MIME. Но проверка `cssRules.length > 0` пропускает лист, у которого есть хотя бы одно правило верхнего уровня, даже если стилей этот лист не даёт. Если такой лист отдан на обоих портах одинаково, сравниваются стили браузера по умолчанию, К2 и К3 зрячи, и итог — «сверено». Это тот же класс ошибки, что stili-1, только для ввода, которого реальная сборка не выдаёт: в листах dist-main и dist-posle 0 вхождений @import (lightningcss их встраивает), а поломка должна быть одинаковой на обоих портах. Итог сессии это не меняет: в stili-s2.json у всех 6 прогонов pravil 176/105 и 525 свойств в строке. Без стилей было бы 477 свойств (нет 48 пользовательских из :root), и читатель JSON это увидит. Починить можно так: дополнительно требовать, чтобы у :root было непустое пользовательское свойство сайта (например --bg), или останавливаться при svoystv_v_stroke_0 без пользовательских свойств.

**Доказательство:** C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/stili/probnik2.mjs, режим e5 (вывод в e5.json). Свой Chrome; stili-404-1440.js s2 взят дословно; page.route подменяет оба LinkList. (а) Тело `@import url("/_astro/net-takogo.css");` даёт вывод «404 1440: элементов 133, строк 399, свойств в строке 477, различий 0; К1 (повтор внутри сборки) 0; зрячесть: К2 3, К3 27 — видит; итог сверено»; в JSON listyDo/listyPosle pravil 1; у h1 шрифт "Times New Roman", letter-spacing normal. (б) Тело `@layer theme,base,components,utilities;` даёт тот же вывод: 477 свойств, pravil 1, «итог сверено». grep -c @import по _astro/*.css обеих копий: 0, 0, 0, 0 (proverka-faylov.mjs).

**Скептик — предел, итог «предел»:** Опровергнуть не удалось, повторил своим опытом. Скрипт — scratchpad/tz/sud-r2/stili-skeptik/opyt.mjs, режим a, вывод в opyt-a.json. Скрипты сверки взяты из моей копии генератора: она побайтно равна коммиту 6df34df, а шесть сгенерированных файлов побайтно равны .playwright-mcp/sesja-13/s2. Оба LinkList подменены через page.route. Тела «@import url(...несуществующего)», «@layer theme,base,components,utilities;» и даже «zzz{}» дают pravil 1 и вывод «404 1440: … свойств в строке 477, различий 0; К1 0; зрячесть: К2 3, К3 27 — видит; итог сверено». Главная с обоими листами «@layer x;» тоже даёт «сверено», 477 свойств, К3 64. При этом у h1 шрифт "Times New Roman", фон body rgba(0,0,0,0), --bg пусто. Тело «@charset» даёт 0 правил и СТОП. Проверка cssRules.length > 0 — заместитель признака «лист сайта применён», а не сам признак. Итог сессии это не меняет: в сборках 0 вхождений @import (grep по dist-*/_astro/*.css), в stili-s2.json у всех шести прогонов pravil 176/105 и 525 свойств в строке. Шапка называет проверку буквально (`sheet.cssRules.length > 0`), так что неверной прозы здесь нет. Остаток требует одинаковой искусственной поломки на обоих портах, поэтому это предел.

### r2-stili-2 — «На порту отвечает та сборка» проверяется только по имени листа в HTML: если под тем же именем отдано содержимое листа main, итог «сверено»

**Где:** stili-gen.mjs:88 (href.endsWith('/' + list)); шапка, строки 9-10 («скрипт сверяет, что на порту отвечает та сборка»)

**Суть:** Имя сверяется с атрибутом href, а pravil — это число правил верхнего уровня. У DHeL5m7t и B4mIKaMJ оно одинаково, 176, потому что .lowercase вложено в @layer utilities. Поэтому подмену содержимого при сохранённом имени не видит ни проверка, ни JSON. Имена листов Vite — хеши содержимого, так что настоящая сборка такого не даёт. Это остаток предела stili-6 («не проверяет, что в листе «до» есть .lowercase, а в «после» нет»), правка раунда 1 его не закрывала. Итог сессии это не меняет: по HTTP с портов отдаются листы, побайтно равные копиям (4341 LinkList d55c10a56a8f5815 = dist-main, 4342 LinkList fd4d087158c84e30 = dist-posle, index 4b787071547e86e4 на обоих). Слова шапки «сверяет, что на порту отвечает та сборка» точнее заменить на «по имени листа (хешу) в HTML».

**Доказательство:** probnik2.mjs, режим e4 (e4.json). page.route отдаёт по адресу http://127.0.0.1:4342/_astro/LinkList.B4mIKaMJ.css байты http://127.0.0.1:4341/_astro/LinkList.DHeL5m7t.css. Вывод: «404 1440: … свойств в строке 525, различий 0; К1 … 0; зрячесть: К2 3, К3 27 — видит; итог сверено». JSON: listyDo [/_astro/LinkList.DHeL5m7t.css:176], listyPosle [/_astro/LinkList.B4mIKaMJ.css:176]. Сверено сборка main с собой. Хеши портов: proverka-faylov.mjs, раздел 7.

**Скептик — предел, итог «предел»:** Подтверждено опытом (opyt.mjs, режим b). Порт 4342 под именем LinkList.B4mIKaMJ.css отдаёт байты DHeL5m7t. Вывод: «… 525, различий 0; К1 0; … видит; итог сверено», listyDo и listyPosle по 176 правил. То есть сборка main сверена сама с собой, потому что проверка идёт только по href (стр. 88). Без подмены получается то же «сверено». Порты отдают ровно копии: 4341 LinkList d55c10a56a8f5815 = dist-main, 4342 fd4d087158c84e30 = dist-posle = sites/7thserpent.com/dist, index 4b787071547e86e4 на обоих. Проверки по содержимому листа нет: в LinkList main .lowercase встречается 1 раз, в «после» 0. HTML /404/ двух сборок различаются только именем листа. Имена листов Vite — хеши содержимого, так что у настоящей сборки имя и содержимое не расходятся. Слова «сверяет, что на порту отвечает та сборка» были и до раунда 1, а правка лишь добавила требование различия аргументов. Это остаток предела stili-6: наблюдение верно, итог сессии не меняет.

### r2-stili-3 — Запрет генератора «листы и порты обязаны различаться» сравнивает строки: один лист в двух написаниях и порт 04342 проходят, итог — сверка сборки с собой

**Где:** stili-gen.mjs:51-54 (listDo === listPosle \|\| portDo === portPosle), при проверке имени на странице, строка 88 (endsWith)

**Суть:** Правка stili-6 ловит прямую ошибку: одно имя или один порт дважды. Но `LinkList.B4mIKaMJ.css` и `_astro/LinkList.B4mIKaMJ.css` — разные строки, и оба проходят endsWith на одной и той же странице. Порты '4342' и '04342' тоже разные строки, а URL это один порт. Генератор принимает такие аргументы, и скрипт даёт «сверено», сравнивая ветку с собой. Для этого оператор должен ошибиться дважды и нарочито странно. В JSON прогона видны do/posle и одинаковые listy, так что по выгрузке ошибку можно заметить. Если чинить: нормализовать имя (basename) и порт (Number) перед сравнением. Итог сессии это не меняет: в s2 имена DHeL5m7t/B4mIKaMJ и порты 4341/4342.

**Доказательство:** Копия генератора в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/stili/gen/a/b/c/d/stili-gen.mjs (ROOT уведён в черновик). `node stili-gen.mjs r2x 404 1440 LinkList.B4mIKaMJ.css _astro/LinkList.B4mIKaMJ.css 4342 04342` завершается с кодом 0, файл записан. Для сравнения: те же листы дважды или порты 4342 4342 дают код 2. probnik2.mjs, режим e8 (e8.json): «404 1440: … 525, различий 0; … К2 3, К3 27 — видит; итог сверено», JSON do http://127.0.0.1:4342/404/, posle http://127.0.0.1:04342/404/, оба листа /_astro/LinkList.B4mIKaMJ.css:176.

**Скептик — предел, итог «предел»:** Подтверждено. Моя копия генератора с аргументами `r2x 404 1440 LinkList.B4mIKaMJ.css _astro/LinkList.B4mIKaMJ.css 4342 04342` завершается с кодом 0. Те же листы дважды или порты 4342 4342 дают код 2. new URL('http://127.0.0.1:04342/404/') нормализуется в :4342. Прогон r2x (opyt.mjs, режим c): «404 1440: … 525, различий 0; К1 0; … видит; итог сверено», оба листа /_astro/LinkList.B4mIKaMJ.css:176, do :4342, posle :04342. Значит, ветка сверена сама с собой. Обход требует двух странных ошибок оператора сразу. Если ошибиться только в написании листа при портах 4341/4342, получится СТОП, потому что на 4341 нет B4mIKaMJ. В s2 аргументы верные, и в JSON do/posle и listy видны. Итог сессии это не меняет.

### r2-stili-4 — Шапка: «лист, не отданный сервером, бросает при чтении правил» верно только для сетевого сбоя; ответ 404 даёт лист с 0 правил без исключения

**Где:** stili-gen.mjs:10-11 (скобка «лист, не отданный сервером, бросает при чтении правил»)

**Суть:** В Chrome l.sheet есть во всех случаях. При обрыве запроса (abort) cssRules бросает SecurityError, отсюда pravil −1. Но при ответе 404 (так server.mjs сессии отвечает на отсутствующий файл: 404 text/plain), при ответе 200 text/html и при пустом 200 text/css cssRules.length = 0, и исключения нет. Проверка `!(pravil > 0)` всё равно останавливает, итог верен и безопасен. Неточна только поясняющая скобка: самый вероятный случай «файла нет» идёт по ветке «0 правил», а не «бросает».

**Доказательство:** probnik2.mjs, режим e1-null: при загрузке http://127.0.0.1:4342/404/ с подменой LinkList получено abort → {sheet:true, «бросает SecurityError»}; 404 text/plain → {sheet:true, r:0}; 200 text/html → {sheet:true, r:0}; 200 css пустой → {sheet:true, r:0}. Режим e1 (e1.json): «СТОП: на http://127.0.0.1:4341/404/ лист не загружен или пуст: /_astro/LinkList.DHeL5m7t.css (правил 0)» для 404, text/html и пустого листа; «(правил -1)» для abort. server.mjs:23-25 отвечает на отсутствующий файл 404 text/plain.

**Скептик — подтверждено, итог «проза»:** Проверено опытом (opyt.mjs, режим d, opyt-d.json). l.sheet существует во всех случаях. При abort cssRules бросает SecurityError. При ответе 404 text/plain, 404 text/css с правилом внутри, 500, 200 text/html и пустом 200 text/css правил 0 и исключения нет. Решающий случай: настоящий server.mjs без подмены ответа (HTML ссылается на отсутствующий LinkList.NETTAKOGO.css, сервер отвечает 404 text/plain) даёт sheet true и 0 правил, без исключения. Инструмент на настоящем 404 сервера останавливается «СТОП: … (правил 0)», то есть поведение безопасно и верно. Неверна только скобка шапки на стр. 10-11 «лист, не отданный сервером, бросает при чтении правил»: самый вероятный случай «файла нет» идёт по ветке «0 правил», а бросает только сетевой обрыв.

### r2-stili-5 — При К1 ≠ 0 печатается «ПРАВИЛО НЕ ИНЕРТНО», а шапка называет две причины: неинертное правило или невоспроизводимую перезагрузку

**Где:** stili-gen.mjs:144 (k1Ok ? '' : ' — ПРАВИЛО НЕ ИНЕРТНО') против шапки, строка 27 («К1 ≠ 0 — правило не инертно или перезагрузка не воспроизводима»)

**Суть:** Правка stili-5 развела К1 и зрячесть, и слова «СЛЕПЫ» при К1 ≠ 0 больше нет. Но новая надпись называет одну причину из двух. Если «после» перезагружается невоспроизводимо, а правило инертно, строка вывода утверждает неинертность правила, то есть то, чего не было. Итог «отказ» при этом верен. Лучше писать нейтрально, например «К1 ≠ 0: правило не инертно или перезагрузка невоспроизводима».

**Доказательство:** probnik2.mjs, режим e3 (e3.json): page.route добавляет style="outline-offset:1px" к h1 только на 2-й загрузке «после» (это загрузка К1, всего 4 запроса HTML «после»). Правило .lowercase при этом инертно. Вывод: «404 1440: … различий 0; К1 (повтор внутри сборки) 1 — ПРАВИЛО НЕ ИНЕРТНО; зрячесть: К2 3, К3 27 — видит; итог отказ»; JSON k1Ok false, zryachest true.

**Скептик — подтверждено, итог «проза»:** Подтверждено опытом (opyt.mjs, режим e). Через page.route добавлял style="word-spacing:1px" к h1 только на 2-й загрузке «после», то есть на загрузке К1; всего запросов 4. Правило .lowercase при этом инертно: класса нет в HTML. Вывод: «К1 (повтор внутри сборки) 3 — ПРАВИЛО НЕ ИНЕРТНО; … итог отказ», k1Ok false. Надпись на стр. 144 называет одну причину, а шапка на стр. 27 — две: неинертность правила или невоспроизводимость перезагрузки. В этом опыте причина вторая, так что строка вывода утверждает ложное. Итог «отказ» верен. Это неверный текст, внесённый правкой stili-5.

### r2-stili-6 — metki удаляются до сохранения: довод «все различия К3 в ::after, среди К2 есть h1» и причина «СЛЕПА» из выгрузки не восстанавливаются

**Где:** stili-gen.mjs:135-140 (zryachest считается по metki, затем delete k.metki и delete osnovnoe.metki)

**Суть:** Порядок верный: условия зрячести вычисляются по полному списку до удаления. Но в JSON остаются только булево zryachest, число различий и первые 8. Для доклада «К3 = 64 (главная) / 27 (/404/), все в ::after» опирается только на zryachest: true: 56 и 19 строк за пределами первых восьми в выгрузке не видны. При отказе «СЛЕПА» JSON не говорит, какое условие не выполнено и какая строка его нарушила. Неверного итога это не даёт, итог сессии не меняет: мой повтор vse.js глубоко равен stili-s2.json, а условия считаются по полному списку (опыт в zakryty_r1, stili-2). Чтобы выгрузка несла довод, стоит сохранять счётчики: у К3 — число строк не в ::after и первые такие метки, у К2 — число строк H1.

**Доказательство:** probnik2.mjs, режим e2 (e2.json), опыт через page.route (style у h1 на 4-й загрузке «после»). JSON: kontrol[2].razlichiy 28, zryachest false, itog «отказ», а в kontrol[2].pervye все 8 меток — …::after (k3neAfter []). Строки-нарушителя «55 H1#page-title.t-headline» в выгрузке нет. В stili-s2.json у всех 6 блоков нет поля metki (proverka-faylov.mjs: «metki удалены true»).

**Скептик — предел, итог «предел»:** Подтверждено чтением и опытом. Код: zryachest считается по metki на стр. 136-137, до удаления (delete на стр. 139-140), так что условие вычисляется по полному списку и верно. Опыт (opyt.mjs, режим f): style у h1 на 4-й загрузке «после» (К3) дал «К3 30 — СЛЕПА; итог отказ». Ключи kontrol[2]: imya, strok, elementov, svoystv_v_stroke_0, razlichiy, pervye, поля metki нет. Все 8 pervye — строки ::after (8 … 110), строк h1-нарушителя в выгрузке нет. Значит, причину «СЛЕПА» из JSON не восстановить. Попутно: К2 с 10 изменёнными <a> до h1 даёт «видит, сверено», так что правка stili-2 ложный отказ К2 закрыла. Выгрузка обеднена, но неверного итога это не даёт: в s2 zryachest true, и по коду это означает «все различия К3 в ::after, в К2 есть H1». Итог сессии не меняет.

### r2-stili-7 — Проверка cssRules отказывает на любом листе с чужого адреса, даже если он загружен и применён: в будущем ложный отказ на сайте с внешним листом шрифтов

**Где:** stili-gen.mjs:85 (try { l.sheet.cssRules.length } catch → -1), 89-90

**Суть:** У листа другого происхождения (другой порт или хост, без CORS) cssRules бросает SecurityError. Поэтому скрипт останавливается и тогда, когда лист загружен, применён и по содержимому верен. Для смешанной сборки, где «после» тянет лист с порта «до», это нужный исход. Для сайта с листом CDN (например, Google Fonts) это ложный отказ. Отказ безопасный, и на сессию он не влияет: у 7thserpent.com внешних загрузок 0 (zamery/vneshnie-zaprosy.json: «чужих» 0 на обеих страницах и ширинах, статически — ни одного внешнего href), в HTML ровно 1–2 <link rel=stylesheet> на /_astro/. Строка в пределы для пачки 1 и следующих сайтов.

**Доказательство:** probnik2.mjs, режим e6 (e6.json). HTML «после» переписан на href="http://127.0.0.1:4341/_astro/LinkList.B4mIKaMJ.css", а по этому адресу page.route отдаёт настоящие байты B4mIKaMJ с 4342. Вывод: «ИСКЛЮЧЕНИЕ: СТОП: на http://127.0.0.1:4342/404/ лист не загружен или пуст: http://127.0.0.1:4341/_astro/LinkList.B4mIKaMJ.css (правил -1)». При этом на странице h1 "Bodoni Moda", … 2.2px, то есть лист применён.

**Скептик — предел, итог «предел»:** Подтверждено опытом (opyt.mjs, режим g). HTML «после» ссылается на http://127.0.0.1:4341/_astro/LinkList.B4mIKaMJ.css, а там через route отданы настоящие байты. Вывод: «СТОП: … (правил -1)», хотя лист применён: h1 "Bodoni Moda", 2.2px, фон rgb(9,12,17), --bg #090c11. Отдельно: лист, вставленный с чужого порта, загружается, но cssRules бросает SecurityError. Отказ ложный, но безопасный. У 7thserpent.com листы только /_astro/, по 1–2 на страницу. Уточнение к доводу находки: слова «статически — ни одного внешнего href» неточны. В каждом HTML есть одна абсолютная ссылка <link rel="canonical" href="https://www.7thserpent.com/...">. Она ничего не загружает, так что вывод не меняется. Для этой сессии это предел, для следующих сайтов с внешними листами (Google Fonts без crossorigin) — строка в пределы.

**Проверено — чисто:**

- Совпадение с генератором. Копия stili-gen.mjs в черновике (ROOT уведён туда, в репозиторий ничего не пишется) с аргументами s2 <страница> <состояние> LinkList.DHeL5m7t.css LinkList.B4mIKaMJ.css дала шесть скриптов. Все шесть побайтно равны D:/SEO/cloud/site-generator/.playwright-mcp/sesja-13/s2/stili-*.js (6255…6264 Б).
- Сборный vse.js совпадает со сборкой по логике sobrat-vse.mjs из моих шести скриптов (36462 знака, строки равны). Каждый скрипт входит ровно 1 раз, порядок: glowna-1440, glowna-390, glowna-390-yashik, 404-1440, 404-390, 404-390-yashik. proba-blok-lista.js содержит stili-404-1440.js дословно.
- Воспроизводимость. vse.js дословно в своём Chrome (headless, DPR 1) дал window.__stili_vse, глубоко равный zamery/v2/stili-s2.json. Шесть строк итога: главная — 395 элементов, 1185 строк, 525 свойств, различий 0, К1 0, К2 3, К3 64; /404/ — 133, 399, 525, 0, 0, 3, 27; везде «видит; итог сверено».
- Согласованность итогов. stili-s2.json — это раскодированный .playwright-mcp/sesja-13/s2/stili-vse.json: тот записан как JSON-строка с JSON внутри, после раскодирования глубоко равен. В s2 нет CR, в конце LF, без BOM. Во всех 6 блоках ключ равен strona-sost, osnovnoe 0, К1 0, k1Ok true, К2 3 (все три строки — H1), К3 64/27 (все 8 первых — ::after), zryachest true, itog «сверено». Числа и pervye всех контролей равны прогону s1 (zamery/stili/*.json).
- Данные портов. По HTTP 4341 отдаёт LinkList.DHeL5m7t.css (sha256 d55c10a56a8f5815) и HTML, побайтно равные копии dist-main. 4342 отдаёт LinkList.B4mIKaMJ.css (fd4d087158c84e30) и HTML, равные dist-posle. index.xofYExtq.css (4b787071547e86e4) одинаков на обоих портах.
- Смысл pravil в выгрузке. pravil — это sheet.cssRules.length, правила верхнего уровня, как и написано в шапке. 176 у обоих LinkList не противоречит «слой утилит 20 → 19»: .lowercase вложено в @layer utilities, на верхнем уровне листы равны по счёту.
- Проверка имени и загрузки идёт на каждой загрузке, включая три загрузки контролей: kontrol вызывает zagruzit. zryachest вычисляется до удаления metki. У меток расхождения ('(метка)', '(число элементов)') нет суффикса ::after, поэтому такие различия в К3 дают «СЛЕПА», а не ложное «видит».
- Лист с другого порта (смешанная сборка) даёт остановку: cssRules бросает, «правил -1» (опыт e6). Одна сборка на обоих портах при верных именах тоже даёт остановку «нет листа» (e9).
- HTML обеих сборок. На главной 2 <link rel="stylesheet">, на /404/ — 1, все на /_astro/, <style> 0. В листах 0 @import. Других листов, которые проверка могла бы пропустить (alternate, preload as=style), нет.
- Состояние репозитория. Опыты шли в своём Chrome через Playwright из кэша npx, а не через MCP: MCP пишет page-*.yml и журналы консоли в .playwright-mcp репозитория. В .playwright-mcp 72 записи до и после работы, в sesja-13/s2 без изменений. Все мои файлы лежат в C:/Users/MSI/AppData/Local/Temp/claude/d--SEO-cloud-site-generator/964bb344-7148-445a-bd5b-86da32465c4d/scratchpad/tz/sud-r2/stili/ (proverka-faylov.mjs, probnik2.mjs, e*.json, vse.json, posl.json, gen/).

