Пересъёмка события «кадры рядам» (П57/П58), сборка 1d9b2ac, драйвер Playwright MCP (headless Chromium), 2026-09-16.
Генератор — sniazka-gen.mjs этой папки (копия П50 + место okruszki, пять якорей слоёв игры, режим visitor); судим двумя линзами до пересъёмки.

Пробы генератора (proba — до поправок судей, proba2 — после): 28 кадров, все побайтово равны между пробами.
  proba2/visitor первым запуском в холодном браузере — СТОП «загружено 15 из 16»: пятая карточка ленты (x = 1185) за правым краем окна,
  Chrome её лениво не грузит; холодный прогон без стопа (visitor-zimny) и тёплый (proba) — оба кадра побайтово равны. Стоп переписан:
  считаются картинки с left < clientWidth (13/13), за краем — строкой лога.

Главная (11 кадров): p1 = p2 = p3 побайтово 11/11; геометрия p1 = p2 = p3; высоты 9957 / 13953.
  p1 vs эталон dc5fc16 (check-p1-glowna-vs-dc5fc16.txt): 8 кадров 0 различий; layer-jerozolima 1250 (макс 2) — дата под шапкой (п. 7);
  полные 1440/390 — размеры разошлись (10270 → 9957, 14719 → 13953) — подвал без блока «Zdjęcia» (п. 4); exit 1.
  Карта по месту (mapa-glowna.txt): остаток вне мест 0 на всех 11 кадрах.
Игра (15 кадров): p1 = p2 = p3 побайтово 15/15; геометрия p1 = p2 = p3 (11 якорей); высоты 11821 / 14831.
  p1 vs эталон dc5fc16 (check-p1-gra-vs-dc5fc16.txt): 10 прежних кадров вне допуска (крошки 43,84 / 66,69 — фаза субпикселя всего ниже шапки, п. 1;
  ряды 2–8 в две колонки, п. 2; подвал, п. 4), 5 новых — «ЛИШНИЙ» (п. 3); exit 1. Карта по месту (mapa-gra.txt): остаток 0 на всех 10 кадрах.
visitor (2 кадра): v1 = v2 побайтово (каждый — в свежем браузере); первый экран = прежний visitor-эталон (47acc0db…) побайтово; полный 390×13953.

Эталон — p1 (главная, игра) и v1 (visitor): выбирать большинством не пришлось.
check p1/p2/p3 против нового эталона: главная 11/11 — 0 различий, игра 15/15 — 0 различий, геометрия совпала (14 / 22 якорей), exit 0 ×6.
Отрицательная проба — прежний эталон dc5fc16 против нового: главная — 3 кадра вне допуска (два полных, jerozolima), exit 1;
  игра — 15 из 15 (10 прежних — крошки/ряды/подвал, 5 — не сняты), exit 1.
npm run accept — 225/225, exit 0.
Кадры прогонов — .playwright-mcp/sesja-2026-09-16/ (proba, proba2, p1, p2, p3, v1, v2; игнорируется git); в репозитории — эталон (8d9ba13) и эти записи.
