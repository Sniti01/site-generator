######## R5-SVERKA-1 [дефект → подтверждено/дефект]
Правка R4-SVERKA-9 открыла обход, которого раньше не было. Сбросы `--font-display-*`, `--f-*`, `---*` после --font-display, а также `--font-*: initial` в позднем `@theme reference` или `@theme default` дают «сверено», а Tailwind убирает --font-display со страницы

ДОКАЗАТЕЛЬСТВО: Стенд D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/sverka/css.mjs, итог в css.json и css.log, выход сборки в css-out/S*.css. Цепочка: настоящая sverka() (HEAD 22234e6) → Tailwind 4.3.3 (compile → Scanner → build → optimize) → lightningcss, как у Vite 8 → Chromium 151.0.7922.34 без окна, порт 4956. Гарнитура h1 взята по CDP getPlatformFontsForNode.
- S1 `--font-display-*: initial;` сразу после --font-display: «сверено». В выходе --font-display 0 раз, h1 — «Public Sans Thin SemiBold».
- S2 `--f-*: initial;` и S3 `---*: initial;`: то же.
- S4 поздний `@theme reference { --font-*: initial; }` и S5 `@theme default { --font-*: initial; }`: «сверено», --font-display 0 раз, h1 — «Segoe UI Semibold».
- Контроль S0 `--font-*: initial` после --font-display: отказ, h1 — Public Sans (вред настоящий).
- Прежняя сверка 75350ea (staraya.mjs, staraya.json) на S1–S5 давала громкий отказ «объявление не читается». Значит, это регресс правки.
Причина в Tailwind (node_modules/tailwindcss/dist/lib.js). `add()` делает `i==="--*"?this.values.clear():this.clearNamespace(i.slice(0,-2),0)`, а `clearNamespace` удаляет каждый ключ, для которого `n.startsWith(i)`. Сброс работает по префиксу и при любых опциях @theme (второй аргумент 0). Сверка же (znak.mjs:339) ловит только имена ровно `*` и `font-*` и только в блоках с `gde === '@theme'`. `@theme reference` и `@theme default` попадают в «другой блок» (THEME :187). Шапка :24 и :38 обещает: «сброс читается как сброс» и «сброса --font-* или --* после него в @theme нет». Браузерный судья гарнитуру не видит: grep «font-display|Bodoni|fontFamily|getPlatformFonts» по instrumenty/wiernosc.mjs даёт 0 совпадений.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:139-141 (имя сброса принимается), :338-340 (poFd и sbrosy), :187 (THEME); обещание — шапка :22-24, :38

ПРЕДЛОЖЕНИЕ: Считать сброс опасным по правилу Tailwind: имя `--X-*` накрывает --font-display, если '--font-display'.startsWith(имя без «-*»). Сюда входят `--*`, `---*`, `--f-*` и `--font-display-*`. Искать сбросы во всех верхних `@theme` с любыми опциями (reference, default, inline, static) по порядку листа после первого --font-display. Либо вернуть громкий отказ на любой сброс, кроме `--x-*` с префиксом, который не является префиксом `--font-display`. Добавить в selftest пробы S1 и S4. После раунда 5 правок нет, поэтому до решения назвать это в ПРЕДЕЛАХ доклада: «сброс по префиксу и сброс в @theme reference или default сверка не видит, а браузерный судья гарнитуру не судит».

СКЕПТИК: Опровергнуть не удалось. Стенд свой, стендом линзы не пользовался: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/skeptik-sverka/proby.mjs, итог в proby.json и proby.log, выход сборки в out/<id>.css. Цепочка: нынешняя sverka() (HEAD) и прежняя (stand/STARY = 75350ea, выгружена git show 875fb7e^) → Tailwind 4.3.3 (compile, oxide Scanner, build, optimize) → lightningcss 1.32 → Chromium 151.0.7922.34 без окна, порт 4977. Страница — dist/index.html, лист — выход цепочки плюс 170 блоков компонентов из dist-листа.

Что показали пробы:
- S1 (--font-display-*), S2 (--f-*) и S3 (---*) после --font-display, S4 (@theme reference { --font-*: initial }), S5 (@theme default { --font-*: initial }): ныне «сверено». --font-display в выходе 0 раз. h1 по CDP — Public Sans Thin SemiBold у S1–S3 и Segoe UI Semibold у S4 и S5.
- Прежняя сверка на всех пяти давала отказ «объявление не читается». Это регресс.
- Сверх линзы, S6 (@theme reference { --*: initial }) и S7 (@theme default { --font-display-*: initial }): тоже «сверено», и вред тот же.
- Контроли. S0 (--font-*) и S9 (поздний @theme inline { --font-* }) — отказ. S8 (--font-display-* до --font-display) и S10 (--fonts-*, не префикс) — «сверено», Бодони на месте. Значит, правило линзы через startsWith отличает опасный сброс от безвредного.

Код Tailwind линза прочла верно. В lib.js add() делает `i==="--*"?values.clear():clearNamespace(i.slice(0,-2),0)`. clearNamespace удаляет ключи по startsWith и опций не смотрит, а обработчик @theme зовёт f.add для каждого `--`-объявления любого @theme, в том числе reference и default. znak.mjs:339 ловит только imie '*' и 'font-*' при gde '@theme', а THEME :187 знает только голый, inline и static.

Это заново открытый класс R3-SVERKA-3: там сброс, убирающий --font-display, признан дефектом и был закрыт громким отказом на любом «*». Правка R4-SVERKA-9 сузила отказ и открыла класс снова. Модель шапки :36-37 («@theme reference в CSS не выходит») неверна: сброс в таком блоке действует. Гарнитуру wiernosc.mjs не судит (grep — 0), а :63-65 называет последним судьёй только краски. В шапке, README эталона, DESIGN.md и BACKLOG §60 это не названо. Формы нарочитые, вес малый, но по R2-SVERKA-7 вес класс не меняет.

######## R5-SVERKA-2 [дефект → иной класс/предел]
Правка R4-SVERKA-2 обходится экранированием в имени: `@property --acc\65nt` даёт «сверено», а lightningcss выпускает `@property --accent`, и трассер красный

ДОКАЗАТЕЛЬСТВО: css.mjs, итог в css.json, css-out/P1.css. Проба P1: `@property --acc\65nt { syntax: '<color>'; inherits: false; initial-value: #ff0000; }` в конце листа. Сверка — «сверено». Tailwind оставил имя, lightningcss раскрыл экранирование: в выходе `@property --accent{syntax:"<color>";inherits:false;initial-value:red}` и запасной `--accent:red` в @layer properties. На странице --accent на :root — rgb(236, 168, 74), а заливка трассера и ромба знака шапки и подвала — rgb(255, 0, 0). Контроль P0 (`@property --accent`): отказ «зарегистрирован @property», страница та же. У прежней сверки (staraya.json, P1) тоже «сверено»: проверки @property не было вовсе, правка неполна. Причина: znak.mjs:321 сравнивает имя сырым текстом `/^@property\s+--([-\w\u0080-￿]+)$/i`, и обратная косая черта в класс не входит. У объявлений экранирование в имени — громкий отказ (:140, шапка :20), у прелюдии @property — молчание. Против шапки :29 («не зарегистрировано `@property`»). Вес малый: краску ловит браузерный судья, по прозе :63-65 он «последний судья» красок. Формы `@propert\79  --accent` и `@property/**/--accent` (P2, P3, R-P2b в razl.json) безвредны: Tailwind выпускает `@property--accent`, страница цела.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:321-322; обещание — шапка :29-30

ПРЕДЛОЖЕНИЕ: Перед сравнением раскрывать экранирование в имени @property (та же замена, что в imyaGarnitury: hex и простое). Либо давать громкий отказ на любую прелюдию `@property`, в которой есть «\», как у объявлений. Добавить пробу `@property --acc\65nt` → «зарегистрирован @property». Иначе — строкой в ПРЕДЕЛЫ доклада.

СКЕПТИК: Факты воспроизведены, класс снижаю. Стенд — proby.mjs, итог в proby.json.
- P1 (`@property --acc\65nt`) и сверх линзы P1b (`@property --\61 ccent`): ныне «сверено». В выходе `@property --accent{syntax:"<color>";inherits:false;initial-value:red}` и запасное `--accent:red` в @layer properties. На :root --accent = rgb(236,168,74), а трассер и ромб знака шапки и подвала — rgb(255,0,0).
- Экранирование раскрывает не только второй проход lightningcss. Страница с выходом optimize Tailwind без этого прохода тоже красная: браузер сам читает `--acc\65nt` как --accent.
- Контроль P0 — отказ.
- Прежняя сверка на P0 и P1 давала «сверено»: проверки @property не было. Значит, это неполнота правки R4-SVERKA-2, а не регресс.
- Код линза прочла верно: :321 сравнивает имя сырым текстом, «\» в класс не входит.

Почему не дефект. Тот же пакет 875fb7e дописал в раздел шапки «Что сверка НЕ судит» строки :63-65: «Разбор — модель CSS… для форм, которых не знает сегодня, последний судья — вычисленные краски на собранной странице (wiernosc.mjs)». Экранированное имя в прелюдии at-правила — как раз такая форма, а вред здесь — только краска. Краску wiernosc судит: вычисленная заливка каждой части шапки и подвала против литерала иконки (wiernosc.mjs:343-344, проба самопроверки «фонарь токеном другой» :223). rgb(255,0,0) против #eca84a даёт его отказ «краска». В раунде 3 (R3-SVERKA-2, та же форма у объявлений — дефект) такой строки ещё не было. Для гарнитуры (R5-SVERKA-1, -3) эта строка не работает: её никто не судит.

Остаток — проза. Обещание :29-30 («не зарегистрировано @property») и DESIGN.md:688 («лист, которого разбор не понимает, — отказ») экранирование в прелюдии не оговаривают. Нужна строка в ПРЕДЕЛАХ: «экранирование в имени @property сверка не раскрывает — краску судит wiernosc». Если читать :29 без оговорки :63-65, это дефект малого веса.

######## R5-SVERKA-3 [дефект → подтверждено/дефект]
Правка R4-SVERKA-3 неполна: продолжение строки (`\` и перевод строки внутри кавычек) и экранирование в имени at-правила (`@font-f\61 ce`) дают «сверено», а заголовки рисуются Georgia

ДОКАЗАТЕЛЬСТВО: css.mjs (F1), razl.mjs (R-F3b); итоги в css.json и razl.json, выход — css-out/F1.css и css-out/R-F3b.css. Во всех пробах src: local('Georgia'), font-weight 600.
- F1 `@font-face { font-family: 'Bodoni \<перевод строки>Moda'; … }`: «сверено». В выходе `@font-face{font-family:Bodoni Moda;font-style:normal;font-weight:600;src:local(Georgia)}`, h1 — «Georgia». В CSS «\» с переводом строки внутри строки — продолжение: оба символа снимаются. В imyaGarnitury (:315) `.replace(/\\(.)/g, '$1')`, а «.» в JS перевод строки не берёт. Остаётся «bodoni \ moda», подстроки «bodoni moda» в нём нет.
- R-F3b `@font-f\61 ce { font-family: 'Bodoni Moda'; … }`: «сверено». lightningcss выпускает `@font-face{font-family:Bodoni Moda;…local(Georgia)}`, h1 — «Georgia». :318 сравнивает имя at-правила сырым текстом `/^@font-face$/i`.
- Обе формы и прежняя сверка пропускала (staraya.json: F1 и F3b — «сверено»). Но правка обещает шапкой :33-35: «своей @font-face для 'Bodoni Moda' в листе нет ни на какой глубине (имя сравнивается как его читает CSS: регистр, кавычки, пробелы, экранирование)». Мерка — R3-SVERKA-2 (обход закрытого класса).
- Контроль F0 (`'Bodoni Moda'`): отказ. F2 (`Bodoni/**/Moda`) безвреден: Tailwind выпускает `BodoniModa`, h1 — Bodoni. Опечатка `@font-f\61ce` без пробела — это U+61CE (Tailwind выпустил `@font-f懎`), не @font-face.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:315 (imyaGarnitury), :318 (имя at-правила); обещание — шапка :33-35

ПРЕДЛОЖЕНИЕ: В imyaGarnitury первым шагом снимать продолжение строки: `\\` вместе с CRLF, LF, CR или FF. Имя at-правила перед `/^@font-face$/i` раскрывать тем же способом, либо давать отказ на at-правило с «\» в имени. Добавить пробы F1 и `@font-f\61 ce`. Иначе — строкой в ПРЕДЕЛЫ доклада.

СКЕПТИК: Для F1 опровергнуть не удалось. Часть `@font-f\61 ce` — вне диффа. Стенд — proby.mjs, итог в proby.json.

F1 (`font-family: 'Bodoni \<LF>Moda'`, local('Georgia')): ныне «сверено». В выходе `@font-face{font-family:Bodoni Moda;…;src:local(Georgia)}`, h1 по CDP — Georgia, и с сырым выходом Tailwind тоже. imyaGarnitury (:315) появилась в 875fb7e: в `.replace(/\\(.)/g,'$1')` точка перевод строки не берёт, остаётся «bodoni \ moda». Поправка к линзе: по CSS Syntax «\» с переводом строки — продолжение строки, формально это не «valid escape». Но шапка :33-35 обещает сравнение «как его читает CSS», а CSS читает это имя как Bodoni Moda. Обещание не держится. Прежняя сверка тоже давала «сверено», так что это неполнота правки R4-SVERKA-3; мерка — R3-SVERKA-2 (обход закрытого класса). Гарнитуру wiernosc не судит, :63-65 закрывает только краски, так что предела нет.

Сверх линзы, F1c (продолжение строки с CRLF): Tailwind падает «Unterminated string» — громко и безопасно. Обход работает только с LF, а листы сайта — в LF.

F3b (`@font-f\61 ce { font-family: 'Bodoni Moda' }`): «сверено», Georgia, и без второго прохода тоже. Но проверка имени at-правила :318 `/^@font-face$/i` в 875fb7e не менялась, а обещание «своей @font-face нет» стояло и до пакета. Это старый класс, который пакет не трогал: строка в отчёт, не находка раунда 5. Вес малый, формы нарочитые.

######## R5-SVERKA-4 [дефект → подтверждено/дефект]
Самопроверка не сторожит четыре новые ветви: мутанты MB1, MB2, MB3 и MB4 проходят 94/94, а на входах, где настоящий судья отказывает, дают «сверено» с вредом на странице. MB2 снимает ровно ту форму G1b, из-за которой R4-SVERKA-8 подняли до дефекта

ДОКАЗАТЕЛЬСТВО: Мутанты — D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/sverka/mutanty.mjs, итог в mutanty.json и mutanty.log. Каждый мутант — копия stand/<id>/ с одной точной заменой (вхождение ровно одно). Контроль M00 — 94/94. Различители — razl.mjs, итог в razl.json.
- MB1: снято `.replace(/\\(.)/g, '$1')` в imyaGarnitury (:315). --selftest 94/94, exit 0; --check — exit 0. Вход `@font-face { font-family: Bodoni\ Moda; … local('Georgia') }` (F3 раунда 4): настоящий — «своя @font-face», мутант — «сверено», h1 — Georgia.
- MB2: `\1` заменено на `['"]` в регулярке --font-display (:334). 94/94, exit 0. Вход `'Bodoni Moda", ui-serif, Georgia, serif'` (G1b раунда 4): настоящий — «не начинается», мутант — «сверено», h1 — Times New Roman.
- MB3: снято `|| pr[1] === 'font-display'` (:322). 94/94, exit 0. Вход `@property --font-display { syntax: '<length>'; inherits: true; initial-value: 0px; }`: настоящий — «зарегистрирован @property», мутант — «сверено». На странице --font-display = «0px», h1 — Public Sans Thin SemiBold.
- MB4: снят флаг /i у регулярки @property (:321). 94/94, exit 0. Вход `@PROPERTY --accent {…inherits: false; initial-value: #ff0000}`: настоящий — отказ, мутант — «сверено», заливка трассера шапки и подвала — rgb(255, 0, 0).
Линза раунда 4 прямо предлагала пробы F3 (R4-SVERKA-3) и F1b (R4-SVERKA-8), коммит 875fb7e говорит «новые — на всё названное», но таких проб в selftest (:444-538) нет. Пробы @property есть только у --accent, --ink и --glow. Мерка — R3-SVERKA-4: мутант с ложным «сверено» и вредом на странице проходит самопроверку. Для сравнения, MB8–MB13 (значение-блок, @property снят, сбросы, FONT-FAMILY) убиты: 91–93/94.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:444-538 (пробы), неохраняемые ветви — :315, :321, :322, :334

ПРЕДЛОЖЕНИЕ: Добавить четыре пробы: `@font-face { font-family: Bodoni\ Moda }` → «своя @font-face»; `--font-display: 'Bodoni Moda", ui-serif, Georgia, serif'` → «не начинается»; `@property --font-display {…}` → «зарегистрирован @property»; `@PROPERTY --accent {…}` → «зарегистрирован @property». Правок после раунда 5 нет, поэтому до решения — строкой в ПРЕДЕЛЫ доклада: «эти четыре ветви ни одна проба не сторожит».

СКЕПТИК: Опровергнуть не удалось. Стенд — mutanty.mjs, итог в mutanty.json и mutanty.log. Копии — stand/<id>/, в каждой одна точная замена, вхождение ровно одно.
- Контроль K00: 94/94, --check exit 0.
- MB1 (снято `.replace(/\\(.)/g,'$1')`), MB2 (`\1` заменено на `['"]`), MB3 (снято `|| pr[1] === 'font-display'`), MB4 (снят флаг /i у регулярки @property): у каждого --selftest 94/94, exit 0, --check exit 0.
- Стенд кусается: MX (снят отказ @property) даёт 92/94, падают обе пробы @property.

Различители (нынешний судья → мутант), вред — по proby.json:
- D1 `font-family: Bodoni\ Moda`: «своя @font-face» → «сверено», h1 — Georgia.
- D2 `'Bodoni Moda", ui-serif, Georgia, serif'`: «не начинается» → «сверено», h1 — Times New Roman.
- D3 `@property --font-display { <length>, 0px }`: «зарегистрирован @property» → «сверено». --font-display на :root = «0px», h1 — Public Sans Thin SemiBold.
- D4 `@PROPERTY --accent`: отказ → «сверено», трассер шапки и подвала — rgb(255,0,0).

Проб на эти ветви в :444-538 нет. F3 (R4-SVERKA-3) и F1b (R4-SVERKA-8) линза раунда 4 предлагала поимённо, а сообщение 875fb7e говорит «новые — на всё названное». Покрытия каждой ветви шапка :50-54 не обещает. Но мерка R3-SVERKA-4 и R4-SVERKA-4 такая: мутант с ложным «сверено» и вредом на странице проходит самопроверку — это дефект. У MB1–MB3 вред в гарнитуре, её не судит никто. MB4 (краску) сам по себе поймал бы wiernosc, но класс находки это не меняет.

######## R5-SVERKA-5 [предел → подтверждено/предел]
Правка R4-SVERKA-8 судит только первый элемент списка гарнитур. Испорченный хвост (`'Bodoni Moda',, …`, `'Bodoni Moda', 'x' 'y', serif`, `'Bodoni Moda', 12px`) даёт «сверено», а заголовки идут Public Sans; в ПРЕДЕЛАХ это не названо

ДОКАЗАТЕЛЬСТВО: css.mjs (G1, G2, G3), итог в css.json. Все три — «сверено», сборка ok. В выходе `--font-display:"Bodoni Moda",, ui-serif, Georgia, serif` (и два других значения), h1 — «Public Sans Thin SemiBold»: font-family недействителен во время вычисления. Контроль G0 (непарные кавычки) — отказ. Буква шапки :37-38 («первым в списке стоит ровно 'Bodoni Moda' (за ним — запятая или конец)») соблюдена. Но линза R4-SVERKA-8 предлагала как альтернативу назвать в ПРЕДЕЛАХ: «грамматику списка гарнитур сверка не судит». Этого не сделано. Скептик раунда 4 относил такие формы (G1, G1c, G1d) к пределу. Браузерный судья гарнитуру не судит (grep по wiernosc.mjs — 0 совпадений), а проза :63-65 называет последним судьёй только краски.

ГДЕ: sites/7thserpent.com/tools/znak.mjs:334; шапка :37-38, ПРЕДЕЛЫ :78-92

ПРЕДЛОЖЕНИЕ: Назвать в ПРЕДЕЛАХ шапки или доклада: «после первой гарнитуры список не судится; испорченный хвост снимает --font-display со страницы, и это никто не судит». Либо (после решения владельца) проверять каждый элемент через запятую: строка в парных кавычках или последовательность идентификаторов, пустых элементов нет.

СКЕПТИК: Факты воспроизведены, класс верен. Стенд — proby.mjs, итог в proby.json.
- G1 (`,,`), G2 (`'x' 'y'`), G3 (`12px`) и сверх линзы G4 (висячая запятая `serif,`): и ныне, и прежде «сверено». Сборка ok, --font-display доходит до выхода с этим значением, h1 по CDP — Public Sans Thin SemiBold.
- Контроль D2 (непарные кавычки) — отказ.

Буква шапки :37-38 («первым ровно 'Bodoni Moda', за ним запятая или конец») соблюдена. Хвост списка не судился и до пакета. По мерке R4-SVERKA-8 (скептик раунда 4: такие формы «сами по себе предел») и R3-SVERKA-5 класс — предел. В ПРЕДЕЛАХ :78-92, в разделе :56-65, в DESIGN.md, README эталона и BACKLOG §60 это не названо (grep «список гарнитур|грамматик» — 0). wiernosc гарнитуру не судит, так что вред молчит. Остаток — строка в ПРЕДЕЛАХ, как предлагает линза.

######## R5-SVERKA-6 [предел → подтверждено/предел]
Правка R4-SVERKA-6/-7 верна, но её не сторожит ни одна проба. Проба «иконка 16 из многоугольников» зовёт только sverka(), а падала строка итога в запуске. Мутант с прежней строкой проходит 94/94 и на диске снова пишет файлы, а потом печатает «ОТКАЗ»

ДОКАЗАТЕЛЬСТВО: mutanty.json (MB5, MB6); stand2.mjs, итог в stand2.json.
- MB5 (вернул прежнюю строку итога :573, `…ikona16.prostokaty.map` без `?? []`): --selftest 94/94, exit 0.
- На копии, где ikona16 ровно такая, как в пробе :530 (`czesci` — один многоугольник, `prostokaty` нет): настоящий код (T-M00) — запись exit 0 «записано», --check exit 0. MB5 (T-MB5) — запись exit 1 «znak: ОТКАЗ — Cannot read properties of undefined (reading 'map')». favicon-16x16.png и favicon.ico при этом уже переписаны. --check — тоже exit 1.
- MB6 (снято `kraski.set(imie, v)`): 94/94. Строка --check: «краски, которые взяли три рисунка: » — пусто.
Сама правка работает: у настоящего кода итог при ikona16.czesci краски surface — ink, accent, bg, surface, при ikona.prostokaty краски danger — с danger. Шапка :50-54 называет «в памяти не пробуется» только katalog. Сбой и пустая строка — на безопасной стороне, мерка — R4-SVERKA-6 (класс «предел»).

ГДЕ: sites/7thserpent.com/tools/znak.mjs:530 (проба), :573-574 (строка итога), :541 (selftest зовёт только sverka); шапка :50-54

ПРЕДЛОЖЕНИЕ: Назвать в шапке рядом с katalog: «строка итога и запись в файлы в памяти не пробуются». Либо собирать строку итога внутри sverka и в пробе :530 ждать kraski = {ink, accent, bg}.

СКЕПТИК: Факты воспроизведены.
- mutanty.json: MB5 (прежняя строка итога без `?? []`) и MB6 (снято `kraski.set`) — оба 94/94, exit 0.
- disk.mjs, итог в disk.json и disk.log; ikona16 ровно как в пробе :530. T-K00 (настоящий код): запись exit 0 «записано», переписаны favicon-16x16.png и favicon.ico, --check exit 0. T-MB5: запись exit 1 «ОТКАЗ — Cannot read properties of undefined (reading 'map')», и те же два файла уже переписаны; --check exit 1.
- MB6 --check: «краски, которые взяли три рисунка:» — пусто. У K00 там ink, accent, bg.

Проба :530 прошла бы и на коде до правки. sverka() на такой иконке не падала никогда (ikonaSvg рисует с `?? []`). Падала только строка итога в запуске, а selftest её не зовёт (:541). Сама правка работает. Сбой и неполная строка остаются на безопасной стороне: либо exit 1, либо верные файлы. Мерка R4-SVERKA-6 — предел. Шапка :50-54 называет «в памяти не пробуется» только katalog.

######## R5-SVERKA-7 [документ → подтверждено/документ]
Новая проза шапки точнее кода. «Имя сравнивается как его читает CSS: … пробелы» — а код сводит пробелы и внутри кавычек и ищет подстроку. «Имена свойств — идентификаторы CSS, в том числе не-ASCII» — это верно только для custom property. Проба :497 закрепляет отказ на форме, которую CSS за Бодони не читает

ДОКАЗАТЕЛЬСТВО: css.mjs (F4, F5), итог в css.json; staraya.mjs (N1, N2), итог в staraya.json.
- F4 `@font-face { font-family: 'bodoni   moda'; … local('Georgia') }` (три пробела в кавычках, та же форма, что у пробы :497 «FONT-FAMILY и лишний пробел»): отказ «своя @font-face». На странице h1 — «Bodoni Moda 11pt SemiBold (свой файл)»: в кавычках CSS пробелы не сводит, это другая гарнитура. Проба ждёт отказ на безвредной форме.
- F5 `font-family: 'Bodoni Moda Fallback'` (обычная запасная гарнитура для метрик): отказ, h1 — Бодони. Причина — `.includes('bodoni moda')` (:318), подстрока, а не равенство.
- N1 `.x { _zoom: 1; }` и N2 `.x { цвет: red; }`: «объявление не читается». Регулярка обычного свойства :139 — `-?[a-zA-Z][a-zA-Z0-9-]*`, не-ASCII и `_` разрешены только после `--`.
Всё это на безопасной стороне (ложный отказ). Но шапка :23-24 и :34-35 обещает модель CSS, а код — модель с запасом.

ГДЕ: sites/7thserpent.com/tools/znak.mjs шапка :23-24, :34-35; код :139, :315, :318; проба :497

ПРЕДЛОЖЕНИЕ: Поправить прозу: «custom property — идентификатор CSS, в том числе не-ASCII; обычное свойство — ASCII» и «имя сравнивается с запасом: пробелы сводятся и в кавычках, вхождение 'bodoni moda' подстрокой — отказ». Либо сравнивать на равенство: в кавычках — строка как есть без учёта регистра ASCII, без кавычек — идентификаторы через один пробел. Пробу :497 назвать запасом, а не формой, которую читает CSS.

СКЕПТИК: Факты воспроизведены. Стенд — proby.mjs, итог в proby.json.
- F4 (`'bodoni   moda'`) и сверх линзы F6 (проба :497 дословно, FONT-FAMILY, но с local('Georgia')): отказ «своя @font-face», а h1 по CDP — Bodoni Moda 11pt SemiBold. В кавычках CSS пробелы не сводит, это другая гарнитура. Прежняя сверка на F4 давала «сверено». Ложный отказ внесла правка, а проба :497 его закрепляет.
- F5 (`'Bodoni Moda Fallback'`): отказ, и прежде тоже; h1 — Бодони. Причина — вхождение подстроки (:318 includes).
- N1 (`_zoom: 1`) и N2 (`цвет: red`): «объявление не читается». Ветвь обычного свойства :139 знает только ASCII `-?[a-zA-Z][a-zA-Z0-9-]*`. Контроль N3 (`--цвет`) — «сверено».

Всё это на безопасной стороне: ложный отказ, вреда нет. Но проза шапки :23-24 («Имена свойств — идентификаторы CSS, в том числе не-ASCII») и :34-35 («как его читает CSS: … пробелы») обещает модель CSS, а код держит модель с запасом. Класс — документ, как у R3-SVERKA-6.

######## ЧИСТО (sverka)
- Исходное состояние (1+1): `node tools/znak.mjs --selftest` — 94/94, exit 0 (selftest-nastoyashchiy.txt); `--check --dist` — exit 0, «сверено — 6 иконок public/ и dist/». znak.mjs после 875fb7e не менялся, окончания i/lf w/lf. git status: файлы репозитория не тронуты, всё моё — в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/sverka/.
- R4-SVERKA-1 закрыт (5 форм): A0 `:root { --accent: {}; }` → отказ «значение-блок» (без него на странице трассер rgb(0,0,0)); V4 `--\61 ccent: {}` → отказ. Формы с экранированным первым дефисом V1 `\2d -accent: {}`, V2 и V3 `-\-accent: {…}` дают «сверено», но Tailwind читает их как вложенное правило и выбрасывает, --accent на странице #eca84a, вреда нет (css2.json). Мутант MB8 убит: 91/94. Ложный отказ на законном не-токене `.x { --mixin: {…} }` (L8 раунда 4) назван в шапке :20-22.
- R4-SVERKA-2 — то, что правка закрывает (4): P0 `@property --accent` → отказ; `@propert\79  --accent` и `@property/**/--accent` безвредны (Tailwind выпускает `@property--accent`, страница цела); проба «@property у чужого токена» (--glow) — «сверено»; MB9 убит: 92/94.
- R4-SVERKA-3 — то, что правка закрывает (4): F0 `'Bodoni Moda'` → отказ; `Bodoni/**/Moda` безвреден (Tailwind выпускает `BodoniModa`, h1 — Бодони); MB13 (регистр font-family) убит 93/94; MISK (без обхода в глубину) убит 93/94. MB7 (снятие кавычек убрано) равносилен настоящему коду: includes() и так находит имя в кавычках — это не находка.
- R4-SVERKA-4 закрыт (2): при строгом критерии :544 MI — 93/94, MFD3 — 92/94, оба убиты своими пробами (mutanty2.json).
- R4-SVERKA-5 закрыт (7 + 1 названо): MT 92/94; MQ, ML, MF, MUQ, MK — по 93/94; MN — самопроверка падает, exit 1. Непроверенный в памяти katalog (MLS, MKF) назван в шапке :54.
- R4-SVERKA-6 и -7 по сути закрыты (3): настоящий код на копии с ikona16 только из многоугольников — запись и --check exit 0; краски итога берутся из вызовов farba() — при ikona16.czesci краски surface и при ikona.prostokaty краски danger они попадают в строку; «ОТКАЗ после записи» из-за итога больше невозможен (итог не падает, а отказ сверки бросается до записи :562).
- R4-SVERKA-8 — форма раунда 4 (3): G0 с непарными кавычками → отказ (h1 без правки — Times New Roman); пробы «без запятой» и «мусор после кавычки» на месте и проходят.
- R4-SVERKA-9 — то, что правка закрывает (5): L11 `--color-*: initial` в начале @theme — «сверено», страница цела (S6); `--font-*: initial` в начале — «сверено»; S0 `--font-*` после --font-display → отказ с настоящим вредом (Public Sans); сброс со значением не initial → отказ (Tailwind на нём сам падает: `Invalid theme value`); не-ASCII имя custom property — «сверено» (N3 `--a·b`, прежняя сверка отказывала). Мутанты MB10, MB11, MB12 убиты: по 93/94.
- Проза R4-PROZA-8 в шапке :72-75 (1): «без ссылки на него — favicon-32x32.png (и при масштабе 1)» — верно, в dowod/dowod.json d-1-svetlaya и d-2-svetlaya zapros_ikon = ["/favicon-32x32.png"].
- Косметика без последствий (1): в znak.mjs:505 две пробы склеены в одну строку. Счёт 94 и прохождение обеих верны.

######## R5-BRAUZER-1 [дефект → подтверждено/дефект]
Правки wiernosc.mjs раунда 4 не защищены самопроверкой: широкая рамка (б), страж движения, обход прокрутки и ответы окон пикселей можно выключить, и --selftest этого не заметит

ДОКАЗАТЕЛЬСТВО: Копии лежат в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/brauzer/. Их собирает kopia.mjs: код суда дословно, заменены только строка импорта помощника и главный блок. Каждый мутант — одна точная строка, отличия записаны в diff-kopij.txt. Прогоны шли на окне 1440 × 1, для обхода — {1440, 1, prokrutka: 1}, как в KROTKI. Критерий sudOk тот же, что у самопроверки.
(1) wF — строка :553, pos.szeroki заменён на pos.clip (узкая рамка вернулась). Проба «тень у знака за коробкой» даёт ok: 9 отказов, её ловят «за коробкой», (а) с 2880 субпикселями и узкое (б) с 2160 (у оригинала 6768). Изолирующая проба `.hdr__brand { filter: drop-shadow(150px 0 0 #eca84a) }` с ожиданием tolko ['наложение']: оригинал — отказ 4, (б) 3629; wF — «сверено». Такой пробы в MUTACJE нет (log-szeroki-w5.txt, log-szeroki-wF.txt).
(2) wG — строка :423, страж всегда возвращает null. Проба «анимация только по наведению» даёт ok с одной строкой от RUCH («h7ths на svg.znak»). У оригинала строк две, вторая — «рамка знака меняется сама за 500 мс». Пробы, где срабатывает только страж, нет; мутация a8 из R4 (WAAPI через 1,5 с) не добавлена (log-ruch-wG.txt, log-ruch-w5.txt).
(3) wA (:586, (б) в обходе выключено) и wB (:585, точки в обходе выключены). Проба «призыв над шапкой при прокрутке» даёт ok на обоих мутантах. У оригинала две строки, обе при scrollY 6234 (крайняя точка): «перекрыт» и «наложение». Обе стоят под одним видом «наложение: шапка, при прокрутке» (:589-593), поэтому строгий критерий по видам их не разделяет (log-prokrutka-w5.txt, -wA, -wB).
(4) wE (:596, ответы окон пикселей не судятся) — видно по коду. PODMENY вызывают только oknoPrzewin или ikony (:851-853). У MUTACJE mut задан, а :596 срабатывает только при !mut. Значит, --selftest с мутантом и без него одинаков. Положительный контроль: мой сервер 4953 (srv-dpr2.mjs) подменил 18 webp, которые берутся только при DPR 2. Копия дала 18 строк «байты не равны dist/» в окне 1440 × 2 (log-dpr2-w5.txt): проверка работает, но проба её не держит.
Контроль: wH (:449, счёт (б) выключен) на изолирующей пробе блока даёт НЕТ — для состояний R4-POLNOTA-2 закрыт. Выгрузка zamery/wiernosc-selftest.json в HEAD снята прежней редакцией судьи (instrument_sha256 3df1c9e3…, head 9b896e57); итога для 9a1ebd3d на момент линзы нет.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:252-258 (новые MUTACJE), :264-270 (PODMENY), :423, :553, :585-593, :596, :847-860

ПРЕДЛОЖЕНИЕ: Раунд 5 последний, поэтому в доклад это идёт пределом самопроверки: «широкая рамка (б), страж движения, каждая из двух проверок обхода и ответы окон пикселей своей убивающей пробы не имеют; мутанты wF, wG, wA, wB, wE переживают --selftest». Если правка будет, нужно четыре вещи. Первое — проба на предке `drop-shadow(150px 0 0)` с tolko ['наложение']. Второе — мигание из JS (setInterval или WAAPI с задержкой), где ждём только строку стража. Третье — в обходе развести виды «перекрыт: шапка, при прокрутке» и «наложение: шапка, при прокрутке» и дать по пробе на каждый. Четвёртое — подмена сервера с oknoPikseli {1440, 2}, где у webp, который берётся только при DPR 2, чужие байты, ждём tolko ['байты не равны dist/'].

СКЕПТИК: Повторил на своих копиях. Стенд: D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/skeptik-brauzer/, копии собирает kopie.mjs, код суда дословно, у каждой замены ровно одно вхождение.
(1) sk5-F — :553, pos.szeroki заменён на pos.clip. Проба «тень у знака за коробкой» прошла на всех семи окнах пикселей KROTKI: ok, 61 отказ, (б) 2160 вместо 6768 (log-szerokiF-sk5-F.txt). Изолирующая проба `.hdr__brand { filter: drop-shadow(150px 0 0) }` с tolko ['наложение'] на sk5-F дала «сверено». Остальные мутации MUTACJE рисуют внутри коробки знака ±2 px, так что широкую рамку не держит ни одна.
(2) sk5-G — :423, страж всегда null. «Анимация только по наведению» — ok одной строкой RUCH (log-straz-sk5-G.txt). Убивающая проба существует: мигание из JS через setInterval. На оригинале она даёт «движение: … рамка знака меняется сама за 500 мс», на sk5-G строки «движение» нет — НЕТ (log-straz-sk5.txt). Уточнение к предложению линзы: WAAPI с задержкой стража не изолирует, его видит RUCH в состоянии, потому что getAnimations() включает WAAPI. Мигание из JS изолируется только без tolko: оно же даёт «растр нестабилен».
(3) sk5-B (:585) на «призыв над шапкой при прокрутке» — ok, осталась одна строка (б) при scrollY 6234 (log-prokrutka-sk5-B.txt). Обе строки обхода стоят под одним видом (:593). wA — по логу линзы и по коду.
(4) wE — по коду. В самопроверке у всех MUTACJE задан mut, поэтому :596 не срабатывает. Чистый прогон (mut null) ответы судит, но без подмены отказов не даёт. PODMENY вызывают только oknoPrzewin и ikony (:852-853).
Всё это — дифф 22234e6 (R4-BRAUZER-1, -2, -4, -5). Шапки и доклад об этом не говорят. Класс тот же, что R4-BRAUZER-6 и -11: мутант переживает самопроверку.
Факт: zamery/wiernosc-selftest.json снят редакцией 3df1c9e3 при HEAD 9b896e57, итога для 9a1ebd3d нет.

######## R5-BRAUZER-2 [дефект → подтверждено/дефект]
«За коробкой» (R4-BRAUZER-1) судится только в покое и только в полосе шапки или ±60 px у подвала: копия знака ниже шапки при наведении, в фокусе или от фильтра предка проходит со «сверено»

ДОКАЗАТЕЛЬСТВО: Копия w5, 1440 × 1, лог log-zaKorobkoj-w5.txt. Четыре мутации дают «сверено», во всех пяти состояниях а=0, б=0:
- `.hdr__brand:hover svg { filter: drop-shadow(0 80px 0 #eca84a) }`;
- `.hdr__brand:focus-visible svg { -webkit-box-reflect: below 40px }`;
- `.ft__brand { filter: drop-shadow(0 100px 0 #eca84a) }`;
- `.hdr__brand { filter: drop-shadow(0 80px 0 #eca84a) }`.
Факт на странице снят fakt.mjs: кадр всего окна в том же состоянии против чистой страницы, зерно снято. Различий 3867, 3868, 23 623 и 3867 субпикселей. Рамка различий доходит до y 131 и 127 при низе шапки 69; в подвале — до y 643. На fakt/hover-cien80.png — полная оранжевая копия знака под шапкой, на fakt/ft-przodek-cien100.png — копия знака поверх ноты подвала.
Контроль: `.hdr__brand drop-shadow(150px 0 0)` даёт отказ 4, (б) 3629. Внутри полосы широкая рамка работает.
Причины три:
- stanZnakow и sadStylu считаются один раз на окно — при прокрутке 0, без наведения и фокуса (:487);
- zaKorobkoj читает только svg и его детей, предков не читает (:300);
- рамка (б) — полоса шапки до её низа или знак подвала ± 60 px (:405-410).
R4-BRAUZER-1 прямо называл случай «под :hover, :focus-visible … не видна никому». Шапка :35-37 и DESIGN.md:696 («смешивания, рисования за коробкой и движения у знака нет») говорят это без оговорки.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:35-37, :300, :405-410, :487; sites/7thserpent.com/DESIGN.md:696

ПРЕДЛОЖЕНИЕ: Раз правок больше нет, в ПРЕДЕЛЫ, DESIGN и доклад: «за коробкой — только в покое и только у svg и частей; то, что знак или предок рисует ниже полосы шапки или дальше 60 px от знака подвала, и то, что появляется при наведении или фокусе ниже шапки, не видит никто». Если правка будет: stanZnakow и zaKorobkoj — после prep каждого состояния; filter, box-shadow и отражение — у предков до .hdr или .ft, кроме backdrop-filter у .hdr; рамку (б) шапки опустить ниже её низа на 60–100 px; мутации «наведение: drop-shadow на 80 px вниз» и «предок: drop-shadow на 150 px» добавить в --selftest.

СКЕПТИК: Повторил копией sk5 на 1440 × 1 (log-korobka-sk5.txt, wyniki/korobka-sk5.json):
- `.hdr__brand:hover svg { filter: drop-shadow(0 80px 0 #eca84a) }` — «сверено», во всех состояниях а=0 и б=0;
- `.ft__brand { filter: drop-shadow(0 100px 0) }` — «сверено».
Контроли:
- то же у svg в покое — отказ «за коробкой»: его ловит только sadStylu, (б) там 0, потому что копия ниже полосы шапки;
- наведение и тень на 20 px, внутри полосы, — отказ (а) 1316 и (б) 3172.
Копия на странице видна: brauzer/fakt/hover-cien80.png, полная оранжевая копия знака под шапкой.
Код:
- stanZnakow и sadStylu выполняются один раз, при прокрутке 0, до всех prep (:486-487);
- zaKorobkoj читает только svg и его детей (:300);
- рамка (б) шапки кончается на низе .hdr (:408-409), у подвала — ±60 px (:410).
Шапка :36-37 («у знака и его частей нет filter…») не оговаривает, что это проверено только в покое. DESIGN.md:696 («рисования за коробкой … у знака нет») не оговаривает ничего. Скептик R4-BRAUZER-1 прямо называл случай «под :hover, :focus-visible». Правка 22234e6 закрыла его только в покое и в полосе шапки. Это неполная правка из диффа, а не старый класс.

######## R5-BRAUZER-3 [дефект → подтверждено/дефект]
Подмены иконок по R4-POLNOTA-3 не доходят до ветвей «по пикселям»: мутанты без сравнения пикселей PNG и записей ICO проходят все три пробы

ДОКАЗАТЕЛЬСТВО: Лог log-ikony.txt. На w5, на w5-wC (:760, проверка пикселей PNG выключена) и на w5-wD (:773, проверка пикселей записей ICO выключена) все три подмены иконок дают ok, чистый сервер — «сверено». Пиксели эти пробы не трогают:
- «favicon-32x32.png подменён записью 16» попадает в ветвь размера (:759 «16×16, ждали 32×32», дальше continue);
- «favicon.ico с одной записью» — в ветвь числа записей (:769);
- «favicon.svg другой» — в сверку байтов.
Положительный контроль ikony-piksel.mjs: размер тот же, один пиксель изменён XOR 0x40, лог log-ikony-piksel.txt. Оригинал даёт «/favicon-32x32.png по пикселям не равен эскизу favicon.svg при 32» и «запись 32 в favicon.ico по пикселям не равна эскизу favicon.svg». wC теряет первую строку, wD — вторую. На ветвь «иконка в dist/ не равна public/» (:751) пробы нет: подменой сервера её не достать. R4-POLNOTA-3 называл именно «PNG ≠ эскиз по пикселям» и «dist/ ≠ public/».

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:268-270, :751, :759-760, :769-773

ПРЕДЛОЖЕНИЕ: Две подмены нужно заменить или добавить. Первая — PNG 32 × 32 с одним чужим пикселем, ждём ['иконки: /favicon-32x32.png по пикселям']. Вторая — ICO, пересобранный с чужим пикселем в записи 32 (приём — в ikony-piksel.mjs), ждём ['по пикселям не равна']. Ветвь «dist/ ≠ public/» назвать пределом самопроверки. После раунда 5 — пределом в докладе.

СКЕПТИК: По коду ikony() все три подмены до ветвей пикселей не доходят:
- PNG 16 на месте 32: :759 пишет отказ по размеру и делает continue, до :760 дело не доходит;
- ICO с числом записей 1: остаётся только запись 16, она равна эскизу, поэтому :773 молчит, а отказ даёт :769;
- favicon.svg: сверка байтов (:750, :755), пиксели PNG сравниваются с эскизом, а не с отданным svg.
Значит, мутанты wC (:760) и wD (:773) дают тот же итог. Лог линзы log-ikony.txt это показывает, положительный контроль (log-ikony-piksel.txt) показывает, что ветви живые. Ветвь «dist/ ≠ public/» (:751) читает файлы с диска, подменой сервера её не достать — это предел самопроверки.
R4-POLNOTA-3 называл «PNG ≠ эскиз по пикселям» и «dist/ ≠ public/» прямо; подмены добавлены в диффе 22234e6. Класс — дефект самопроверки, как R4-BRAUZER-6.

######## R5-BRAUZER-4 [дефект → подтверждено/дефект]
kontrast-znaka: «контраст по вычисленной заливке частей» ромб (узел) не читает — тёмный ромб проходит со «сверено»

ДОКАЗАТЕЛЬСТВО: Строка :98 берёт фонарь только как getComputedStyle(p[1]).fill, то есть трассер. Ромб p[2] (zk1-latarnia, voprosy/A-semerka-trassa/znak.html:4) не читается; для снега при этом берётся минимум по трём частям. Копия k5.mjs (k-run.mjs, 390 × 1, 600 px, лог log-k-run.txt): `.hdr__brand .znak polygon:nth-of-type(3) { fill: #333 !important }` даёт «сверено». Граница — 7,05 / 11,9 / 8,43, в zalivka_znaka фонарь rgb(236, 168, 74). Контроль: тот же цвет у polygon 2 даёт «граница: фонарь 1.14:1 ниже порога 3:1». Коммит 22234e6 обещает «контраст — по вычисленной заливке частей» (R4-BRAUZER-10). Шапка :12-14 не говорит, что ромб не меряется. Заливку ромба поймал бы wiernosc п. 3, но о своей мере kontrast говорит неправду.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs:12-14, :96-100

ПРЕДЛОЖЕНИЕ: Фонарь брать минимумом по обоим многоугольникам фонаря (p[1], p[2]); пробу «тёмный ромб» добавить в --selftest. Раз правок нет — в шапке и докладе: «фонарь — только трассер; ромб держит wiernosc п. 3».

СКЕПТИК: Код :98: fonar = getComputedStyle(p[1]).fill, то есть трассер. Для снега берётся минимум по трём частям. Ромб — третий polygon эскиза, class zk1-latarnia (voprosy/A-semerka-trassa/znak.html:4), он не читается совсем. Тёмный ромб даёт «сверено», контроль с тёмным трассером — отказ (лог линзы log-k-run.txt); чтение кода это подтверждает однозначно.
Смягчение: комментарий :97 честно пишет «фонарь — трассер», а wiernosc п. 3 поймал бы тёмный ромб как «краска ≠ литерал», так что сквозной дыры нет. Но шапка :12-14 («контраст красок знака (фонарь, снег)») и коммит 22234e6 («по вычисленной заливке частей», R4-BRAUZER-10) обещают больше. Разбор асимметричен: снег — минимум по всем частям, фонарь — одна часть из двух. Правка в одну строку (минимум по p[1] и p[2]), поэтому класс — дефект кода, а не только прозы.

######## R5-BRAUZER-5 [дефект → подтверждено/дефект]
zapas-menu: «бургер или знак за краем окна или бургер не виден» — проверяются только правый край и ширина бургера 0

ДОКАЗАТЕЛЬСТВО: Строка :55: za_kraem = b.right > innerWidth + 0.5 || brand.right > innerWidth + 0.5 || b.width === 0. Копия z5.mjs (z-run.mjs, ширины 320, 400, 960, лог log-z-run.txt):
- `@media (max-width: 960px) { .hdr__brand { margin-left: -100px } }` — «сверено»; на 320 у знака left −84 и right 54, то есть 84 px знака за левым краем;
- `.hdr__burger { visibility: hidden }` — «сверено»; elementFromPoint в центре бургера возвращает .hdr__inner, бургера на экране нет;
- контроль `left: 300px` — «за краем … на ширинах 320, 400, 960».
Шапка :13-14 обещает все три случая.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/zapas-menu.mjs:11-14, :55

ПРЕДЛОЖЕНИЕ: Добавить в za_kraem условия brand.left < −0,5, b.left < −0,5 и «в центре бургера elementFromPoint — не бургер (или visibility не visible)». Либо сузить шапку: «правый край знака и бургера за окном или ширина бургера 0».

СКЕПТИК: Код :55: za_kraem = b.right > innerWidth+0,5 || brand.right > innerWidth+0,5 || b.width === 0. Левый край знака и бургера не проверяется. «Бургер не виден» сведено к ширине 0, а visibility, opacity и перекрытие не читаются. Значит, отрицательный margin-left у знака и visibility: hidden у бургера проходят (лог линзы log-z-run.txt; по коду вывод однозначен).
Шапка :13-14 («бургер или знак за краем окна или бургер не виден») и текст отказа :99 обещают все три случая. Правка из диффа 22234e6 (R4-BRAUZER-9). Инструмент выносит «сверено» случаю, который, по своей шапке, судит, — это дефект. Исправить можно кодом или сужением шапки.

######## R5-BRAUZER-6 [дефект → подтверждено/дефект]
kontrast --selftest: у ветвей «замер светлее границы» и «не загружено картинок» по-прежнему нет проб — R4-BRAUZER-11 закрыт не полностью

ДОКАЗАТЕЛЬСТВО: В zamery/kontrast-znaka-selftest.json 9 проб. Строк со «светлее» среди отказов 0 из 9, с «не загружено» — 0 из 9 (подсчёт node). R4-BRAUZER-11 предлагал три пробы: .noc z-index 60 («замер светлее границы»), токен rgb() и visibility у ссылки, а также картинку 404. Пакет добавил «не #rrggbb», «фокус не встаёт» (tabindex −1), «кольца нет» и «тёмную заливку»; пробы на :141 нет. Удаление :141 или :142 оставит самопроверку 9/9: чистая страница этих строк не даёт, и ни одна проба их не вызывает. Ветвь :142 мутацией в странице не вызвать: niezaladowane считается до мутаций (:67-74), а сверка сборки на старте (:57) отказала бы свой сервер с 404. Коммит пишет «самопроверка строгая 9/9 (R4-BRAUZER-10, -11)».

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs:67-74, :141-142, :164-175; zamery/kontrast-znaka-selftest.json

ПРЕДЛОЖЕНИЕ: Добавить пробу `.noc { position: relative; z-index: 60 }` с ожиданием ['замер светлее границы', 'ниже порога']. Ветвь «не загружено картинок» назвать пределом самопроверки в шапке и докладе.

СКЕПТИК: В zamery/kontrast-znaka-selftest.json 9 проб, все ok. Строк со «светлее» в отказах 0, с «не загружено» — 0 (мой подсчёт node).
Ни одна проба не делает содержимое под шапкой светлее белого листа границы. У «прозрачной шапки» и «листа над шапкой» граница сама становится белой. Поэтому удаление :141 самопроверка не заметит.
:142 мутацией не вызвать: niezaladowane считается до мутаций (:67-74), а свой сервер с 404 отказал бы уже sborkaIliOtkaz на старте (:57). Это предел самопроверки.
R4-BRAUZER-11 прямо предлагал пробу .noc z-index 60 и картинку 404. Коммит 22234e6 пишет «самопроверка строгая 9/9 (R4-BRAUZER-10, -11)». Остаток находки, которую правил дифф, класс — дефект самопроверки (как R4-BRAUZER-11). Для :142 — предел.

######## R5-BRAUZER-7 [дефект → подтверждено/дефект]
sborka --selftest 4/4 не судит расширенный разбор R4-BRAUZER-12: все четыре мутанта нового разбора проходят; итог самопроверки нигде не записан

ДОКАЗАТЕЛЬСТВО: Копия sb-kopia.mjs: код дословно, порты заменены на 4970–4973. Она даёт 4/4. Мутанты:
- bezStyl — url() в style="" выключен (:89);
- bezImport — @import выключен (:93);
- bezPoster — атрибуты poster, data и xlink:href убраны (:86);
- bezTegow — теги use, image и object убраны (:80).
Каждый даёт 4/4 (log-sb-selftest.txt). Причина: в dist/index.html 0 тегов <use>, <image>, <object>, 0 атрибутов poster=, xlink:href и data=, 0 url() в style=; в листе index.CmgQJVSa.css 0 @import. Пробы до нового разбора не доходят. Положительный контроль sb5.mjs (свой сервер 4952): разбор действительно берёт rel='stylesheet', оба вида @import и вложенный url(), poster, use, xlink:href, image, object и url() в style (wyniki/sb5.json). Итог самопроверки только печатается (:137-139), выгрузки в zamery/, как у других судей, нет. У «--selftest 4/4» в коммите нет пути.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/sborka.mjs:80, :86, :89, :93, :115-140

ПРЕДЛОЖЕНИЕ: Пробу собрать так: «/» с дописанными <video poster>, <object data>, <use href>, url() в style и листом с @import на пути вне dist/; ждём каждый путь в «не равен dist/ (в dist/ файла нет)». Итог писать в zamery/sborka-selftest.json. Раз правок нет — в доклад предел: «новый разбор без проб».

СКЕПТИК: Проверил dist/index.html. Тегов <use>, <image>, <object>, <embed>, <input>, <style> — 0. Атрибутов poster=, xlink:href, data= — 0. url() в style="" — 0. В листе index.CmgQJVSa.css @import — 0.
Четыре пробы --selftest (:117-122) — чистая, лист 203, шрифт 404 из url() листа и «/» без листа. Ни одна не вносит новых конструкций. Значит, ветви :80 (новые теги), :86 (poster, data, xlink:href), :89 (style) и :93 (@import и рекурсия) не срабатывают ни в одной пробе, а мутанты дают 4/4 (лог линзы log-sb-selftest.txt; по коду вывод однозначен).
Итог самопроверки только печатается (:137-139), выгрузки в zamery/ нет. У «--selftest 4/4» в коммите нет пути.
Новый разбор — дифф 22234e6 (R4-BRAUZER-12). Это правка судьи без убивающей пробы, дефект самопроверки.

######## R5-BRAUZER-8 [дефект → иной класс/документ]
Три выгрузки пакета сняты прежним помощником sborka.mjs: первая строка git status в них по-прежнему искажена, и R4-BRAUZER-13 в этих записях не закрыт

ДОКАЗАТЕЛЬСТВО: В zamery/zapas-menu.json, zapas-menu-selftest.json и kontrast-znaka-selftest.json pomoshchnik_sha256 = 826bae3c…. Это sborka.mjs коммита 875fb7e (`git show 875fb7e:…/sborka.mjs | sha256sum`), нынешний помощник — acdb34d9…. В них gryaz_instrumentov[0] = "M docs/…/wiernosc.mjs" (zapas-menu) и "M docs/…/kontrast-znaka.mjs" (kontrast-selftest) — ровно та порча, которую чинит R4-BRAUZER-13. Остальные четыре новые выгрузки (wiernosc.json, kontrast-znaka.json, faza-perekladiny.json, znak-podvala-okno.json) сняты помощником acdb34d9, и строки у них « M …» и «?? …». Сверку сборки в zapas-menu.json делал ещё прежний, узкий разбор. instrument_sha256 у всех семи равен нынешним файлам.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/zamery/zapas-menu.json, zapas-menu-selftest.json, kontrast-znaka-selftest.json (поля pomoshchnik_sha256, gryaz_instrumentov)

ПРЕДЛОЖЕНИЕ: Переснять zapas-menu в обоих режимах и kontrast-znaka --selftest с помощником HEAD. Либо назвать в докладе: «три выгрузки сняты помощником 826bae3c — первая строка грязи искажена, сборка сверена прежним разбором».

СКЕПТИК: Факты верны. zapas-menu.json, zapas-menu-selftest.json и kontrast-znaka-selftest.json несут pomoshchnik_sha256 826bae3c. Это sborka.mjs коммита 875fb7e (проверил `git show 875fb7e:…/sborka.mjs | sha256sum`), нынешний — acdb34d9. Первая строка gryaz_instrumentov у них «M …» без ведущего пробела. У остальных четырёх выгрузок помощник acdb34d9 и строки « M …» и «?? …». instrument_sha256 всех семи равен нынешним файлам.
Но код уже исправлен, а выгрузки честно называют помощника, которым сняты. Сверка сборки прежним разбором на нынешней dist/ даёт то же самое: у zapas-menu.json, kontrast-znaka.json, faza-perekladiny.json и znak-podvala-okno.json по 82 файла, набор (путь, sha256) одинаков — 52c7cb62c67c, ошибок 0. «Узкий разбор» на данные не повлиял.
Остаётся устаревшая запись с искажённой первой строкой грязи. Чинится она пересъёмкой или строкой в докладе, без правки кода. Поэтому класс — документ (записи), а не дефект.

######## R5-BRAUZER-9 [дефект → иной класс/предел]
R4-POLNOTA-6 закрыт наполовину: у znak-podvala-okno.mjs самопроверки нет, его ветви отказа не вызывались ни разу

ДОКАЗАТЕЛЬСТВО: sha256 znak-podvala-okno.mjs — 19e5f66c…, тот же, что в 9de3a99; пакет 22234e6 переснял только выгрузку. Grep «selftest|SELFTEST» по файлу — 0 совпадений. R4-POLNOTA-6 требовал ещё «знак подвала, сдвинутый на 1 px только в кадре окна»: отказы «различия в знаке подвала» и «полный кадр снят растром окна» не проверяла ни одна проба. Коммит связывает R4-POLNOTA-6 только с «sborka --selftest 4/4».

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/znak-podvala-okno.mjs; сообщение 22234e6

ПРЕДЛОЖЕНИЕ: В доклад пределом: «у znak-podvala-okno.mjs самопроверки нет, ветви отказа не проверены». Если правка будет — --selftest с мутацией «знак подвала на 1 px только в кадре окна».

СКЕПТИК: Факт верен: sha256 znak-podvala-okno.mjs — 19e5f66c. Последняя правка файла — 9de3a99, пакет 22234e6 его не трогал и лишь переснял выгрузку. Слов selftest и «самопровер» в файле нет. Ветви «различия в знаке подвала» и «полный кадр в растре окна» проба не вызывала ни разу. В прозе (DESIGN, README эталона, CLAUDE.md сайта, BACKLOG) это не названо.
Однако это унаследованный пробел — R3-POLNOTA-2, затем R4-POLNOTA-6. Пакет его не создал и не расширил; вердикт инструмента не ложный, не проверены только его ветви отказа. Коммит связывает R4-POLNOTA-6 только с sborka --selftest и полного закрытия не утверждает.
Это неназванный предел самопроверки: идёт в доклад строкой, а не дефектом диффа.

######## R5-BRAUZER-10 [документ → подтверждено/документ]
«При прокрутке — при DPR 1 по всей странице» (DESIGN, README): судья берёт точки шагом в полвысоты окна, а перекрытие между точками проходит

ДОКАЗАТЕЛЬСТВО: Копия w5 на окне {1440, 1, prokrutka: 0.5}, как в PELNY (log-pasek700-w5.txt). Мутация DOM: полоса position:absolute высотой 30 px с z-index 60 на y 700 документа. Итог — «сверено», обход прошёл 15 точек, zle []. Факт при scrollY 680 (fakt.mjs, wyniki/fakt-pasek.json): полоса закрывает полосу шапки — 183 130 субпикселей различий в рамке 0–68; elementFromPoint во всех пяти центрах частей возвращает полосу (div[style]). Точки 450 и 900 обходят диапазон ≈647–705. Шапка судьи честно пишет «шагом в полвысоты окна и в крайней точке» (:59-60). Проза говорит шире.

ГДЕ: sites/7thserpent.com/DESIGN.md:693-694; sites/7thserpent.com/_baseline/README.md:126

ПРЕДЛОЖЕНИЕ: «При DPR 1 — в точках шагом в полвысоты окна (450 px на 1440, 422 px на 390) и в крайней точке; перекрытие короче шага может пройти между точками».

СКЕПТИК: Код :571-575: точки обхода — y = 0, krok, 2·krok…, где krok = floor(высота · 0,5), то есть 450 на 1440 × 900 и 422 на 390 × 844, плюс крайняя точка maxY. Полоса высотой 30 px на y 700 документа лежит над знаком (y 15–53 окна) при scrollY примерно 647–715. В этот промежуток не попадает ни точка 450, ни 900, ни состояние «при прокрутке» на 2000. Итог «сверено» — лог линзы log-pasek700-w5.txt (сам не повторял: по коду вывод однозначен).
Шапка судьи :59-60 шаг называет честно. DESIGN.md:693-694 («при прокрутке — при DPR 1 по всей странице») и README эталона :126 («при DPR 1 — по всей странице») добавлены в 22234e6 (проверил git show) и говорят шире кода. Формулировка линзы с шагом 450 и 422 и оговоркой о перекрытии короче шага верна.

######## R5-BRAUZER-11 [документ → подтверждено/документ]
ПРЕДЕЛЫ и README о плотностях вне набора расходятся и с находкой, на которую ссылаются, и с замером нынешним судьёй

ДОКАЗАТЕЛЬСТВО: ПРЕДЕЛЫ wiernosc.mjs:88-91: «при других (замерено: 2,5) судья даёт ложный отказ на нетронутой сборке, а 0,9, 1,1, 1,925 и 2,625 не снимались вовсе (раунд 4, R4-BRAUZER-7, R4-POLNOTA-4)». README:124-125: «другие плотности не сняты: при 2,5 судья даёт ложный отказ». Но сама R4-BRAUZER-7 (SVODKA-r4.md:353) пишет «чисто в 11 других окнах», в том числе 1440 × 1,1 и 412 × 2,625. Копия нынешнего судьи (log-gestosci-w5.txt): 1440 × 1,1 — «сверено» (макс (б) 3), 412 × 915 × 2,625 — «сверено», 1440 × 0,9 — «сверено», 1440 × 1,925 — «сверено». 1440 × 2,5 — ложный отказ воспроизведён: «пиксели: подвал … 1970 субпикселей, макс 255». Выходит, «при других — ложный отказ» неверно для всех четырёх названных, а «не снимались вовсе» противоречит источнику.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:88-91; sites/7thserpent.com/_baseline/README.md:124-125

ПРЕДЛОЖЕНИЕ: Нужна такая формулировка: «(а) = 0 и допуск 3 замерены в полном прогоне на семи плотностях набора. Вне набора 2,5 даёт ложный отказ (подвал, (а)). 0,9, 1,1, 1,925, 2,625 и окна R4-BRAUZER-7 копия судьи прошла со «сверено» (раунды 4–5). Прочие плотности не снимались».

СКЕПТИК: Повторил копией sk5 (log-gestosci-sk5.txt):
- 1440 × 1,1 — «сверено», а=0 и б=0, максимум (б) 3;
- 1440 × 2,5 — ложный отказ «пиксели: подвал … 1970 субпикселей, макс 255», как у линзы и в R4.
Источник, на который ссылаются ПРЕДЕЛЫ, — SVODKA-r4.md:353 (R4-BRAUZER-7) и :445 (ЧИСТО). Там 1440 × 1,1 / 1,2 / 1,33 / 2,25, 412 × 2,625 и другие окна чисты при (а)=0 и (б) ≤ 3.
Значит, ПРЕДЕЛЫ wiernosc.mjs:89-91 неверны в двух местах, оба добавлены в 22234e6:
- «при других (замерено: 2,5) судья даёт ложный отказ» обобщает единственный случай 2,5 на все плотности;
- «0,9, 1,1, 1,925 и 2,625 не снимались вовсе» противоречит R4-BRAUZER-7 для 1,1 и 2,625; 0,9 и 1,925 линза раунда 5 сняла чистыми.
README:124-125 («другие плотности не сняты») тоже противоречит R4. Предложенная формулировка верна.

######## R5-BRAUZER-12 [предел → подтверждено/предел]
ПРЕДЕЛ sborka.mjs называет только import() и «позже»: браузер берёт ещё из <style>-блоков, <embed>, <input type=image> и статический import, а url() листа с rel="preload stylesheet" не читается (остаток R4-BRAUZER-12)

ДОКАЗАТЕЛЬСТВО: sb5.mjs, свой сервер 4952: к «/» дописаны ссылки на пути вне dist/ (wyniki/sb5.json, log-sb5.txt). Браузер запросил, разбор не взял:
- /t10-style-blok.css и /t11-style-blok.png — @import и url() во встроенном <style>;
- /t12-embed.svg;
- /t13-input.png;
- /t14-import-statyczny.js — статический import во встроенном модуле.
Лист с rel="preload stylesheet" взят файлом, но его url() не прочитан: /t01a из листа с rel='stylesheet' взят, /t02a — нет. Это ровно d.css из R4-BRAUZER-12. На нынешней сборке дыры нет: в dist/index.html 0 <style>, 0 embed, 0 input и 0 import, а wiernosc видит такие запросы по ответам в окнах.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/sborka.mjs:10-12, :80, :86

ПРЕДЛОЖЕНИЕ: Дописать в ПРЕДЕЛ: «<style> в HTML, embed, input type=image, статический import модулей; url() листов, у которых rel — не stylesheet первым токеном». Либо rel разбирать по токенам.

СКЕПТИК: По коду sborka.mjs:
- теги (:80) — link, script, img, source, video, audio, iframe, use, image, object: без embed и input;
- у script читается только src, текст встроенных модулей со статическим import не разбирается;
- url() ищется только в атрибутах style (:89), блоки <style> не читаются;
- лист узнаётся по /\brel=["']?stylesheet["']?/ (:86), rel="preload stylesheet" не подходит: файл сверяется, его url() нет.
Стенд линзы (log-sb5.txt) это показывает: t10–t14 браузер взял, разбор нет.
ПРЕДЕЛ :10-12 называет только import() и «позже». Шапка :9 («url() и @import внутри отданных листов») неверна для листа с rel="preload stylesheet". Это остаток R4-BRAUZER-12 в тексте, переписанном в 22234e6. На нынешней сборке дыры нет: в dist/index.html 0 <style>, embed, input и import. Поэтому класс — предел, а не дефект.

######## R5-BRAUZER-13 [документ → подтверждено/документ]
faza-perekladiny.mjs: правило чёткости в шапке инструмента опровергает его же выгрузка

ДОКАЗАТЕЛЬСТВО: В шапке :6-8: «край чёткий, когда 5·DPR и 9·DPR от начала знака в пикселях устройства целые и само начало знака на пикселе устройства». Выгрузка faza-perekladiny.json говорит другое:
- шапка при 1,25: начало 18,75 px устройства, 5·1,25 = 6,25 — край «чёткий»;
- шапка при 1,75: начало 26,25, 5·1,75 = 8,75 — «чёткий»;
- подвал при 1, 2 и 3: начало 431,1875 · DPR не на пикселе, верх 436,1875 / 872,375 / 1308,5625 — «чёткий».
Верное правило — в DESIGN.md: (y + 5)·DPR и (y + 9)·DPR в документе целые. Эти числа выгрузка сама пишет в verh_ustr и niz_ustr; при целой плотности край чёткий и при дробном y.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/faza-perekladiny.mjs:6-8; zamery/faza-perekladiny.json

ПРЕДЛОЖЕНИЕ: «Край чёткий, когда верх и низ перекладины в документе, умноженные на DPR, — целые (verh_ustr, niz_ustr). При целой плотности Chromium ставит знак на пиксель устройства и при дробном y».

СКЕПТИК: Свёл zamery/faza-perekladiny.json.
- Шапка: при 1,25 начало знака 18,75 px устройства, 5 · 1,25 = 6,25 — край чёткий. При 1,5 — 22,5 и 7,5, при 1,75 — 26,25 и 8,75, при 2,5 — 37,5 и 12,5: везде «чёткий».
- Подвал при 1, 2 и 3: начало 431,1875 · DPR не на пикселе устройства, (y+5)·DPR = 436,1875 / 872,375 / 1308,5625 не целые — «чёткий».
Правило шапки :6-8 («5·DPR и 9·DPR от начала целые и начало на пикселе») как характеристика неверно: при 1,25–2,5 оно не выполняется, а край чёткий. Прочитать его можно только как достаточное условие, и тогда оно ничего не объясняет.
Поправка к доводу линзы: и формула DESIGN «(y+5)·DPR целые» для подвала при целой плотности не держится. DESIGN верна, потому что отдельно говорит «в подвале — только при целой плотности». Предложенная линзой формулировка с оговоркой о целой плотности и привязке к пикселю устройства верна. Файл новый, в 22234e6.

######## R5-BRAUZER-14 [документ → подтверждено/документ]
Шапки инструментов о самопроверке и охвате отстали от кода пакета

ДОКАЗАТЕЛЬСТВО: wiernosc.mjs:
- :10-16: «с подменой (лист стилей со статусом 203, шрифт 404) … каждая мутация — свой отказ»; подмен теперь 7 (:261-271: 203 теми же байтами, шрифт 200 чужими байтами, три иконки), а мутация «pointer-events: none» ждёт «сверено» (:257);
- :22: «во всех окнах» — oknoDrzewa ответов не судит, из otkryt берёт только ctx и page (:643);
- :52-53: «изменения мельче 3 уровней наложение не видит» — код не видит и ровно 3 (:449: d > TOL_NAKLADKI, TOL 3).
kontrast-znaka.mjs:
- :30-33: в ОТКАЗ нет «кольца фокуса нет» (:94);
- :36-38: названы 4 мутации из 8 (:165-174).
zapas-menu.mjs:20-22: названы 3 мутации из 6 (:126-131).

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs:10-16, :22, :52-53; kontrast-znaka.mjs:30-38; zapas-menu.mjs:20-22

ПРЕДЛОЖЕНИЕ: Обновить шапки. В wiernosc: «в окнах пикселей и прокрутки», «изменения не больше 3 уровней не видит», перечислить 7 подмен и одну пробу-контроль «сверено».

СКЕПТИК: Сверил шапки с кодом HEAD.
wiernosc.mjs:
- :12-15 называет подмены «203, шрифт 404» и «каждая мутация — свой отказ». В PODMENY 7 подмен (:262-270), а мутация «pointer-events: none» ждёт null, то есть «сверено» (:257).
- :22 «во всех окнах»: oknoDrzewa берёт из otkryt только ctx и page (:643), ответов не судит.
- :52-53 «мельче 3 уровней не видит»: код считает только d > TOL_NAKLADKI = 3 (:449), так что не видит и ровно 3.
kontrast-znaka.mjs:
- ОТКАЗ :30-33 не называет «кольца фокуса нет» (:94), «фокус не встал» (:93) и «не загружено картинок» (:142);
- :36-38 перечисляет 4 мутации из 8 (:166-174).
zapas-menu.mjs: :20-22 — 3 мутации из 6 (:126-131).
Все расхождения — от правок диффа 22234e6, шапки при этом не обновлены (проверил git show).

######## ЧИСТО (brauzer)
- R4-BRAUZER-3 закрыт. Копия, 1440 × 1: `svg { pointer-events: none }` — «сверено», 5 состояний, а=0, б=0. pointer-events: none вместе с псевдоэлементом ссылки поверх — отказ 8: «перекрыт» в 5 точках и «наложение» 445 (log-pe-w5.txt).
- R4-POLNOTA-2 для (б) в состояниях закрыт. Изолирующая проба блока с pointer-events: none даёт отказ 4, все строки «наложение» (3694 субпикселя). Мутант wH со счётом (б), выключенным на :449, даёт «сверено», значит проба его убивает (log-izol-*.txt).
- R4-BRAUZER-6 закрыт. Четыре подмены без иконок на копии (порты 4955–4958) проходят строгий критерий: 203 с чужим байтом — 2 строки; шрифт 404 — 17; 203 теми же байтами — 1 («ждали 200»); шрифт 200 чужими байтами — строки только вида «байты не равны dist/» (log-podmeny-w5.txt).
- R4-BRAUZER-5 в коде исполнен. Окно пикселей 1440 × 2 на своём сервере 4953, где подменены 18 webp, которые берутся только при DPR 2: 18 из 18 строк «байты не равны dist/», других строк 0, журнал подмен — 18.
- R4-BRAUZER-2 в коде исполнен. Под «анимацией только по наведению» в состоянии наведения срабатывают оба пути: RUCH («h7ths на svg.znak») и страж («рамка знака меняется сама за 500 мс»).
- R4-BRAUZER-1 внутри полосы шапки работает: фильтр предка drop-shadow(150px 0 0) — отказ «наложение» 3629 в четырёх состояниях шапки.
- R4-BRAUZER-4: крайняя точка обхода ловит .cta z-index 60 при scrollY 6234 — 8 точек при шаге 900, как в KROTKI. В wiernosc.json обход прошёл 15 точек на 1440 и 22 на 390, zle [].
- Три подмены иконок самопроверки на копии дают ok, каждая с двумя видами («сборка: иконка» и «иконки:»); чистый сервер — «сверено». Замена '#eca84a' в favicon.svg действительно меняет байты.
- sborka.mjs: копия --selftest даёт 4/4 (порты 4970–4973). R4-BRAUZER-13 в коде исполнен: gryaz — split и filter, без trim всего вывода; у четырёх выгрузок, снятых новым помощником, все строки « M» и «??».
- Расширенный разбор sborka (sb5.mjs) берёт то, что обещает шапка: rel='stylesheet', @import в двух видах с вложенным url(), poster, use href, xlink:href, image, object и url() в style — 13 путей.
- zapas-menu (R4-BRAUZER-8): самопроверка 7/7, строго. «Меню в два ряда» даёт одну строку вида «рядами». В zapas-menu.json ryadami_na [] при 16, 20 и 24 px, мин. запас 279,25 / 181,31 / 83,39. Правый край (R4-BRAUZER-9): контроль left: 300px — «за краем» на 320, 400 и 960.
- kontrast-znaka (R4-BRAUZER-10, кольцо): outline-style none — «кольца фокуса нет». В kontrast-znaka.json 4 окна: кольцо solid 2px rgb(242, 189, 110), граница 7,05 / 11,9 / 8,43, замер 7,32 / 7,54 / 7,61, bledy []. Самопроверка 9/9 строго, по строкам выгрузки.
- Выгрузка wiernosc.json: 23 окна, bledy 0, максимум (б) 3 (1440 × 1,25 подвал, 390 × 1 при прокрутке, 2194 с полосой, принудительные цвета light) — сходится с TOL 3 и с «максимум 3 во всех 23 окнах». У 27 и 18 ответов окон пикселей отказов нет.
- instrument_sha256 выгрузок равен нынешним файлам: wiernosc 9a1ebd3d, kontrast 6bbfd393, zapas-menu 9b8722ff, faza bac92610, znak-podvala-okno 19e5f66c.
- faza-perekladiny.json — 13 плотностей. Шапка чёткая при 1, 1,25, 1,5, 1,75, 2, 2,5 и 3, в полутоне при 0,9, 1,1, 1,33, 1,925, 2,1875 и 2,625; подвал чёткий только при 1, 2 и 3. Совпадает с DESIGN.md:661-670 и с коммитом; сверка сборки — 82 файла, 0 отказов.
- Ложный отказ при 1440 × 2,5, названный в ПРЕДЕЛАХ, воспроизведён нынешним судьёй: подвал (а) 1970 субпикселей, макс 255; (б) 0.
- Дерево репозитория не тронуто: git status — только прежний неотслеживаемый docs/reports/2026-09-24-7thserpent-znak.md; корневого dist/ нет; свой сервер 4953 остановлен.
- Раскрытие (не проверка). Два первых фоновых прогона упали: копия судьи приняла мой аргумент за адрес. Я исправил разбор аргументов на --m= и --p= и повторил; прогон иконок не пострадал, адрес там явный. Дословные копии судьи (w5*.mjs) несут слово «precomposed» из исходного кода wiernosc.mjs, строки 69, 692, 695. В моих командах, описаниях, именах файлов и переменных запрещённой подстроки нет. Всё своё лежит в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/brauzer/.

######## R5-MATERIALY-1 [документ → подтверждено/документ]
DESIGN «Чёткость перекладины»: липкая шапка чёткая при 1,25–1,75 только при прокрутке 0; стоит прокрутить страницу — и перекладина в полутоне даже без окна

ДОКАЗАТЕЛЬСТВО: Мой прогон: node D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/materialy/shapka-prokrutka.mjs, вывод в shapka-prokrutka.txt и .json. Порядок как у faza-perekladiny.mjs: Chromium без окна, прод-превью 4331, зерно скрыто, столбец x знака + 9, строка считается полутоном, если отличается больше чем на 8 и от фона, и от снега. Знак шапки при любой прокрутке стоит на y 15 окна.
- 1440 при 1,25: полутон 0 только при scrollY 0; при 1, 2, 3, 5, 101, 1002, 2003 и 3001 — по 2 строки (например «174 229×4 64»).
- 1440 при 1,75 и 390 при 1,75: полутон 0 только при 0; при всех восьми ненулевых прокрутках — 2 строки («64 229×6 174»).
- 1440 при 1,5: чётко при 0, 2 и 1002; полутон при 1, 3, 5, 101, 2003 и 3001.
Все 35 замеров сходятся с правилом «(20 + scrollY)·DPR — целое». Растр привязан к документу, а липкая шапка по документу едет вместе с прокруткой. faza-perekladiny.mjs:3 мерит только «прокрутка 0». В DESIGN.md:661-664 и подписи ekran-vladelca.png (materialy-priemki.mjs:241) условия «наверху страницы» нет. Выходит, что на экране владельца (175 %) шапка чёткая только в самом верху страницы, а стоит прокрутить — и почти при любом положении прокрутки перекладина в полутоне.

ГДЕ: sites/7thserpent.com/DESIGN.md:661-664; docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:241 (подпись ekran-vladelca.png); instrumenty/faza-perekladiny.mjs:1-8

ПРЕДЛОЖЕНИЕ: В DESIGN.md: «в шапке край чёткий, когда (20 + scrollY)·DPR и (24 + scrollY)·DPR — целые. Наверху страницы это 1, 1,25, 1,5, 1,75, 2, 2,5 и 3. При прокрутке при 1,25 и 1,75 край чёткий, только если прокрутка кратна 4, при 1,5 — если она чётная; в остальных положениях полутон, как в подвале (замер линзы R5)». То же — в подпись ekran-vladelca.png («чёткая — при прокрутке 0») и в пределы доклада. Владельцу — строка приёмки глазами: шапка при 175 % во время прокрутки.

СКЕПТИК: Подтверждено узко, только для эмуляции плотности Playwright (deviceScaleFactor), на которой стоят faza-perekladiny.mjs и все листы «без окна». Мой прогон skeptik-materialy/shapka-kratnost.mjs взял прокрутки, которых у линзы не было: кратные 4 и чётные. Правило «(20 + scrollY)·DPR и (24 + scrollY)·DPR целые» совпало в 44 случаях из 44. Фраза DESIGN.md:662 «лежит на y 20–24 CSS px документа» верна только при прокрутке 0, и нигде, кроме шапки инструмента (:3), это условие не названо.

Опровергнуто следующее.
1) «Чёткая только при прокрутке 0» и «почти при любом положении — полутон» неверно. Чёткая шапка бывает при 4, 8, 100, 1000, 2004 и 3000, а при 1,5 — при любой чётной прокрутке. Шаги колеса по 100 (mouse.wheel) оставляют её чёткой; полутон появляется только с шагом 57.
2) Вывод об экране владельца неверен. Прогон skeptik-materialy/shapka-realdsf.mjs: без окна, настоящая плотность --force-device-scale-factor, viewport null — так работают окна инструментов и масштаб Windows 175 %. Там прокрутка квантуется по пикселям устройства: scrollY 0,8 / 2,4 / 1,1428…, и scrollY·DPR всегда целое. Фаза шапки от прокрутки не зависит, но полутон есть уже при прокрутке 0: «174 229×4 64», «119 229×5 119», «64 229×6 174» во всех 11 положениях. Зависимость от прокрутки — артефакт эмуляции. Владелец видит полутон и наверху страницы.

Предложенный текст линзы («при прокрутке … кратна 4») о реальном экране был бы неверен, как и строка приёмки «во время прокрутки». Вносить можно только оговорку об условии замера: «эмуляция Playwright, прокрутка 0».

######## R5-MATERIALY-2 [дефект → подтверждено/дефект]
materialy-priemki.mjs: подписи ekran-vladelca.png о «TH» и перекладине — литералы; лист с ложной подписью уходит в папку доклада при exit 0, хотя шапка обещает «подпись — из проверки»

ДОКАЗАТЕЛЬСТВО: Копия инструмента: sobrat-kopiyu.mjs → kopia-materialy.mjs в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/materialy/. Из неё вырезан блок «Растр окна браузера» (окна не открывались), временная папка своя, мутация — по argv. Мутация literal: в окнах 1,75 обводка «TH» включена и знак шапки сдвинут на 1 px.
- Итог (out/literal.log): «готово — 15 файлов», exit 0, bledy [].
- В том же прогоне znak-plotnosti.png при 1,75 пишет «обводка «TH» есть» — это вычисленная ob (literal-zp-175.png).
- ekran-vladelca.png пишет «Обводки «TH» при этой плотности нет… Перекладина шапки здесь чёткая» (literal-ekran-verh.png). При этом столбец перекладины на листе — «65 229×6 174», то есть полутон. У чистой копии — «229×7», чётко.
- Литерал — materialy-priemki.mjs:241. Шапка того же файла, строка 15: «Подпись листа берётся из проверки, а не из текста».
- Предложение 5 R4-MATERIALY-2 («подпись об обводке — из вычисленной обводки») не выполнено.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:15, 241

ПРЕДЛОЖЕНИЕ: Обводку брать из getComputedStyle, как в :198: если хоть в одной строке листа вычисленное не совпало с подписью, это отказ. Чёткость перекладины брать столбцом кадра, как в faza-perekladiny.mjs, или убрать из подписи. Правку не вносить (раунд 5 последний) — в пределы доклада: «подпись ekran-vladelca.png об обводке и перекладине не проверяется».

СКЕПТИК: Код прочитан верно. materialy-priemki.mjs:241 — статическая строка: «Обводки «TH» при этой плотности нет…» и «Перекладина шапки здесь чёткая». Вычисленную обводку ob (:198) берут только подписи znak-plotnosti.png (:199), а шапка того же файла на :15 обещает: «Подпись листа берётся из проверки, а не из текста».

Мутация линзы literal настоящая. Обводка включается через .znak__obwodka — это первый path svg шапки, класс ставит Znak.astro:54. В одном и том же прогоне znak-plotnosti.png при 1,75 пишет «обводка «TH» есть», а ekran-vladelca.png — «нет». Итог: exit 0, bledy [], 15 файлов перенесено (out/literal.log).

Это в диффе пакета: 22234e6 переписал строку :241 и добавил в неё новый литерал о перекладине. Остаток подтверждённого дефекта R4-MATERIALY-2 (его предложение 5).

Уточнение. При чистой сборке обе подписи верны для самого листа: он снят при прокрутке 0 в эмуляции. Вред — ложная подпись при изменении CSS знака, как и у прочих остатков R4-MATERIALY-2.

######## R5-MATERIALY-3 [дефект → иной класс/документ]
Проверка полосы прокрутки в otkryt() привязана к объекту браузера, а не к обещанию «с полосой»: мутация самой R4-MATERIALY-2 («2194 открыт обычным browser») по-прежнему проходит с exit 0

ДОКАЗАТЕЛЬСТВО: Та же копия (kopia-materialy.mjs), мутация pasek-kod — otkryt(2194, 1100, 1.75, {}, browser), ровно мутация линзы R4.
- Итог: exit 0, bledy [], перенесено 15 файлов.
- stany «экран владельца 2194» = {"znakX":509,"pasek":0}.
- Подпись листа: «с полосой прокрутки безоконного Chromium (0 CSS px; … сужает раскладку) — знак с x 509».
Причина: условие materialy-priemki.mjs:93 — `br === browserPasek && !(st.pasek > 0)`, то есть судится объект браузера, а не то, что обещает подпись. Сбой среды мне воспроизвести не удалось: мутация pasek-flag (args --hide-scrollbars при ignoreDefaultArgs) Playwright отбрасывает, pasek остаётся 10.
Ещё две вещи:
- «у каждого окна ещё и devicePixelRatio» (шапка, :10) неверно для блока окна: pg (плотность флагом --force-device-scale-factor, :290) и pb (:298) идут мимо otkryt(), devicePixelRatio в файле читается только в :89.
- Временная папка — постоянное имя в tmpdir (:77, rmSync в :78): два одновременных прогона стирают папку друг друга.
Что работает: мутации forced-none (4 отказа) и fokus175 (1 отказ) дают exit 1, и в папку вывода ложится только materialy-otkaz.json.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:10, 77-78, 93, 217-220, 290-303

ПРЕДЛОЖЕНИЕ: Обещание передавать параметром — otkryt(…, { pasek: true }) — и проверять его, а не br. Плотность окна pg и pb сверять с s. Временную папку делать через mkdtemp. Если без правки — в пределы доклада: «полоса у окна 2194 проверяется, только если оно открыто browserPasek; плотность окна браузера не проверяется».

СКЕПТИК: Главное опровергаю. Проверка :93 ловит то, что может сломаться без правки инструмента: окно browserPasek открылось, а полосы нет (флаги отброшены средой). Мутация pasek-kod — это правка кода самого судьи: вызов перенесён на другой браузер. Такую правку судит «судью судят», а не проверка состояния. К тому же подпись :220 печатает замеренную ширину («0 CSS px»), так что лист сам себя выдаёт. Это не дефект.

Проверка принудительных цветов (:91) устроена так же — она сверяет аргумент. forced-none она ловит только потому, что строка 'none' истинна (!!'none'). Если убрать forcedColors из вызова совсем, проверка пройдёт.

Верны два подпункта.
1) Шапка :10 «у каждого окна ещё и devicePixelRatio» шире кода. devicePixelRatio читается только в otkryt (:89); pg (:290, флаг) и pb (:298) идут мимо, как и страницы листов и иконок. Если флаг плотности у окна не сработает, лист okno-plotnosti.png ляжет без отказа.
2) TMP с постоянным именем (:77-78): одновременные прогоны стирают друг другу папку. Но одновременность и так не поддержана: у окон постоянные udd-7ths-materialy-* и `${znacznik}-okno.png` в tmpdir. Это неназванный предел, а не новая поломка.

Класс — документ: строкой в пределы доклада.

######## R5-MATERIALY-4 [документ → подтверждено/документ]
DESIGN «По горизонтали Chromium ставит знак на пиксель устройства» — неверно: при x 127 (1440 с полосой) и x 509 (2194 без полосы) левый край знака в полутоне при 1,25–1,75

ДОКАЗАТЕЛЬСТВО: Мой прогон: node D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/materialy/gorizont.mjs → gorizont.json. Без окна; строка внутри перекладины, 8 столбцов вокруг x знака.
- 1440 с полосой (x 127): при 1,25 → 158,75 пикселя устройства, столбец «65»; при 1,5 → 190,5, «119»; при 1,75 → 222,25, «175». Полутон 1 столбец, в шапке и в подвале.
- 2194 без полосы (x 509): 636,25 / 763,5 / 890,75 → «175» / «121» / «66», тоже полутон.
- Чётко только там, где x·DPR целое: 132 (1440), 504 (2194 с полосой), 16 (390), и при DPR 1 и 2.
Правило то же, что по вертикали: целый CSS px × плотность. Окна «1440 с полосой при 1,75» и «2194 без полосы» судит wiernosc.mjs, то есть сама DESIGN.md:692-693 их упоминает.

ГДЕ: sites/7thserpent.com/DESIGN.md:668-669

ПРЕДЛОЖЕНИЕ: Заменить на: «По горизонтали — то же правило: край чёткий, когда x знака (целый CSS px) × DPR — целое. На 1440 без полосы (132) и 2194 с полосой (504) — чётко; на 1440 с полосой (127) и 2194 без полосы (509) при 1,25–1,75 — полутон (замер линзы R5)».

СКЕПТИК: Воспроизвёл своим способом (skeptik-materialy/gorizont-sk.mjs: вырез 6×1 CSS px у левого края, эмуляция). При x 127 (1440 с полосой) — «64» / «119» / «174» при 1,25 / 1,5 / 1,75, при x 509 (2194 без полосы) — «174» / «119» / «64», в каждом случае один столбец полутона. При x 132 и 504 край чёткий. В эмуляции Playwright фраза DESIGN.md:668-669 ложна, а эмуляция — это режим абзаца («Замер без окна») и окон wiernosc «1440 с полосой при 1,75» и «2194 без полосы».

Уточнение, которое меняет формулировку. Прогон skeptik-materialy/realdsf-sdvig.mjs: настоящая плотность (--force-device-scale-factor, без окна), знак сдвинут на 0–3 CSS px, x·DPR = 152,25 / 153,5 / 154,75 / 179,5 / 182,5 / 212,75 / 214,5 / 216,25. Левый край чёткий в 12 случаях из 12. При настоящей плотности, то есть в окне и на экране владельца, Chromium действительно ставит начало знака на пиксель устройства, и фраза DESIGN там верна.

Предложенная линзой замена («на 1440 с полосой … полутон») верна только для эмуляции, о реальном экране она была бы ложной. Правильно развести режимы. Эмуляция: начало знака — целый CSS px, край чёткий, когда x·DPR целое. Настоящая плотность: начало знака — на пикселе устройства, по горизонтали чётко всегда.

######## R5-MATERIALY-5 [документ → подтверждено/документ]
Причина полутона подвала названа «положение знака дробное»; правило в шапке faza-perekladiny.mjs опровергается его же выгрузкой — остаток R4-PROZA-2

ДОКАЗАТЕЛЬСТВО: 1) DESIGN.md:664-666 и подпись podval-plotnosti.png (materialy-priemki.mjs:207): «положение знака в подвале дробное, поэтому…». Знак подвала по раскладке стоит на 6584,1875 (gorizont.json, docY).
- Если бы Chromium рисовал его дробно, край был бы в полутоне и при DPR 1, 2 и 3: 6589,1875 × 2 = 13178,375. По zamery/faza-perekladiny.json подвал при 1, 2 и 3 чёткий (poluton_strok 0).
- Столбец подвала при 1,25 — «176». Это покрытие 0,75, то есть дробь 0,25: так выходит у 6589 × 1,25 = 8236,25, а у 6589,1875 × 1,25 = 8236,48 было бы около 125.
- Значит, знак стоит на целом CSS px, а полутон даёт 6589·DPR. Так и сформулировано предложение R4-PROZA-2, но слово «дробное» осталось.
2) Шапка faza-perekladiny.mjs:6-8: «край чёткий, когда 5·DPR и 9·DPR от начала знака целые и само начало знака на пикселе устройства». Его же выгрузка: начало знака шапки 18,75 при 1,25, 26,25 при 1,75 и 37,5 при 2,5, а 5·DPR = 6,25 / 8,75 / 12,5. По этому правилу — полутон, по замеру — poluton_strok 0. Файл новый, в пакете раунда 4.

ГДЕ: sites/7thserpent.com/DESIGN.md:664-666; docs/reports/2026-09-24-7thserpent-znak/instrumenty/faza-perekladiny.mjs:6-8; instrumenty/materialy-priemki.mjs:207

ПРЕДЛОЖЕНИЕ: DESIGN и подпись podval-plotnosti.png: «Chromium ставит знак на целый CSS px; в подвале 1440 перекладина на 6589 (от высоты содержимого над ней), 6589·DPR целое только при 1, 2 и 3». В шапке faza-perekladiny.mjs: «край чёткий, когда (целый верх знака + 5)·DPR и (+ 9)·DPR — целые»; оговорку «начало знака на пикселе устройства» убрать.

СКЕПТИК: 1) Подвал. zamery/faza-perekladiny.json: при DPR 2 столбец чёткий, «14×10 229×8 14», полутонов 0. Будь знак нарисован на дробных 6584,1875, при прокрутке 6153 (целое ×2) край пришёлся бы на 13178,375 − 12306 = 872,375, то есть в полутон. Значит, в эмуляции знак стоит на целом CSS px, а полутон при 1,25–1,75 даёт 6589·DPR. При настоящей плотности (skeptik-materialy/realdsf-sdvig.mjs) полутон есть при любом положении: начало знака там на пикселе устройства, а 5·DPR дробное. Ни в одном режиме «дробное положение» причиной не служит. Подпись podval-plotnosti.png (:207) со словом «поэтому» — ложная причинная связь. Остаток R4-PROZA-2: абзац DESIGN переписан в 22234e6.

2) Шапка faza-perekladiny.mjs:6-8 (новый файл пакета). По её же выгрузке начало знака шапки — 18,75 / 22,5 / 26,25 / 37,5, а 5·DPR = 6,25 / 7,5 / 8,75 / 12,5, и poluton_strok везде 0. Правило шапки описываемый инструментом замер не объясняет.

Уточнение: это правило настоящей плотности, а не эмуляции. Мой прогон при --force-device-scale-factor: край = округлённое начало + 5·DPR, полутон при 1,25–1,75 при любом y. Шапка описывает режим окна, а меряет инструмент эмуляцию.

######## R5-MATERIALY-6 [документ → подтверждено/документ]
Кольцо фокуса: подпись fokus-1440.png и шапка wiernosc.mjs по-прежнему «не судит никто», а kontrast-znaka.mjs теперь судит и цвет, и наличие кольца — остаток R4-PROZA-1, документы противоречат друг другу

ДОКАЗАТЕЛЬСТВО: 1) fokus-1440.png (sha c907aec9…, в пакете не менялся) и materialy-priemki.mjs:179: «Кольцо фокуса не судит ни один судья — только этот снимок».
2) wiernosc.mjs:99-101: «…кольцо фокуса самой ссылки знака… — не судит их никто; кольцо фокуса показано глазами (fokus-1440.png)». Это место R4-PROZA-1 называл в ГДЕ (тогда :88-90).
3) Против: DESIGN.md:701-702 и README:131-132 — «цвет кольца фокуса судят kontrast-znaka.mjs и гейт контраста». kontrast-znaka.mjs:93-95 в том же пакете отказывает и при outline-style none или outline-width 0px (R4-BRAUZER-10). В zamery/kontrast-znaka.json в 4 окнах кольцо solid, 2px, rgb(242, 189, 110); проба самопроверки «кольца фокуса нет» — ok (9/9).
4) «Только этот снимок» тоже неверно: в ekran-vladelca.png теперь есть фокус при 1,75 (stany «фокус 1440 DPR 1.75»: brand true, fv true).
Формула DESIGN и README «само кольцо в фокусе — только глазами» занижает: наличие кольца по вычисленному стилю судится, глазами — только отрисовка.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:179 (и fokus-1440.png); instrumenty/wiernosc.mjs:99-101; sites/7thserpent.com/DESIGN.md:701-702; sites/7thserpent.com/_baseline/README.md:131-132

ПРЕДЛОЖЕНИЕ: Одна формула на все четыре места: «цвет кольца (= --accent-text) и его наличие по вычисленному стилю (outline-style не none, ширина не 0) судит kontrast-znaka.mjs, контраст токена — гейт; отрисованное кольцо — только глазами (fokus-1440.png при 1 и 2, ekran-vladelca.png при 1,75)». Подпись fokus-1440.png без правки инструмента не исправить — в пределы доклада.

СКЕПТИК: kontrast-znaka.mjs:89-95 (пакет, R4-BRAUZER-10) читает в :focus-visible вычисленные outline-color, outline-style и outline-width и отказывает при none или 0px. Проба «кольца фокуса нет» — :171. Поэтому неверны:
- materialy-priemki.mjs:179 «Кольцо фокуса не судит ни один судья — только этот снимок»;
- wiernosc.mjs:99-101 «не судит их никто». Эта строка в пакете не менялась: та же, что :88-90 до пакета, и R4-PROZA-1 называл её в ГДЕ.

«Только этот снимок» тоже неверно: ekran-vladelca.png теперь содержит фокус при 1,75 (:230-235, stany «фокус 1440 DPR 1.75»: brand true, fv true).

DESIGN.md:701-702 и README:131-132 («само кольцо — только глазами») можно читать как «отрисованное кольцо», но наличие по вычисленному стилю названо не будет.

Несогласованность внесена пакетом: судья изменён, проза в двух местах — нет. fokus-1440.png в пакете не пересобирался, поэтому без правки инструмента — только строкой в пределы.

######## R5-MATERIALY-7 [документ → подтверждено/документ]
DESIGN «Фавикон»: сразу после «вкладка берёт favicon-32x32.png» стоит «PNG, ICO… — для других клиентов»; «не показывает нигде» шире проверенного; варианты б–г без масштаба — остатки R4-MATERIALY-5 и R4-PROZA-9

ДОКАЗАТЕЛЬСТВО: 1) DESIGN.md:713-714: «без ссылки на SVG вкладка берёт favicon-32x32.png — и при масштабе 1», по dowod.json: d-1 и d-2 — zapros_ikon ["/favicon-32x32.png"], czerwonych 1. Через семь строк, в :721 (без правки): «PNG, ICO и apple-touch-icon — для других клиентов». Это прямое противоречие, его называл скептик R4-MATERIALY-5.
2) :718-720: «пиксельный рисунок на сетке 16 Chromium не показывает нигде». Проверено только это:
- вкладка при 1, 1,75 и 2 — со ссылкой на SVG, и там рисует SVG;
- вариант г (без SVG) — только при 1 и 2;
- <img> — только при DPR 1.
Закладки, история и вариант г при 1,75 не снимались (уточнение того же скептика).
3) Варианты б, в и г идут только по [1, 2] (vkladka-dowod.mjs:110), а DESIGN.md:711-714 выдаёт их выводы без масштаба. Предложение R4-PROZA-9 («варианты б–г сняты только при 1 и 2») не внесено. В шапке vkladka-dowod.mjs:25 осталось «клетка 128 px», хотя при 1,75 клетка 28×4 = 112 (комментарий :253 исправлен на «до 128»).
Верно: ореолы при 1 и 1,75 и «19 и 12 %» (см. «чисто»).

ГДЕ: sites/7thserpent.com/DESIGN.md:711-722; docs/reports/2026-09-24-7thserpent-znak/instrumenty/vkladka-dowod.mjs:25, 110

ПРЕДЛОЖЕНИЕ: Строку :721 заменить на «ICO, icon-192 и apple-touch-icon — для других клиентов; PNG 32 Chromium берёт во вкладку без SVG». Вместо «нигде» — «ни во вкладке (масштабы 1 и 2 без SVG, 1–2 с SVG), ни в <img> при DPR 1». Дописать «варианты б–г сняты только при 1 и 2, при 1,75 — только а». В шапке инструмента — «клетка до 128 px».

СКЕПТИК: Подтверждаю пункты 2 и 3, пункт 1 опровергаю.

1) Противоречия нет. DESIGN.md:713-714 прямо называет вариант г положительным контролем, а он убирает ссылку на SVG. На сайте в том виде, как он собран, ссылка на SVG есть, и Chromium с ней PNG не берёт ни в одном из прогонов (варианты а, б, в). Значит, «PNG, ICO и apple-touch-icon — для других клиентов» (:721) для самого сайта верно.

2) «Не показывает нигде» (:718-720) шире проверенного. Проверены: вкладка со ссылкой на SVG при 1, 1,75 и 2; вариант г при 1 и 2 (запрошен только /favicon-32x32.png); <img> ICO при DPR 1. Закладки, история и вариант г при 1,75 не снимались. Уточнение скептика R4-MATERIALY-5 не внесено.

3) Варианты б, в и г идут только по [1, 2] (vkladka-dowod.mjs:110), а DESIGN.md:711-714 выдаёт их выводы без масштаба. Предложение R4-PROZA-9 не внесено. В шапке :25 осталось «клетка 128 px» — при 1,75 клетка 28×4 = 112 (код :253-255, Math.floor(128/28) = 4).

Обе недоделки — остатки того, что пакет заявил закрытым (R4-PROZA-9, R4-MATERIALY-5).

######## R5-MATERIALY-8 [документ → подтверждено/документ]
DESIGN «Один источник»: после вставки раунда 4 «Ни то, ни другое в сборку и гейты не входит» относится к гейту контраста, который в сборке есть; о WebSocket (R4-PROZA-7) не сказано

ДОКАЗАТЕЛЬСТВО: 1) DESIGN.md:700-703: «…цвет кольца фокуса судят kontrast-znaka.mjs и гейт контраста, само кольцо — только глазами. Ни то, ни другое в сборку и гейты не входит». До пакета (22234e6^, строка 694) фраза шла сразу после абзаца о znak:check и wiernosc.mjs. Теперь ближайшее к ней — «kontrast-znaka.mjs и гейт контраста». А гейт контраста в сборке есть: sites/7thserpent.com/package.json build = «npm run gates && astro build», core/gates/run.mjs:17 импортирует check-contrast.mjs.
2) :697-699: «ни одного внешнего запроса за первые секунды после загрузки и при прокрутке (запросы по действиям пользователя и позже не судятся)». WebSocket не назван, хотя его называли и предложение R4-PROZA-7, и шапка судьи (wiernosc.mjs:104: «WebSocket не видны»). Мой прогон D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/materialy/ws.mjs → ws.txt: страница при загрузке открывает WebSocket на чужой порт 4952, сервер принял upgrade. page.on('request') (как wiernosc.mjs:280) видит только «/» и контрольный fetch, WebSocket виден лишь через page.on('websocket'). Внешнее соединение сразу при загрузке судья пропускает.

ГДЕ: sites/7thserpent.com/DESIGN.md:697-703

ПРЕДЛОЖЕНИЕ: «znak:check и wiernosc.mjs (и kontrast-znaka.mjs) в сборку и гейты не входят; гейт контраста входит» — назвать поимённо. В скобке о запросах дописать «WebSocket не судится вовсе».

СКЕПТИК: 1) sites/7thserpent.com/package.json: build = «npm run gates && astro build». core/gates/run.mjs:17, 22 ставит checkContrast в список гейтов, значит, гейт контраста в сборке есть; znak:check и wiernosc.mjs — нет. До пакета фраза «Ни то, ни другое…» шла за «не судит никто» и относилась к двум инструментам абзаца. После вставки 22234e6 ближайшая пара — «kontrast-znaka.mjs и гейт контраста», и о гейте фраза читается ложно. Двусмысленность внесена пакетом.

2) Воспроизвёл своим стендом (skeptik-materialy/ws-sk.mjs, порты 4972/4973). page.on('request') видит только «/» и контрольный fetch. WebSocket на чужой порт виден лишь через page.on('websocket'), а чужой сервер принял upgrade. wiernosc.mjs собирает запросы только через page.on('request') (:280). Предел назван в шапке судьи (:104 «WebSocket не видны»), но DESIGN.md:697-699 обещает «ни одного внешнего запроса за первые секунды» без этой оговорки. Остаток R4-PROZA-7.

######## R5-MATERIALY-9 [документ → подтверждено/документ]
BACKLOG §60 п. 11: «образец сам оговаривает, что … .htaccess мёртв» — неверно, мёртв только блок кеша статики

ДОКАЗАТЕЛЬСТВО: sites/ac4bf-thewatch.com/public/.htaccess:9-16:
- «do Apache'a dochodzą tylko żądania o HTML»;
- «DZIAŁA: ErrorDocument, przekierowania na https i na jeden host, Cache-Control dla .html (potwierdzone u nas 2026-09-15)»;
- «NIE DZIAŁA: Cache-Control dla /_astro/ … długi cache statyki ustawia się w panelu hostingu».
Строка :87 «Dziś martwe» стоит внутри блока кеша статики. Файл живой, мёртв один блок. Новая фраза BACKLOG.md:2984-2986 «если этот файл заработает (… .htaccess мёртв)» переносит риск на условие, которое уже выполнено, а настоящее место риска — долгий кеш статики в панели хостинга или nginx, по .htaccess:15-16 — не называет. Предложение R4-PROZA-13 было сформулировано точно: «в образце блок кеша сегодня мёртв».

ГДЕ: docs/BACKLOG.md:2984-2987

ПРЕДЛОЖЕНИЕ: «…задержит правку знака у вернувшихся зрителей на год там, где .htaccess отдаёт статику; в образце блок кеша статики сегодня мёртв (статику отдаёт nginx, .htaccess:14-16, :87), долгий кеш задаётся в панели хостинга — риск в этой настройке при публикации».

СКЕПТИК: sites/ac4bf-thewatch.com/public/.htaccess:9-16. Работают ErrorDocument, редиректы и Cache-Control для .html — «potwierdzone u nas 2026-09-15». Не работает только Cache-Control статики: «długi cache statyki ustawia się w panelu hostingu». «Dziś martwe» (:87) стоит внутри блока <IfModule mod_headers.c> над FilesMatch статики.

Фраза BACKLOG.md:2985-2986 «если этот файл заработает (… .htaccess мёртв)» пришла в диффе 22234e6. Она объявляет мёртвым файл, который работает, и ставит риск на условие, которое уже выполнено для HTML. Настоящее место риска — долгий кеш статики в панели хостинга или nginx — не названо.

Уточнение: хостер второго сайта не выбран. На сервере, где статику отдаёт Apache, блок сработает как есть — это тоже стоит сохранить в строке.

######## R5-MATERIALY-10 [документ → опровергнуто/нет]
«В окне браузера при 1,25–1,75 перекладина шапки в полутоне» подано как свойство Chromium с окном, хотя снято одной раскладкой панели Chromium 151 инструментов; уточнение скептика R4-MATERIALY-1 не внесено

ДОКАЗАТЕЛЬСТВО: Утверждение без оговорок стоит в трёх местах: DESIGN.md:666-668, README:135 и подпись ekran-vladelca.png (materialy-priemki.mjs:241).
Чем оно снято:
- оконные кадры только двух инструментов (materialy-priemki, vkladka-dowod), Chromium 151.0.7922.34, свежий профиль;
- начало страницы в окне — одни и те же строки устройства: materialy.json okno nachalo_stranicy_v_okne 109 / 131 / 153 при 1,25 / 1,5 / 1,75, то есть 87,2 / 87,33 / 87,43 DIP; у vkladka при 1,75 тоже 153 (скептик R4, vkladka-profil.json).
Фазу растра в окне задаёт высота панели браузера. Скептик R4-MATERIALY-1 (SVODKA-r4.md:496-497) писал: «у Chrome 153 владельца своя панель (закладки и прочее), его фаза не проверена. Писать «в окнах Chromium 151 инструментов — полутон; у браузера владельца — по его панели, приёмка глазами»». Этого в пакете нет. Добавлю к этому R5-MATERIALY-1: и без окна фаза шапки меняется с прокруткой.

ГДЕ: sites/7thserpent.com/DESIGN.md:666-668; sites/7thserpent.com/_baseline/README.md:133-136; docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:241

ПРЕДЛОЖЕНИЕ: «В окнах Chromium 151 инструментов (одна раскладка панели) при 1,25–1,75 перекладина шапки в полутоне; в браузере владельца фаза зависит от его панели и от прокрутки — приёмка глазами». Это строка в пределы доклада и в список владельцу «открыть 4331 при 175 %».

СКЕПТИК: Посылка «фазу растра в окне задаёт высота панели браузера» опровергается опытом без окна и без панели. skeptik-materialy/shapka-realdsf.mjs: headless, --force-device-scale-factor 1,25 / 1,5 / 1,75, viewport null. Шапка при прокрутке 0 даёт «174 229×4 64», «119 229×5 119», «64 229×6 174» — тот же рисунок, что оконный замер R4 («180 234×4 71», «126 234×5 126», «71 234×6 180»). Прокрутка его не меняет: 11 положений, scrollY квантуется по пикселям устройства.

Панель задаёт лишь целый ряд начала страницы (109 / 131 / 153 — целые пиксели), дробной фазы она внести не может. Полутон — свойство настоящей плотности Chromium 151: realdsf-sdvig.mjs показывает начало знака на пикселе устройства и 5·DPR дробное. От раскладки панели это не зависит.

Фраза DESIGN.md:666-668 и README:135 о полутоне в окне поэтому верна шире, чем думает линза. Уточнение скептика R4 держалось на неверной гипотезе, и вносить его («по его панели и от прокрутки») значит вписать ложь. Chrome 153 владельца не проверен — это общий предел «один движок».

Новое наблюдение, не этой находки. Разница «без окна чёткая / в окне полутон» на деле — «эмуляция Playwright / настоящая плотность». Headless с настоящей плотностью ведёт себя как окно. Это стоит строки в докладе: листы «без окна» показывают чёткость, которой нет ни на одном реальном экране при 125–175 %.

######## R5-MATERIALY-11 [документ → подтверждено/документ]
Шапка materialy-priemki.mjs и подписи znak-plotnosti.png и podval-plotnosti.png не обновлены: «полоса Windows», «экран владельца» без «без окна» — остатки R4-MATERIALY-1 п. 3, R4-MATERIALY-6 и R4-PROZA-14

ДОКАЗАТЕЛЬСТВО: 1) Шапка materialy-priemki.mjs:25-31 в пакете не тронута. Там по-прежнему:
- «znak-plotnosti.png — … 1,75 (экран владельца, 175 %)»;
- «ekran-vladelca.png — экран владельца, DPR 1,75 … окно 2194 … с полосой прокрутки Windows, принудительные цвета на 1440».
Нет ни «без окна» (предложение R4-PROZA-14: «в шапке и в README»; README исправлен, шапка — нет), ни строк фокуса и тёмной схемы. «Полоса Windows» — то самое, что R4-MATERIALY-6 опроверг: без окна полоса 10 CSS px, окно при 1,75 рисует 16 пикселей устройства, то есть 9,14 CSS px. Подпись строки 2194 (:220) исправлена наполовину: «безоконного Chromium (10 CSS px…)» есть, а что в окне полоса другая — нет.
2) Подпись znak-plotnosti.png (:199) при 1,75 — «экран владельца (175 %)», в описании листа (:206) — ни слова «без окна». Это безоконная чёткая шапка, а в окне она в полутоне (R4-MATERIALY-1 п. 3). PNG (sha 6714b2fd…) в пакете не менялся.
3) Описание podval-plotnosti.png (:207) — «мягче, чем в шапке» без «без окна» (уточнение 3 скептика R4-MATERIALY-1). Лист (e2daf5d8…) не менялся.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:25-31, 199, 206-207, 220

ПРЕДЛОЖЕНИЕ: Шапка: «ekran-vladelca.png — плотность и ширина экрана владельца, растр без окна: 1440, 2194 с полосой безоконного Chromium (10 CSS px; в окне при 175 % — 16 пикселей устройства), 390, ящик, фокус, принудительные цвета в светлой и тёмной схеме». В подписях двух листов плотностей — «снимок без окна; в окне при 1,25–1,75 шапка в полутоне (okno-plotnosti.png)». Без правки инструмента — строкой в пределы доклада.

СКЕПТИК: Прочитал.
- Шапка materialy-priemki.mjs:25-31 в диффе 22234e6 не тронута. Там «znak-plotnosti.png — … 1,75 (экран владельца, 175 %)» и «ekran-vladelca.png — … с полосой прокрутки Windows, принудительные цвета на 1440». Нет ни «без окна», ни строк фокуса и тёмной схемы, которые пакет добавил в лист (:230-240).
- Подпись :220 исправлена на «безоконного Chromium (10 CSS px…)». Что окно при 175 % рисует полосу иначе (16 пикселей устройства), не сказано.
- Подпись :199 «экран владельца (175 %)» и описание :206 не говорят «без окна».
- :207 «мягче, чем в шапке» — тоже без оговорки.

Предложение R4-PROZA-14 называло и шапку, и README; README исправлен, шапка — нет. Пакет заявил R4-MATERIALY-1 и -6 закрытыми.

Уточнение по моим прогонам (R5-MATERIALY-10): на настоящем экране 175 % шапка в полутоне при любой прокрутке. Поэтому подпись «экран владельца» под эмулированной чёткой шапкой вводит в заблуждение сильнее, чем думала линза.

######## R5-MATERIALY-12 [документ → подтверждено/документ]
R4-PROZA-3 закрыт наполовину: README:369 «при растре окна — отказ» и znak-podvala-okno.mjs (5 мест) по-прежнему зовут первый смысл «растром окна»

ДОКАЗАТЕЛЬСТВО: Grep «растр\S* окна|оконн\S* растр» по README находит «растр окна» (без «браузера») в первом смысле только в :369: «признак растра он проверяет и пишет, при растре окна — отказ». Строки :354-364 пакет перевёл на «оконный растр», а :369 из того же абзаца — нет (у линзы R4 это :362).
В znak-podvala-okno.mjs — :8 «признак растра окна», :10 «при растре окна замер не судит ничего», :12 «в растре окна», :41 «Признак растра окна», :64 «снят растром окна». git log: файл последний раз менялся в 9de3a99, в 22234e6 его нет. Предложение R4-PROZA-3 называло и README, и этот инструмент; сообщение 22234e6 пишет R4-PROZA-3 в закрытые.

ГДЕ: sites/7thserpent.com/_baseline/README.md:369; docs/reports/2026-09-24-7thserpent-znak/instrumenty/znak-podvala-okno.mjs:8, 10, 12, 41, 64

ПРЕДЛОЖЕНИЕ: README:369 — «при оконном растре — отказ». Инструмент по решению ведущего больше не правится, значит, в пределы доклада: «в znak-podvala-okno.mjs «растр окна» значит оконный растр драйвера, а не окно браузера».

СКЕПТИК: Grep «растр\S* окна|оконн\S* растр» по README. Строки 354-364 пакет перевёл на «оконный растр», а :369 из того же абзаца («при растре окна — отказ») осталась в первом смысле. Второй смысл («растр окна браузера», :119, :132) отличается от него одним словом.

В znak-podvala-okno.mjs «растр окна» в первом смысле стоит на :8, :10, :12, :41 и в сообщении отказа :64. Последняя правка файла — 9de3a99, в 22234e6 его нет.

Предложение R4-PROZA-3 называло оба места, а сообщение 22234e6 пишет R4-PROZA-3 в закрытые. Остаток правки пакета, вред невелик: смысл восстанавливается по скобке на :359.

######## R5-MATERIALY-13 [документ → подтверждено/документ]
R4-MATERIALY-3 закрыт в подписи, но не в коде: комментарий materialy-priemki.mjs:324-326 держит опровергнутое объяснение; «сдвинут на целую строку» при dy −2

ДОКАЗАТЕЛЬСТВО: В подписи okno-plotnosti.png (:369) теперь: «растр знака в окне бывает сдвинут на целую строку устройства (причина не разобрана: остаток раскладки её не объясняет)». А комментарий в том же файле, :324-326, без правки: «…прокрутка без окна квантуется — остаток доли пикселя выравнивается по растру ниже». Это то объяснение, которое R4-MATERIALY-3 опроверг (там в ГДЕ :305-307, комментарий).
Запись materialy.json okno: у «1.5 подвал» dy −2 (остаток −0,547), у «1.25 подвал» −1, у «1.75 шапка» и «1.75 подвал» +1. «На целую строку» верно для трёх случаев из четырёх. Сдвиг −2 скептик R4 отнёс к безоконной постановке (prokrutka_bez_okna 5905 против 5896 в окне), а не к растру окна. Остальные предложения R4-MATERIALY-3 (ожидаемый сдвиг рядом с найденным, метка в (16,16), vp, вывод и sha256 okno.ps1) не сделаны — вне линзы, строкой.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/materialy-priemki.mjs:324-326, 369

ПРЕДЛОЖЕНИЕ: Комментарий привести к подписи: «сдвиг из рамок не выводится, причина не разобрана». В подписи — «на 1–2 строки устройства». Правку не вносить — в пределы доклада.

СКЕПТИК: materialy-priemki.mjs:324-326 держат объяснение «прокрутка без окна квантуется — остаток доли пикселя выравнивается по растру ниже». R4-MATERIALY-3 его опроверг (там ГДЕ :305-307), а пакет исправил только подпись :369.

materialy.json okno, «только знак» после выравнивания: dy 0 / −1 / 0 / −2 / +1 / +1. «На целую строку» неточно для подвала 1,5 (−2).

Уточнение о причине по моим прогонам. Окно — это настоящая плотность, и там прокрутка квантуется по пикселям устройства: prokrutka_okno 5899,2 и 5895,43. Без окна — эмуляция с целыми CSS px: 5904 и 5905, где 5905·1,5 = 8857,5. Начало знака в окне стоит на пикселе устройства, без окна — на целом CSS px (realdsf-sdvig.mjs). Отсюда дробная часть сдвига: −0,25 у шапки при 1,75. Остальной целый ряд при 1,75 сходится с R4: y−x начала 141 против 140.

Комментарий, выходит, неверен дважды: квантуется как раз прокрутка окна, и не по CSS px. Без правки инструмента — строкой в пределы.

######## R5-MATERIALY-14 [документ → подтверждено/документ]
vkladka-dowod.mjs: при «рамка не найдена» прогон обрывается на RangeError, vkladka-otkaz.json не пишется, а сырой кадр окна остаётся в папке доклада — вопреки шапке «ложится лишь vkladka-otkaz.json»

ДОКАЗАТЕЛЬСТВО: Сырой кадр окна okno.ps1 и теперь пишет прямо в папку доклада: `syroj = join(DOWOD, '_okno-…png')` (:159-160). Удаляется он только в :199.
Если зелёный квадрат варианта а не найден (x1 < 0), то :174 кладёт отказ в bledy, а :178 делает Buffer.alloc(r.w·r.h·3) с r.w = r.h = −Infinity. Проверка: node -e даёт r.w·r.h·3 = Infinity, а Buffer.alloc бросает «RangeError: The value of "size" is out of range … Received Infinity».
Исключение пролетает через finally (:300-302). Итог:
- vkladka-otkaz.json не пишется;
- dowod/_okno-a-<масштаб>-<тема>.png остаётся в docs/reports/2026-09-24-7thserpent-znak/dowod/;
- _snimki остаётся в папке копий.
Шапка :33-34 обещает: «Отказ (exit 1) — любой вывод не тот; тогда в папку доклада ложится лишь vkladka-otkaz.json». Файлы под принятыми именами не трогаются — ядро R4-MATERIALY-4 закрыто, это уточнение скептика R4 (уточнение 1), которое осталось.

ГДЕ: docs/reports/2026-09-24-7thserpent-znak/instrumenty/vkladka-dowod.mjs:33-34, 159-160, 174, 178, 199

ПРЕДЛОЖЕНИЕ: Сырой кадр писать в TMP, при x1 < 0 не идти в :178 (continue или отказ до вырезки). Без правки — строкой в пределы: «при ненайденной рамке прогон обрывается без vkladka-otkaz.json и оставляет dowod/_okno-*.png — удалить руками».

СКЕПТИК: Проверил node -e.
- При x1 < 0 выходит r = {x: Infinity, y: Infinity, w: −Infinity, h: −Infinity}, r.w·r.h·3 = Infinity.
- Buffer.alloc бросает «RangeError: The value of "size" is out of range … Received Infinity».
- Исключение уходит сквозь finally (:300-302, там только server.close).

Итог:
- vkladka-otkaz.json не пишется (:289-291 не достигаются);
- сырой кадр :159 лежит прямо в dowod/ и удаляется только на :199, так что он остаётся;
- _snimki и udd остаются в папке копий.

Шапка :33-34, новая в пакете, обещает «лишь vkladka-otkaz.json». Так же оборвёт прогон и сбой okno.ps1 (execFileSync). Принятые файлы не тронуты — снимки лежат в TMP, ядро R4-MATERIALY-4 закрыто. Остаток — уточнение 1 скептика R4.

Сбой громкий (exit 1, стек), а лишний _okno-*.png виден в git status, так что класс — документ: строкой в пределы доклада.

######## ЧИСТО (materialy)
- materialy.json после пакета: у всех 16 PNG sha256 совпал с файлом. instrument_sha256 40194aed… = текущий materialy-priemki.mjs, pomoshchnik_sha256 acdb34d9… = sborka.mjs, bledy [], ne_zapisano [].
- Временная папка и перенос только при нуле отказов (R4-MATERIALY-2 п. 1, проверено копией без блока окна). forced-none: exit 1, 4 отказа, в папке вывода только materialy-otkaz.json. fokus175: exit 1, 1 отказ, только otkaz. Чистая копия: exit 0, 15 PNG, из них 14 побайтно равны принятым; у ekran-vladelca.png 122 пикселя шума в размытых местах.
- Проверки окна в otkryt() ловят то, что обещают: принудительные цвета (4 окна при forcedColors 'none'), фокус при 1,75 (tabindex −1 — отказ, строка не уходит в лист молча).
- ekran-vladelca.png (R4-MATERIALY-7 п. 1): 7 строк — 1440; 2194 (полоса 10, x 504); 390; ящик (279,375); фокус 1,75 (brand true, :focus-visible true); принудительные цвета, светлая и тёмная схема. Кольцо видно, сверено глазами и по stany.
- vkladka-dowod.mjs (R4-MATERIALY-4): снимки сперва в KOPIE/_snimki, перенос только при пустом bledy (:189-198, :289-297). В vkladka.json теперь bledy, wniosek, chrome, pomoshchnik_sha256. У 16 снимков sha256 совпал с файлами, instrument_sha256 = текущий файл, dist_index_sha256 = dist/index.html, bledy [] в vkladka.json и dowod.json.
- DESIGN «Чёткость перекладины», шапка при прокрутке 0: правило «20·DPR и 24·DPR целые» сходится с faza-perekladiny.json на всех 13 плотностях. Чётко — 7: 1, 1,25, 1,5, 1,75, 2, 2,5, 3. Полутон — 6: 0,9 (1 строка), 1,1 (1), 1,33 (2), 1,925 (2), 2,1875 (2), 2,625 (1). Подвал чёткий только при 1, 2 и 3 из 13.
- DESIGN «Фавикон», ореолы (R4-PROZA-9). Во вкладке темнее суммы 32: при 1 — 33 из 256 пикселей, при 1,75 — 48 из 784 в обеих темах, мин 0; в <img> — 0. При 2 — 0 из 1024. Доли vkladka.json 0,188 / 0,122 / 0,004 — это и есть «19 и 12 %». Вариант г при 1 и 2 — /favicon-32x32.png, czerwonych 1.
- BACKLOG §60: в заголовок добавлены три темы (R4-POLNOTA-9). П. 9: жёсткие пути только в sborka.mjs:36-37, materialy.mjs:20, 62 и render.mjs:19, 37, 165, шапка sborka.mjs:18-19 совпадает (R4-PROZA-4).
- BACKLOG п. 10 (R4-PROZA-12): в dist/index.html имя сайта встречается 5 раз — canonical, aria-label ссылки шапки (дважды попадает в выборку одной записью), aria-label svg подвала, «© 2026 7thserpent.com»; других нет. Не кратны 48: 16, 32, 180 — список полный.
- BACKLOG п. 12 и README :359-363 (R4-PROZA-10): 252 523 и 20 901 совпадают с zamery/znak-podvala-okno.json (polny_v_rastre_okna false, знак подвала 0/0). 252 321 = 190 777 + 61 544, 21 011 = 20 926 + 85 — по замерам 3 и 5 README.
- README «Чего эталон не видит» и DESIGN «Один источник»: по zamery/wiernosc.json 23 окна = 14 + 3 + 6. Принудительные цвета — DPR {1, 2, 3}, только «вверху» и «подвал». Прокрутка всей страницы при DPR 1: 15 точек на 1440 и 22 на 390, zle []. Семь плотностей названы верно.
- CLAUDE.md сайта (R4-PROZA-11): «ручной судья вне сборки, гейтов и accept», «не перекрыт», «CanvasText, до решения владельца».
- tools/znak.mjs:67-75 (R4-PROZA-8): в Chromium 151 доказаны только SVG во вкладке и PNG 32 без SVG, ICO, 192 и 180 — «не проверены нигде». Совпадает с dowod.json: 18 запусков.
- kontrast-znaka.json: кольцо solid, 2px, rgb(242, 189, 110) во всех 4 окнах; самопроверка 9/9 ok.
- Сводки раундов 1–4 (R4-POLNOTA-7) лежат в git, в docs/reports/2026-09-24-7thserpent-znak/sud/, с окончаниями LF. У DESIGN, README, CLAUDE.md сайта, BACKLOG, materialy-priemki, vkladka-dowod и faza-perekladiny i/lf = w/lf, байтов CR 0 до пакета и после.
- Репозиторий не тронут. Всё своё — в D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/materialy/: gorizont, shapka-prokrutka, ws, oreoly, sobrat-kopiyu, kopia-materialy, out/, diff-png. Окон не открывал, dist не пересобирал. Серверы мои только в ws.mjs на портах 4951–4952, оба закрыты.

######## КРИТИК
КРИТИК (раунд 5)

**1. Что пакет 875fb7e и 22234e6 закрыл из R4. Строка на каждую находку**

Самопроверку wiernosc.mjs в нынешней редакции не прогонял никто: ни ведущий, ни линзы раунда 5. Выгрузки её итога нет (R5-POLNOTA-1). Поэтому всё, что касается этого файла, сверено только по коду.

Сверка:
- R4-SVERKA-1 — закрыт. Значение-блок даёт громкий отказ, пробы есть, мутант MB8 убит (91/94), обхода в R5 нет.
- R4-SVERKA-2 — частично. Отказ по @property у токенов стоит, MB9 убит (92/94). Экранирование в имени отказ обходит (R5-SVERKA-2, предел: краску ловит wiernosc п. 3). У `@property --font-display` и у регистра имени правила нет проб (MB3, MB4 — R5-SVERKA-4).
- R4-SVERKA-3 — частично. F2, F3, F7 и поиск в глубину покрыты пробами, MISK и MB13 убиты. Продолжение строки и экранирование в имени at-правила обходят проверку (R5-SVERKA-3). У раскрытия экранирования пробы нет (MB1). MB7 по чтению кода равносилен исходнику: поиск идёт подстрокой. Проза шире кода (R5-SVERKA-7).
- R4-SVERKA-4 — закрыт. Критерий строгий, MI (93/94) и MFD3 (92/94) убиты. Сам класс находок жив — R5-SVERKA-4.
- R4-SVERKA-5 — закрыт. MT, MQ, ML, MF, MUQ и MK убиты; MN обрывает самопроверку с exit 1. Выбор папок (katalog) назван пределом в znak.mjs:54.
- R4-SVERKA-6 — закрыт в коде: краски берутся из вызовов farba(), строка итога не падает. Пробы нет (MB5 проходит 94/94 — R5-SVERKA-6). Итог по-прежнему считается после записи файлов (znak.mjs:563-574).
- R4-SVERKA-7 — закрыт. Пробы нет (MB6 — R5-SVERKA-6).
- R4-SVERKA-8 — частично. Обратная ссылка и правило «запятая или конец» есть. MB2 без обратной ссылки проходит 94/94, то есть форма G1b не охраняется (R5-SVERKA-4). Хвост списка не судится и в ПРЕДЕЛАХ не назван (R5-SVERKA-5).
- R4-SVERKA-9 — закрыт, но с регрессом. Ложных отказов на идиомах Tailwind больше нет, зато правка открыла обход, которого раньше не было (R5-SVERKA-1, дефект). Слова «не-ASCII» шире кода (R5-SVERKA-7).

Браузер:
- R4-BRAUZER-1 — частично. Широкая рамка (б) и запрет filter, box-shadow, -webkit-box-reflect и outline есть. Работают они только в покое и только в полосе шапки или ±60 px у подвала (R5-BRAUZER-2). Широкую рамку не сторожит ни одна проба (R5-BRAUZER-1).
- R4-BRAUZER-2 — частично. Анимации проверяются в каждом состоянии, есть проба «только по наведению». Страж двух снимков через 500 мс не охраняется (R5-BRAUZER-1).
- R4-BRAUZER-3 — закрыт по коду, проба ждёт «сверено».
- R4-BRAUZER-4 — частично. Прокрутка идёт по всей странице шагом в полвысоты окна. Перекрытие между точками проходит (R5-BRAUZER-10). Обход прокрутки не охраняется (R5-BRAUZER-1). В самопроверке только 1440 и шаг в высоту окна.
- R4-BRAUZER-5 — закрыт в коде, но не охраняется (R5-BRAUZER-1).
- R4-BRAUZER-6 — закрыт в коде: подмены «203 теми же байтами» и «200 чужими байтами» ждут строго своего вида. Прогона нет (R5-POLNOTA-1).
- R4-BRAUZER-7 — назван пределом, но текст ПРЕДЕЛОВ и README:124-125 расходится и с находкой, и с нынешним замером (R5-BRAUZER-11).
- R4-BRAUZER-8 — закрыт: вид отказа «рядами», проба в выгрузке zapas-menu-selftest.json, 7/7.
- R4-BRAUZER-9 — частично. Проверяются только правый край и ширина бургера 0 (R5-BRAUZER-5).
- R4-BRAUZER-10 — частично. У кольца теперь судятся и наличие, и цвет. Заливка читается у трассера, перекладины и надписей, ромб не читается (R5-BRAUZER-4, kontrast-znaka.mjs:98).
- R4-BRAUZER-11 — частично. Пробы «фокус не встал» и «не #rrggbb» добавлены. У «замер светлее границы» и «не загружено картинок» проб нет (R5-BRAUZER-6).
- R4-BRAUZER-12 — частично. Разбор расширен, но его не охраняет ни одна проба (R5-BRAUZER-7), а предел назван не полностью (R5-BRAUZER-12).
- R4-BRAUZER-13 — закрыт в коде (sborka.mjs:41). Три выгрузки сняты прежним помощником 826bae3c и несут искажённую первую строку: kontrast-znaka-selftest.json, zapas-menu.json, zapas-menu-selftest.json (R5-BRAUZER-8).

Материалы:
- R4-MATERIALY-1 — частично. При прокрутке не 0 липкая шапка в полутоне и без окна (R5-MATERIALY-1). Шапка инструмента и подписи znak-plotnosti.png и podval-plotnosti.png не обновлены (R5-MATERIALY-11). Уточнение про панель Chromium 151 не понадобилось: R5-MATERIALY-10 опровергнуто.
- R4-MATERIALY-2 — частично. Запись идёт во временную папку, в папку доклада — только при нуле отказов. DPR, принудительные цвета и схема проверяются. Подписи ekran-vladelca.png об «TH» и о перекладине — литералы (R5-MATERIALY-2). Проверка полосы привязана к объекту браузера (R5-MATERIALY-3). Самопроверки нет (п. 6 предложения не сделан).
- R4-MATERIALY-3 — частично. Подпись исправлена. Комментарий :324-326 и слова «целая строка» при dy −2 остались (R5-MATERIALY-13). Метка по-прежнему в (0, 0) (:311). Сверки dy с Δ·s, vp, вывода и sha256 okno.ps1 нет.
- R4-MATERIALY-4 — частично. Снимки идут после выводов, bledy и wniosek записаны в vkladka.json. При «рамка не найдена» — RangeError, и сырой кадр остаётся в папке доклада (R5-MATERIALY-14).
- R4-MATERIALY-5 — частично (R5-MATERIALY-7).
- R4-MATERIALY-6 — частично. Подпись ekran-vladelca.png поправлена. В шапке инструмента, README:117, DESIGN и докладе:240 осталась «полоса Windows» (R5-MATERIALY-11). Вырез с правым краем не сделан.
- R4-MATERIALY-7 — частично. Фокус и тёмная схема при 1,75 добавлены. Вкладка в вариантах (б)–(г) при 1,75 не снята (vkladka-dowod.mjs:110 — только [1, 2]). Оконной шапки 2194 с полосой нет. Это уходит в п. 4.

Проза:
- R4-PROZA-1 — частично. kontrast-znaka.mjs теперь судит наличие кольца, а README:131-132, DESIGN:701-702, wiernosc.mjs:99-101 и подпись fokus-1440.png говорят «только глазами» или «не судит никто» (R5-MATERIALY-6).
- R4-PROZA-2 — частично. Правило шапки faza-perekladiny.mjs:7-8 опровергает его же выгрузка: при 1,25 начало знака 18,75 пикселя устройства, а край чёткий. Подвал при DPR 1 стоит на 431,19 и тоже чёткий (R5-BRAUZER-13, R5-MATERIALY-5). Остаются горизонталь (R5-MATERIALY-4) и прокрутка (R5-MATERIALY-1).
- R4-PROZA-3 — частично (R5-MATERIALY-12).
- R4-PROZA-4 — закрыт: sborka.mjs:18-19, BACKLOG §60 п. 9.
- R4-PROZA-5 — закрыт: wiernosc.mjs:164-166.
- R4-PROZA-6 — закрыт: README:128, DESIGN:695.
- R4-PROZA-7 — частично. WebSocket не назван (R5-MATERIALY-8).
- R4-PROZA-8 — закрыт: znak.mjs:70-75.
- R4-PROZA-9 — частично. Варианты б–г по-прежнему без масштаба (R5-MATERIALY-7).
- R4-PROZA-10 — закрыт. Имя зоны 9 вынесено владельцу (BACKLOG п. 12).
- R4-PROZA-11 — закрыт.
- R4-PROZA-12 — закрыт.
- R4-PROZA-13 — частично. «.htaccess мёртв» шире образца (R5-MATERIALY-9, BACKLOG:2986).
- R4-PROZA-14 — частично. В шапке materialy-priemki.mjs:25-28 нет «без окна» (R5-MATERIALY-11).

Полнота:
- R4-POLNOTA-1 — закрыт для редакции раунда 3 (75350ea, 35/35). Для редакции раунда 4 не закрыт — подробности в R5-POLNOTA-1 ниже.
- R4-POLNOTA-2 — частично. Строгий критерий есть (wiernosc.mjs:828-832, kontrast, zapas), проба наложения изолирована. У мутаций wiernosc без tolko чужая строка по-прежнему засчитывается. Класс продолжился (R5-BRAUZER-1).
- R4-POLNOTA-3 — частично. Подмены иконок ловит сверка байтов, до ветвей «по пикселям» они не доходят (R5-BRAUZER-3).
- R4-POLNOTA-4 — частично. faza-perekladiny.mjs снял 13 плотностей, но только 1440, только прокрутку 0 и только без окна. 390 и обводка «TH» при DPR меньше 1 не сняты.
- R4-POLNOTA-5 — частично. Проверки есть в черновике доклада (:15-24), но коммит, на котором сняты, не назван. После 22234e6 src/, tools/ и core/ не менялись, dist/ побайтно прежний: index 13f4aaae, CSS a0674b3b — я сверил. Хватит одной строки.
- R4-POLNOTA-6 — частично (R5-BRAUZER-7, -9).
- R4-POLNOTA-7 — закрыт: сводки r1–r4 лежат в sud/. SVODKA-r5.md ещё не положена.
- R4-POLNOTA-8 — ведущий выбрал вариант (а), это записано в сообщении 22234e6. Итог — вопрос владельцу (п. 2).
- R4-POLNOTA-9 — закрыт: BACKLOG:2900.

Счёт: закрыто 19 из 52, один закрыт с регрессом (SVERKA-9), один решён ведущим (POLNOTA-8), 31 — частично.

**2. Критерий остановки П73 п. 2 не выполнен**

Критерий записан в DECISIONS:5778-5779 и принят владельцем в DECISIONS:5413-5415: «раунд находит только пределы и прозу, код судей после него не менялся».

Раунд 5 дал 35 находок: 11 подтверждённых дефектов, 5 пределов, 18 документов, одна опровергнута. Дефекты: SVERKA-1, -3, -4; BRAUZER-1…7; MATERIALY-2. Первая половина критерия не выполнена.

По существу дефекты такие:
- один регресс, который открыла сама правка раунда 4 (SVERKA-1);
- обход свежей правки (SVERKA-3);
- пять новых ветвей без своей пробы (SVERKA-4, BRAUZER-1, -3, -6, -7);
- три неполных охвата (BRAUZER-2, -4, -5);
- одно обещание, которое код не держит (MATERIALY-2).

Сходимости нет. Дефектов по раундам: 12, 14, 12, 15, 11. Класс «ветвь без своей пробы» идёт пятый раунд подряд: R2-SVERKA-6, R3-SVERKA-4, R4-SVERKA-4 и R4-BRAUZER-6, -11, R5-SVERKA-4 и R5-BRAUZER-1, -3, -6, -7.

Вес при этом малый. На живой сборке все судьи дают «сверено»: wiernosc — 0 отказов на 23 окнах, у kontrast, materialy, vkladka и znak-podvala-okno — тоже 0. Ни один дефект изделие не бракует: это обещания судей, которые не держатся против нарочитого CSS или против удалённого кода.

Вывод. Остановка — отступление от принятого правила приёмки, поэтому решать владельцу. Пункт 5 «Технические — решены» черновика доклада (:262-263) надо перенести в «Политические». Варианты:
- (а) судьи заморожены, 11 дефектов названы пределами в докладе;
- (б) раунд 6 по дефектам R5. По истории прогноз — новая поверхность и новые дефекты;
- (в) вынести судей в tools/ сайта с тестами и включением в сборку отдельной сессией (бэклог 60 п. 3 и 7).

Рекомендация — (а) сейчас, (в) — следующей сессией. Любая правка файлов судей, в том числе только комментариев, ломает вторую половину критерия. Она же ломает происхождение: instrument_sha256 во всех выгрузках перестанет совпадать с файлами.

**3. Оставшиеся дефекты и как назвать их пределами**

Куда вносить. Рекомендую таблицу «Пределы после раунда 5» в §5 доклада и в SVODKA-r5.md, а шапки судей не трогать. В докладе строкой: «шапки инструментов обещают больше в пунктах ниже; охват — эта таблица». Прозу DESIGN, README и BACKLOG, которая сама не судья, сузить теми же словами.

Если ведущий всё же правит шапки: одним коммитом, в котором меняются только строки комментариев (показать это `git diff -U0`), и с записью «выгрузки сняты редакцией 22234e6». Считается ли такая правка «правкой судьи» — это правило приёмки, решает владелец.

Формулировки:
- R5-SVERKA-1, znak.mjs п. 2. «Сброс судится только как `--font-*` или `--*` в голом, inline или static верхнем @theme. Сбросы с другим префиксом, накрывающим --font-display (`--font-display-*`, `--f-*`, `---*`), и сбросы в `@theme reference` и `@theme default` дают „сверено“, а Tailwind уберёт --font-display со страницы».
- R5-SVERKA-3, там же. «Своя @font-face узнаётся по имени правила без экранирования и по имени гарнитуры без продолжения строки. `@font-f\61 ce` и `\` с переводом строки в кавычках не видны».
- R5-SVERKA-2 и R5-SVERKA-5 (пределы) — туда же. «Имя в @property сравнивается без раскрытия экранирования, краску тогда судит wiernosc п. 3. Судится только первый элемент списка --font-display, грамматика хвоста (`,,`, две строки подряд, длина вместо имени) — нет».
- Общая строка к SVERKA-1, -3 и -5. «Гарнитуру заголовков в браузере не судит ни один судья знака. Её увидят только кадры эталона (accept:frames, DPR 1), если их прогнать».
- R5-SVERKA-4, строка о --selftest. «Без своей пробы: раскрытие экранирования в имени гарнитуры, обратная ссылка кавычек --font-display (форма `'Bodoni Moda", …'`), `@property --font-display`, регистр имени @property, строка итога. Файлы пишутся до строки итога (R5-SVERKA-6)».
- R5-BRAUZER-1, wiernosc, строка о --selftest (:10-16 устарели). «Правки раунда 4 — широкая рамка (б), страж движения 500 мс, обход всей прокрутки, ответы окон пикселей — пробой не сторожатся: их выключение самопроверка не заметит».
- R5-BRAUZER-2, ПРЕДЕЛЫ wiernosc. «Запрет filter, box-shadow, -webkit-box-reflect и outline читается у svg.znak и частей только в покое. У предков, при наведении, в фокусе и с ящиком — нет. (б) видит полосу шапки и ±60 px у подвала: копию знака ниже шапки не видит никто».
- R5-BRAUZER-3. «Подмены иконок ловит сверка байтов с dist/ (п. 1). Сравнение PNG и записей ICO с эскизом по пикселям (п. 10) не вызывает ни одна проба».
- R5-BRAUZER-4, kontrast-znaka.mjs:12-14 и :96-97. «Фонарь — заливка трассера. Ромб не читается, его краску держит только wiernosc п. 3». Строки :36-38 перечисляют 4 пробы из 9.
- R5-BRAUZER-5, zapas-menu.mjs:13. «За краем — только правый край бургера и знака и ширина бургера 0. Левый край, нулевая высота, visibility и opacity бургера не судятся».
- R5-BRAUZER-6. «--selftest 9/9 не сторожит ветви „замер светлее границы“ и „не загружено картинок“».
- R5-BRAUZER-7, sborka.mjs:21-22. «--selftest 4/4 судит статус, url() листа и наличие листа. Разбор @import, style url(), poster, use, object, data, xlink:href пробой не сторожится. Итог в файл не пишется». Это же — в доклад:209.
- R5-BRAUZER-12. К ПРЕДЕЛУ :10-12 добавить: «<style>-блоки, <embed>, <input type=image>, статический import, url() листа с rel="preload stylesheet"».
- R5-MATERIALY-2, materialy-priemki.mjs:10-11. «Подпись из проверки — только у znak-plotnosti.png (обводка). Подписи ekran-vladelca.png об „TH“ и перекладине — текст инструмента, а не вывод проверки».
- R5-MATERIALY-3. «Полоса проверяется у окон браузера с полосой, а не у каждого окна, которому её обещают».
- R5-MATERIALY-14, шапка vkladka-dowod.mjs. «При „рамка не найдена“ прогон обрывается исключением до vkladka-otkaz.json, и сырой кадр окна остаётся в папке доклада».
- R5-BRAUZER-9. «У znak-podvala-okno.mjs самопроверки нет, его ветви отказа не вызывались ни разу».
- faza-perekladiny.mjs в докладе назвать замером, а не судьёй. «Только 1440, прокрутка 0, без окна, без отказов и без контроля».

Прозе DESIGN, README, BACKLOG и черновика — правки по R5-*-документ, как в сводке.

**Новые находки критика (не вошли в R5)**

- R5-POLNOTA-1 [самопроверка нынешнего wiernosc.mjs не прогонялась ни разу]
  - zamery/wiernosc-selftest.json снят редакцией 3df1c9e3 (9b896e5): 35 проб.
  - Нынешний файл — 9a1ebd3d: 45 проб по коду (чистая страница, 37 мутаций, 7 подмен).
  - 22234e6 обещал «итог — отдельным коммитом», такого коммита нет. Рабочее дерево не менялось, а выгрузка пишется при любом исходе, значит полного прогона не было.
  - Линзы R5 гоняли только копии и sborka (sud-r5/brauzer/log-sb-selftest.txt).
  - Как закрыть: `node wiernosc.mjs --selftest` на HEAD — это прогон, а не правка судьи. Выгрузку закоммитить отдельно, число — в доклад:26 и :205. Любое НЕТ — дефект в таблицу пределов.
- R5-POLNOTA-2 [черновик доклада:171 «у каждого — самопроверка на мутациях или положительный контроль»] Неверно для materialy-priemki.mjs, faza-perekladiny.mjs и znak-podvala-okno.mjs: в них ноль упоминаний selftest, у faza-perekladiny нет и отказов. В доклад:169 faza-perekladiny стоит среди судей.
- R5-POLNOTA-3 [черновик доклада наследует R5-документ]
  - :237-239 «край чёткий, когда 20·DPR и 24·DPR целые» верно только при прокрутке 0 (R5-MATERIALY-1).
  - :240 «полоса прокрутки Windows» (R5-MATERIALY-11).
  - :134 «ваш экран» — снимки без окна.
  - :263 — остановка записана в «технических» (п. 2).
- R5-POLNOTA-4 [к R5-BRAUZER-8] Три выгрузки со старым помощником 826bae3c сверяли сборку ещё старым, узким разбором. Это одной строкой в доклад, не пересъёмка.
- R5-POLNOTA-5 [мелочь в DESIGN.md:703] Правка 22234e6 склеила два предложения в одну длинную строку («…решение владельца. Новый контур буквы…»).
- R5-POLNOTA-6 [напоминание] «Дополнение к П83, 2026-09-25» в DECISIONS.md не записано (R3-POLNOTA-8 открыт). SVODKA-r5.md не лежит в sud/ (доклад:321).

**4. Что владельцу проверить глазами (175 %, 3840 px, свой Chrome)**

Страница: `npm run preview` в sites/7thserpent.com, затем http://localhost:4331/.

1. Перекладина семёрки в шапке — вверху и во время прокрутки колесом. При 1,75 край чёткий, только когда прокрутка кратна 4 CSS px, поэтому при плавной прокрутке он может «дышать» (R5-MATERIALY-1). Фазу окна своего Chrome 153 с его панелью не снимет ни один инструмент.
2. Левый край знака — семёрка и «T». С полосой прокрутки x знака дробный, при 1,25–1,75 край в полутоне (R5-MATERIALY-4).
3. «TH» без обводки при 1,75: читается ли волос. Это политический п. 4.
4. Подвал вблизи: перекладина в полутоне — терпимо ли.
5. Наведение и Tab на знаке: кольцо фокуса видно целиком. Нет свечения, тени, копии знака под шапкой и мигания — эти состояния за коробкой не судятся, страж движения не охраняется (R5-BRAUZER-1, -2).
6. Узкое окно (меньше 961 CSS px, это меньше примерно 1680 px экрана): бургер виден целиком, знак на него не налезает, ящик открывается (R5-BRAUZER-5).
7. Окно шириной 961–1180 CSS px (примерно 1680–2065 px): меню в одну строку, шапка не выросла.
8. Настоящая контрастная тема Windows, светлая и тёмная (не эмуляция): знак — одним цветом текста, трассер и ромб не пропали. При 1,75 принудительные цвета не судит никто.
9. Вкладка: иконка 28 px с тёмными ореолами на вашей панели в светлой и тёмной теме. Ещё закладка (Ctrl+D), панель закладок и история: там могут появиться PNG 16 или ICO, которых не показал ни один прогон.
10. Заголовки страницы набраны Bodoni Moda — тем же рисунком, что «SERPENT». Гарнитуру в браузере не судит ни один судья знака.
11. Масштаб страницы (Ctrl + / Ctrl −) — на 100 %. При 110 % (DPR 1,925) перекладина шапки в полутоне и в покое, судья таких плотностей не снимал.

**Файлы:**
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/zamery/wiernosc-selftest.json (устаревшая выгрузка, редакция 3df1c9e3)
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/wiernosc.mjs (9a1ebd3d)
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/kontrast-znaka.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/zapas-menu.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/sborka.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/instrumenty/faza-perekladiny.mjs
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak/zamery/faza-perekladiny.json
- D:/SEO/cloud/site-generator/docs/reports/2026-09-24-7thserpent-znak.md (черновик, строки 26, 169-171, 205, 209, 237-240, 262-263)
- D:/SEO/cloud/site-generator/sites/7thserpent.com/tools/znak.mjs (строки 54, 315-340, 563-574)
- D:/SEO/cloud/site-generator/sites/7thserpent.com/DESIGN.md (строки 661-722)
- D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/sverka/mutanty.json, mutanty2.json
- D:/SEO/cloud/site-generator/.playwright-mcp/sesja-11/sud-r5/materialy/shapka-prokrutka.txt
- D:/SEO/cloud/site-generator/DECISIONS.md (строки 5413-5415, 5778-5779)
