# 2026-09-23 · Сессия 8 трека «второй сайт» — полоса прокрутки шасси и проход темы `7thserpent.com` по референсам; эталон главной переснят

Ветка `7thserpent-tema-2`, заведена от `main` = `3e3b26c`: к старту
перевод первого сайта (`ac4bf-en`, П76, П77) оказался влит в `main`
поверх `48243b8`. Сверка объёма с влитым переводом показала одно
расхождение — польский эталон первого сайта при английском сайте; вопрос
владельцу до правок, ответ (а): ветка от `3e3b26c`, кадры первого сайта —
двумя сверками (П75 п. 3–5). Слова владельца на старте — П75, дословно.
**Объём не расширялся;** найденное вне объёма — бэклог 57.
**Стоп — приёмка глазами:** главная в обновлённой теме, её новый эталон
и макет палитры (раздел 3).

---

## Первой строкой, как велит П33 — и раскрытие

**Гейты `4/4`, `exit=0` у обоих сайтов (контраст второго — 18/18 с парой
ползунка, первого — 45/45). Сборка и `astro check` второго сайта —
`exit=0`, 0 ошибок, 0 предупреждений, 1 подсказка (как в сессии 7).
Сборка первого сайта `exit=0`, `accept` 225/225; кадры первого сайта —
две сверки по ответу (а), стоп по кадрам снят ответом владельца (а)
(раздел 2). `accept` второго сайта против нового эталона — 223/224
(пропуск «нет szum.json», намеренно), `accept:frames` — 0 различий на
итоговом `HEAD`. `structure.json` обоих сайтов — ни байта; в ядре
изменён только `core/styles/base.css`; у первого сайта — ровно одна
строка в `gates/contrast.mjs`, CRLF как были.**

**Раскрытие — слово `sed` в командах (правило памяти исполнителя,
нарушено по букве; инвариант 1 не нарушен, `sed -i` не было, файлы
целы):** (1) в начале сессии, до чтения файла памяти, дважды читал
диапазоны строк `DECISIONS.md` через `sed -n`; (2) в одной служебной
команде проверки оказалась лишняя переменная с этой подстрокой в имени
(присвоение, не вызов); (3) слово стояло в тексте сообщения коммита
`67d4dde`, переданного в оболочку heredoc'ом (описание раскрытия п. 1).
Урок дописан в память: файл памяти читать до первой команды; текст со
словом — файлом через Write и `git commit -F`. Решение о последствиях —
«Политические» п. 2.

## Сверка открытых вопросов прошлого доклада — по П38

| Вопрос доклада `2026-09-23-7thserpent-tema.md` | Исход | Где |
|---|---|---|
| 1. Слово `sed` (только чтение) | принято, решения обзора стоят | П73 п. 2 |
| 2. Приёмка глазами темы и первого эталона | заменена проходом этой сессии | П74 п. 4 |
| 3. Полоса прокрутки шасси | (а) — **сделано:** ядро, пары обоих сайтов, исключение снято | П73 п. 1; `b4a39c0`, `af4e9ff` |
| 4. Остановка «судью судят» после раунда 2 | принято | П73 п. 2 |
| 5. Вливание `7thserpent-tema` | `main` перемотан владельцем до `48243b8` | П75 (слова владельца) |
| 6. Переносятся | по-прежнему переносятся | «Политические» п. 11 |

---

# 1. Счёт сессии

| Что | Число |
|---|---|
| Коммитов в `7thserpent-tema-2` | 10 + записи и доклад: `67d4dde` П75 · `b4a39c0` ядро: полоса прокрутки · `af4e9ff` пары ползунка обоих сайтов · `40bd708` капслок, `Grain` · `aad036d` сверка палитры, макет · `ad8a9d0` ядро: комментарий по раунду 1 · `b43b5d6` проза по раунду 1 · `8759010` ядро: комментарий по раунду 2 · `13a8b2a` проза по раунду 2 · `d08d24c` эталон, инструменты |
| Ядро | `core/styles/base.css` — один механизм полосы прокрутки, ползунок `ink-muted` (плюс два коммита только комментариев) |
| Первый сайт | `gates/contrast.mjs` — одна строка (пара ползунка) |
| Второй сайт | `src/styles/global.css` (`.t-headline`), `src/layouts/Base.astro` (`Grain`), `gates/contrast.mjs`, `DESIGN.md`, `CLAUDE.md`, `_baseline/` (4 кадра, `geometria.json`, README) |
| Корень | `DECISIONS.md` (П75 с дополнением), `docs/BACKLOG.md` (56, 57), этот доклад и папка материалов |
| Workflow | оценка темы — 7 агентов; «судью судят» раунд 1 — 9, раунд 2 — 4; «ломает» — 0 и 0 |
| Прогоны кадров | первый сайт — 11 прогонов (английское дерево 4, польское 7), второй — 4 |

# 2. Полоса прокрутки шасси (п. 1 объёма)

**Правка ядра** (`b4a39c0`): остался один механизм — стандартные
`scrollbar-color` и `scrollbar-width` (MDN: `scrollbar-color` — Chrome 121,
Firefox 64, Safari 26.2; `scrollbar-width` — Safari 18.2); правила
`::-webkit-scrollbar*` сняты — Chromium при заданном `scrollbar-color`
их не применял. Браузер без этих свойств рисует системную полосу по
`color-scheme: dark`.

**Токен ползунка — `ink-muted`** (выбор исполнителя из двух, названных
владельцем):

| Сайт | `ink-muted` на `bg-band` | `accent-2` на `bg-band` | было (`surface-2`) |
|---|---|---|---|
| первый | **7,37:1** | 6,09:1 | 1,13:1 |
| второй | **8,05:1** | 6,61:1 | 1,15:1 |

Довод: ползунок — служебный элемент; `accent-2` у каждого сайта свой
и может быть насыщенным (у первого — тёплая латунь на краю каждой
страницы). Замер Chrome 153: ползунок рисуется ровно токенами
(`#a8a199` на `#131110` у первого сайта — вырезка
`docs/reports/2026-09-23-7thserpent-tema-2/polosa-prokrutki-ac4bf-1440.png`),
под курсором браузер затемняет его сам, но не ниже 3:1 (около 4,4:1).

**Пары гейта** (`af4e9ff`): `['ink-muted', 'bg-band', 3, …]` в обоих
сайтах; у первого — ровно одна строка, CRLF 61/61, одиночных LF 0.
Исключение про полосу прокрутки во втором сайте снято (`contrast.mjs`,
`DESIGN.md`). **Предел, найденный раундом 1:** пара связана с `base.css`
только подписью (гейт `base.css` не читает) и перекрыта текстовой парой
4,5 — возврат тусклого ползунка в ядре гейт бы не поймал. Названо
в комментарии второго сайта; связать гейт с ядром — бэклог 57 п. 3
(«Политические» п. 8).

**Кадры первого сайта — две сверки по ответу (а)** (все прогоны, без
отбора; сырые вердикты — `docs/reports/2026-09-23-7thserpent-tema-2/kadry-pervogo-sajta/`):

| Сверка | Прогоны | Итог |
|---|---|---|
| (1) английское дерево, «до» и «после» `base.css` | до ×2 (`en-do`, `en-do2` — прежний `base.css` временно, без коммита), после ×2 | у каждого из 28 кадров (главная 11, игра 15, `visitor` 2) есть побайтово равная пара «до»/«после». Попарно «до × после» — по одному полному кадру вне допуска: дрожь полных кадров бывает и между прогонами одного состояния (главная 390 «до» против «до» — 135 924 субпикселя, макс 11) |
| (2) польское `48243b8` + только `base.css` против польского эталона, главная и `visitor` | до ×1, после ×1 | главная 11/11 и `visitor` 2/2 — 0 различий |
| (2) то же, страница игры | до ×3, после ×4 | «до» — 3/3 по 15/15 без различий; «после» — `pl-po3`, `pl-po4` 15/15 без различий; `pl-po` — 3 кадра вне допуска (картинка `freedom-cry`, макс 20), `pl-po2` — 1 (герой в полном 390, 23 субпикселя, макс 3) |

По букве стопа («эталон разошёлся — стоп и вопрос») — вопрос владельцу
в ходе сессии; **ответ (а): эталон не разошёлся** (дополнение к П75).
Полоса прокрутки в кадры не попадает (драйвер снимает со скрытой полосой),
геометрия во всех прогонах совпала до сотых.

# 3. Проход темы по референсам (п. 2 объёма)

**(а) `.t-headline` — капслок с разрядкой 0.05em** (`40bd708`), как
у шасси первого сайта: восемь объявлений роли совпадают с первым сайтом.
**(б) Зерно ядра `Grain`** — в `Base.astro` после подвала, как у первого
сайта: `soft-light`, 0.085, `prefers-reduced-transparency` его снимает.
**(в) Палитра и гарнитуры — сверены и оставлены** (`aad036d`). Оценка —
workflow из 7 агентов: замер пикселей `references/1.png`–`5.png`
в OKLCH, три независимые линзы палитры (верность референсам и `archiv`,
мир и бриф, гейт), судья гарнитур, встройка `Grain`, синтез. Все пять
линз сошлись: ни одно значение не меняется.

- Фон `bg` (L 0,153) — внутри тёмных полей референсов (рядом с тёмным
  кластером `1.png` и `5.png`); лестница поверхностей — шаги шасси
  `archiv` §1.1; чернила 16,2:1 — как у референсов (13,1–16,3:1). Поднять
  фон к медианам референсов — опустить все шесть пар на `bg`.
- Bodoni Moda 600 + Public Sans 400/500/600: правило `archiv` §2
  выполнено, `tnum` подтверждён разбором GSUB всех отдаваемых файлов
  и замером; внешних запросов нет. Oswald отвергнут (нет `tnum`),
  переменный Bodoni — «Политические» п. 5.
- **Неверный довод исполнителя — назван:** я написал «у всех пяти
  референсов заголовки прописными»; в `references/1.png` — гротеск
  обычным регистром, верно «у четырёх из пяти». Исправлено в `global.css`
  и `DESIGN.md`; сообщение `40bd708` не переписывается. Решение П74 п. 1
  держится на доводе «как у шасси первого сайта» и остаётся в силе.
- **Зерно и гейт:** гейт его не считает; граница по формуле W3C
  soft-light в крайнем, недостижимом случае — самая тесная пара
  (опасность на фоне) не ниже 5,35:1 при 5,69 без зерна.
- `DESIGN.md` сайта — по итогу: фронтматтер `headline` 0.05em, «Правило
  капслока», зерно, итог сверки палитры числами, полный перечень того,
  чего гейт не считает. **Сверка «фронтматтер = `global.css`» — скриптом:**
  83 значения, 0 расхождений (скрипт сессии 7 в репозиторий не попал;
  новый — в папке материалов, пробы подменой 26/26).
- `design.*` брифа сверены — утверждения верны, правка не нужна.

**Приёмка глазами — где смотреть:**
- главная, новый эталон: `sites/7thserpent.com/_baseline/baseline-desktop-hero-1440x900.png`,
  `baseline-mobile-hero-390x844.png`, `baseline-mobile-full-390.png`;
- макет палитры на элементах шасси, которых на главной пока нет (кнопки,
  ссылка, фокус, выделение, карточка с табличными цифрами и счётчиком
  стали, полоса подвала, опасность, окно с ползунком прокрутки, образцы
  токенов) — **макет, не сайт:** `docs/reports/2026-09-23-7thserpent-tema-2/palette-specimen.png`
  (исходник — `palette-specimen.html`);
- ползунок полосы прокрутки первого сайта в Chrome — `docs/reports/2026-09-23-7thserpent-tema-2/polosa-prokrutki-ac4bf-1440.png`.

# 4. Пересъёмка эталона главной (п. 3 объёма)

Сборка `b43b5d6`, драйвер — генератор сессии (профиль `7s`, сверка сайта
на порту по `canonical`), три прогона одной сборки побайтово равны на всех
четырёх кадрах — зон шума не нужно; эталон — прогон 1. Прогон 4 на
итоговом `HEAD` (`13a8b2a`) против нового эталона — 0 различий, `exit 0`.
Против прежнего эталона геометрия совпала целиком (капслок не изменил
число строк `h1`), диффы по месту:

| Место | 1440 | 390, первый экран | 390, полный | Причина |
|---|---|---|---|---|
| рамка `h1` | 126 035 субпикселей, макс 222 | 50 882, макс 222 | 50 882, макс 222 | капслок |
| окно вне `h1` | 2 506 887, макс 3 | 657 780, макс 3 | то же, только строки 0–843 | зерно |
| полный 390, строки 844–936 | — | — | 0 | зерно в `fullPage` лежит только на первом окне |

README эталона — раздел пересъёмки, таблица, хеши прежних блобов по П17,
условия съёмки (HeadlessChrome 153, зерно видно), правда о `ГОТОВНОСТИ`
(первая съёмка называла её «дословной»).

# 5. «Судью судят» — два раунда (п. 4 объёма)

**Раунд 1** (workflow, 9 агентов: линзы «правка ядра», «пары гейта обоих
сайтов», «данные контраста второго сайта с зерном», «инструменты приёмки
сессии»; скептик на каждую; критик полноты). «Ломает» — 0. Подтверждено
и исправлено: «худший пиксель» зерна — на деле верхняя граница
недостижимого случая; «самая тесная текстовая пара на поверхности» —
сталь на карточке 6,27, не 7,02 (устарело с сессии 7); неполный перечень
«чего гейт не считает»; «на главной шапки нет» — ложно; пара ползунка
связана с ядром только подписью; версии Safari у двух свойств разные;
дыры сверки `DESIGN.md` (переопределения роли вне своего правила, второй
`:root`, капслок не сверялся); строка «драйвер» звала `ГОТОВНОСТЬ`
дословной; нет проверки, какой сайт на порту. Коммиты `ad8a9d0`,
`b43b5d6` и доработка инструментов. Критик добавил: `astro check`
и маркер `@source` не были записаны (сделано, раздел 6), печать и дерево
доступности (бэклог 57 п. 5–6), полоса снята только в headless (57 п. 4).

**Раунд 2** (узкий, 4 агента по правкам раунда 1). «Ломает» — 0;
подтверждены дыры покрытия новой проверки капслока (повтор
`text-transform`, отрицание «не капслок», вложенное правило в теле роли,
блок `:root, :host`), неполные «пределы» сверки и проза. Исправлено
(`8759010`, `13a8b2a`, инструменты). **Остановка** — критерий П68, П70,
П72: последний раунд нашёл только дыры покрытия и прозу, нейтральные на
живых данных. Третий раунд — по слову владельца («Политические» п. 10).

# 6. Таблица гейтов и проверок

| Проверка | Результат |
|---|---|
| `npm run gates -w 7thserpent.com` / `-w ac4bf-thewatch.com` | 4/4, `exit=0` (контраст 18/18, ползунок 8,05:1) / 4/4, `exit=0` (45/45, ползунок 7,37:1) |
| `npm run build -w 7thserpent.com`, `npm run check -w 7thserpent.com` | `exit=0`; 0 ошибок, 0 предупреждений, 1 подсказка |
| `npm run build -w ac4bf-thewatch.com` (английское дерево) | `exit=0`, 32 страницы |
| `npm run accept -w ac4bf-thewatch.com` | 225/225, `exit=0` |
| Кадры первого сайта | две сверки по ответу (а) — раздел 2; стоп снят ответом (а) |
| `npm run accept -w 7thserpent.com` | 223/224, `exit=0` (пропуск — «нет szum.json», намеренно) |
| `accept:frames` второго сайта против нового эталона | прогоны 2, 3, 4 — 0 различий, геометрия совпала, `exit 0` |
| Живость `@source` (инвариант 8) | маркер `z-[61]` в `core/chrome/SiteHeader.astro` → `z-index:61` в CSS есть; `z-[62]` в `src/pages/index.astro` сайта → есть; контрмаркер `z-[63]` в `core/gates/check-tokens.mjs` → нет; маркеры откачены, `git diff` пуст, чистая пересборка без них |
| Полоса прокрутки в браузере | Chrome 153 headless без скрытия полосы: ползунок = `ink-muted`, дорожка = `bg-band` на обоих сайтах; раунд 1 — то же в Edge 153 |
| `DESIGN.md` = `global.css` (второй сайт) | 83 значения, 0 расхождений; пробы подменой 26/26 |
| Детектор impeccable (`detect.mjs`) | 0 находок |
| `structure.json` обоих сайтов против `3e3b26c` | ни байта |
| Ядро против `3e3b26c` | только `core/styles/base.css` |
| Первый сайт против `3e3b26c` | только `gates/contrast.mjs`, +1 строка |
| Окончания | изменённые текстовые файлы — `i/lf w/lf` (36), `sites/ac4bf-thewatch.com/gates/contrast.mjs` — `i/crlf w/crlf` (61/61); `git add --renormalize .` — только мои незакоммиченные правки; `dist/` в корне нет |
| Git | ветка = `origin/7thserpent-tema-2` после каждого коммита; `main` не тронут (`origin/main` = `3e3b26c`, ветка — его потомок) |

# 7. Факты, важные будущим сессиям

1. **Эталон первого сайта польский, сайт английский** (П76 п. 3):
   `accept:frames` первого сайта против эталона на дереве `main` больше
   не работает. Проверка правок ядра — как в этой сессии: «до/после» на
   английском дереве и буквально на польском `48243b8` (профили
   `ac4bf-en` и `ac4bf-pl` генератора). Бэклог 57 п. 8.
2. **Кадры не видят полосу прокрутки:** Playwright MCP снимает со скрытой
   полосой. Цвет ползунка проверяется Chrome headless=new без
   `--hide-scrollbars` (снимок CLI или CDP) и замером пикселей у края.
3. **Зерно в `fullPage` лежит только на первом окне:** ниже 844 px полного
   кадра 390 зерна нет — сторож судит там чистый фон.
4. **Картинки страницы игры первого сайта дрожат** между прогонами одной
   сборки (`freedom-cry` в ряду, герой в полном 390) без зон — при сверке
   нужен контрольный прогон, а не один (бэклог 57 п. 7).
5. **`astro preview` — демон:** запуск возвращается сразу, сервер живёт
   до `npx astro preview stop` из папки сайта; превью первого и второго
   сайта на одном порту подменяют друг друга — генератор сверяет сайт
   по `canonical`.
6. **Инструменты сессии** — `docs/reports/2026-09-23-7thserpent-tema-2/instrumenty/`:
   `kadry-gen.mjs` (драйвер кадров, профили `ac4bf-en`, `ac4bf-pl`, `7s`),
   `geo.mjs` (геометрия из прогона), `sverka-design.mjs` (фронтматтер
   `DESIGN.md` = `global.css`, `--selftest`), `zerno.mjs` (граница зерна
   по парам гейта), `mesto.mjs` (дифф по месту), `kraj.mjs` и
   `vyrezka.mjs` (полоса прокрутки на снимке).
7. **Агентам workflow — запрет `git checkout` и сборок** в общей папке:
   переключение дерева или `npm run build` агентом ломает работу ведущего.

# 8. Открытые вопросы

## Технические — решены

- Ветка от `3e3b26c`, кадры первого сайта двумя сверками — по ответу (а).
- Один механизм полосы — стандартный; токен ползунка — `ink-muted`.
- Палитра и гарнитуры оставлены по замеру; `design.*` брифа без правок.
- Капслок и зерно — «как у первого сайта»; неверный довод назван.
- Эталон главной: три прогона побайтово, зон шума нет; README по П17.
- «Судью судят» — два раунда, всё подтверждённое исправлено, остановка.
- Инструменты — в папке доклада, пределы названы в шапках.

## Политические — владельцу

1. **Приёмка глазами** — главная в обновлённой теме, новый эталон,
   макет палитры (пути — раздел 3). (а) принять; (б) правка словами —
   тогда правка темы и пересъёмка эталона. **Рекомендация — (а).**
2. **Правило памяти о слове `sed`** (раскрытие выше; инвариант 1
   не нарушен). (а) принять с записью в докладе и памяти; (б) иное
   следствие. **Рекомендация — (а).**
3. **Тон фонаря и знак издателя.** `accent` `#eca84a` по тону почти
   совпадает с жёлтым знаком Rockstar (`#ffab00` по сторонним справочникам;
   ΔH 1,7°, ΔE 0,043); хрома ниже заметно — «не точная палитра» соблюдено.
   (а) оставить и записать в `DESIGN.md` коридор: при H ≈ 72 не поднимать
   хрому к 0,17 и светлоту к 0,80; (б) натриевый тон `#f7a05a`/`#fdb677`
   (18/18, но фонарь сближается с красным опасности). **Рекомендация —
   (а)**, строка коридора — после «да».
4. **Капслок на длинных текстах** (следствие П74 п. 1; бэклог 57 п. 6):
   цитаты `band-quote` и `/quotes/` наберутся прописным Бодони, `h1`
   в 56–75 знаков — 4–6 строк на 390px. (а) как у первого сайта,
   вернуться с первыми текстами пачки 0; (б) решать сейчас — правка роли
   или конструкции ядра. **Рекомендация — (а).**
5. **Бэклог 56 п. 7 — переменный Bodoni Moda:** замер — волосяные линии
   на тёмном при 28–44px пропадают, файл втрое тяжелее. (а) закрыть
   пункт; (б) оставить открытым. **Рекомендация — (а).**
6. **Эталон первого сайта на английском дереве** (бэклог 57 п. 8).
   (а) правки ядра и дальше сверять «до/после» и на польском дереве;
   (б) переснять эталон первого сайта по английскому дереву отдельной
   сессией (вместе с сессией ошибок источника, П77 п. 5). **Рекомендация —
   (б):** приёмка должна судить то, что живёт на домене.
7. **Дрожь двух картинок страницы игры первого сайта** (бэклог 57 п. 7).
   (а) завести зоны по измерению при ближайшей работе с эталоном первого
   сайта (с п. 6); (б) не заводить — каждая такая сверка идёт с
   контрольными прогонами. **Рекомендация — (а).**
8. **Связать гейт контраста с `base.css`** (бэклог 57 п. 3): гейт читает
   `scrollbar-color` ядра и требует пару с этими токенами. (а) при
   ближайшей правке ядра; (б) оставить подписью. **Рекомендация — (а).**
9. **Вливание `7thserpent-tema-2` в `main`** — перемоткой (ветка — потомок
   `main` = `3e3b26c`, слияний нет); владелец или слово «влей сам».
   **Рекомендация — влить после приёмки глазами.**
10. **Остановка «судью судят» после раунда 2** («ломает» 0 и 0).
    (а) принять; (б) третий раунд. **Рекомендация — (а).**
11. **Переносятся без ответа:** лицензия кадров и арта издателя, П42
    тексты заново, маршрут `[...slug]` и `links`/`corridor`, реализация
    `video` (пачка 0); приватность США / ящик / деплой; бэклог 54 п. 2,
    4–11, 13; 55; 56 п. 2–6, 8; 57 п. 1–2, 4–5, 9–11.

**Дальше — по команде владельца:** после приёмки — пачка 0 второго сайта
(П42 заново, маршрут `[...slug]` и подключение `links`/`corridor` —
бэклог 54 п. 9, реализация `video`, лицензия кадров), как и назначено
П73; без команды не идти.

---

# Записи, обещанные этой сессией — по П36

| Что | Где | Источник |
|---|---|---|
| П75 на старте | `DECISIONS.md` (`67d4dde`) | форма П60, П70, П72 |
| полоса прокрутки: ядро и пары | `b4a39c0`, `af4e9ff`, `ad8a9d0`, `8759010` | П75 п. 1 объёма |
| капслок, `Grain`, сверка палитры, макет | `40bd708`, `aad036d`, `b43b5d6`, `13a8b2a` | П75 п. 2 объёма |
| пересъёмка эталона, инструменты | `d08d24c` | П75 п. 3 объёма |
| дополнение П75 (ответ (а) по стопу, технические решения); бэклог 56–57; `CLAUDE.md` сайта; доклад | коммит записей | П75 п. 5 объёма |
| память исполнителя: слово `sed` — пятый раз, урок | `~/.claude/projects/…/memory/edit-tool-crlf.md` | раскрытие этой сессии |

**Обещано и НЕ сделано:** ничего из объёма. Вне объёма и не тронуто:
композиция и новые блоки, арт и лицензия, пачка 0, вынос инструментов,
бэклог 56 п. 2–8, ветка `ac4bf-en`, первый сайт сверх пары ползунка,
публикация.
