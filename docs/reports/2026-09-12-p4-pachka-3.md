# 2026-09-12 · P4, пачка 3 — девять игр по объёму; допуск шума — числом в коде (П45); поле `era`

Ветка `phase-3`, вершина на старте — `00a8f4f` (доклад пачки 2), на финале —
см. последний коммит; дерево чистое, всё запушено. Объём — П45: пачка 3 без
Valhalla (девять игр, линза фактов по принятым текстам), допуск шума
из прозы в проверку числом до любой следующей пересъёмки, эпоха страницы
для игр из пяти, вердикты по умовным ключам и зоне 4 — без стопа.

---

## Первой строкой, как велит П33

**`npm run accept` — `201/201`, `exit=0`. `npm run gates` — `4/4`, `exit=0`.**
`npm run build` — `exit=0`, **двадцать пять страниц** (было 16), четыре
сторожа результата зелёные: `h1: 25`, `kotwice: 25`, `linki: 25 stron, 753
linków`, **`korytarz: 25 stron — w korytarzu 22, bez korytarza 3`** — все
девять новых текстов в коридоре с первой сборки. `astro check` — 0 ошибок.
**`npm run accept:frames -- check` на контрольном прогоне пересъёмки против
эталона — `ИТОГ ПИКСЕЛЕЙ: 0 различий 11/11 и 10/10`, `exit=0`.**

## Сверка открытых вопросов прошлого доклада — по П38

Доклад пачки 2 вопросов не оставил; П45 принял сессию вида и пачку 2.
Одна строка по зоне шума 4, как велено: **не было числом — сделано** (ниже).
Список записей П36 прошлого доклада — все на месте.

---

# Что сделано

## 1. Допуск шума — числом в коде (П45)

**Было:** зоны шума — проза README эталона; `frames.mjs check` сверял состав,
размеры и геометрию; пиксели — руками `pixels.mjs`, без порога, приговор
выносил человек. **Стало** (`14a1b82`): `szum.json` рядом с эталоном — список
зон числом: кадры (regex или список), рамка `{x,y,w,h}` или весь кадр, маска
«только светлые пиксели эталона» с порогом яркости, амплитуда `макс`;
`core/accept/szum.mjs` — чистая `ocenSzum`: каждый различающийся субпиксель
обязан попасть в первую зону, которая его принимает (кадр, рамка, яркость,
|Δ| ≤ макс), иначе — вне допуска; размеры разошлись — вне целиком; нет
`szum.json` — любой дифф отказ. `check` после геометрии сверяет каждую пару,
печатает строку на кадр и `ИТОГ ПИКСЕЛЕЙ`, **отказывает кодом 1**; крах
инструмента — 2; не-судьи из `ne-sudyi.json` — отдельной графой, не в код;
лишний снятый кадр — строкой. CLI — `npm run accept:szum`.

**Зоны:** главная — зона 1 (полоса шкалы x 37–46, четыре измеренных кадра,
макс 2), ленивые фото `wlochy` (шесть рамок, макс 11), зона 4 (полные кадры,
весь кадр, макс 2); игра — галерея (две рамки, макс 23), ряд (макс 13),
зона 4. Числа — из README, нулевого прогона и пересъёмки; границы
рамок измерены (столбец 47 снаружи).

**Калибровка опровергла довод «только светлые»** (доклад сессии вида,
F12): прежние блобы против нынешнего эталона — остаток ±1–2 лежит и на самом
фоне `--bg` (13,12,10 → 13,13,10: 81 547 из 82 842 субпикселей у игры 1440),
порога яркости «весь остаток, но не фон» не существует. Зона 4 записана как
весь полный кадр при макс 2, маска реализована и выключена — включается
одним числом. **Цена зоны 4 числом** (в `откуда`): правка амплитудой ≤ 2
в областях, которые видит только полный кадр — 36 % высоты главной на 1440,
94 % на 390; 11 % / 87 % у игры — для `check` невидима. Полный кадр —
сторож, не судья (П12); оконные кадры зоны 4 не имеют.

**Судью судили** (независимый агент, 46 мутаций): стопов нет; 7 находок
и 13 непойманных мутаций — все починены: пустой эталон давал `exit 0`
(теперь 2), краш сталкивался с кодом 1 (теперь 2), не-судьи попадали
в отказ, проза README/`11_PRIEMKA`/бэклог 26 противоречила файлу, обвязка
`check` не была под батареей (теперь прогоняется процессом на scratch-папках),
лишний кадр молчал. Батарея — 59 случаев, `accept` 201/201.

**Вопрос владельцу, найденный судьёй** (без стопа): смысл `ne-sudyi.json`
при кодовом вердикте и два правила приёмки — «реестр принятых расхождений»
README против «эталон равен сборке»; рекомендация — второе, реестр пуст
и так.

## 2. Поле `era` — эпоха страницы для палитры (П45)

`content.config.ts`: `era` необязательное, из пяти; маршрут — эпоха ряда →
эпоха страницы по арту → `era` → без умолчания (`570fc3a`). Пачка 3:
`discovery` → `wlochy` (Эцио, Испания 1491, дитя `/assassins-creed-2/`);
остальные восемь — вне пяти, accent. Бэклог 37 закрывается инкрементально;
кандидаты пачки 2 по спискам `guides` главной — строкой в пункте, не правкой.

## 3. Девять текстов — счёт гейта коридора

Тот же способ, что пачка 2; линзе фактов даны **все пятнадцать принятых
текстов** — она поймала расхождения с ними: AC3 «пять лет Десмонда» против
`wrzesień 2012` первой части; Mirage «Alamut znany z jedynki» против Масьяфа
в `assassins-creed-1.md`; Chronicles «Revelations — ostatnia gra o Eziu»
против Nexus VR в `ezio-auditore.md` и «między Rogue a Syndicate» против
годов действия; Unity «na Switchu wyszedł tylko pakiet…» против Ezio
Collection; Odyssey «Bayek związany kredem» против проводника. Форма:
Odyssey `year: 431 p.n.e.` (10 знаков) → «Misthios»; Unity — арт ряда
`londyn` (кадр Syndicate на странице Unity) → снят линзой; Mirage — абзац
по канве одной статьи (окна по 7 слов) → переписан. Дословных совпадений
≥ 8 слов — 0 по всем девяти.

| Страница | Коридор | Знаков без пробелов | Рядов | `era` | Коммит |
|---|---|---:|---:|---|---|
| `/assassins-creed-3/` | 5365–7259 | **6622** | 7 | — | `dc14952` |
| `/assassins-creed-origins/` | 3138–4246 | **4035** | 5 | — | `a6045cc` |
| `/assassins-creed-mirage/` | 7292–9866 | **9106** | 8 | — | `6524d80` |
| `/assassins-creed-odyssey/` | 4271–5779 | **5402** | 6 | — | `768f0ed` |
| `/assassins-creed-unity/` | 4514–6106 | **5765** | 6 (без byline — по `blocks[]`) | — | `a5c29df` |
| `/assassins-creed-liberation/` | 6567–8885 | **8469** | 7 | — | `8ea33d6` |
| `/assassins-creed-chronicles/` | 4665–6311 | **6094** | 6 (без toc) | — | `b06cf65` |
| `/assassins-creed-valhalla/dawn-of-ragnarok/` | 2583–3495 | **3293** | 4 | — | `b6b7e40` |
| `/assassins-creed-2/discovery/` | 2462–3332 | **3140** | 4 (без byline/toc/gallery/verdict) | `wlochy` | `5a44b2d` |

Коммит на страницу — девять коммитов `P4 пачка 3: текст <slug> — N в a–b…`
(`git log 00a8f4f..5a44b2d`).

### Отчёт помеченного и выкинутого — по П42 п. 2

Полный список — **`docs/reports/2026-09-12-p4-pachka-3/fakty-pomeczone-i-snyatye.md`**.
Итог: 282 факта в девяти черновиках, **73 переформулированы или сняты**
черновиками, **10 стопов линзы фактов** и 37 «исправить» — все починены её
правкой. Расхождения источников, сглаженные в тексте: годы действия AC3
(1753 у pl.wikipedia против 1754 у en и принятых текстов — взято 1754),
дата PC-премьеры AC3 (22/23 листопада — «w listopadzie»), Liberation HD
(14/15 stycznia — «w połowie stycznia»), Discovery Tour Odyssey (wrzesień/
październik). Несколько черновиков честно назвали: тема тянет на 8–12 тысяч
(AC3, Unity, Odyssey, Discovery) — резали до коридора, снятые подтверждённые
факты перечислены как `usunięte`.

**Покрытие ключей** — `docs/reports/2026-09-12-p4-pachka-3/pokrycie-kluczy.md`:
как и в пачке 2, точные формы запросов редки (1–2 на страницу); Discovery — 0
(ключи «assassins creed 2 nintendo ds» и т. п. — в тексте «Nintendo DS»
и «Assassin’s Creed II: Discovery» с двоеточием).

## 4. Прочее

Агент линзы оставил в папке содержания временный файл
`assassins-creed-rogue.md.txt` вопреки запрету — удалён до коммита, в git
не попал. `.gitkeep` пачки 2 — урок учтён, скрипт разделителей обошёл его.

---

# Гейты и проверки

| Проверка | Результат |
|---|---|
| `npm run accept` | 201/201, `exit=0` (142 + 59 шума) |
| `npm run gates` | 4/4, `exit=0` |
| `npm run build` | `exit=0`, 25 страниц; `after-build`, `anchors`, `links` (753), `corridor` (22 в коридоре, 3 `null`) |
| `astro check` | 0 ошибок |
| `accept:frames check` p2 → эталон | геометрия совпала; 0 различий 11/11, 10/10; `exit=0` |
| `check` на старых блобах (нулевой прогон) | «в допуске 7, вне 0», `exit=0`; сдвиг полосы на 1 px — «ВНЕ ДОПУСКА», `exit=1` |
| `git ls-files --eol` | `i/` = `w/`; `package.json` CRLF сохранён; новые файлы ядра LF |

Коммиты: `570fc3a` (П45, `era`), девять текстов (`…`–`5a44b2d`),
`14a1b82` (допуск шума), далее — доклад.

---

# Факты, важные будущим сессиям

1. **`check` теперь несёт вердикт кодом:** пиксель вне зон `szum.json` —
   `exit 1`; СДВИГ геометрии сам по себе — 0, но тот же сдвиг обычно
   приходит `exit 1` через пиксели (пункт 26 закрыт с обратной стороны).
2. **Зоны шума живут в `szum.json`**, проза README — объяснение; новая зона —
   только с измерением в `откуда`.
3. **Зона 4 — весь полный кадр ±2, включая фон**; «только светлые» опровергнуто
   данными; маска в коде есть.
4. **Линза фактов с принятыми текстами ловит межстраничные расхождения** —
   пять в этой пачке; давать ей все принятые тексты обязательно.
5. **`era` страницы — третья ступень палитры**; поле только из пяти.
6. **Valhalla — отдельный сеанс:** план содержания до текста (S4, П45).

---

# Открытые вопросы

## Технические — решены

- Форма `szum.json` и правило «первая принявшая зона» — в шапке `szum.mjs`.
- Зона 1 сужена до четырёх измеренных кадров; остальные — при измерении.
- Discovery — `era: wlochy` по родству с `/assassins-creed-2/`.

## Политические — владельцу (без стопа, по П45)

1. **Не-судьи при кодовом вердикте:** `ne-sudyi.json` теперь печатается
   отдельной графой и в код не идёт; смысл списка при `exit 1` по пикселям —
   ваш. И два правила рядом: «реестр принятых расхождений» README против
   «эталон равен сборке» (`11_PRIEMKA`) — **рекомендация:** второе; реестр
   и так пуст.
2. **Зона 4 как записана** (весь полный кадр ±2, без маски) — принять или
   включить маску числом.
3. **Эпохи страниц пачки 2** по спискам `guides` главной (семь страниц) —
   поставить следующей пачкой или оставить accent до карты.

---

# Записи, обещанные этой сессией — по П36

| Что | Где | Источник |
|---|---|---|
| вердикты владельца | **П45** `DECISIONS.md` | владелец |
| допуск шума числом: инструмент, зоны, батарея, записи | `core/accept/szum.mjs`, `frames.mjs`, `selftest.mjs`, `_baseline/szum.json` ×2, `_baseline/README.md`, `docs/11_PRIEMKA.md`, бэклог 26 | П45 |
| поле `era`, бэклог 37 инкрементально | `content.config.ts`, маршрут, `docs/BACKLOG.md` | П45 |
| девять текстов | `src/content/tresc/*.md` | пачка 3 |
| помеченное и выкинутое, покрытие ключей | `docs/reports/2026-09-12-p4-pachka-3/` | П42 п.2 |

**Обещано и НЕ сделано:** ничего. **Дальше:** Valhalla — отдельный сеанс
(план содержания → текст); пачка 4 — темы и карта (Eivor, II wojna, za darmo
474–642, `/mapa-miejsc-historycznych/`).
